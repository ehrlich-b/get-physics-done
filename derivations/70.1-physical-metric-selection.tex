% ASSERT_CONVENTION: natural_units=dimensionless, metric_signature=mostly_minus,
%   jordan_product=(1/2)(ab+ba), octonion_basis=fano_e1e2=e4,
%   complex_structure=u_equals_e7, potential=minus_log_det,
%   cone_metric=hess(minus_log_det), cubic_norm_det=bulk_geometry_verification_det_3,
%   curvature_engine=totaro_closed_form, arithmetic=exact_over_Q
%
% Phase 70.1 (A0' -- REVISE A0), Plan 01: select the physical spacetime metric --
%   the Riemannian cone-Hessian g_X = Hess(-log det_3) vs the construction-(ii)
%   bridge g = eta + h -- and restate the route thesis accordingly, for the v17.0
%   milestone "Gravity as Intrinsic Curvature of the h_3(O) Bulk Geometry".
%
% This phase does NOT rebuild the engine, does NOT recompute the Totaro Riemann, and
% does NOT run a numerical sweep. The engine, det_3 (SSOT), the Totaro curvature
% method, and the {0,-1,-1,-1} Ricci spectrum are ALREADY certified exact-over-Q
% (code/bulk_geometry_verification.py; reproduced for THIS phase by
% .gpd/phases/70.1-.../70.1-01-metric-selection.py, final line
% METRIC_SELECTION_INPUTS_OK, exit 0). We reuse, we do not re-derive.
%
% STATUS: Tasks 1-3 written (headline + Einstein verdict + the two interpretation
%   analyses). Task 4 is a BLOCKING human-decision checkpoint (ratify the thesis
%   restatement: cone-Hessian / eta+h / escalate). Tasks 5 (selection verdict) and 6
%   (consequence propagation to Phases 72/73) are written ONLY AFTER ratification.
%
% References (project-consistent tags; cf. 72-matter-sourcing.tex, 70-signature-bridge.tex):
%   [TOT04] Totaro, "The curvature of a Hessian metric," arXiv:math/0401381
%           (Int. J. Math. 15 (2004) 369-391), Cor. 2.3 closed form; constant
%           sectional curvature -d^2/4 for the rank-1 cone slice. PLAN-LOCAL id
%           ref-totaro (the curvature reference lives in CONVENTIONS.md, not in the
%           project contract reference list). ALREADY in the warm engine -- cite the
%           method, do NOT recompute.
%   [FK94]  Faraut & Koranyi, Analysis on Symmetric Cones (OUP 1994), Ch. II-IV:
%           g_X = Hess(-log det) positive-definite / Riemannian (WHY a Riemannian ->
%           Lorentzian bridge is forced at all).
%   [Besse] A. L. Besse, Einstein Manifolds (Springer 1987): Einstein condition
%           Ric = lambda g, lambda = R/n (n>=3 => lambda const); traceless-Ricci
%           S = Ric - (R/n)g, Einstein <=> S == 0; metric cones (Sec 9 / Ch. 1).
%   [52KKT] derivations/52-kkt-spacetime.tex + 52-observer-uniqueness.tex:
%           h_2(C_u) ~ R^{3,1}, det_2 = x0^2-x1^2-x2^2-x3^2, signature (1,3)
%           mostly-minus, Minkowski coords x0=(beta+gamma)/2, x3=(beta-gamma)/2,
%           timelike x_0 ~ (beta+gamma) = (1,1,0,0); H^3 = SL(2,C)/SU(2).
%   [h3o]   ~/repos/blog/research/qualia-fixed-point/h3o_tower.py: the corrected
%           cubic-norm cross-term order (the warm engine det_3 is byte-identical to
%           the certified SSOT; the buggy (x1 x2)x3 / octonion_algebra.py order is
%           banned -- fp-wrong-cross-term).
%   [70-02] derivations/70-signature-bridge.tex + 70-02-SUMMARY.md: construction-(ii)
%           bridge g = eta + h with h := Hess - Hess|center; Minkowski-reduction gate
%           flagged tautological-by-construction (Note B); construction (i) REJECTED.
%   [72-01] derivations/72-matter-sourcing.tex (Sec obstruction, Sec adjudication,
%           Sec phase70-task): the VALD-04 finding (the non-Einstein M=0 vacuum IS
%           the result, not a reference choice); reconciliation (a) REJECTED as
%           fp-relabel, (b) CONDITIONAL; the two-outcome framing this phase resolves.
%   [FP-cb] Fierz-Pauli / linearized-Einstein on a curved background: the massless
%           spin-2 gauge (vector) symmetry extends only to an Einstein background
%           (Ric proportional to g). arXiv:2503.11304 (EPJC 2025); arXiv:1109.1382.
%           Background-/dimension-agnostic; see Task 3.

\section{Phase 70.1-01: selecting the physical spacetime metric (cone-Hessian vs $\eta+h$)}

\subsection{Conventions and scope (carried verbatim from the v17.0 lock)}

All decisive quantities are EXACT over $\mathbb{Q}$ (no float on any verdict; ranks and
eigenvalues via \texttt{sympy.Matrix}, \texttt{.eigenvals()}). The cone metric is the
Hessian of the potential $\Phi=-\log\det_3$, with $\det_3$ the certified Freudenthal
cubic norm (engine SSOT, cross-term order $2\,\mathrm{Re}((x_2 x_1)x_3)$;
\texttt{octonion\_algebra.py} banned -- \texttt{fp-wrong-cross-term}). The Riemann
tensor is the Totaro [TOT04] closed form
\begin{equation}
  R_{ijkl} = -\tfrac14\, g^{pq}\big(C_{jlp}C_{ikq}-C_{ilp}C_{jkq}\big),
  \qquad C_{ijk}=\Phi_{,ijk},
  \label{eq701:totaro}
\end{equation}
with $\mathrm{Ric}_{jl}=g^{ik}R_{ijkl}$, $R=g^{jl}\mathrm{Ric}_{jl}$ (already implemented
and certified in the warm engine -- not recomputed here). The spacetime sub-slice is
$h_2(C_u)$, engine-native indices $\{1,2,3,10\}=(\beta,\gamma,p,q)$, mostly-minus
$(-,+,+,+)$ on the slice, Riemannian positive-definite in the bulk; the center is
$X_{\mathrm{bg}}=I/3$. The Riemann/Ricci sign is pinned NEGATIVE and constant
(cone-Hessian-slice $K(p,q)=-\tfrac12$, round $H^3$ $K=-1$; the factor-of-$2$ is BENIGN
-- the load-bearing fact is the negative SIGN; we do NOT force $-1$:
\texttt{fp-assume-einstein}).

\paragraph{The one load-bearing question.} Phase 72's first-result gate computed the
$M=0$ cone-Hessian vacuum exactly over $\mathbb{Q}$ and found it is the non-Einstein
static product $\mathbb{R}_{\text{time}}\times H^3$ ([72-01], Sec.~obstruction). The
Riemannian cone-Hessian $g_X=\mathrm{Hess}(-\log\det_3)$ (the route thesis) and the
Lorentzian construction-(ii) bridge $g_{\mu\nu}=\eta_{\mu\nu}+h_{\mu\nu}$ ([70-02])
therefore \emph{disagree} about the $M=0$ geometry (cone-Hessian $\to
\mathbb{R}\times H^3$; $\eta+h\to$ flat Minkowski), and that disagreement IS the
underdetermination of the A0 signature bridge. This plan selects which object is the
physical spacetime metric and restates the thesis at its TRUE strength -- OR, if neither
restatement is defensible, escalates (binding backtracking trigger). The decisive inputs
below are reproduced by \texttt{70.1-01-metric-selection.py} (imports the warm engine;
\texttt{main()} not run; final line \texttt{METRIC\_SELECTION\_INPUTS\_OK}; exit $0$,
$\sim$3.5\,s).

\subsection{Headline: the $M=0$ cone-Hessian vacuum is the non-Einstein static
            product $\mathbb{R}_{\text{time}}\times H^3$ (reproduced, not re-derived)}
\label{sec701:headline}

Reproduced EXACT over $\mathbb{Q}$ from the warm engine ([72-01] eq:ricci-center;
\texttt{70.1-01-metric-selection.py} steps 2--5):
\begin{equation}
  g = \mathrm{diag}(9,9,18,18)\ \ (\det g = 26244),\qquad
  \mathrm{Ric} =
  \begin{pmatrix} -\tfrac92 & \tfrac92 & 0 & 0\\
                  \tfrac92 & -\tfrac92 & 0 & 0\\
                  0 & 0 & -18 & 0\\
                  0 & 0 & 0 & -18\end{pmatrix},
  \label{eq701:ricci-center}
\end{equation}
\begin{equation}
  g^{-1}\mathrm{Ric}\ \text{has eigenvalues}\ \{\,0,-1,-1,-1\,\}\
  (\text{mult } 0{:}1,\ {-1}{:}3),\qquad R = -3.
  \label{eq701:spectrum}
\end{equation}
The coordinate sectional curvatures name the structure:
\begin{equation}
  K(e_0,e_1) = 0\ \ (\text{timelike/dilation 2-plane FLAT}),\qquad
  K(p,q) = K(e_2,e_3) = -\tfrac12\ \ (\text{$H^3$ base}),
  \label{eq701:sectional}
\end{equation}
the $H^3$ value identical to the engine's certified
\texttt{h3\_cone\_hessian\_benchmark} ($K_{\text{value}}=-\tfrac12$; round-$H^3$
cross-check $K_{\text{round}}=-1=2\cdot(-\tfrac12)$, the benign factor-of-$2$). The
eigenvalue-$0$ eigenvector is $(1,1,0,0)=\beta+\gamma$; under the Lorentzian reading
$h_2(C_u)\simeq\mathbb{R}^{3,1}$ with $\det_2$ Minkowski form
$\beta\gamma/3-p^2/3-q^2/3$ ([52KKT]), this is the \textbf{timelike $x_0$} direction (the
$\beta\gamma/3$ block is the hyperbolic $1{+}1$ part; $(1,1)$ its timelike eigenvector).
So the flat factor of the cone is \emph{time}, and the $M=0$ vacuum is the static product
$\mathbb{R}_{\text{time}}\times H^3_{\text{space}}$. This spectrum is the HEADLINE: it is
the VALD-04 finding (negative-result-is-success), reported as such and never papered over.

\paragraph{Inherited and confirmed (carry-forward gates).} The construction-(ii)
Minkowski reduction (test-minkowski-reduction) is satisfied by the warm engine:
$g(\text{center},M{=}0)=\eta$ exactly, since $h:=\mathrm{Hess}-\mathrm{Hess}|_{\text{center}}=0$
at the center by construction ([70-02] Note B; \texttt{70.1-01-metric-selection.py}
step 6: \texttt{offcenter\_slice\_metric}$\{\}$ gives $h=\mathbf{0}_{4\times4}$,
$g=\eta_{\mathrm{bg}}$; \texttt{minkowski\_reduction()} residual $=0$, Sylvester minors
$[1,-1,1,-1]\Rightarrow$ signature $(1,3)$). The corrected cubic-norm cross-term
(test-cross-term-association) is confirmed via the engine SSOT: $\det_3$ minus the
block-diagonal norm $\alpha\beta\gamma-\alpha|x_1|^2-\beta|x_2|^2-\gamma|x_3|^2$ equals
\emph{exactly} $2\,\mathrm{Re}((x_2 x_1)x_3)$, with $\alpha,\beta,\gamma$ absent from the
triple (\texttt{70.1-01-metric-selection.py} step 8); the buggy $(x_1 x_2)x_3$ /
\texttt{octonion\_algebra.py} order is excluded ([h3o]; \texttt{fp-wrong-cross-term}).

\subsection{Einstein-condition verdict: the cone-Hessian $M=0$ vacuum is NOT a GR
            $\Lambda$-vacuum}
\label{sec701:einstein}

An Einstein space satisfies $\mathrm{Ric}_{\mu\nu}=\lambda\,g_{\mu\nu}$ with
$\lambda=R/n$ (for $n\ge3$, $\lambda$ is necessarily constant) [Besse]. Equivalently the
traceless Ricci tensor
\begin{equation}
  S_{\mu\nu} := \mathrm{Ric}_{\mu\nu}-\tfrac{R}{n}\,g_{\mu\nu}
  \qquad\text{satisfies}\qquad
  \text{Einstein}\iff S_{\mu\nu}\equiv 0
  \label{eq701:tracelessricci}
\end{equation}
[Besse; the Einstein criterion]. For $n=4$ and $R=-3$ the Einstein value is
\begin{equation}
  \lambda = \frac{R}{4} = -\frac34,
  \label{eq701:einstein-value}
\end{equation}
so an Einstein space in $n=4$ with $R=-3$ would require \emph{all} Ricci-endomorphism
eigenvalues to equal $-\tfrac34$. The computed spectrum~\eqref{eq701:spectrum} is
$\{0,-1,-1,-1\}$, which contains \textbf{no} eigenvalue equal to $-\tfrac34$. Therefore
the cone-Hessian $M=0$ vacuum is NOT Einstein, hence NOT a GR $\Lambda$-vacuum. The
direct exact-over-$\mathbb{Q}$ witness is the nonzero traceless Ricci tensor
(\texttt{70.1-01-metric-selection.py} step 4):
\begin{equation}
  S = \mathrm{Ric}-\tfrac{R}{4}\,g
    = \mathrm{Ric}+\tfrac34\,g
    =
  \begin{pmatrix} \tfrac94 & \tfrac92 & 0 & 0\\
                  \tfrac92 & \tfrac94 & 0 & 0\\
                  0 & 0 & -\tfrac92 & 0\\
                  0 & 0 & 0 & -\tfrac92\end{pmatrix}
  \;\neq\; \mathbf{0},
  \qquad \operatorname{tr}_g S = g^{\mu\nu}S_{\mu\nu} = 0
  \label{eq701:S}
\end{equation}
(genuinely traceless yet nonzero -- the exact signature of a non-Einstein space). The
$n=4$ Ricci decomposition reconstruction residual $R_{ijkl}-(\mathrm{Scal}+E+\mathrm{Weyl})=0$
holds exactly over $\mathbb{Q}$, confirming $S\neq0$ is a genuine curvature fact, not a
decomposition artifact.

\paragraph{Signature-independence of the verdict.} The eigenvalues
$\{0,-1,-1,-1\}$ are invariants of the \emph{$(1,1)$} Ricci endomorphism
$g^{-1}\mathrm{Ric}$, not of the raw $(0,2)$ Ricci components. A change of frame
(congruence) $P$ acts on the $(0,2)$ tensors by $g\mapsto P^{\!\top}gP$,
$\mathrm{Ric}\mapsto P^{\!\top}\mathrm{Ric}\,P$, so the endomorphism transforms by
\emph{similarity},
\begin{equation}
  g^{-1}\mathrm{Ric}\;\longmapsto\;(P^{\!\top}gP)^{-1}(P^{\!\top}\mathrm{Ric}\,P)
  = P^{-1}\,(g^{-1}\mathrm{Ric})\,P,
  \label{eq701:similarity}
\end{equation}
and its eigenvalues are invariant. Converting between the $(-,+,+,+)$ and $(+,-,-,-)$
labelings is such a congruence, so it does NOT change the verdict; this is verified
exact over $\mathbb{Q}$ (\texttt{70.1-01-metric-selection.py} step 7: a generic
invertible rational $P$ leaves the spectrum $\{0,-1,-1,-1\}$ fixed).

\emph{Caution (why the invariant is the $(1,1)$ endomorphism, index raised).} A naive
\emph{overall} metric sign flip $g\mapsto-g$ is \textbf{not} a congruence and is NOT the
same as a signature change: it leaves the $(0,2)$ tensor $\mathrm{Ric}_{\mu\nu}$
unchanged but negates the $(1,1)$ endomorphism $g^{-1}\mathrm{Ric}$ (eigenvalues
$\{0,-1,-1,-1\}\mapsto\{0,+1,+1,+1\}$, verified over $\mathbb{Q}$ in step 7). This is
exactly the contract's disconfirming observation -- ``a sign-dependent verdict means the
$(0,2)$ Ricci components were used without raising the index.'' The verdict we report is
the \emph{similarity-invariant} $(1,1)$ spectrum~\eqref{eq701:spectrum}, computed with
the index raised; no verdict in this phase uses the raw $(0,2)$ entries or an overall
scalar flip.

\paragraph{Explicit rejection of the ``which $\Lambda$ reference?'' baseline.} No
baseline subtraction turns a non-Einstein vacuum into an Einstein one. Asking ``which
$\Lambda$ reference?'' presupposes a maximally-symmetric vacuum that this construction
does not produce ([72-01], Sec.~adjudication; \texttt{fp-assume-einstein}). The spectrum
$\{0,-1,-1,-1\}$ IS the result -- headline it, do not paper it over. We insert no factor
to force $\mathrm{Ric}\propto g$ or to force flatness, and we keep $K(p,q)=-\tfrac12$
(no factor of $2$ to ``match'' $-1$).

\paragraph{Why a metric cone is never Einstein (corroborative, not load-bearing).} The
dim-$4$ cone-Hessian sub-slice is a metric cone $C(H^3)=\mathbb{R}_{>0}\times H^3$: a
flat radial (dilation) line over the constant-curvature $H^3$ base. A non-trivial metric
cone in dimension $n$ has a radial $0$-eigenvalue plus shifted base eigenvalues, here
$\{0,-1,-1,-1\}$, and is never Einstein [Besse Sec.~9; Gallot]. This EXPLAINS why the
spectrum has the form it does; the exact-over-$\mathbb{Q}$
computation~\eqref{eq701:spectrum}--\eqref{eq701:S} \emph{is} the proof. (The radial
$0$-direction is at once the Riemannian scale modulus $R(X)=R(2X)$ found in Phase 71 and
-- under the Lorentzian reading -- the timelike $x_0$; the cone's flat factor is time.)

\subsection{Interpretation analysis I: linearized spin-2 on a fixed non-Einstein
            background -- the option-(1) defensibility test}
\label{sec701:spin2}

Suppose the cone-Hessian is named the physical metric (option (1)). Its $M=0$ vacuum is
the provably non-Einstein $\mathbb{R}\times H^3$~\eqref{eq701:spectrum}. What, then, can
a perturbation $h_{\mu\nu}$ on this background legitimately be called?

\paragraph{Standard result (background-/dimension-agnostic).} The massless Fierz--Pauli
/ linearized-Einstein (Lichnerowicz) operator propagates a \emph{consistent massless
spin-2 field} only when its linearized gauge (vector) symmetry
$\delta h_{\mu\nu}=\nabla_\mu\xi_\nu+\nabla_\nu\xi_\mu$ is preserved; that gauge symmetry
of the massless equations extends to a curved background \emph{if and only if the
background is Einstein} ($\mathrm{Ric}_{\mu\nu}\propto g_{\mu\nu}$, including the
Ricci-flat and (A)dS cases) [FP-cb]. On a generic non-Einstein background the would-be
gauge variation fails to leave the linearized operator invariant (the obstruction is
proportional to the traceless Ricci $S_{\mu\nu}\neq0$), so there is no consistent
massless spin-2 gauge symmetry. This theorem is \emph{background- and
dimension-agnostic}: it is not a de Sitter/AdS-specific statement, and it applies to any
non-Einstein Lorentzian background, a metric cone $\mathbb{R}\times H^3$ included.

\paragraph{Confidence and the cone-specific caveat.} This is a MEDIUM-confidence
\emph{application} of a HIGH-confidence theorem. The theorem (gauge invariance
$\Rightarrow$ Einstein background) is multiply confirmed and background-agnostic; but no
reference treats massless spin-2 on a \emph{metric cone} $\mathbb{R}\times H^3$
specifically. We therefore rely on the theorem's stated generality, not on a
cone-specific citation, and flag this as the weakest anchor of the phase.

\paragraph{Consequence for the option-(1) ``cone-Hessian IS the metric'' thesis (the
physics analysis stands; the verdict label is FALSIFIED -- see
Sec.~\ref{sec701:selection}).} The physics above is robust: a spin-2 field $h_{\mu\nu}$
on the \emph{fixed} non-Einstein $\mathbb{R}\times H^3$ carries no linearized-diffeomorphism
gauge symmetry, so it is not a graviton and the cone-Hessian curvature is not a
self-consistent gravitational field. What survives as a bare mathematical object is at
most a fixed-background wave operator / Green's function for $h_{\mu\nu}$ -- carrying
\emph{no} gauge symmetry and \emph{no} back-reaction -- which is NOT ``GR derived,'' NOT
``a graviton,'' and NOT ``a self-consistent gravitational spin-2 sector''
(\texttt{fp-overclaim-spin2}). \textbf{The ratified verdict (Sec.~\ref{sec701:selection})
does NOT record this surviving wave operator as a ``weaker-but-honest success.''} Because
a spin-2 field on a fixed non-Einstein background is provably non-gauge -- hence not a
graviton and not gravity -- the literal option-(1) thesis ``the cone-Hessian
$g_X=\mathrm{Hess}(-\log\det_3)$ IS the gravitational metric'' is \textbf{FALSIFIED}: a
NULL result, not a lesser win. Negative-result-is-success applies to this falsification.
Relabeling the non-gauge wave operator a ``gravitational matter-response success'' is
exactly the over-claim \texttt{fp-overclaim-spin2} forbids.

\paragraph{What this leaves standing (preview of the selection).} The falsification of
option (1) is a falsification of the \emph{literal} ``cone-Hessian is the metric''
identification only. It does NOT abandon the route ``gravity from $h_3(O)$ geometry'': as
Sec.~\ref{sec701:selection} records under the ratified outcome (2), the cone-Hessian
remains the \emph{source / information structure} (its $V_0\!\leftrightarrow\!V_{1/2}$
cross-terms $2\,\mathrm{Re}((x_2 x_1)x_3)$ source the perturbation $h$), while spacetime
is the KKT slice carrying the flat $\eta$. The route survives in that restated form; only
the identification of the cone curvature \emph{as} the spacetime metric dies here.

\subsection{Interpretation analysis II: the $\eta+h$ bridge -- the flat vacuum is
            DERIVED from KKT, not inserted}
\label{sec701:circularity}

Now the construction-(ii) bridge $g_{\mu\nu}=\eta_{\mu\nu}+h_{\mu\nu}$ (the ratified
choice, Sec.~\ref{sec701:selection}). Here $\eta$ is \textbf{not} a free reference: it is
the KKT Minkowski form of the spacetime slice $h_2(C_u)\simeq\mathbb{R}^{3,1}$, fixed by
the $\det_2$ quadratic norm $\beta\gamma/3-p^2/3-q^2/3$ (signature $(1,3)$, timelike
$x_0=(\beta+\gamma)$), a trusted DERIVED result ([52KKT], derivation 52). The matter
deviation is
\begin{equation}
  h_{\mu\nu} := \big[\mathrm{Hess}(-\log\det_3)\ \text{restricted to } V_0\big]
              - \big[\text{its value at } (M{=}0,\ \text{center } I/3)\big],
  \label{eq701:hdef}
\end{equation}
so $h_{\mu\nu}\equiv0$ at the center identically ([70-02] Note B; reproduced in
\texttt{70.1-01-metric-selection.py} step 6 as $h=\mathbf{0}_{4\times4}$).

\paragraph{Why the $M=0$ flat vacuum is DERIVED, not an inserted $\Lambda=0$
(circularity-tripwire framing struck on the vacuum).} The $M=0$ vacuum is flat
\emph{because} $\eta$ is the KKT Minkowski form -- a derived, trusted fact about the
spacetime slice ($\det_2$ mostly-minus, $h_2(C_u)\simeq\mathbb{R}^{3,1}$, [52KKT]), NOT
because of any reference subtraction. The earlier ``$\Lambda=0$ is inserted by the
center-subtraction'' / ``circularity tripwire on the vacuum'' reading is WITHDRAWN: the
centered definition~\eqref{eq701:hdef} makes $h$ a \emph{deviation-from-center}, which is
purely definitional bookkeeping for the perturbation -- it is NOT the source of the
flatness and carries NO circularity. The fact $h\equiv0$ at the center remains true as
that bookkeeping statement, and the Minkowski-reduction gate (test-minkowski-reduction)
correctly confirms the construction is implemented and the signature is $(1,3)$; what it
is \emph{not} is a smuggled cosmological constant. The flat empty vacuum is the genuine
KKT-Minkowski content, the GR-correct empty spacetime.

\paragraph{The genuine open question is the legitimate physics test, carried to Phase 73
(not a circularity audit).} The substantive question the bridge must answer downstream is
the standard linearized-gravity test:
\begin{quote}\itshape
``Does the matter-sourced perturbation $h_{\mu\nu}$ (from the $\det_3$ cross-terms
$2\,\mathrm{Re}((x_2 x_1)x_3)$) satisfy the LINEARIZED EINSTEIN equation on the KKT-flat
$\eta$ background -- $\Box\,\bar h_{\mu\nu}\sim\kappa\,T_{\mu\nu}$ (trace-reversed,
harmonic gauge) with the correct tensor structure and gauge behaviour? This test CAN
fail: if $h$ obeys a non-Einstein PDE, the route's GR claim dies as an honest negative
result. THAT -- not a self-inserted $\Lambda$ -- is the real gate.''
\end{quote}
This reframes Phase 73 as a derivation step (Sec.~\ref{sec701:propagation}) rather than a
self-circularity audit; the flatness of the background is settled (DERIVED), so the
content lives entirely in whether $h$ obeys the linearized Einstein operator.

\paragraph{What conditions each branch.} The literal option-(1) ``cone-Hessian is the
metric'' thesis is FALSIFIED on the non-Einstein background (Sec.~\ref{sec701:spin2}); the
option-(2) $\eta+h$ bridge is the physical metric (Sec.~\ref{sec701:selection}) with the
flat vacuum DERIVED from KKT and the substance deferred to the Phase-73 linearized-Einstein
test. The selection below (ratified at the checkpoint, Sec.~\ref{sec701:checkpoint}) records
exactly this.

\subsection{Decision checkpoint: ratify the thesis restatement (RESOLVED)}
\label{sec701:checkpoint}

This was the BLOCKING human-decision point. The thesis restatement redefines the
milestone's central claim and re-scopes Phases 72/73, so under balanced autonomy the
adopted outcome was ratified before the consequence-propagation was written. The
decision evidence assembled above was:
\begin{itemize}
  \item the reproduced exact-over-$\mathbb{Q}$ spectrum $\{0,-1,-1,-1\}$, $R=-3$,
        $S\neq0$~\eqref{eq701:spectrum}--\eqref{eq701:S}; $K(e_0,e_1)=0$,
        $K(p,q)=-\tfrac12$~\eqref{eq701:sectional} ($\mathbb{R}\times H^3$); $\eta+h$
        $h\equiv0$-at-center~\eqref{eq701:hdef}; signature-independence~\eqref{eq701:similarity};
  \item the Einstein verdict (Sec.~\ref{sec701:einstein}): NOT a GR $\Lambda$-vacuum;
  \item option (1) physics: a spin-2 field on the fixed non-Einstein
        $\mathbb{R}\times H^3$ is provably non-gauge $\Rightarrow$ the literal
        ``cone-Hessian IS the metric'' thesis is FALSIFIED (Sec.~\ref{sec701:spin2});
        option (2) $\eta+h$: the flat $M=0$ vacuum is DERIVED from the KKT $\det_2$
        Minkowski form, not an inserted $\Lambda=0$ (Sec.~\ref{sec701:circularity});
  \item the binding backtracking trigger: escalate (do NOT force a choice) IFF option (1)
        does not admit even a fixed-background reading AND option (2) cannot be justified
        without circularity. (It did NOT fire: option (2) is justified -- the flatness is
        KKT-derived.)
\end{itemize}
The admissible outcomes were exactly three: \textbf{(1)} cone-Hessian; \textbf{(2)}
$\eta+h$ bridge; \textbf{(escalate)} gravitational interpretation NOT established.

\paragraph{RATIFIED OUTCOME (human decision, 2026-05-31): (2) $\eta+h$ is THE physical
spacetime metric -- with four binding framing corrections.} The human ratified outcome (2)
and explicitly OVERRODE the RESEARCH recommendation (which had favoured outcome (1)) and
the plan's literal option-(2) wording. The four binding corrections (authoritative; they
supersede the plan's literal option-(2) text and the provisional Task-3 text above, which
has been reconciled to match):
\begin{enumerate}
  \item \textbf{The flat vacuum is DERIVED, not inserted.} $M=0$ is flat because $\eta$ is
        the KKT Minkowski form ([52KKT]: $h_2(C_u)\simeq\mathbb{R}^{3,1}$, $\det_2$
        mostly-minus -- a trusted derived result). The ``$\Lambda=0$ inserted by
        center-subtraction'' / ``circularity tripwire on the vacuum'' framing is STRUCK:
        center-subtraction only makes $h$ a deviation-from-center (definitional); the
        flatness comes from KKT (derived). [Applied in Sec.~\ref{sec701:circularity}.]
  \item \textbf{This RESTATES the route, it does not abandon it.} The cone-Hessian is the
        source/information structure: its $V_0\!\leftrightarrow\!V_{1/2}$ cross-terms
        $2\,\mathrm{Re}((x_2 x_1)x_3)$ source the perturbation $h$; spacetime is the KKT
        slice (flat $\eta$). Only the LITERAL ``cone-Hessian IS the metric'' thesis
        ($\equiv$ option (1)) is falsified by the non-Einstein $4$-slice. The route
        ``gravity from $h_3(O)$ geometry'' SURVIVES in this form.
  \item \textbf{Phase 72/73 re-scope is the legitimate physics test, NOT a circularity
        audit.} The open question: does the matter-sourced $h$ satisfy the LINEARIZED
        EINSTEIN equation on the KKT-flat $\eta$ background ($\Box\bar h\sim\kappa T$,
        correct tensor $+$ gauge structure)? This test CAN fail -- if $h$ obeys a
        non-Einstein PDE the route's GR claim dies as an honest negative result. That is
        the real gate. Keep ONE genuine consistency check (the $\det_2$-background /
        $\det_3$-perturbation splice), not a tripwire. [Applied in
        Sec.~\ref{sec701:propagation}.]
  \item \textbf{Do NOT ratify (1) as a success.} The non-Einstein $\mathbb{R}\times H^3$
        $4$-slice FALSIFIES ``cone-Hessian $=$ GR.'' A spin-2 field on a fixed non-Einstein
        background is provably non-gauge $\Rightarrow$ not a graviton, not gravity $\Rightarrow$
        a NULL result. Negative-result-is-success applies to (1)'s FALSIFICATION, not to
        relabeling the wave operator a win. [Applied in Sec.~\ref{sec701:spin2},
        Sec.~\ref{sec701:selection}.]
\end{enumerate}
The selection verdict (Sec.~\ref{sec701:selection}) and consequence propagation
(Sec.~\ref{sec701:propagation}) below implement this ratified outcome.

\subsection{Selection verdict and restated thesis: the physical metric is the
            $\eta+h$ bridge (cone-Hessian FALSIFIED as the metric)}
\label{sec701:selection}

\paragraph{Verdict (ratified outcome (2)).} \textbf{The physical spacetime metric is the
construction-(ii) bridge $g_{\mu\nu}=\eta_{\mu\nu}+h_{\mu\nu}$}, where $\eta$ is the KKT
Minkowski form of the spacetime slice ([52KKT], derivation 52: $h_2(C_u)\simeq\mathbb{R}^{3,1}$,
$\det_2 = \beta\gamma/3-p^2/3-q^2/3$, signature $(1,3)$, timelike $x_0=(\beta+\gamma)$) and
$h_{\mu\nu}$~\eqref{eq701:hdef} is the matter perturbation. \textbf{The bridge metric (KKT
$\eta$ $+$ matter perturbation $h$), NOT the cone-Hessian curvature, is the gravitational
field.} The Riemannian cone-Hessian $g_X=\mathrm{Hess}(-\log\det_3)$ is \emph{not} the
spacetime metric; it is the \emph{source / information structure} whose
$V_0\!\leftrightarrow\!V_{1/2}$ cross-terms $2\,\mathrm{Re}((x_2 x_1)x_3)$ source $h$.

\paragraph{The $M=0$ vacuum is flat by a DERIVED, trusted fact (not an inserted
$\Lambda$).} $M=0$ is flat because $\eta$ IS the KKT Minkowski form
(Sec.~\ref{sec701:circularity}; [52KKT], $\det_2$ mostly-minus -- a trusted derived
result), the GR-correct empty spacetime. The centered definition~\eqref{eq701:hdef} makes
$h$ a deviation-from-center (definitional bookkeeping); it is NOT the source of the
flatness and carries NO circularity. The ``$\Lambda=0$ inserted by center-subtraction'' /
``circularity tripwire on the vacuum'' framing is withdrawn on this branch.

\paragraph{The route SURVIVES, restated (it is not abandoned).} Selecting $\eta+h$ does
NOT abandon the milestone route ``gravity from $h_3(O)$ geometry.'' The cone-Hessian
remains the source/information structure (its cross-terms source $h$); spacetime is the
KKT slice carrying flat $\eta$. Only the LITERAL thesis ``the cone-Hessian
$g_X=\mathrm{Hess}(-\log\det_3)$ IS the spacetime metric'' ($\equiv$ option (1)) is
falsified by the non-Einstein $4$-slice. The route survives in this restated form: the
gravitational field is the bridge metric, sourced by the cone-Hessian information
structure.

\paragraph{Criterion that decided it (PRIMARY: causal/geodesic structure).} The selection
is DECIDABLE on the primary causal/geodesic criterion. The slice has its own intrinsic
Minkowski causal structure from $\det_2$ ([52KKT]): timelike $x_0=(\beta+\gamma)$, null
cones set by $\beta\gamma/3-p^2/3-q^2/3$. The bridge $\eta+h$ matches that intrinsic causal
structure exactly ($\eta$ IS the $\det_2$ Minkowski form). The cone-Hessian, by contrast,
makes that same timelike direction the \emph{flat dilation factor} of
$\mathbb{R}_{\text{time}}\times H^3$ (its $K(e_0,e_1)=0$ plane, eigenvalue-$0$ eigenvector
$(1,1,0,0)=\beta+\gamma$, Eq.~\eqref{eq701:sectional}) -- the wrong causal structure for
spacetime (a static hyperbolic-space product, not the slice's flat Minkowski causal
geometry). The secondary matter-coupling channel ($h$ sourced via the $\det_3$ cross-term)
is consistent with this and now reads as ``the cone-Hessian sources the bridge
perturbation,'' not ``the cone-Hessian is the metric.''

\paragraph{Option (1) is recorded as the FALSIFIED branch (NULL result, not a
weaker-but-honest success).} The literal option-(1) thesis -- the cone-Hessian IS the
spacetime metric -- is \textbf{FALSIFIED}. Its $M=0$ vacuum is the provably non-Einstein
$\mathbb{R}\times H^3$ (Sec.~\ref{sec701:einstein}, $S\neq0$); a massless spin-2 field on a
fixed non-Einstein background has no linearized-diffeomorphism gauge symmetry
(Sec.~\ref{sec701:spin2}, [FP-cb]), so it is not a graviton and the cone curvature is not a
self-consistent gravitational field. This is a NULL result. We do NOT relabel the
surviving non-gauge fixed-background wave operator / Green's function a
``weaker-but-honest success'' (\texttt{fp-overclaim-spin2}): the cardinal over-claim is
refused. Negative-result-is-success applies to the FALSIFICATION of option (1), not to a
demotion of it to a lesser win.

\paragraph{Rejected reconciliations and constructions (recorded, unchanged).}
\begin{itemize}
  \item \textbf{Reconciliation (a) -- Einstein-on-the-$H^3$-leaf -- REJECTED}
        (\texttt{fp-relabel}). Restricting to the $\det_2=1$ $H^3$ leaf (or quotienting the
        dilation/timelike $x_0=(\beta+\gamma)$ direction) drops the timelike dimension and
        swaps the $4$-dim spacetime for a $3$-dim spatial slice to manufacture a
        maximally-symmetric ``Einstein'' pass. GR's empty hyperbolic-slice vacuum is
        Milne $=$ flat Minkowski, NOT $H^3$; $H^3$ is the wrong symmetric space and one
        dimension short. The verdict holds on the full $4$-dim slice including the timelike
        direction. (Not relevant to the chosen $\eta+h$ anyway, whose vacuum is flat
        Minkowski, not $H^3$.)
  \item \textbf{Construction (i) -- Wick-rotate via $u=e_7$ -- remains REJECTED}, not
        reopened ([70-02]).
  \item \textbf{The ``which $\Lambda$ reference?'' baseline -- REJECTED}
        (\texttt{fp-assume-einstein}): no baseline subtraction turns a non-Einstein vacuum
        Einstein (Sec.~\ref{sec701:einstein}). It does not arise on the $\eta+h$ branch:
        the flat vacuum is KKT-derived, not a reference choice.
\end{itemize}

\paragraph{The verdict SUPERSEDES a falsified project-level expectation
(notation-coordinator follow-up).} The headline (Sec.~\ref{sec701:headline}) FALSIFIES, for
the cone-Hessian metric, the project-level expectation recorded in METHODS.md
Validation-(a) (``at $X=I/3$ the cone is EINSTEIN of negative curvature (Cartan); computed
Ricci at center must be proportional to $g$ with a negative constant'') and CONVENTIONS.md
\S6 (``the center is Einstein with negative Ricci (Cartan), so $\Lambda<0$''). The
$M=0$ cone-Hessian center is the non-Einstein $\mathbb{R}\times H^3$ (Ricci-endomorphism
$\{0,-1,-1,-1\}$, $S\neq0$), NOT Einstein. (The cone-Hessian is not the spacetime metric
under the chosen verdict, so this falsified expectation does not bear on the physical
$\eta+h$ metric; but the stale text must be corrected.) This is flagged for a
notation-coordinator follow-up -- non-blocking.

\subsection{Consequence propagation: re-scoping Phases 72 and 73 to the $\eta+h$ metric}
\label{sec701:propagation}

The ratified verdict (Sec.~\ref{sec701:selection}) fixes the spacetime metric as the
$\eta+h$ bridge. This re-scopes the two downstream phases. (The actual phase re-planning is
a follow-up plan-phase action; here we specify precisely what must change. The re-scope is
the LEGITIMATE PHYSICS TEST, not a circularity audit.)

\paragraph{Phase 72 (B, matter-sourcing): MATTER-ON-FLAT.} The $M=0$ baseline is flat
Minkowski (the KKT $\eta$), the GR-correct empty spacetime. Matter in $V_1$ and $V_{1/2}$
sources the perturbation $h$ of $g=\eta+h$ via the $\det_3$ cross-term
$2\,\mathrm{Re}((x_2 x_1)x_3)$ (the unique octonionic $V_0\!\leftrightarrow\!V_{1/2}$
channel). Consequently:
\begin{itemize}
  \item The cross-term ON/OFF test (test-cross-term-onoff) applies AS ORIGINALLY FRAMED:
        switching the cross-term off must kill the $V_0$-sourcing of $h$, isolating the
        matter contribution.
  \item The $\Lambda$-vs-matter test (test-lambda-vs-matter) applies as originally framed:
        the baseline is flat ($\Lambda=0$ from the KKT $\det_2$, DERIVED), so any curvature
        of $g=\eta+h$ is matter-sourced -- there is NO non-Einstein background to subtract,
        and NO $\Lambda$-reference tripwire (that was the falsified option-(1)/cone-Hessian
        framing).
  \item The preserved Phase-71/72 facts stand: $V_1$ ($\alpha$) inertness (the cross-term
        carries no $\alpha,\beta,\gamma$, Sec.~\ref{sec701:headline} step 8; confirmed by
        the splice check below, where $V_1$ matter gives $h\equiv0$), the $\Delta R$
        isolation pipeline, and the $n=4$ Ricci decomposition.
\end{itemize}

\paragraph{Phase 73 (C, Einstein structure): the LINEARIZED-EINSTEIN TEST (the real
gate).} Phase 73 is reframed as a concrete derivation step, NOT a self-circularity audit:
\begin{quote}\itshape
Does the matter-sourced perturbation $h_{\mu\nu}$ (from the $\det_3$ cross-terms) satisfy
the LINEARIZED EINSTEIN equation on the KKT-flat $\eta$ background,
\[
  \Box\,\bar h_{\mu\nu} \;\propto\; \kappa\,T_{\mu\nu}
  \quad(\bar h_{\mu\nu}=h_{\mu\nu}-\tfrac12\eta_{\mu\nu}h,\ \ \text{harmonic gauge }
  \partial^\mu\bar h_{\mu\nu}=0),
\]
(the overall numerical coefficient -- e.g.\ $-16\pi G$ in mostly-minus with
$G_{\mu\nu}=8\pi G\,T_{\mu\nu}$ -- is convention-fixed and not load-bearing here) with the
correct TENSOR STRUCTURE and the correct gauge behaviour (the linearized contracted Bianchi
identity $\partial^\mu G^{(1)}_{\mu\nu}=0$ consistent with $\partial^\mu T_{\mu\nu}=0$)?
\end{quote}
This test \textbf{CAN FAIL}: if the cross-term-sourced $h$ obeys a \emph{non-Einstein} PDE
(wrong tensor structure, or gauge-inconsistent, or not reducible to
$\Box\bar h\sim\kappa T$), then the route's GR claim dies as an HONEST NEGATIVE result.
THAT is the real gate -- not a self-inserted $\Lambda$, not a circularity audit. There is
NO $\Lambda=0$ tripwire on this branch (the flat background is KKT-derived). The previous
strong-form $G_{\mu\nu}=\kappa T_{\mu\nu}+\Lambda g_{\mu\nu}$ self-consistent-Einstein
framing is replaced by this linearized-on-flat test, which is the correct level for a
matter perturbation of a flat background.

\paragraph{ONE genuine consistency check (the $\det_2$/$\det_3$ splice -- PASS, NOT a
tripwire).} As a single genuine consistency check (NOT a gate), we verify the
$\det_2$-background / $\det_3$-perturbation SPLICE is consistent: that the cone-Hessian
deviation $h$ is a VALID perturbation of the KKT $\eta$ -- symmetric $(0,2)$, same $4\times4$
tensor space and $(\beta,\gamma,p,q)$ frame, correct signature/index behaviour. This was
confirmed NOW by extending \texttt{70.1-01-metric-selection.py} (step 9, exact over
$\mathbb{Q}$):
\begin{itemize}
  \item $\eta_{\mathrm{bg}}$ is symmetric $(0,2)$ with signature $(1,3)$ -- computed via
        eigenvalue SIGNS (eigenvalues $\{+\tfrac12,-\tfrac12,-1,-1\}$), NOT Sylvester
        leading minors, because the frame is null-aligned ($x_0\sim\beta+\gamma\Rightarrow
        \eta_{\mathrm{bg}}[0,0]=0$, so the first leading minor vanishes and the
        leading-minor test is invalid).
  \item $h$ is symmetric $(0,2)$ and lives in the same $4\times4$ frame as
        $\eta_{\mathrm{bg}}$. $V_1$ ($\alpha$) matter gives $h\equiv0$ (consistent with the
        $\alpha$-absent inertness); small $V_{1/2}$ matter gives $h\neq0$ yet
        $g=\eta+h$ PRESERVES the Lorentzian signature $(1,3)$.
  \item Linear-regime caveat (expected, non-decisive): a LARGE $V_{1/2}$ deviation
        eventually flips the signature -- the $\eta+h$ splice is perturbative with a finite
        radius of validity (this is what ``linearized matter perturbation'' means, not a
        defect). The Phase-72 matter-response works in the linear regime.
\end{itemize}
\textbf{Splice-consistency: PASS (as expected.)} $h$ is a valid symmetric $(0,2)$ deviation
of the KKT $\eta_{\mathrm{bg}}$ in the same Lorentzian frame; small matter perturbations
preserve signature $(1,3)$. This is a consistency confirmation, not a gate.

\subsection{Summary block (for the phase SUMMARY and the next plan-phase action)}
\label{sec701:summaryblock}

\begin{itemize}
  \item \textbf{Headline (reproduced exact over $\mathbb{Q}$, HEADLINE not papered over):}
        the $M=0$ cone-Hessian vacuum is the non-Einstein static product
        $\mathbb{R}_{\text{time}}\times H^3$ -- $g=\mathrm{diag}(9,9,18,18)$, Ricci-endomorphism
        $g^{-1}\mathrm{Ric}$ eigenvalues $\{0,-1,-1,-1\}$, $R=-3$, traceless Ricci $S\neq0$,
        $K(e_0,e_1)=0$ (timelike/dilation plane flat), $K(p,q)=-\tfrac12$ ($H^3$ base);
        $n=4$ Einstein value $R/4=-\tfrac34$ is NOT in the spectrum $\Rightarrow$ NOT
        Einstein $\Rightarrow$ NOT a GR $\Lambda$-vacuum. Signature-independent (a $(1,1)$
        similarity invariant).
  \item \textbf{VERDICT (ratified outcome (2)):} the physical spacetime metric is the
        $\eta+h$ bridge (KKT $\eta$ $+$ matter perturbation $h$), NOT the cone-Hessian
        curvature. The flat $M=0$ vacuum is DERIVED from the KKT $\det_2$ Minkowski form
        ([52KKT]), not an inserted $\Lambda$. Decided on the PRIMARY causal/geodesic
        criterion (the bridge matches the slice's intrinsic $\det_2$ Minkowski causal
        structure; the cone-Hessian makes the timelike direction the flat factor of
        $\mathbb{R}\times H^3$ -- wrong causal structure).
  \item \textbf{Restated thesis (route SURVIVES):} ``gravity from $h_3(O)$ geometry'' --
        the cone-Hessian is the source/information structure (its $V_0\!\leftrightarrow\!V_{1/2}$
        cross-terms $2\,\mathrm{Re}((x_2 x_1)x_3)$ source $h$); spacetime is the KKT slice
        with flat $\eta$. The LITERAL ``cone-Hessian IS the metric'' thesis ($\equiv$ option
        (1)) is FALSIFIED (NULL result: spin-2 on a fixed non-Einstein background is
        non-gauge $\Rightarrow$ not a graviton; NOT relabeled a weaker-but-honest success).
  \item \textbf{Superseded project-level expectation (notation-coordinator follow-up,
        non-blocking):} METHODS.md Validation-(a) and CONVENTIONS.md \S6 (``center is
        Einstein with negative Ricci, $\Lambda<0$'') are FALSIFIED for the cone-Hessian
        metric (which is not the spacetime metric anyway).
  \item \textbf{Phase 72 re-scope:} MATTER-ON-FLAT -- $M=0$ baseline flat Minkowski (KKT
        $\eta$); matter in $V_1/V_{1/2}$ sources $h$ via the $\det_3$ cross-term;
        cross-term ON/OFF and $\Lambda$-vs-matter tests apply as originally framed; NO
        $\Lambda$ tripwire.
  \item \textbf{Phase 73 re-scope:} the LINEARIZED-EINSTEIN test --
        $\Box\bar h_{\mu\nu}\sim\kappa T_{\mu\nu}$ on the KKT-flat $\eta$, correct tensor $+$
        gauge structure. CAN FAIL (non-Einstein PDE $\Rightarrow$ honest negative result) --
        the real gate. NO $\Lambda=0$ tripwire.
  \item \textbf{Genuine consistency check (done now):} $\det_2$/$\det_3$ splice-consistency
        $=$ PASS ($h$ a valid symmetric $(0,2)$ perturbation of the Lorentzian
        $\eta_{\mathrm{bg}}$; small matter preserves signature $(1,3)$;
        \texttt{70.1-01-metric-selection.py} step 9, exact over $\mathbb{Q}$). NOT a gate.
  \item \textbf{Rejected (recorded):} reconciliation (a) Einstein-on-$H^3$-leaf
        (\texttt{fp-relabel}); the ``which $\Lambda$ reference?'' baseline
        (\texttt{fp-assume-einstein}); construction (i) Wick-via-$u=e_7$ (not reopened).
        All decisive arithmetic exact over $\mathbb{Q}$ (\texttt{fp-float-decisive}); SSOT
        $=$ \texttt{bulk\_geometry\_verification.py} $\det_3$ (\texttt{fp-wrong-cross-term};
        \texttt{octonion\_algebra.py} untouched); no factor inserted to force
        Einstein/flatness (\texttt{fp-assume-einstein}).
\end{itemize}

\medskip\noindent\emph{End of Phase 70.1-01. Negative-result-is-success honored: option (1)
is reported FALSIFIED (NULL), the route survives restated, and the substantive GR test is
deferred to a Phase-73 linearized-Einstein derivation that can itself fail.}
