# 87 — GATE 5 SUMMARY: the v28 ledger

**v27.0 Phase 87. EXPLORATORY, NON-BLOCKING — NO claims/verdicts.** Prices the v28 object;
the v27.0 LIVE verdict is independent of everything here.

## (b-continuation) The spinor-moment map `p ↦ π_{1/2}^{(p)}(X)`

The tangent-valued matter field (the Peirce-1/2 projection of the compressed state at each
event). Well-defined: the Peirce projection is canonical at each `p`. Its U(1)/gluing
covariance (v22 contact) is the v28 object — "matter forces the connection" would mean this
tangent-valued field has non-flat holonomy. **Because v27 came up LIVE (the scalar law
closes), this object is the natural next step but OPTIONAL, not forced** — the scalar sector
already balances locally. Filed as the v28 candidate.

## (c) Claim-2 (J4 per-point-time) contact

`K_face = −log ρ_face` is explicit in `(m,q)` (face eigenvalues `½ ± ½√(1 − 4q/m²)`). A
per-point-time (J4) check would consume `K_face(m,q)` as the modular generator —
consumption note only.

**Filed for v28. No verdicts.** Language fence held throughout: no Einstein, no Newton
constant, no `G = κT`, no dark matter, no geodesic-motion or force language; "equation of
state" = multiplier-locality only.
