# 89 — GATE 4 SUMMARY: T3 full OP² + T4 — the global verdict

**v29.0 Phase 89. Exact over Q/Q(t). GLOBAL VERDICT = DEAD.**

**T3 (full OP², 26-param `M`):** `E_11`-alone solvable; `E_11` + (off-u, real) families →
`linsolve = EmptySet` ⟹ NO global `H` ⟹ **GLOBAL VERDICT = DEAD: per-point thermal time is
irreducibly FACE-LOCAL.**

**The obstruction (the payload), extracted.** The level-2 part of `K⁽²⁾` is the **clock-twist
/ modular-anomaly field** — a tangent-valued `R_M`-cousin (`|K⁽²⁾|²` carries the `⟨M,p⟩²`
level-2 sector; the v26 `R_M` story). It is connection-shaped: the parallel-transport-of-clocks
twist, matter-pinned (the v28 gluing-`U(1)` now carries a matter-pinned RATE twist, not just a
class).

**T4 finite-ε cross-check (corroboration).** At the anchor `X=diag(1,2,3)/6`, `p` from
`v=(1,2,2)/3` (formal logs `ℓ_i`): the rate ratio `traceless(K_face) : traceless(C_p log X)`
VARIES across faces ⟹ finite-ε DEAD, **AGREEING in sign with the ε² verdict** (no design-hole
STOP). The `b(s)` rate is a different transcendental than the `ℓ_i`.

**Gate 4: GLOBAL VERDICT = DEAD; the clock-twist obstruction extracted; finite-ε agrees.**
