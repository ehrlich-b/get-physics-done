# Phase 90 — GATE 0 SUMMARY (the cast)

**PASS (fail-fast).** The cast is certified.

- **0.a** D|_face = the v28 slice-rotation phase element: traceless, in-face V₀(E₁₁); D=[dU,·]
  (dU=diag(0,−e₇/2,e₇/2)) rotates it within the slice 2-plane {Re x₁, ⟨x₁,e₇⟩} (weight +1) —
  the ±i:1 slice pair of the recorded v28 spectrum {0:9, ±i:1, ±i/2:8}.
- **0.b** guard-6 battery: the 3×3-complex cut rep == the octonion engine (compress0, trace form,
  the v29 clock field) under e₇↔i.
- **0.c** canonical transport regression: ΔP_cut == −λ₁(E₁₁−I/3), λ₁(cut)=12 (the certified v25
  families machinery — the frozen geometry used for ∇).
- **0.d** v29 K_face⁽²⁾ re-verified: traceless K⁽²⁾ == −(9/2)⟨M,p⟩·traceless(C_pM) (symbolic cut M).
