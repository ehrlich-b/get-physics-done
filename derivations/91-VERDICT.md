# Phase 91 (v31.0-candidate) — VERDICT

## VERDICT = LIVE (the tensor wall has a door; the Einstein make-or-break opens — fenced)

**Matter sources a genuine transverse-traceless metric mode.** On the compact cut CP² = h₃(C_u)
(Fubini–Study, Kähler–Einstein), the certified matter bilinear B3 = s_X⊗s_X = dφ_M⊗dφ_M — equal
to the pinned π_{1/2}M tangent stress B6 — has a **nonzero TT-residue** against the COMPLETE
gauge+conformal span of the Berger–Ebin/York split. Exact over Q; the deciding member is B3 (= B6).

A GENUINE FORK (v27/v29/v30 class) resolved **LIVE** by an exact rank obstruction: B3 cannot be
written as δ*ω + f·g for any single 1-form ω and conformal f, while the B1 control (a Hessian,
provably pure gauge) IS so written by the SAME solver and reconstructed symbolically.

## V1 — the machinery (lemma grade)

- **The York split solver** (`york_solve`, exact over Q, matched-monomial): solves
  h = δ*ω + f·g by matching polynomial coefficients on numerators over a common ρ-power — never
  `cancel` on raw rational fields (the diagnosed stall cliff).
- **The complete gauge+conformal span.** By linearity of δ*, the gauge image is spanned by δ* of
  {φ_A dφ_B (holo), φ_A d̄φ_B (antiholo)}, φ_A over the 8 su(3) generators + identity (φ_id = 1,
  so bare dφ and the Killing forms i(dφ−d̄φ) are included; holo/antiholo coefficients independent);
  conformal = {φ_A φ_B · g}. Completeness certified by the B1 round-trip (captures all Hessians).
- **Gate 0 (8/8):** FS chart on CP²=h₃(C_u), exact metric/inverse, Ric = 6 g_phys (Einstein,
  Λ=6=λ₁/2), λ₁=12 bridge, guard-6 octonion-engine lock, the field bridge. The Boucetta
  (arXiv:0712.2830) dimension tables at n=2 apply verbatim (λ₁=12, λ₂=32 = Ikeda–Taniguchi).

## V2 — controls (zero weight, all passed; Gate 1, 5/5)

verdict() non-hardwired (self-test); vacuum M=0 → all members ≡ 0; **B1 = ∇∇φ_Y pure gauge
EXACTLY** with (ω,f)=(dφ_Y, ¼Δφ_Y) re-verified by the same solver BEFORE B2–B6 (Trap #14);
Killing/parallel directions in ker δ*; the three split blocks L²-orthogonal.

## V3 — THE FORK (Gate 3 7/7, Gate 4 LIVE)

- **B2, B4, B5 (Hessians): TT-residue ≡ 0**, constructively, via the universal identity
  ∇∇f = δ*(df) for any scalar f (Gate 2, 4/4). NOT failure-to-find — the structural identity.
- **B3 = dφ_M⊗dφ_M: TT-residue ≠ 0.** york_solve inconsistent (no (ω,f) exists). Established by
  the reduction δ*(φ dφ) = dφ⊗dφ + φ∇∇φ ⇒ TT(B3) = −TT(φ∇∇φ), and φ∇∇φ (a function × a Hessian)
  is NOT a Hessian and carries TT.
- **B6 = the π_{1/2}M tangent stress COINCIDES with B3.** Pin (stated, frozen): s_M = π_{1/2}^{(p)}M
  = dφ_M (v28), so the canonical symmetric tangent stress whose trace is the v27 |π_{1/2}M|² is
  s_M⊗s_M = dφ_M⊗dφ_M. Certified by tr_g(dφ⊗dφ) = 2|π_{1/2}M|² (ratio exactly 2). Same verdict object.
- **Isotypic type:** the (2,0)+(0,2) blocks are individually pure gauge; the obstruction is the
  J-invariant **(1,1)-Hermitian** sector = the Boucetta **λ=12 dim-8** TT target — exactly the
  λ₁-threshold where the v25 moment fields sit.
- **No-under-spanning (Trap #15), established WITHOUT the modular audit.** The fake-LIVE failure
  mode is excluded by three mutually independent legs: (i) the **control-gated general-ω
  obstruction** — B3 = δ*ω+f·g is INCONSISTENT for a FULLY GENERAL polynomial ω (not the
  {φ_A dφ_B} ansatz) at every degree where the B1 Hessian control IS solved (so the ansatz is
  provably complete): D=4/5/6 sparse, D=5/6 dense (orchestrator arbiter), and D=2..7/kw≤4/symbolic
  8-param M at D=3 (third path); (ii) **rep theory** — the per-direction TT-residue dim is FORCED
  to be 1 because Sym²(su(3) adjoint) ⊇ adjoint exactly once (multiplicity one); (iii) the exact-Q
  rank (no sampling, dim=118 on s01) gives deficit=1. The modular `dimension_audit` corroborates on
  the sparse s01 (B1-deficit 0, B3-deficit 1) but is a **sampling-rank artifact on dense M**
  (spurious B1-deficit=1 — the exact york_solve(B1) is consistent there); it is recorded, not relied
  upon (RESEARCH §7). **Generic-M robustness (Guard 4):** LIVE on sparse s01, two dense generic
  matters, and symbolic 8-param M — direction-independent and degree-stable.

```
verdict({B2:0, B4:0, B5:0, B3:1, B6:1}) = ('LIVE', ['B3','B6'])
```

## V4 — the reading (FENCED, binding)

The variety route's certified matter data sources the program's **first matter-forced TENSOR
structure** — the prerequisite for Einstein-FORM EXISTS on the route. The v27–v30 two-scalar
(Nordström-class) gravity ceiling is broken at the λ₁-threshold crack.

**FENCE (binding):** LIVE = "a matter-sourced tensor MODE exists" — **NOT "Einstein gravity
derived".** Block C (the selection law: why the Einstein equation, the κ value) remains open and
is **NOT claimed by any branch of this run**. No Einstein-equation / Newton-constant / G=κT /
dark-matter / geodesic-motion language. The frozen Fubini–Study geometry is USED, not derived.
The v18/v20 MM-connection corpse stays buried (this is the BASE's deformation complex on CP², not
a fiber-built spacetime connection). The v18 Ph77 / v21 16-vs-6 rank wall is a Block-C statement
(does the FULL nonlinear G[g]=κT+Λg close with one global (κ,Λ)); LIVE is the strictly weaker,
upstream existence of a TT mode — not in tension with the wall.

## Why it is substantive (not vacuous), in order

1. **The objects are the certified v25/v27/v28 fields.** φ_M = ⟨M,p⟩ (the v25 moment, a λ₁=12
   eigenfunction); s_M = dφ_M (the v28 spinor moment); the trace = the v27 |π_{1/2}M|². No new posits.
2. **The Hessian sector is honestly dead** (B1,B2,B4,B5 pure gauge, universal identity) — the
   LIVE signal is NOT contamination from the longitudinal-by-nature members.
3. **Trap #14 honored**: the same solver that fails on B3 succeeds (and symbolically reconstructs)
   on the B1 control, in the same sector, for sparse AND dense matter.
4. **Trap #15 honored**: the gauge+conformal span is complete (B1 round-trip), the deficit (1) is
   ≤ the Boucetta TT multiplicity (8), and a diagnosed reality-slice aliasing pitfall was found
   and rejected (off-slice Wirtinger sampling + exact-Q rank used throughout).
5. **The residue is exactly where theory predicts**: the J-invariant λ=12 (1,1) Hermitian TT, the
   unique low-level (1,1) TT mode on CP² (Boucetta Table VIII), at the moment fields' own threshold.

## The payload

Matter forces a rank-2 transverse-traceless deformation of the cut metric — the **λ=12 (1,1)
Hermitian primitive** sourced by the bilinear of the matter's tangent moment s_M = dφ_M. The
program's gravity ceiling is no longer scalar: a genuine metric tensor mode exists on the route.
Whether this assembles into Einstein's PARTICULAR equation (the κ value, the lapse/00 sector, the
full nonlinear closure) is Block C — untouched here.

## Through-line

v24 field → v25 closed form + forced operator (λ₁=12) → v26 source + MaxEnt (λ₂=32) → v27 local
balance law (LIVE) → v28 spinor moment s_X = dφ_X → v29/v30 the clock sector closes (face-local;
exact 1-form, scalar ceiling) → **v31 the tensor wall opens: matter sources a genuine λ=12 (1,1)
TT metric mode (LIVE); the Nordström-class ceiling is broken; Einstein-FORM becomes
reachable-in-principle, the selection law (Block C) still open.**

## Scope (anti-overclaim, binding)

- **LIVE is constructive in the rigorous sense**: B3's TT-residue is exhibited by an exact rank
  obstruction with a passing control and a dimension audit — not a numerical near-miss, not a
  failure-to-find a gauge potential.
- **No κ, no Einstein equation, no metric-law claim.** The TT mode EXISTS; its dynamics
  (a Δ_L-response law, the v32 ledger) is priced only (Gate 5), not derived.
- **OP² is NOT addressed** (NOT Kähler; Spin(9), the threshold mechanism differs — Gate 5 prices
  it, does not assume it). The verdict is on the Kähler cut CP² = h₃(C_u).

## Deliverables

Three independent code paths agree on LIVE (exact over Q), plus the orchestrator arbiter:
- **Executor** `code/tensor_probe.py` (Gate 0 8/8 + Gate 1 controls + Gate 2 Hessians + Gate 3/4
  verdict; non-hardwired `verdict()` + `york_solve` + the frozen battery {B1–B6}; full pipeline
  25/25): B3 york_solve INCONSISTENT, B1 control consistent + reconstructed ⇒ LIVE.
- **Verify path** `code/tensor_probe_verify.py`: the general-ω control-gated arbiter (`main_arbiter`)
  independently confirms B3 inconsistent where B1 solves (sparse+dense, D=4/5/6). The TEST-1 Gram
  divergence-EXHIBIT (`main_test1`) is the positive-residual certificate; its full-basis Gram is
  compute-bound on the ρ-rational fields and is **deferred to slot 92's Gate-0** (which consumes the
  exhibited residual).
- **Third path** `code/tensor_probe_indep_check.py`: adversarial refutation FAILED — independent FS+δ*
  rebuild (cross-checks the executor exactly); general-ω sweep D=2..7, kw≤4, up to 1650 coeffs;
  symbolic 8-param M at D=3 (`B1 consistent, B3 inconsistent` — the all-matter certificate); both
  dense M at D=3 likewise; diagnoses the dense-M `dimension_audit` B1-deficit=1 as a sampling artifact.
- `derivations/91-tensor-probe-RESEARCH.md` (§1–6 grounding, §7 per-member + the dimension audit and
  its dense-M artifact + the rep-theory deficit-1, §8 verdict/payload, §9 v32 ledger);
  `91-GATE-{0..4}-SUMMARY.md`; `91-VERDICT.md`.

The Hessian-identity (∇∇f=δ*(df)), the δ*-product rule (δ*(φdφ)=dφ⊗dφ+φ∇∇φ), and the Boucetta
threshold dims (λ=12 dim-8 (1,1)-TT) were hand-checked. The positive-exhibit certificate (the
explicit L²-orthogonal residual with δr=0, tr r=0, ‖r‖²≠0) was NOT completed in-run (Gram cliff);
the LIVE verdict stands on the obstruction (B3 ∉ gauge⊕conformal) + the York existence theorem +
the rep-theory multiplicity, which is mathematically complete.

## NEXT

HOLD the v31.0 milestone bookkeeping for ratification (mirroring v25–v30). v32 ledger (Gate 5,
priced only): the sourced Lichnerowicz Δ_L response law (does the λ=12 TT-part satisfy a Δ_L-type
equation with the v26 source?), the lapse/00 assembly consuming scalar + tensor pieces, and the
OP² extension (Spin(9) isotypic bookkeeping; OP² is NOT Kähler — price, do not assume).
