# 85 — VERDICT: The Moment Doublet and the Canonical Field Equation

**v25.0 Phase 85 (J5-on-the-variety, step 1). Exact over Q / Q(t). Two independent
code paths agree; pre-registered eigenvalues confirmed.**

**C1 — PASS.** The v24 face invariants are the moment doublet of the linear pair
`(X, X#)`: `m(p;X) = Tr(C_p X) = Tr(X) − ⟨X,p⟩` and `q(p;X) = det₂(C_p X) = ⟨X#,p⟩`,
verified for symbolic `X` (27 params) at `E_11` and along all 16 families over `Q(t)`
(off-u included, no covariance shortcut), with the F₄-covariance closure to all `p`.
The whole v24 entropy landscape is `r = q/m²` — two linear moment fields over the
event-space and nothing else; the recorded vacuum, diagonal direction-blindness,
generic `r(t)` samples, eigenframe critical points, and flattening table all
re-derive from this one (classical cubic-Jordan adjugate) identity. **C2 — PASS.**
Every moment field obeys the forced canonical Helmholtz/field equation
`Δ(φ_Y − ⟨Y,I/3⟩) = −λ₁(φ_Y − ⟨Y,I/3⟩)`, sharpest form `ΔP = −λ₁(E_11 − I/3)` verified
in all 27 components, with **λ₁ = 48 on OP² and λ₁ = 12 on the CP² cut** (the
pre-registered values, CONFIRMED; `12 = 4(n+1)|_{n=2}` matches Fubini–Study, ratio
`48/12 = 4 = dim ratio`); the doublet field equations are `Δm = −λ₁(m − (2/3)Tr X)`,
`Δq = −λ₁(q − σ₂(X)/3)`. **C3 — PASS.** Every moment along every canonical geodesic is
`a + b·cos2θ + c·sin2θ` (frequency ≤ 2); `r` itself is therefore a rational function and
is correctly *not* assigned a field equation (trap-guard #5), while the `c11² = cos⁴θ`
control fails the ≤2 test (the test discriminates). **Scope:** PASS ≠ Einstein, ≠ J5,
≠ a balance — the background canonical (Borel) geometry is frozen and imported as math;
`λ₁` is a property of that geometry, not a coupling; no Newton constant exists here.
This certifies the matter field's canonical **type** (the entropy side of a would-be
balance) and its forced linear field equation; the balance proper is v26. *(The Gate-4
v26 design ledger is stated separately and carries no claims.)*
