# Phase 90 (v30.0) — The Clock Connection: Matter-Sourced Curvature on the Gluing U(1)?

**Slot 90 / v30.0-candidate. GENUINE FORK (v27/v29 class). VERDICT = DEAD.**
Source: `paper6-clock-connection-prompt.md` (RATIFIED-BY-STANDING-DIRECTIVE).
Exact over Q / Q(t); octonion engine + 3×3-complex cut rep (guard-locked); u = e₇.

## The question

The v29 payload is the matter-pinned **clock-twist field** — the level-2 part of
K_face⁽²⁾ = −(9/2)⟨M,p⟩·traceless(C_pM). This run casts the twist into honest
**connection data** and asks, constructively: does matter force **curvature** on the
v22/v28 gluing U(1) over the event-space OP² = F₄/Spin(9) (cut CP² = h₃(C_u))?

- **LIVE** = the clock connection has nonzero, matter-shaped, background-subtracted
  curvature at ε² (the program's first matter-FORCED form-level structure; Maxwell-shaped).
- **DEAD** = the twist is exact as a 1-form (the clock connection is gauge-flat; matter pins
  class + rate + twist but NOT form; the v28 class-only ceiling is the ceiling).

## The cast (verified, Gate 0)

At each event p the face V₀(p) carries the clock K_face(p) = −log ρ_face(p),
ρ_face = C_pX / Tr(C_pX). The v28 generator **D = [dU,·]** (dU = diag(0,−e₇/2,e₇/2))
restricted to the face is the **slice-rotation** generator (the ±i:1 slice pair of the
recorded v28 spectrum {0:9, ±i:1, ±i/2:8}); as an element, **D|_face = the traceless
C_u-phase reference** (the coord-10 element x₁ = e₇ at E₁₁; in-face, traceless — verified).

Transport uses the **canonical F₄-invariant face-bundle connection**
∇_v Y = C_p(∂_v Y) (the Peirce-0 projection of the ambient derivative; the same frozen
geometry behind the v25 ΔP/λ₁ families, regression-checked: λ₁(cut)=12). D_p is the
**canonical transport** of D (∇-parallel). The **clock-drift 1-form**:

> **a_X(v) := ⟨∇_v K_face, D_p|_face⟩**, ⟨A,B⟩ = Tr(A∘B).

**ε-grading.** ε⁰ vacuum: K ∝ I ⇒ a ≡ 0 (flat). ε¹: K⁽¹⁾ realized by a global H⁽¹⁾ ~ M
(integrable; zero loop holonomy; forced control). The verdict lives at **ε²**, where the
traceless clock field is the v29 object **𝒦(p) := traceless K_face⁽²⁾ = −(9/2)⟨M,p⟩·traceless(C_pM)**.

## U1 — the ε² pairing (Gate 2)

The a²·I-part of K⁽²⁾ drops (D traceless ⇒ ⟨I_face, D⟩ = 0). The face Cayley–Hamilton
reconciles the two written forms: with N = C_pM, traceless(N²) = Tr(N)·traceless(N) and
Tr(C_pM) = m(p;M) = Tr(M) − ⟨M,p⟩ = −⟨M,p⟩, so
(9/2)traceless((C_pM)²) = −(9/2)⟨M,p⟩·traceless(C_pM) = 𝒦. Hence
**a_X⁽²⁾(v) = (9/2)⟨∇_v (C_pM)², D⟩ = ⟨∇_v 𝒦, D⟩.**

## The verdict object (U3, Gate 3) — exact, non-prejudged

The background-subtracted curvature 2-form is F⁽²⁾ = d(a_X⁽²⁾) MINUS the canonical-holonomy
background ⟨R(v,w)𝒦, D⟩ (trap #13). The decisive computation found:

1. **∇D = 0** — the canonical C_u-phase reference is **covariantly constant**: ∂_v D lands
   purely in V_{1/2}(p) (the cone-normal/tangent sector), which the face projection C_p kills.
   Verified at the base and along all four cut families, in BOTH the 3×3-complex rep and the
   octonion engine.

2. **Peirce orthogonality** ⟨𝒦, ∂_v D⟩ = 0: 𝒦 ∈ V₀(p) (a face element), ∂_v D ∈ V_{1/2}(p),
   and ⟨V₀, V_{1/2}⟩ = 0 (the trace form pairs Peirce blocks to zero) — convention-independent.

3. Therefore **a_X⁽²⁾ = dχ EXACT**, with the constructive potential
   **χ(p) = ⟨𝒦, D⟩ = −(9/2)⟨M,p⟩·⟨M, D_p⟩** (the clock's C_u-phase content), since
   a(v) − ∂_v χ = −⟨𝒦, ∂_v D⟩ = 0. Hence **F⁽²⁾ = da_X⁽²⁾ = 0 identically**.

4. **trap #13 calibration (Gate 1, mandatory).** The canonical transport's own holonomy is
   genuinely nonzero — the recorded v22 slot-82 slice holonomy h(φ) is flat at φ=0 (h(0)=I)
   and nontrivial at φ≠0 (the Fubini–Study background). The background machinery has teeth; it
   is NOT vacuous. Yet for the matter clock the subtracted curvature is exactly 0: the Bott
   background ⟨R𝒦, D⟩ = naive da = 0 (the FS holonomy paired with this face-valued clock
   vanishes), while ⟨R·Y, D⟩ ≠ 0 for a generic face field Y. The spectrum-matched diagonal
   reference (v24 direction-blind) gives zero subtracted holonomy (co-diagonalization stratum).

5. **Loop route (Stokes, independent).** ∮ a_X⁽²⁾ = ∮ dχ = 0 around any closed face-loop — an
   exact 1-form has zero holonomy. Corroborates the local F⁽²⁾ = 0 globally.

## Verdict and reading (U3/U4) — fenced

**DEAD.** The clock connection is **gauge-flat at form level**: matter pins the clock's
C_u-phase **potential** χ (class + rate + twist) but a_X⁽²⁾ = dχ is exact ⇒ **F⁽²⁾ = 0** ⇒
NO matter-forced U(1) curvature. The **v28 class-only ceiling stands**; the gluing U(1) is
unforced at form level for ALL the matter data the route possesses (s_X class + K rate + 𝒦
twist exhausted). Recorded as the form-selection boundary stone.

**Guard 5 (binding):** this does NOT follow from v29's no-global-H. The operator-valued
synchronization being over-determined and the 1-form being non-exact are INDEPENDENT
statements: a 1-form can be exact even when modular synchronization fails. v29 is motivation,
never evidence. **Language fence:** Maxwell-SHAPED only (had it been LIVE); no
Einstein/metric-law/Newton-constant/dark-matter/geodesic language; the v18/v20 MM corpse stays
buried; the frozen canonical geometry is used, not derived.

## Verification (three paths agree)

- Executor `code/clock_connection.py` (3×3-complex cut rep, guard-locked to the octonion
  engine): Gates 0–4, ∇D=0 + a=dχ exact + F_sub=0.
- Independent verifier `code/clock_connection_verify.py` (octonion engine; cut families built
  via the engine's own GS complement; eigenvalue-log K⁽²⁾; loop route): 6/6 PASS, DEAD.
- gpd-verifier `code/clock_connection_indep_check.py` (adversarial third path).

## v31 ledger (Gate 5, exploratory, NO claims)

(i) Block B Lichnerowicz/tensor probe (the Einstein make-or-break at λ₁=12), designed
blog-side. (iii) DEAD branch: the matter data (class + rate + twist) is exhausted and gives an
exact 1-form; the unforced-U(1) looks final at this order — open: higher-ε orders, or a
different pairing object. Priced only.
