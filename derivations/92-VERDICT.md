# Phase 92 (v32.0-candidate) — VERDICT

## VERDICT = LIVE (the tensor sector's source data CLOSES in canonical form — a dictionary fact)

**The matter-sourced transverse-traceless metric mode's NORM closes in the certified dictionary with
a FORCED exact constant and no free function.** On the compact cut CP² = h₃(C_u) (Fubini–Study,
Kähler–Einstein, Ric = 6g, Λ = λ₁/2 = 6), the certified matter bilinear B3 = s_M⊗s_M = dφ_M⊗dφ_M
(= the pinned π_{1/2}M tangent stress B6, v31) has a transverse-traceless residue `TT(B3)` in the
λ=12 (1,1)-Hermitian su(3)-adjoint multiplet whose **squared L²-norm is**

> **‖TT(B3)‖² = (1/54)·(TrM²)²**   — single FORCED rational constant κ = 1/54, **detM ABSENT**

— exactly the strong closure RESEARCH §1 predicts in shape (the only degree-4 SU(3)-invariant of a
traceless 3×3 Hermitian M is (TrM²)²; detM-blindness echoes v27). The Lichnerowicz threshold number is

> **ε = λ_L − 2Λ = 32 − 12 = 20**   (λ_L = 32 the Δ_L^{(1,1)} Schur scalar; ε ≠ 0, NON-marginal).

**Exact over Q.** This is the deferred v31 positive-exhibit certificate, obtained cliff-free, PLUS
the strictly higher v32 bar: the geometry tells matter exactly how much TT it gets, with no fitted
constant. The verdict CENTER is the norm identity (ii); (i)/(iv) are the dictionary, (iii) the
genuine sub-fork.

## V1 — the machinery (the cliff-free extraction, lemma grade)

- **Cliff-free TT-residue extraction** (RESEARCH §5A, the load-bearing method): the unique York TT
  part `r` of B3 (tr_g r = 0 AND δr = 0) is extracted explicitly by a MATCHED-MONOMIAL York solve —
  the gauge+conformal basis `(tr_g, δ)` images are computed once as monomial-coefficient dicts over a
  common ρ⁷ (cached; conformal blocks via the analytic `tr_g(f·g)=4f`, `δ(f·g)_b=−∂_b f`), and the
  target is matched by an EXACT CRT modular solve (40-prime Gaussian elimination + rational
  reconstruction). **NO full-basis Gram** (v31's CPU-roaster); ‖r‖² is one exact `l2_tensor` integral.
- **Point-evaluation is ill-conditioned** (diagnosed): point evaluation at small-denominator
  rationals produces gauge coefficients of ≥186 bits that fail reconstruction; matched-monomial keeps
  small coefficients and reconstructs cleanly. The exact residual over Q (all rows) is the genuine TT
  certificate.
- **Gate 0 (6/6):** v31 geometry reproduced (Ric=6g, λ₁=12 bridge, octonion↔3×3-complex field
  bridge); Kähler Riemann contracts to Ricci; the rough-Laplacian SIGN PIN (∇*∇ = −Δ_analyst gives
  +12 on λ₁, +32 on λ₂); the dim-8 (1,1)-Hermitian basis (every residue tr_g=0/δ=0; single-generator
  Gram rank-6 = the d-symbol image, generic-M {N(M)} rank-8 = the full multiplet); the full Kähler
  Weitzenböck R̊ per block (cross-checked by R̊(g)=Ric=6g, Δ_L(g)=0); Δ_L^{(1,1)} eigentensor.

## V2 — controls (zero weight, all passed; Gate 1, 6/6)

`verdict()` NON-HARDWIRED (self-test on synthetic flags); vacuum M=0 → every object ≡ 0; **the
sign-pin regression** λ₁=12/λ₂=32 through the rough-Laplacian code path; **Δ_L(g) = 0** (the metric is
Δ_L-harmonic on KE — the known structural answer, pinning the R̊ normalization); **Δ_L^{(1,1)} preserves
the gauge image** (Δ_L δ*ω = δ* Δ_H ω IN-REP; a Hessian's (1,1) TT-residue stays zero); **the SCHUR
check** Δ_L^{(1,1)} t_a = 32 t_a for d1, d2, s01 (same λ_L, forced by equivariance + multiplicity-one).

## V3 — THE CLOSED FORM (Gate 3, the verdict center)

- **(3a) The residue closes.** Every matter direction's TT residue is exhibited (tr_g=0, δ=0,
  nonzero) in the λ=12 (1,1)-Hermitian multiplet — confirming v31's existence cliff-free, for sparse,
  dense, and generic matter. Consistent with v31; no contradiction.
- **(3b) The direction hypothesis c(M) ∝ N(M) FAILS (a FINDING, not a failure).** Although
  `N(s01) = N(d1) = diag(1,1,0) − ⅔I`, the residues differ: `r^{(1,1)}(s01) ≠ r^{(1,1)}(d1)`. So the
  TT tensor direction is NOT the d-symbol square `N(M) = M² − ⅓Tr(M²)I` — it carries MORE M-information
  than N(M) (the full dφ_M⊗dφ_M structure, not just the adjoint square). This refines object (i): the
  closed form `TT(B3) = Σc_a t_a` exists, but `c(M)` is richer than the named hypothesis. **The
  dictionary's first nuance — named.**
- **(3c) THE NORM IDENTITY closes with a FORCED constant.** `‖TT(B3)‖² = (1/54)(TrM²)²`, κ = 1/54 the
  SAME rational for s01, d1, **d2 (detM=−2)**, a01, and **a dense generic matter (detM=−7)** — so
  **detM is ABSENT** (the v27 cubic-blindness; the only degree-4 SU(3)-invariant for traceless 3×3 is
  (TrM²)², verified). No fitted constant, no free function — the FORCED closed form.
- **(3d) The York dictionary closes:** `B3 = r + δ*ω + f·g` EXACT over Q with (ω, f) in certified
  closed form (object (iv)); r tr_g=0, δ=0.

## V3-bis — ε, the genuine sub-fork (Gate 2)

`λ_L = 32` (Δ_L^{(1,1)} Schur scalar), so **ε = 32 − 12 = 20 ≠ 0**: the λ=12 (1,1) multiplet is
**NON-marginal** under our certified convention. The trap-#16 tension (Besse 12.28 ker(Δ_L−2Λ)|_TT =
Einstein deformations; Besse 12.98 CP^n Koiso-RIGID; Boucetta's (1,1) TT "at" λ=12) is resolved
**factually and mechanically**: the Lichnerowicz eigenvalue λ_L=32 is the SCALAR Laplacian eigenvalue
λ₁=12 of the carrier PLUS the Weitzenböck shift — they are NOT the same operator. λ_L = 32 ≠ 12 = 2Λ,
so `ker(Δ_L−2Λ)|_TT = ∅`, fully CONSISTENT with Koiso rigidity. ε = 20 is canonical
response-STIFFNESS data (named mechanically; NO convention picked to force a marginal answer).

## V4 — the reading (FENCED, binding)

LIVE = **"the tensor sector's source data CLOSES in canonical form"** — a DICTIONARY fact: the
matter-sourced TT mode's NORM is `(1/54)(TrM²)²` with a forced constant, and its Δ_L-stiffness is
ε = 20, in the certified deformation-complex dictionary of the FROZEN cut geometry. This is the
program's first forced tensor-RESPONSE-SHAPED coefficient (the norm), strictly stronger than v31's
EXISTENCE (which generic-position DOF counting already predicted).

**FENCE (binding, verbatim):** this is the deformation-complex DICTIONARY of a FROZEN imported
geometry. **No dynamical metric, no selection law, no κ.** LIVE is a DICTIONARY fact, NOT a dynamics
fact. **NO Einstein-equation / Newton-constant / G=κT / dark-matter / geodesic language.** The frozen
Fubini–Study geometry is USED, not derived. The v18/v20 MM-connection corpse stays buried (this is the
BASE's deformation complex on CP², not a fiber-built spacetime connection). The v18 Ph77 / v21
16-vs-6 rank wall, the constructed-vs-extremized gap, the Riemannian signature, and the base/format
clamp are untouched by construction. This run does NOT retract v17–v21 (Block-C statements; v32 is the
strictly weaker upstream dictionary). Paper 5 remains the only result in the more-than-nothing column.

## Scope (anti-overclaim, binding)

- **LIVE is the NORM closure, NOT the direction.** The direction hypothesis c(M)∝N(M) FAILED (3b); the
  closed form `TT(B3)=Σc_a t_a` exists but `c(M)` is richer than the d-symbol square. The verdict rests
  on the FORCED norm identity ‖TT(B3)‖² = (1/54)(TrM²)² (object ii), which closes with no free function
  — the licensed v32 LIVE criterion (RESEARCH §6 Gate 4, verbatim).
- **ε = 20 is a Δ_L-stiffness of the frozen deformation complex, NOT a dynamical response.** The
  Fredholm/sourced-response EQUATION (Δ_L−2Λ)h = source is Block-C-shaped (where five gravity routes
  died) — NAMED and FENCED, priced only in the Gate-5 ledger, NOT run.
- **OP² is NOT addressed** (Spin(9), not Kähler; the (1,1) split does not transfer — Gate 5 prices it).
  The verdict is on the Kähler cut CP² = h₃(C_u).

## Deliverables

- **Executor** `code/lichnerowicz_response.py` (Gate 0 6/6 + Gate 1 6/6 + the norm/ε/direction
  computations; non-hardwired `verdict()`, cliff-free `extract_tt` matched-monomial CRT, full Kähler
  Weitzenböck `Rdot`, `lichnerowicz_11`; exact over Q): B3 TT residue exhibited (tr=0,δ=0,≠0), λ_L=32
  Schur-confirmed, ε=20, ‖TT(B3)‖²=(1/54)(TrM²)² (detM absent), c(M)∝N(M) FAILS, dictionary closes.
- `derivations/92-tensor-dictionary-RESEARCH.md` §§7–9 (the closed form, the norm identity + ε, the
  v33 ledger); `92-GATE-{0,1,2,3,4}-SUMMARY.md`; this `92-VERDICT.md`.

The cliff-free extraction, the full Kähler Weitzenböck (cross-checked by R̊(g)=Ric, Δ_L(g)=0), the
sign pin (λ₁=12/λ₂=32 through ∇*∇), the Schur scalar λ_L=32, and the forced norm κ=1/54 (detM absent,
across detM≠0 matters) are all exact over Q. The direction-N(M) failure was a genuine finding caught by
the N(s01)=N(d1) ⇒ r(s01)≠r(d1) test.

## Through-line

v24 field → v25 closed form + forced operator (λ₁=12) → v26 source + MaxEnt (λ₂=32) → v27 local balance
law (LIVE) → v28 spinor moment s_X = dφ_X → v29/v30 the clock sector closes (face-local; scalar
ceiling) → v31 the tensor wall opens: matter sources a genuine λ=12 (1,1) TT metric mode (LIVE,
EXISTENCE) → **v32 the tensor DICTIONARY closes: the TT mode's norm is the FORCED ‖TT(B3)‖² =
(1/54)(TrM²)² (detM absent) and its Δ_L-stiffness is ε = 20 — the source data closes in canonical form
(LIVE, the program's first forced tensor-response coefficient), the direction is richer than the
d-symbol square (a named finding), the selection law (Block C) still open.**

## NEXT

HOLD the v32.0 milestone bookkeeping for human ratification (mirroring v25–v31). The v33 ledger (Gate
5, priced only) is in RESEARCH §9: the lapse/00 assembly (consuming ε, c(M), the v26/v27 scalar
sector; needs the 4d-slice embedding), the OP² lift (Spin(9), not Kähler — price, do not assume), and
the Fredholm/response READING of ε (a Block-C-shaped import — named, fenced, NOT run).
