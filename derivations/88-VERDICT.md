# 88 — VERDICT: The Spinor Moment — the Tangent-Valued Matter Field and its Topology

**v28.0 Phase 88 (J5-on-the-variety, step 4). Verification-and-extraction. Exact over
Q/Q(t). Two independent code paths agree (executor 14/14, eigenprojector path 9/9).**

**E1 — PASS (the identification).** The spinor moment s_X(p) = π_{1/2}^{(p)}(X) EQUALS dφ_X,
the gradient of the v24 landscape φ_X(p)=⟨X,p⟩ (verified at E_11 and along all 16 families,
two projection paths); ‖s_X‖²_tr = TrX² − a² − (TrX−a)² + 2c; the direction is NOT
scalar-determined (the v24 direction data). **E2 — PASS (zeros = the eigenframe).** s_X(p)=0
⟺ X ∈ J_1(p)⊕J_0(p) ⟺ [X,p]=0; distinct spectrum ⟹ exactly the three eigenframe idempotents
{p_1,p_2,p_3} (verified diagonal, permutation-rotated, and rotated-idempotent frames with
F_4-covariance explicit); degenerate strata recorded & excluded (X=I/3 ⟹ s≡0; double
eigenvalue ⟹ a positive-dim zero locus). **E3 — PASS (the topology).** For diagonal X with
x_1>x_2>x_3: Morse indices (4,2,0) on the cut and (16,8,0) on OP² (from real Hessian signs,
φ''(0)=8(x_2−x_1)), Poincaré–Hopf index +1 at each (all even), so **Σ = 3 = χ(CP²) = χ(OP²)
— the Euler closure exact on BOTH spaces** (χ=3 cited, Borel / Atiyah–GS — not claimed); the
v25 cross-check Δφ(E_11)=4(x_2+x_3−2x_1)=−12(φ−φ̄) exact; the C_u-winding decomposition (the
extracted new numbers) gives per-zero weight-sign patterns E_11:(−,−), E_22:(+,−),
E_33:(+,+) with C_u-phase winding +1 per line. **Gate 0 (the v22 dictionary) holds:** the
v22-unforced gluing U(1) circle (slot 82, Spin(8), the C_u phase) acts on the V_0 slice as
+J_Cu and on the J_{1/2} tangent as −½J_Cu — v28's object is the v22-U(1)'s action on the
base tangent. **E4 (the reading, fenced):** the v22-unforced U(1) now carries matter-pinned
TOPOLOGICAL data (zeros = the matter eigenframe, indices/weights, X-determined); the LOCAL
FORM of the gluing stays unforced (the v29 fork). The vacuum pins nothing (s≡0) — matter
creates the topological skeleton. **Scope:** connection-KINEMATICS on the candidate BASE —
NOT gravity, NOT a metric law, NOT a selection law (the v18/v20 Cartan/MM corpse stays
buried); "matter-pinned" = the topological class only; no Einstein, no Newton constant, no
G=κT, no dark-matter or geodesic-motion language; signature OPEN. *(The Gate-5 v29 ledger is
stated separately and carries no claims.)*
