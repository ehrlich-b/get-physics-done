# 89 — GATE 1 SUMMARY: controls (the trap inventory; ZERO evidential weight)

**v29.0 Phase 89. Exact over Q/Q(t). ALL PASS.** Every trivial stratum behaves; none can
produce the verdict.

- **verdict() self-test (non-hardwired):** H-exists→LIVE, no-H→DEAD.
- **ε⁰ vacuum:** `X=I/3` ⟹ `ρ_face=I₂/2`, `traceless(K)=0` (`K ∝ I`).
- **Trap #6:** eigenframe / single-rotation faces co-diagonalize (`C_{E_ii}X` diagonal) ⟹
  structurally consistent, ZERO evidence — EXCLUDED from the verdict.
- **Trap #7:** `traceless(K)⁽¹⁾ = −3·traceless(C_pM)` (LINEAR) ⟹ `H⁽¹⁾=M` always coherent;
  ε¹ carries ZERO verdict weight — **the verdict is at ε²**.
- The **generic anchor face** `v=(1,2,2)/3` mixes all three eigendirections (`[X,p]≠0` for
  `X=diag(1,2,3)/6`) — a valid verdict face (not in any trivial stratum).

**Gate 1: ALL PASS.**
