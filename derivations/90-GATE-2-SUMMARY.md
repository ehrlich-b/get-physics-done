# Phase 90 — GATE 2 SUMMARY (U1 — the ε² pairing)

**PASS.** a_X⁽²⁾(v) = (9/2)⟨∇_v (C_pM)², D⟩, derived & verified symbolically (cut M at E₁₁).

- **Face Cayley–Hamilton:** traceless((C_pM)²) = Tr(C_pM)·traceless(C_pM), and
  Tr(C_pM) = m(p;M) = Tr(M) − ⟨M,p⟩ = −⟨M,p⟩ ⇒ (9/2)(C_pM)² and the v29 form
  −(9/2)⟨M,p⟩·traceless(C_pM) AGREE.
- **I-drop:** ⟨face-identity, D⟩ = 0 (D traceless) ⇒ the −(9/8)a²·I part of K⁽²⁾ contributes
  nothing to a_X.
- **Pairing:** ⟨𝒦, D⟩ == (9/2)⟨(C_pM)², D⟩ (symbolic cut M) ⇒ a_X⁽²⁾ = ⟨∇_v 𝒦, D⟩ as stated.
