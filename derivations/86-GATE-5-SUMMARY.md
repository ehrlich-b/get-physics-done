# 86 — GATE 5 SUMMARY: the v27 fork ledger

**v26.0 Phase 86. EXPLORATORY, NON-BLOCKING — NO claims/verdicts.** Prices the three
genuine forks of the route for v27; the v26.0 verdict is independent of everything here.

## (a) The constrained-balance equation-of-state fork

With `G_M` (the second-order response field) and the canonical moment `c11` both now
explicit level-fields, the locus `∇G_M ∥ ∇c11` is ALGEBRAIC. The fork question:
**is the Lagrange multiplier a state-universal function of the LOCAL field values
`(m, q, c11)` alone?** Invariant-counting note: mixed `U_X`-type invariants of `(X,p)`
exist beyond the moments (e.g. `⟨X∘X, p⟩`, `⟨X#, p∘p⟩`-types), so state-dependence
BEYOND the local fields is POSSIBLE — a genuine fork, not a foregone conclusion.
Computability: `G_M` and `c11` are both frequency-≤2, so `∇`-parallel is a low-degree
algebraic variety — tractable symbolically (estimate degree from one instance).

## (b) The U(1)/connection fork

The scalar moments `(m, q)` are gluing-INVARIANT: the v22 U(1) phase acts inside
`V_{1/2}`, while `⟨M,p⟩`, `⟨M#,p⟩` are F₄ trace-form scalars ⇒ **the U(1) cannot couple
to `(m,q)`** (one-line invariance check, confirmed). So a matter-forces-the-connection
test needs the `V_{1/2}` SPINOR-valued data (which v24 proved the landscape resolves
directionally). Object to build for v27: the spinor-moment map `p ↦ π_{1/2}(C_pX)` and
its U(1)-covariance.

## (c) Claim-2 (J4 per-point-time) contact

`K_face = −log ρ_face` is now an EXPLICIT function of `(m, q)` (face eigenvalues
`½ ± ½√(1 − 4q/m²)`). A per-point-time (J4) check would consume `K_face(m,q)` as the
modular generator. Filed for v27.

**All three forks filed. No verdicts.** The genuine route-forks (equation-of-state;
connection coupling) are explicitly NOT tested in v26 — this gate only prices them.
