# 86 — VERDICT: The Sourced Field Equation (MaxEnt Derived + Second-Order Vacuum Structure)

**v26.0 Phase 86 (J5-on-the-variety, step 2). Exact over Q[ε]/(ε³), Q(t). Two
independent code paths agree; λ₂(OP²) confirmed four ways.**

**B1 — PASS (MaxEnt derived).** Perturbing the vacuum `X = I/3 + εM` (traceless `M`),
the doublet collapses: `m = 2/3 − ε⟨M,p⟩`, `q = 1/9 − (ε/3)⟨M,p⟩ + ε²⟨M#,p⟩`, so
`δq = δm/3` and **`δr = 0` identically at every event `p`** (symbolic `M` at `E_11` + all
16 families over `Q(t)`) — the vacuum's MaxEnt property is now a theorem of the doublet
(first order empty; the honest restatement of v23's kill). **B2 — PASS (the response
field).** `δ²r = (9/4)G_M ε²` with `G_M(p) = ⟨M#,p⟩ − ¼⟨M,p⟩²`; the 2-face entropy is
analytic at `r = 1/4` (`f'(1/4)=2`), so `δ²S = (9/2)G_M ε²`; `S(ρ_face‖ρ_vac) = log2 −
S` exactly; and **`G_M ≤ 0`** structurally (`= −¼(face eigengap of C_pM)²`, equality iff
`C_pM ∝ I₂`; anchors `G(E_11)=0`, `G(E_22)=−9/4`). **B3 — PASS (the sourced equation).**
The level split `⟨M,p⟩² = α⟨M#,p⟩ + β TrM² + R_M` is consistent (the overdetermined
symbolic solve is unique): **OP² `(α,β,λ₂) = (1/7, 9/182, 104)`, cut `(2/5, 3/20, 32)`**;
`λ₂(cut)=32` confirms Fubini–Study and `λ₂(OP²)=104` is derived (matching the closed-form
Cayley-plane spectrum `4k(k+11)`, ratio `13/6`). The sourced field equation, constants
derived exactly: **`(Δ+λ₁)G_M = −κ₀ TrM² + ((λ₂−λ₁)/4)R_M`**, `κ₀ = 108/13` (OP²) `= 9/4`
(cut) — free propagation (v25) plus an explicit matter source quadratic in `M`. **B4 —
PASS (hidden sector).** Off-u matter is first-order INVISIBLE on the CP² cut (`⟨M,p⟩≡0`)
yet still SOURCES it (`G_M|_cut = ⟨M#,p⟩ ≠ 0`) with a SQUARE-FREE (level-≤1) signature;
on the cut, first-order-invisible ⟺ off-u-supported ⟺ square-free source. **Scope:** PASS
≠ Einstein, ≠ J5 complete, ≠ a balance with an independent geometric variation — the
canonical geometry is STILL FROZEN; `λ₁, λ₂, α, β, κ₀` are spectral/structural data of
that frozen geometry (imports-as-math), NOT couplings; no Newton constant, no `G=κT`, no
dark-matter language; signature OPEN. The genuine forks (equation-of-state; connection
coupling) are explicitly NOT tested — Gate 5 prices them for v27. *(The Gate-5 v27 fork
ledger is stated separately and carries no claims.)*
