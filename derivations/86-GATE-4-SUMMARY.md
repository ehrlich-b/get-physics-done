# 86 — GATE 4 SUMMARY: B4 — the hidden sector (the 4-vs-16 gap materialized)

**v26.0 Phase 86. Exact over Q/Q(t). VERDICT = B4 PASS.**

For matter `M` supported entirely off-u (octonion directions `e_1..e_6` of the entries),
all three statements verified symbolically + with explicit instances:

- **(invisibility)** `⟨M,p⟩ ≡ 0` for ALL `p` in the CP² cut (`E_11` + the 4 cut families
  over `Q(t)`) — trace-form block orthogonality of off-u `M` to `h_3(C_u)`. The cut has
  NO first-order moment and NO direction data for hidden `M`.
- **(it still sources)** `G_M|_cut = ⟨M#,p⟩|_cut ≠ 0` for `M ≠ 0` (e.g. `⟨M#,E_11⟩ =
  −(|x1_offu|²) = −(m0²+…+m5²)`) — the hidden squares land on the diagonal / in `C_u`;
  different cut points see different hidden octonion blocks.
- **(signature dichotomy)** the cut source is **square-free** (the `−¼⟨M,p⟩²` term
  vanishes identically on the cut ⇒ level ≤ 1, no anisotropic level-2 signature), while
  every u-aligned `M' ≠ 0` is first-order VISIBLE on the cut (cut-moment injectivity,
  v25's `9 = 1⊕8`). So on the cut: **first-order-invisible ⟺ off-u-supported ⟺
  square-free source.**

Instances: hidden `e_1` in the `(2,3)` block — `⟨M,E_11⟩ = 0` (invisible), `G|_cut =
−1/4` (sources); a u-aligned contrast — `⟨M,E_11⟩ = 1` (first-order visible).

**Scope:** NO dark-matter language — these are statements about THIS landscape's cut
restriction, nothing else; the CGM/Speranza question stays out of scope (finite face-
purity, not a QFT entropy). **Gate 4: B4 PASS.**
