# 86 — GATE 1 SUMMARY: controls (known answers; ZERO evidential weight)

**v26.0 Phase 86. Exact over Q[ε]/Q(t). ALL PASS.** Carries no evidence; proves the
machinery behaves and the level-2 path is independent.

- **(i) v25 free-equation regression:** one moment obeys `Δφ_Y(E_11) == −48(φ_Y −
  ⟨Y,I/3⟩)` — the v25 result re-verified.
- **(ii) level-2 control (independent λ₂ path):** `c11² ` via `M₀ = E_11 − I/3` yields
  `λ₂ = 104` on OP² — the SAME `λ₂` as the 26-param symbolic solve, obtained from a
  single function `c11` (not the full M-battery). Independent confirmation.
- **(iii) `M = diag(2,−1,−1)` anchors:** `G(E_11) = 0` (the face sees only the `∝I₂`
  block), `G(E_22) = −9/4 < 0`, `⟨M#,E_22⟩ = −2` — the design-sketch hand values
  reproduced exactly.
- **(iv) `M = 0` (vacuum):** `G_M ≡ 0`, `⟨M,p⟩ ≡ 0`, `TrM² = 0` — everything vanishes.

**Gate 1: ALL PASS** — all four known-answer controls behave; the level-2 eigenvalue
has an independent route.
