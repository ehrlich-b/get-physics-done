# 84 — GATE 1 SUMMARY: vacuum-homogeneity calibration

**v24.0 Gate 1. Exact over Q. PASS (carries ZERO evidential weight — pre-registered tautology).**

For the faithful state X = I/3, the rank-2 complement face state is computed along
all four families:

- Tr(C_p(I/3)) = **2/3** constant along every family.
- face purity r(p) = det(ρ_face) = **1/4** constant along every family.

⟹ spectrum of ρ_face(p) = (1/2, 1/2) constant ⟹ **S_face ≡ log 2** along the whole
variety. The vacuum landscape is **HOMOGENEOUS**.

This is the F_4-invariance tautology (I/3 is fixed by all of F_4, so S_{I/3}(g·p) =
S_{I/3}(p) for every g; the landscape is constant by symmetry). It carries **zero
evidential weight** — it only confirms the machinery is wired correctly (the face
object correctly returns a constant for the maximally symmetric state). The
substance is entirely in Gate 2.

Independently reproduced by the verifier via the 27×27-projector compression path
(r=1/4 at sample t).

Gate 1 PASS — proceed to Gate 2.
