# 87 — GATE 3 SUMMARY: D3 Tier A — the decisive sector (the verdict)

**v27.0 Phase 87. Exact over Q. VERDICT (sector) = LIVE.** Independently confirmed
(`87-GATE-3-VERIFICATION.md`, separate eigenprojector path + adversarial certificate hunt).

## The reduction (the load-bearing move)

On the parallel locus `dG_M = λ·dc_R`, metric-free, so `λ² = |dG_M|²/|dc_R|²` and
`|dc_R|² = |π_{1/2}(R)|² = 2c_R(1−c_R)` (exact for every rank-1 idempotent). Hence
> **LIVE-in-sector ⟺ `Q_M := |π_{1/2}(M# − ½aM)|²` is a function of the tuple
> `(a, c, TrM², detM)`.**

## Tier A — the u-complex sector elimination

For `M ∈ h₃(C_u)` traceless (8 params), `Q_M` fits the degree-4 tuple ansatz EXACTLY
(residual identically 0), coefficients `(A,B,C,D,E,F,G) = (0,−2,0,¼,−½,5/2,−1)`. So
`Q_M` is tuple-determined ⇒ **LIVE-in-sector**, with the law
> **`4 c_R(1−c_R) λ² = 2 Q_M = (½TrM² − a² + c)(a² − 4c) = −4·|π_{1/2}M|²·G_M`.**

The factored form is verified exactly (`a² − 4c = −4G_M`, the squared face eigengap;
`|π_{1/2}M|² = ½TrM² − a² + c`). The anchor `4c_R(1−c_R)λ² = 1` is its specialization.

**`detM` does not appear** (`C = 0`): the law is cubic-norm-blind. `G_M ≤ 0` (v26
positivity) ⇒ `λ² ≥ 0`. The v26 entropy-response field IS the multiplier's source.

**Gate 3: D3 Tier A = LIVE-in-sector; the exact law `P` exhibited.** (Globalization to OP²
in Gate 4.)
