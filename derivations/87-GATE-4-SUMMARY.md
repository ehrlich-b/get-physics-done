# 87 — GATE 4 SUMMARY: D3 Tiers B/C + the global verdict + D4

**v27.0 Phase 87. Exact over Q. GLOBAL VERDICT = LIVE.** Independently confirmed
(`87-GATE-3-VERIFICATION.md`, incl. adversarial certificate hunt — none found).

## Globalization (Tiers B, C)

The Tier-A law verifies UNCHANGED on the larger sectors:

| tier | `M` | params | tuple-fit | coeffs |
|---|---|---|---|---|
| B | diag + one octonion block | 10 | EXACT, residual ≡ 0 | `(0,−2,0,¼,−½,5/2,−1)` |
| C | full traceless OP² | 26 | EXACT, residual ≡ 0 | `(0,−2,0,¼,−½,5/2,−1)` |

Identical coefficients across A/B/C — `Q_M` is tuple-determined by a polynomial IDENTITY
through the full 26 parameters. Since the fit is an exact identity (not least-squares),
**no certificate pair can exist** — a symbolic PROOF of LIVE.

## The global verdict (verdict rule applied)

`Q_M` tuple-determined symbolically through Tier C ⇒ no `(same tuple, different λ)`
certificate ⇒ **LIVE** (verdict rule: "P exhibited AND verified symbolically through
Tier C = LIVE"). Corroboration: a `44/44` rational on-locus battery spread across octonion
directions satisfies the law exactly; the adversarial certificate hunt (matched-tuple
non-stabilizer-related `M` pairs) finds `Q_M` always equal.

> **THE LAW: `c_R(1−c_R)·λ² = −|π_{1/2}M|²·G_M(p)`,**
> i.e. `4 c_R(1−c_R)λ² = (½TrM² − a² + c)(a² − 4c)`. The multiplier² is the J_{1/2}-norm of
> the matter times `−G_M` (the v26 entropy response), over the canonical factor.

## D4 — the reading (LIVE branch, fenced)

The route's FIRST selection-shaped LOCAL law: on the parallel locus, the second-order
entropy response trades against the canonical coordinate at a rate fixed by local field
values ALONE. **STILL NOT Einstein, NOT a metric law; the canonical geometry is FROZEN; `λ`
is a multiplier NOT a coupling; no Newton constant; signature OPEN.** LIVE makes the
spinor-moment object `p ↦ π_{1/2}^{(p)}(X)` (v26 Gate-5(b)) the natural but OPTIONAL v28
object (the scalar law already closes). **We are in the LIVE world: the scalar sector closes
into a local balance.**

**Gate 4: GLOBAL VERDICT = LIVE.**
