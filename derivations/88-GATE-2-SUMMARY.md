# 88 — GATE 2 SUMMARY: E1 (s_X = dφ_X) + E2 (zeros = eigenframe)

**v28.0 Phase 88. Exact over Q/Q(t). VERDICT = E1 PASS, E2 PASS.** Independently confirmed
(`88-GATE-2-VERIFICATION.md`; eigenprojector + operator-commutator paths).

## E1 — the identification
`s_X = π_{1/2}^{(p)}(X) = dφ_X`, the gradient of the v24 landscape φ_X(p) = ⟨X,p⟩: verified
for symbolic X via the gradient representation at E_11 AND `d/dt φ_X(p(t))|_0 = ⟨s_X, ṗ(0)⟩`
along all 16 families. Norm `‖s_X‖²_tr = TrX² − a² − (TrX−a)² + 2c` (general X). **Direction
NOT scalar-determined:** two X with equal (a, c, TrX², detX) at E_11 have different s_X
direction (F_12(1) vs F_12(e_1)) — the v24 direction data.

## E2 — zeros = the matter eigenframe
`s_X(p) = 0 ⟺ X ∈ J_1(p)⊕J_0(p) ⟺ [X,p] = 0` (operator characterization, independent of the
projection). Distinct spectrum ⟹ exactly {p_1, p_2, p_3}; verified for diagonal X, a
permutation-rotated frame (zeros at g·E_ii — F_4-covariance EXPLICIT), and a rotated
idempotent frame. **Degenerate strata recorded & excluded:** X = I/3 ⟹ s ≡ 0 (vacuum
control); double eigenvalue ⟹ one isolated zero + a positive-dim zero locus (CP¹ cut / S⁸
OP²), no index claim there.

**Gate 2: E1 PASS, E2 PASS.**
