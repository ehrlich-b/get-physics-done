# Phase 90 — GATE 4 SUMMARY (U3 full + U4)

**PASS. Verdict = DEAD.**

- **(a) full OP² spot:** along an off-u (e₁) octonion direction the load-bearing facts survive —
  the clock 𝒦 is a V₀ (face) element and the tangent is V_{1/2}, so the Peirce orthogonality
  ⟨𝒦, ∂_v D⟩ = 0 that gives a = dχ is direction-agnostic; the exactness is NOT a cut artifact.
- **(b,c) loop set:** by Stokes the subtracted clock holonomy around any small loop (encircling
  an eigenframe zero of s_X, or a generic loop) equals the integral of F⁽²⁾ = 0 — corroborates
  the local 2-form globally, no new evidence.
- **U4 reading (fenced) — DEAD:** the clock connection is gauge-flat at form level. Matter pins
  the clock's C_u-phase potential χ = ⟨𝒦, D⟩ = −(9/2)⟨M,p⟩⟨M, D_p⟩ (class + rate + twist) but
  a_X⁽²⁾ = dχ is exact ⇒ NO matter-forced U(1) curvature. The v28 class-only ceiling STANDS;
  the gluing U(1) is unforced at form level for ALL the matter data the route possesses.
  Recorded as the form-selection program's third boundary stone of its kind.
  (Guard 5: independent of v29's no-global-H. Maxwell-shaped only; no EM/Einstein/metric claims.)
