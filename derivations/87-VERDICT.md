# 87 — VERDICT: The Constrained Balance — the Equation-of-State Fork

**v27.0 Phase 87 (J5-on-the-variety, step 3). A GENUINE FORK. Exact over Q/Q(t). Two
independent code paths + an adversarial certificate hunt agree.**

**VERDICT = LIVE — the equation-of-state closes into a local balance law.** The fork: on
the parallel locus `dG_M = λ·dc_R` (covector proportionality, metric-free), is the
multiplier `λ` a state-universal function of the frozen local tuple `(a, c, c_R, TrM², detM)`,
`a=⟨M,p⟩`, `c=⟨M#,p⟩`? **D1** the locus is non-empty off the trivial strata (the hand anchor
is the existence certificate); the strata `{dc_R=0, dG_M=0, λ=0}` are recorded and excluded.
**D2** the hand anchor reproduces exactly over Q(t) (`M₀#`, tuple `(1,0,4,1)`, `λ=1/(2cs)`,
`4c_R(1−c_R)λ²=1`); the tuple Jacobian has generic rank 4 (no forced relation). **D3** the
reduction `λ² = |dG_M|²/|dc_R|² = Q_M/(2c_R(1−c_R))` (with `|dc_R|²=2c_R(1−c_R)` exact for
every rank-1 idempotent) makes `LIVE ⟺ Q_M := |π_{1/2}(M#−½aM)|²` tuple-determined; and the
elimination shows `Q_M` IS tuple-determined by an exact polynomial IDENTITY through Tier C
(full 26-param `M` on OP², residual identically 0, identical coefficients on the cut, the
one-octonion-block, and full sectors) — a symbolic proof that **no certificate pair exists**.
Hence **LIVE**, with the exact law

> **`c_R(1−c_R)·λ² = −|π_{1/2}M|²·G_M(p)`**, i.e.
> **`4 c_R(1−c_R)λ² = (½TrM² − a² + c)(a² − 4c)`**

— the multiplier² is the J_{1/2}-norm of the matter times `−G_M` (the v26 second-order
entropy-response field, `≤0`, making `λ²≥0`), over the canonical-coordinate factor; `detM`
does not appear (cubic-norm-blind). (Convention: `|π_{1/2}M|² := ½TrM²−a²+c`, the reduced
norm `|x₂|²+|x₃|²`; the trace-form norm is twice this — the reduction `λ²=|dG|²/|dc_R|²` uses
the trace form consistently top and bottom, so the bookkeeping is exact. The alignment
invariant `⟨π_{1/2}M, π_{1/2}M#⟩ = −2ac + 2detM` is itself tuple-forced, confirming the fit is
a genuine identity.) Corroborated by a `44/44` rational on-locus battery and
an adversarial matched-tuple certificate hunt (none found). **D4 (the reading, fenced):** the
route's FIRST selection-shaped LOCAL law — on the locus, the second-order entropy response
trades against the canonical coordinate at a rate fixed by local field values alone, the v26
response field itself the multiplier's source. **STILL NOT Einstein, NOT a metric law; the
canonical geometry is FROZEN; `λ` is a Lagrange multiplier NOT a coupling; `λ₁,λ₂` remain
spectral data; no Newton constant; no `G=κT`; no dark-matter or geodesic-motion language;
signature OPEN.** "Equation of state" denotes multiplier-locality only (Jacobson's phrase as a
structural analogy). We are in the LIVE world: the scalar sector closes into a local balance,
making the spinor-moment object `p ↦ π_{1/2}^{(p)}(X)` the natural but OPTIONAL v28 object.
*(The Gate-5 v28 ledger is stated separately and carries no claims.)*
