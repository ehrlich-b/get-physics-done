# 89 — VERDICT: Per-Point Thermal Time — the Referent-Free Consistency Test (Claim 2 / J4)

**v29.0 Phase 89. A GENUINE FORK. Exact over Q/Q(t). Two independent code paths agree
(executor 12/12, eigenvalue-log + explicit-certificate path 5/5).**

**VERDICT = DEAD — per-point thermal time is irreducibly face-local.** The question: is there
ONE global Hermitian `H` with `traceless(C_pH) = β·traceless(K_face(p))` for all events `p`
(one global `β`, per-face `I`-shifts quotiented), `K_face = −log ρ_face` — an emergent global
state-time? **T1 (the reduction):** ε⁰ trivial, ε¹ always coherent (`H⁽¹⁾∝M`, trap #7), so the
verdict is at ε², where the traceless modular field is `K_face⁽²⁾(p) = −(9/2)·⟨M,p⟩·
traceless(C_pM)`; since compressions of a fixed `H` are level ≤1 in `p`, **a global `H` exists
⟺ the level-2 part of `K⁽²⁾` vanishes**. **T2 (traps fenced):** #5 direction-only is vacuous
(2×2 parallelism — never used as evidence), #6 eigenframe/single-rotation faces co-diagonalize
(excluded; verdict faces fully generic, the anchor `v=(1,2,2)/3`), #7 ε¹ is structurally LIVE
(the verdict is at ε²). **T3 (the verdict):** the global-`H` solvability `traceless(C_pH) =
⟨M,p⟩·traceless(C_pM)` is `EmptySet` from `E_11` + any one family, in BOTH the u-complex sector
and full OP² (`E_11`-alone IS solvable — genuine over-determination, not a bug; the `I`-shift
gauge quotiented — not a gauge artifact; an explicit constructive certificate exhibits the
un-killable residual). So the level-2 part is NONZERO ⟹ **DEAD**. **The payload:** the level-2
obstruction is the **clock-twist / modular-anomaly field**, a tangent-valued `R_M`-cousin
(connection-shaped — the parallel-transport-of-clocks twist, matter-pinned). **T4
(corroboration):** the finite-ε cross-check at `X=diag(1,2,3)/6` AGREES in sign (the rate ratio
varies across faces) — no design hole. **Scope (binding):** DEAD is a scope theorem — time is
face-relative; J4 stays IMPORTED (`{J5}` not promoted to `{J4,J5}`); Block A consumes the twist
field as connection data. Thermal time = Connes–Rovelli state flow (no thermodynamic postulate
in any verdict path); no proper-time/metric/Einstein/dark-matter language; the lapse/twist are
design inputs, not physics. *(The Gate-5 consumption ledger is stated separately, no claims.)*
