# 87 — GATE 1 SUMMARY: controls (known answers; ZERO evidential weight)

**v27.0 Phase 87. Exact over Q/Q(t). ALL PASS.** Proves the machinery and the verdict
logic behave; carries no evidence.

- **verdict() self-test (non-hardwired):** `(fit-exact-through-C, no certificate) → LIVE`;
  `(certificate found) → DEAD`; `(fit not exact) → OPEN-per-tier`. The verdict is derived
  from the elimination, not a literal.
- **(i) diagonal `M` ⇒ `dG(E_11) = 0`** — the eigenframe-critical stratum (excluded from
  fork evidence by D1, as designed).
- **(ii) `M = 0`** ⇒ `dG = 0`, tuple `= 0` (trivial).
- **(iii) `M_{e₁}` vs `M₀`:** SAME tuple `(1,0,4,1)`, SAME `|dG|² = ½`, DIFFERENT direction
  (`F_12(e₁)` vs `F_12(1)`) — a Spin(9) pair, NOT a certificate (the magnitude is
  tuple-fixed; only the direction rotates, the LIVE mechanism).
- **(v) anchor `λ` by direct ratio (no solver):** `2Q_{M₀} = 1` ⇒ `4c_R(1−c_R)λ² = 1`,
  `λ² = 1/(4c_R(1−c_R))` — a function of `c_R` alone.

**Gate 1: ALL PASS.**
