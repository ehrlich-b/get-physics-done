# Phase 90 (v30.0) — VERDICT

## VERDICT = DEAD (the form-selection fork closes negative; constructive)

**The clock connection is gauge-flat at form level: the matter-pinned clock-twist is an
EXACT 1-form, a_X⁽²⁾ = dχ, so the background-subtracted curvature F⁽²⁾ ≡ 0 — matter does
NOT force form-level curvature on the v22/v28 gluing U(1).** Triple-checked (executor
3×3-complex rep + independent octonion-engine verifier 6/6 + gpd-verifier), exact over Q/Q(t).

A GENUINE FORK resolved DEAD by an **unconditional exactness identity** (a constructive
certificate, not a failure-to-find): the clock-drift 1-form built from the v29 clock-twist
field 𝒦 = −(9/2)⟨M,p⟩·traceless(C_pM), paired with the canonically-transported C_u-phase
reference D, is the exterior derivative of the scalar potential

> **χ(p) = ⟨𝒦, D_p⟩ = −(9/2)⟨M,p⟩·⟨M, D_p⟩**  (the clock's C_u-phase content),

so F⁽²⁾ = d(a_X⁽²⁾) = d(dχ) = 0 identically.

## Why it is substantive (not vacuous), in order

1. **The cast is the v28/v29 object, certified.** D|_face = the v28 slice-rotation phase
   element (±i:1 pair, traceless, in-face); ∇ = the canonical F₄-invariant face transport
   (λ₁(cut)=12 regression); the ε² clock field = the v29 twist 𝒦 (re-derived two ways).

2. **The verdict lives at ε² and the ε²-ring is rational** (no logs in the verdict path; logs
   only at finite ε, unused). ε⁰ vacuum a≡0; ε¹ integrable (global H⁽¹⁾~M) — both forced
   controls, zero evidence.

3. **∇D = 0** (the canonical phase reference is covariantly constant: ∂D ∈ V_{1/2}, killed by
   C_p) AND **⟨𝒦, ∂D⟩ = 0** (Peirce orthogonality ⟨V₀,V_{1/2}⟩=0) ⇒ **a_X⁽²⁾ = dχ exact.**
   Verified in the 3×3-complex rep AND independently in the octonion engine along all four cut
   families, plus the Stokes loop route ∮a = 0.

4. **trap #13 is honored and genuine.** The canonical transport's own holonomy is nonzero
   (the recorded v22 slot-82 slice holonomy h(φ≠0) ≠ I, h(0)=I — the Fubini–Study background).
   The background-subtraction has teeth: ⟨R·Y,D⟩ ≠ 0 for a generic face field Y, while for the
   matter clock the subtracted curvature is exactly 0. The spectrum-matched diagonal reference
   (v24 direction-blind) gives zero subtracted holonomy (co-diagonalization stratum).

## The payload

Matter pins the clock's **C_u-phase potential** χ over the event-space (the integral of the
class + rate + twist data the route possesses) but adds **no form-level curvature**. The v28
**class-only ceiling stands**: the gluing U(1) is unforced at form level for ALL the matter
data the route has. This is the form-selection program's third boundary stone of its kind.

## Scope (anti-overclaim, binding — verbatim)

- **DEAD is constructive:** the potential χ = ⟨𝒦,D⟩ is exhibited explicitly; this is not a
  failure-to-find.
- **Guard 5:** DEAD does NOT follow from v29's no-global-H. Operator-valued modular
  synchronization being over-determined and a 1-form being non-exact are INDEPENDENT; a 1-form
  can be exact while synchronization fails. v29 is cited as motivation, never as evidence.
- **Language fence:** Maxwell-SHAPED only (had it been LIVE — a U(1) over the candidate base,
  no physical-EM identification). No Einstein / metric-law / Newton-constant / G=κT /
  dark-matter / geodesic-motion language. The v18/v20 MM-connection corpse stays buried (this
  is the BASE's own gluing, not a fiber-built spacetime connection). The frozen canonical
  geometry is USED, not derived.

## Through-line

v24 field → v25 closed form + forced operator (λ₁) → v26 source + MaxEnt → v27 local balance
law (LIVE) → v28 topological skeleton (χ=3, gluing-U(1) class) → v29 global-time question
closed (face-local; the clock-twist) → **v30 the form-selection fork closes: the clock-twist
is an exact 1-form; the gluing U(1) stays unforced at form level (the v28 ceiling).**

## Deliverables

`code/clock_connection.py` (executor) + `code/clock_connection_verify.py` (independent
octonion-engine path, 6/6) + `code/clock_connection_indep_check.py` (gpd-verifier);
`derivations/90-clock-connection-RESEARCH.md`, `90-GATE-{0..5}-SUMMARY.md`,
`90-GATE-{2,3}-VERIFICATION.md`, `90-VERDICT.md`.

## NEXT

HOLD the v30.0 milestone bookkeeping for ratification (mirroring v25–v29). v31 ledger
(Gate 5, priced only): Block B Lichnerowicz/tensor probe at λ₁=12 (the Einstein
make-or-break, blog-side); whether anything beyond class+rate+twist could force form.
