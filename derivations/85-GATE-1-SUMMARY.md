# 85 — GATE 1 SUMMARY: controls (known answers; ZERO evidential weight)

**v25.0 Phase 85. Exact over Q / Q(t). ALL PASS.** Carries no evidence; proves the
test discriminates.

- **Known-LIVE control:** `c11 = φ_{E_11}` passes the Helmholtz test with `λ₁` *exactly*
  — `λ₁(OP²) = 48` (16-frame) and `λ₁(CP² cut) = 12` (4-frame), each as the **full
  h_3(O)-vector identity** `ΔP = −λ₁(E_11 − I/3)` (not just the `E_11` component).

- **Known-MIXED control (discriminating power):** `c11² = cos⁴θ` has frequency 4 — it
  **FAILS** the first-order (frequency ≤ 2) test, while `c11` passes. Without this the
  fingerprint gate would be decoration; it can say no.

- **Trivial control:** `X = I/3` ⇒ `m ≡ 2/3`, `q ≡ 1/9` constant along every family,
  `Δ = 0`, the field equation holds as `0 = 0` (pre-registered tautology, carried for
  the record).

**Gate 1: ALL PASS** — machinery behaves on all three known-answer controls.
