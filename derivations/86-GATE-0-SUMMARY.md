# 86 — GATE 0 SUMMARY: machinery (fail-fast)

**v26.0 Phase 86. Exact over Q[ε]/Q(t). ALL PASS.** Driver `code/variety_sourced_field_equation.py`.

- **v25 frames reloaded (regression):** `λ₁(OP²) = 48`, `λ₁(cut) = 12`; `ΔP_full/cut ==
  −λ₁(E_11 − I/3)` (all 27 components) — the v25 machinery is intact.
- **Δ-of-product assembler:** the second-derivative assembler on a PRODUCT of moments
  matches the symbolic product rule `d²/dt²(fg) = f''g + 2f'g' + g''f` exactly along a
  family (the new ingredient for `Δ⟨M,·⟩²`).
- **ε-truncation ring:** `X = I/3 + εM` ⇒ `deg_ε(m) = 1`, `deg_ε(q) = 2` (the sharp is
  quadratic) — order-counting exact (guard 1).

**Gate 0: ALL PASS** — the v25 frame machinery, the product-Laplacian, and the ε-ring
are certified before any claim.
