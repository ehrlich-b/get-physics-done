# 88 — GATE 0 SUMMARY: the v22 dictionary (fail-fast)

**v28.0 Phase 88. Exact over Q. ALL PASS — the dictionary holds.** Driver
`code/variety_spinor_moment.py`. The ONE genuinely-open identification of the run.

On the cut, `J_{1/2}(E_11) ∩ h_3(C_u)` is two `C_u`-lines (the `(0,1)`, `(0,2)` entries);
`e_7`-multiplication is the `C_u` complex structure `J` (one compact SO(2)). The v22.0
result (slot 82, `kkt_gluing_holonomy.py`): the inter-observer gluing holonomy is an
UNFORCED U(1) — joint pair-stabilizer Spin(8), slice action one SO(2) = the `C_u` phase
(`KK._Cu_complex_structure_slice` on the V_0 slice).

**The dictionary:** constructing the torus generator `D(·) = [δU, ·]`, `δU = diag(0,
−e_7/2, e_7/2)` (the f₄ element generating the slot-82 circle), `D` acts on the V_0 slice
(`x1`) as **`+1·J_Cu`** (the v22 slice SO(2)) and on the `J_{1/2}` tangent (`x2, x3`) as
**`−½·J_Cu`** — the SAME circle, NONZERO on the tangent. So the v22-unforced gluing U(1) IS
the `C_u` phase, and v28's object is its action on the variety's own tangent (tangent weight
`−½` vs slice `+1`, recorded). Verified independently via a diagonal-unitary-conjugation
torus generator.

**Gate 0: ALL PASS** — the framing is correct; proceed.
