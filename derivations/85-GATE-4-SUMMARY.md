# 85 — GATE 4 SUMMARY: the v26 design ledger

**v25.0 Phase 85. EXPLORATORY, NON-BLOCKING — NO law / balance / coupling claims.**
The v25.0 verdict (C1/C2/C3 PASS) is INDEPENDENT of everything here. This is design
input for v26 (the balance proper), produced now that both the matter side `(m,q;
sources X, X#)` and the canonical side (`c11`-type reference moments) are certified to
be the **same type** of object: level-≤1 moment fields with the same `λ₁`.

## (b) Per-class doublet table (m, q at E_11; means; level)

| state | `m(E_11)` | `q(E_11)` | `m̄ = (2/3)Tr X` | `q̄ = σ₂/3` |
|---|---|---|---|---|
| `I/3` (vacuum) | 2/3 | 1/9 | 2/3 | 1/9 |
| `diag(7,5,3)` | 8 | 15 | 10 | 71/3 |
| v18-matter (V_{1/2}) | 5 | 6 | 6 | 4559819/529200 |
| GENERIC | 8 | 18301/1225 | 10 | 49873627/2116800 |

Both sources `(X, X#)` are level-≤1 moment fields with the **same** `λ₁`. **No level
≥ 2 contamination is possible in this landscape** — the CGM/Speranza exposure is
structurally absent (this is the finite face-purity landscape, not a QFT entanglement
entropy; scope fence: the CGM question returns only when/if a base QFT is built).

## (a) Candidate J5/Jacobson-type balances on the variety (DESIGN INPUT ONLY)

1. **Linear moment relation** `α·m + β·q = (c11-reference moment)`. Both are level-1;
   a balance fixes `(α,β)`. *Vacuity risk:* trivially solvable unless a second
   constraint (e.g. `q` from `X#` vs `m` from `X`) is imposed — the two-moment doublet
   gives exactly that second handle.
2. **Constrained extremization** of `S(face) = S(m,q)` at fixed canonical moment
   `⟨Y_0, p⟩`. Multiplier nonzero iff `dS/dp` is not parallel to the canonical
   gradient; v24 Gate-3 distinctness (the landscape's eigenframe-tracking critical
   points vs the direction-blind canonical geometry) says it can be — **the v26 test
   object.**
3. **All-balls quantifier analog:** demand the balance hold for ALL faces (all `p`);
   the C1 covariance closure collapses this to a single-point condition — low vacuity
   risk, but possibly content-free; flag for v26 triage.

## (c) Cut-vs-mother comparison (the bottleneck's first dynamical fingerprint)

`λ₁`-ratio `48/12 = 4` = dim ratio. The OP² 16-frame carries 12 extra non-`C_u`
directions `{12..17, 20..25}` that the CP² 4-frame does not: matter living in the
off-u octonion directions is **invisible to the cut** but sourced on the mother. This
4-vs-16 gap is the first place the OP²→CP² bottleneck could leave a dynamical
fingerprint — the comparison v26 should exploit.

**Filed as v26 design input. No claims attached.**
