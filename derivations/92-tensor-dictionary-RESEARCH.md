# Phase 92 (v32.0-candidate) — The Tensor Dictionary: Does the TT Sector Close in Canonical Form?

**Slot 92 / the v31 sequel (EXTRACTION-leaning, v25/v26 class, with ONE genuine factual
sub-fork at Gate 2 and one dictionary-closure question at Gate 3/4).
Source: `paper6-lichnerowicz-response-law-prompt.md` (RATIFIED-BY-STANDING-DIRECTIVE).**
Exact over Q / Q(t); octonion engine + 3×3-complex cut rep (guard-locked); u = e₇; cut = CP² = h₃(C_u).

> **STATUS OF THIS FILE.** Sections 1–6 are the orchestrator-staged grounding (the certified
> machinery to reuse, the frozen Δ_L convention, the Schur/multiplicity-one argument, the
> cliff-free extraction method, the verdict objects + LIVE/PARTIAL criteria, the Boucetta/Besse
> literature the no-web executor cannot fetch). Sections 7–9 (results, the norm identity + ε, the
> v33 ledger) are filled by the executor/verifier from the exact computation. **NOTHING here
> prejudges the verdict.** §6 fixes only the *objects*, the *method*, and the *frozen criteria*.
> Any verdict object not frozen at Gate 0 is a new run (STOP rule 3).

---

## 0. What v31 delivered (ratified) and what v32 extracts

**v31 (ratified, LIVE — the program's first gravity-side positive):** on the cut CP² = h₃(C_u)
(Fubini–Study, Kähler–Einstein, Ric = 6g, Λ = λ₁/2 = 6, λ₁ = 12, λ₂ = 32), the certified matter
stress bilinear

> **B3 = s_M⊗s_M = dφ_M⊗dφ_M** (= the pinned π_{1/2}M tangent stress **B6**, `s_M = π_{1/2}^{(p)}M = dφ_M`, v28)

has a **nonzero transverse-traceless residue**, dim 1 per matter direction, in the **λ = 12
(1,1)-Hermitian Boucetta dim-8 multiplet**, with the count FORCED by **Sym²(8) = 27 ⊕ 8 ⊕ 1 ⊃ 8
(multiplicity one)**. Established by the control-gated obstruction (`york_solve(B3)` inconsistent
where the `B1 = ∇∇φ_Y` Hessian control IS solved) + the York existence theorem + rep theory; three
independent code paths agreed. The Nordström-class two-scalar ceiling was thereby broken.

**The v31→v32 calibration (binding, from the 2026-06-12 steelman — embedded so the run cannot
re-inflate):** v31 proved **EXISTENCE** of the TT residue, which was the *less*-informative branch
(generic-position DOF counting already predicted a nonzero residue). **v32's bar for LIVE is
strictly higher: a FORCED closed form** — the geometry telling matter exactly how much TT it gets,
in the certified dictionary, with **no free function and no fitted constant**. Anything short of
that is more kinematics and must be reported as such. The phrase "first selection-shaped tensor
statement" is licensed ONLY if Gate 4's LIVE criteria (§6) are met verbatim. Paper 5 remains the
only result in the more-than-nothing column.

**What v32 extracts (the four verdict objects, frozen at Gate 0):**
- **(i)** the TT closed form `TT(B3) = Σᵢ cᵢ(M)·tᵢ` over the explicit dim-8 TT basis {tᵢ}, with the
  NAMED HYPOTHESIS (verify, do not assume) that `c(M) ∝ N(M)` = the traceless adjoint square of M;
- **(ii)** the norm identity `‖TT(B3)‖²` as a closed form in the frozen v27 tuple with FORCED constants;
- **(iii)** the threshold number `ε = λ_L − 2Λ = λ_L − 12` (the genuine factual sub-fork);
- **(iv)** the full York dictionary `B3 = TT + δ*ω + f·g` with `ω, f` in certified closed form.

---

## 1. The question, as exact linear algebra

On the compact cut (CP², FS, KE), symmetric 2-tensor fields decompose **L²-orthogonally**
(Berger–Ebin 1969 / York):

> **h = h_TT ⊕ δ*ω ⊕ f·g**,  TT = (`δh = 0` AND `tr_g h = 0`), `(δ*ω)_{ab} = ½(∇_aω_b + ∇_bω_a)`, conformal `f·g`.

v31 settled `h_TT ≠ 0` for `h = B3`. v32 asks whether the **map `M ↦ h_TT`** and its **norm** close
in canonical form:

- **Multiplicity one ⇒ the map is rep-theoretically PINNED up to one scalar.** The λ=12 (1,1)
  TT multiplet IS the su(3) adjoint **8**. `B3 = dφ_M⊗dφ_M` is the symmetric square of the
  adjoint-valued moment gradient, so `TT(B3)` is the image of the equivariant projection
  `Sym²(8) → 8`. Since **Sym²(8) ⊃ 8 exactly once**, that projection is UNIQUE up to scale — the
  symmetric invariant **d-symbol** of su(3). Hence `c(M)` must point along the d-symbol square
  `N(M)` (object (i)'s hypothesis), and `‖TT(B3)‖²` must be the unique invariant of the right
  degree (object (ii)). v32 *verifies* this pinning computationally and *extracts the constants*.

- **The norm is an M-invariant of degree 4.** `‖TT(B3)‖²` is L²-integrated over the homogeneous
  CP², hence depends on **M only** through SU(3)-invariants. `B3` is degree-2 in M, so `‖TT(B3)‖²`
  is **degree-4** in M. The polynomial SU(3)-invariants of a traceless 3×3 Hermitian M are
  generated by `p₂ = TrM²` (deg 2) and `p₃ = detM` (deg 3, `= ⅓TrM³`). The ONLY degree-4 invariant
  is **`(TrM²)²`** — because Cayley–Hamilton for traceless 3×3 (`M³ = ½TrM²·M + detM·I`) gives
  `TrM⁴ = ½(TrM²)²`, and `detM·TrM` (deg 4 from p₃) vanishes (`TrM = 0`). So the *strong* LIVE
  outcome is forced in shape: `‖TT(B3)‖² = κ·(TrM²)²` for a single rational κ, with **`detM`
  absent** (echoing v27's cubic-blindness). **This is a prediction of the method, NOT an input —
  the executor fits over the full invariant basis {(TrM²)², detM·(deg-1, none), …} and reports
  whether the closure is exactly `κ(TrM²)²` (LIVE) or demands an invariant outside the frozen
  tuple (PARTIAL).** detM is KEPT in the ansatz (finding either way).

Over the finite polynomial sectors the split is **exact rational linear algebra**. No elliptic
inversion, no numerics. `p = vv†` (rank-1 projector); functions and tensors are low-degree
rationals in `(z, z̄)` over a common ρ-power, ρ = 1 + |z₁|² + |z₂|².

---

## 2. Gate-0 threshold facts (verify in-rep; do NOT import blind)

**(i) The cut metric is Einstein, Λ = λ₁/2 = 6.** Boucetta (arXiv:0712.2830) Lemma 2.1: P^n(C) with
the FS metric (quotient of unit S^{2n+1}) has Ric = 2(n+1) g; **n=2 ⇒ Ric = 6g**. The codebase
normalization matches: `variety_moment_doublet` certifies λ₁(cut)=12, λ₂(cut)=32 = Boucetta's
scalar spectrum `4k(n+k)|_{n=2}` (Ikeda–Taniguchi). So the Boucetta dimension tables (§4) apply
verbatim. (`tensor_probe.gate0_geometry` already verifies `Ric = 6·g_phys` and the λ₁=12 bridge.)

**(ii) The λ=12 (1,1)-Hermitian TT multiplet IS the su(3) adjoint 8.** Boucetta Table VIII at n=2:
the ONLY Hermitian-(1,1) TT mode on all of CP² is the single **dim-8** multiplet at λ=12 (every
higher (1,1) mode is conformal or longitudinal). Its carrier space `T^{1,1}_{0,0}` = the constant
trace-free Hermitian 3×3 matrices = **𝔰𝔲(3) adjoint**. This is the multiplet {tᵢ}.

**(iii) The moment fields are the λ₁=12 eigenfunctions.** `φ_M(p) = ⟨M,p⟩` obeys
`Δ(φ_M − φ̄_M) = −12(φ_M − φ̄_M)` (cut; recorded, regression at Gate 1e).

**(iv) Matsushima / the Hessian-is-gauge identity (the B1 control).** On KE, for any scalar φ:
`∇∇φ = ½ L_{grad φ} g = δ*(dφ)` (since `L_X g = 2δ*(X♭)`). So **every Hessian is pure gauge**
(TT-residue ≡ 0 constructively), `(ω, f) = (dφ, ¼Δφ)`. This is why v31's B1,B2,B4,B5 are dead by
the universal identity, and it is the B1 control that gates every degree (Trap #14).

**(v) The d-symbol square N(M) (object (i)).** For a trace-free Hermitian 3×3 M, the **adjoint
square** is `N(M) := traceless(M∘M) = M² − ⅓Tr(M²)·I` (∘ = Jordan product = ½(AB+BA); `M∘M = M²`).
In the Gell-Mann basis `M = Σ_a M_a λ_a`, `N(M)_c ∝ d_{abc} M_a M_b` (the totally-symmetric su(3)
d-symbol). `N(M)` is a trace-free Hermitian matrix ⇒ an 8-vector in the adjoint = the natural
"direction" the multiplicity-one projection must produce. **Standing guard:** re-verify any
hand-derived d-symbol/Jordan-square against BOTH recorded product tables (octonion product-order
trap) — but here M is a 3×3-complex Hermitian matrix and `M∘M = M²` is plain matrix multiplication,
so the trap is mild; still cross-check `N(M)` two ways (matrix `M²−⅓TrM²I` vs the Gell-Mann d-symbol).

---

## 3. The frozen Δ_L convention + the Besse Einstein-deformation facts (Gate 1f / Gate 2, traps #16/#18)

**The frozen Lichnerowicz Laplacian (state at Gate 0, never vary):**

> **Δ_L h = ∇*∇h + Ric∘h + h∘Ric − 2R̊h**,  `(R̊h)_{ab} = R_{acbd} h^{cd}`,

with `∇*∇ = −g^{μν}∇_μ∇_ν` the **rough/connection Laplacian in the POSITIVE (geometer's)
convention** (so eigenvalues are ≥ 0 and comparable to `2Λ = 12`), built from the certified
connection (`christoffel_hol`, `_christoffel_antihol`), and `(Ric, R)` the certified curvature.
On the Einstein background `Ric = Λg = 6g`, so `Ric∘h + h∘Ric = 2Λh = 12h`, and

> **Δ_L h = ∇*∇h + 12h − 2R̊h**.

**Sign-convention pin (mandatory before any ε reading).** The engine's `laplacian` is the analyst
`+div grad = 2g^{ab̄}∂_a∂̄_b` (negative-semidefinite: `Δ(φ−φ̄) = −12(φ−φ̄)`). The rough Laplacian
in Δ_L is the **opposite sign**: `∇*∇φ = −Δ_{analyst}φ`, so a λ₁ scalar has `∇*∇φ = +12φ`. Build
`∇*∇` on tensors as `−g^{μν}∇_μ∇_ν` and CHECK it reproduces `+12` on a λ₁ scalar (Gate 1e
regression) and `+32` on a λ₂ scalar BEFORE trusting any tensor eigenvalue. A sign slip here
fakes ε — the Gate-1 controls exist precisely to catch it.

**The trap-#16 tension, to be resolved FACTUALLY at Gate 2 (NO prior registered):**
- Besse 12.28: infinitesimal Einstein deformations = `ker(Δ_L − 2Λ)|_TT`.
- Besse 12.98: **CP^n is Koiso-RIGID** — it has NO infinitesimal Einstein deformations.
- Boucetta's table puts the (1,1) TT multiplet AT eigenvalue 12 = 2Λ.

These three cannot all hold under one convention. EITHER (a) Boucetta's Δ_L normalization differs
from the Besse convention as realized by OUR certified connection (so the computed `λ_L ≠ 12`,
`ε ≠ 0` — the multiplet is non-marginal, `ε` is canonical response-stiffness data), OR (b) the
multiplet is formally Einstein-marginal under our convention (`ε = 0`) and Koiso-rigidity lives at
**second order** (obstruction-theoretic). **COMPUTE `ε = λ_L − 12` exactly over Q; do not
adjudicate from the literature.** Both outcomes are informative and both are consumed by the
lapse/00 assembly later. Whatever the discrepancy vs Boucetta's table, NAME it mechanically (which
term, which sign, which normalization) — never resolve it by picking the convenient convention
(trap #18). If `ε = 0`, fence any Fredholm/response reading as second-order (trap #19).

**Trap #17 (eigen-tautology).** `(Δ_L − λ_L)tᵢ ≡ 0` *within* the multiplet is KINEMATICS (Schur);
any "law" that reduces to it is vacuous. The verdict objects are the COEFFICIENTS (c(M), the norm
identity, ε) — never the eigen-identity. The Gate-1f Schur check is a CONTROL, not a result.

---

## 4. The Boucetta dimension anchor (Trap #15, condensed from 91 §4)

Boucetta, arXiv:0712.2830, Tables VI–VIII at n=2 (recovering Warner 1982, building on
Ikeda–Taniguchi 1978). Real symmetric 2-tensors on CP² split by the Kähler bigraduation into
J-invariant (Hermitian (1,1)) and J-anti-invariant ((2,0)⊕(0,2)).

**Hermitian (1,1) — Table VIII (n=2):** the **only** (1,1) TT mode on all of CP² is the single
**λ=12, dim-8** primitive (= su(3) adjoint); every higher (1,1) mode is conformal `4m(m+2)` or
longitudinal `4(m+2)(m+3)` / `4(m+2)(m+4)`.

**Anti-invariant — Tables VII/VI:** lowest TT at **λ=32, dim-27 (2,0)** ⊕ **dim-27 (0,2)** (real
54). v31's residue is purely (1,1) (the (2,0)+(0,2) blocks are jointly gauge). v32 stays in the
(1,1) sector unless the closed-form extraction says otherwise.

**The audit caveat carried from v31 (RECORDED, do NOT rely on the modular audit):** the modular
`dimension_audit` returns a SPURIOUS B1-control deficit on DENSE M (a sampling-rank artifact); the
exact `york_solve(B1)` is consistent on dense M at D=3. v31's deficit=1 rested on the sparse-s01
audit + the exact-Q rank + the rep-theory multiplicity-one — NOT the dense modular audit. v32 does
not need the audit at all (it extracts the residue explicitly, §5).

---

## 5. The cliff-free extraction methodology (V1, the load-bearing method)

**The cliff (v31's CPU-roaster), and how v32 routes around it.** `york_tt_residue` computes
`‖h_TT‖²` by a full-basis L² **Gram** projection — on the ρ-rational fields this stalls the
watchdog (v31's TEST-1). v32 NEVER builds that Gram. Instead:

**(A) Explicit TT-residue extraction by matched-monomial York solve (the deferred v31 certificate,
obtained cliff-free).** The York decomposition `B3 = r + δ*ω + f·g` is unique (up to Killing in ω),
with `r` the TT part: `tr_g r = 0` AND `δr = 0`. So **find `(ω, f)` such that
`r := B3 − δ*ω − f·g` satisfies `tr_g r = 0` and `δ(r) = 0`, matched monomial-by-monomial over a
common ρ-power** (a linear system over Q in the `(ω, f)` coefficients — the SAME complete
gauge+conformal ansatz as `york_solve`: `ω` from `{φ_A dφ_B (holo), φ_A d̄φ_B (antiholo)}` over the
8 su(3) generators + identity, `f` from `{φ_A φ_B}`). The leftover `r = B3 − δ*ω − f·g` is then the
**explicit TT tensor**, exhibited in closed form — this is object (i)'s `Σcᵢtᵢ` AND object (iv)'s
`(ω, f)` simultaneously. Operators used (all `together`-based, cliff-safe): `delta_star`,
`conformal_block`, `divergence`, `trace_g`. NEVER `cancel` on raw ρ-fields.

**(B) The dim-8 basis {tᵢ} (Gate 0).** Run (A) for the 8 Gell-Mann matter directions `M = λ_a`
(`a = 1..8`); the 8 residues `r(λ_a)` are explicit TT tensors. Verify each is traceless (`trace_g`)
and divergence-free (`divergence`) symbolically or at off-slice rational points, and verify the
8×8 Gram (`l2_tensor`) has rank 8 ⇒ they span the multiplet. (If a cheaper single-element seed is
preferred: extract `t₀ = r(s01)` for the sparse direction, then generate the others by su(3)
equivariance and re-verify — but the 8-direction route is the most robust and needs no isometry
pushforward.) FREEZE {tᵢ}.

**(C) The norm, cliff-free.** For the extracted explicit `r`, `‖r‖² = l2_tensor(r, r)` is a SINGLE
exact L² integral (phase-averaged, matched-monomial, one radial integral — NOT a Gram). Compute
`‖TT(B3)‖²` for sparse s01, both dense matters, and **symbolic 8-parameter M**, then fit over the
SU(3)-invariant basis (§1: `(TrM²)²` and `detM`-containing degree-4 terms — none exist for traceless
3×3, so the basis is effectively `{(TrM²)²}`; keep a `detM`-probe term to confirm absence).

**(D) Δ_L as a LOCAL operator on the explicit `r` / `tᵢ`.** Build `Δ_L` (§3) as covariant
derivatives + curvature contractions of the explicit tensor (matched-monomial). Gate 1f: compute
`Δ_L tᵢ` for ≥ 2 independent basis elements and verify `Δ_L tᵢ = λ_L tᵢ` with the SAME `λ_L`
(Schur — forced by equivariance + multiplicity-one; deviation = engine bug, STOP). Gate 2:
`ε = λ_L − 12`.

**Control-gating (the v31 close-out lesson):** cite results only at degrees where the B1 Hessian
control passes — **D = 3 for dense/symbolic M** (at D = 2 the control is degree-insufficient). The
B1 control (`∇∇φ_Y` solved exactly with `(dφ_Y, ¼Δφ_Y)`, reconstructed by the SAME solver) must
pass BEFORE any B3 line is trusted (Trap #14).

**Anti-stall protocol (binding for the executor — see [[feedback_executor_watchdog_stall_long_symbolic]]):**
per-object separate `python3 -u` runs; progress prints ≤ 90 s apart; **commit after each gate**;
matched-monomial on numerators, never `cancel`/`traceless_part`/`divergence` on raw ρ-denominator
fields before reduction; symbolic-M only at D = 3 with the B1 control gating; no full-basis Gram.

---

## 6. The verdict objects, the frozen criteria, the traps

**The four objects (frozen at Gate 0; §0 (i)–(iv)). The verdict CENTER is (ii), with (i)/(iv) the
dictionary and (iii) the genuine sub-fork.**

**Gate 0 — machinery + freeze.** Reuse `tensor_probe.py` (FS chart, metric/inverse, Christoffels,
`cov_hessian`, `delta_star`, `conformal_block`, `divergence`, `trace_g`, `york_solve`, `l2_tensor`,
the certified fields) and `tensor_probe_indep_check.py` (independent geometry + `general_york_solve`).
Construct + freeze {tᵢ} (§5B); build Δ_L (§3); freeze objects (i)–(iv) and the Gate-4 criteria.

**Gate 1 — controls (zero evidential weight).** (a) `verdict()` non-hardwired self-test. (b) Vacuum
M=0: every object ≡ 0. (c) Δ_L on `f·g` (λ₁ eigenfunction f): known answer via the certified scalar
Δ — report the identity used. (d) Δ_L on the gauge tensor `δ*(dφ_Y)`: verify the Einstein-background
identity `Δ_L δ*ω = δ* Δ_H ω` IN-REP on the certified objects before using it; **if it fails the
convention bookkeeping is wrong — STOP** (do not reinterpret as physics). (e) Scalar regressions
λ₁=12, λ₂=32 through the tensor-machinery code path (pins the ∇*∇ sign). (f) **The Schur check
(mandatory):** Δ_L acts as a SCALAR on the dim-8 multiplet — ≥ 2 basis elements, same constant.

**Gate 2 — the threshold number (genuine sub-fork; both branches informative).** `λ_L` = the Schur
scalar (exact over Q); report `ε = λ_L − 12`. `ε = 0` ⇒ formally Einstein-marginal, Koiso-rigidity
recorded as second-order (trap #19 fence). `ε ≠ 0` ⇒ non-marginal, `ε` = canonical
response-stiffness. EITHER WAY: ONE exact rational, with any Boucetta-vs-Besse discrepancy explained
mechanically (trap #18).

**Gate 3 — the closed form (verdict center; fast matched-monomial, control-gated at D=3).**
- **(3a)** Extended solve `B3 = Σcᵢtᵢ + δ*ω + f·g` over `(cᵢ, ω, f)`: sparse s01, both dense, then
  SYMBOLIC 8-param M. Consistency with some `cᵢ ≠ 0` is FORCED by ratified v31 — **if inconsistent
  or all-zero `c` for generic M, that CONTRADICTS v31: STOP and report** (do not paper over, do not
  self-amend the v31 record — STOP rule 2).
- **(3b)** Direction hypothesis: verify `c(M) ∝ N(M)` (compute `N(M) = M² − ⅓TrM²·I` two ways,
  §2v; compare the 8-vectors exactly). PASS/FAIL — a FAIL is a FINDING (re-examine the
  (1,1)-projection step), not a failure.
- **(3c)** Norm identity: `‖TT(B3)‖²` as a polynomial in the frozen tuple — fit over the certified
  invariant basis with EXACT coefficients, residual ≡ 0 through full symbolic M (v27 standard: no
  locus constraint, identical constants in all sectors). `detM` kept in the ansatz (finding either
  way; §1 predicts absent).
- **(3d)** The `ω, f` closed forms: complete the dictionary; verify `B3 − Σcᵢtᵢ − δ*ω − f·g ≡ 0`
  symbolically.

**Gate 4 — verdict (FROZEN criteria, verbatim):**
- **LIVE ⟺ (3a) consistent for symbolic M AND (3c) closes with FORCED exact constants over the
  frozen tuple (residual ≡ 0, no fitted/free function).** Then and only then is the sentence "the
  tensor sector closes in the canonical dictionary — the program's first forced tensor-response
  coefficient" licensed, with the §5/fences verbatim.
- **PARTIAL (informative): (3a) closes but (3c) requires invariants OUTSIDE the frozen tuple.**
  Report WHICH new invariant the closure demands (the dictionary's first incompleteness names the
  next object). No LIVE language.
- **DEAD-equivalent here = contradiction with v31 (3a STOP) or a Schur/identity failure
  (machinery, not physics) — STOP rather than verdict.**

**Gate 5 — v33 ledger (priced only, NO claims, NO computation).** (a) lapse/00 assembly (consumes
ε, c(M), the v26/v27 scalar sector; needs the 4d-slice embedding — price it). (b) OP² lift (Spin(9),
no Kähler split, the (1,1) story does not transfer — price, do not assume). (c) the Fredholm/response
READING of ε (`(Δ_L − 2Λ)h = source`) — a Block-C-shaped IMPORT; NAME it, FENCE it, do NOT run it
(trap #19).

**The traps (pre-registered, instances 17–19 of the designed-in-verdict-bug class):** #17
eigen-tautology (objects are the COEFFICIENTS, never the Schur identity); #18 convention skew (build
from the certified connection, literature numbers are cross-checks, name the discrepancy mechanism);
#19 the Einstein-import smuggle (NO sourced response EQUATION `(Δ_L−2Λ)h = κT` — that is Block C
where five routes died; "response" appears only in Gate-5 pricing, fenced). Standing guards: octonion
product-order trap (cross-check `N(M)`); generic matter only for verdicts (diagonal strata zero
weight); control-gating (D=3); exact over Q (no floats); the v31 fences verbatim.

**Scope fence (binding, verbatim in every summary):** whatever Gate 4 returns, this is the
deformation-complex DICTIONARY of a FROZEN imported geometry. No dynamical metric. No selection law.
No κ. The constructed-vs-extremized gap, the 16-vs-6 rank wall, the Riemannian signature, and the
base/format clamp are untouched by construction. LIVE means "the tensor sector's source data closes
in canonical form" — a DICTIONARY fact, not a dynamics fact. No Einstein-equation / Newton-constant /
G=κT / dark-matter / geodesic language; frozen FS geometry USED not derived; the v18/v20 MM corpse
stays buried; OP² priced only. This run does NOT retract v17–v21 (Block-C statements; v32 is the
strictly weaker upstream dictionary).

---

## 7. Results — the closed form, the direction, the dictionary  *(executor)*

**Verdict = LIVE** (the tensor sector's source data closes in canonical form). All exact over Q;
driver `code/lichnerowicz_response.py` (Gate 0 6/6, Gate 1 6/6, the norm/direction/ε computations).

**The cliff-free extraction (V1) — the deferred v31 certificate, obtained.** The unique York TT
residue `r` of `B3 = dφ_M⊗dφ_M` (`tr_g r = 0` AND `δr = 0`) is EXTRACTED EXPLICITLY by a
matched-monomial York solve: the gauge+conformal basis `(tr_g, δ)` images are computed once as
monomial-coefficient dicts over a common ρ⁷ (cached; conformal blocks via the analytic
`tr_g(f·g)=4f`, `δ(f·g)_b=−∂_b f`), and the target is matched by an EXACT CRT modular solve (40-prime
Gaussian elimination + rational reconstruction). NO full-basis Gram. **Point-evaluation is
ill-conditioned** (its gauge coefficients explode to ≥186 bits and fail reconstruction; the
matched-monomial route keeps small coefficients). `r` is exhibited nonzero with `tr_g r = 0`, `δr = 0`
for sparse (s01), all 8 single generators, and dense generic matter — confirming v31's EXISTENCE
cliff-free.

**The dim-8 multiplet (object (i) basis {t_a}).** The verdict object is the J-invariant
(1,1)-Hermitian sector (v31: the (2,0)+(0,2) blocks are individually pure gauge); `t_a = r^{(1,1)}`.
The single-generator residues span the **d-symbol image of single generators (rank 6)** — a genuine
su(3) fact (for a single off-diagonal generator λ_a, the adjoint square N(λ_a) collapses into the
Cartan). The **full dim-8 multiplet** is reached by GENERIC matter: the quadratic map M→N(M) is
surjective onto the dim-8 adjoint, and `{N(M)}` for generic dense M has **rank 8** (verified). The
multiplet is dim-8 (Boucetta Table VIII, v31); single generators probe a rank-6 slice (sufficient for
the Schur scalar, which is a single eigenvalue because the multiplet is irreducible).

**The direction c(M) ∝ N(M) — FAILS (a FINDING).** The named hypothesis (object (i)) was that the
TT direction is the d-symbol square `N(M) = M² − ⅓Tr(M²)I`. **It is FALSE.** The clean test:
`N(s01) = N(d1) = diag(1,1,0) − ⅔I` exactly, yet `r^{(1,1)}(s01) ≠ r^{(1,1)}(d1)`. So the TT tensor
direction carries MORE M-information than N(M) — the full `dφ_M⊗dφ_M` gradient-bilinear structure
(φ_{s01} and φ_{d1} are different λ₁ eigenfunctions), not merely the adjoint square. This refines
object (i): the closed form `TT(B3) = Σ_a c_a t_a` EXISTS but `c(M)` is richer than the d-symbol
square — the dictionary's first named nuance. (It does NOT touch the LIVE criterion, which is the
norm.)

**The York dictionary (object (iv)) — closes EXACT.** `B3 = r + δ*ω + f·g` exact over Q
(`B3 − r − δ*ω − f·g ≡ 0` block-for-block), with `(ω, f)` assembled in certified closed form from the
gauge+conformal potentials; `r` is the explicit TT tensor (tr_g r = 0, δr = 0).

| object | result |
|---|---|
| (i) `TT(B3) = Σ_a c_a t_a` | residue exhibited (tr=0,δ=0,≠0); basis = (1,1)-Hermitian multiplet (single-gen rank-6, generic rank-8); **c(M) ∝ N(M) FAILS** (richer than the d-symbol square) |
| (ii) `‖TT(B3)‖²` | **= (1/54)(TrM²)²** — FORCED κ=1/54, **detM absent** (the verdict center, §8) |
| (iii) `ε` | **= λ_L − 2Λ = 32 − 12 = 20** ≠ 0, non-marginal (§8) |
| (iv) `B3 = TT + δ*ω + f·g` | closes EXACT over Q; (ω,f) in closed form |

`verdict(closes_3a=True, norm_forced=True, norm_needs_outside=None, schur_ok=True,
contradicts_v31=False) = ('LIVE', ...)` — LIVE because (3a) the residue closes for all matter AND (3c)
the norm closes with a FORCED exact constant (detM absent, no free function). The direction-N(M)
failure (3b) is a documented finding, NOT a PARTIAL trigger.

## 8. The norm identity and the threshold number ε  *(executor)*

**The norm identity (object (ii), the verdict center):**

> **‖TT(B3)‖² = (1/54)·(TrM²)²**   — single FORCED rational κ = 1/54, **detM ABSENT**.

Confirmed exact over Q across matters with VARIED detM:

| matter M | ‖TT(B3)‖² | (TrM²)² | detM | κ |
|---|---|---|---|---|
| s01 | 2/27 | 4 | 0 | **1/54** |
| d1 | 2/27 | 4 | 0 | **1/54** |
| d2 | 2/3 | 36 | **−2** | **1/54** |
| a01 | 2/27 | 4 | 0 | **1/54** |
| dense generic | 128/27 | 256 | **−7** | **1/54** |

κ = 1/54 is the SAME for matters with detM ≠ 0 (d2: −2; dense: −7), so **detM is ABSENT** — the norm
does not depend on the cubic invariant (v27 cubic-blindness). This is forced by SU(3)-invariance +
Cayley–Hamilton (`TrM⁴ = ½(TrM²)²`, `detM·TrM = 0` for traceless 3×3 ⇒ the only degree-4 invariant is
(TrM²)²). **No fitted constant, no free function — the FORCED closed form that meets the v32 LIVE bar.**
Method: `‖r^{(1,1)}‖² = l2_tensor((0,r^{(1,1)},0), same)` — ONE exact L² integral, NOT a Gram.

**The threshold number ε (object (iii), the genuine sub-fork):**

> **ε = λ_L − 2Λ = 32 − 12 = 20**   (λ_L = 32 the Δ_L^{(1,1)} Schur scalar; ε ≠ 0).

`Δ_L^{(1,1)} t_a = 32 t_a` for d1, d2, s01 (the Schur scalar, same constant — the multiplet is
irreducible). So **ε = 20 ≠ 0: the λ=12 (1,1) multiplet is NON-marginal** under our certified
convention.

**The trap-#16 tension, resolved factually and mechanically (NO convention picked).** The three
statements — Besse 12.28 (`ker(Δ_L − 2Λ)|_TT` = infinitesimal Einstein deformations), Besse 12.98
(CP^n Koiso-RIGID), Boucetta's (1,1) TT multiplet "at" λ=12 = 2Λ — are reconciled by recognizing that
the LICHNEROWICZ eigenvalue `λ_L = 32` is the SCALAR Laplacian eigenvalue `λ₁ = 12` of the carrier
PLUS the Weitzenböck shift; they are NOT the same operator. Since `λ_L = 32 ≠ 12 = 2Λ`,
`ker(Δ_L − 2Λ)|_TT = ∅` here — fully CONSISTENT with Koiso rigidity (CP² has no infinitesimal Einstein
deformations exactly because the (1,1) multiplet sits a finite gap ε = 20 above the marginal
threshold). Boucetta's "λ=12" labels the scalar carrier's threshold, not the Lichnerowicz eigenvalue.
The Δ_L convention was pinned independently (the rough-Laplacian sign pin +12 on λ₁ / +32 on λ₂; the
full Kähler Weitzenböck cross-checked by R̊(g)=Ric=6g and Δ_L(g)=0), so ε = 20 stands as computed.

ε = 20 is canonical Δ_L-STIFFNESS data of the frozen geometry's deformation complex — NOT a dynamical
response (the Fredholm/sourced-response equation is priced only, §9, trap #19).

## 9. v33 ledger (Gate 5, priced only, NO claims)

**(a) The lapse / 00 assembly.** Consumes ε = 20, the direction c(M) (now known to be richer than the
d-symbol square), and the v26/v27 scalar sector. Needs the 4d-slice EMBEDDING of CP² into the bulk
(the h₂(C_u) Lorentzian slice). PRICE: a new milestone — build the slice embedding, pull the (1,1) TT
+ scalar data through the lapse decomposition. NOT run. (The direction-N(M) finding means the assembly
must use the genuine c(M).)

**(b) The OP² lift.** OP² = h₃(O) is Spin(9), NOT Kähler — there is NO (1,1)/(2,0)+(0,2) bigraduation,
so the dim-8 (1,1) multiplet story does NOT transfer. PRICE: redo the Boucetta-class eigentensor
bookkeeping for Spin(9) isotypic types; the analogue of λ_L=32 / ε=20 / κ=1/54 differs. Do NOT assume
the CP² result lifts. NOT run.

**(c) The Fredholm / response READING of ε.** The equation `(Δ_L − 2Λ)h = source` is a Block-C-shaped
IMPORT (where five gravity routes died: v17/v18/v19/v20/v21). NAMED and FENCED (trap #19): "response"
appears only in this pricing. Since ε = 20 ≠ 0 the stiffness reading is available in principle, but
turning it into a sourced response EQUATION `(Δ_L−2Λ)h = κT` is Block-C dynamics, NOT a v32 dictionary
fact — a v33+ import. NOT run.

---

### References (orchestrator-verified, for the no-web executor)
- M. Boucetta, *Spectra and symmetric eigentensors of the Lichnerowicz Laplacian on P^n(C)*,
  arXiv:0712.2830 (Tables VI–VIII at n=2). The dimension anchor; the (1,1) λ=12 dim-8 multiplet.
- A. L. Besse, *Einstein Manifolds*, §1.143 (the Lichnerowicz Laplacian Δ_L), §4.57 (the orthogonal
  York split), §12.28 (infinitesimal Einstein deformations = ker(Δ_L−2Λ)|_TT), §12.98ff (Koiso
  rigidity of CP^n — Trap #16).
- A. Ikeda, Y. Taniguchi, Osaka J. Math. 15 (1978) 515 (scalar spectrum 4k(n+k); 1-form eigenforms).
- N. P. Warner, Proc. R. Soc. Lond. A 383 (1982) 217 (the spectra of operators on CP^n).
- M. Berger, D. Ebin, J. Diff. Geom. 3 (1969) 379 (the TT ⊕ δ*ω split).
- Y. Matsushima (holomorphic gradients of λ₁-eigenfunctions on KE manifolds) — the B1 Hessian-gauge
  control.
- GPD internal: v25 `variety_moment_doublet` (φ_M, λ₁=12), v26 `variety_sourced_field_equation`
  (G_M, R_M cut α=2/5,β=3/20,λ₂=32), v27 phase 87 `87-VERDICT.md` (the tuple (a,c,c_R,TrM²,detM)
  and the law c_R(1−c_R)λ²=−|π_{1/2}M|²G_M, detM absent), v28 `variety_spinor_moment` (s_M=dφ_M),
  v30 `clock_connection` (χ), v31 `91-VERDICT.md` + `code/tensor_probe.py` (the ratified LIVE +
  machinery).
