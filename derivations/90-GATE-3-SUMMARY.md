# Phase 90 — GATE 3 SUMMARY (U3, u-complex sector — THE VERDICT)

**PASS. Sector verdict = DEAD.** F⁽²⁾ = d(a_X⁽²⁾) = 0 identically: the clock-drift 1-form
is EXACT, a_X⁽²⁾ = dχ.

- **(a) ∇D = 0** — the canonical C_u-phase reference D is covariantly constant: ∂_v D lands in
  V_{1/2}(p) (the cone-normal/tangent sector), killed by the face projection C_p. Verified at
  the base for both tangent directions (3×3-complex GS-parallel gauge; confirmed independently
  in the octonion engine along all four cut families).
- **(b) a_X⁽²⁾ = dχ EXACT** — with χ(p) = ⟨𝒦, D⟩, a(v) − ∂_v χ = −⟨𝒦, ∂_v D⟩ = 0 by Peirce
  orthogonality ⟨V₀, V_{1/2}⟩ = 0 (𝒦 ∈ V₀ a face element, ∂D ∈ V_{1/2}). ⇒ **F⁽²⁾ = da = 0.**
- **(c) trap #13, honest not vacuous** — the canonical transport's own holonomy is GENUINELY
  nonzero (the recorded v22 slot-82 slice holonomy h(φ≠0) ≠ I, h(0) = I — the Fubini–Study
  background, the thing the prompt insists be subtracted), yet a_X⁽²⁾ is an exact 1-form so its
  closed-loop holonomy ∮a = 0 (Stokes): the real FS background does NOT leak into the
  matter-intrinsic curvature. The matter contributes ZERO curvature.

**Non-hardwired verdict():** F⁽²⁾ = da = 0 iff a_X⁽²⁾ is exact; computed exact ⇒ DEAD.
(A surface bug — undeclared-real parameters polluting an antiholomorphic derivative, and a
naive nested-∇ "Bott background" that is the wrong object for the two-sided-compression
connection — first produced a spurious LIVE; the genuine verdict object is da of the 1-form,
which the exactness a=dχ forces to 0. Both the complex-rep and the octonion-engine paths agree.)
