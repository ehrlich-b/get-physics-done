# DRAFT — slot 93 candidate: v33.0 THE BLOCK-C CONFRONTATION (what, if anything, forces the metric response)
# STATUS: DRAFT-FOR-STEELMAN. Do NOT paste until (a) the v32.0-B close-out is ratified,
# (b) Bryan decides lapse/00 ordering, (c) the steelman pass runs on this draft.
# This is THE tripwire run: gravity or genuinely-don't-know. CANNOT-FORMULATE or
# FORCES-NOTHING ⇒ the route parks at fork A (honest-scope write-up = the deliverable).

## The question, sharply

The framework owns the MATTER side of a variational pair, as theorems:
- E[φ_M, g] = ∫(|dφ_M|²_g − λ₁ φ_M²) dvol_g is framework-native (v25's field equation is
  its Euler–Lagrange identity), and its metric variation δE/δg is the B3-shaped stress
  whose unique transverse part is the certified v31/v32 mode
  (TT(B3) = the λ_L=32 triple-27 straddle, ‖TT‖² = (1/30)(TrM²)², ε = 20).
- The v26 sourced equation and the v27 balance law are exact identities on FROZEN g
  (v27 is metric-free by construction — pre-checked: demanding its persistence under
  g → g+h forces NOTHING; that candidate is dead on arrival and excluded from gates).

What the framework has never produced is the GEOMETRY side: a functional A[g] from its
OWN resources (no imported ∫R√g) whose coupled extremization with E forces a sourced
response (Δ_L − 2Λ)h = κ·TT(B3) or names what replaces it. The five fiber kills
(v17–v21) do not transfer: Molien forbids derivative functionals on the ALGEBRA, but the
variety is a manifold and spectral functionals of g exist. Wall 1 has never been asked
here. This run asks it.

## Track A (the positive candidate): the λ₁-extremal selection principle

The one geometry-side functional the framework arguably FORCES: the variety's defining
embedding is BY λ₁-eigenfunctions (the moments; v25 theorem — the embedding is
algebraic, not chosen). By the Takahashi / El Soufi–Ilias circle: an isometric minimal
immersion by first eigenfunctions ⟺ the metric is λ₁-extremal (extremizes
λ₁[g]·Vol[g]^{2/4} on its conformal/Kähler class — exact scope to be pinned at Gate A1).
If that biconditional applies, "FS extremizes λ₁" is a THEOREM of the construction —
a native selection principle, the first candidate for A[g] = λ₁[g]·Vol[g]^{1/2}.

GATES (each falsifiable, exact over Q where in-rep):
- A1 (literature + scope pin): state the exact Takahashi/El Soufi–Ilias theorem; verify
  the moment embedding satisfies its hypotheses (isometric up to the v25 normalization?
  minimal? into which sphere — the trace-form sphere of the Jordan algebra?). Deliverable:
  PROPERTY vs PRINCIPLE verdict — is λ₁-extremality FORCED by the algebraic embedding,
  or merely true? (This is the load-bearing joint; if CLAMP, say so and grade Track A
  accordingly — joint test discipline.)
- A2 (in-rep check): minimality of the moment embedding CP² → S(trace form) — the mean
  curvature vanishes (computable from the v25/v31 machinery; Takahashi: immersion by
  λ-eigenfunctions of a NORMALIZED metric is minimal-into-sphere automatically — verify
  which direction is theorem and which needs computing).
- A3 (the second variation): compute δ²(λ₁·Vol^{1/2}) at FS on the TT sector — in-rep on
  the v32 multiplet: the stiffness of the λ₁-extremal functional on the certified mode.
  λ₁ is degenerate (multiplicity 8) ⇒ the variation is an eigenvalue-of-a-matrix problem
  (the 8×8 ⟨dφ_a ⊗ dφ_b , h⟩ Gram — the v32 machinery computes this). Deliverable: the
  quadratic form Q_A[h] on the 27-multiplet, exact.
- A4 (the coupled equation): extremize A[g] + μ·E[φ_M, g] over h at fixed M (μ a
  multiplier, NOT a coupling — fence). Deliverable: the sourced linear equation for h,
  exact; STATE whether it is (i) Einstein-form ((Δ_L − 2Λ)h ∝ TT(B3) on the TT sector),
  (ii) a different named operator, (iii) degenerate (no TT response — e.g. the λ₁
  variation only sees the level-1 sector and is BLIND to the 27 — pre-registered risk:
  ⟨dφ_a⊗dφ_b, t_27⟩ might vanish by rep theory! CHECK THIS FIRST as gate A3a — if the
  8⊗8 product contains no 27-overlap with the straddle... Sym²(8) ⊃ 27 once says it CAN
  couple; whether it DOES is the computation).
- A5 (the number): if (i), read off κ as the spectral ratio it is; fence: κ is a ratio of
  framework numbers, NOT Newton's constant; signature still Riemannian (Wall 2 unpaid —
  NO outcome is called "gravity").

## Track B (the exhaustion negative): the native-functional menu

Enumerate ALL framework-native functionals of g available on the base (the menu is
short and auditable): (i) ∫G_M dvol (entropy field — metric-blind integrand, gives at
most trace/conformal response: prove the one-line lemma and retire it); (ii) spectral
functionals (λ₁ tower = Track A; λ₂, the heat invariants — each priced); (iii) Vol[g]
(cosmological-only); (iv) anything from the moment map beyond (i)–(iii) — sweep and
name. Deliverable: either Track A is the UNIQUE candidate (the menu argument), or the
missed candidates are named and priced. If Track A fails and the menu is exhausted:
**FORCES-NOTHING is the verdict, the route parks at fork A** — record it as the
RESOLUTION of don't-know (the tripwire accepts this; what it forbids is wandering).

## Fenced / priced only (NOT run, NOT the verdict)

- Base-Sakharov (one-loop det of the 8 moment scalars on CP²; a_1 ∝ ∫R√g): an IMPORT
  (the functional-integral machinery + extremize-the-effective-action are not native).
  The v21 kind-4 kill was the FIBER version (16-vs-6 rank wall); the base version is
  un-run but stays PRICED-ONLY in this run — if Track A/B both land negative, it is the
  named off-menu option for a later Bryan decision, not a silent fallback here.
- The thermo scope: cite the 2026-06-09 unban memo's exact boundary before using ANY
  entropy-extremization language beyond v26's proved vacuum-MaxEnt (which is a theorem
  of the doublet, not a thermodynamic postulate).

## Verdict taxonomy (frozen at Gate 0; STOP rule 3 binding)

- FORCES-EINSTEIN-FORM: A4 lands (i) with forced constants. (The first selection law.)
- FORCES-OTHER: A4 lands (ii) — name the operator; that IS the framework's gravity-shaped
  law, Einstein or not; do NOT relabel to fit.
- FORCES-NOTHING: A3a blindness, or A4 degenerate, AND Track B menu exhausted ⇒ park at
  fork A. This is a RESOLUTION, not a failure — write it that way.
- CANNOT-FORMULATE: if Gate A1 cannot even pin the principle sharply ⇒ same as
  FORCES-NOTHING per the tripwire.

Traps: #20 (extremality-as-property smuggled as principle — A1's joint test is the
guard); #21 (the multiplier μ relabeled a coupling — it is not); #22 (a degenerate A4
"fixed" by adding imported terms — adding ∫R√g at any gate = STOP). Controls: the v32
fingerprint identities must reproduce before any new computation (machinery freeze);
A3's quadratic form must be SU(3)-equivariant (Schur block-diagonal — deviation = bug).
Exact over Q; three-path verification as standing; milestone HOLD for human ratification.
