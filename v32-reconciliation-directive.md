# v32.0-B FINAL RECONCILIATION DIRECTIVE — every number placed, primary source fetched
# Paste into the GPD session. Supersedes the §3/§4 amendment path of
# v32-ratification-directive.md (its T1/T2/T3 fingerprint results STAND and are cited
# below; its §4.3 "c8=0 via B1-mechanism" story and "pure-(1,1) i∂∂̄ multiplet" language
# are RETRACTED and replaced by the structural account here).

## 0. The verdict-vs-verifier conflict is RESOLVED — both were partly right

Blog-side fetched and read Boucetta arXiv:0712.2830 directly (Tables V–VIII + Lemma 2.1
+ the eigenvalue Lemma 3.4). The decisive entry the verifier missed:

> **Table VIII (S^{1,1}(P²C)), row 2, m=0: eigenvalue 4(m+2)(m+4) = 32, complex
> dimension (m+3)³ = 27, eigenspace φ∘δ*_h∘δ̄*_h(T^{0,0}_{2,2})** — the (1,1)-sector
> dim-27 multiplet at λ_L=32, built from the level-2 scalars by the two half-gauge
> operators. (Same row in Table V general-n: 4(k+2)(n+k+2), dim formula → 27 at n=2,k=0.)

The verifier's B.2 read row 1 of Table V/VIII (λ=12, dim 8) and Tables VI/VII (anti 27s
at 32), and attributed r[1] to row 1. But r[1] has ZERO overlap with the row-1 multiplet:
the blog-side Gram fingerprint (ratification directive §7, exact over Q on YOUR basis)
proved r[1] is pure 27-rep (rank-6 P27 Gram, both null identities, all 36 entries), and
BOTH independent Δ_L^{(1,1)} implementations (executor's and verifier's own) measured 32
on r[1] — a row-1 member would have returned 12. r[1] lives in row 2.

## 1. THE RECONCILED SCOREBOARD (all claims placed, receipts named)

| quantity | executor said | verifier said | TRUTH + receipt |
|---|---|---|---|
| λ_L (residue) | 32 | 12 | **32** — Table V/VIII row 2 (m=0); fingerprint (pure 27); both operators agree on r[1] |
| ε = λ_L−2Λ | 20 | 0 (REFUTED-claim) | **20** — and for the FULL mode (see §2); verifier's ε=0 attributed r[1] to the wrong table row |
| κ (verdict obj.) | 1/54 | 1/30 | **1/30 = the TT-mode norm** (verifier RIGHT); 1/54 = the (1,1)-block share (5/9), 2/135 = anti share (4/9); all forced, detM absent |
| v31 "jointly gauge" | inherited | REFUTED | **REFUTED** (verifier RIGHT) — the anti blocks carry genuine transverse content; v31's localization falls, v31's EXISTENCE verdict stands (re-confirmed 3 ways) |
| multiplet ID | "λ=12 dim-8 adjoint" | "8⊕27 mix" | **NEITHER**: a single λ_L=32 eigentensor STRADDLING the three sectors — (1,1)-27 (row 2) ⊕ (2,0)+(0,2)-27s (Tables VI/VII row 1); pure 27-channel, c₈ = c₁ = 0 |
| Koiso/trap-#16 | "ε≠0, different operator" (wrong gloss) | "ε=0, 2nd-order rigidity" (wrong) | **No TT mode at 12 exists at all**: the two dim-8s at 12 (row 1 primitive + row 3 m=1 conformal φ·g) are non-transverse/trace — verifier's OWN A.3 proved no standalone transverse (1,1) tensor exists. First-order Koiso rigidity intact. No tension for anyone. |
| direction ∝N(M) | FAILS (finding) | FAILS (real) | FAILS ✓ — true direction = P27(M⊗M) (fingerprint T1/T2/T3) |
| detM absence | ✓ | ✓ (all blocks) | ✓ forced by degree-4 invariant theory (Cayley–Hamilton) |
| full-Δ_L anti path 28/4 | (dead code) | "looks mis-normalized" | BUG in the dead-code path, confirmed by §2: the anti blocks must sit at 32 |

## 2. The full residue is a SINGLE λ_L=32 eigentensor (new, forced by degree counting)

The anti content of r is built from bilinears of degree-(1,1) moments ⇒ coefficient
bidegree (2,2). The only Z-balanced anti space reachable (k+p = l+q with (p,q)=(0,2),
k ≤ 2) is T^{0,2}_{2,0} — Boucetta's 27 at eigenvalue 32 (Table VI row 1). Nothing else
in the anti spectrum (48, 60, 72, 80, 96 need higher bidegree) is reachable. So ALL
THREE block-components of r sit at eigenvalue 32 ⇒ **Δ_L r = 32 r as a full tensor**:
ε = 20 is the stiffness of the WHOLE mode, not just a block. (This also proves the
verifier's 28/4 full-path readings were the flagged dead-code bug. NEW CONTROL for the
fix: a corrected full-tensor Δ_L must return 32 on every block of r.)

The straddle's structure: Sym²(8) ⊃ 27 exactly once, but the 32-eigenspace contains
THREE copies of the 27 (one per sector). Equivariance gives one scalar per copy;
TRANSVERSALITY of the total (the divergences of r[1] and r_anti cancel — verifier A.1)
locks their ratio. Hence the FORCED norm split ‖r[1]‖² : ‖r_anti‖² = 5 : 4 (verified on
4 matters by the verifier: 2/27 vs 8/135, etc.) and the single overall constant
κ_full = 1/30. The dictionary closed form: TT(B3[M]) = T₂₇[P27(M⊗M)] with T₂₇ the
fixed straddling equivariant map — ONE forced constant. Object (i) COMPLETE.

Where the 8-channel went (replaces the retracted B1-mechanism story): the 8-channel of
Sym²(8) has NO transverse home at this level — the only 8s in the low spectrum sit at 12
(row 1 = non-transverse primitive; row 3 m=1 = conformal trace modes), and r is TT hence
L²-orthogonal to both. c₈ = 0 is STRUCTURAL. (Empirically confirmed by the fingerprint.)

## 3. What the executor must now do (the v32.0-B close-out)

1. **Re-verdict with the corrected objects** (LIVE survives, strengthened):
   - object (ii): **‖TT(B3)‖² = (1/30)(TrM²)²** (the full TT mode; detM absent, forced),
     with the forced 5:4 block split and the (1,1) share 1/54 recorded as structure;
   - object (iii): **ε = 20** for the full mode (Table V/VIII row 2 + degree counting);
   - object (i): TT(B3[M]) = T₂₇[P27(M⊗M)], the triple-27 straddle at 32, fingerprint
     citation (T1/T2/T3 exact);
   - object (iv): unchanged (dictionary exact).
2. **Doc corrections (all 92-*):** retire "λ=12 (1,1) multiplet" and "dim-8 su(3)-adjoint"
   everywhere; replace the V3-bis trap-#16 gloss ("scalar carrier + Weitzenböck shift" —
   wrong) with the Table V/VIII row-2 account; fix Gate-0's "rank-6 = the d-symbol image"
   (FALSE: that image is rank 2; rank 6 = the P27-image rank of single generators);
   record the two drift instances (Gate-0 criterion rewrite; 3a redefinition).
3. **Commit the fingerprint tests in-repo** (from the ratification directive §3:
   T1 4(t_s01+t_a01+t_d1) + (t_s02+t_a02+t_s12+t_a12) = 0;
   T2 t_d2 = 9(t_s01+t_a01+t_d1); T3 the 36-entry Gram table — all verified blog-side
   exact over Q on your own driver; reproduce executor-side and commit).
4. **v31 amendment note:** (a) EXISTENCE verdict unchanged (triple-confirmed); (b) the
   "jointly gauge ⇒ purely (1,1)" localization RETRACTED (the residue straddles, all
   three blocks nonzero, single eigentensor at 32); (c) the "λ=12 Boucetta dim-8"
   attribution corrected to the row-2/VI/VII triple-27 at 32; (d) diagnose in one line
   why v31's jointly-gauge solve succeeded (ansatz-space artifact) — if quick; else mark
   diagnosed-pending.
5. **Flag the full-tensor `lichnerowicz` anti path** as untrusted dead code, or fix it
   and validate with the new control (32 on every block of r). Remove the duplicate def.
6. **Boucetta re-map note in RESEARCH:** rows 1/2/3 of Table VIII placed (12-primitive
   non-transverse / 32-dim-27 δ*δ̄* / conformal tower); Tables VI/VII row 1 (anti 27s at
   32); Koiso clean (no TT at 12). Note v31 AND the verifier both stopped at row 1 —
   the failure mode was reading the lowest row as "the" TT multiplet.
7. THEN: VERDICT = LIVE (v32.0-B), milestone HOLD for human ratification as usual.

## 4. Credit where due (for the record)

The three-path protocol worked exactly as designed: the executor's numbers were right
on λ_L/ε and wrong on the κ-object and the multiplet name; the verifier caught the
κ-object and the v31 localization and was wrong on ε (wrong table row); the blog side
caught the multiplet ID (fingerprint) and fetched the primary source that arbitrates.
No single path had the full picture. Fences held throughout; exact over Q throughout.
