# slot 95 — v35.0-candidate: THE AREA-PER-BIT KILL-TEST (native-holography diagnostic / entanglement-route Limb A)
# "Does the variety natively set a CONSTANT bits↔area exchange rate (→ background scale, no gravity, fork A),
#  or a SHEARING one (→ candidate gravitational field)?"
# STATUS: READY (registered by Bryan's request 2026-06-14). Paste AFTER the v34 milestone is recorded.
# This is the OFF-FAITHFUL / on-variety form of the entanglement route's sharp-claim-1: slot 83 ran that claim
# AT the faithful point I/3 (= v23) and DIED (∇S=0, point-specific). This runs it on STRUCTURED states with the
# area pairing. CHEAP — built from CERTIFIED objects (v25 moment doublet + v26 G_M). Run it to CLOSE the route.
# Milestone HOLD for human ratification; do NOT self-register the next slot.

## 0. Where this sits (read before running — the honest gate is load-bearing)

The 2026-06-14 brainstorm hit gravity from SIX independent angles (modular-rate, light-crossing, off-faithful,
ρ_J-source, holography, area-per-bit) and ALL reduced to ONE clamp: the bits↔geometric-area exchange rate
(= Ryu–Takayanagi's G = state-fp ⟹ metric-fp). Holography is theorem-blocked NATIVELY (h₃(O) is finite
Type-I₃ by Zelmanov; the one "Area/4G from an algebra" result, CPW arXiv:2302.01938, needs a Type III₁ factor
the exceptional algebra cannot have). The program has the Bekenstein BOUND (S ≤ Area, **G-free**) but NOT the
RT EQUALITY (Area = 4G·S, **G-valued**). The G-valued equality is the unpaid step = the clamp.

This run is the ONE cheap, un-run, decisive check the brainstorm left standing: does the VARIETY (CP², where
v23's fiber kill does not reach) set the exchange rate as a UNIVERSAL CONSTANT (→ a background scale; gravity
is a contingent import; fork A) or does it SHEAR with matter (→ a candidate gravitational field)?

It is NOT a claim that FS-area derives gravity (the FS metric is IMPORTED — see Fence 2 + Bug-guard 2). It is a
DIAGNOSTIC of whether native area↔entropy variation exists at all on the variety.

THE HONEST PRIOR: **expected DEAD-CONSTANT.** Both the matter-induced area and the v26 entropy response G_M are
leading-order ∝ |π_{1/2}M|², so their ratio is likely a pure constant (the FOURTH death of the read-off/extremize
family, consistent with v33 FORCES-NOTHING and the v23 ∇S=0 death). A DEAD verdict is a REAL result: it closes
the entanglement route and confirms the bits↔area rate is a contingent import (→ fork A, the honest-scope TOE).
gravity or genuinely-don't-know — never math-for-math.

## The objects (all certified, exact over ℚ)

- Variety: CP² (the C*-cut of OP² = F₄/Spin(9)), Fubini–Study metric, Ric = 6g. USED, not derived.
- Structured (off-faithful) state: X = I/3 + εH, with H a Peirce-block matter direction M (the v24 families;
  ρ_J > 0, i.e. genuinely off the faithful point).
- v25 moment doublet at address p ∈ CP²: m(p) = TrX − ⟨X,p⟩, q(p) = ⟨X#,p⟩; face entropy S_face(p) = f(r),
  r = q/m².
- v26 entropy response (the "bits" side): δ²S = (9/2)·G_M(p)·ε², G_M(p) = ⟨M#,p⟩ − ¼⟨M,p⟩² ≤ 0
  (= minus the second-order relative entropy). Exact rational on CP².
- v31/v32 (for Gate 2): the matter-sourced TT metric mode dφ_M⊗dφ_M, ‖TT(B3)‖² = (1/30)(TrM²)², ε = λ_L−2Λ = 20.

## Gates (exact over ℚ; fail-fast)

**G0 — SETUP + PIN THE AREA OBJECT (the load-bearing definitional gate).**
Reproduce the v25 doublet + v26 G_M on ≥3 matter directions (s01, a01, d1) plus one generic detM≠0. Confirm the
structured states are genuinely off I/3 (∇S_face ≠ 0 — else the test is vacuous, as v23 was; Bug-guard 4).
Then DEFINE the matter-induced area A_M(p) CANONICALLY from the FS geometry. Candidates (pick by FS-canonicity,
not convenience): (i) the FS Riemannian measure of the perturbation's support; (ii) the FS area-element contracted
with the v31 metric-mode dφ_M⊗dφ_M; (iii) the moment-map (KKS) symplectic area of the perturbation. REQUIREMENT:
the definition must be FS-canonical and matter-functorial (no hand-tuning); state it explicitly and justify why it
is the area RT would pair with S. Bug-guard 1 applies.

**G1 — THE KILL-TEST (cheap, decisive).**
Compute R(p, M) = A_M(p) / |G_M(p)| exactly over ℚ along the structured families and across the ≥3 matter
directions.
- **R CONSTANT** (p- and M-independent up to the single overall scale) ⇒ **DEAD-CONSTANT**: the variety sets a
  UNIVERSAL bits↔area rate = a background scale = NO native gravitational field. Fork A. (Expected.) STOP.
- **R SHEARS** (genuine p- or M-dependence not removable by one global normalization) ⇒ candidate scalar exchange
  field. Go to G2.
Bug-guards 1 + the homogeneity check apply (a ratio that "varies" only because A_M is homogeneous-constant while
G_M varies is NOT a shear of the RATE — a real shear needs A_M and G_M to have DIFFERENT M-structure).

**G2 (only if G1 SHEARS) — THE RANK WALL.**
Is the shear field a SCALAR (trace-only → Nordström-class, empirically dead: no light bending) or does it carry the
v31 TT tensor structure? Decompose the shear's source against dφ_M⊗dφ_M and test whether it is the TRACE of the
v31 TT(B3) mode (→ LIVE-TENSOR, connects to v32 ‖TT(B3)‖² = (1/30)(TrM²)²) or pure trace (→ LIVE-SCALAR/Nordström).

## Verdict taxonomy
- **DEAD-CONSTANT** — R constant ⇒ universal background rate, no native gravity; the bits↔area rate is a contingent
  import (κ, Λ company). Fork A. **EXPECTED.**
- **LIVE-SCALAR** — R shears but trace-only ⇒ Nordström ceiling (empirically dead gravity); partial, NOT Einstein.
- **LIVE-TENSOR** — R shears and carries the v31 TT structure ⇒ the variety natively sets a SHEARING tensor exchange
  rate; the surprise; upstream-but-pointing-at the clamp (does NOT by itself = Einstein — see anti-overclaim).

## Bug guards (pre-registered)
1. AREA-RIG: A_M must be FS-canonical + matter-functorial; re-run G1 with ≥2 canonical definitions. A verdict that
   FLIPS between them is an artifact ⇒ report INCONCLUSIVE; do NOT pick the favorable one.
2. CIRCULARITY: the FS metric is IMPORTED. This run does NOT derive gravity from FS-area; it DIAGNOSES whether native
   area↔entropy variation exists. Do NOT record G as derived. A LIVE verdict is necessary-not-sufficient for gravity
   and must route through the v34 induce process to mean anything dynamical.
3. FISHER-CORPSE: G_M is the state-space Fisher–Bures object (v17's corpse — positive-(semi)definite on STATES, not a
   Lorentzian spacetime metric). It is the DENOMINATOR (the bit-counter), never the metric. Do NOT read R's shear as
   a spacetime curvature directly.
4. CONSTANT-BY-SYMMETRY: at I/3 everything is F₄-homogeneous (∇S=0, the v23 death). This run is OFF I/3 by
   construction; confirm ∇S_face ≠ 0 on the test states (else vacuous, as v23 was).
5. NUMERIC LEAKAGE: exact over ℚ throughout; no floats in the verdict.

## Fences (binding, verbatim)
NO Einstein-equation / G=κT / gravity / Newton / dark-matter / geodesic language as a DERIVED result; the bits↔area
rate is a framework ratio (a contingent import like κ, Λ unless G1 forces otherwise), NOT Newton's G; FS is USED,
not derived; signature Riemannian (Wall 2 unpaid — NOTHING is called gravity until signature is paid); DEAD-CONSTANT
and LIVE-* are NOT derivations of gravity. This run does NOT retract v33 (extremize FORCES-NOTHING), v34 (induce
CLOSES-CONDITIONAL), v17–v21 (fiber kills), or v23 (I/3 death). Paper 5 remains the only result in the
more-than-nothing column.

## Anti-overclaim
LIVE (either) = the variety sets a non-constant exchange rate — NECESSARY, not sufficient, for gravity; it is the
area-side diagnostic of the entanglement route, UPSTREAM of the clamp (state-fp ⟹ metric-fp). Even LIVE-TENSOR shows
native area↔entropy variation, NOT that the metric obeys a field equation. Per Jaksland (arXiv:2005.05055), a generic
Jacobson/RT recovery validates nothing program-specific — the originality is whether the variety FORCES the rate, not
that an area law exists. DEAD-CONSTANT is the expected and honest outcome; it closes the entanglement route and is the
green light for fork A.

## Verification
Triple-path (orchestrator + independent verifier on a SEPARATE code path + adversarial). Exact over ℚ. Non-hardwired
verdict ladder with self-tests: a rigged-constant input → DEAD; a hand-built shearing input → LIVE; a verdict that
flips under area-redefinition → INCONCLUSIVE. Milestone HOLD for human ratification; do NOT self-register the next slot.
