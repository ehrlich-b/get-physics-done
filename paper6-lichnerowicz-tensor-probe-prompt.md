# GPD Prompt — The Tensor Probe: Does Matter Source a Genuine Metric Mode? (Block B, the Einstein make-or-break)

**Slot 91 / v31.0-candidate. STATUS: RATIFIED-BY-STANDING-DIRECTIVE
(Bryan, 2026-06-11/12). Run AFTER the v30.0 ratification directive is
pasted.**

_Context: v30.0 COMPLETE (commit ec548519 + close-out) — the
form-selection fork closed DEAD-constructive (a_X⁽²⁾ = dχ exact; the
gluing U(1) unforced at form level; matter pins class + rate + twist
only). This run executes the recorded Block-B forecast: the route's
gravity ceiling so far is TWO SCALARS (clock-rate/lapse-class K-data +
conformal/landscape-class r, G_M data) = Nordström-class, empirically
dead as gravity. Einstein-FORM needs a RANK-2, non-scalar,
non-gauge metric mode sourced by matter — the 16-vs-6 rank wall
relocated to the variety route lives HERE. The recorded ONE crack in
the tensor wall: the cut CP² is Kähler–Einstein and the certified v25
moment fields sit EXACTLY at the Lichnerowicz–Matsushima λ₁ = 12
threshold — the eigenvalue where scalar data first touches the metric
deformation complex. Certified assets consumed: v25 moment fields
φ_Y (Δφ_Y = −λ₁(φ_Y − ⟨Y,I/3⟩), λ₁ = 12 cut / 48 OP²); v26 response
fields G_M, R_M (λ₂ = 32 cut / 104 OP² sector content; the sourced
equation); v28 spinor moment s_X = dφ_X; v29 twist 𝒦; v30 potential
χ; the canonical frozen geometry (Fubini–Study on the cut — USED, not
derived; the same fence as v24–v30)._

_Discipline: v21–v30 pattern — executor + independent verifier
(separate code path) + adversarial third path, exact verdicts over
Q/Q(t), fail-fast gates, controls with known answers, background
machinery calibrated BEFORE verdicts. No Einstein-equation claim, no
Newton constant, no G = κT, no dark matter, no geodesic-motion
language. LIVE here = a sourced tensor MODE EXISTS (the prerequisite);
the selection law (why Einstein's particular equation) is Block C and
is NOT claimed by any branch of this run._

## Pre-read: milestone type (pre-registered)

**GENUINE FORK** (v27/v29/v30 class): no expected verdict.
**LIVE** = some certified matter-built rank-2 object has a nonzero
transverse-traceless (TT) residue at generic M — matter forces a
genuine metric mode; the tensor wall has a door; Einstein-FORM
becomes reachable-in-principle (κ contingent, Block C still open).
**DEAD** = every certified rank-2 object decomposes EXACTLY as
gauge + conformal (h = δ*ω + f·g, with (ω, f) exhibited per object —
constructive); the route's gravity ceiling is scalar
(Nordström-class); Einstein-FORM is unreachable from the matter data
the route possesses, and fork A's floor becomes the route's gravity
ceiling. Both branches are major payloads. Honest prior: genuinely
open. Structural lean DEAD-ward (Hessians are longitudinal-by-nature;
two genuine forks in a row closed DEAD), BUT the bilinear sector
(dφ ⊗ dφ, the π_{1/2}M bilinear) carries λ₂-sector content where TT
harmonics can first live — whether the certified sector actually
intersects the TT harmonics is exactly the question, and NOTHING in
the design prejudges it.

## The setup (verify every step)

**The split (canonical, frozen geometry).** On the compact cut
(CP², Fubini–Study, Kähler–Einstein), symmetric 2-tensor fields
decompose L²-orthogonally (Berger–Ebin/York):

> h = h_TT ⊕ δ*ω ⊕ f·g

(TT = divergence-free traceless; δ*ω = the Lie-derivative/gauge part,
(δ*ω)_{vw} = ½(∇_v ω_w + ∇_w ω_v); f·g = conformal). Over the FINITE
polynomial sectors below this split is EXACT RATIONAL LINEAR ALGEBRA
per SU(3)-isotypic block (the 3×3-complex rep: p = vv†, functions and
tensors are low-degree polynomials in v, v̄; the same weight class as
the v26 adversarial λ₂ = 104 derivation). No elliptic inversion, no
numerics: the verdict object is the TT-residue of h against the
gauge + conformal span, decided by exact orthogonality in the sector.

**Gate-0 threshold facts (verify in-rep, do not import blind):**
(i) the cut metric is Einstein with Λ = λ₁/2 = 6 in the recorded
normalization (check Ric = Λg via the recorded curvature/holonomy
machinery); (ii) the v25 moment fields ARE λ₁-eigenfunctions
(recorded; regression); (iii) the Matsushima mechanism made concrete:
for a λ₁-threshold eigenfunction φ on a Kähler–Einstein space,
grad φ + J grad φ is holomorphic and ∇∇φ = ½ L_{grad φ} g — the
Hessian of every v25 field is PURE GAUGE. This is the wall, verified,
and it is the Gate-1 control with known answer.

**THE BATTERY (frozen at Gate 0 — the rank-2 objects the route
possesses).** All symmetric rank-2 tangent forms of degree ≤ 2 in M
buildable from certified objects + the Jordan product + the C_p/π
projections. Expected members (enumerate exhaustively at Gate 0,
declare any additional member BEFORE any verdict computation; the
battery is then FROZEN — post-hoc additions are a new run):

- **B1 (control, known answer: pure gauge).** ∇∇φ_Y, Y traceless
  (deg 1 — the Matsushima wall).
- **B2.** ∇∇G_M (deg 2 — the response field's Hessian; λ₂-sector
  content).
- **B3.** s_X ⊗ s_X = dφ ⊗ dφ, traceless part (deg 2 — the spinor
  moment bilinear; note dφ⊗dφ = ½∇∇(φ²) − φ∇∇φ links B3 to
  λ₁×λ₁ → λ₂ products: an internal cross-check, verify it).
- **B4.** ∇∇χ (deg 2 — the v30 potential's Hessian).
- **B5.** ∇∇R_M (deg 2).
- **B6 (the stress-shaped member).** The π_{1/2}M tangent bilinear:
  the symmetric form T_M(v,w) built from the matter's tangent content
  (the v27 |π_{1/2}M|² is its trace — pin the canonical form at
  Gate 0 from the Jordan structure, state it explicitly, and freeze
  it).

**The verdict object.** For each battery member h: its exact
TT-residue in the sector (h minus its projection onto
span{δ*ω} ⊕ span{f·g} at the member's polynomial degree), at generic
M (symbolic where feasible; rational-instance battery with exact
per-instance verdicts otherwise; co-diagonalization strata carry zero
weight).

## Claims

**V1 — the machinery (lemma grade).** The split solver: the
gauge image δ*(Ω¹) and the conformal space {f·g} spanned COMPLETELY
at each relevant polynomial degree (closure of the certified sector
under δ*, trace, and the split — verify; under-spanning is the
fake-LIVE failure mode and gets a dimension audit). External anchor:
the SU(3)-isotypic dimension table for scalar / 1-form / S²-sections
at the degrees used, cross-checked against the recorded scalar
spectrum (Ikeda–Taniguchi 4k(k+2): 12, 32 reproduced) and the
published CP² tensor-harmonic content (Gibbons–Pope-class CP²
harmonic analysis) where available.

**V2 — controls (zero evidential weight, all must behave).**
(i) B1 = pure gauge EXACTLY (the Matsushima wall — known answer; its
(ω, f) exhibited); (ii) Killing fields land in the kernel of δ*
(su(3) reproduced); (iii) vacuum M → all members ≡ 0; (iv) the
spectrum-matched diagonal reference (v24 direction-blind contrast)
behaves per its stratum; (v) the L²-orthogonality of the three split
blocks verified numerically-exactly on the sector basis.

**V3 — THE FORK (the verdict, exact).** Run the battery B2–B6 through
the split at generic M. **LIVE ⟺ at least one member has TT-residue
≢ 0**, with the payload: WHICH member(s), the TT-part expressed in
the certified field basis, which SU(3)-isotypic type carries it, and
whether the λ₂-sector content (the v26 fields) is where it lives.
**DEAD ⟺ every member's TT-residue ≡ 0 identically**, with the
constructive certificate: (ω, f) exhibited explicitly PER MEMBER
(DEAD must be constructive, not a failure-to-find), plus the scope
theorem: the certified matter data sources scalars and gauge only —
the route's gravity ceiling is Nordström-class.

**V4 — the reading (fenced).** IF LIVE: the program's first
matter-forced TENSOR structure — the prerequisite for Einstein-FORM
exists on the route; Block C (the selection law: WHY the Einstein
equation, the κ value) remains open and is NOT claimed; no
Einstein-equation/Newton-constant/G = κT language. IF DEAD: the
tensor wall holds at the variety route's strongest point (the
λ₁-threshold crack probed and closed); the route's gravity ceiling is
scalar; record it as Block B closed-negative and route the
consequence to the fork-A write-up (the floor stands). One sentence
stating which world.

## Gates

- **Gate 0 — the machinery + the freeze (fail-fast).** The 3×3-rep
  sector spans; the split solver assembled; the isotypic dimension
  table (external anchors reproduced); threshold facts (i)–(iii)
  verified; THE BATTERY ENUMERATED AND FROZEN (any member beyond
  B1–B6 declared here or never).
- **Gate 1 — controls (zero weight).** All of V2; B1's (ω, f)
  exhibited and re-verified by the independent path BEFORE any
  B2–B6 computation (the solver must succeed on the known-gauge case
  first — trap #14).
- **Gate 2 — the Hessian sector** (B2, B4, B5 through the split;
  these may be all-trivial without deciding the fork — report, no
  verdict language).
- **Gate 3 — the bilinear sector** (B3, B6 — the verdict center;
  u-complex sector first, then full where feasible).
- **Gate 4 — V3 + V4 full** (generic-M verdict assembled; the payload
  in certified fields, or the per-member constructive certificates;
  the one-sentence world statement).
- **Gate 5 — the v32 ledger (exploratory, NO claims).** Price only:
  IF LIVE — the sourced Lichnerowicz equation (does the TT-part
  satisfy a Δ_L-type response law with the v26 source?), the
  lapse/00 assembly consuming scalar + tensor pieces, and the OP²
  extension (Spin(9) isotypic bookkeeping; OP² is NOT Kähler — the
  threshold mechanism differs, price it, do not assume it). IF DEAD —
  the fork-A write-up consequence (the floor = the ceiling), whether
  higher-degree matter data could reopen the battery (priced only),
  and the OP² question's status.
- **STOP rule:** B1 control failure (= normalization/design hole, NOT
  LIVE evidence), isotypic dimension mismatch vs the external anchors
  (= under-spanning risk), or engine disagreement on the octonionic
  battery ⇒ report and STOP.

## Guards (pre-registered)

1. **Trap #14 (control inversion):** a solver that fails to find B1's
   gauge potential would "discover" fake TT. LIVE requires B1 passing
   AND the LIVE member's residue surviving the SAME solver in the
   SAME sector. No solver-asymmetry between control and verdict.
2. **Trap #15 (under-spanning = fake-LIVE):** the gauge + conformal
   span must be COMPLETE at the battery's polynomial degree —
   dimension-audited per isotypic block against the external anchors.
   A TT-residue claim without the dimension audit is no claim.
3. **Trap #16 (rigidity conflation):** Koiso-class rigidity of the
   Einstein metric (no Einstein-deformation zero modes) is about the
   KERNEL of the deformation operator, NOT about whether matter
   sources TT — cite it only for well-posedness color, never as DEAD
   evidence.
4. **Strata discipline (standing):** generic M; symbolic where
   feasible; co-diagonalization strata and the diagonal reference
   carry zero weight.
5. **DEAD must be constructive:** (ω, f) per member, exhibited and
   independently verified — not a failure-to-find.
6. **The octonion product-order trap (standing):** independent
   implementations agree on an octonionic battery first; the
   3×3-complex rep stays guard-locked to the engine.
7. **Language fence:** LIVE = "a matter-sourced tensor mode EXISTS"
   — NOT "Einstein gravity derived"; no Einstein-equation, no Newton
   constant, no G = κT, no dark matter, no geodesics; Block C is not
   claimed; the frozen canonical geometry is USED, not derived; the
   v18/v20 MM corpse stays buried (this is the BASE's deformation
   complex, not a fiber-built spacetime connection).

## Deliverables

`derivations/91-VERDICT.md` (V1–V4, the verdict rule applied, scope
stated); `derivations/91-GATE-N-SUMMARY.md` per gate;
`derivations/91-tensor-probe-RESEARCH.md` (the split machinery, the
dimension audit, the battery, the per-member results, the payload or
the certificates, the v32 ledger); `code/tensor_probe.py` (executor)
+ `code/tensor_probe_verify.py` (independent path; an
`_indep_check.py` third path expected, v26–v30 parity). Commit; HOLD
the v31.0 milestone bookkeeping for ratification, mirroring v25–v30.
