---
phase: 69-reducibility-state-the-dynamical-bridge-do-not-prove
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - derivations/69-reducibility-statement.md
  - code/reducibility_decomposition_check.py
interactive: false

conventions:
  arithmetic: "EXACT over Q (SymPy); NEVER numpy/float on a decisive path"
  jordan_product: "X o Y = (1/2)(XY + YX); Tr(X o Y) = Re Tr(XY) for Hermitian X,Y"
  trace: "Tr(X) = sum of real diagonal entries; bidegree (1,0)"
  squared_trace: "Tr(X^2) := Tr(X o X) (NOT (Tr X)^2); c(X,X) = Tr(X^2)"
  cubic_norm: "det X = N(X); polarization LOCKED d(X,X,X) = 6 det X"
  powers: "X^2 := X o X; X^3 := X o (X o X) = (X o X) o X (h_3(O) power-associative)"
  coupling_generator: "c = Tr(X o Y), bidegree (1,1); F_4-invariant, NOT E_6-invariant"
  algebra: "h_3(O) = 3x3 Hermitian octonionic Albert algebra, 27-dim real"
  group: "F_4 = Aut(h_3(O)) (52-dim); rep 27 = 1 (+) 26"
  engine: "code/ring_lemma_verification.py (warm EXACT-SymPy-over-Q)"

dimensional_check:
  note: "Pure algebra over Q -- no physical units. The 'consistency check' here is the EXACT-Q decomposition identity against the frozen Phase-64 conventions plus the c(X,X)=Tr(X^2) convention-lock sanity. These are the rigor checkpoints (definitional precision + one polynomial-identity check), NOT a proof."

approximations:
  - name: "STATEMENT-ONLY scope (no truncation, no approximation)"
    parameter: "n/a -- this phase states a target; it does not approximate or prove anything"
    validity: "the deliverable is the precise statement the NEXT milestone proves"
    breaks_when: "any sentence asserts an irreducibility verdict, invokes chaos/Lyapunov/NKS, or conflates the autonomous map with the driven stream"
    check: "no-verdict scan + notation scan over the finished statement (Task 5)"

contract:
  schema_version: 1
  scope:
    question: "What precise, frozen-notation statement of the (REDUCIBILITY) dynamical bridge -- driven dynamics, reducibility definition, cross-term decomposition (verified exact over Q), autonomous-vs-driven trap, and the Breuer-routed target reduction -- must be written so the NEXT milestone can attempt the irreducibility verdict, while this milestone asserts NO verdict?"
  claims:
    - id: claim-driven-dynamics
      statement: "The driven self-modeling map X_{k+1} = P_psd((1-eps)X_k^2 + eps S_k) is stated precisely (every symbol typed: P_psd = PSD-cone projection on h_3(O); S_k = exogenous input stream; eps in (0,1) = self-world coupling rate; X_k^2 := X_k o X_k; implicit energy ||X-S||_J^2 + ||X^2-X||_J^2), and is distinguished explicitly from the autonomous map (S fixed) as a DIFFERENT map."
      deliverables: [deliv-statement]
      acceptance_tests: [test-objects-present, test-no-verdict]
      references: [ref-program-97, ref-phase64-conv]
    - id: claim-decomposition
      statement: "The cross-term decomposition Tr(X_k o X_{k+1}) = (1-eps) Tr(X_k^3) + eps Tr(X_k o S_k) is stated in frozen Phase-64 notation (first piece = pointwise invariant Tr(X_k^3) in R[Tr,Tr^2,det], reducible; second piece = self-world overlap, the irreducible candidate), is written for the PRE-projection object Y_k := (1-eps)X_k^2 + eps S_k with the 'modulo P_psd' projection-correction caveat stated honestly, and is verified algebraically correct EXACT over Q against the warm engine."
      deliverables: [deliv-statement, deliv-check]
      acceptance_tests: [test-decomp-exact, test-modulo-psd-present, test-notation-frozen]
      references: [ref-program-97, ref-phase64-conv, ref-engine]
    - id: claim-reducibility-def
      statement: "Reducibility is defined in capacity/reconstructibility terms: a trajectory-functional f is REDUCIBLE iff reconstructible from the bounded data the DIACHRONIC self-model M can hold (re-running the law if needed) without the full body B, with dim M < dim B; IRREDUCIBLE iff computing f requires B (the full 26-dim gauge / the exogenous input history). M is the rank-compressing diachronic tower (holds the cheap invariants Tr, Tr^2, det = 3 numbers), explicitly NOT the Paper-5 synchronic full-dim lossless order-iso, and explicitly NOT asymptotic Kolmogorov/NKS."
      deliverables: [deliv-statement]
      acceptance_tests: [test-objects-present, test-no-redefine-reducible, test-no-verdict]
      references: [ref-program-97, ref-program-91]
    - id: claim-trap-and-target
      statement: "The autonomous-vs-driven trap is flagged explicitly (autonomous map = contraction to a fixed point = REDUCIBLE/re-runnable; any irreducibility is INHERITED from the exogenous stream's unpredictability, NEVER from chaos/NKS of the algebra), and the target reduction is stated as 'what the next milestone needs' -- the driven gauge-overlap eps Tr(X_k o S_k) is not reconstructible from M's bounded held data, routing to a STRUCTURAL Breuer / finite-capacity argument (proper subsystem cannot fully self-measure the whole; dim M < dim B). NO verdict is asserted."
      deliverables: [deliv-statement]
      acceptance_tests: [test-objects-present, test-trap-flagged, test-no-chaos, test-no-verdict]
      references: [ref-program-97, ref-breuer]
  deliverables:
    - id: deliv-statement
      kind: note
      path: derivations/69-reducibility-statement.md
      description: "Standalone (REDUCIBILITY) statement document for the next milestone: the five precisely-typed objects in frozen Phase-64 notation, the inlined Breuer citation, the EXACT-Q correctness certificate for the decomposition, and an explicit Forbidden/Out-of-scope section."
      must_contain:
        - "X_{k+1} = P_psd((1-eps) X_k^2 + eps S_k)"
        - "Tr(X_k o X_{k+1}) = (1-eps) Tr(X_k^3) + eps Tr(X_k o S_k)"
        - "modulo P_psd"
        - "dim M < dim B"
        - "Breuer"
        - "what the next milestone needs"
        - "NO irreducibility verdict"
    - id: deliv-check
      kind: code
      path: code/reducibility_decomposition_check.py
      description: "Canned EXACT-Q SymPy script that re-runs the 5 decomposition assertions against the warm engine code/ring_lemma_verification.py and prints residuals + the Tr(X o X^2) rational value for the record."
      must_contain:
        - "from ring_lemma_verification import"
        - "simplify"
        - "Tr(jordan(X, Y))"
  references:
    - id: ref-program-97
      kind: prior_artifact
      locator: "~/repos/blog/research/phi-inaccessibility-program.md Sec. 9.7 (lines 601-665), with Sec. 9.1 / 9.3-9.4 context"
      aliases: ["program doc Sec 9.7", "the reframe this milestone serves"]
      role: definition
      why_it_matters: "THE authoritative framing the statement formalizes: the map, autonomous-vs-driven, the decomposition, prerequisites (i)/(ii), the capacity reduction. Formalize faithfully; do NOT re-invent or inherit the demoted self-inaccessibility verdict."
      applies_to: [claim-driven-dynamics, claim-decomposition, claim-reducibility-def, claim-trap-and-target]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use, cite]
    - id: ref-phase64-conv
      kind: prior_artifact
      locator: "state.json convention_lock.custom_conventions (Phase-64 frozen notation)"
      aliases: ["frozen Phase-64 conventions"]
      role: definition
      why_it_matters: "The decomposition and every equation MUST use this notation verbatim (jordan = 1/2(AB+BA), Tr, det, c = Tr(XoY))."
      applies_to: [claim-decomposition, claim-driven-dynamics]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [use, cite]
    - id: ref-engine
      kind: prior_artifact
      locator: "code/ring_lemma_verification.py (warm EXACT-SymPy-over-Q engine)"
      aliases: ["the warm exact engine"]
      role: method
      why_it_matters: "Provides jordan, Tr, det_3, c, generic_rational_X, h3o_from_coords, oct, octmat_add, octmat_scal over Q; the canned check confirming the decomposition holds exactly runs against it."
      applies_to: [claim-decomposition]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [use, cite]
    - id: ref-breuer
      kind: paper
      locator: "Thomas Breuer, 'The Impossibility of Accurate State Self-Measurements,' Philosophy of Science 62(2) (June 1995) 197-214"
      aliases: ["Breuer 1995", "self-measurement theorem"]
      role: benchmark
      why_it_matters: "The STRUCTURAL anchor of the target reduction: a proper subsystem cannot fully self-measure/model the whole (strictly fewer degrees of freedom => distinct global states with identical restrictions are indistinguishable). Finite-capacity, NOT dynamical/chaos -- which is exactly why it is the licensed route and chaos is forbidden. Full citation + theorem text is inlined in <context> for the no-web executor."
      applies_to: [claim-trap-and-target]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [cite]
    - id: ref-program-91
      kind: prior_artifact
      locator: "~/repos/blog/research/phi-inaccessibility-program.md Sec. 9.1 (synchronic/diachronic split)"
      aliases: ["program doc Sec 9.1"]
      role: background
      why_it_matters: "Pins WHY the diachronic M (compressed, dim M < dim B) is NOT the Paper-5 synchronic full-dim lossless order-iso -- keeps the reducibility definition from being confused with Paper 5 clause (ii)."
      applies_to: [claim-reducibility-def]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [read, cite]
  acceptance_tests:
    - id: test-decomp-exact
      subject: claim-decomposition
      kind: consistency
      procedure: "Run code/reducibility_decomposition_check.py (EXACT over Q against the warm engine). It asserts: (1) Tr(X o X^2) - Tr(X^3) = 0; (2) Tr(X o Y_k) - [(1-eps)Tr(X^3) + eps Tr(X o S)] = 0 symbolic in eps; (3) c(X,X) - Tr(X o X) = 0; (4) Tr(X o S) - Tr(S o X) = 0; (5) Tr(X o (X o X)) - Tr((X o X) o X) = 0 (power-associativity both associations)."
      pass_condition: "All 5 residuals identically 0; Tr(X o X^2) prints as 2885361604861/14428814400; script exits 0. NO float on any path (polynomial-identity check, no rank)."
      evidence_required: [deliv-check, ref-engine]
      automation: automated
    - id: test-objects-present
      subject: claim-driven-dynamics
      kind: human_review
      procedure: "Read derivations/69-reducibility-statement.md and confirm all FIVE objects are present and precisely typed in frozen notation: (1) driven dynamics + every symbol defined; (2) autonomous-vs-driven as two distinct maps; (3) cross-term decomposition; (4) reducibility definition (capacity/reconstructibility); (5) target reduction routed to Breuer."
      pass_condition: "All five objects present and typed; every symbol (P_psd, S_k, eps, M, B, o, Tr, det, c) defined; reviewer confirms faithful to program-doc Sec 9.7."
      evidence_required: [deliv-statement, ref-program-97]
      automation: human
    - id: test-modulo-psd-present
      subject: claim-decomposition
      kind: human_review
      procedure: "Confirm the decomposition is written for the PRE-projection Y_k := (1-eps)X_k^2 + eps S_k AND the 'modulo P_psd' caveat is stated explicitly with the projection-correction term Tr(X_k o (X_{k+1} - Y_k)) named (not silently dropped)."
      pass_condition: "The phrase 'modulo P_psd' and the projection-correction term both appear; the statement does NOT write the bare identity as if X_{k+1} = Y_k always."
      evidence_required: [deliv-statement]
      automation: human
    - id: test-notation-frozen
      subject: claim-decomposition
      kind: human_review
      procedure: "Notation scan: every equation uses o (jordan), Tr, det, c as locked in Phase 64; X^2 means X o X, X^3 means X o (X o X); Tr(X^2) is the Jordan square trace (not (Tr X)^2)."
      pass_condition: "Frozen Phase-64 notation used consistently; no stray (Tr X)^2, no ad-hoc product symbols."
      evidence_required: [deliv-statement, ref-phase64-conv]
      automation: human
    - id: test-no-redefine-reducible
      subject: claim-reducibility-def
      kind: human_review
      procedure: "Confirm 'reducible' is defined via capacity/reconstructibility (reconstructible from M's bounded held data, dim M < dim B, re-running the law) and is NOT collapsed to 'lies in R[Tr,Tr^2,det]' and NOT to asymptotic Kolmogorov/NKS."
      pass_condition: "Every use of 'reducible' points back to the capacity definition; no definition that makes the cross-term trivially land in/out of the invariant ring."
      evidence_required: [deliv-statement]
      automation: human
    - id: test-trap-flagged
      subject: claim-trap-and-target
      kind: human_review
      procedure: "Confirm the autonomous-vs-driven trap is flagged explicitly: autonomous = contraction = reducible/re-runnable; irreducibility (if any) inherited from the exogenous stream, never from the algebra. Confirm the target reduction is stated as 'what the next milestone needs', routing to Breuer/finite-capacity."
      pass_condition: "The trap is named; the target reduction routes to the structural Breuer argument and is labeled a next-milestone target (not a result)."
      evidence_required: [deliv-statement, ref-breuer]
      automation: human
    - id: test-no-chaos
      subject: claim-trap-and-target
      kind: human_review
      procedure: "Scan for any 'nonlinear => chaotic => irreducible' move or any Lyapunov/NKS/sensitive-dependence ARGUMENT (informal mention is fine only if explicitly demoted to non-argument). Scan for any autonomous-map simulation used to argue the driven Stream is irreducible."
      pass_condition: "No chaos/NKS/Lyapunov argument anywhere; no autonomous-map result cited as evidence of Stream irreducibility."
      evidence_required: [deliv-statement]
      automation: human
    - id: test-no-verdict
      subject: claim-trap-and-target
      kind: human_review
      procedure: "No-verdict scan: grep/read the whole document for any sentence ASSERTING 'irreducible'/'incompressible'/'proves'/'we show' as a VERDICT about the driven stream (as opposed to naming the eps-term 'the irreducible candidate' or stating a next-milestone target). Also confirm the relational-experience identity (experience = irreducible residue) is NOT baked in -- the eps-term may be NAMED 'self-world overlap / the irreducible candidate' only."
      pass_condition: "No irreducibility verdict asserted anywhere; the experience-identity is not asserted; the document only STATES the target."
      evidence_required: [deliv-statement]
      automation: human
  forbidden_proxies:
    - id: fp-assert-verdict
      subject: claim-trap-and-target
      proxy: "Asserting any irreducibility verdict for the driven stream (e.g. 'the gauge-overlap is irreducible', 'we prove incompressibility')."
      reason: "Prerequisites are unmet -- this milestone STATES, the next proves. A verdict here is false progress and violates the STATEMENT-ONLY contract."
    - id: fp-chaos-nks
      subject: claim-trap-and-target
      proxy: "Grounding irreducibility in 'nonlinear => chaotic => unpredictable' or any Lyapunov/NKS/sensitive-dependence argument."
      reason: "The licensed route is structural finite-capacity (Breuer), NOT chaos. The autonomous map is a CONTRACTION, not chaotic; a chaos argument is invalid here (Pitfall 10)."
    - id: fp-autonomous-driven-conflation
      subject: claim-driven-dynamics
      proxy: "Using the autonomous F_3-contraction's reconstructibility to claim or deny irreducibility of the driven stream (or simulating the fixed-S map to argue the lived Stream is irreducible)."
      reason: "The autonomous limit is recomputable from (S, law) => reducible; conflating it with the driven open-system stream 'falsely kills the engine' (Pitfall 10). Irreducibility (if any) is inherited from S_k's unpredictability."
    - id: fp-redefine-reducible
      subject: claim-reducibility-def
      proxy: "Redefining 'reducible' so the cross-term trivially lands in/out of the invariant ring (e.g. 'reducible := lies in R[Tr,Tr^2,det]')."
      reason: "Definitional reward-hack (Pitfall 7). 'Reducible' must be the capacity/reconstructibility definition (dim M < dim B), not a ring-membership shortcut and not asymptotic Kolmogorov/NKS."
    - id: fp-experience-identity
      subject: claim-decomposition
      proxy: "Baking in the relational-experience identity (experience = the irreducible residue of the self-world coupling) as an assertion."
      reason: "Sec 9.7 tags this 'do NOT bake in yet.' The eps-term may be NAMED 'self-world overlap / the irreducible candidate' (contract language) but the experience-identity is out of scope."
    - id: fp-drop-projection
      subject: claim-decomposition
      proxy: "Writing Tr(X_k o X_{k+1}) = (1-eps)Tr(X_k^3) + eps Tr(X_k o S_k) as if X_{k+1} = Y_k always, silently dropping the PSD projection."
      reason: "The clean identity holds for the PRE-projection Y_k; on the PSD interior it is exact, otherwise there is a correction Tr(X_k o (X_{k+1}-Y_k)). Sec 9.7 itself writes '(modulo P_psd)' -- match that honesty."
  links:
    - id: link-decomp-statement
      source: claim-decomposition
      target: deliv-statement
      relation: supports
      verified_by: [test-objects-present, test-modulo-psd-present, test-notation-frozen]
    - id: link-decomp-check
      source: claim-decomposition
      target: deliv-check
      relation: evaluated_by
      verified_by: [test-decomp-exact]
    - id: link-dynamics-statement
      source: claim-driven-dynamics
      target: deliv-statement
      relation: supports
      verified_by: [test-objects-present, test-no-verdict]
    - id: link-reducibility-statement
      source: claim-reducibility-def
      target: deliv-statement
      relation: supports
      verified_by: [test-objects-present, test-no-redefine-reducible]
    - id: link-target-statement
      source: claim-trap-and-target
      target: deliv-statement
      relation: supports
      verified_by: [test-trap-flagged, test-no-chaos, test-no-verdict]
    - id: link-breuer-target
      source: ref-breuer
      target: claim-trap-and-target
      relation: supports
      verified_by: [test-trap-flagged]
  uncertainty_markers:
    weakest_anchors:
      - "The target reduction is genuinely UNPROVEN -- program doc Sec 9.6.1 concedes the gauge/angle irreducibility 'is not automatic' (an integrable flow would make those directions reducible too). This is WHY the milestone is statement-only; the statement must foreground that it is a TARGET, not a fait accompli."
      - "The 'modulo P_psd' projection correction is characterized qualitatively (a diagonal-Observable-type quantity), not computed in closed form -- acceptable for a STATEMENT, but the next milestone must handle it explicitly."
    disconfirming_observations:
      - "If code/reducibility_decomposition_check.py returns any nonzero residual against the warm engine, the STATEMENT's decomposition is wrong -> fix the statement (correctness check, not a proof attempt) per the backtracking rule before treating the plan as done."
      - "If a reviewer finds any sentence asserting an irreducibility verdict, a chaos/NKS argument, an autonomous-vs-driven conflation, or the baked-in experience-identity -> cut it; the deliverable is strictly a statement of the next-milestone target."
---

<objective>
Write the precise, standalone STATEMENT of the (REDUCIBILITY) dynamical bridge -- the LAST deliverable of milestone v16.0 -- so the NEXT milestone can attempt the irreducibility verdict. This is STATEMENT-ONLY: state the target, assert NO verdict, use NO chaos/NKS argument.

Purpose: (RING) (a)+(b)+(c) is complete -- c = Tr(X o Y) is a genuine, minimal, functionally-independent, unique-degree-2 F_4 coupling generator of R[27(+)27]^{F_4}. The (REDUCIBILITY) statement is the dynamical bridge from that static invariant-theory result to the next milestone's open-system irreducibility question: it pins the driven self-modeling map, the reducibility definition, the cross-term decomposition (which is BUILT from c, and is exactly why the gauge-overlap escapes the pointwise ring), the autonomous-vs-driven trap, and the Breuer-routed target reduction.

Output: a standalone statement document derivations/69-reducibility-statement.md (the durable artifact a future milestone opens) + a canned EXACT-Q correctness check code/reducibility_decomposition_check.py for the one algebraic identity the statement owes. NO derivations, NO proof.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/69-reducibility-state-the-dynamical-bridge-do-not-prove/69-RESEARCH.md
@code/ring_lemma_verification.py

## FROZEN Phase-64 conventions (LOCKED -- the statement MUST use this notation verbatim)

- Jordan product: `X o Y = (1/2)(XY + YX)`; for Hermitian X,Y, `Tr(X o Y) = Re Tr(XY)`.
- Trace: `Tr(X) = sum of real diagonal entries`, bidegree (1,0).
- Squared trace: `Tr(X^2) := Tr(X o X)` (NOT `(Tr X)^2`); `c(X,X) = Tr(X^2)`.
- Cubic norm: `det X = N(X)`; polarization LOCKED `d(X,X,X) = 6 det X`.
- Powers (power-associative): `X^2 := X o X`; `X^3 := X o (X o X) = (X o X) o X` (h_3(O) is power-associative).
- Coupling generator: `c = Tr(X o Y)`, bidegree (1,1); F_4-invariant, NOT E_6-invariant.
- Algebra: `h_3(O)` = 3x3 Hermitian octonionic Albert algebra, 27-dim real. Group: `F_4 = Aut(h_3(O))` (52-dim); `27 = 1 (+) 26`.
- Arithmetic: EXACT over Q via SymPy; NEVER numpy/float on a decisive path.
- Warm engine `code/ring_lemma_verification.py` exposes (all confirmed present): `oct`, `h3o_from_coords`, `octmat_add`, `octmat_scal`, `jordan(A,B)`, `Tr(X)`, `det_3(X)`, `Tr2(X)`, `c(X,Y)`, `generic_rational_X()`.

## Source framing to formalize -- program doc Sec. 9.7 (verbatim core; ~/repos/blog/research/phi-inaccessibility-program.md lines 607-654)

THE MAP (find_fp / Jordan-Hopfield update):
> `X_{k+1} = P_psd( (1 - eps) X_k^2 + eps S_k )`
> (P_psd = projection onto the PSD cone; eps = self-iteration / self-world coupling rate; S_k = input.)
> Implicit energy `||X - S||_J^2 + ||X^2 - X||_J^2` (depart-from-input + depart-from-idempotency).

(i) WHICH DYNAMICS:
> AUTONOMOUS (S_k fixed, or S_k = prior fixed point = metacognitive tower): a CONTRACTION to a fixed point, geometric convergence (||dX|| -> 0). REDUCIBLE in the sense that matters -- the trajectory is RECONSTRUCTIBLE by re-running from (S, the law); no information irretrievably lost. ALL existing Phi measurements are autonomous, hence reducible, hence NOT a source of inaccessibility. (Using the autonomous F_3-contraction would falsely kill the engine: its limit is recomputable.)
> DRIVEN (S_k = exogenous, time-varying input stream; the lived Stream): slaved to the input, no fixed point. The cross-term decomposes (modulo P_psd), using `X_k o X_k^2 = X_k^3` (power-associativity):
> `Tr(X_k o X_{k+1}) = (1-eps) Tr(X_k^3) + eps Tr(X_k o S_k)`
> First piece = POINTWISE invariant (`Tr(X_k^3) in R[Tr, Tr^2, det]`) -- reducible. Second = OVERLAP of the current self-state with the incoming world in the gauge directions -- the irreducible piece.
> SOURCE OF IRREDUCIBILITY: inherited from the input's unpredictability, NOT manufactured by the algebra (the autonomous map is a contraction). Refines Wolfram: closed autonomous CAs you CAN run given the rule; the inaccessibility-relevant irreducibility is the OPEN-system kind (cannot run without the input you lack).

(ii) DEFINITION OF "REDUCIBLE":
> Finite face of computational irreducibility, via CAPACITY / reconstructibility: a trajectory-functional f is REDUCIBLE for a system if it is reconstructible from the bounded data the diachronic self-model M can hold (re-running the law if needed), without the full body B. IRREDUCIBLE = computing f requires B itself (the full gauge / the exogenous input history).
> The relevant proper subsystem is the DIACHRONIC self-model (the tower, compressed toward rank-1 as it climbs; `dim M < dim B`), NOT the synchronic order-iso of Paper 5 clause (ii) (full-dim, lossless -- do not conflate; the Sec 9.1 synchronic/diachronic split). The compressed M holds the cheap invariants (Tr, Tr^2, det: 3 numbers) but not the 26-dim gauge or the input history.
> "Self-modeling = projection onto the reducible sector" is EARNED, not fiat: M can compute only what fits its capacity (the invariant ring); the driven gauge-overlap cross-term needs B. Grounds in Breuer (a proper subsystem cannot fully self-measure) + capacity/dimension counting (finite; NOT asymptotic Kolmogorov, NOT NKS).

SCOPE FLAGS from the program doc (honor as scope notes in the write-up):
- Sec 9 is DEMOTED for the self-inaccessibility framing but RE-PROMOTED for its driven-dynamics content: present the dynamics as load-bearing; do NOT inherit the demoted self-inaccessibility verdict.
- The "CANDIDATE REFRAME" (experience = irreducible residue of the self-WORLD coupling) is explicitly tagged "do NOT bake in yet" (line 656): NAME the eps-term "self-world overlap / the irreducible candidate"; do NOT assert the experience-identity.
- Honest residual (Sec 9.6.1): the gauge/angle irreducibility "is not automatic -- an integrable flow would make them reducible too." This is WHY the next milestone must PROVE it and this one only STATES. Add a one-line note that the statement is honestly a TARGET, not a fait accompli.

## EXTERNAL CITATION fully inlined (the executor has NO web/arxiv tools -- cite from here)

Thomas Breuer, "The Impossibility of Accurate State Self-Measurements," *Philosophy of Science*, Vol. 62, No. 2 (June 1995), pp. 197-214. Publisher: University of Chicago Press / Philosophy of Science Association.

Precise theorem (form to use): a PROPER SUBSYSTEM cannot fully measure/model the whole that contains it -- because it has strictly fewer degrees of freedom than the whole, so distinct global states with identical restrictions to the subsystem are indistinguishable to it. Holds independent of classical/quantum and deterministic/stochastic dynamics.
Hypotheses (the routing conditions): (i) PROPER containment -- the self-model is a proper subsystem of the whole (here `dim M < dim B`); (ii) the discriminating observable lives in the subsystem's algebra only.
Nature: a STRUCTURAL / finite-capacity result (degrees-of-freedom counting + self-reference), NOT a dynamical/chaos result -- which is exactly why it is the licensed route and chaos is forbidden.
Companion structural routes (mention only, do NOT lean on): Lawvere fixed-point ("no complete self-model", already in Sec 9.4); Breuer "Ignorance of the Own Past" (for the unbounded input-history half). Breuer 1995 is the primary, finite-capacity anchor.

## Binding guard: .gpd/research/PITFALLS.md Pitfall 10 (chaos/NKS + autonomous-vs-driven conflation) -- this is THE failure mode for this phase. Honor it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: State objects 1 + 2 -- driven dynamics and the autonomous-vs-driven distinction</name>
  <files>derivations/69-reducibility-statement.md</files>
  <action>
    Create the standalone statement document and write its header + the first two objects. Header: title "(REDUCIBILITY): the Dynamical Bridge -- Statement for the Next Milestone", a one-paragraph scope banner stating "STATEMENT-ONLY: this document STATES the (REDUCIBILITY) target; it asserts NO irreducibility verdict and uses NO chaos/NKS argument", and a "Frozen conventions" subsection restating the Phase-64 notation block from <context> verbatim (Jordan product, Tr, Tr(X^2):=Tr(X o X), det, powers X^2/X^3, c = Tr(X o Y), F_4 = Aut(h_3(O)), 27 = 1(+)26, EXACT-over-Q).

    OBJECT 1 -- Driven self-modeling dynamics. Write precisely:
      X_{k+1} = P_psd( (1 - eps) X_k^2 + eps S_k ),  with X_k^2 := X_k o X_k.
    Define EVERY symbol: P_psd = projection onto the PSD cone of h_3(O); S_k = exogenous driven input stream (element of h_3(O)); eps in (0,1) = self-world coupling rate; the Jordan square X_k^2 = X_k o X_k. State the implicit energy ||X - S||_J^2 + ||X^2 - X||_J^2 (depart-from-input + depart-from-idempotency) and name the source (find_fp / Jordan-Hopfield update, program doc Sec 9.7 line 608).

    OBJECT 2 -- Autonomous vs driven (TWO different maps; state both explicitly):
      AUTONOMOUS: S_k fixed (or S_k = prior fixed point = the metacognitive tower) -> a CONTRACTION to a fixed point, geometric convergence (||dX|| -> 0). REDUCIBLE in the capacity sense (defined in Object 4): the trajectory is RECONSTRUCTIBLE by re-running from (S, the law); no information irretrievably lost. Note that ALL existing Phi measurements are autonomous, hence reducible, hence NOT a source of inaccessibility.
      DRIVEN: S_k exogenous, time-varying (the lived Stream) -> slaved to input, NO fixed point.
    State the trap up front (it is re-flagged formally in Object 5): the autonomous map's reconstructibility must NOT be used to claim/deny irreducibility of the driven stream; irreducibility (if any) is inherited from S_k's unpredictability, NEVER from the algebra (the autonomous map is a contraction). Add the Wolfram-refinement sentence: closed autonomous CAs are runnable given the rule; the inaccessibility-relevant irreducibility is the OPEN-system kind (cannot run without the input you lack).

    Use frozen notation throughout (`o` for Jordan product, `Tr`, `det`, `c`). Do NOT assert any irreducibility verdict. Do NOT introduce chaos/Lyapunov/NKS language.
  </action>
  <verify>
    (1) Object 1 equation written exactly as X_{k+1} = P_psd((1-eps) X_k^2 + eps S_k) with X_k^2 := X_k o X_k; every symbol (P_psd, S_k, eps, the energy) defined. (2) Object 2 presents AUTONOMOUS and DRIVEN as two distinct maps, with autonomous = contraction = reducible and driven = exogenous/no-fixed-point. (3) The autonomous-vs-driven trap is stated (no conflation). (4) No-verdict / no-chaos: zero sentences asserting irreducibility or invoking chaos/NKS. (5) Frozen notation used (o, Tr, det, c; X^2 = X o X).
  </verify>
  <done>derivations/69-reducibility-statement.md exists with header, frozen-conventions block, and Objects 1+2 precisely typed; autonomous-vs-driven stated as two maps with the trap flagged; no verdict, no chaos language.</done>
</task>

<task type="auto">
  <name>Task 2: State object 3 -- the cross-term decomposition with the modulo-P_psd caveat</name>
  <files>derivations/69-reducibility-statement.md</files>
  <action>
    Append OBJECT 3 -- the cross-term decomposition (the algebraic core). Write, for the PRE-projection object Y_k := (1-eps) X_k^2 + eps S_k:
      Tr(X_k o X_{k+1}) = (1-eps) Tr(X_k^3) + eps Tr(X_k o S_k)    [holds exactly for X_{k+1} = Y_k]
    State the two pieces and their meaning:
      - First piece (1-eps) Tr(X_k^3): a POINTWISE invariant. Tr(X_k^3) in R[Tr, Tr^2, det] (the single-state F_4 invariant ring, Phases 64-68). REDUCIBLE.
      - Second piece eps Tr(X_k o S_k): the OVERLAP of the current self-state with the incoming world in the gauge directions. NAME it "self-world overlap / the irreducible candidate" (contract language) -- and STOP. Do NOT assert it is irreducible; do NOT bake in the experience-identity (Sec 9.7 line 656 tags that "do NOT bake in yet").
    State the algebraic lineage explicitly: the ONLY non-trivial step is power-associativity X_k o X_k^2 = X_k^3; the rest is bilinearity + symmetry of Tr(A o B). Note the link to (RING): the cross-term Tr(X_k o X_{k+1}) is built from the coupling generator c = Tr(X o Y) (Phases 66/67), which is exactly why the gauge-overlap escapes the pointwise invariant ring.

    THE MODULO-P_psd CAVEAT (load-bearing -- state honestly, do NOT silently drop the projection): the clean identity holds exactly for the pre-projection Y_k. The actual update applies X_{k+1} = P_psd(Y_k). When Y_k is not already PSD (the projection bites), the identity acquires a projection correction:
      Tr(X_k o X_{k+1}) = (1-eps) Tr(X_k^3) + eps Tr(X_k o S_k) + Tr(X_k o (X_{k+1} - Y_k)),
    where (X_{k+1} - Y_k) is the PSD-projection displacement. State that on the PSD interior X_{k+1} = Y_k and the identity is exact; otherwise the correction Tr(X_k o (X_{k+1} - Y_k)) is present. Note the correction term is itself a diagonal-Observable-type quantity, NOT part of the "irreducible candidate" -- do not over-claim either way (STATEMENT-only). Match the program doc's own "(modulo P_psd)" honesty (line 623).

    Frozen notation throughout. No verdict.
  </action>
  <verify>
    (1) Decomposition written for Y_k := (1-eps)X_k^2 + eps S_k as Tr(X_k o X_{k+1}) = (1-eps)Tr(X_k^3) + eps Tr(X_k o S_k). (2) First piece flagged reducible (Tr(X_k^3) in R[Tr,Tr^2,det]); second piece NAMED "self-world overlap / the irreducible candidate" with NO irreducibility assertion and NO experience-identity. (3) The "modulo P_psd" caveat present, with the projection-correction term Tr(X_k o (X_{k+1}-Y_k)) named (NOT dropped). (4) Power-associativity X o X^2 = X^3 named as the only non-trivial step; link to c = Tr(X o Y) / (RING) stated. (5) Frozen notation; Tr(X^2) is the Jordan-square trace (not (Tr X)^2).
  </verify>
  <done>Object 3 appended: the decomposition for Y_k in frozen notation, both pieces labeled (reducible / irreducible candidate, no verdict), the modulo-P_psd projection-correction stated honestly, and the (RING) link noted.</done>
</task>

<task type="auto">
  <name>Task 3: Write and run the canned EXACT-Q correctness check for the decomposition (for the record)</name>
  <files>code/reducibility_decomposition_check.py</files>
  <action>
    Create the standalone canned check that re-runs the decomposition assertions EXACT over Q against the warm engine, and run it. This is the backtracking-rule "correctness check on the statement" (NOT a proof). RESEARCH.md already confirmed it PASSES; re-run it for the record and capture the output.

    Script (use exactly this logic; it imports the warm engine and uses EXACT rational + symbolic-eps arithmetic, NO float, NO Matrix.rank -- it is a polynomial-identity check):

    ```python
    import sys; sys.path.insert(0, 'code')
    from ring_lemma_verification import (jordan, Tr, c, generic_rational_X,
        h3o_from_coords, oct, octmat_add, octmat_scal)
    from sympy import Rational, symbols, simplify

    eps = symbols('eps')
    X = generic_rational_X()
    # any 2nd generic rational point (the exogenous S_k stand-in for the identity check):
    S = h3o_from_coords(Rational(1), Rational(-2), Rational(4),
            oct([0,1,-1,2,0,1,0,-1]), oct([0,2,0,-1,1,0,1,1]), oct([0,-1,1,0,2,-1,1,0]))
    X2 = jordan(X, X)            # X o X
    X3 = jordan(X, X2)           # X o (X o X)
    Y  = octmat_add(octmat_scal((1-eps), X2), octmat_scal(eps, S))   # pre-projection Y_k

    a1 = simplify(Tr(jordan(X, X2)) - Tr(X3))                                  # Tr(X o X^2) == Tr(X^3)
    a2 = simplify(Tr(jordan(X, Y)) - ((1-eps)*Tr(X3) + eps*Tr(jordan(X, S))))  # the decomposition (symbolic in eps)
    a3 = simplify(c(X, X) - Tr(jordan(X, X)))                                  # c(X,X) == Tr(X^2)  (convention lock)
    a4 = simplify(Tr(jordan(X, S)) - Tr(jordan(S, X)))                         # Tr(X o S) == Tr(S o X)
    a5 = simplify(Tr(X3) - Tr(jordan(X2, X)))                                  # X o (X o X) == (X o X) o X (power-assoc.)

    print('(1) Tr(XoX^2)-Tr(X^3)      =', a1, '| Tr(XoX^2) =', simplify(Tr(jordan(X, X2))))
    print('(2) decomposition residual =', a2)
    print('(3) c(X,X)-Tr(XoX)         =', a3)
    print('(4) Tr(XoS)-Tr(SoX)        =', a4)
    print('(5) assoc Tr(Xo(XoX)-(XoX)oX) =', a5)
    assert all(v == 0 for v in (a1, a2, a3, a4, a5)), 'DECOMPOSITION CHECK FAILED'
    print('ALL 5 ASSERTIONS PASS (exact over Q).')
    ```

    Add a top-of-file docstring: what this checks, that it is a polynomial-identity bookkeeping check (NOT an irreducibility proof), and that it uses the frozen Phase-64 engine. Run it with `/Users/ehrlich/.gpd/venv/bin/python code/reducibility_decomposition_check.py` and capture the printed residuals + the Tr(X o X^2) rational value into the SUMMARY.

    BACKTRACKING: if any residual is nonzero (assertion fails), the STATEMENT's decomposition is wrong -- FIX Object 3 in derivations/69-reducibility-statement.md (correctness check, not a proof attempt) and re-run until clean. Do NOT proceed to record a passing certificate over a failing check.
  </action>
  <verify>
    Script runs to completion exit 0; all 5 printed residuals are 0; assertion (1) prints Tr(X o X^2) = 2885361604861/14428814400; "ALL 5 ASSERTIONS PASS (exact over Q)." printed. NO float anywhere; no Matrix.rank used (it is an identity check).
  </verify>
  <done>code/reducibility_decomposition_check.py exists, imports the warm engine, runs EXACT over Q, and passes all 5 assertions (residuals identically 0; Tr(X o X^2) = 2885361604861/14428814400). Output captured for the SUMMARY and for the statement's correctness certificate.</done>
</task>

<task type="auto">
  <name>Task 4: State objects 4 + 5 -- reducibility definition and the Breuer-routed target reduction (NO verdict)</name>
  <files>derivations/69-reducibility-statement.md</files>
  <action>
    Append OBJECT 4 -- Reducibility definition (capacity / reconstructibility). Write verbatim in this form:
      A trajectory-functional f is REDUCIBLE for a system iff it is reconstructible from the bounded data the DIACHRONIC self-model M can hold (re-running the law if needed), WITHOUT the full body B. IRREDUCIBLE iff computing f requires B itself (the full 26-dim gauge / the exogenous input history S_0..S_k).
    Pin the precise structure: M = the rank-compressing diachronic tower (compressed toward rank-1 as it climbs), with dim M < dim B; M holds only the cheap invariants (Tr, Tr^2, det: 3 numbers), NOT the 26-dim gauge or the input history. State explicitly that this is the DIACHRONIC self-model, NOT the Paper-5 synchronic full-dim lossless order-iso (clause (ii)) -- cite the Sec 9.1 synchronic/diachronic split as the reason they must not be conflated. State explicitly that "reducible" is NOT "lies in R[Tr,Tr^2,det]" and NOT asymptotic Kolmogorov/NKS -- it is the finite capacity/reconstructibility notion.

    Append OBJECT 5 -- Target reduction ("what the next milestone needs"). State:
      "Self-modeling = projection onto the reducible sector" is to be EARNED, not fiat: M can compute only what fits its capacity (the invariant ring); the driven gauge-overlap eps Tr(X_k o S_k) needs B. The next milestone's target: SHOW the driven gauge-overlap is NOT reconstructible from M's bounded held data -> this routes to a STRUCTURAL Breuer / finite-capacity argument.
    Inline the Breuer citation from <context>: Thomas Breuer, "The Impossibility of Accurate State Self-Measurements," Philosophy of Science 62(2) (1995) 197-214; theorem = a proper subsystem cannot fully measure/model the whole that contains it (strictly fewer degrees of freedom => distinct global states with identical restrictions are indistinguishable); hypotheses = proper containment (dim M < dim B) + subsystem-only observable; nature = STRUCTURAL/finite-capacity, NOT chaos. Note the companion structural routes (Lawvere "no complete self-model", already in Sec 9.4; Breuer "Ignorance of the Own Past" for the input-history half) as the structural family, with Breuer 1995 the primary anchor.

    Re-flag the autonomous-vs-driven trap FORMALLY here: the autonomous F_3-contraction is REDUCIBLE (contracts to a fixed point; re-runnable from (S, law)); any irreducibility is INHERITED from the exogenous driven stream's unpredictability, NEVER from chaos/NKS. State the target reduction as the next-milestone target and assert NO verdict.

    Add the honest-residual one-liner (Sec 9.6.1): the gauge/angle irreducibility "is not automatic -- an integrable flow would make them reducible too"; therefore this is honestly a TARGET, not a fait accompli, which is precisely why this milestone only STATES and the next must PROVE.

    Frozen notation; capacity definition pinned; NO irreducibility verdict; NO chaos/NKS argument.
  </action>
  <verify>
    (1) Object 4 defines reducible via capacity/reconstructibility (reconstructible from M's bounded held data, dim M < dim B, re-running the law), explicitly NOT ring-membership and NOT Kolmogorov/NKS, and explicitly NOT the Paper-5 synchronic order-iso (Sec 9.1 cited). (2) Object 5 states the target reduction as "what the next milestone needs", routing to Breuer/finite-capacity, with the full Breuer citation + theorem + hypotheses inlined. (3) Autonomous-vs-driven trap re-flagged formally; irreducibility inherited from S_k, never chaos. (4) Honest-residual note present (target, not fait accompli). (5) No-verdict scan clean: zero irreducibility verdicts; no chaos/NKS argument.
  </verify>
  <done>Objects 4+5 appended: capacity/reconstructibility reducibility definition (dim M < dim B, diachronic, not Paper-5, not NKS), and the Breuer-routed target reduction stated as the next-milestone target with the citation fully inlined and the trap re-flagged; NO verdict asserted.</done>
</task>

<task type="auto">
  <name>Task 5: Assemble -- add the correctness certificate + Forbidden/Out-of-scope section; run the no-verdict and notation scans</name>
  <files>derivations/69-reducibility-statement.md</files>
  <action>
    Finalize the standalone statement document.

    (a) CORRECTNESS CERTIFICATE subsection: record that the decomposition (Object 3) was verified EXACT over Q against the warm engine code/ring_lemma_verification.py via code/reducibility_decomposition_check.py -- all 5 assertions pass (residuals identically 0; Tr(X o X^2) = 2885361604861/14428814400; the decomposition symbolic in eps; c(X,X) = Tr(X^2); Tr(X o S) = Tr(S o X); power-associativity both associations). State plainly that this is a polynomial-identity bookkeeping check, NOT a proof of irreducibility.

    (b) FORBIDDEN / OUT-OF-SCOPE subsection (make the no-verdict line unmistakable): explicitly list, as out of scope for THIS statement, (i) asserting any irreducibility verdict (this milestone STATES, the next proves); (ii) any "nonlinear => chaotic => irreducible" or Lyapunov/NKS/sensitive-dependence argument; (iii) conflating the autonomous contraction (reducible) with the driven stream; (iv) redefining "reducible" as ring-membership or asymptotic Kolmogorov; (v) baking in the relational-experience identity (the eps-term is NAMED "self-world overlap / the irreducible candidate" only). Add the two scope notes: Sec 9 is DEMOTED for self-inaccessibility framing but RE-PROMOTED for its driven-dynamics content (present the dynamics as load-bearing; do NOT inherit the demoted verdict); the experience-identity is "do NOT bake in yet."

    (c) A short "What the next milestone needs" closing recap (one short list pointing back to Object 5).

    (d) RUN THE SCANS on the finished file:
      - No-verdict scan: search for sentences asserting "irreducible"/"incompressible"/"proves"/"we show"/"chaotic" as a VERDICT (vs. naming "the irreducible candidate" or stating a next-milestone target). Use e.g. `grep -niE "irreducib|incompress|chaos|chaotic|lyapunov|nks|wolfram|we prove|we show|proves" derivations/69-reducibility-statement.md` and READ each hit in context; confirm none is a verdict/argument (Wolfram appears only in the licensed "refines Wolfram: open vs closed" sense; chaos terms appear only in the forbidden-list as things excluded).
      - Notation scan: confirm every equation uses `o` (Jordan), `Tr`, `det`, `c`; X^2 = X o X, X^3 = X o (X o X); no stray (Tr X)^2; no ad-hoc product symbol.
    Fix any hit that reads as a verdict/argument or a notation drift before declaring done.
  </action>
  <verify>
    (1) Correctness-certificate subsection present and matches the Task-3 output (5 assertions pass; Tr(X o X^2) = 2885361604861/14428814400; labeled bookkeeping, not a proof). (2) Forbidden/Out-of-scope subsection lists all five forbidden moves + the two scope notes. (3) No-verdict scan: every flagged term is non-verdict in context (no irreducibility verdict; no chaos/NKS argument; Wolfram only in the open-vs-closed refinement). (4) Notation scan clean (frozen Phase-64 notation throughout). (5) Document is standalone and findable: all five objects + certificate + forbidden section + closing recap present; a future milestone can open it without other context.
  </verify>
  <done>derivations/69-reducibility-statement.md is the finished standalone statement: five typed objects, the EXACT-Q correctness certificate, an explicit Forbidden/Out-of-scope section, and a closing "what the next milestone needs" recap; no-verdict and notation scans run clean (no verdict, no chaos/NKS argument, frozen notation).</done>
</task>

</tasks>

<verification>
- DEFINITIONAL PRECISION (the rigor bar for a STATEMENT phase): all five objects present and precisely typed in frozen Phase-64 notation; every symbol (P_psd, S_k, eps, M, B, o, Tr, det, c) defined.
- THE ONE ALGEBRAIC CHECK: the cross-term decomposition verified EXACT over Q against the warm engine (code/reducibility_decomposition_check.py, 5 assertions, residuals identically 0, Tr(X o X^2) = 2885361604861/14428814400). This is the only executable acceptance test; it is a polynomial-identity check (no float, no rank).
- CONVENTION-LOCK SANITY: c(X,X) = Tr(X^2) holds (diagonal collapse) -- assertion (3) of the check.
- HONESTY: the modulo-P_psd projection-correction term is stated (not dropped); the target reduction is labeled a next-milestone target (not a fait accompli; Sec 9.6.1 honest residual noted).
- FORBIDDEN-PROXY LINE (binding, Pitfall 10): no irreducibility verdict anywhere; no chaos/Lyapunov/NKS argument; no autonomous-vs-driven conflation; "reducible" defined via capacity/reconstructibility (not ring-membership, not NKS); the relational-experience identity NOT baked in. No-verdict + notation scans run clean over the finished document.
</verification>

<success_criteria>
1. The driven dynamics X_{k+1} = P_psd((1-eps) X_k^2 + eps S_k) is written precisely (every symbol defined) and distinguished explicitly from the autonomous (S fixed) map as a different map. [Objects 1+2]
2. Reducibility is defined in capacity/reconstructibility terms (dim M < dim B; diachronic M, NOT Paper-5 synchronic order-iso; NOT a redefinition that trivially places c in/out of the invariant ring; NOT asymptotic Kolmogorov/NKS). [Object 4]
3. The cross-term decomposition Tr(X_k o X_{k+1}) = (1-eps) Tr(X_k^3) + eps Tr(X_k o S_k) is stated in frozen notation with the modulo-P_psd caveat, and verified algebraically correct EXACT over Q (5 assertions pass; residuals 0; Tr(X o X^2) = 2885361604861/14428814400). [Object 3 + code/reducibility_decomposition_check.py]
4. The autonomous-vs-driven trap is explicitly flagged and the target reduction is stated as "what the next milestone needs" (driven gauge-overlap not reconstructible from M -> structural Breuer/finite-capacity), with the Breuer citation fully inlined. [Objects 2+5]
5. NO irreducibility verdict is asserted anywhere; NO chaos/NKS argument; the experience-identity is not baked in. The deliverable is a standalone, findable statement document (derivations/69-reducibility-statement.md) a future milestone can open. [Task 5 scans]

FORBIDDEN (must NOT appear): any irreducibility verdict; "nonlinear => chaotic => irreducible" / Lyapunov / NKS; autonomous-vs-driven conflation (autonomous contraction used to claim/deny Stream irreducibility); "reducible := ring-membership" or asymptotic-Kolmogorov redefinition; the baked-in relational-experience identity; the bare decomposition with the P_psd projection silently dropped.
</success_criteria>

<output>
After completion, create `.gpd/phases/69-reducibility-state-the-dynamical-bridge-do-not-prove/69-01-SUMMARY.md`.
Record: the path to the standalone statement (derivations/69-reducibility-statement.md), the verification output (5 assertions, residuals, Tr(X o X^2) rational value), confirmation that all five objects are present and typed, and explicit confirmation that NO irreducibility verdict / chaos argument appears (no-verdict + notation scans clean). This SUMMARY closes Phase 69 and milestone v16.0's statement-only deliverable.
</output>
