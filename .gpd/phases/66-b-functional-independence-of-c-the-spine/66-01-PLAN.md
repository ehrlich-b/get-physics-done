---
phase: 66-b-functional-independence-of-c-the-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [code/spine_independence.py]
interactive: false

conventions:
  field: "exact over Q (SymPy Rational); NEVER float64"
  rank: "exact_qq_rank = DomainMatrix.from_Matrix(A).convert_to(QQ).rank() -- NEVER numpy.linalg.matrix_rank / SVD tolerance"
  group: "F_4 = Aut(h_3(O)), 52-dim (NOT E_6); fixes Tr, trace form, det"
  jordan: "X o Y = (1/2)(XY + YX)  [E.jordan]"
  coupling: "c = Tr(X o Y) = Tr(jordan(Xsym, Ysym)) = E.inv_c; bidegree (1,1); c(X,X) = Tr X^2"
  det_3: "cross-term (x2 x1) x3 (x2 BEFORE x1); d(X,X,X) = 6 det_3  [Phase-64.1 fix, frozen in E.det_3]"
  fano: "e1 e2 = e4"
  f4_basis: "span{[L_a, L_b]}, 52-independent rref subset of the 324 inner derivations  [_f4_basis -> _select_independent_basis(E.inner_derivations())]"
  coordinate_layout: "54 symbols xs = x0:27, ys = y0:27; [alpha,beta,gamma, x1(8), x2(8), x3(8)] per copy"
  generic_point: "integer/rational, all-nonzero, distinct diagonals (distinct spectra), X NOT proportional to Y, X != Y"
  aggregator: "MAX over >= 3 generic points (rank lower-semicontinuous; a special point can only UNDER-report)"
  metric: "Riemannian Fisher (pure algebra; no field theory / gauge / Fourier -- those convention fields are na)"

dimensional_check:
  spine_jacobian_shape: "7 x 54 (7 invariants {6 pointwise + c} x 54 real coordinates = 27 X-block + 27 Y-block)"
  baseline_jacobian_shape: "6 x 54 (pointwise sextet)"
  route2_separating_vector_length: "52 (= dim f_4)"
  route1_rank_verdict: "exactly one of {7 = c INDEPENDENT, 6 = c DEPENDENT}; a 7x54 matrix has rank <= 7"
  orbit_derived_trdeg: "10 (= 54 - pair_orbit_dim 44, Phase 65 CERTIFIED); rank 7 <= 10 (NOT = 10)"

approximations:
  - name: "none -- exact symbolic proof over Q"
    parameter: "n/a"
    validity: "the verdict (7 or 6) is a definite integer computed exactly over Q; no tolerance, no rounding"
    breaks_when: "n/a (pure exact-over-Q algebra)"
    check: "exact_rank_route_crosscheck (QQ-frac == QQ-int == ZZ-int) certifies exact_qq_rank is exact at width 54, not a float proxy"

key_links:
    - from: "code/ring_generating_set.py (CANDIDATE_GRADS[0:7], prefix_rank, PAIR_POINTS)"
      to: "code/spine_independence.py (Route 1: 7x54 sub-Jacobian rank)"
      via: "slice first-7 cached gradients; substitute generic point FIRST then exact_qq_rank"
      check: "prefix_rank(6,.) == 6 and prefix_rank(7,.) MAX over >=3 pairs is the verdict"
    - from: "code/orbit_dimension_gate.py (_f4_basis via _select_independent_basis, infinitesimal_action, exact_qq_rank) + E.jordan/E.Tr/E.X_from_symbols"
      to: "code/spine_independence.py (Route 2: orbit-derivative separating direction)"
      via: "s_i = Tr((xi_i . X) o Y) over the 52 f_4 generators; pointwise derivatives via gradient-contraction"
      check: "s = (s_0..s_51) NOT all zero (separating xi exists) AND all 6 pointwise derivatives == 0 at the SAME point"
    - from: "Route 1 verdict (rank 7 vs 6) + Route 2 verdict (separating xi exists vs none)"
      to: "two-route adjudicator"
      via: "report a verdict ONLY on agreement; STOP (NO VERDICT) on either off-diagonal cell"
      check: "(7, exists) => INDEPENDENT; (6, none) => DEPENDENT + explicit P; (7,none) or (6,exists) => STOP, do not tune"

contract:
  schema_version: 1
  scope:
    question: "On the actual non-associative h_3(O), by exact computation over Q, is c = Tr(X o Y) functionally (= algebraically, char 0) independent of the six pointwise generators {Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y} (Jacobian rank 7, c INDEPENDENT) or expressible in the pointwise ring (rank 6, decisive NEGATIVE)?"
  claims:
    - id: claim-spine-verdict
      statement: "The 7x54 Jacobian of {Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y, c} has a definite exact rank over Q, stable across >= 3 generic rational point-pairs (MAX = the verdict): exactly one of rank 7 (c functionally INDEPENDENT of the six pointwise generators -- positive pass) or rank 6 (c DEPENDENT / in the pointwise ring -- decisive NEGATIVE, full pass)."
      deliverables: [deliv-spine-harness, deliv-verdict]
      acceptance_tests: [test-route1-rank, test-baseline-6, test-rank-stability, test-xeqy-control, test-exactness-crosscheck]
      references: [ref-derksen-kemper, ref-ring-generating-set, ref-warm-engine, ref-garibaldi-guralnick]
    - id: claim-route2-separating
      statement: "An f_4 direction xi exists along the F_4-orbit of X on which c has nonzero derivative D_xi c = Tr((xi.X) o Y) != 0 while every pointwise generator has zero derivative (X-pointwise constant on the orbit by F_4-invariance; Y-pointwise zero since delta Y = 0). This confirms c INDEPENDENT by a route computationally independent of the 7x54 rank."
      deliverables: [deliv-spine-harness, deliv-separating-vector]
      acceptance_tests: [test-route2-separating, test-pointwise-derivs-zero]
      references: [ref-springer-veldkamp, ref-schafer, ref-orbit-gate]
    - id: claim-two-route-agreement
      statement: "A verdict is reported ONLY when Route 1 (rank 7 vs 6) and Route 2 (separating xi exists vs none) AGREE; the off-diagonal cells (7/none, 6/exists) emit NO VERDICT and STOP (the reward-hacking guard)."
      deliverables: [deliv-spine-harness, deliv-verdict]
      acceptance_tests: [test-adjudicator, test-negative-branch-wired]
      references: [ref-ring-generating-set, ref-orbit-gate]
    - id: claim-corrected-consistency
      statement: "rank 7 <= ORBIT_DERIVED_TRDEG (== 10, from Phase 65 pair orbit dim 44). rank 7 saturates the {6 pointwise + c} SUBSET (a 7x54 matrix has rank <= 7) but does NOT saturate the full transcendence degree -- c is the FIRST of FOUR mixed joint invariants {c, Tr(X^2 o Y), Tr(X o Y^2), Tr(X^2 o Y^2)}. The roadmap success-criterion-5 wording 'rank 7 saturates trdeg = 54 - orbit_dim = 7' is STALE."
      deliverables: [deliv-spine-harness, deliv-consistency-note]
      acceptance_tests: [test-consistency-7-le-10]
      references: [ref-phase65-summary, ref-phase651-summary]
  deliverables:
    - id: deliv-spine-harness
      kind: code
      path: code/spine_independence.py
      description: "Pre-registered, foreground-runnable (python -u, chatty) exact-over-Q harness: pre-registration block, Route 1 (7x54 sub-Jacobian rank reusing CANDIDATE_GRADS[0:7]/prefix_rank), Route 2 (52-generator orbit-derivative separating-direction test, NEW), the two-route adjudicator, the NEGATIVE-branch constructor (conditional), and the corrected consistency assert. main() exits 0 iff all checks pass."
      must_contain: ["exact_qq_rank", "prefix_rank", "Tr((xi.X) o Y)", "two-route", "ORBIT_DERIVED_TRDEG", "PRE-REGISTER"]
    - id: deliv-verdict
      kind: report
      path: code/spine_independence.py
      description: "The decisive SPINE verdict printed by the harness: rank (7 or 6), the agreement table, and the verdict label (c INDEPENDENT / c DEPENDENT / NO VERDICT)."
      must_contain: ["VERDICT", "rank"]
    - id: deliv-separating-vector
      kind: report
      path: code/spine_independence.py
      description: "The Route-2 separating vector s = (s_0,...,s_51) with s_i = Tr((xi_i.X) o Y), the index of the first separating xi (the witness), reported exact over Q at each generic pair."
      must_contain: ["separating"]
    - id: deliv-negative-P
      kind: report
      path: code/spine_independence.py
      description: "On the NEGATIVE branch (rank 6) ONLY: the explicit polynomial P = a*(Tr X)(Tr Y) (the unique matching-bidegree pointwise product) with the rational a, verified as a SYMBOLIC identity over Q. Almost certainly a no-op (c(X,X)=Tr X^2 != (Tr X)^2), but wired and reported honestly."
      must_contain: ["(Tr X)(Tr Y)"]
    - id: deliv-consistency-note
      kind: note
      path: code/spine_independence.py
      description: "The corrected consistency statement: rank 7 <= 10 (NOT = 7); c is 1 of 4 mixed joint invariants; the stale roadmap criterion-5 wording explicitly flagged and not used."
      must_contain: ["7 <= 10"]
  references:
    - id: ref-derksen-kemper
      kind: paper
      locator: "Derksen, H.; Kemper, G., Computational Invariant Theory, 2nd ed., Springer (2015), Jacobian-criterion section"
      aliases: ["Derksen-Kemper 2015"]
      role: method
      why_it_matters: "Char-0 Jacobian criterion: trdeg = generic Jacobian rank. THE theorem licensing 'rank 7 <=> the 7 are algebraically (hence functionally) independent <=> c not in R_pt'. Route 1's license."
      applies_to: [claim-spine-verdict]
      carry_forward_to: [planning, verification, writing]
      must_surface: true
      required_actions: [read, use, cite]
    - id: ref-springer-veldkamp
      kind: paper
      locator: "Springer, T.A.; Veldkamp, F.D., Octonions, Jordan Algebras and Exceptional Groups, Springer (2000); Springer, Jordan Algebras and Algebraic Groups (1973)"
      aliases: ["Springer-Veldkamp 2000"]
      role: method
      why_it_matters: "F_4 = automorphisms preserving Tr and the cubic norm => the trace form Tr(X o Y) is F_4-equivariant and non-degenerate on the 26 -- exactly what makes the Route-2 pairing xi -> Tr((xi.X) o Y) a nonzero linear functional for generic X, Y (so the separating direction exists)."
      applies_to: [claim-route2-separating]
      carry_forward_to: [planning, verification, writing]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-schafer
      kind: paper
      locator: "Schafer, R.D., An Introduction to Nonassociative Algebras (1966)"
      aliases: ["Schafer 1966"]
      role: method
      why_it_matters: "Der(h_3(O)) = f_4; inner-derivation formula D_{a,b} = [L_a, L_b] -- grounds the f_4 builder reused for Route 2."
      applies_to: [claim-route2-separating]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [cite]
    - id: ref-garibaldi-guralnick
      kind: paper
      locator: "Garibaldi, S.; Guralnick, R.M., 'Generic Stabilizers for Simple Algebraic Groups', arXiv:2105.09486 (Lemma 8.1); also 2308.08214"
      aliases: ["Garibaldi-Guralnick 2021"]
      role: benchmark
      why_it_matters: "Single-copy F_4-on-26 orbit 24 / Spin(8) / trdeg 3 (reproduced by the certified gate) grounds the 6x54 baseline == 6 cross-check (3 X-block + 3 Y-block)."
      applies_to: [claim-spine-verdict]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [cite, compare]
    - id: ref-ring-generating-set
      kind: prior_artifact
      locator: "code/ring_generating_set.py (Phase 65.1, FROZEN)"
      aliases: ["ring_generating_set"]
      role: method
      why_it_matters: "Holds the ordered 10-candidate list (first 7 = 6 pointwise + c in fixed order), CANDIDATE_GRADS, candidate_jacobian_matrix_at, prefix_rank, check_tier_increments, ORBIT_DERIVED_TRDEG == 10, the _f4_basis builder, and the check_f4_invariance 54-wide diagonal-contraction pattern Route 2 reuses. REUSE verbatim; slice first-7 for Route 1."
      applies_to: [claim-spine-verdict, claim-two-route-agreement, claim-corrected-consistency]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use]
    - id: ref-orbit-gate
      kind: prior_artifact
      locator: "code/orbit_dimension_gate.py (Phase 65, CERTIFIED)"
      aliases: ["orbit_dimension_gate"]
      role: method
      why_it_matters: "The 52-generator f_4 basis builder, exact_qq_rank (the anti-float guard), infinitesimal_action, _select_independent_basis, PAIR_POINTS, the genericity gates (_pair_not_proportional, _is_genuinely_octonionic_integer), and exact_rank_route_crosscheck. REUSE for Route 2 + the exactness cross-check."
      applies_to: [claim-route2-separating, claim-two-route-agreement]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use]
    - id: ref-warm-engine
      kind: prior_artifact
      locator: "code/ring_lemma_verification.py (Phase 64, FROZEN; imported as module E)"
      aliases: ["E", "ring_lemma_verification"]
      role: method
      why_it_matters: "Octonion + h_3(O) primitives: jordan, Tr, Tr2, det_3, c(X,Y), Xsym/Ysym, xs/ys, X_from_symbols, _flat27, octonionic_points, inner_derivations, the 7 inv_* invariants. REUSE all primitives; the trace form Tr(A o B) = E.Tr(E.jordan(A, B)) is c when A=X, B=Y."
      applies_to: [claim-spine-verdict, claim-route2-separating]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [read, use]
    - id: ref-phase65-summary
      kind: prior_artifact
      locator: "Phase 65 SUMMARY / GATE result (pair orbit dim 44 => trdeg = 54 - 44 = 10)"
      aliases: ["Phase 65 GATE"]
      role: must_consider
      why_it_matters: "The CORRECTED trdeg target (10, not the naive Spin(8)-triality 7). The basis of the ITEM-4a consistency reframing: rank 7 <= 10, NOT = 10."
      applies_to: [claim-corrected-consistency]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [read, use]
    - id: ref-phase651-summary
      kind: prior_artifact
      locator: "Phase 65.1 SUMMARY (10-candidate set Jacobian rank 10; r6==6, r7==7 SPINE preview; ITEM-4a correction verbatim)"
      aliases: ["Phase 65.1"]
      role: must_consider
      why_it_matters: "States the r7==7 SPINE preview already holds AND the corrected-consistency wording verbatim ('rank 7 no longer saturates trdeg; (b) unaffected/strengthened'). Phase 66 RE-DEMONSTRATES r7==7 here as its own pre-registered decisive result, not merely citing the preview."
      applies_to: [claim-spine-verdict, claim-corrected-consistency]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [read, compare]
  acceptance_tests:
    - id: test-route1-rank
      subject: claim-spine-verdict
      kind: benchmark
      procedure: "Route 1: for each generic pair in PAIR_POINTS (>= 3, all gated _pair_not_proportional with distinct diagonals), build the 7x54 sub-Jacobian from CANDIDATE_GRADS[0:7] (substitute the integer point FIRST, then exact_qq_rank); equivalently prefix_rank(7, pp). Take MAX over points."
      pass_condition: "MAX over >= 3 generic pairs is a definite integer == 7 (c INDEPENDENT) OR == 6 (c DEPENDENT). Verdict reported honestly whichever fires."
      evidence_required: [deliv-spine-harness, deliv-verdict, ref-derksen-kemper, ref-ring-generating-set]
      automation: automated
    - id: test-baseline-6
      subject: claim-spine-verdict
      kind: consistency
      procedure: "Route 1 baseline control: prefix_rank(6, pp) (the pointwise sextet, CANDIDATE_GRADS[0:6]) at every generic pair."
      pass_condition: "exactly 6 at every generic pair (3 X-block + 3 Y-block, Garibaldi-Guralnick). If != 6, the engine/builder is broken -- STOP, do NOT tune."
      evidence_required: [deliv-spine-harness, ref-garibaldi-guralnick]
      automation: automated
    - id: test-rank-stability
      subject: claim-spine-verdict
      kind: consistency
      procedure: "Compute prefix_rank(7, pp) at EACH of >= 3 generic pairs; compare."
      pass_condition: "the SAME rank at every generic pair (rank lower-semicontinuous; MAX is the generic value). Instability => a secretly non-generic pair (check _pair_not_proportional + distinct diagonals) before doubting the engine."
      evidence_required: [deliv-spine-harness]
      automation: automated
    - id: test-xeqy-control
      subject: claim-spine-verdict
      kind: limiting_case
      procedure: "Degeneracy control: evaluate prefix_rank(7, (P, P)) at a diagonal pair X = Y."
      pass_condition: "<= 6 (X=Y collapses c to Tr X^2 in R_pt). This is a control EXPECTED to fail to reach 7 -- NOT a counterexample, NOT a valid independence-test point."
      evidence_required: [deliv-spine-harness]
      automation: automated
    - id: test-exactness-crosscheck
      subject: claim-spine-verdict
      kind: cross_method
      procedure: "Run exact_rank_route_crosscheck on the 7x54 at one generic pair (QQ-on-fractional vs QQ-on-integer-cleared vs ZZ-on-integer-cleared)."
      pass_condition: "all three exact domains agree (QQ-frac == QQ-int == ZZ-int), certifying exact_qq_rank is genuinely exact at width 54, not a float proxy."
      evidence_required: [deliv-spine-harness, ref-orbit-gate]
      automation: automated
    - id: test-route2-separating
      subject: claim-route2-separating
      kind: existence
      procedure: "Route 2: for each generic pair, build the 52 f_4 basis matrices (_f4_basis); for each xi_i compute s_i = Tr((xi_i . X) o Y) exact over Q (orbit tangent t_i = M_i.v_x, reconstruct T_i = E.X_from_symbols(t_i), s_i = E.Tr(E.jordan(T_i, Y_mat)); the cheaper gradient-contraction grad_X(c).(M_i.v_x) must agree). Assert the vector s = (s_0..s_51) NOT all zero; record the first nonzero index (the witness xi)."
      pass_condition: "at a generic pair, some s_i != 0 as a rational (a separating direction exists) => c varies along the F_4-orbit of X while the pointwise sextet is pinned. Checked at ALL generic PAIR_POINTS (guards a Route-2 false positive)."
      evidence_required: [deliv-spine-harness, deliv-separating-vector, ref-springer-veldkamp]
      automation: automated
    - id: test-pointwise-derivs-zero
      subject: claim-route2-separating
      kind: symmetry
      procedure: "Confirm every pointwise generator has zero derivative along orbit tangents: X-pointwise {Tr X, Tr X^2, det X} via grad_X(f).(M_i.v_x) for all i (reuses infinitesimal_action / CANDIDATE_GRADS[0:3] X-blocks); Y-pointwise {Tr Y, Tr Y^2, det Y} by asserting their X-block gradient CANDIDATE_GRADS[3:6][0:27] == 0 (delta Y = 0)."
      pass_condition: "all 6 pointwise directional derivatives == 0 over Q at the SAME generic point where c separates (X-pointwise by certified F_4-invariance; Y-pointwise trivially). Any nonzero D_M f for a pointwise generator => port/basis bug, STOP."
      evidence_required: [deliv-spine-harness, ref-orbit-gate]
      automation: automated
    - id: test-adjudicator
      subject: claim-two-route-agreement
      kind: cross_method
      procedure: "The two-route adjudicator: compare Route-1 verdict (rank 7 vs 6) against Route-2 verdict (separating xi exists vs none) against the pre-registered agreement table."
      pass_condition: "report a verdict ONLY on a diagonal cell: (7, exists) => c INDEPENDENT; (6, none) => c DEPENDENT. On either off-diagonal cell (7/none or 6/exists) the harness emits NO VERDICT and STOPs (do NOT report, do NOT tune; isolate by diffing against check_f4_invariance)."
      evidence_required: [deliv-spine-harness, deliv-verdict]
      automation: automated
    - id: test-negative-branch-wired
      subject: claim-two-route-agreement
      kind: existence
      procedure: "NEGATIVE-branch constructor (conditional): IF rank 6, by bidegree (1,1) the ONLY matching pointwise product is (Tr X)(Tr Y); solve c - a*(Tr X)(Tr Y) for the rational a at >= 2 generic pairs and verify simplify(E.inv_c - a*E.inv_Tr_X*E.inv_Tr_Y) == 0 as a SYMBOLIC identity over Q. The branch is WIRED and exercised (code present and reachable) regardless of which verdict fires."
      pass_condition: "the constructor code exists and is reachable on the rank-6 branch; if rank 6 fires, an explicit verified P (or, if no rational a makes it an identity, a STOP flagging the rank-6 finding as a non-generic-point artifact) is reported. On the rank-7 branch the constructor is a recorded no-op."
      evidence_required: [deliv-spine-harness, deliv-negative-P]
      automation: automated
    - id: test-consistency-7-le-10
      subject: claim-corrected-consistency
      kind: consistency
      procedure: "Assert the verdict rank <= ORBIT_DERIVED_TRDEG (== 10, imported / re-derived as 54 - 44 from Phase 65). Print the corrected statement; do NOT write '= 7 saturates trdeg'."
      pass_condition: "rank 7 <= 10 (CONSISTENT; c is the FIRST of four mixed joint invariants, does NOT saturate the full trdeg). The stale roadmap criterion-5 wording is explicitly flagged as superseded."
      evidence_required: [deliv-spine-harness, deliv-consistency-note, ref-phase65-summary, ref-phase651-summary]
      automation: automated
  forbidden_proxies:
    - id: fp-assert-without-demonstration
      subject: claim-spine-verdict
      proxy: "Asserting c independent by appeal to theory (it 'should' be, the preview says so) WITHOUT actually running the orbit/Jacobian computation on the actual h_3(O)."
      reason: "Pitfall 2. The milestone demands a DEMONSTRATED computation on the algebra; both routes must be computed exactly over Q and the adjudicator must see agreement."
    - id: fp-float-rank
      subject: claim-spine-verdict
      proxy: "numpy.linalg.matrix_rank / sympy.Matrix.rank() float fallback / any SVD-tolerance rank on the decisive path."
      reason: "Pitfall 3, the GLOBAL forbidden proxy. Rank is discontinuous; a float tolerance fabricates the 6-vs-7 verdict. Decisive rank = exact_qq_rank (DomainMatrix over QQ) ONLY; the exact-only source guard asserts 0 numpy float-rank calls and 0 octonion_algebra imports on the decisive path."
    - id: fp-non-generic-point
      subject: claim-spine-verdict
      proxy: "Using a non-generic evaluation point as a TEST point: X=Y, X proportional to Y, or repeated diagonal spectra (any of which DROPS the rank)."
      reason: "Pitfall 4. These under-report the rank. Gate every test pair with _pair_not_proportional + distinct diagonals; X=Y is run ONLY as a control EXPECTED to give <= 6, never as evidence for/against independence."
    - id: fp-force-positive
      subject: claim-two-route-agreement
      proxy: "Forcing a positive over an honest NEGATIVE: re-rolling points until rank 7 appears, or tuning a point/invariant to make the routes agree."
      reason: "Pitfall 5. Defeated by PRE-REGISTERING PAIR_POINTS + the exact rank test BEFORE evaluating, MAX-over-fixed-points, the fully-wired NEGATIVE-branch constructor, and the two-route agreement gate (off-diagonal => STOP, do not tune)."
    - id: fp-rank-before-substitution
      subject: claim-spine-verdict
      proxy: "Taking the rank of the SYMBOLIC 54-variable Jacobian (or simplify-ing the cached gradients) before substituting the rational point."
      reason: "Expression swell stalls det_3 (deg 3) and the 54-var gradients. Substitute the integer point FIRST (the cached CANDIDATE_GRADS are deliberately un-simplified), then rank."
    - id: fp-route2-false-positive
      subject: claim-route2-separating
      proxy: "Declaring a separating direction from a single non-generic Route-2 point, or accepting s != 0 without confirming the pointwise derivatives vanish at the SAME point."
      reason: "A diagonal-locus cancellation or wrong gradient block could spuriously fire (or kill) s. Check s at ALL generic PAIR_POINTS, confirm the 6 pointwise derivatives are 0 at the same point, and cross-check the contraction against the check_f4_invariance 54-wide split."
    - id: fp-two-route-mismatch-ignored
      subject: claim-two-route-agreement
      proxy: "Reporting a verdict when the two routes disagree (e.g. Route 1 says 7 but Route 2 finds no separating xi)."
      reason: "The adjudicator MUST emit NO VERDICT + STOP on the off-diagonal cells; a single route 'passing' is insufficient (this is the reward-hacking guard)."
    - id: fp-det3-port-slip
      subject: claim-spine-verdict
      proxy: "Re-implementing det_3 / jordan / the f_4 builder from scratch (risking the wrong cross-term order (x1 x2) x3, which is NOT F_4-invariant)."
      reason: "A port slip silently corrupts the rank. Inherit the FROZEN, certified primitives from E (Phase-64.1 det_3 fix) and the certified gate; do NOT rebuild the engine."
    - id: fp-stale-saturation
      subject: claim-corrected-consistency
      proxy: "Stating consistency as 'rank 7 = 54 - orbit_dim = 7 saturates trdeg' (the stale roadmap criterion-5 / forbidden Spin(8)-triality back-of-envelope)."
      reason: "Phase 65 COMPUTED pair orbit dim = 44 => trdeg = 10. The correct statement is rank 7 <= 10 (c is 1 of 4 mixed invariants, does NOT saturate). Writing '= 7' reintroduces the refuted naive value."
  uncertainty_markers:
    weakest_anchors:
      - "Genericity is empirical (chosen PAIR_POINTS), not a proven Zariski-open statement; a pathological common non-generic locus across all chosen points is not formally excluded (only made very unlikely by MAX over >= 3 gated-generic points + the explicit X=Y degeneracy control + a fresh-pair re-confirmation)."
      - "Route 2's computational independence from Route 1: they are linked by the trdeg = 54 - orbit_dim duality and share the certified f_4 basis + engine (a builder bug would fail both -- mitigated by the upstream GATE certification and the r6==6 control). They remain computationally distinct questions (full-gradient rank vs trace-form pairing of orbit tangents)."
    disconfirming_observations:
      - "prefix_rank(6,.) != 6 at a generic point (engine/builder bug; Garibaldi-Guralnick says 6) -- STOP, do not tune."
      - "prefix_rank(7,.) differs across generic points (a point is secretly non-generic) -- recheck _pair_not_proportional + distinct diagonals before doubting the engine."
      - "Route-2 separating vector s identically 0 at a generic point while Route 1 says 7 (or s != 0 while Route 1 says 6) -- two-route mismatch => NO VERDICT, STOP, diff against check_f4_invariance."
      - "Any nonzero D_M f for a pointwise generator along an orbit tangent -- certified F_4-invariance violated => port/basis bug, STOP."
      - "A rank-6 result with NO rational a making c = a*(Tr X)(Tr Y) a symbolic identity -- the rank-6 finding is itself a non-generic-point artifact, STOP and re-examine the point (do not report a NEGATIVE)."
---

<objective>
Demonstrate, by exact computation over Q on the actual non-associative h_3(O), whether the coupling c = Tr(X o Y) is functionally (= algebraically, in char 0) INDEPENDENT of the six pointwise generators {Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y}. This is THE SPINE -- the single load-bearing result of milestone v16.0.

The verdict is exactly one of:
- rank 7 => c INDEPENDENT (positive pass: "the complete third-person single-frame record does not determine Phi"), OR
- rank 6 => c DEPENDENT (decisive NEGATIVE, equally a full pass: c expressible in the pointwise ring, kills the Phi mechanism).

Both branches are reported honestly. A NEGATIVE ships the explicit pointwise expression for c. The verdict is reached by TWO INDEPENDENT MANDATORY ROUTES that must AGREE (reward-hacking guard).

Purpose: RING-02 (the SPINE) -- the decisive Jacobian-rank verdict that grounds "the complete single-frame record does not determine Phi". The chain-critical contract item of v16.0.
Output: code/spine_independence.py -- a single pre-registered, foreground-runnable, chatty exact-over-Q harness producing the rank, the Route-2 separating vector, the two-route agreement table, the (conditional) NEGATIVE-branch P, and the corrected consistency statement; main() exits 0 iff all checks pass.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/66-b-functional-independence-of-c-the-spine/66-RESEARCH.md
@code/ring_generating_set.py
@code/orbit_dimension_gate.py
@code/ring_lemma_verification.py
</context>

<execution_environment_note>
RUN THE HARNESS FOREGROUND with `python -u` (unbuffered) and PRINT progress between every check (per-pair, per-route) so it never goes silent: the executor's stream-watchdog kills long no-output symbolic runs (~150s precedent in Phase 65.1) at 600s. Each rank is exact-symbolic over Q at width 54 -- substitute the generic integer point FIRST (the cached CANDIDATE_GRADS are deliberately un-simplified) so each evaluation is bounded (~0.01s per rank via exact_qq_rank). REUSE the frozen engine -- do NOT rebuild octonion/jordan/f_4/exact_qq_rank/det_3 from scratch. The harness imports `ring_lemma_verification as E`, the gate functions, and the candidate machinery exactly as `ring_generating_set.py` does (sys.path insert then import). If a background resume stalls, the orchestrator can commit + SUMMARY on stall (per project memory).
</execution_environment_note>

<tasks>

<task type="auto">
  <name>Task 1: PRE-REGISTER the exact test (points + rank test + verdict map) BEFORE any evaluation</name>
  <files>code/spine_independence.py</files>
  <action>
    Create the harness skeleton and the pre-registration block FIRST, before any rank is computed, so an honest NEGATIVE cannot be re-rolled (defeats fp-force-positive).

    1. Header: module docstring + an ASSERT_CONVENTION line restating the frozen conventions (jordan 1/2; c = Tr(X o Y); det_3 cross-term (x2 x1) x3; f4 = span{[L_a,L_b]} dim 52; arithmetic exact over Q; ranks = exact_qq_rank, NEVER float). sys.path insert + `import ring_lemma_verification as E`, and import from orbit_dimension_gate: exact_qq_rank, infinitesimal_action, _select_independent_basis, PAIR_POINTS, _pair_not_proportional, _is_genuinely_octonionic_integer, exact_rank_route_crosscheck; from ring_generating_set: CANDIDATE_GRADS, prefix_rank, candidate_jacobian_matrix_at, _f4_basis, ORBIT_DERIVED_TRDEG, check_tier_increments. Print SymPy version and confirm imports.
    2. PRE-REGISTRATION block (printed at startup, BEFORE evaluation):
       - The fixed test points: TEST_PAIRS = the >= 3 generic pairs from orbit_dimension_gate.PAIR_POINTS, PLUS at least ONE FRESH generic integer pair defined inline (all-nonzero coords; X, Y each distinct-diagonal / distinct-spectrum; X NOT proportional to Y; X != Y) that is NOT in PAIR_POINTS. Gate EVERY pair with assert _pair_not_proportional(X, Y) and assert distinct-diagonal (and _is_genuinely_octonionic_integer where applicable); print the genericity gate result per pair.
       - The rank test: exact_qq_rank (DomainMatrix over QQ); the aggregator: MAX over the fixed TEST_PAIRS.
       - The verdict map (frozen here, not after seeing the rank): SPINE_RANK == 7 -> "c INDEPENDENT"; SPINE_RANK == 6 -> "c DEPENDENT (NEGATIVE)"; anything else -> "ANOMALY / STOP".
       - Pre-register ORBIT_DERIVED_TRDEG (== 10) as the consistency CHECK constant.
    3. Define a chatty `_report(label, ok)` (reuse the pattern from the gate) and a module-level results accumulator. Stub `main()` that will call the per-task checks in order and exit 0 iff all pass.

    Do NOT compute any rank in this task -- this task ONLY fixes the points, the test, and the verdict map. The FRESH pair is mandatory (orchestrator/verifier-independent re-confirmation, mirroring Phase 65/65.1 hardening).
  </action>
  <verify>
    1. The pre-registration block prints BEFORE any rank computation (grep the source: the verdict map and TEST_PAIRS are defined above any exact_qq_rank / prefix_rank call).
    2. >= 3 PAIR_POINTS + >= 1 FRESH inline pair, each passing _pair_not_proportional AND distinct-diagonal (print PASS per pair); the fresh pair is NOT a member of PAIR_POINTS.
    3. The verdict map is frozen as code (7->INDEPENDENT, 6->DEPENDENT, else->STOP) before evaluation.
    4. Imports resolve; ASSERT_CONVENTION line present; no float-rank symbol (numpy.linalg / .rank()) anywhere on the decisive path.
  </verify>
  <done>Harness skeleton + pre-registration block written: fixed TEST_PAIRS (>= 3 PAIR_POINTS + >= 1 gated FRESH pair), exact_qq_rank as the rank test, MAX aggregator, frozen verdict map (7/6/else), ORBIT_DERIVED_TRDEG==10 pre-registered. No rank computed yet.</done>
</task>

<task type="auto">
  <name>Task 2: Route 1 -- exact 7x54 sub-Jacobian rank over Q + baseline + X=Y control + exactness cross-check</name>
  <files>code/spine_independence.py</files>
  <action>
    Implement Route 1 by REUSING the frozen candidate machinery (slice first-7). Substitute each generic point FIRST, then exact_qq_rank.

    1. SPINE rank: for each pair in TEST_PAIRS, compute the 7x54 sub-Jacobian rank = prefix_rank(7, pp) (rows = CANDIDATE_GRADS[0:7] = 6 pointwise + c, fixed order; substitute-first then exact_qq_rank). Print the per-pair rank. Set SPINE_RANK = MAX over TEST_PAIRS (rank lower-semicontinuous; a special point can only UNDER-report).
    2. Rank stability: assert prefix_rank(7, pp) is the SAME at every generic pair (instability => a secretly non-generic pair; print which pair if it differs and STOP rather than tune).
    3. 6x54 baseline control: for each pair, prefix_rank(6, pp) (CANDIDATE_GRADS[0:6], the pointwise sextet); assert == 6 at every generic pair (3 X-block + 3 Y-block, Garibaldi-Guralnick). If != 6 -> engine/builder bug, STOP, do NOT tune.
    4. X=Y degeneracy control: pick a diagonal pair (P, P) (P from octonionic_points / a generic single point) and compute prefix_rank(7, (P, P)); assert <= 6. This is a control EXPECTED to fail to reach 7 (X=Y collapses c to Tr X^2 in R_pt) -- NOT a counterexample, NOT used as a test point.
    5. Exactness cross-check: run exact_rank_route_crosscheck on the 7x54 at ONE generic pair (build the 7x54 via candidate_jacobian_matrix_at(pp)[0:7, :] or stack CANDIDATE_GRADS[0:7]) and assert QQ-frac == QQ-int == ZZ-int (certifies exact_qq_rank is exact at width 54, not a float proxy). If exact_rank_route_crosscheck expects the full candidate stack, adapt it to the 7-row slice or replicate its three-domain logic on the 7x54.

    Print each result as it computes (watchdog discipline). Record SPINE_RANK and per-pair ranks in the accumulator for the adjudicator (Task 4).
  </action>
  <verify>
    1. SPINE_RANK is a definite integer (7 or 6), computed as MAX over >= 3 generic pairs + the fresh pair, via exact_qq_rank (NOT float).
    2. prefix_rank(7, pp) is STABLE (same) across all generic pairs.
    3. prefix_rank(6, pp) == 6 at every generic pair (baseline; Garibaldi-Guralnick).
    4. X=Y control: prefix_rank(7, (P,P)) <= 6 (degenerate, excluded as test point).
    5. exact_rank_route_crosscheck on the 7x54: QQ-frac == QQ-int == ZZ-int (exactness certified at width 54).
    6. The 7x54 shape is correct (7 rows {6 pointwise + c}, 54 columns = 27 X + 27 Y).
  </verify>
  <done>Route 1 complete: SPINE_RANK (= MAX prefix_rank(7,.)) a definite integer over Q, rank-stable across all generic pairs; baseline == 6; X=Y control <= 6; exactness three-domain cross-check passes at width 54. All exact over Q, no float rank.</done>
</task>

<task type="auto">
  <name>Task 3: Route 2 (NEW) -- orbit-derivative separating-direction test over the 52 f_4 generators</name>
  <files>code/spine_independence.py</files>
  <action>
    Implement the genuinely NEW Route 2: an infinitesimal orbit-derivative separating-direction argument, computationally independent of the 7x54 Jacobian rank. REUSE the certified f_4 basis + the check_f4_invariance 54-wide diagonal-contraction pattern.

    The argument (state in the docstring): along the F_4-orbit of X (tangent T_X O_X = {xi.X : xi in f_4}, fixed Y), every pointwise generator is constant (X-pointwise by F_4-invariance; Y-pointwise since delta Y = 0), while D_xi c = Tr((xi.X) o Y). If c were in R_pt, c would be constant on the orbit (D_xi c = 0 for ALL xi). Exhibiting one xi with D_xi c != 0 contradicts that => c functionally independent. The trace form is F_4-equivariant and non-degenerate on the 26 (Springer-Veldkamp), and generic single-copy orbit dim = 24 > 0 (certified gate), so a separating xi generically exists -- the computation CONFIRMS non-vanishing at the chosen generic points, it does not assume it.

    Recipe, for each generic pair (X, Y) in TEST_PAIRS:
    1. v_x = Matrix(E._flat27(X)), v_y = Matrix(E._flat27(Y)); Y_mat = E.X_from_symbols(list(v_y)).
    2. f4 = _f4_basis() (52 independent 27x27 rational matrices; reuse verbatim).
    3. Separating vector s for c: for each xi_i = f4[i] (i = 0..51) compute the orbit tangent t_i = M_i * v_x (27-vector = xi_i.X in coords); reconstruct T_i = E.X_from_symbols(list(t_i)); s_i = E.Tr(E.jordan(T_i, Y_mat)) -- exact over Q. ALSO compute the cheaper gradient-contraction form g_i = grad_X(c).(M_i * v_x) using CANDIDATE_GRADS[6][0:27] substituted at the point (matches the gate's contraction); ASSERT s_i == g_i for all i (the explicit trace-form form is the conceptual check on the gradient form). Length-52 vector.
    4. Separating-direction exists iff s is NOT all zero: assert any(s_i != 0) as a rational; record the first nonzero index i* (the witness xi). Set ROUTE2_VERDICT = "exists" if separating at a generic pair, else "none".
    5. Pointwise derivatives vanish along orbit tangents (at the SAME generic point):
       - X-pointwise {Tr X, Tr X^2, det X}: for each i, D_{xi_i} f = grad_X(f).(M_i * v_x) using CANDIDATE_GRADS[0:3] X-blocks (reuse infinitesimal_action(grad_X_at, M_i, v_x)); assert == 0 for all i.
       - Y-pointwise {Tr Y, Tr Y^2, det Y}: assert CANDIDATE_GRADS[3:6] X-block ([0:27]) is identically the zero vector (delta Y = 0 => zero derivative trivially).
    6. Check s at ALL generic PAIR_POINTS + the fresh pair (guards fp-route2-false-positive: a single diagonal-locus cancellation cannot mask the verdict). Print s's first-nonzero index and the killed-count of pointwise derivatives per pair.

    Use the gradient-contraction form for speed; keep the explicit Tr(jordan(.,.)) form as the asserted cross-check. Substitute the point FIRST. Print per-pair progress (watchdog).
  </action>
  <verify>
    1. The separating vector s = (s_0..s_51) has length 52 (= dim f_4) and is NOT all zero at a generic pair => a separating xi exists; record the witness index.
    2. s_i (explicit trace-form Tr((xi_i.X) o Y)) == g_i (gradient-contraction grad_X(c).(M_i.v_x)) for all i at every pair (the two Route-2 forms agree).
    3. All 6 pointwise directional derivatives == 0 over Q at the SAME generic point: X-pointwise {Tr X, Tr X^2, det X} via grad.(M.v_x) for all 52 generators; Y-pointwise X-block gradient == 0 vector.
    4. s is checked at ALL generic PAIR_POINTS + the fresh pair (not just one) -- consistent verdict across points.
    5. ROUTE2_VERDICT in {"exists", "none"} recorded for the adjudicator.
    6. No nonzero D_M f for any pointwise generator (else port/basis bug -> STOP).
  </verify>
  <done>Route 2 complete and independent of Route 1: separating vector s (length 52) computed exact over Q at all generic pairs, NOT all zero (witness xi recorded), explicit trace-form == gradient-contraction; all 6 pointwise derivatives == 0 at the same point. ROUTE2_VERDICT = exists (or none) recorded.</done>
</task>

<task type="auto">
  <name>Task 4: Two-route adjudicator (reward-hacking guard) + corrected consistency assert (7 <= 10)</name>
  <files>code/spine_independence.py</files>
  <action>
    Implement the adjudicator and the corrected consistency check. The adjudicator emits a verdict ONLY on agreement.

    1. Pre-registered agreement table (as code, matching the RESEARCH adjudication table):
       - (Route1 == 7) AND (Route2 == "exists")  -> VERDICT = "c INDEPENDENT (positive pass)"; report rank 7 + witness xi + s; (the baseline==6 and X=Y<=6 controls from Task 2 are already asserted).
       - (Route1 == 6) AND (Route2 == "none")     -> VERDICT = "c DEPENDENT (decisive NEGATIVE, full pass)"; trigger the Task 5 constructor.
       - (Route1 == 7) AND (Route2 == "none")     -> NO VERDICT; print "CONTRADICTION -- STOP" and a diff-hint (compare the Route-2 contraction to check_f4_invariance's 54-wide split); do NOT report, do NOT tune; main() returns nonzero.
       - (Route1 == 6) AND (Route2 == "exists")   -> NO VERDICT; print "CONTRADICTION -- STOP" (likely a non-generic Route-1 point dropping rank, or a Route-2 false positive); main() returns nonzero.
       - any other Route1 (not 6 or 7) -> ANOMALY -> STOP.
       Print the full 2x2 agreement table with the observed cell highlighted.
    2. Corrected consistency assert (ITEM 4a): assert SPINE_RANK <= ORBIT_DERIVED_TRDEG (== 10). Print the CORRECT statement verbatim: "rank 7 <= trdeg 10 (CONSISTENT). c is ONE of the FOUR mixed joint invariants {c, Tr(X^2 o Y), Tr(X o Y^2), Tr(X^2 o Y^2)}; rank 7 saturates the {6 pointwise + c} SUBSET (7x54 maxes at 7) but does NOT saturate the full transcendence degree." Explicitly flag that the roadmap success-criterion-5 wording "rank 7 saturates trdeg = 54 - orbit_dim = 7" is STALE (the forbidden Spin(8)-triality back-of-envelope) and is NOT used. Do NOT write "= 7 saturates trdeg".
    3. r6==6 / r7==7 corroboration (optional, cheap): call ring_generating_set.check_tier_increments(...) head or recompute r6,r7 to confirm the committed Phase-65.1 preview (6, 7, ...) still holds. Print as corroboration (NOT the decisive result -- the decisive result is THIS phase's pre-registered Route-1 MAX + Route-2).

    The adjudicator's output is the SPINE deliverable: the verdict, the agreement table, the witness, and the corrected consistency line.
  </action>
  <verify>
    1. A verdict is reported ONLY on a diagonal cell ((7,exists) or (6,none)); both off-diagonal cells ((7,none),(6,exists)) print "CONTRADICTION -- STOP" and force main() nonzero (no verdict, no tuning).
    2. The 2x2 agreement table is printed with the observed cell identified.
    3. SPINE_RANK <= 10 asserted; the corrected "7 <= 10" statement printed; the stale "= 7 saturates trdeg" wording explicitly flagged as superseded and NOT used.
    4. r6==6, r7==7 corroboration matches the Phase-65.1 preview (printed as corroboration, not as the decisive result).
  </verify>
  <done>Adjudicator wired: verdict emitted ONLY on two-route agreement (off-diagonal => NO VERDICT + STOP + nonzero exit); 2x2 table printed; corrected consistency 7 <= 10 asserted with the stale criterion-5 wording flagged; r6==6/r7==7 corroboration confirmed.</done>
</task>

<task type="auto">
  <name>Task 5: NEGATIVE-branch constructor (conditional; explicit P = a*(Tr X)(Tr Y), verified as a symbolic identity)</name>
  <files>code/spine_independence.py</files>
  <action>
    Wire the NEGATIVE-branch constructor so a rank-6 outcome SHIPS the explicit pointwise expression for c (success criterion 4). This branch is reachable only when the adjudicator (Task 4) returns the (6, none) cell; on the (7, exists) cell it is a recorded no-op. The code must be PRESENT and reachable regardless of which verdict fires.

    Bidegree pins P to ONE candidate: c has bidegree (1,1); the pointwise generators have bidegrees Tr X (1,0), Tr X^2 (2,0), det X (3,0), Tr Y (0,1), Tr Y^2 (0,2), det Y (0,3). The ONLY product of pointwise generators with bidegree (1,1) is (Tr X)(Tr Y). So necessarily P = a*(Tr X)(Tr Y) for a single rational a.

    Constructor (function present; executed only on the rank-6 branch):
    1. Evaluate c and m := (Tr X)(Tr Y) at >= 2 generic rational pairs (reuse TEST_PAIRS via substitution into E.inv_c and E.inv_Tr_X*E.inv_Tr_Y); solve c - a*m = 0 for the rational a (over-determined => consistency is itself a check; if the candidate a's disagree across pairs, report STOP -- no single a).
    2. Verify the IDENTITY, not just the points: assert simplify(E.inv_c - a*E.inv_Tr_X*E.inv_Tr_Y) == 0 as a symbolic identity over Q. If it holds, c = a*(Tr X)(Tr Y) and the NEGATIVE is constructed -- print the explicit P with the rational a.
    3. If NO rational a makes it vanish identically, then c is NOT expressible in the matching-bidegree pointwise product, which CONTRADICTS a rank-6 finding -- print "CONTRADICTION: rank-6 finding is a non-generic-point artifact -- STOP, re-examine the point", and force nonzero exit (do NOT report a NEGATIVE).

    Foreshadowing (print as a no-op note on the positive branch): c(X,X) = Tr X^2 != (Tr X)^2, so c differs from (Tr X)(Tr Y) on the diagonal -- the constructor is expected to be a no-op (rank 7).
  </action>
  <verify>
    1. The NEGATIVE-branch constructor function EXISTS and is reachable on the (6, none) adjudicator cell (grep the source: the function is called from the rank-6 path).
    2. If exercised (rank 6): an explicit P = a*(Tr X)(Tr Y) is produced with the rational a AND verified as a SYMBOLIC identity over Q (not merely point-matched); OR, if no such a is an identity, the harness prints the rank-6-artifact CONTRADICTION and exits nonzero.
    3. On the (7, exists) cell: the constructor is a recorded no-op, with the c(X,X)=Tr X^2 != (Tr X)^2 foreshadowing printed.
    4. The bidegree argument (only (Tr X)(Tr Y) matches (1,1)) is stated in the code/comment.
  </verify>
  <done>NEGATIVE-branch constructor wired and reachable: on rank 6, produces and SYMBOLICALLY verifies P = a*(Tr X)(Tr Y) (or flags a rank-6 artifact + STOP); on rank 7, a recorded no-op with the c(X,X)!=(Tr X)^2 foreshadowing. Honest reporting of either branch.</done>
</task>

<task type="auto">
  <name>Task 6: Assemble main(), exit-0-iff-all-pass, fresh-pair re-confirmation, exact-only source guard</name>
  <files>code/spine_independence.py</files>
  <action>
    Assemble the full harness and enforce the run discipline.

    1. main() calls, in order: pre-registration print (Task 1) -> Route 1 (Task 2) -> Route 2 (Task 3) -> adjudicator + corrected consistency (Task 4) -> NEGATIVE-branch constructor (Task 5, conditional). Collect every _report ok/fail into the accumulator. main() returns 0 iff ALL checks pass AND a verdict was emitted on a diagonal agreement cell; returns nonzero on any failed check, any off-diagonal (NO VERDICT) cell, or any STOP.
    2. Fresh-pair re-confirmation: ensure BOTH routes are evaluated at the fresh inline generic pair from Task 1 (not only PAIR_POINTS) -- the orchestrator/verifier-independent re-confirmation (rank 7 + separating xi at the fresh pair), mirroring the Phase-65/65.1 hardening. Print the fresh-pair results explicitly.
    3. Exact-only source guard (reuse the pattern from orbit_dimension_gate.exact_only_guard / ring_generating_set.exact_only_guard): assert this module makes 0 numpy.linalg / numpy float-rank / .rank()-float calls on the decisive path and 0 `octonion_algebra` imports (the float64 module must NEVER touch the decisive ranks). Run it as a check inside main().
    4. Watchdog discipline: every check prints a one-line progress marker as it runs (per-pair, per-route); the run must never go silent for long. Add a top-of-main print of the pre-registration and a bottom-of-main print of the final VERDICT + agreement table.
    5. `if __name__ == "__main__": sys.exit(0 if main() else 1)`.

    Confirm the harness runs FOREGROUND with `python -u code/spine_independence.py` end-to-end (well under a minute), prints progress throughout, and exits 0.
  </action>
  <verify>
    1. `python -u code/spine_independence.py` runs foreground end-to-end (well under a minute), printing per-pair / per-route progress (never silent), and exits 0.
    2. main() returns 0 IFF all checks pass AND a verdict was emitted on a diagonal agreement cell (off-diagonal / STOP => nonzero).
    3. Both routes are re-confirmed at the FRESH inline pair (not only PAIR_POINTS); the fresh-pair rank 7 + separating xi printed.
    4. The exact-only source guard passes: 0 numpy float-rank calls, 0 octonion_algebra imports on the decisive path.
    5. Final output prints: SPINE_RANK, the 2x2 agreement table, the VERDICT (c INDEPENDENT expected), the witness xi, and the corrected consistency line (7 <= 10).
  </verify>
  <done>Full harness assembled: main() runs both routes (incl. the fresh pair) + adjudicator + consistency + NEGATIVE constructor, exits 0 iff all pass and a verdict is emitted on agreement; exact-only guard passes; foreground `python -u` run is chatty and exits 0 with the SPINE verdict printed.</done>
</task>

</tasks>

<verification>
- **Exact over Q only:** every decisive rank is exact_qq_rank (DomainMatrix over QQ); the exact-only source guard asserts 0 numpy float-rank calls and 0 octonion_algebra imports on the decisive path (fp-float-rank rejected).
- **Substitute-first:** the generic integer point is substituted into the cached (un-simplified) gradients BEFORE any rank (fp-rank-before-substitution rejected; det_3 / 54-var swell controlled).
- **Genericity discipline:** every TEST pair is gated (_pair_not_proportional + distinct diagonals); the verdict is MAX over >= 3 generic pairs + a fresh inline pair; X=Y is a control EXPECTED to give <= 6, never a test point (fp-non-generic-point rejected).
- **Pre-registration:** points + rank test + verdict map are frozen as code BEFORE evaluation; the NEGATIVE branch is fully wired (fp-force-positive rejected).
- **Two-route agreement (the reward-hacking guard):** a verdict is reported ONLY when Route 1 (rank 7 vs 6) and Route 2 (separating xi exists vs none) agree; both off-diagonal cells emit NO VERDICT + STOP (fp-two-route-mismatch-ignored rejected). Route 2's two internal forms (explicit Tr(jordan(.,.)) vs gradient-contraction) must also agree (fp-route2-false-positive guard).
- **Frozen primitives:** jordan / Tr / det_3 / f_4 builder / exact_qq_rank are REUSED from the certified engine, never re-implemented (fp-det3-port-slip rejected).
- **Corrected consistency:** SPINE_RANK <= ORBIT_DERIVED_TRDEG (== 10); the statement is "7 <= 10" (c is 1 of 4 mixed invariants), NOT the stale "= 7 saturates trdeg" (fp-stale-saturation rejected).
- **Demonstration, not assertion:** both routes are COMPUTED on the actual h_3(O) (fp-assert-without-demonstration rejected).
- **Shapes:** Route-1 Jacobian is 7x54 (rank <= 7); baseline is 6x54; Route-2 separating vector is length 52 (= dim f_4).
- **Honest negative:** on rank 6, an explicit P = a*(Tr X)(Tr Y) is constructed and verified as a symbolic identity over Q (or the rank-6 finding is flagged as a non-generic artifact and STOPped); never buried, never forced.
</verification>

<success_criteria>
- code/spine_independence.py runs foreground via `python -u` end-to-end (well under a minute), prints per-pair/per-route progress throughout (watchdog-safe), and exits 0.
- **Route 1:** the 7x54 sub-Jacobian {Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y, c} has a definite exact rank over Q, STABLE across >= 3 generic pairs + a fresh pair, MAX = SPINE_RANK in {7, 6}; the 6x54 pointwise baseline == 6 at every pair; the X=Y control <= 6; the three-domain exactness cross-check (QQ-frac == QQ-int == ZZ-int) passes at width 54.
- **Route 2 (independent):** a separating f_4 direction xi exists (the length-52 vector s = (Tr((xi_i.X) o Y))_i is NOT all zero, witness recorded) while all 6 pointwise directional derivatives along orbit tangents are exactly 0 -- checked at all generic pairs + the fresh pair; the explicit trace-form and gradient-contraction forms of s agree.
- **Two-route agreement:** the adjudicator emits a verdict ONLY on a diagonal cell -- (7, exists) => c INDEPENDENT, or (6, none) => c DEPENDENT; both off-diagonal cells emit NO VERDICT + STOP + nonzero exit (the reward-hacking guard). The 2x2 table is printed with the observed cell.
- **Expected verdict (per the committed Phase-65.1 r7==7 preview, RE-DEMONSTRATED here):** rank 7, separating xi exists => c INDEPENDENT (positive). The NEGATIVE branch is wired and is a recorded no-op (c(X,X) = Tr X^2 != (Tr X)^2). A rank-6 NEGATIVE, if it fired, would ship the explicit symbolically-verified P = a*(Tr X)(Tr Y) -- equally a full pass.
- **Corrected consistency:** SPINE_RANK <= 10 asserted; the statement printed is "rank 7 <= trdeg 10" (c is the FIRST of four mixed joint invariants, does NOT saturate the full trdeg); the stale roadmap criterion-5 wording is explicitly flagged as superseded and NOT used.
- All forbidden proxies (fp-assert-without-demonstration, fp-float-rank, fp-non-generic-point, fp-force-positive, fp-rank-before-substitution, fp-route2-false-positive, fp-two-route-mismatch-ignored, fp-det3-port-slip, fp-stale-saturation) are explicitly rejected by a concrete guard in the harness.
</success_criteria>

<output>
After completion, create `.gpd/phases/66-b-functional-independence-of-c-the-spine/66-01-SUMMARY.md`.

The SUMMARY must record: the SPINE_RANK verdict (7 or 6) and its label; the two-route agreement cell; the Route-2 witness xi and a sample of s; the per-pair ranks (>= 3 PAIR_POINTS + the fresh pair) confirming stability; the 6x54 baseline (==6) and X=Y control (<=6); the exactness three-domain cross-check; the corrected consistency line (7 <= 10, c is 1 of 4 mixed invariants); on a NEGATIVE, the explicit verified P. Note exact-over-Q throughout, the frozen engine reuse, and that this RE-DEMONSTRATES (not merely cites) the Phase-65.1 r7==7 preview as the phase's own pre-registered decisive result. Flag SCOPE: FIELD-level functional independence of c ONLY (NOT ring generation = Phase 68; NOT degree-2 uniqueness = Phase 67).
</output>
