---
phase: 41-o-9-s-8-quantitative-verification
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified: [derivations/40-derivation-chain.md]
interactive: false

conventions:
  units: "natural (hbar=1, k_B=1, a=1)"
  metric: "(-,+,+,+) Lorentzian emergent; (+,...,+) Riemannian Fisher/sigma"
  clifford: "Cl(9,0), {T_a,T_b}=(1/2)delta_{ab}I_{16}"
  coupling: "J>0, H_eff = J sum T_a^(i) T_a^(j)"
  sigma_model: "S = (rho_s/2) int (d_mu n)^2, n in S^8"
  modular_hamiltonian: "K_A = -ln(rho_A) (positive operator)"
  lattice_bw: "H_ent = (2pi/c_s) sum x_perp h_x"

dimensional_check:
  c_s_in_metric: "[Ja] = [velocity]"
  G_N: "[depends on UV completion]"

approximations:
  - name: "classical spin-wave theory"
    parameter: "1/S"
    validity: "leading order"
    breaks_when: "S_eff=1/2"
    check: "Quantum correction caveat preserved in updated chain"

estimated_execution:
  total_minutes: 25
  breakdown:
    - task: 1
      minutes: 25
      note: "Update links (i)-(l) with O(9) numbers, remove carry-forward caveat"

contract:
  scope:
    question: "Are all Heisenberg carry-forward numbers in derivation chain links (i)-(l) replaced with O(9)-specific values?"
  claims:
    - id: claim-chain-update
      statement: "Links (i)-(l) of the v10.0 derivation chain are updated with O(9) model-specific numbers from Plan 01, removing the carry-forward caveat"
      deliverables: [deliv-updated-chain]
      acceptance_tests: [test-no-heisenberg, test-caveat-removed, test-structural-preserved]
      references: [ref-plan01-results, ref-phase40-chain]
    - id: claim-chain-consistency
      statement: "The updated chain is internally consistent: O(9) numbers in links (i)-(l) match the O(9) results in links (d')-(h')"
      deliverables: [deliv-updated-chain]
      acceptance_tests: [test-internal-consistency]
      references: [ref-plan01-results, ref-phase40-chain]
  deliverables:
    - id: deliv-updated-chain
      kind: derivation
      path: "derivations/40-derivation-chain.md"
      description: "Updated v10.0 derivation chain with O(9) numbers in links (i)-(l)"
      must_contain: ["c_s = J*sqrt(3/2)", "v_LR(O(9))", "O(9) lattice-BW universality", "O(9) Fisher metric"]
  acceptance_tests:
    - id: test-no-heisenberg
      subject: claim-chain-update
      kind: consistency
      procedure: "Search links (i)-(l) for Heisenberg-specific numbers: 1.659, 0.181, 0.0657, 12.66, 7.63, 0.9993"
      pass_condition: "None found in the O(9) results (may appear only in comparison/historical context)"
      evidence_required: [deliv-updated-chain]
    - id: test-caveat-removed
      subject: claim-chain-update
      kind: existence
      procedure: "Check that the 'v9.0 Carry-Forward Caveat for Links (i)-(l)' section is replaced with O(9)-specific content"
      pass_condition: "Carry-forward caveat removed or downgraded to historical note"
      evidence_required: [deliv-updated-chain]
    - id: test-structural-preserved
      subject: claim-chain-update
      kind: consistency
      procedure: "Verify links (i)-(l) still cite the correct Phase 32-35 derivation files and equation numbers. Only NUMBERS change, not theorem references."
      pass_condition: "All structural citations preserved; only quantitative values updated"
      evidence_required: [deliv-updated-chain]
    - id: test-internal-consistency
      subject: claim-chain-consistency
      kind: consistency
      procedure: "Verify: (1) rho_s = J/8 in link (g') matches rho_s used in link (i) Fisher metric. (2) c_s from link (j) matches c_s in link (k) BW ansatz. (3) SSB pattern Spin(9)->Spin(8) in link (f') matches sigma model O(9)/S^8 in link (g') matches Fisher/correlation in link (i)."
      pass_condition: "All cross-link numbers consistent"
      evidence_required: [deliv-updated-chain]
  references:
    - id: ref-plan01-results
      kind: prior_artifact
      locator: "derivations/41-o9-quantitative-results.md"
      role: definition
      why_it_matters: "Contains all O(9)-specific numbers to substitute into chain links (i)-(l)"
      applies_to: [claim-chain-update, claim-chain-consistency]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-phase40-chain
      kind: prior_artifact
      locator: "derivations/40-derivation-chain.md"
      role: definition
      why_it_matters: "Current chain with carry-forward caveat; this is the file being updated"
      applies_to: [claim-chain-update]
      must_surface: true
      required_actions: [read]
  forbidden_proxies:
    - id: fp-unreplaced
      subject: claim-chain-update
      proxy: "Leaving any Heisenberg-specific number unreplaced in links (i)-(l)"
      reason: "The entire point of Phase 41 is removing carry-forward numbers"
    - id: fp-empty-removal
      subject: claim-chain-update
      proxy: "Removing the carry-forward caveat without actually inserting O(9) numbers"
      reason: "Caveat removal must be paired with actual O(9) value substitution"
    - id: fp-structural-change
      subject: claim-chain-consistency
      proxy: "Changing the structural argument or theorem citations -- only numbers change"
      reason: "Theorem references from Phases 32-35 are model-independent and must be preserved"
    - id: fp-rigor-upgrade
      subject: claim-chain-consistency
      proxy: "Upgrading any link's rigor level without justification"
      reason: "Classical c_s does not improve rigor; only quantitative values change"
  uncertainty_markers:
    weakest_anchors:
      - "Classical c_s propagates into link (j) emergent metric -- quantum correction unknown"
      - "SRF for O(9) is universality argument, not computed number"
    disconfirming_observations:
      - "If O(9) numbers create internal inconsistency in the chain -> error in Plan 01 computation"
      - "If updated links change the rigor assessment of any link -> re-examine"
---

<objective>
Update the v10.0 derivation chain (derivations/40-derivation-chain.md) to replace all Heisenberg carry-forward numbers in links (i)-(l) with the O(9)-specific values computed in Plan 01.

Purpose: Remove the carry-forward caveat from the derivation chain, making links (i)-(l) self-consistent with the O(9) model established in links (a')-(h'). After this update, the entire 12-link chain operates on a single model (O(9)/S^8 on Z^3), not a hybrid of O(9) structure + O(3) numbers.
Output: Updated derivations/40-derivation-chain.md with O(9) numbers, carry-forward caveat replaced, internal consistency verified.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md

@derivations/41-o9-quantitative-results.md
@derivations/40-derivation-chain.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update derivation chain links (i)-(l) with O(9) numbers</name>
  <files>derivations/40-derivation-chain.md</files>
  <action>
    Read derivations/41-o9-quantitative-results.md (Plan 01 output) for all O(9)-specific values.
    Read derivations/40-derivation-chain.md (current chain).

    Make the following targeted updates to links (i)-(l):

    A. Replace the "v9.0 Carry-Forward Caveat for Links (i)-(l)" section.
    Remove the carry-forward caveat block. Replace with a brief historical note:
    "**Note:** Links (i)-(l) were originally derived in Phases 32--35 for the Heisenberg model (v9.0). Phase 41 recomputed all model-specific numbers for the O(9)/S^8 sigma model. The structural arguments are model-independent; only the quantitative values have been updated. See derivations/41-o9-quantitative-results.md for the full computation."

    B. Update Link (i): Fisher manifold from sigma model correlations
    - Replace "CORR-03, Eq. (33.19)" reference: keep the theorem reference, add "verified for O(9) in Phase 41"
    - Add O(9) correlation function: C(r) ~ 8 * Gamma(d/2-1) / (4 pi^{d/2} * (J/8) * r^{d-2})
    - Keep H1-H4 conditionality, noting H1 is now SATISFIED for O(9) (d>=3, Phase 39 FSS)
    - Add: "Phase 41 derivation: derivations/41-o9-quantitative-results.md, Part E"

    C. Update Link (j): Emergent Lorentz from sigma model rescaling
    - Replace c_s = 1.659 Ja with c_s = J*sqrt(3/2) = 1.225 Ja (classical O(9) on Z^3)
    - Replace v_LR/c_s = 7.63 with v_LR/c_s ~ 60 (O(9) on Z^3)
    - Replace ds^2 = -c_s^2 dt^2 + g_ij dx^i dx^j with O(9) c_s value
    - Keep all 4 arguments (dispersion, EFT, universality, Hamma) -- they are structural
    - Add quantum correction caveat: "c_s = 1.225 Ja is the classical value; quantum corrections (unknown for O(9)) may reduce this by ~20% (O(3) analogy)."
    - Add: "Phase 41 derivation: derivations/41-o9-quantitative-results.md, Parts A, C, F"

    D. Update Link (k): BW + KMS
    - Replace "SRF = 0.9993 was computed for Heisenberg" with the universality argument from Plan 01
    - State: "The BW ansatz applies to the O(9) model by universality (Giudici et al. 2018): the O(9) NL sigma model is Lorentz invariant (Phase 39). The SRF for O(9) has not been computed numerically; the universality argument predicts SRF close to 1."
    - Keep the structural content (BW -> KMS at beta=2pi) unchanged -- this is model-independent
    - Replace the lattice-BW formula: H_ent = (2pi/c_s(O(9))) sum x_perp h_x with O(9) c_s and O(9) h_x
    - Add: "Phase 41 derivation: derivations/41-o9-quantitative-results.md, Part D"

    E. Update Link (l): Jacobson -> Einstein
    - This link is structurally model-independent (it depends on BW + KMS, not on the sigma model directly)
    - Add note: "Numerical prefactors (G_N, Lambda) depend on UV data including c_s. With the O(9) c_s, the Newton constant G_N inherits an O(9)-specific value, but the STRUCTURAL result G_ab + Lambda g_ab = 8pi G_N T_ab is unchanged."
    - Keep all conditions (UC5, UC6, UC8, UC9, UC10 for Gap C; UC5, CS, TL for Gap D)

    F. Update the v9.0 vs v10.0 comparison table at the top of the document.
    - Replace "Specific c_s = 1.659 Ja, rho_s = 0.181 J, QMC 0.3% match are Heisenberg-specific" with the O(9) values
    - Update the carry-forward column entries

    G. Update the "Conditionality Summary" and "Complete Chain Diagram" if needed.
    - The conditionality of links (i)-(l) may slightly change:
      Link (i): H1 is now SATISFIED (not just inherited) for O(9) d>=3
      Links (j)-(l): conditionality unchanged structurally
    - Add the classical-only caveat as a new uncertainty marker for links (j)-(k)

    Do NOT change links (a')-(h') -- those are already O(9)-specific from Phase 40.
    Do NOT change the rigor taxonomy labels unless the O(9) computation genuinely changes the assessment.
    Do NOT change theorem references or equation numbers from Phases 32-35 -- only the model-specific numbers.
  </action>
  <verify>
    1. Search for Heisenberg-specific numbers in links (i)-(l):
       - "1.659" should not appear as an O(9) result (only in historical comparison)
       - "0.181" should not appear as O(9) rho_s
       - "0.0657" should not appear as O(9) chi_perp
       - "12.66" should not appear as O(9) v_LR
       - "7.63" should not appear as O(9) ratio
       - "0.9993" should not appear as O(9) SRF
    2. Internal consistency:
       - rho_s = J/8 in link (g') matches rho_s in link (i) correlator formula
       - c_s = J*sqrt(3/2) in link (j) matches c_s in link (k) BW ansatz
       - N=9 in link (g') matches N-1=8 in link (i) Goldstone count
    3. Structural preservation:
       - Phase 32-35 equation numbers still cited
       - All theorem references preserved
       - Rigor taxonomy labels unchanged (or justified if changed)
    4. Carry-forward caveat: removed or replaced with historical note
    5. Chain diagram: still valid and self-consistent
    6. Convention consistency: ASSERT_CONVENTION line at top still correct
  </verify>
  <done>Links (i)-(l) updated with O(9)-specific numbers, carry-forward caveat replaced with historical note, internal consistency verified (rho_s, c_s, N-1 all match across links), no Heisenberg numbers remain in O(9) results, structural citations preserved</done>
</task>

</tasks>

<verification>
- Zero Heisenberg-specific numbers in O(9) results of links (i)-(l)
- Carry-forward caveat removed
- Internal consistency: all cross-link numbers match (rho_s, c_s, N, Goldstone count)
- Structural citations (Phase 32-35 equations) preserved unchanged
- Convention consistency maintained throughout
- Chain diagram still valid
</verification>

<success_criteria>
The v10.0 derivation chain is fully self-consistent on the O(9)/S^8 model. All 12 links (a')-(l) now reference O(9)-specific results. No Heisenberg carry-forward numbers remain in the active chain (only in historical comparison). The carry-forward caveat is removed.
</success_criteria>

<output>
After completion, create `.gpd/phases/41-o-9-s-8-quantitative-verification/41-02-SUMMARY.md`
</output>
