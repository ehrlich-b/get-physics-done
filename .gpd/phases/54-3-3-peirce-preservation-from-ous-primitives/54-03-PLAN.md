---
phase: 54-3-3-peirce-preservation-from-ous-primitives
plan: 03
type: execute
wave: 3
depends_on: ["54-02"]
files_modified:
  - derivations/paper5-peirce-preservation/s0-axiom.md
  - derivations/paper5-peirce-preservation/closeout-sympy.py
  - derivations/paper5-peirce-preservation/paper5-s3-revision.tex
  - ~/repos/research/paper5/main.tex
  - .gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md
interactive: true

conventions:
  units: "N/A (pure algebra)"
  sequential_product_symbol: "a ∘ b"
  compression_symbol: "C_p"
  order_unit_space: "finite-dim archimedean OUS over ℝ with distinguished unit 1"
  peirce_2_space: "V_2(p_i) := range(C_{p_i})"
  peirce_1_space: "V_1(p_i, p_j) := (C_{p_i} + C_{p_j})V - C_{p_i}V - C_{p_j}V"
  allowed_axiom_scope_for_A: "{S1, S3, linearity, A-S compression axioms}"
  allowed_axiom_scope_for_Ci: "{S0, S1, S3, linearity, A-S compression axioms}"
  revision_file_target: "~/repos/research/paper5/main.tex (NOT main-jmp-submitted.tex — the submitted file is frozen at git tag paper5-jmp-submitted)"
  forbidden_tokens:
    - "M_n(ℂ)"
    - "M_n(C)"
    - "Jordan"
    - "EJA"
    - "Lüders"
    - "Luders"
    - "pxp"
    - "√a b √a"
    - "sqrt(a) b sqrt(a)"
    - "operator product"
    - "f(λ,μ)=√(λμ)"
    - "f(lambda,mu)=sqrt(lambda*mu)"
    - "h_n(ℂ)"
    - "h_n(C)"
    - "spin factor"
  forbidden_tokens_exception_scope: "If outcome is (C-i), the canonical-example defense paragraph may reference M_n(ℂ)^sa, C(X), and spin factors as the three canonical models where S0 is automatic — this is legal INSIDE the model-instantiation defense context per CONTEXT.md Decisions §S0 axiom form §Defense strategy. Outside this paragraph, the forbidden-token list is strict."
  grep_targets:
    - "derivations/paper5-peirce-preservation/s0-axiom.md"
    - "derivations/paper5-peirce-preservation/paper5-s3-revision.tex"
    - ".gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md"

contract:
  schema_version: 1
  scope:
    question: "Given the Plan 54-02 outcome tag ((A) via attempt-NN OR PIVOT-TO-C-I), close Phase 54 with: (a) if (A), a closeout SymPy verification including R3 cross-term coverage + §3.3 revision text + adversarial review + exit gate; (b) if (C-i), an S0 axiom statement with canonical-example defense + OUS-compatibility proof sketch + closeout SymPy + §3.3 revision text + adversarial review + exit gate. The single exit gate (lemma statement verbatim in §3.3 revision text, by grep AND manual review) fires in either branch."
  claims:
    - id: claim-outcome-tag-consumed
      statement: "Plan 54-03 reads the Plan 54-02 outcome tag from attempt-log.md and routes its own execution: '(A) via attempt-NN' → tasks select the winning attempt-NN as the lemma proof basis; 'PIVOT-TO-C-I' → tasks produce the S0 axiom + canonical-example defense + OUS-compatibility sketch + (C-i)-assumption-set-clause lemma; '(C-ii)' is NOT reached in this plan (Plan 54-02 does not emit it; (C-ii) would require a different Phase 54 branch outside the current plan architecture)."
      deliverables: [deliv-result-md]
      acceptance_tests: [test-result-md-outcome-tag, test-plan-54-02-tag-consumed]
      references: [ref-plan-54-02, ref-ctx-54, ref-research-54]
    - id: claim-s0-axiom-and-defense-if-ci
      statement: "If outcome is PIVOT-TO-C-I: `s0-axiom.md` states S0 at the compression level (weakest form per CONTEXT.md Decisions §S0 axiom form): `{C_{p_i}} pairwise commute AND C_{p_i} C_{p_j} = 0 for i ≠ j` for an orthogonal projective-unit family `{p_i}` in a spectral OUS. Three canonical-example defenses (M_n(ℂ)^sa via spectral-theorem + pxp-in-model; C(X) trivially; spin factors via Clifford relation) establish S0 is AUTOMATIC in each. Independence-from-S1-S7 defense (form = Agent's Discretion: counterexample-model OR parameter-counting OR hybrid) shows S0 is not derivable from vdW S1-S7. OUS-compatibility proof sketch (DERV-54-06) shows {S0, S1, S3, linearity, A-S compressions} implies the three target inclusions (i), (ii), (iii) of claim.md."
      deliverables: [deliv-s0-axiom]
      acceptance_tests: [test-s0-statement-compression-level, test-s0-canonical-defenses, test-s0-independence-defense, test-s0-implies-peirce-invariance]
      references: [ref-ctx-54, ref-research-54, ref-as-notes, ref-niestegge, ref-claim-md, ref-addendum]
    - id: claim-closeout-sympy-r3-coverage
      statement: "`closeout-sympy.py` is a separate artifact (NOT embedded in an attempt-NN.py) with fuller SymPy verification on H_3(ℝ) or H_4(ℝ) that includes the R3 cross-term case: `a ∘ V_1(p_k, p_l) ⊆ V_1(p_k, p_l)` for `{k, l} ∩ supp(a) = ∅`. At least three orthogonal rank-1 projectors are used so the cross-term exists non-trivially. Symbolic-exact via sympy.Rational or sympy.Symbol. Runtime < 5 sec. PASS required."
      deliverables: [deliv-closeout-sympy]
      acceptance_tests: [test-closeout-r3-cross-term, test-closeout-separate-artifact, test-closeout-runtime]
      references: [ref-ctx-54, ref-research-54, ref-claim-md]
    - id: claim-s3-revision-and-exit-gate
      statement: "§3.3 revision text (≥ 20 lines per R4) is staged in `paper5-s3-revision.tex` and integrated into `~/repos/research/paper5/main.tex` (NOT main-jmp-submitted.tex). The revision text (a) names the Peirce-Preservation Lemma verbatim (modulo whitespace) as stated in claim.md using the active assumption-set clause, (b) either exhibits the outcome (A) proof or introduces S0 with canonical-example defense paragraph (C-i) and proves the three target inclusions from {S0, S1, S3, linearity, A-S compressions}, (c) does not use any forbidden token outside the canonical-example defense paragraph. Exit gate: lemma-statement grep against §3.3 revision text passes AND manual semantic review passes (BOTH required per CONTEXT.md)."
      deliverables: [deliv-revision-text, deliv-main-tex-integration]
      acceptance_tests: [test-revision-r4-line-count, test-revision-lemma-verbatim-grep, test-revision-forbidden-token-discipline, test-revision-in-main-tex-not-submitted]
      references: [ref-claim-md, ref-paper5-submitted, ref-paper5-main, ref-ctx-54]
    - id: claim-adversarial-review-final
      statement: "Final adversarial review (VALD-54-02) runs on `54-RESULT.md` (which aggregates proof/axiom, defense, closeout, revision text). `gpd-review-math` with full Phase 54 priming (54-CONTEXT, 54-RESEARCH, alfsen-shultz-notes, claim.md, peirce-post-jordan-finding memory, forbidden-token list, R1-R5 pitfalls, framing discipline) returns PASS / BORDERLINE / FAIL. BORDERLINE escalates to Paper-5-primed Opus subagent (CONTEXT.md Decisions §Adversarial fresh-eyes review). Phase 54 CANNOT close until the review returns PASS at the primary level OR PASS at the escalation level."
      deliverables: [deliv-result-md, deliv-adversarial-review-log]
      acceptance_tests: [test-adversarial-review-invoked, test-adversarial-review-pass-or-escalated, test-borderline-escalation-path]
      references: [ref-ctx-54, ref-research-54, ref-peirce-post-jordan-memory]
    - id: claim-phase-54-close
      statement: "54-RESULT.md contains: (1) explicit outcome tag `(A)` or `(C-i)` (NOT `(C-ii)` unless a separate Phase 54 branch was activated, which is outside this plan's scope); (2) named Peirce-Preservation Lemma statement verbatim; (3) if (C-i): S0 axiom + canonical-example defense + OUS-compatibility sketch; if (A): the closed proof referencing attempt-NN.md; (4) closeout SymPy verification result; (5) §3.3 revision text reference (embedded or pointer); (6) exit-gate verification record (grep PASS + manual review PASS); (7) adversarial review verdict (PASS at primary or escalation); (8) ADDENDUM citation for (B)-unavailability; (9) cross-phase coupling note for Phases 55, 57, 58 (DERV-00-01 Phase 54 slice)."
      deliverables: [deliv-result-md]
      acceptance_tests: [test-result-md-all-sections-present, test-result-md-no-forbidden-tokens, test-result-md-addendum-cite]
      references: [ref-ctx-54, ref-addendum, ref-claim-md, ref-lean-self-modeling, ref-plan-54-02]
  deliverables:
    - id: deliv-s0-axiom
      kind: derivation
      path: "derivations/paper5-peirce-preservation/s0-axiom.md"
      description: "(conditional on PIVOT-TO-C-I outcome from Plan 54-02) S0 axiom statement at compression level, three canonical-example defenses (M_n(ℂ)^sa, C(X), spin factors), independence-from-S1-S7 defense, OUS-compatibility proof sketch showing S0 + {S1, S3, linearity, compressions} implies the three target inclusions of claim.md."
      must_contain:
        - "S0 statement: `{C_{p_i}} pairwise commute AND C_{p_i} C_{p_j} = 0 for i ≠ j` for an orthogonal projective-unit family {p_i} in a spectral OUS"
        - "canonical-example defense: M_n(ℂ)^sa via spectral theorem + pxp-in-model (pxp token allowed HERE as a within-model computational step)"
        - "canonical-example defense: C(X) — projectors are characteristic functions of disjoint sets; compressions pairwise commute and annihilate trivially"
        - "canonical-example defense: spin factors — from Clifford relation {e_i, e_j} = 2 δ_{ij} 1 (spin-factor token allowed HERE as the defense context)"
        - "independence-from-S1-S7 defense — counterexample model OR parameter-counting OR hybrid (Agent's Discretion per CONTEXT.md)"
        - "OUS-compatibility proof sketch: under {S0, S1, S3, linearity, A-S compressions}, derive (i) `a ∘ V_2(p_i) ⊆ V_2(p_i)`, (ii) `a ∘ V_1(p_i, p_j) ⊆ V_1(p_i, p_j)`, (iii) `a ∘ V_1(p_k, p_l) ⊆ V_1(p_k, p_l)` for {k,l}∩supp(a)=∅"
        - "Niestegge 2008 citation as literature analogue for OUS-level compression-base axiomatization"
        - "(optional) Plan 54-02 verbatim adversarial objections carried forward as motivating evidence ('we tried (A) and failed because [X], S0 resolves this by [Y]')"
    - id: deliv-closeout-sympy
      kind: code
      path: "derivations/paper5-peirce-preservation/closeout-sympy.py"
      description: "Separate closeout SymPy artifact (NOT embedded in an attempt-NN.py per CONTEXT.md Decisions §SymPy validation wiring). Covers V_2(p_i), V_1(p_i, p_j), and the R3 cross-term V_1(p_k, p_l) for {k,l}∩supp(a)=∅. At least three orthogonal rank-1 projectors on H_3(ℝ) or larger. Symbolic-exact. Runtime < 5 sec."
      must_contain:
        - "at least three orthogonal rank-1 projectors on H_3(ℝ) or H_4(ℝ)"
        - "a = Σᵢ λᵢ pᵢ for i in a selected support subset (e.g., i ∈ {1, 2}); remaining projectors p_k, p_l with {k,l}∩supp(a)=∅ are used to construct cross-term test elements"
        - "assertion: for b in V_2(p_1), a ∘ b ∈ V_2(p_1)"
        - "assertion: for b in V_1(p_1, p_2), a ∘ b ∈ V_1(p_1, p_2)"
        - "assertion: for b in V_1(p_3, p_4) with {3,4}∩{1,2}=∅, a ∘ b ∈ V_1(p_3, p_4) — R3 cross-term coverage"
        - "exit code 0 PASS; runtime checked < 5 sec"
        - "referenced in 54-RESULT.md (not buried in an attempt file)"
    - id: deliv-revision-text
      kind: derivation
      path: "derivations/paper5-peirce-preservation/paper5-s3-revision.tex"
      description: "§3.3 revision text staged in a separate .tex file; ≥ 20 lines substantive (R4); names the Peirce-Preservation Lemma verbatim; exhibits the (A) proof OR introduces S0 with canonical-example defense; does not use forbidden tokens outside the canonical-example defense paragraph (if present)."
      must_contain:
        - "Peirce-Preservation Lemma statement VERBATIM from claim.md (modulo whitespace; must grep-match against claim.md's lemma statement line)"
        - "active assumption-set clause: {S1, S3, linearity, A-S compressions} if (A), OR {S0, S1, S3, linearity, A-S compressions} if (C-i)"
        - "three target inclusions (i), (ii), (iii) stated as separate propositions"
        - "if (C-i): S0 axiom statement, canonical-example defense paragraph (M_n(ℂ)^sa + C(X) + spin factors), OUS-compatibility proof sketch"
        - "if (A): explicit proof referencing the winning attempt-NN.md"
        - "≥ 20 lines of substantive content (R4); NO padding ('obvious', 'clearly', 'immediately follows')"
    - id: deliv-main-tex-integration
      kind: derivation
      path: "~/repos/research/paper5/main.tex"
      description: "Integration of paper5-s3-revision.tex into the Paper 5 main.tex at §3.3 — replaces the submitted non-sequitur at lines 508-528 (main-jmp-submitted.tex baseline)."
      must_contain:
        - "§3.3 section in main.tex contains the revised lemma statement + proof (or axiom + defense)"
        - "main-jmp-submitted.tex is NOT modified (frozen at git tag paper5-jmp-submitted)"
        - "git diff main.tex shows only the §3.3 region changed; no accidental spillover into other sections"
    - id: deliv-result-md
      kind: report
      path: ".gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md"
      description: "Phase 54 RESULT.md — aggregates outcome tag, lemma statement, proof or axiom+defense, closeout SymPy result, revision text pointer, exit-gate verification record, adversarial review verdict, ADDENDUM citation for (B)-unavailability, and cross-phase coupling notes for Phases 55, 57, 58 (DERV-00-01 Phase 54 slice)."
      must_contain:
        - "Section: Outcome Tag — exactly one of `(A)` or `(C-i)` (or `(C-ii)` only if a separate Phase 54 branch activated — outside this plan's scope)"
        - "Section: Peirce-Preservation Lemma — statement VERBATIM from claim.md with active assumption-set clause"
        - "Section: Proof or Axiom+Defense — if (A), pointer to attempt-NN.md + brief summary; if (C-i), inline S0 axiom + canonical-example defense + OUS-compatibility sketch (pointer to s0-axiom.md for full detail)"
        - "Section: Closeout SymPy — result of closeout-sympy.py with R3 cross-term PASS; pointer to the artifact"
        - "Section: §3.3 Revision Text — pointer to paper5-s3-revision.tex and main.tex §3.3 region; exit-gate verification record (grep PASS + manual review PASS)"
        - "Section: Adversarial Review — verdict (PASS at primary or escalation); pointer to review log"
        - "Section: (B)-Unavailability — explicit citation `(B) ruled out per ADDENDUM §[ref]` as required by CONTEXT.md anchor-registry §ADDENDUM entry"
        - "Section: Cross-phase coupling — explicit notes for Phase 55 (S4 argument uses S0 if (C-i)), Phase 57 (phi-inertness may share R11 restructuring), Phase 58 (_peirce_preservation axiom re-classification based on outcome tag: (A) → theorem-in-disguise; (C-i) → type-iv primitive with S0 defense)"
        - "FORBIDDEN-TOKEN grep on 54-RESULT.md returns zero hits outside the (C-i) canonical-example defense paragraph (if present)"
    - id: deliv-adversarial-review-log
      kind: note
      path: ".gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-ADVERSARIAL-REVIEW.md"
      description: "Log of adversarial review runs: primary `gpd-review-math` verdict; escalation to Opus if BORDERLINE; final PASS basis."
      must_contain:
        - "primary reviewer: gpd-review-math with Phase 54 priming (54-CONTEXT + 54-RESEARCH + alfsen-shultz-notes + claim.md + peirce-post-jordan-finding memory + forbidden-token list + R1-R5 pitfalls + CIRCULAR-vs-IDENTITY framing discipline)"
        - "primary verdict: PASS / BORDERLINE / FAIL with full review text"
        - "(conditional) escalation: if BORDERLINE, Opus subagent Paper-5-primed verdict with full review text"
        - "final basis for Phase 54 close: PASS at primary OR PASS at escalation"
  references:
    - id: ref-plan-54-02
      kind: prior_artifact
      locator: ".gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-02-PLAN.md + derivations/paper5-peirce-preservation/attempt-log.md"
      aliases: ["Plan 54-02", "attempt-log"]
      role: definition
      why_it_matters: "Plan 54-02's outcome tag ('(A) via attempt-NN' or 'PIVOT-TO-C-I') drives Plan 54-03's task routing; the CARRY-FORWARD block of verbatim objections (if PIVOT-TO-C-I) motivates the S0 defense"
      applies_to: [claim-outcome-tag-consumed, claim-s0-axiom-and-defense-if-ci]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [read, use]
    - id: ref-claim-md
      kind: prior_artifact
      locator: "derivations/paper5-peirce-preservation/claim.md (produced in Plan 54-01 Task 3)"
      aliases: ["claim.md", "Peirce-Preservation Lemma statement"]
      role: definition
      why_it_matters: "Stable lemma target; the revision text in paper5-s3-revision.tex must grep-match against claim.md's lemma statement (exit gate)"
      applies_to: [claim-s3-revision-and-exit-gate, claim-s0-axiom-and-defense-if-ci]
      carry_forward_to: [execution, writing]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-as-notes
      kind: prior_artifact
      locator: "derivations/paper5-peirce-preservation/alfsen-shultz-notes.md (produced in Plan 54-01 Task 4)"
      aliases: ["alfsen-shultz-notes.md"]
      role: method
      why_it_matters: "Every A-S compression-axiom citation in s0-axiom.md, paper5-s3-revision.tex, and main.tex §3.3 revision must resolve via this shared artifact; Prop/Thm numbers verified against the actual book text"
      applies_to: [claim-s0-axiom-and-defense-if-ci, claim-s3-revision-and-exit-gate]
      carry_forward_to: [execution, writing]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-ctx-54
      kind: prior_artifact
      locator: ".gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-CONTEXT.md"
      aliases: ["54-CONTEXT"]
      role: definition
      why_it_matters: "Locked decisions on S0 axiom form (compression-level weakest), defense strategy, closeout SymPy separation, adversarial review policy, exit gate (grep + manual review), cross-phase coupling (DERV-00-01 Phase 54 slice)"
      applies_to: [claim-s0-axiom-and-defense-if-ci, claim-closeout-sympy-r3-coverage, claim-s3-revision-and-exit-gate, claim-adversarial-review-final, claim-phase-54-close]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [read, use]
    - id: ref-research-54
      kind: prior_artifact
      locator: ".gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESEARCH.md"
      aliases: ["54-RESEARCH"]
      role: method
      why_it_matters: "Defines S0 compression-level form (Approach 3), canonical-example defense strategy, Niestegge 2008 literature analogue, SymPy closeout design with R3 cross-term coverage"
      applies_to: [claim-s0-axiom-and-defense-if-ci, claim-closeout-sympy-r3-coverage]
      carry_forward_to: [execution]
      must_surface: true
      required_actions: [read, use]
    - id: ref-niestegge
      kind: paper
      locator: "Niestegge (2008), 'A Representation of Quantum Measurement in Order-Unit Spaces,' Found. Phys. 38, 783, arXiv:1001.3633"
      aliases: ["Niestegge 2008"]
      role: method
      why_it_matters: "Literature analogue for OUS-level axiomatization of compression-related coherence; canonical citation in the (C-i) S0 defense paragraph"
      applies_to: [claim-s0-axiom-and-defense-if-ci]
      carry_forward_to: [writing]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-paper5-submitted
      kind: prior_artifact
      locator: "~/repos/research/paper5/main-jmp-submitted.tex §3.3 lines 483-562 (frozen at git tag paper5-jmp-submitted)"
      aliases: ["Paper 5 submitted"]
      role: definition
      why_it_matters: "Baseline for R4 20-line floor; §3.3 revision text in main.tex must not reduce substantive content below this floor"
      applies_to: [claim-s3-revision-and-exit-gate]
      carry_forward_to: [writing]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-paper5-main
      kind: prior_artifact
      locator: "~/repos/research/paper5/main.tex"
      aliases: ["Paper 5 main", "main.tex"]
      role: definition
      why_it_matters: "Target file for §3.3 revision integration; main-jmp-submitted.tex is frozen and MUST NOT be edited"
      applies_to: [claim-s3-revision-and-exit-gate]
      carry_forward_to: [writing]
      must_surface: true
      required_actions: [use]
    - id: ref-addendum
      kind: prior_artifact
      locator: ".gpd/research/ADDENDUM-independent-literature-check.md"
      aliases: ["ADDENDUM"]
      role: benchmark
      why_it_matters: "54-RESULT.md must cite ADDENDUM as the justification for '(B) ruled out' (per CONTEXT.md anchor registry)"
      applies_to: [claim-phase-54-close]
      carry_forward_to: [writing]
      must_surface: true
      required_actions: [cite]
    - id: ref-peirce-post-jordan-memory
      kind: prior_artifact
      locator: "peirce-post-jordan-finding memory"
      aliases: ["peirce-post-jordan-finding"]
      role: must_consider
      why_it_matters: "Loaded into adversarial reviewer priming; prior incident record of Paper 5 §3.3 Jordan-smuggling"
      applies_to: [claim-adversarial-review-final]
      carry_forward_to: [execution]
      must_surface: true
      required_actions: [read, use]
    - id: ref-lean-self-modeling
      kind: prior_artifact
      locator: "~/repos/research/lean/Paper5/ (pinned lean4 v4.28.0 + mathlib v4.28.0)"
      aliases: ["Lean Paper5"]
      role: must_consider
      why_it_matters: "Phase 58 axiom audit consumer of outcome tag: (A) → `_peirce_preservation` re-classified as theorem-in-disguise; (C-i) → type-iv primitive with S0 defense; outcome tag must be explicit in 54-RESULT.md for routing"
      applies_to: [claim-phase-54-close]
      carry_forward_to: [writing]
      must_surface: true
      required_actions: [use]
  acceptance_tests:
    - id: test-plan-54-02-tag-consumed
      subject: claim-outcome-tag-consumed
      kind: existence
      procedure: "Grep `derivations/paper5-peirce-preservation/attempt-log.md` for the outcome-tag line; verify it is exactly one of `outcome: (A) via attempt-NN` (for some attempt-NN) or `outcome: PIVOT-TO-C-I`. Plan 54-03's Task 1 reads this tag and routes subsequent tasks accordingly."
      pass_condition: "Outcome tag from Plan 54-02 is cleanly extracted and routing in 54-RESULT.md matches the tag (no confused routing, e.g., (A) tag with (C-i) defense paragraphs)."
      evidence_required: [ref-plan-54-02, deliv-result-md]
      automation: automated
    - id: test-result-md-outcome-tag
      subject: claim-outcome-tag-consumed
      kind: schema
      procedure: "Verify 54-RESULT.md §Outcome-Tag contains exactly one line matching `^Outcome: \\((A|C-i|C-ii)\\)$` (regex)."
      pass_condition: "Exactly one outcome tag line; no composite or ambiguous tag."
      evidence_required: [deliv-result-md]
      automation: automated
    - id: test-s0-statement-compression-level
      subject: claim-s0-axiom-and-defense-if-ci
      kind: schema
      procedure: "(conditional on (C-i)) Verify s0-axiom.md contains the S0 statement at compression level: `{C_{p_i}} pairwise commute AND C_{p_i} C_{p_j} = 0 for i ≠ j` (or semantically equivalent compression-level formulation). Peirce invariance must be DERIVED from S0 + {S1, S3, linearity, A-S compressions}, not posited directly."
      pass_condition: "S0 is at compression level (weakest form); Peirce invariance is derived, not axiomatized directly. Avoids the 'By S0, done' short-circuit per CONTEXT.md contract_coverage false-progress."
      evidence_required: [deliv-s0-axiom]
      automation: human
    - id: test-s0-canonical-defenses
      subject: claim-s0-axiom-and-defense-if-ci
      kind: existence
      procedure: "(conditional on (C-i)) Verify s0-axiom.md contains three canonical-example defense paragraphs: (1) M_n(ℂ)^sa via spectral theorem + pxp-in-model; (2) C(X) trivial via disjoint-characteristic-function projectors; (3) spin factors via Clifford relation. Each paragraph shows S0 is AUTOMATIC in that model."
      pass_condition: "Three distinct defense paragraphs present; each shows S0 holds in that model without additional assumption. M_n(ℂ)^sa paragraph explicitly labeled 'canonical-example defense' and scope-limited to the model instantiation (pxp token allowed HERE per plan forbidden_tokens_exception_scope)."
      evidence_required: [deliv-s0-axiom]
      automation: human
    - id: test-s0-independence-defense
      subject: claim-s0-axiom-and-defense-if-ci
      kind: existence
      procedure: "(conditional on (C-i)) Verify s0-axiom.md contains an independence-of-S0-from-S1-S7 defense. Form is Agent's Discretion per CONTEXT.md: counterexample-model OR parameter-counting OR hybrid. The defense must show S0 is NOT derivable from vdW S1-S7."
      pass_condition: "Independence defense present in one of the permitted forms; conclusion is 'S0 is a genuine new OUS-level assumption, not a consequence of S1-S7'."
      evidence_required: [deliv-s0-axiom]
      automation: human
    - id: test-s0-implies-peirce-invariance
      subject: claim-s0-axiom-and-defense-if-ci
      kind: consistency
      procedure: "(conditional on (C-i)) Verify s0-axiom.md §OUS-compatibility proof sketch shows: under {S0, S1, S3, linearity, A-S compressions}, the three target inclusions (i) a ∘ V_2(p_i) ⊆ V_2(p_i), (ii) a ∘ V_1(p_i, p_j) ⊆ V_1(p_i, p_j), (iii) a ∘ V_1(p_k, p_l) ⊆ V_1(p_k, p_l) for {k,l}∩supp(a)=∅ hold. Covers R3 explicitly."
      pass_condition: "Three sub-derivations present; R3 cross-term case is explicit (not deferred or omitted)."
      evidence_required: [deliv-s0-axiom, ref-claim-md]
      automation: human
    - id: test-closeout-r3-cross-term
      subject: claim-closeout-sympy-r3-coverage
      kind: benchmark
      procedure: "Run `python3 derivations/paper5-peirce-preservation/closeout-sympy.py`; verify output includes PASS assertions for (a) V_2(p_1) invariance, (b) V_1(p_1, p_2) standard invariance, (c) V_1(p_3, p_4) cross-term invariance with {3,4}∩supp(a)=∅ where supp(a)={1,2}."
      pass_condition: "Exit code 0; all three test assertions print PASS; R3 cross-term case is explicit in output."
      evidence_required: [deliv-closeout-sympy]
      automation: automated
    - id: test-closeout-separate-artifact
      subject: claim-closeout-sympy-r3-coverage
      kind: existence
      procedure: "Verify `closeout-sympy.py` exists as a SEPARATE file (NOT embedded in attempt-NN.py); verify it is referenced in 54-RESULT.md §Closeout-SymPy with the pointer `derivations/paper5-peirce-preservation/closeout-sympy.py`."
      pass_condition: "File exists; referenced in 54-RESULT.md; downstream phases (58, Lean sync) can re-run closeout-sympy.py against the final lemma statement without excavating from an attempt file."
      evidence_required: [deliv-closeout-sympy, deliv-result-md]
      automation: automated
    - id: test-closeout-runtime
      subject: claim-closeout-sympy-r3-coverage
      kind: benchmark
      procedure: "Measure runtime of `python3 derivations/paper5-peirce-preservation/closeout-sympy.py` (e.g., `time python3 closeout-sympy.py`). Verify < 5 sec."
      pass_condition: "Runtime < 5 sec."
      evidence_required: [deliv-closeout-sympy]
      automation: automated
    - id: test-revision-r4-line-count
      subject: claim-s3-revision-and-exit-gate
      kind: consistency
      procedure: "Count lines of substantive content in paper5-s3-revision.tex and/or the main.tex §3.3 region. R4 floor: ≥ 20 lines. The submitted Paper 5 §3.3 lines 508-528 is the 20-line baseline. Forbidden padding phrases: 'obvious', 'clearly', 'immediately follows' (acceptable only with a cited theorem + page reference). Adversarial reviewer explicitly checks for padding."
      pass_condition: "Line count ≥ 20; line-by-line check shows substantive content, not padding; no forbidden padding phrases without citation."
      evidence_required: [deliv-revision-text, deliv-main-tex-integration, ref-paper5-submitted]
      automation: hybrid
    - id: test-revision-lemma-verbatim-grep
      subject: claim-s3-revision-and-exit-gate
      kind: existence
      procedure: "Extract the Peirce-Preservation Lemma statement from claim.md. Grep the exact statement (modulo whitespace) against paper5-s3-revision.tex AND against the main.tex §3.3 region. Both greps must return a match. This is one half of the exit gate (the other half is manual semantic review)."
      pass_condition: "Lemma statement grep returns at least one match in both paper5-s3-revision.tex and main.tex §3.3; whitespace/LaTeX-markup differences are tolerated; semantic content is IDENTICAL."
      evidence_required: [deliv-revision-text, deliv-main-tex-integration, ref-claim-md]
      automation: automated
    - id: test-revision-forbidden-token-discipline
      subject: claim-s3-revision-and-exit-gate
      kind: consistency
      procedure: "Run forbidden-token grep on paper5-s3-revision.tex AND on the main.tex §3.3 region. Allowed exception: if outcome is (C-i), the canonical-example defense paragraph may contain M_n(ℂ)^sa, pxp, and spin-factor tokens — these are explicitly allowed INSIDE the demarcated defense paragraph per CONTEXT.md Decisions §S0 axiom form §Defense strategy. Outside this paragraph, zero hits required."
      pass_condition: "Forbidden-token hits occur ONLY inside the explicit canonical-example defense paragraph (if (C-i)) — they MUST be demarcated with a LaTeX comment or section marker. No hits outside the demarcated scope."
      evidence_required: [deliv-revision-text, deliv-main-tex-integration]
      automation: hybrid
    - id: test-revision-in-main-tex-not-submitted
      subject: claim-s3-revision-and-exit-gate
      kind: consistency
      procedure: "Verify `main-jmp-submitted.tex` is UNCHANGED (diff against git tag paper5-jmp-submitted); verify the revision is in `main.tex` §3.3 region."
      pass_condition: "Submitted file diff is empty; main.tex §3.3 contains the revision; no accidental edits to main-jmp-submitted.tex."
      evidence_required: [deliv-main-tex-integration, ref-paper5-submitted, ref-paper5-main]
      automation: automated
    - id: test-adversarial-review-invoked
      subject: claim-adversarial-review-final
      kind: existence
      procedure: "Verify `gpd-review-math` subagent was spawned with Phase 54 priming on 54-RESULT.md (or on the aggregate of paper5-s3-revision.tex + s0-axiom.md + closeout-sympy.py if 54-RESULT.md aggregates by reference). Verify priming includes: 54-CONTEXT, 54-RESEARCH, alfsen-shultz-notes, claim.md, peirce-post-jordan-finding memory, forbidden-token list, R1-R5 pitfalls, CIRCULAR-vs-IDENTITY framing discipline."
      pass_condition: "Review log (54-ADVERSARIAL-REVIEW.md) records the spawn with explicit priming content list; primary verdict recorded."
      evidence_required: [deliv-adversarial-review-log]
      automation: human
    - id: test-adversarial-review-pass-or-escalated
      subject: claim-adversarial-review-final
      kind: human_review
      procedure: "Verify either (a) primary `gpd-review-math` verdict is PASS, or (b) primary was BORDERLINE and escalation to Paper-5-primed Opus subagent returned PASS. FAIL at primary and BORDERLINE-then-FAIL at escalation both BLOCK Phase 54 close (trigger CONTEXT.md stop/rethink or milestone pause)."
      pass_condition: "Phase 54 can close only if a PASS verdict is recorded at primary OR at escalation; no FAIL path allows close."
      evidence_required: [deliv-adversarial-review-log]
      automation: human
    - id: test-borderline-escalation-path
      subject: claim-adversarial-review-final
      kind: consistency
      procedure: "If primary verdict was BORDERLINE, verify the escalation path: (a) Opus subagent spawned with Paper-5-primed rubric pass (per CONTEXT.md Decisions §Adversarial fresh-eyes review escalation path); (b) priming includes peirce-post-jordan-finding memory, A-S 2001 vs 2003 distinction, forbidden-token list, R1-R7 pitfalls, 'compressions preserve Peirce ≠ L_a preserves Peirce' (R2) explicit; (c) escalation verdict recorded."
      pass_condition: "BORDERLINE → escalation path is taken with correct priming; if primary PASS, escalation may be skipped."
      evidence_required: [deliv-adversarial-review-log]
      automation: human
    - id: test-result-md-all-sections-present
      subject: claim-phase-54-close
      kind: schema
      procedure: "Verify 54-RESULT.md has all required sections: (1) Outcome Tag, (2) Peirce-Preservation Lemma, (3) Proof or Axiom+Defense, (4) Closeout SymPy, (5) §3.3 Revision Text, (6) Adversarial Review, (7) (B)-Unavailability, (8) Cross-phase coupling. Grep for section headers."
      pass_condition: "All 8 sections present with explicit headers."
      evidence_required: [deliv-result-md]
      automation: automated
    - id: test-result-md-no-forbidden-tokens
      subject: claim-phase-54-close
      kind: consistency
      procedure: "Run forbidden-token grep on 54-RESULT.md. Allowed exception scope (same as revision text): canonical-example defense paragraph if outcome is (C-i); demarcated by section header or comment."
      pass_condition: "Zero hits outside the explicit canonical-example defense paragraph scope."
      evidence_required: [deliv-result-md]
      automation: automated
    - id: test-result-md-addendum-cite
      subject: claim-phase-54-close
      kind: existence
      procedure: "Grep 54-RESULT.md §(B)-Unavailability for an explicit citation of ADDENDUM (e.g., `(B) ruled out per ADDENDUM §[ref]`)."
      pass_condition: "ADDENDUM citation present in the required form."
      evidence_required: [deliv-result-md, ref-addendum]
      automation: automated
  forbidden_proxies:
    - id: fp-c-i-short-circuit
      subject: claim-s0-axiom-and-defense-if-ci
      proxy: "(C-i) revision text that reduces to 'by S0, the three invariances hold' without substantive derivation of (i), (ii), (iii) from {S0, S1, S3, linearity, compressions}."
      reason: "CONTEXT.md contract_coverage explicitly rejects this: 'revision text must have substantive derivation of V_2 and V_1 invariance from S0 + compressions, not merely state S0 and conclude.' R4's 20-line floor must be substantive content, not 'we axiomatized it, done'."
    - id: fp-c-i-s0-higher-level
      subject: claim-s0-axiom-and-defense-if-ci
      proxy: "S0 stated directly as 'Peirce invariance of L_a holds' rather than at the compression level '{C_{p_i}} pairwise commute AND C_{p_i} C_{p_j} = 0 for i ≠ j'."
      reason: "CONTEXT.md Decisions §S0 axiom form locks S0 at the compression level (weakest). Directly axiomatizing the invariance short-circuits the derivation; the referee sees a trivial lemma and rejects. Compression-level S0 leaves Peirce invariance as a substantive derived consequence."
    - id: fp-closeout-no-cross-term
      subject: claim-closeout-sympy-r3-coverage
      proxy: "closeout-sympy.py that only tests V_2(p_i) and V_1(p_i, p_j) with matching indices, not the R3 cross-term V_1(p_k, p_l) with {k,l}∩supp(a)=∅."
      reason: "R3 is the most commonly missed case per CONTEXT.md Pitfall 3; the per-attempt SymPy gate in Plan 54-02 does not probe cross-terms; closeout EXISTS to catch R3. Absence of cross-term test means Phase 54 closes with the proof's most subtle case un-verified numerically."
    - id: fp-revision-padding-r4
      subject: claim-s3-revision-and-exit-gate
      proxy: "Revision text that hits R4's 20-line floor by rhetorical filler: 'By the structure of the decomposition, it follows that...', 'Obviously...', 'Clearly...', 'Immediately follows without further argument...' without a specific cited theorem + page reference."
      reason: "R4 is a SUBSTANTIVE-content floor. Forbidden phrases: 'obvious', 'clearly', 'immediately follows' (unless followed by a specific reference). Adversarial reviewer is primed to catch padding."
    - id: fp-revision-in-submitted-tex
      subject: claim-s3-revision-and-exit-gate
      proxy: "Editing `main-jmp-submitted.tex` instead of `main.tex`."
      reason: "`main-jmp-submitted.tex` is FROZEN at git tag paper5-jmp-submitted per CONTEXT.md and user guidance. Revisions go into main.tex. Editing the submitted file corrupts the baseline."
    - id: fp-revision-forbidden-token-outside-defense
      subject: claim-s3-revision-and-exit-gate
      proxy: "Forbidden tokens (M_n(ℂ), pxp, Jordan, spin factor, etc.) appearing in the revision text outside the canonical-example defense paragraph (if (C-i))."
      reason: "The canonical-example defense paragraph is the ONLY scope where some forbidden tokens (M_n(ℂ)^sa, pxp-in-model, spin factors) are allowed. Outside the demarcated paragraph, tokens are strict-forbidden per CONTEXT.md forbidden-token list."
    - id: fp-exit-gate-grep-only
      subject: claim-s3-revision-and-exit-gate
      proxy: "Treating the exit gate as satisfied by grep alone, skipping the manual semantic review."
      reason: "CONTEXT.md locked decision: exit gate = grep AND manual semantic review. Grep is mechanical; it catches whitespace-level verbatim match. Manual review catches 'the lemma is stated but the proof doesn't actually prove the stated form' — both required."
    - id: fp-adversarial-review-skipped
      subject: claim-adversarial-review-final
      proxy: "Closing Phase 54 without a PASS verdict from `gpd-review-math` (primary) or escalation Opus."
      reason: "VALD-54-02 is a mandatory success criterion per ROADMAP Phase 54 §Success-Criteria. FAIL at primary + FAIL at escalation (if reached) BLOCKS Phase 54 close — this is a milestone pause trigger."
    - id: fp-adversarial-skip-escalation-for-borderline
      subject: claim-adversarial-review-final
      proxy: "Accepting a BORDERLINE verdict from gpd-review-math as sufficient for Phase 54 close, without escalating to Paper-5-primed Opus."
      reason: "CONTEXT.md Decisions §Adversarial fresh-eyes review: BORDERLINE triggers escalation. Accepting BORDERLINE directly skips the mitigation layer for the 'framing amplification' failure mode (Paper-5-primed Opus catches subtle cases gpd-review-math misses)."
    - id: fp-result-md-b-unavailability-omitted
      subject: claim-phase-54-close
      proxy: "54-RESULT.md that omits the '(B) ruled out per ADDENDUM' citation or phrases it as 'we didn't find an external citation'."
      reason: "CONTEXT.md anchor registry §ADDENDUM entry requires explicit citation as '(B) ruled out per ADDENDUM §[ref]'. The ADDENDUM is the formal basis for excluding (B); omission invites referee re-litigation."
  links:
    - id: link-plan-54-02-to-routing
      source: ref-plan-54-02
      target: claim-outcome-tag-consumed
      relation: depends_on
      verified_by: [test-plan-54-02-tag-consumed, test-result-md-outcome-tag]
    - id: link-s0-to-revision
      source: deliv-s0-axiom
      target: deliv-revision-text
      relation: supports
      verified_by: [test-s0-implies-peirce-invariance, test-revision-lemma-verbatim-grep]
    - id: link-closeout-to-result
      source: deliv-closeout-sympy
      target: deliv-result-md
      relation: supports
      verified_by: [test-closeout-r3-cross-term, test-closeout-separate-artifact]
    - id: link-revision-to-main-tex
      source: deliv-revision-text
      target: deliv-main-tex-integration
      relation: supports
      verified_by: [test-revision-in-main-tex-not-submitted]
    - id: link-revision-to-exit-gate
      source: deliv-revision-text
      target: claim-s3-revision-and-exit-gate
      relation: supports
      verified_by: [test-revision-lemma-verbatim-grep, test-revision-r4-line-count, test-revision-forbidden-token-discipline]
    - id: link-adversarial-to-close
      source: deliv-adversarial-review-log
      target: deliv-result-md
      relation: supports
      verified_by: [test-adversarial-review-pass-or-escalated]
    - id: link-as-notes-to-revision
      source: ref-as-notes
      target: deliv-revision-text
      relation: supports
      verified_by: [test-revision-lemma-verbatim-grep]
  uncertainty_markers:
    weakest_anchors:
      - "If Plan 54-01 marked multiple A-S compression axioms as VERIFICATION-DEFERRED or QUOTE-PENDING (because the A-S 2001 book was not accessible to the executor), the (C-i) S0 defense and the §3.3 revision text cite compression axioms that are resolved only by paraphrase in this iteration. Downstream Phase 55 extension of alfsen-shultz-notes.md must close these before its own use. Phase 54 close in this state is CONDITIONAL-on-downstream-verification, which is a milestone-level risk."
      - "The independence-of-S0-from-S1-S7 defense (test-s0-independence-defense) is Agent's Discretion (counterexample / parameter-counting / hybrid). A weak independence defense may be referee-attackable; RESEARCH.md Open Question 4 flags that framing matters ('in revising we found that the Peirce invariance step requires an OUS-level input beyond S1-S7' is the RECOMMENDED framing). If the independence defense is the WEAKEST link in the final review, adversarial reviewer likely flags it."
    disconfirming_observations:
      - "If the adversarial review returns FAIL with a specific objection that neither the (A) proof (if outcome is (A)) nor the S0 defense (if outcome is (C-i)) can address without adopting forbidden tools, Phase 54 close is BLOCKED and milestone pauses. This is the ROADMAP backtracking case: 'If adversarial fresh-eyes review in VALD-54-02 finds a prohibited structure, mark attempt-NN.md as failed and open the next attempt or pivot.' If we've exhausted attempts and pivoted to (C-i) and (C-i) still fails adversarial review, declare Phase 54 BLOCKED."
      - "If the exit-gate grep fails (lemma statement in claim.md does not grep-match the revision text), it means the revision drifted from the locked lemma statement during writing. This is a structural error — the named-lemma conditional form is the stable API; drift signals the writer did not treat claim.md as the API. Re-align revision to claim.md before closing."
      - "If the (C-i) canonical-example defense for M_n(ℂ)^sa, C(X), or spin factors fails to establish S0 is automatic (e.g., a subtle Clifford-relation-dependent step for spin factors breaks down), S0 is not defensible as 'automatic in canonical examples', which weakens the (C-i) position. Phase 54 CLOSE becomes conditional on the broken defense; consider declaring BLOCKED or milestone-pause per CONTEXT.md risk register line 867."

estimated_execution:
  total_minutes: 300
  breakdown:
    - task: 1
      minutes: 30
      note: "Read Plan 54-02 outcome tag; route subsequent tasks; if (A), branch to Task 2; if PIVOT-TO-C-I, branch to Task 3"
    - task: 2
      minutes: 20
      note: "(conditional on (A)) Reference the winning attempt-NN.md as the proof basis; summarize in 54-RESULT.md §Proof"
    - task: 3
      minutes: 120
      note: "(conditional on (C-i)) Author s0-axiom.md: S0 compression-level statement, three canonical-example defenses, independence defense, OUS-compatibility proof sketch"
    - task: 4
      minutes: 45
      note: "Author closeout-sympy.py with R3 cross-term coverage; run it; verify PASS in < 5 sec"
    - task: 5
      minutes: 60
      note: "Author paper5-s3-revision.tex (≥ 20 lines substantive, lemma statement verbatim from claim.md); integrate into main.tex §3.3; verify submitted file unchanged"
    - task: 6
      minutes: 20
      note: "Exit gate: lemma-statement grep against revision text PASS; manual semantic review PASS"
    - task: 7
      minutes: 30
      note: "Author 54-RESULT.md with all required sections; run adversarial review via gpd-review-math; record verdict"
    - task: 8
      minutes: 15
      note: "(conditional on BORDERLINE primary verdict) Escalate to Paper-5-primed Opus subagent; record escalation verdict"
    - task: 9
      minutes: 20
      note: "Checkpoint: final review of 54-RESULT.md, exit-gate verification, adversarial review verdict — Phase 54 close confirmation"

patterns_consulted:
  insights: []
  error_patterns: []
  adjustments_made:
    - "Closeout SymPy is a separate artifact per CONTEXT.md Decisions §SymPy validation wiring — it is cited in 54-RESULT.md by pointer, not buried in an attempt file"
    - "Canonical-example defense paragraph is the sole exception to forbidden-token discipline; scope must be explicitly demarcated in the LaTeX source"
    - "Exit gate is grep AND manual review (not one or the other) per CONTEXT.md locked decision"
    - "Adversarial review primary is gpd-review-math (not cold-read Opus); escalation to Paper-5-primed Opus only on BORDERLINE per CONTEXT.md Decisions §Adversarial fresh-eyes review"
---

<objective>
Close Phase 54 by routing on the Plan 54-02 outcome tag ((A) via attempt-NN OR PIVOT-TO-C-I). Produce: (if (C-i)) S0 axiom + three canonical-example defenses + independence defense + OUS-compatibility proof sketch; closeout SymPy with R3 cross-term coverage (separate artifact); §3.3 revision text integrated into `~/repos/research/paper5/main.tex` (≥ 20 lines substantive, lemma statement verbatim from claim.md); adversarial review via gpd-review-math with Phase 54 priming (escalation to Paper-5-primed Opus on BORDERLINE); exit gate (lemma-statement grep PASS + manual semantic review PASS); 54-RESULT.md with all required sections.

Purpose: Phase 54's deliverable gate. The named lemma is the stable citation target for Phases 55, 57, 58; the outcome tag routes Phase 58 Lean axiom re-classification; the §3.3 revision text is what the JMP referee reads. No Phase 54 close without PASS from adversarial review.
Output: s0-axiom.md (conditional on (C-i)); closeout-sympy.py; paper5-s3-revision.tex + main.tex integration; 54-ADVERSARIAL-REVIEW.md; 54-RESULT.md.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-CONTEXT.md
@.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESEARCH.md
@.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-01-PLAN.md
@.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-02-PLAN.md
@.gpd/research/ADDENDUM-independent-literature-check.md
@derivations/paper5-peirce-preservation/claim.md
@derivations/paper5-peirce-preservation/alfsen-shultz-notes.md
@derivations/paper5-peirce-preservation/attempt-log.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read Plan 54-02 outcome tag; route subsequent tasks</name>
  <files>.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md</files>
  <action>
    Read `derivations/paper5-peirce-preservation/attempt-log.md` and extract the final outcome tag:

    - If the tag is `(A) via attempt-NN`: record the winning attempt-NN; skip Task 3 (S0 axiom authoring); proceed with Tasks 2, 4, 5, 6, 7, 8, 9.
    - If the tag is `PIVOT-TO-C-I`: record the carry-forward objections; skip Task 2 (no (A) proof to reference); proceed with Tasks 3, 4, 5, 6, 7, 8, 9.
    - If no tag or ambiguous: halt with error; attempt-log.md did not satisfy Plan 54-02's test-plan-outcome-tag-binary acceptance test; return to Plan 54-02 for correction.

    Initialize `54-RESULT.md` with Section 1: Outcome Tag (exactly one of `(A)` or `(C-i)` — never `(C-ii)` unless a separate Phase 54 branch ran, which is outside this plan's scope). Forward the outcome tag to all subsequent tasks.
  </action>
  <verify>
    1. attempt-log.md outcome-tag line extracted unambiguously.
    2. 54-RESULT.md §Outcome Tag populated with exactly one outcome ((A) or (C-i)).
    3. Task routing decision (skip Task 2 or skip Task 3) recorded.
  </verify>
  <done>
    54-RESULT.md §Outcome Tag exists with binary value; task routing set.

    REJECTED: Composite or ambiguous tags (e.g., 'partial (A) with S0 supplement').
  </done>
</task>

<task type="auto">
  <name>Task 2: (conditional on (A)) Reference the winning attempt-NN.md as the proof basis</name>
  <files>.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md</files>
  <action>
    Execute only if Task 1 recorded outcome `(A) via attempt-NN`.

    Populate 54-RESULT.md §Proof with:
    - Pointer: `See `derivations/paper5-peirce-preservation/attempt-NN.md` for the full (A) proof.`
    - Brief summary (3-5 sentences): which approach (1 or 2) was used, the structural strategy, which A-S compression axioms were invoked, how R3 cross-term was handled.
    - Adversarial review PASS verdict summary.
    - Note: "The Peirce-Preservation Lemma is proved under the (A) assumption set {S1, S3, linearity, A-S compression axioms}; S0 is NOT needed."

    Set the active assumption-set clause in the lemma statement (Section 2) to `{S1, S3, linearity, A-S compression axioms}`.
  </action>
  <verify>
    1. 54-RESULT.md §Proof references the winning attempt-NN.md by filename.
    2. Assumption-set clause matches (A).
    3. No S0 axiom introduced in the (A) branch.
  </verify>
  <done>
    54-RESULT.md §Proof populated; (A)-branch routing complete for this task.
  </done>
</task>

<task type="auto">
  <name>Task 3: (conditional on (C-i)) Author s0-axiom.md</name>
  <files>derivations/paper5-peirce-preservation/s0-axiom.md</files>
  <action>
    Execute only if Task 1 recorded outcome `PIVOT-TO-C-I`.

    **Structure of s0-axiom.md:**

    **Section 1: Header**
    - Title: "S0 — OUS-level Peirce Coherence Axiom (Compression Level)" — or equivalent executor-chosen name per CONTEXT.md S0-naming-style Agent's Discretion
    - Convention block: copy from plan frontmatter
    - Provenance: "Produced in Phase 54-03 (C-i branch) after Plan 54-02 emitted PIVOT-TO-C-I with [N] failed (A) attempts. Carry-forward objections from attempt-log.md recorded in Section 6."

    **Section 2: S0 statement (compression level)**
    - Formal statement: "**Axiom S0 (Compression Coherence).** Let `V` be a finite-dim spectral OUS over ℝ with distinguished unit `1`, and let `{p_1, ..., p_n}` be an orthogonal family of projective units in `V`. Then:
      (a) the compressions `{C_{p_1}, ..., C_{p_n}}` pairwise commute: `C_{p_i} C_{p_j} = C_{p_j} C_{p_i}` for all i, j;
      (b) for i ≠ j, the compressions mutually annihilate on Peirce 2-spaces: `C_{p_i} C_{p_j} = 0`."
    - Commentary: "This is the WEAKEST form that yields the needed Peirce invariance (CONTEXT.md Decisions §S0 axiom form). Peirce invariance is DERIVED from S0 + {S1, S3, linearity, A-S compressions}, not axiomatized directly — this avoids the 'By S0, done' short-circuit."

    **Section 3: Canonical-example defenses**
    **(3.a) M_n(ℂ)^sa:**
    - "In M_n(ℂ)^sa, a projective unit p is a self-adjoint projection. By the spectral theorem, orthogonal projective units commute and their corresponding compressions `C_p(b) = pbp` (pxp-in-model computation, legal inside the matrix-algebra model per CONTEXT.md Decisions §Defense strategy 'pxp is forbidden in §3.3 broad scope, but legal inside the model') satisfy (a) pairwise commutation and (b) mutual annihilation for distinct orthogonal projectors. Thus S0 holds automatically in M_n(ℂ)^sa."
    - This paragraph is demarcated with a LaTeX comment or section marker `% BEGIN canonical-example defense` … `% END canonical-example defense` (forbidden-token exception scope).

    **(3.b) C(X) (commutative):**
    - "In the commutative algebra C(X) of continuous functions on a compact set X, a projective unit is the characteristic function of a measurable subset. Compressions are multiplications by characteristic functions; the orthogonality of projectors corresponds to disjoint supports. Multiplications by disjoint-support characteristic functions (a) pairwise commute trivially, and (b) mutually annihilate (`χ_A · χ_B = 0` for disjoint A, B). Thus S0 holds automatically in C(X)."

    **(3.c) Spin factors:**
    - "In a spin factor (Clifford algebra), the projective units associated with orthogonal generators satisfy the Clifford relation `{e_i, e_j} = 2 δ_{ij} 1`. This gives pairwise commutation of compressions (a) and mutual annihilation (b) directly. The use of the term 'spin factor' here is within the (C-i) canonical-example defense paragraph per CONTEXT.md Decisions §Defense strategy — scope-limited to this defense context."
    - Demarcated scope.

    **Section 4: Independence from S1-S7**
    - Agent's Discretion per CONTEXT.md: choose counterexample-model, parameter-counting, or hybrid.
    - Suggested counterexample-model form: exhibit a hypothetical finite-dim OUS satisfying S1-S7 but NOT satisfying S0 (e.g., a non-commutative OUS structure in which compressions on orthogonal projectors do not commute). This establishes S0 is not derivable from S1-S7.
    - Alternative parameter-counting form: dimension argument showing that the space of S1-S7-compatible compression families is strictly larger than the space of S0-compatible compression families.
    - Conclusion: "S0 is a genuine new OUS-level assumption, not a consequence of S1-S7."

    **Section 5: OUS-compatibility proof sketch (DERV-54-06)**
    Show that under `{S0, S1, S3, linearity, A-S compressions}`:

    **(5.a) `a ∘ V_2(p_i) ⊆ V_2(p_i)`:** For `a = Σ_j λ_j p_j` and `b ∈ V_2(p_i)` (i.e., `b = C_{p_i}(b)`):
    - `a ∘ b = Σ_j λ_j (p_j ∘ b)` by S1 additivity and linearity
    - `p_j ∘ b = C_{p_j}(b)` by S3 sharp constraint
    - `C_{p_j}(b) = C_{p_j}(C_{p_i}(b)) = 0` for `j ≠ i` by S0(b) (mutual annihilation); `= b` for `j = i` (compression idempotency, `C_{p_i}² = C_{p_i}`)
    - Therefore `a ∘ b = λ_i b`, which is in `V_2(p_i)`. Q.E.D.

    **(5.b) `a ∘ V_1(p_i, p_j) ⊆ V_1(p_i, p_j)`:** For `b ∈ V_1(p_i, p_j)` with i, j ∈ supp(a):
    - Decompose `b = b_i + b_j - b_{ij}` (where `b_i = C_{p_i}(b), b_j = C_{p_j}(b)`, etc., per V_1 definition) — use S0(a) pairwise commutation to order compressions consistently
    - Apply `a ∘` to each term; show each lands in V_1(p_i, p_j) by S0(b) annihilation of cross-terms + compression algebra
    - Brief explicit sketch; full detail in the §3.3 revision text paper5-s3-revision.tex

    **(5.c) R3 cross-term `a ∘ V_1(p_k, p_l) ⊆ V_1(p_k, p_l)` for `{k, l} ∩ supp(a) = ∅`:** For `b ∈ V_1(p_k, p_l)`:
    - `a = Σ_{j ∈ supp(a)} λ_j p_j`; each `C_{p_j}` with `j ∈ supp(a)` annihilates V_1(p_k, p_l) — because, by S0(b), `C_{p_j} C_{p_k} = 0` and `C_{p_j} C_{p_l} = 0` (since `j ≠ k, j ≠ l`), so any element of V_1(p_k, p_l) lies in the joint kernel of the `C_{p_j}` with j ∈ supp(a)
    - Therefore `a ∘ b = Σ_{j ∈ supp(a)} λ_j C_{p_j}(b) = 0`
    - `0 ∈ V_1(p_k, p_l)` trivially. Q.E.D.
    - Note: the cross-term subspace is ANNIHILATED by L_a, not just preserved. This is stronger than mere preservation; it matches the expected Peirce-invariance behavior.

    **Section 6: Carry-forward objections from Plan 54-02**
    - Copy VERBATIM the carry-forward block from attempt-log.md (if PIVOT-TO-C-I).
    - For each objection: "This objection [quote] is resolved by S0 because [brief mechanism]."
    - This section motivates S0's necessity from the accumulated (A) attempt failure record.

    **Section 7: References**
    - Niestegge 2008 (`U_e` compression convention, OUS-level compression-base axiomatization precedent)
    - A-S 2001 vol. 179 Ch. 7-8 (compression axioms, via alfsen-shultz-notes.md)
    - vdW 2019 Def. 2 (S1, S3, Def. 7 sharp effects)
    - claim.md (lemma target)
    - 54-CONTEXT.md Decisions §S0 axiom form, §Defense strategy
    - ADDENDUM ((B)-unavailability)
  </action>
  <verify>
    1. s0-axiom.md exists with all 7 sections.
    2. S0 statement is at compression level (test-s0-statement-compression-level); Peirce invariance is DERIVED, not directly axiomatized.
    3. Three canonical-example defenses present, each scope-demarcated (test-s0-canonical-defenses).
    4. Independence-from-S1-S7 defense present in one of the permitted forms (test-s0-independence-defense).
    5. OUS-compatibility sketch covers (i), (ii), (iii) — including R3 cross-term explicitly (test-s0-implies-peirce-invariance).
    6. Carry-forward objections section present (if PIVOT-TO-C-I branch).
    7. Forbidden tokens appear ONLY within the demarcated canonical-example defense paragraphs.
  </verify>
  <done>
    s0-axiom.md exists with compression-level S0, three canonical-example defenses, independence defense, OUS-compatibility proof sketch covering R3 cross-term, carry-forward objections, and references.

    REJECTED (forbidden proxies): fp-c-i-short-circuit ('by S0, done'), fp-c-i-s0-higher-level (S0 stated at invariance level instead of compression level).
  </done>
</task>

<task type="auto">
  <name>Task 4: Author closeout-sympy.py with R3 cross-term coverage</name>
  <files>derivations/paper5-peirce-preservation/closeout-sympy.py</files>
  <action>
    Produce a separate closeout SymPy artifact covering the R3 cross-term case. This is NOT embedded in any attempt-NN.py — downstream phases (58, Lean sync) must re-run closeout-sympy.py against the final lemma statement without excavating from an attempt file.

    ```python
    #!/usr/bin/env python3
    """
    Closeout SymPy for Phase 54 Peirce-Preservation Lemma.

    Covers V_2(p_i), V_1(p_i, p_j) [matching indices], and R3 cross-term
    V_1(p_k, p_l) for {k, l} ∩ supp(a) = ∅.

    At least three orthogonal rank-1 projectors on H_3(ℝ) or H_4(ℝ).
    Symbolic-exact (sympy.Rational or sympy.Symbol).
    Runtime < 5 sec.
    """
    import sympy as sp
    import time

    start = time.time()

    # H_4(ℝ): symmetric real 4x4 matrices; four orthogonal rank-1 diagonal projectors
    # (use H_4 to get a non-trivial cross-term: supp(a) = {1, 2}, cross-term indices {3, 4})
    p1 = sp.diag(1, 0, 0, 0)
    p2 = sp.diag(0, 1, 0, 0)
    p3 = sp.diag(0, 0, 1, 0)
    p4 = sp.diag(0, 0, 0, 1)

    # Symbolic scalar weights for a = λ_1 p_1 + λ_2 p_2 (supp(a) = {1, 2})
    lam1, lam2 = sp.symbols('lambda_1 lambda_2', real=True)
    a = lam1 * p1 + lam2 * p2

    # Executor: plug in the actual sequential product formula from the outcome:
    # - If (A): use the derivation from the winning attempt-NN.md
    # - If (C-i): use the derivation from s0-axiom.md §OUS-compatibility sketch
    # The sequential product a ∘ b is NOT implemented via pxp (forbidden token);
    # it is implemented via the A-S compression characterization + S0 structure.
    # For this H_4(ℝ) rank-1-diagonal-projector symbolic model, the compression
    # C_{p_i} acts as extraction of the (i,i) entry — an INSTANTIATION of the
    # abstract compression in the concrete model, documented as such.

    def seq_prod(a, b):
        """a ∘ b for the lemma's active assumption set (A or C-i).
        Implementation instantiates the abstract formula in the concrete H_4(ℝ) model."""
        # Executor: fill this in with the actual derivation from the outcome artifact
        # For H_4(ℝ) with a = Σ λᵢ pᵢ (diagonal projectors) and b self-adjoint,
        # the S1-additive + S3-sharp-constraint + linearity-over-basis expression yields
        # a specific symbolic result. Reference the outcome artifact for the exact formula.
        raise NotImplementedError("Executor fills in based on outcome (A) proof or (C-i) s0-axiom.md §5")

    def compress(b, p):
        """A-S compression C_p(b) realized in H_4(ℝ) as block extraction onto range(p).
        Documented as an INSTANTIATION of the abstract compression; not used as a proof
        device for the abstract lemma."""
        # For diagonal projectors on H_4(ℝ), C_p(b) extracts the block where p = 1
        # (This is the model instantiation, not the proof device — the abstract lemma
        # is established in s0-axiom.md / attempt-NN.md without matrix-block structure.)
        # Implementation: mask b by p (pre- and post-multiply is forbidden as a proof device;
        # implement via explicit entry extraction matching the diagonal-projector structure)
        # ... executor provides the correct implementation
        raise NotImplementedError("Executor fills in")

    # Test (i): b ∈ V_2(p_1), i.e., b = C_{p_1}(b). Verify a ∘ b ∈ V_2(p_1).
    x = sp.Symbol('x', real=True)
    b_V2 = sp.zeros(4); b_V2[0, 0] = x  # element of V_2(p_1)
    result_V2 = seq_prod(a, b_V2)
    assert compress(result_V2, p1) == result_V2, "V_2(p_1) invariance FAILED"
    print("Test (i) V_2(p_1) invariance PASS")

    # Test (ii): b ∈ V_1(p_1, p_2), standard case. Verify a ∘ b ∈ V_1(p_1, p_2).
    y = sp.Symbol('y', real=True)
    b_V1 = sp.zeros(4); b_V1[0, 1] = y; b_V1[1, 0] = y  # element of V_1(p_1, p_2)
    result_V1 = seq_prod(a, b_V1)
    # Verify result_V1 is in V_1(p_1, p_2): its only non-zero entries are at (1,2) and (2,1)
    assert result_V1[0, 0] == 0 and result_V1[1, 1] == 0, "V_1 diagonal contamination"
    assert result_V1[2, 2] == 0 and result_V1[3, 3] == 0, "V_1 off-support contamination"
    assert result_V1[0, 2] == 0 and result_V1[0, 3] == 0, "V_1 off-support entries"
    print("Test (ii) V_1(p_1, p_2) invariance PASS")

    # Test (iii) R3: b ∈ V_1(p_3, p_4) with {3, 4} ∩ supp(a) = ∅ (supp(a) = {1, 2}).
    # Verify a ∘ b ∈ V_1(p_3, p_4) — should be ANNIHILATED (= 0), which is trivially in V_1(p_3, p_4).
    z = sp.Symbol('z', real=True)
    b_cross = sp.zeros(4); b_cross[2, 3] = z; b_cross[3, 2] = z  # element of V_1(p_3, p_4)
    result_cross = seq_prod(a, b_cross)
    assert result_cross == sp.zeros(4), "R3 cross-term: a ∘ V_1(p_3, p_4) should be 0 when supp(a) = {1,2}"
    print("Test (iii) R3 cross-term V_1(p_3, p_4) invariance PASS (annihilation)")

    elapsed = time.time() - start
    print(f"\nRuntime: {elapsed:.3f} sec (budget: < 5 sec)")
    assert elapsed < 5, f"Runtime {elapsed:.3f} sec exceeds 5 sec budget"

    print("\nCloseout SymPy: ALL TESTS PASS")
    ```

    Executor note: the `seq_prod` and `compress` implementations are placeholders. Fill in based on the outcome-specific derivation (A-branch references the winning attempt-NN.md; (C-i) branch references s0-axiom.md §5). The TEST LOGIC stays the same; only the structural implementation changes.

    Execute `python3 derivations/paper5-peirce-preservation/closeout-sympy.py`; verify all three tests PASS and runtime < 5 sec.
  </action>
  <verify>
    1. closeout-sympy.py exists at the correct path.
    2. Three test assertions cover V_2(p_1), V_1(p_1, p_2), and R3 cross-term V_1(p_3, p_4).
    3. At least three (here: four) orthogonal rank-1 projectors used.
    4. Symbolic-exact (sympy.Rational or sympy.Symbol).
    5. Exit code 0 on execution; all three tests print PASS.
    6. Runtime < 5 sec.
    7. File is REFERENCED in 54-RESULT.md §Closeout SymPy by pointer (not embedded in an attempt-NN.py).
  </verify>
  <done>
    closeout-sympy.py exists as separate artifact; runs successfully with R3 cross-term PASS in < 5 sec; referenced in 54-RESULT.md.

    REJECTED (forbidden proxies): fp-closeout-no-cross-term (closeout that only tests V_2 and V_1-with-matching-indices, skipping R3 cross-term).
  </done>
</task>

<task type="auto">
  <name>Task 5: Author paper5-s3-revision.tex and integrate into main.tex §3.3</name>
  <files>derivations/paper5-peirce-preservation/paper5-s3-revision.tex, ~/repos/research/paper5/main.tex</files>
  <action>
    **Part A: Author `derivations/paper5-peirce-preservation/paper5-s3-revision.tex`**

    This is the staged revision text, ≥ 20 lines of substantive content. Structure:

    **(1) Brief setup (1-2 lines):** "Let `V` be a finite-dim spectral OUS with `a = Σᵢ λᵢ pᵢ` a spectral decomposition with orthogonal projective units `{p_i}`. We establish the preservation of Peirce subspaces under `L_a(b) := a ∘ b`."

    **(2) Lemma statement verbatim from claim.md:**
    - Copy the Peirce-Preservation Lemma statement from claim.md with the active assumption-set clause:
      - If (A): "Under {S1, S3, linearity, A-S compression axioms}, …"
      - If (C-i): "Under {S0, S1, S3, linearity, A-S compression axioms}, …"
    - Three target inclusions (i), (ii), (iii) as separate sub-claims — anti-R2, anti-R3.

    **(3) Proof / axiomatic derivation (≥ 15 lines substantive):**
    - If (A): present the proof from the winning attempt-NN.md. Reference A-S compression axioms via alfsen-shultz-notes.md with explicit volume/chapter/prop-thm numbers (no paraphrase).
    - If (C-i):
      - State S0 axiom (one paragraph).
      - Canonical-example defense paragraph (M_n(ℂ)^sa + C(X) + spin factors) — THIS IS THE ONLY SCOPE WHERE FORBIDDEN TOKENS ARE ALLOWED. Demarcate with a LaTeX comment: `% BEGIN canonical-example defense for S0` … `% END canonical-example defense for S0`.
      - Derive (i), (ii), (iii) from S0 + {S1, S3, linearity, A-S compressions} per s0-axiom.md §5.
    - Forbidden phrases: 'obvious', 'clearly', 'immediately follows' (unless followed by a specific cited theorem + page reference).

    **(4) Explicit R3 cross-term statement (1-2 lines):** "Note that the cross-term case `a ∘ V_1(p_k, p_l) ⊆ V_1(p_k, p_l)` for `{k, l} ∩ supp(a) = ∅` is established by the compression annihilation (S0(b)) — the cross-term subspace is annihilated by L_a."

    **(5) Brief note on (B)-unavailability (1 line):** "An external citation resolution route (B) is ruled out per ADDENDUM; accessible OUS-native Peirce-preservation theorems do not exist pre-Jordan."

    **Part B: Integrate into `~/repos/research/paper5/main.tex`**

    - Locate the §3.3 region of main.tex (should be structurally identical to main-jmp-submitted.tex §3.3 lines 483-562 unless main.tex has diverged in other sections).
    - Replace the submitted non-sequitur (lines 508-528 of the submitted version) with the paper5-s3-revision.tex content.
    - Do NOT modify main-jmp-submitted.tex — it is frozen at git tag paper5-jmp-submitted (verify via `git diff paper5-jmp-submitted -- main-jmp-submitted.tex`; should show zero changes).

    **Part C: Verify**
    - Count substantive lines in the revision (≥ 20 per R4).
    - Forbidden-token grep on paper5-s3-revision.tex AND on the main.tex §3.3 region (main.tex §3.3 lines):
      - If (A): zero hits.
      - If (C-i): hits ONLY within the demarcated canonical-example defense paragraph.
    - Verify main-jmp-submitted.tex is unchanged: `git diff paper5-jmp-submitted -- ~/repos/research/paper5/main-jmp-submitted.tex` empty.
  </action>
  <verify>
    1. paper5-s3-revision.tex exists with the five structural parts.
    2. Line count ≥ 20 substantive lines (test-revision-r4-line-count).
    3. Lemma statement verbatim from claim.md — grep-match against paper5-s3-revision.tex AND main.tex §3.3 (test-revision-lemma-verbatim-grep).
    4. Forbidden-token grep: zero hits outside the demarcated canonical-example defense paragraph (test-revision-forbidden-token-discipline).
    5. main-jmp-submitted.tex unchanged (test-revision-in-main-tex-not-submitted).
    6. main.tex §3.3 integration complete; git diff shows only §3.3 region modified.
  </verify>
  <done>
    paper5-s3-revision.tex exists; integrated into main.tex §3.3; R4 line count satisfied; lemma statement grep passes; forbidden-token discipline preserved; submitted file unchanged.

    REJECTED (forbidden proxies): fp-revision-padding-r4 ('obvious', 'clearly' padding to meet 20 lines); fp-revision-in-submitted-tex (editing main-jmp-submitted.tex); fp-revision-forbidden-token-outside-defense (forbidden tokens outside demarcated scope).
  </done>
</task>

<task type="auto">
  <name>Task 6: Exit gate — lemma-statement grep + manual semantic review</name>
  <files>.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md</files>
  <action>
    Execute the exit gate (both halves required per CONTEXT.md locked decision):

    **Half A (mechanical grep):**
    - Extract the Peirce-Preservation Lemma statement from claim.md (the entire lemma block from the "**Lemma …**" opening through the third target inclusion (iii)).
    - Grep the exact statement (modulo whitespace) against paper5-s3-revision.tex AND against the main.tex §3.3 region.
    - Both greps must return at least one match. If either returns zero matches, mark exit-gate HALF-A FAIL; return to Task 5 for alignment.

    **Half B (manual semantic review):**
    - Read the revision text end-to-end in the context of the submitted Paper 5 §3.3 lines 483-562.
    - Verify:
      - The revision REPLACES the submitted non-sequitur on lines ~508-528 (rather than adding alongside it).
      - The three target inclusions are PROVED or AXIOMATIZED+DERIVED — not merely stated as theorems then assumed true.
      - If (C-i), S0 is stated at compression level (not invariance level).
      - The canonical-example defense paragraph scope is demarcated.
      - R3 cross-term case is explicitly covered in the text.
    - Mark exit-gate HALF-B PASS or FAIL with a specific line reference for FAIL.

    **Record the exit gate verdict in 54-RESULT.md §§3.3 Revision Text:**
    - Exit gate HALF-A (grep): PASS / FAIL
    - Exit gate HALF-B (manual semantic review): PASS / FAIL
    - Overall: PASS (both halves PASS) / FAIL (either half FAIL)
  </action>
  <verify>
    1. HALF-A grep run and verdict recorded.
    2. HALF-B manual review completed and verdict recorded.
    3. Overall exit-gate verdict is PASS (both halves) or FAIL (at least one half).
    4. FAIL returns Task 5 for correction; PASS proceeds to Task 7.
  </verify>
  <done>
    Exit gate verdict recorded in 54-RESULT.md. If PASS, proceed to Task 7. If FAIL, return to Task 5.

    REJECTED (forbidden proxies): fp-exit-gate-grep-only (treating grep PASS alone as sufficient; skipping the manual review).
  </done>
</task>

<task type="auto">
  <name>Task 7: Author 54-RESULT.md final structure + spawn gpd-review-math adversarial review (primary)</name>
  <files>.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md, .gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-ADVERSARIAL-REVIEW.md</files>
  <action>
    **Part A: Populate 54-RESULT.md with all required sections:**

    1. **Outcome Tag:** `Outcome: (A)` or `Outcome: (C-i)` (exactly one line).
    2. **Peirce-Preservation Lemma:** statement verbatim from claim.md with active assumption-set clause.
    3. **Proof or Axiom+Defense:**
       - If (A): pointer to winning attempt-NN.md + brief summary.
       - If (C-i): inline S0 axiom statement + canonical-example defense (demarcated) + OUS-compatibility sketch summary + pointer to s0-axiom.md for full detail.
    4. **Closeout SymPy:** result of closeout-sympy.py — three tests (V_2, V_1 standard, R3 cross-term) PASS; runtime ~ _____ sec; pointer to the artifact.
    5. **§3.3 Revision Text:** pointer to paper5-s3-revision.tex and main.tex §3.3 region; exit-gate record (HALF-A grep PASS / HALF-B manual review PASS) from Task 6.
    6. **Adversarial Review:** (populated in Part B below).
    7. **(B)-Unavailability:** "(B) ruled out per ADDENDUM (`.gpd/research/ADDENDUM-independent-literature-check.md`). Accessible pre-Jordan OUS-native Peirce-preservation theorems do not exist; A-S 2003 Thm 9.37 is post-Jordan per ADDENDUM and is not a valid pre-Jordan citation."
    8. **Cross-phase coupling (DERV-00-01 Phase 54 slice):**
       - Phase 55 (S4 facial structure lemma): consumes the Peirce-Preservation Lemma; if outcome is (C-i), Phase 55's S4 argument must reference S0 rather than implicit Peirce appeal.
       - Phase 57 (phi-inertness): may share R11 restructuring if outcome is (C-i).
       - Phase 58 (Lean axiom audit): re-classifies `_peirce_preservation` axiom based on outcome tag — (A) → theorem-in-disguise (promote to theorem); (C-i) → type-iv primitive with S0 defense documentation.
       - `alfsen-shultz-notes.md` shared artifact available to Phases 55, 57, 58; change-log tracks extensions.

    **Part B: Adversarial review via gpd-review-math (primary, VALD-54-02)**

    Spawn `gpd-review-math` subagent with Phase 54 priming. Priming MUST include:
    - 54-CONTEXT.md (locked user decisions)
    - 54-RESEARCH.md (approach routing, pitfalls)
    - `derivations/paper5-peirce-preservation/alfsen-shultz-notes.md`
    - `derivations/paper5-peirce-preservation/claim.md`
    - The `peirce-post-jordan-finding` memory (prior Jordan-smuggling incident)
    - Forbidden-token list (from plan frontmatter)
    - R1-R5 pitfalls with verbatim descriptions
    - 'CIRCULAR vs IDENTITY' framing discipline per sms:minimal (reviewer MUST state which framing it is applying when flagging circularity)
    - 'compressions preserve Peirce ≠ L_a preserves Peirce' (R2) explicit warning
    - Artifacts to review: 54-RESULT.md + paper5-s3-revision.tex + main.tex §3.3 + s0-axiom.md (if (C-i)) + closeout-sympy.py output

    Reviewer task: "Review these Phase 54 artifacts for Jordan-smuggling, R1-R5 pitfalls, and closure integrity. Return PASS if no prohibited structure was silently invoked AND the three target inclusions are proven or properly axiomatized with substantive derivation. Return BORDERLINE if you find a concerning but non-decisive pattern that warrants escalation. Return FAIL with a specific line-number quote of the problem, distinguishing CIRCULAR (proof-step inversion) from IDENTITY (definitional clause that looks self-referential but isn't a proof step)."

    Record the review output verbatim in 54-ADVERSARIAL-REVIEW.md:
    - Header: timestamp, reviewer = gpd-review-math, priming content list
    - Primary verdict: PASS / BORDERLINE / FAIL
    - Full review text verbatim
    - If FAIL: specific objection quote (for carry-forward if a future attempt is needed)
    - If BORDERLINE: reason flagged; escalation path activated (Task 8)

    Populate 54-RESULT.md §Adversarial Review with:
    - Primary reviewer: gpd-review-math with Phase 54 priming
    - Primary verdict: [from review]
    - Pointer to 54-ADVERSARIAL-REVIEW.md for full text
  </action>
  <verify>
    1. 54-RESULT.md has all 8 required sections (test-result-md-all-sections-present).
    2. Outcome tag regex-matches `^Outcome: \\((A|C-i|C-ii)\\)$` (test-result-md-outcome-tag).
    3. ADDENDUM cite present in §(B)-Unavailability (test-result-md-addendum-cite).
    4. 54-ADVERSARIAL-REVIEW.md records primary review with explicit priming content (test-adversarial-review-invoked).
    5. Primary verdict recorded (PASS / BORDERLINE / FAIL).
    6. Forbidden-token grep on 54-RESULT.md: zero hits outside canonical-example defense paragraph (if (C-i)) (test-result-md-no-forbidden-tokens).
  </verify>
  <done>
    54-RESULT.md populated with all 8 sections; adversarial review (primary) executed via gpd-review-math with full Phase 54 priming; verdict recorded.

    REJECTED (forbidden proxies): fp-adversarial-review-skipped (skipping gpd-review-math); fp-result-md-b-unavailability-omitted (missing ADDENDUM cite).
  </done>
</task>

<task type="auto">
  <name>Task 8: (conditional on BORDERLINE primary) Escalate to Paper-5-primed Opus subagent</name>
  <files>.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-ADVERSARIAL-REVIEW.md, .gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md</files>
  <action>
    Execute only if Task 7's primary gpd-review-math verdict was BORDERLINE.

    Per CONTEXT.md Decisions §Adversarial fresh-eyes review escalation path: spawn a general-purpose Opus subagent with Paper-5-primed rubric priming. Priming MUST include:
    - `peirce-post-jordan-finding` memory
    - A-S 2001 vol. 179 vs 2003 vol. 190 distinction (from alfsen-shultz-notes.md)
    - Forbidden-token list
    - R1-R7 pitfalls (not just R1-R5 — R6 C-iii drift and R7 "carries" ambiguity included for completeness even though R7 is Phase 56's concern)
    - "compressions preserve Peirce ≠ L_a preserves Peirce" (R2) explicit warning
    - CIRCULAR-vs-IDENTITY framing discipline

    Escalation reviewer task: "The primary reviewer (gpd-review-math) returned BORDERLINE with reason [X]. Review the same Phase 54 artifacts with the Paper-5-primed rubric. Return PASS / FAIL (no BORDERLINE at escalation — provide a decisive verdict). If FAIL, supply a specific line-number quote and frame as CIRCULAR or IDENTITY."

    Record the escalation verdict in 54-ADVERSARIAL-REVIEW.md and update 54-RESULT.md §Adversarial Review:
    - Escalation reviewer: Paper-5-primed Opus subagent
    - Escalation verdict: PASS / FAIL
    - Full escalation review text verbatim
    - Final basis for Phase 54 close: "PASS at primary" OR "PASS at escalation" OR "FAIL (milestone pause triggered)".

    If escalation returns FAIL, Phase 54 CANNOT close. Trigger the milestone pause per CONTEXT.md / ROADMAP Phase 54 §Backtracking. Do NOT update 54-RESULT.md with a close status; instead, add a §MILESTONE-PAUSE section recording the blocker and forwarding the verbatim FAIL objection to the milestone-level human decision queue.
  </action>
  <verify>
    1. Opus subagent spawned with Paper-5-primed rubric priming (test-borderline-escalation-path).
    2. Priming content list explicit in 54-ADVERSARIAL-REVIEW.md.
    3. Escalation verdict PASS or FAIL (no BORDERLINE at escalation).
    4. If FAIL: milestone-pause section added to 54-RESULT.md; Phase 54 does not close.
    5. If PASS: Phase 54 close basis = "PASS at escalation".
  </verify>
  <done>
    Escalation path executed (if triggered); verdict recorded; Phase 54 close basis explicit.

    REJECTED (forbidden proxies): fp-adversarial-skip-escalation-for-borderline (accepting BORDERLINE as sufficient for close).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Phase 54 close confirmation</name>
  <files>.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md, .gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-ADVERSARIAL-REVIEW.md, derivations/paper5-peirce-preservation/paper5-s3-revision.tex, ~/repos/research/paper5/main.tex, derivations/paper5-peirce-preservation/closeout-sympy.py, derivations/paper5-peirce-preservation/s0-axiom.md, derivations/paper5-peirce-preservation/alfsen-shultz-notes.md, derivations/paper5-peirce-preservation/claim.md, derivations/paper5-peirce-preservation/audit-04-06.md, derivations/paper5-peirce-preservation/attempt-log.md</files>
  <action>
    Present the full Phase 54 deliverable set to the user for final close confirmation.

    **Final Phase 54 close checklist (user reviews):**
    - [ ] 54-RESULT.md outcome tag is exactly (A) or (C-i) (or (C-ii) only if separate branch was activated).
    - [ ] Named lemma statement is verbatim from claim.md with correct assumption-set clause.
    - [ ] (If (A)) attempt-NN.md exists with CLOSURE status and all three early gates + adversarial review PASSED.
    - [ ] (If (C-i)) s0-axiom.md exists with S0 at compression level, three canonical-example defenses, independence defense, OUS-compatibility sketch covering R3.
    - [ ] closeout-sympy.py ran with all three assertions PASS (V_2, V_1 standard, R3 cross-term) in < 5 sec.
    - [ ] §3.3 revision text integrated into main.tex (NOT main-jmp-submitted.tex); R4 line count ≥ 20 substantive; forbidden-token discipline preserved (exception: demarcated canonical-example defense paragraph for (C-i)).
    - [ ] Exit gate: HALF-A grep PASS + HALF-B manual semantic review PASS (both).
    - [ ] Adversarial review: PASS at primary (gpd-review-math) OR PASS at escalation (Paper-5-primed Opus).
    - [ ] ADDENDUM cited as (B)-unavailability basis.
    - [ ] Cross-phase coupling notes for Phases 55, 57, 58 explicit.
    - [ ] alfsen-shultz-notes.md finalized (any VERIFICATION-DEFERRED / QUOTE-PENDING entries flagged for Phase 55 / 57 / 58 follow-up).

    **Options to present to user:**

    - **If all items PASS:** Resume signal `close-phase-54` — authorize Phase 54 close; emit the final outcome tag to the milestone tracker; Phases 55, 56, 57, 58 become eligible to run per ROADMAP dependency graph.
    - **If a remediable item FAILS:** Resume signal `return-to-task-NN` — go back to the failing plan's task for remediation.
    - **If a hard blocker is reached (e.g., adversarial review FAIL at escalation):** Resume signal `milestone-pause` — do not close Phase 54; trigger the milestone pause per CONTEXT.md / ROADMAP Phase 54 §Backtracking.
  </action>
  <verify>
    1. User has walked through the checklist and selected one of: close-phase-54, return-to-task-NN, milestone-pause.
    2. If close-phase-54: every checklist item confirmed PASS.
    3. If return-to-task-NN: specific failing task identified; remediation path recorded.
    4. If milestone-pause: hard-blocker basis recorded; 54-RESULT.md §MILESTONE-PAUSE populated.
  </verify>
  <done>
    Phase 54 close decision recorded: either cleanly closed, looping back to a remediable task, or escalated to milestone pause.

    REJECTED: close-phase-54 with any checklist item failing; milestone-pause without explicit hard-blocker basis.
  </done>
</task>

</tasks>

<verification>
- **Outcome routing:** Plan 54-03 consumes the Plan 54-02 outcome tag unambiguously; (A) branch or (C-i) branch executes exactly one.
- **S0 form:** compression-level weakest statement (if (C-i)); Peirce invariance DERIVED, not directly axiomatized.
- **R3 cross-term coverage:** closeout-sympy.py tests V_1(p_k, p_l) for {k, l} ∩ supp(a) = ∅ explicitly.
- **R4 line count:** §3.3 revision text ≥ 20 substantive lines; no padding.
- **Forbidden-token discipline:** zero hits outside the demarcated canonical-example defense paragraph (if (C-i)).
- **Exit gate:** BOTH lemma-statement grep PASS AND manual semantic review PASS required for Phase 54 close.
- **Adversarial review:** PASS at primary OR PASS at escalation; FAIL at escalation blocks Phase 54 close.
- **main.tex target:** revision integrated into main.tex; main-jmp-submitted.tex unchanged.
- **ADDENDUM cite:** (B)-unavailability explicit in 54-RESULT.md.
- **Cross-phase coupling:** Phase 55/57/58 routing notes explicit for DERV-00-01 Phase 54 slice.
</verification>

<success_criteria>
- 54-RESULT.md exists with all 8 required sections (Outcome Tag, Lemma, Proof/Axiom+Defense, Closeout SymPy, §3.3 Revision Text, Adversarial Review, (B)-Unavailability, Cross-phase coupling).
- (If (C-i)) s0-axiom.md exists with compression-level S0, three canonical-example defenses, independence defense, OUS-compatibility sketch including R3 cross-term.
- closeout-sympy.py exists as SEPARATE artifact (not embedded in attempt-NN.py); runs with R3 cross-term PASS in < 5 sec; referenced in 54-RESULT.md.
- paper5-s3-revision.tex exists with ≥ 20 substantive lines; integrated into main.tex §3.3; submitted file unchanged.
- Exit gate PASS: lemma-statement grep match in paper5-s3-revision.tex AND main.tex §3.3; manual semantic review PASS.
- Adversarial review (VALD-54-02) PASS at primary (gpd-review-math) OR at escalation (Paper-5-primed Opus); PASS recorded in 54-ADVERSARIAL-REVIEW.md.
- No forbidden token used outside the demarcated canonical-example defense paragraph.
- Cross-phase coupling notes for Phases 55, 57, 58 explicit in 54-RESULT.md.
- alfsen-shultz-notes.md finalized with change-log entry for Phase 54-03 close.
- Plan 54-03 validates via `gpd validate plan-contract` with zero errors.
</success_criteria>

<output>
After completion, create `.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-03-SUMMARY.md` and update the phase-level 54-SUMMARY.md (aggregating 54-01-SUMMARY.md, 54-02-SUMMARY.md, 54-03-SUMMARY.md) recording: outcome tag, exit-gate verdict, adversarial review verdict, any VERIFICATION-DEFERRED / QUOTE-PENDING entries forwarded to Phases 55, 57, 58, and the cross-phase coupling routing for DERV-00-01 Phase 54 slice.
</output>
