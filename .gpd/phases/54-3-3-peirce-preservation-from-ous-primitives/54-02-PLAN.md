---
phase: 54-3-3-peirce-preservation-from-ous-primitives
plan: 02
type: execute
wave: 2
depends_on: ["54-01"]
files_modified:
  - derivations/paper5-peirce-preservation/attempt-01.md
  - derivations/paper5-peirce-preservation/attempt-01.py
  - derivations/paper5-peirce-preservation/attempt-02.md
  - derivations/paper5-peirce-preservation/attempt-02.py
  - derivations/paper5-peirce-preservation/attempt-03.md
  - derivations/paper5-peirce-preservation/attempt-03.py
  - derivations/paper5-peirce-preservation/attempt-log.md
interactive: true

conventions:
  units: "N/A (pure algebra)"
  sequential_product_symbol: "a ∘ b (Paper 5 convention)"
  compression_symbol: "C_p"
  order_unit_space: "finite-dim archimedean OUS over ℝ with distinguished unit 1"
  peirce_2_space: "V_2(p_i) := range(C_{p_i})"
  peirce_1_space: "V_1(p_i, p_j) := (C_{p_i} + C_{p_j})V - C_{p_i}V - C_{p_j}V"
  allowed_axiom_scope: "S1 (additivity in 2nd arg), S3 (unitality + sharp constraint), linearity (derived from S1 + finite-dim), A-S compression axioms from alfsen-shultz-notes.md. S4-S7 FORBIDDEN pre-S4. Jordan product FORBIDDEN."
  forbidden_tokens:
    - "M_n(ℂ)"
    - "M_n(C)"
    - "Jordan"
    - "EJA"
    - "Lüders"
    - "Luders"
    - "pxp"
    - "√a b √a"
    - "sqrt(a) b sqrt(a)"
    - "operator product"
    - "f(λ,μ)=√(λμ)"
    - "f(lambda,mu)=sqrt(lambda*mu)"
    - "h_n(ℂ)"
    - "h_n(C)"
    - "spin factor"
  grep_targets:
    - "derivations/paper5-peirce-preservation/attempt-01.md"
    - "derivations/paper5-peirce-preservation/attempt-02.md"
    - "derivations/paper5-peirce-preservation/attempt-03.md"
    - "derivations/paper5-peirce-preservation/attempt-log.md"

attempt_cap: 3
stop_rethink_triggers:
  - "end_of_attempt_02_regardless_of_outcome (CONTEXT.md stop/rethink #1): pause for human review before authorizing attempt-03 or pivot to (C-i). Budget discipline — strategy must change at this boundary if attempt-01 and attempt-02 both failed; another draft is not the right next move."
  - "c_iii_drift_detected (CONTEXT.md stop/rethink #3): any attempt that starts constructing Jordan structure before S4 must escalate with explicit drift-line VERBATIM QUOTE (not just 'Jordan detected')."
  - "compression_algebra_circularity_at_audit (Plan 54-01 Task 2 option-c-fails-compression-algebra): if Plan 54-01 selected this routing, Plan 54-02 skips (A) entirely and routes directly to Plan 54-03 (C-i)."

contract:
  schema_version: 1
  scope:
    question: "Can the Peirce-Preservation Lemma (as stated in claim.md) be proved from {S1, S3, linearity, A-S compression axioms} alone (outcome (A)), under the routing selected in Plan 54-01's Task 2 checkpoint? If not, what is the verbatim-quoted adversarial objection or drift that blocks closure and forces pivot to Plan 54-03 (C-i)?"
  claims:
    - id: claim-attempt-seed-and-route
      statement: "The first (A) attempt uses the seed strategy selected by Plan 54-01's Task 2 checkpoint: either Approach 1 (seed from v2.0 Phase 4-06 Eq. 04-06.4) if AUDIT-PASSES, or Approach 2 (compression combinatorics, no 4-06 seed) if AUDIT-FAILS in a non-compression-algebra step. If AUDIT-FAILS in compression algebra, Plan 54-02 is skipped entirely and Plan 54-03 is entered directly."
      deliverables: [deliv-attempt-01]
      acceptance_tests: [test-attempt-01-seed-match, test-attempt-01-sympy, test-attempt-01-early-gates]
      references: [ref-plan-54-01, ref-claim-md, ref-as-notes, ref-research-54, ref-ctx-54]
    - id: claim-attempt-proof-closure-or-failure
      statement: "For each (A) attempt, one of the following holds: (CLOSURE) the attempt proves parts (i), (ii), (iii) of the Peirce-Preservation Lemma from the allowed-tool set and passes the three early falsifier gates (forbidden-token self-grep returns zero hits; per-attempt SymPy rank-1 gate on H_3(ℝ) passes; model-instantiation check on M_n(ℂ)^sa and C(X) does not produce a false statement); OR (FAILURE) the attempt is marked FAILED with (a) the specific early-gate failure, or (b) the verbatim-quoted adversarial objection from `gpd-review-math` (forwarded to attempt-(NN+1).md header per CONTEXT.md objection-routing protocol)."
      deliverables: [deliv-attempt-01, deliv-attempt-02, deliv-attempt-03, deliv-attempt-log]
      acceptance_tests: [test-per-attempt-falsifier-gates, test-per-attempt-adversarial-review, test-attempt-closure-or-failure-recorded]
      references: [ref-claim-md, ref-research-54, ref-ctx-54, ref-peirce-post-jordan-memory]
    - id: claim-plan-outcome-routing
      statement: "At plan close, exactly one of the following is true: (A-CLOSED) an attempt-NN passed all three early falsifier gates AND the adversarial review returned PASS (not borderline, not FAIL); Plan 54-02 emits outcome tag '(A) via attempt-NN' forwarded to Plan 54-03 closeout; OR (PIVOT-TO-C-I) all attempts within the attempt cap failed; Plan 54-02 emits outcome tag 'PIVOT-TO-C-I' with a log of the failure modes and CARRY-FORWARD objection quotes for use by Plan 54-03's (C-i) defense."
      deliverables: [deliv-attempt-log]
      acceptance_tests: [test-plan-outcome-tag-binary, test-carryforward-objections]
      references: [ref-ctx-54, ref-research-54]
  deliverables:
    - id: deliv-attempt-01
      kind: derivation
      path: "derivations/paper5-peirce-preservation/attempt-01.md"
      description: "First (A) proof attempt of the Peirce-Preservation Lemma using the seed selected in Plan 54-01 Task 2. Prose derivation + embedded claim reference + drift log. Sibling file attempt-01.py contains the per-attempt SymPy rank-1 gate on H_3(ℝ)."
      must_contain:
        - "seeded-from: [Approach 1: Eq. 04-06.4] OR [Approach 2: compression combinatorics, no 4-06 seed]"
        - "avoiding-because: [per Plan 54-01 Task 2 decision]"
        - "assumption set: {S1, S3, linearity, A-S compression axioms}"
        - "lemma statement copied verbatim from claim.md"
        - "three separate sub-proofs for (i) V_2(p_i), (ii) V_1(p_i, p_j), (iii) V_1(p_k, p_l) cross-term with {k,l} ∩ supp(a) = ∅"
        - "every citation to A-S compression axioms resolved via alfsen-shultz-notes.md (Phase 54-01 Task 4)"
        - "drift log — running record of any near-miss or temptation toward forbidden tokens, with how it was avoided"
        - "status: CLOSURE (all gates passed, adversarial review PASSED) or FAILED (early-gate failure OR adversarial objection verbatim)"
    - id: deliv-attempt-01-sympy
      kind: code
      path: "derivations/paper5-peirce-preservation/attempt-01.py"
      description: "Per-attempt SymPy rank-1 sanity gate on H_3(ℝ): two orthogonal rank-1 projectors p_1, p_2; a = λ_1 p_1 + λ_2 p_2; test a ∘ V_2(p_1), a ∘ V_1(p_1, p_2) invariance. Symbolic-exact via sympy.Rational or sympy.Symbol. Runtime < 1 sec."
      must_contain:
        - "H_3(ℝ) self-adjoint real matrices"
        - "two orthogonal rank-1 projectors p_1, p_2"
        - "scalar weights λ_1, λ_2 (symbolic: sympy.Symbol or sympy.Rational)"
        - "sequential product a ∘ b computed via attempt-01's derived formula or structural argument"
        - "assertion: C_{p_1}(a ∘ b) == a ∘ b for b ∈ V_2(p_1) (i.e., b = C_{p_1}(b))"
        - "assertion for V_1(p_1, p_2) invariance on a sample basis element"
        - "exit code: 0 PASS / non-zero FAIL; PASS required before attempt-01.md may be sealed"
    - id: deliv-attempt-02
      kind: derivation
      path: "derivations/paper5-peirce-preservation/attempt-02.md"
      description: "Second (A) proof attempt. ONLY executed if attempt-01 FAILED. Opens with 'addresses prior objection: [verbatim quote from attempt-01 adversarial review]' header. Strategy for attempt-02 must differ from attempt-01 at the structural level — not merely reword. If attempt-01 used Approach 1 (4-06 seed), attempt-02 uses Approach 2 (compression combinatorics) and vice versa."
      must_contain:
        - "header: addresses prior objection: [verbatim quote from attempt-01 adversarial review]"
        - "structural difference from attempt-01 explicitly stated (not just rewording)"
        - "status: CLOSURE or FAILED (verbatim objection carried forward)"
        - "(conditional) appears only if attempt-01 FAILED"
    - id: deliv-attempt-02-sympy
      kind: code
      path: "derivations/paper5-peirce-preservation/attempt-02.py"
      description: "Per-attempt SymPy rank-1 gate for attempt-02; same structure as attempt-01.py. Conditional on attempt-02 being executed."
      must_contain:
        - "same structure as attempt-01.py"
        - "(conditional) appears only if attempt-02 is executed"
    - id: deliv-attempt-03
      kind: derivation
      path: "derivations/paper5-peirce-preservation/attempt-03.md"
      description: "Third (A) proof attempt. ONLY executed if attempt-02 FAILED AND human has explicitly authorized attempt-03 after the stop/rethink-triggered pause at end of attempt-02 (CONTEXT.md #1). Strategy must differ from both attempt-01 and attempt-02."
      must_contain:
        - "header: addresses prior objections: [verbatim quotes from attempt-01 and attempt-02]"
        - "strategy change justified at a structural level (not another draft)"
        - "status: CLOSURE or FAILED"
        - "(conditional) appears only if both attempt-01 and attempt-02 FAILED and human authorized attempt-03"
    - id: deliv-attempt-03-sympy
      kind: code
      path: "derivations/paper5-peirce-preservation/attempt-03.py"
      description: "Per-attempt SymPy rank-1 gate for attempt-03. Conditional on attempt-03 being executed."
      must_contain:
        - "same structure as attempt-01.py"
        - "(conditional) appears only if attempt-03 is executed"
    - id: deliv-attempt-log
      kind: note
      path: "derivations/paper5-peirce-preservation/attempt-log.md"
      description: "Running log of all attempts with: attempt-NN / seed strategy / status (CLOSURE / FAILED) / failure mode (early-gate failure type OR verbatim adversarial objection). Final entry emits the plan's outcome tag: '(A) via attempt-NN' or 'PIVOT-TO-C-I'."
      must_contain:
        - "one row per attempt-NN actually executed"
        - "columns: attempt number, seed strategy (Approach 1 or 2), early-gate results (self-grep / SymPy / model-instantiation), adversarial review verdict (PASS / BORDERLINE / FAIL), status (CLOSURE / FAILED)"
        - "verbatim quote of every FAIL objection (carried forward to the next attempt AND to Plan 54-03 if pivot)"
        - "final outcome tag: either '(A) via attempt-NN' (with attempt-NN identified) OR 'PIVOT-TO-C-I' (with failure-mode summary)"
  references:
    - id: ref-plan-54-01
      kind: prior_artifact
      locator: ".gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-01-PLAN.md (deliverables: audit-04-06.md, claim.md, alfsen-shultz-notes.md)"
      aliases: ["Plan 54-01", "audit-04-06"]
      role: definition
      why_it_matters: "Plan 54-01 Task 2 checkpoint determines Plan 54-02 seed strategy (Approach 1 vs Approach 2); Plan 54-01 Task 3 claim.md is the lemma target; Plan 54-01 Task 4 alfsen-shultz-notes.md supplies verified A-S prop/thm numbers"
      applies_to: [claim-attempt-seed-and-route, claim-attempt-proof-closure-or-failure]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [read, use]
    - id: ref-claim-md
      kind: prior_artifact
      locator: "derivations/paper5-peirce-preservation/claim.md (produced by Plan 54-01 Task 3)"
      aliases: ["claim.md", "Peirce-Preservation Lemma statement"]
      role: definition
      why_it_matters: "Stable lemma target that every attempt-NN.md proves or axiomatizes; the three sub-claims (V_2, V_1, V_1 cross-term) are separately tested"
      applies_to: [claim-attempt-proof-closure-or-failure]
      carry_forward_to: [planning, execution, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-as-notes
      kind: prior_artifact
      locator: "derivations/paper5-peirce-preservation/alfsen-shultz-notes.md (produced by Plan 54-01 Task 4)"
      aliases: ["alfsen-shultz-notes.md"]
      role: method
      why_it_matters: "Every A-S compression-axiom citation in attempt-NN.md must resolve via this shared artifact (volume / chapter / prop-or-thm / exact-book-statement)"
      applies_to: [claim-attempt-proof-closure-or-failure]
      carry_forward_to: [execution]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-ctx-54
      kind: prior_artifact
      locator: ".gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-CONTEXT.md"
      aliases: ["54-CONTEXT"]
      role: definition
      why_it_matters: "Locked decisions: attempt-cap = 3 (outer bound), stop/rethink triggers (#1 end-of-attempt-02 pause; #3 C-iii drift escalation), objection-routing protocol (verbatim-quote carry-forward), adversarial review policy (gpd-review-math primary, Opus escalation only on borderline)"
      applies_to: [claim-attempt-proof-closure-or-failure, claim-plan-outcome-routing]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [read, use]
    - id: ref-research-54
      kind: prior_artifact
      locator: ".gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESEARCH.md"
      aliases: ["54-RESEARCH"]
      role: method
      why_it_matters: "Defines Approach 1 (4-06 seed) and Approach 2 (compression combinatorics) in detail; specifies per-attempt SymPy gate design on H_3(ℝ); specifies adversarial review priming protocol; lists common pitfalls R1-R5, R6 (C-iii drift)"
      applies_to: [claim-attempt-seed-and-route, claim-attempt-proof-closure-or-failure]
      carry_forward_to: [execution]
      must_surface: true
      required_actions: [read, use]
    - id: ref-peirce-post-jordan-memory
      kind: prior_artifact
      locator: "peirce-post-jordan-finding memory (from user's global memory store; load into adversarial reviewer priming)"
      aliases: ["peirce-post-jordan-finding"]
      role: must_consider
      why_it_matters: "Prior incident where Paper 5's §3.3 already fell into the Jordan-smuggling trap; adversarial reviewer must be primed with this memory so it recognizes the subtle R1 pattern"
      applies_to: [claim-attempt-proof-closure-or-failure]
      carry_forward_to: [execution]
      must_surface: true
      required_actions: [read, use]
    - id: ref-paper5-submitted
      kind: prior_artifact
      locator: "~/repos/research/paper5/main-jmp-submitted.tex §3.3 lines 483-562 (frozen at git tag paper5-jmp-submitted)"
      aliases: ["Paper 5 submitted"]
      role: definition
      why_it_matters: "Baseline non-sequitur being replaced; R2 example at line ~525; R4 line-count floor = 20 lines"
      applies_to: [claim-attempt-proof-closure-or-failure]
      carry_forward_to: [execution, writing]
      must_surface: true
      required_actions: [read, compare]
  acceptance_tests:
    - id: test-attempt-01-seed-match
      subject: claim-attempt-seed-and-route
      kind: existence
      procedure: "Read attempt-01.md header; verify the 'seeded-from' declaration matches the Plan 54-01 Task 2 checkpoint decision (Approach 1 if option-a-passes; Approach 2 if option-b-fails-compression; plan skipped entirely if option-c-fails-compression-algebra and Plan 54-03 entered directly)."
      pass_condition: "attempt-01.md seed strategy matches Task 2 decision. If option-c-fails-compression-algebra was selected in Plan 54-01, Plan 54-02 emits 'SKIPPED (direct pivot to C-i per option-c)' in attempt-log.md and no attempt-NN files are created."
      evidence_required: [deliv-attempt-01, deliv-attempt-log, ref-plan-54-01]
      automation: human
    - id: test-attempt-01-sympy
      subject: claim-attempt-seed-and-route
      kind: benchmark
      procedure: "Run `python3 derivations/paper5-peirce-preservation/attempt-01.py`; verify exit code 0 (PASS) and output affirms both the V_2(p_1) invariance test and V_1(p_1, p_2) invariance test on H_3(ℝ) with two orthogonal rank-1 projectors."
      pass_condition: "Exit code 0; test output includes 'V_2 invariance PASS' and 'V_1 invariance PASS'; runtime < 1 sec."
      evidence_required: [deliv-attempt-01-sympy]
      automation: automated
    - id: test-attempt-01-early-gates
      subject: claim-attempt-proof-closure-or-failure
      kind: consistency
      procedure: "Three early falsifier gates on attempt-01.md (CONTEXT.md Decisions §Early falsifier gates): (a) forbidden-token self-grep with the plan's forbidden_tokens list — zero hits required (or only within explicit forbidden-tool declaration sections); (b) SymPy per-attempt rank-1 gate (test-attempt-01-sympy); (c) model-instantiation check: instantiate attempt-01's argument in M_n(ℂ)^sa and C(X); must not produce a false statement."
      pass_condition: "All three early gates PASS before adversarial review is invoked. Any gate failure → attempt-01.md marked FAILED; no review; open attempt-02."
      evidence_required: [deliv-attempt-01, deliv-attempt-01-sympy]
      automation: hybrid
    - id: test-per-attempt-falsifier-gates
      subject: claim-attempt-proof-closure-or-failure
      kind: consistency
      procedure: "For every attempt-NN that reaches adversarial review, verify all three early falsifier gates PASSED. For every attempt-NN that did NOT reach adversarial review, verify attempt-log.md records which gate failed and what the failure was."
      pass_condition: "Per-attempt gate results recorded in attempt-log.md for every attempt; no attempt advanced to adversarial review without all three gates PASS."
      evidence_required: [deliv-attempt-01, deliv-attempt-02, deliv-attempt-03, deliv-attempt-log]
      automation: hybrid
    - id: test-per-attempt-adversarial-review
      subject: claim-attempt-proof-closure-or-failure
      kind: human_review
      procedure: "For every attempt-NN that PASSED all three early gates, spawn `gpd-review-math` with Phase 54 priming (load: 54-CONTEXT.md, 54-RESEARCH.md, alfsen-shultz-notes.md, claim.md, attempt-NN.md, peirce-post-jordan-finding memory, forbidden-token list, R1-R5 pitfalls, 'CIRCULAR vs IDENTITY' framing discipline per sms:minimal). Verdict: PASS / BORDERLINE / FAIL. If BORDERLINE, escalate to Paper-5-primed general-purpose Opus subagent per CONTEXT.md."
      pass_condition: "Every attempt-NN that reached adversarial review has a verdict recorded in attempt-log.md; BORDERLINE triggers escalation; objection quotes are verbatim (no paraphrase) when FAIL."
      evidence_required: [deliv-attempt-01, deliv-attempt-02, deliv-attempt-03, deliv-attempt-log, ref-peirce-post-jordan-memory]
      automation: human
    - id: test-attempt-closure-or-failure-recorded
      subject: claim-attempt-proof-closure-or-failure
      kind: schema
      procedure: "For every attempt-NN.md file that exists, verify its `status:` line is exactly one of {CLOSURE, FAILED}. CLOSURE requires all three early gates PASS + adversarial review PASS. FAILED requires one of: (a) specific early-gate failure recorded, or (b) verbatim adversarial objection quoted."
      pass_condition: "Every attempt-NN.md has a clear binary status; no 'mostly worked' or 'needs more work' language."
      evidence_required: [deliv-attempt-01, deliv-attempt-02, deliv-attempt-03]
      automation: automated
    - id: test-plan-outcome-tag-binary
      subject: claim-plan-outcome-routing
      kind: existence
      procedure: "Grep attempt-log.md for the final outcome tag; verify exactly one of '(A) via attempt-NN' (with the attempt identified) OR 'PIVOT-TO-C-I' appears."
      pass_condition: "Exactly one outcome tag line matching one of the two forms; no ambiguous or composite tags."
      evidence_required: [deliv-attempt-log]
      automation: automated
    - id: test-carryforward-objections
      subject: claim-plan-outcome-routing
      kind: existence
      procedure: "For each FAILED attempt, verify that the verbatim adversarial objection (or early-gate failure description) is recorded in attempt-log.md AND forwarded to the next attempt's header (attempt-02 'addresses prior objection: [quote]') OR to Plan 54-03's S0-defense input if PIVOT-TO-C-I."
      pass_condition: "Every FAIL objection is preserved verbatim and traceable from attempt-log.md to the next step (next attempt or Plan 54-03)."
      evidence_required: [deliv-attempt-log, deliv-attempt-02, deliv-attempt-03]
      automation: hybrid
  forbidden_proxies:
    - id: fp-attempt-jordan-smuggle
      subject: claim-attempt-proof-closure-or-failure
      proxy: "Any attempt-NN.md that uses a forbidden token as a proof device (e.g., 'Since a ∘ b is a Jordan product …' or 'By the pxp matrix-block computation, …') or implicitly invokes S4-S7 pre-S4."
      reason: "This is R1 (Circularity via Jordan) — the central pitfall. The forbidden-token self-grep gate catches explicit tokens; the adversarial reviewer catches subtle cases where the token is absent but the structure is imported (e.g., 'generates a two-level face isomorphic to a spin factor' — spin-factor proof device)."
    - id: fp-attempt-decomposition-non-sequitur
      subject: claim-attempt-proof-closure-or-failure
      proxy: "An attempt that cites 'A-S Peirce decomposition' as justification for INVARIANCE of L_a, e.g., 'Compressions project onto these subspaces (A-S 2003), so linearity gives a block decomposition: L_a maps each Peirce subspace to itself.'"
      reason: "This is R2 — exactly the submitted Paper 5 §3.3 non-sequitur on lines ~524-528. Decomposition is a cited A-S fact; invariance is the claim to prove. Conflating them is precisely what Phase 54 exists to prevent."
    - id: fp-attempt-single-compression-conflation
      subject: claim-attempt-proof-closure-or-failure
      proxy: "An attempt that proves 'compressions C_{p_i} preserve their own Peirce 2-spaces' (A-S fact about the LIST) and claims this gives 'L_a = Σ λᵢ C_{p_i} + mixing term preserves Peirce subspaces' (claim about the SUM) without separate case analysis for the mixing term and cross-term V_1(p_k, p_l) with {k,l} ∩ supp(a) = ∅."
      reason: "This is R3 — single-compression fact does not imply composite-map invariance. Every (A) proof must do explicit case analysis on (i) V_2(p_i), (ii) V_1(p_i, p_j), (iii) V_1(p_k, p_l) cross-term. Absence of the cross-term case in attempt-NN.md triggers FAIL at the adversarial review step even if the per-attempt SymPy gate passed (SymPy closeout artifact in Plan 54-03 catches the cross-term numerically)."
    - id: fp-attempt-padding-for-r4
      subject: claim-attempt-proof-closure-or-failure
      proxy: "An attempt whose revision draft hits the R4 ≥ 20-line floor by rhetorical padding ('by the structure of the decomposition, it follows that …', 'obviously', 'clearly', 'immediately follows without further argument')."
      reason: "R4 is a SUBSTANTIVE-content floor, not a stylistic target. Forbidden phrases: 'obvious', 'clearly', 'immediately follows' (acceptable only following a specific cited theorem with page reference). Adversarial reviewer is primed to catch padding (R4 pitfall)."
    - id: fp-attempt-c-iii-silent-drift
      subject: claim-attempt-proof-closure-or-failure
      proxy: "An attempt that defines `a · b := (a ∘ b + b ∘ a)/2` or uses `{a, b, c}` (Jordan triple product) or invokes Macdonald's theorem, thereby constructing Jordan structure before S4 has been verified."
      reason: "C-iii (restructure-before-S4) is UNAVAILABLE per CONTEXT.md / ADDENDUM / SUMMARY.md. Any such drift triggers stop/rethink #3: escalate IMMEDIATELY with the explicit drift-line VERBATIM QUOTE (not a summary). Do not attempt to salvage."
    - id: fp-attempt-framing-error-review
      subject: claim-attempt-proof-closure-or-failure
      proxy: "Accepting a 'CIRCULAR' verdict from gpd-review-math that is actually a framing error — calling a definitional clause 'circular' when it's a self-referential definition, not a proof-step inversion."
      reason: "sms:minimal lesson: 'CIRCULAR' and 'IDENTITY' verdicts are distinct. The reviewer must state WHICH framing it is applying. If the reviewer returns CIRCULAR without a specific line-number quote of the circularity, escalate to the Opus Paper-5-primed rubric pass."
    - id: fp-attempt-same-strategy-twice
      subject: claim-attempt-proof-closure-or-failure
      proxy: "attempt-02 that uses the same structural strategy as attempt-01 with only surface-level rewording (e.g., both are Approach 1 with different phrasing, or both are Approach 2 with different intermediate steps). Budget discipline per CONTEXT.md: 'Just be reasonable … we probably won't let it get that far though.'"
      reason: "Attempts exist to explore DIFFERENT structural strategies. If attempt-01 (Approach 1, 4-06 seed) fails, attempt-02 should be Approach 2 (compression combinatorics), or vice versa — not the same approach reworded. If the two viable approaches both fail, pivot to (C-i) rather than draft attempt-03 with a marginal variation."
  links:
    - id: link-plan-54-01-to-seed
      source: ref-plan-54-01
      target: claim-attempt-seed-and-route
      relation: depends_on
      verified_by: [test-attempt-01-seed-match]
    - id: link-claim-to-attempts
      source: ref-claim-md
      target: claim-attempt-proof-closure-or-failure
      relation: supports
      verified_by: [test-attempt-closure-or-failure-recorded]
    - id: link-as-notes-to-citations
      source: ref-as-notes
      target: deliv-attempt-01
      relation: supports
      verified_by: [test-per-attempt-falsifier-gates]
    - id: link-attempts-to-plan-outcome
      source: deliv-attempt-log
      target: claim-plan-outcome-routing
      relation: supports
      verified_by: [test-plan-outcome-tag-binary, test-carryforward-objections]
  uncertainty_markers:
    weakest_anchors:
      - "The meta-claim that 'gpd-review-math with Phase 54 priming is substantively better than cold-read at catching subtle Jordan-smuggling' is unvalidated per RESEARCH.md Open Question 3. The two-layer design (primary + borderline escalation to Opus) is a mitigation, but a blind spot in both layers would produce a false CLOSURE on an attempt that subtly smuggles Jordan. The per-attempt SymPy gate catches WRONG lemmas but not RIGHT-LEMMA-WRONG-PROOF; the closeout SymPy in Plan 54-03 catches R3 cross-term issues."
      - "The assumption that Approach 1 (4-06 seed) and Approach 2 (compression combinatorics) are substantively different routes is unvalidated per RESEARCH.md Open Question 2. If 4-06's Eq. 04-06.4 is itself derivable from A-S compression axioms alone, the two routes collapse to one — reducing attempt diversity and potentially making attempt-02 redundant with attempt-01 structurally."
    disconfirming_observations:
      - "If attempt-01 (Approach 1) CLOSES cleanly and the SymPy gate passes, but the ADVERSARIAL review returns FAIL with a subtle R1 objection that is not a forbidden-token hit — the attempt has smuggled Jordan through a structural pattern the token list did not cover. Carry forward the objection verbatim to attempt-02 with explicit protection against the mechanism."
      - "If BOTH attempt-01 (Approach 1) AND attempt-02 (Approach 2) FAIL with RELATED objections (e.g., 'the mixing term P_{ij} cannot be constructed from compressions alone without axiomatizing it'), this is evidence that (C-i) is the correct outcome — the mixing-term contribution genuinely requires an OUS-level input beyond S1-S7 + compressions. Pivot to Plan 54-03 (C-i) is well-motivated; do NOT attempt attempt-03 with another rewording."

estimated_execution:
  total_minutes: 420
  breakdown:
    - task: 1
      minutes: 120
      note: "attempt-01.md + attempt-01.py: full (A) proof draft from seed strategy selected in Plan 54-01 Task 2; three early falsifier gates; adversarial review"
    - task: 2
      minutes: 30
      note: "Task 2 checkpoint: review attempt-01 outcome (CLOSURE or FAILED); if FAILED, carry forward objection; if end-of-attempt-01 is also end-of-budget (e.g., option-c was selected), emit PIVOT-TO-C-I and skip remaining tasks"
    - task: 3
      minutes: 120
      note: "(conditional on attempt-01 FAILED) attempt-02.md + attempt-02.py: different structural approach; full early gates + adversarial review"
    - task: 4
      minutes: 30
      note: "(mandatory) end-of-attempt-02 stop/rethink pause per CONTEXT.md #1 — human reviews before authorizing attempt-03 or authorizing PIVOT-TO-C-I"
    - task: 5
      minutes: 90
      note: "(conditional on attempt-02 FAILED AND human authorizes attempt-03) attempt-03.md + attempt-03.py: structural change from both prior attempts; full early gates + adversarial review"
    - task: 6
      minutes: 30
      note: "Plan outcome: write final attempt-log.md entry with '(A) via attempt-NN' or 'PIVOT-TO-C-I' tag; carry forward objections to Plan 54-03 if pivot"

patterns_consulted:
  insights: []
  error_patterns: []
  adjustments_made:
    - "Per-attempt SymPy rank-1 gate on H_3(ℝ) is embedded as a per-attempt acceptance test (test-attempt-01-sympy), not a plan-level closeout — closeout with cross-term coverage is in Plan 54-03 per RESEARCH.md"
    - "Adversarial review routing (gpd-review-math primary, Opus escalation on borderline) mirrored from CONTEXT.md Decisions §Adversarial fresh-eyes review"
    - "Objection-routing protocol (verbatim-quote carry-forward to next attempt's header) enforced at test-carryforward-objections"
---

<objective>
Attempt to prove the Peirce-Preservation Lemma (from `claim.md`) as outcome (A), using only S1, S3, linearity, and A-S compression axioms. Up to three sequential attempts permitted, each with per-attempt SymPy rank-1 gate, forbidden-token self-grep, model-instantiation check, and adversarial `gpd-review-math` fresh-eyes review. If all attempts fail within the cap, emit PIVOT-TO-C-I tag with CARRY-FORWARD objection quotes to Plan 54-03.

Purpose: Outcome (A) closes §3.3 with a rigorous proof rather than a new axiom. If infeasible, the accumulated failure record of each (A) attempt justifies the (C-i) fallback in Plan 54-03 — the S0 defense is stronger when the reader sees that compression-level (A) was tried and failed with specific structural objections.
Output: attempt-NN.md + attempt-NN.py files for each attempt actually executed; attempt-log.md with outcome tag '(A) via attempt-NN' or 'PIVOT-TO-C-I'.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-CONTEXT.md
@.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESEARCH.md
@.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-01-PLAN.md
@derivations/paper5-peirce-preservation/audit-04-06.md
@derivations/paper5-peirce-preservation/claim.md
@derivations/paper5-peirce-preservation/alfsen-shultz-notes.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: attempt-01.md + attempt-01.py (seed strategy from Plan 54-01 Task 2 checkpoint)</name>
  <files>derivations/paper5-peirce-preservation/attempt-01.md, derivations/paper5-peirce-preservation/attempt-01.py</files>
  <action>
    Read Plan 54-01's Task 2 checkpoint decision recorded in the 54-01-SUMMARY.md or audit-04-06.md routing-consequence section:

    - If `option-a-passes` (AUDIT-PASSES): attempt-01 uses **Approach 1** (seed from v2.0 Phase 4-06 Eq. 04-06.4). Seeded-from: "Phase 4-06 Eq. 04-06.4 (audit verdict AUDIT-PASSES per 54-01)". Avoiding-because: N/A.
    - If `option-b-fails-compression`: attempt-01 uses **Approach 2** (compression combinatorics, no 4-06 seed). Seeded-from: "A-S 2001 Ch. 7-8 compression algebra (per alfsen-shultz-notes.md)". Avoiding-because: "Phase 4-06 Eq. 04-06.4 audit verdict AUDIT-FAILS; 4-06 seed is unavailable per Plan 54-01."
    - If `option-c-fails-compression-algebra`: DO NOT PROCEED with attempt-01. Emit in attempt-log.md: "Plan 54-02 SKIPPED — compression-algebra circularity at audit forces direct pivot to (C-i) per Plan 54-01 Task 2 option-c. No (A) attempt attempted." Then skip to final-outcome-tag task with 'PIVOT-TO-C-I'.

    **attempt-01.md structure (if Approach 1 or Approach 2 proceeds):**

    1. **Header:**
       - `seeded-from: [one-line strategy pointer]`
       - `avoiding-because: [one-line justification, or N/A]`
       - `assumption set: {S1, S3, linearity, A-S compression axioms}`
       - `addresses prior objection: N/A (first attempt)`

    2. **Conventions block** (copy from plan frontmatter).

    3. **Lemma statement** (copy verbatim from claim.md, using the (A) assumption set clause).

    4. **Proof:** Three separate sub-proofs, one for each target inclusion:
       - **(i) V_2(p_i) invariance:** `a ∘ V_2(p_i) ⊆ V_2(p_i)`. Use S3 sharp constraint `p_i ∘ b = C_{p_i}(b)`, spectral decomposition `a = Σ_j λ_j p_j`, S1 additivity, and A-S orthogonal-projective-unit compressional annihilation (`C_{p_i} C_{p_j} = 0` for i ≠ j, cited from alfsen-shultz-notes.md).
       - **(ii) V_1(p_i, p_j) invariance:** `a ∘ V_1(p_i, p_j) ⊆ V_1(p_i, p_j)` for indices (i, j) with i, j ∈ supp(a). Requires engagement with the mixing term (in Approach 1, from Eq. 04-06.4's second sum Σ_{i<j} √(λ_i λ_j) P_{ij}; in Approach 2, from compression-only reconstruction of the L_a map restricted to V_1 spaces).
       - **(iii) V_1(p_k, p_l) cross-term invariance:** `a ∘ V_1(p_k, p_l) ⊆ V_1(p_k, p_l)` for {k, l} ∩ supp(a) = ∅. MANDATORY COVERAGE per R3. In Approach 1, show that each C_{p_i} in the first sum annihilates V_1(p_k, p_l) (orthogonal-projective-unit compressional annihilation applied to Peirce 1-spaces), and that each P_{ij} in the second sum either annihilates or preserves V_1(p_k, p_l). In Approach 2, build the same conclusion from compression combinatorics without the 4-06 seed.

    5. **Every citation to an A-S compression axiom references alfsen-shultz-notes.md** (e.g., `C_{p_i} C_{p_j} = 0` orthogonal annihilation → alfsen-shultz-notes.md §Section-6; idempotency `C_p² = C_p` → §Section-5 row `idempotency`).

    6. **Drift log:** Running record during drafting. Any temptation toward forbidden tokens + how avoided. Example: "Considered invoking Jordan identities to simplify the mixing term, REJECTED because Jordan structure is forbidden pre-S4; instead, used S1 additivity + orthogonal annihilation."

    7. **Status line:**
       - If all three early gates PASS and adversarial review PASS: `status: CLOSURE`.
       - Otherwise: `status: FAILED — [gate or objection]`.

    **attempt-01.py structure (per-attempt SymPy rank-1 gate):**

    ```python
    import sympy as sp

    # H_3(ℝ): symmetric real 3x3 matrices
    # Two orthogonal rank-1 projectors p_1, p_2 (choose canonical diagonal
    # or parametric; executor's discretion per CTX Decisions §SymPy projector family)
    p1 = sp.Matrix([[1, 0, 0], [0, 0, 0], [0, 0, 0]])
    p2 = sp.Matrix([[0, 0, 0], [0, 1, 0], [0, 0, 0]])

    # Symbolic scalar weights
    lam1, lam2 = sp.symbols('lambda_1 lambda_2', real=True)
    a = lam1 * p1 + lam2 * p2

    # Define the sequential product a ∘ b per the attempt-01 structural argument.
    # (Executor: use the specific formulation from attempt-01.md — for Approach 1,
    # apply Eq. 04-06.4; for Approach 2, apply the compression-only decomposition.)

    def seq_prod(a, b):
        """Sequential product a ∘ b as structured in attempt-01 (Approach 1 or 2)."""
        ...  # executor fills in based on attempt-01 derivation
        return result

    # Test (i) V_2(p_1) invariance: for b in V_2(p_1) (i.e., b = C_{p_1}(b)),
    # verify a ∘ b is in V_2(p_1) (i.e., C_{p_1}(a ∘ b) == a ∘ b).
    b_V2 = sp.Matrix([[sp.Symbol('x', real=True), 0, 0], [0, 0, 0], [0, 0, 0]])
    # compression C_{p_1} on H_3(ℝ): for rank-1 diag projector, C_{p_1}(b) = p_1 b p_1 is NOT permitted
    # (pxp is forbidden!). Instead: use the A-S compression characterization cited in alfsen-shultz-notes.md.
    # For this symbolic rank-1 diagonal case, the compression acts as picking the (1,1) block — implement
    # via the projection onto the span of p_1 in H_3(ℝ), realized via sympy block extraction.
    # (Executor: document the A-S compression realization used in attempt-01.py.)

    result_V2 = seq_prod(a, b_V2)
    assert compress(result_V2, p1) == result_V2, "V_2(p_1) invariance FAILED"
    print("V_2 invariance PASS")

    # Test (ii) V_1(p_1, p_2) invariance: analogous.
    b_V1 = ...  # basis element in V_1(p_1, p_2); off-diagonal (1,2)/(2,1) block in H_3(ℝ) rank-1 projector setting
    result_V1 = seq_prod(a, b_V1)
    assert b_V1 in V_1(p1, p2) and result_V1 in V_1(p1, p2), "V_1 invariance FAILED"
    print("V_1 invariance PASS")

    # Exit code 0 = PASS, non-zero = FAIL
    ```

    NOTE on the pxp constraint: `pxp` is a forbidden token. The SymPy test CANNOT implement the compression via `p @ b @ p` because that imports matrix-level structure pxp is forbidden for the abstract lemma. Instead, use the A-S compression CHARACTERIZATION cited in alfsen-shultz-notes.md (e.g., `C_p(b)` is realized as the projection onto V_2(p), which for a diagonal rank-1 projector p on H_3(ℝ) happens to coincide with the (block extraction) — BUT the executor must document this as an INSTANTIATION of the abstract compression in the concrete H_3(ℝ) model, NOT as a proof-device definition. The model-instantiation gate (three early gates, gate (c)) verifies that attempt-01's argument works in both M_n(ℂ)^sa and C(X) — if the argument implicitly relies on pxp, it fails in C(X) where pxp would just be pointwise multiplication and the "block" interpretation doesn't apply structurally.

    **Three early falsifier gates (all three must PASS before adversarial review is invoked):**

    **Gate (a) — Forbidden-token self-grep:**
    ```bash
    rg -n -w "(Jordan|EJA|Lüders|Luders|pxp|h_n|operator product|spin factor)" derivations/paper5-peirce-preservation/attempt-01.md
    rg -n "M_n\\(.\\)" derivations/paper5-peirce-preservation/attempt-01.md
    rg -n "√a\\s*b\\s*√a|sqrt\\(a\\)\\s*b\\s*sqrt\\(a\\)" derivations/paper5-peirce-preservation/attempt-01.md
    rg -n "f\\(λ,μ\\)=√\\(λμ\\)|f\\(lambda,mu\\)=sqrt\\(lambda\\*mu\\)" derivations/paper5-peirce-preservation/attempt-01.md
    ```
    Any hit outside the explicit Forbidden-Tools declaration section → attempt-01 FAIL; close and route per Task 2.

    **Gate (b) — SymPy per-attempt rank-1 gate:**
    `python3 derivations/paper5-peirce-preservation/attempt-01.py`. Exit code 0 required.

    **Gate (c) — Model-instantiation check:**
    Reason through attempt-01's argument in M_n(ℂ)^sa and C(X). The argument must NOT produce a false statement in either model. Concretely: check that attempt-01's derivation, when instantiated in C(X) (commutative pointwise algebra), yields a statement consistent with the trivial Peirce invariance in C(X); check that, when instantiated in M_n(ℂ)^sa, it yields a statement consistent with the known Jordan-theoretic result (even though Jordan structure is not used in the proof). Record instantiation checks in attempt-01.md §Model-Instantiation.

    **Adversarial review (VALD-54-02 primary):**

    If all three gates PASS, spawn `gpd-review-math` with Phase 54 priming:
    - Load: 54-CONTEXT.md, 54-RESEARCH.md, alfsen-shultz-notes.md, claim.md, attempt-01.md
    - Prime with: `peirce-post-jordan-finding` memory, forbidden-token list, R1-R5 pitfalls, "CIRCULAR vs IDENTITY" framing discipline (per sms:minimal: reviewer must state which framing it is applying when flagging something circular)
    - Instruction: verify attempt-01 proves all three subclaims (i), (ii), (iii) without smuggling Jordan / M_n(ℂ) / spin-factor proof devices or other forbidden structure. Return PASS / BORDERLINE / FAIL with a verbatim objection quote if FAIL.

    If BORDERLINE, escalate to Paper-5-primed Opus subagent (CONTEXT.md Decisions §Adversarial fresh-eyes review escalation path). If FAIL, carry the verbatim objection forward to attempt-02's header (if authorized).
  </action>
  <verify>
    1. attempt-01.md header declares seed strategy matching Plan 54-01 Task 2 decision (test-attempt-01-seed-match).
    2. Three sub-proofs for (i), (ii), (iii) present as separate propositions (not conflated).
    3. Every A-S compression citation in attempt-01.md resolves to an alfsen-shultz-notes.md row.
    4. Drift log present; any considered-and-rejected paths recorded.
    5. attempt-01.py exits 0; both V_2 and V_1 invariance tests print PASS (test-attempt-01-sympy).
    6. Forbidden-token grep on attempt-01.md returns zero hits outside explicit forbidden-tool declaration sections.
    7. Model-instantiation check recorded in attempt-01.md §Model-Instantiation, consistent with C(X) trivial and M_n(ℂ)^sa Jordan-consistent results.
    8. Adversarial review verdict recorded in attempt-01.md footer AND attempt-log.md.
    9. Status line is exactly one of {CLOSURE, FAILED}.
  </verify>
  <done>
    attempt-01.md and attempt-01.py exist; all three early gates PASS or one explicitly FAILS; adversarial review invoked only if all gates PASS; status line binary; attempt-log.md row appended with result.

    REJECTED (forbidden proxies): fp-attempt-jordan-smuggle (any forbidden-token proof device), fp-attempt-decomposition-non-sequitur (R2), fp-attempt-single-compression-conflation (R3 — no cross-term case), fp-attempt-padding-for-r4 (rhetorical filler), fp-attempt-c-iii-silent-drift (pre-S4 Jordan construction).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — attempt-01 status and attempt-02 authorization (or PIVOT-TO-C-I if option-c)</name>
  <files>derivations/paper5-peirce-preservation/attempt-01.md, derivations/paper5-peirce-preservation/attempt-log.md</files>
  <action>
    Present attempt-01's outcome to the user for routing decision. Inspect attempt-01.md footer and attempt-log.md.

    **Options to present to user:**

    - **If attempt-01 CLOSURE and adversarial review PASS:** Plan 54-02 may emit outcome tag '(A) via attempt-01' immediately; skip attempts 02, 03; advance to final-outcome-tag task (Task 6). Resume signal: `attempt-01-CLOSURE-accept`.
    - **If attempt-01 FAILED (early gate) or adversarial review FAIL:** Verbatim objection available. Authorize attempt-02 (Task 3) with structural change from attempt-01 (if attempt-01 was Approach 1, attempt-02 is Approach 2; or vice versa). Resume signal: `authorize-attempt-02`.
    - **If attempt-01 adversarial review BORDERLINE and Opus escalation also BORDERLINE:** flag as unresolvable at primary; authorize attempt-02 with both verbatim objections carried forward. Resume signal: `authorize-attempt-02`.
    - **If the Plan 54-01 Task 2 selection was option-c-fails-compression-algebra:** this Task 2 checkpoint confirms that no attempt was attempted; emit 'PIVOT-TO-C-I' in attempt-log.md and advance to Task 6. Resume signal: `pivot-to-C-i-now`.

    Check also: is the R3 cross-term case meaningfully addressed in attempt-01, or should the next attempt be instructed to expand this case? (Common structural failure mode per Pitfall 3.)

    Record the user's decision in attempt-log.md as the routing for Task 3 / Task 6.
  </action>
  <verify>
    1. User has selected exactly one of: attempt-01-CLOSURE-accept, authorize-attempt-02, pivot-to-C-i-now.
    2. Selection recorded in attempt-log.md.
    3. Selection is consistent with attempt-01 state (e.g., attempt-01-CLOSURE-accept requires attempt-01 CLOSURE + adversarial PASS).
  </verify>
  <done>
    User selection recorded in attempt-log.md; task routing for Task 3 (or skip to Task 6) gated.

    REJECTED: Proceeding without user selection; inconsistent routing (e.g., attempt-01-CLOSURE-accept when attempt-01 status is FAILED).
  </done>
</task>

<task type="auto">
  <name>Task 3: (conditional) attempt-02.md + attempt-02.py with structural change</name>
  <files>derivations/paper5-peirce-preservation/attempt-02.md, derivations/paper5-peirce-preservation/attempt-02.py</files>
  <action>
    Execute only if Task 2 resume-signal was "authorize-attempt-02".

    **attempt-02.md header:**
    - `seeded-from: [Approach 1 or Approach 2 — must be the ALTERNATIVE to attempt-01's approach]`
    - `avoiding-because: [attempt-01's failure mode]`
    - `addresses prior objection: "[verbatim quote from attempt-01's adversarial review FAIL or early-gate failure]"`
    - `assumption set: {S1, S3, linearity, A-S compression axioms}`
    - Structural difference from attempt-01 explicitly stated. Examples:
      - If attempt-01 was Approach 1 (4-06 seed) and failed on the mixing term: attempt-02 uses Approach 2 (compression combinatorics) to construct the mixing term from A-S compression algebra alone.
      - If attempt-01 was Approach 2 (compression combinatorics) and the mixing term could not be constructed from compressions alone: attempt-02 may either (a) re-seed from Approach 1 with the specific mixing-term objection addressed, or (b) declare 'mixing term is not constructible from S1+S3+compressions alone' — which is EVIDENCE FOR (C-i), and forwards to Plan 54-03's S0 defense.

    Repeat Task 1's internal structure (three sub-proofs, three early gates, adversarial review).

    **Reject fp-attempt-same-strategy-twice:** if attempt-02 is Approach X with only rewording of attempt-01's Approach X, attempt-02 is structurally redundant; mark FAILED and pivot to (C-i) at the end-of-attempt-02 stop/rethink (Task 4). The executor must verify the structural difference is real.
  </action>
  <verify>
    1. attempt-02 executed only if Task 2 resume-signal authorized.
    2. Header includes verbatim objection from attempt-01's FAIL.
    3. Structural difference from attempt-01 is real (not just rewording) — fp-attempt-same-strategy-twice rejected.
    4. Same three early gates and adversarial review as attempt-01.
    5. Status line binary {CLOSURE, FAILED}.
    6. attempt-log.md row appended.
  </verify>
  <done>
    attempt-02.md and attempt-02.py exist; structural change from attempt-01 verified; early gates run; adversarial review verdict recorded; status line binary.

    REJECTED (forbidden proxies): fp-attempt-same-strategy-twice (marginal variation of attempt-01); same list as Task 1.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: (mandatory) End-of-attempt-02 stop/rethink pause per CONTEXT.md #1</name>
  <files>derivations/paper5-peirce-preservation/attempt-01.md, derivations/paper5-peirce-preservation/attempt-02.md, derivations/paper5-peirce-preservation/attempt-log.md</files>
  <action>
    CONTEXT.md stop/rethink trigger #1 FIRES at end of attempt-02 REGARDLESS of outcome. Human reviews before authorizing attempt-03 or PIVOT-TO-C-I. Budget discipline: "if we're still failing after two, strategy needs to change before attempt-03, not just another draft."

    Present attempt-01 + attempt-02 outcomes and accumulated objection record to the user.

    **Options to present to user:**

    - **If attempt-02 CLOSURE:** emit '(A) via attempt-02' → Task 6. Resume signal: `attempt-02-CLOSURE-accept`.
    - **If both attempts FAILED with convergent objections (same root cause, e.g., 'mixing term is not constructible'):** PIVOT-TO-C-I is well-motivated; carry the convergent objection forward as a direct input to Plan 54-03's S0 defense (evidence for why S0 is necessary). Resume signal: `pivot-to-C-i-now`.
    - **If both attempts FAILED with divergent objections suggesting a novel strategy attempt-03 could address:** authorize attempt-03 with explicit structural-change requirement beyond Approaches 1 and 2 (e.g., a hybrid that combines compression combinatorics with a specific lemma from A-S that neither attempt-01 nor attempt-02 used; this requires a rationale, not just another draft). Resume signal: `authorize-attempt-03`.
    - **If uncertain:** default to PIVOT-TO-C-I (conservative; attempt cap is 3 as OUTER bound, "probably won't let it get that far"). Resume signal: `pivot-to-C-i-now`.

    Record the user's decision in attempt-log.md. If `authorize-attempt-03`, also record the specific structural-change rationale the user provided (no rationale → do not authorize attempt-03; default to PIVOT-TO-C-I).
  </action>
  <verify>
    1. User has selected exactly one of: attempt-02-CLOSURE-accept, authorize-attempt-03, pivot-to-C-i-now.
    2. Selection recorded in attempt-log.md.
    3. If authorize-attempt-03, structural-change rationale is recorded (not just a rewording justification).
    4. Stop/rethink trigger #1 was honored (pause DID fire regardless of attempt-02 outcome).
  </verify>
  <done>
    User selection recorded in attempt-log.md; task routing for Task 5 (or skip to Task 6) gated; structural-change rationale present if Task 5 authorized.

    REJECTED: Skipping the pause even if attempt-02 outcome is "clearly" one direction; authorizing attempt-03 without a structural-change rationale (rejects fp-attempt-same-strategy-twice).
  </done>
</task>

<task type="auto">
  <name>Task 5: (conditional) attempt-03.md + attempt-03.py with structural change from both prior attempts</name>
  <files>derivations/paper5-peirce-preservation/attempt-03.md, derivations/paper5-peirce-preservation/attempt-03.py</files>
  <action>
    Execute only if Task 4 resume-signal was "authorize-attempt-03".

    **attempt-03.md header:**
    - `seeded-from: [structural change beyond Approaches 1 and 2]`
    - `avoiding-because: [attempt-01 objection] AND [attempt-02 objection]`
    - `addresses prior objections: "[verbatim quote from attempt-01 adversarial review]" AND "[verbatim quote from attempt-02 adversarial review]"`
    - `assumption set: {S1, S3, linearity, A-S compression axioms}`
    - Strategy change justified at a STRUCTURAL level — a hybrid, a new specific A-S lemma application, or a case-distinction that neither attempt-01 nor attempt-02 exploited. Not just reworded.

    Repeat Task 1's internal structure (three sub-proofs, three early gates, adversarial review). This is the last (A) attempt within the attempt cap; FAILURE at this point means PIVOT-TO-C-I is mandatory.
  </action>
  <verify>
    1. attempt-03 executed only if Task 4 authorized.
    2. Header includes verbatim objections from BOTH attempt-01 and attempt-02.
    3. Structural change from both prior attempts explicitly stated.
    4. Same early gates and adversarial review.
    5. Status line binary.
  </verify>
  <done>
    attempt-03.md and attempt-03.py exist; structural change verified; gates and review run; status line binary.
  </done>
</task>

<task type="auto">
  <name>Task 6: Final attempt-log.md entry and outcome tag</name>
  <files>derivations/paper5-peirce-preservation/attempt-log.md</files>
  <action>
    Emit the final attempt-log.md entry with the plan's outcome tag:

    - **`outcome: (A) via attempt-NN`** — if any attempt-NN reached CLOSURE with adversarial review PASS. Record which attempt, which approach, and forward this tag to Plan 54-03 Task 1.
    - **`outcome: PIVOT-TO-C-I`** — if all executed attempts FAILED OR if Plan 54-01 Task 2 was option-c (direct pivot). Collect and forward ALL verbatim adversarial objections (from attempt-01, attempt-02, attempt-03 if executed) to Plan 54-03 Task 1 as input to the S0 defense — "we tried (A) and failed specifically because [X], which S0 resolves by [Y]."

    attempt-log.md final structure:
    - Per-attempt row: attempt-NN / seed strategy / early-gate results / adversarial review verdict / status / verbatim objection (if FAIL)
    - Final outcome tag line: exactly one of the two forms above
    - Carry-forward block: ALL verbatim objections assembled for Plan 54-03 input (if PIVOT-TO-C-I)
  </action>
  <verify>
    1. Outcome tag is exactly one of the two allowed forms; no composite or ambiguous tag.
    2. All verbatim objections carried forward (if PIVOT-TO-C-I).
    3. attempt-log.md closes with a line that Plan 54-03 Task 1 can grep for to determine its own task routing.
  </verify>
  <done>
    attempt-log.md final tag emitted; Plan 54-03 can read the tag and route its Task 1 accordingly.

    REJECTED (forbidden proxies): Emitting 'mostly (A)' or 'partial PIVOT' — outcome tag is binary.
  </done>
</task>

</tasks>

<verification>
- **Seed-strategy routing:** attempt-01 respects Plan 54-01 Task 2 decision (option-a, -b, or -c).
- **Attempt cap:** ≤ 3 attempts; end-of-attempt-02 stop/rethink pause enforced (Task 4 is mandatory; cannot be skipped even if both attempts fail "obviously").
- **Per-attempt gates:** three early falsifier gates + adversarial review for every attempt that reaches the review stage.
- **Objection carry-forward:** every FAIL objection is verbatim (not paraphrased), forwarded to the next attempt's header OR to Plan 54-03 (if PIVOT-TO-C-I).
- **Binary outcome tag:** '(A) via attempt-NN' or 'PIVOT-TO-C-I'; no ambiguous language.
- **C-iii drift guard:** any attempt constructing Jordan structure pre-S4 escalates IMMEDIATELY with verbatim drift-line quote (CONTEXT.md stop/rethink #3).
- **Framing discipline:** adversarial reviewer must distinguish CIRCULAR (proof-step inversion) from IDENTITY (definitional clause); borderline escalates to Opus.
</verification>

<success_criteria>
- At least attempt-01.md + attempt-01.py exist (unless Plan 54-01 option-c was selected, in which case attempt-log.md records 'SKIPPED (direct pivot per option-c)').
- Every executed attempt has all three early falsifier gates run; results recorded in attempt-log.md.
- Every attempt that reached adversarial review has a PASS / BORDERLINE / FAIL verdict recorded.
- End-of-attempt-02 stop/rethink pause (Task 4) was reached if attempt-02 was executed, regardless of outcome.
- Final attempt-log.md line contains exactly one outcome tag: '(A) via attempt-NN' (with attempt-NN named) OR 'PIVOT-TO-C-I'.
- All FAIL objections are recorded VERBATIM (no paraphrase) and available to Plan 54-03.
- Plan 54-02 validates via `gpd validate plan-contract` with zero errors.
</success_criteria>

<output>
After completion, create `.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-02-SUMMARY.md` recording: number of attempts executed, each attempt's seed strategy, each attempt's gate + review outcome, the final outcome tag, and any verbatim objections carried forward to Plan 54-03.
</output>
