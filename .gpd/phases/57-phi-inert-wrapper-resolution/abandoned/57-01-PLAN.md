---
phase: 57-phi-inert-wrapper-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - derivations/paper5-peirce-preservation/phi-audit.md
interactive: false
researcher_setup: []

conventions:
  units: "N/A (pure algebra, notation audit)"
  metric: "N/A"
  coordinates: "N/A"
  tracking_macro: "\\varphi (single existing macro — NOT \\phi, NOT \\Phi, NOT \\widehat{\\Phi})"
  sequential_product: "a \u2218 b (Paper 5 v14.0 convention)"
  append_only_shared_artifact: "alfsen-shultz-notes.md mutations = append only; modifying prior rows FORBIDDEN"
  frozen_submission_tag: "paper5-jmp-submitted (read-only baseline; NEVER edit main-jmp-submitted.tex)"

dimensional_check:
  varphi_occurrence_count: "[dimensionless count; expected 48 total = 16+2+7+2+7+13+1]"
  prop_inheritance_internal_phi_count: "[dimensionless count; unknown until deep-dive]"

approximations:
  - name: "Single-macro-source assumption"
    parameter: "Paper 5 uses only \\varphi; zero \\phi/\\Phi/\\widehat occurrences"
    validity: "Confirmed by preflight grep (Phase 57 RESEARCH §1); must be re-verified by Task 1 with broadened patterns"
    breaks_when: "Hidden macro in preamble.sty (e.g., \\newcommand{...phi...}) or \\def-based redefinition; patch of paper in transit"
    check: "Task 1 grep includes -e '\\\\phi' -e '\\\\Phi' -e '\\\\widehat' -e '\\\\hat' and scans preamble.sty for \\newcommand.*[Pp]hi / \\def.*[Pp]hi"

  - name: "prop:inheritance phi-independence is route-re-routable through Peirce-Preservation Lemma"
    parameter: "Phase 56 CLOSE flags prop:inheritance as primary phi-audit target but does not pre-classify its internal \\varphi usage"
    validity: "If every \\varphi in the prop:inheritance proof resolves through lem:peirce-preservation or cor:S4-phi-indep, outcome (B) holds; if a genuinely \\varphi-specific step exists, outcome shifts to (C-i) structural revision"
    breaks_when: "Proof of prop:inheritance uses \\varphi^{-1} or selects a \\varphi-specific property (e.g., faithful) as a non-re-routable step"
    check: "Task 3 deep-dive per-line classification of every \\varphi in composite-lt.tex:66 proposition statement + proof + 3 consumer sites (appendix-proofs.tex:235; composite-lt.tex:217; type-exclusion.tex:127, 246)"

contract:
  scope:
    question: "What is the evidentiary baseline for Paper 5's \\varphi notation (exact occurrence list with file:line provenance), and does prop:inheritance — Phase 56's explicitly named primary phi-audit target — contain a \\varphi-dependent step that would force outcome (C-i) structural revision rather than (B) notation audit?"
  claims:
    - id: "claim-grep-baseline"
      statement: "Paper 5 contains exactly 48 \\varphi occurrences with zero \\phi/\\Phi/\\widehat occurrences across main.tex + 6 section files + preamble.sty; this raw grep output is the evidentiary baseline for all subsequent classification and revision work (DERV-57-01 grep-reconciliation requirement)."
      deliverables: ["deliv-phi-audit-s1-s2"]
      acceptance_tests: ["test-grep-reconciliation", "test-preamble-scan"]
      references: ["ref-paper5-living", "ref-preamble", "ref-research-preflight"]
    - id: "claim-prop-inheritance-phi-classification"
      statement: "Every \\varphi occurrence in prop:inheritance (composite-lt.tex:66 proposition statement + proof) and its three consumer sites (appendix-proofs.tex:235; composite-lt.tex:217; type-exclusion.tex:127, 246) is classified with a 'minimal property used' column, and a verdict is reached on whether a \\varphi-specific (non-re-routable) step exists; this resolves open question Q2 from 57-RESEARCH and determines outcome (B) vs (C-i) pivot before Plan 57-02 classification begins (DERV-57-02 equivocation-site identification; Phase 56 CLOSE primary target)."
      deliverables: ["deliv-phi-audit-s3"]
      acceptance_tests: ["test-prop-inheritance-lineaudit", "test-outcome-pivot-gate"]
      references: ["ref-phase56-close", "ref-phase54-result", "ref-phase55-result", "ref-pitfalls-r8"]
    - id: "claim-derv-00-01-phase57-slice-plan01"
      statement: "Plan 57-01 deliverable phi-audit.md §1-§3 preserves append-only shared-artifact discipline (alfsen-shultz-notes.md NOT modified in this plan; only quoted-with-line-numbers); frozen-file zero-diff discipline (main-jmp-submitted.tex NOT opened); and the four forbidden proxies (R8-single-fix, Chiribella purification, global-identity assumption, rename-without-classification) are explicitly not invoked in §1-§3 prose (DERV-00-01 Phase 57 slice, Plan 57-01 portion)."
      deliverables: ["deliv-phi-audit-s1-s2", "deliv-phi-audit-s3"]
      acceptance_tests: ["test-forbidden-proxy-absence-plan01", "test-frozen-file-untouched"]
      references: ["ref-phase56-close", "ref-pitfalls-r8", "ref-roadmap-p57-forbidden"]
  deliverables:
    - id: "deliv-phi-audit-s1-s2"
      kind: "audit-document"
      path: "derivations/paper5-peirce-preservation/phi-audit.md"
      description: "phi-audit.md §1 Methodology (grep command used; preamble.sty scan result; 4-role taxonomy summary; R8 heuristic) and §2 Raw Grep Output (verbatim rg -nH output for all 48 \\varphi lines + preamble.sty \\newcommand scan result); evidentiary baseline for Plans 57-02/03."
      must_contain:
        - "§1 Methodology"
        - "§2 Raw Grep Output (Evidentiary Baseline)"
        - "main.tex:16"
        - "sections/composite-lt.tex:7"
        - "sections/discussion.tex:13"
        - "preamble.sty: 0 custom phi macros"
        - "48 total"
    - id: "deliv-phi-audit-s3"
      kind: "audit-document"
      path: "derivations/paper5-peirce-preservation/phi-audit.md"
      description: "phi-audit.md §3 prop:inheritance Deep-Dive: per-line classification of every \\varphi in composite-lt.tex:66 proposition statement + proof body + 3 consumer sites; explicit verdict 'prop:inheritance is re-routable through lem:peirce-preservation / cor:S4-phi-indep' (outcome B remains feasible) OR 'prop:inheritance has \\varphi-specific step at line LL' (outcome pivot to C-i required)."
      must_contain:
        - "§3 prop:inheritance Deep-Dive"
        - "composite-lt.tex:66"
        - "appendix-proofs.tex:235"
        - "composite-lt.tex:217"
        - "type-exclusion.tex:127"
        - "type-exclusion.tex:246"
        - "Verdict:"
        - "Outcome (B) feasible"
  references:
    - id: "ref-paper5-living"
      kind: "prior-artifact"
      locator: "~/repos/blog/landing/papers/qm-from-self-modeling/main.tex + sections/*.tex"
      role: "grep-source"
      why_it_matters: "Living editable copy of Paper 5 is the grep source for DERV-57-01 and the edit target for Plan 57-03 integration; read-only in Plan 57-01 (no edits)."
      applies_to: ["claim-grep-baseline", "claim-prop-inheritance-phi-classification"]
      must_surface: true
      required_actions: ["read", "grep-with-line-numbers", "quote-verbatim"]
    - id: "ref-preamble"
      kind: "prior-artifact"
      locator: "~/repos/blog/landing/papers/qm-from-self-modeling/preamble.sty"
      role: "macro-definition-source"
      why_it_matters: "Must confirm ZERO custom phi-family \\newcommand / \\def definitions; if any are found, the single-macro assumption breaks and the grep scope must be broadened in-plan."
      applies_to: ["claim-grep-baseline"]
      must_surface: true
      required_actions: ["read", "grep-newcommand-pattern", "confirm-zero-phi-macros"]
    - id: "ref-research-preflight"
      kind: "prior-artifact"
      locator: ".gpd/phases/57-phi-inert-wrapper-resolution/57-RESEARCH.md"
      role: "plan-input"
      why_it_matters: "Preflight grep counts, 4-role taxonomy, R8 heuristic, and prop:inheritance deep-dive methodology; Plan 57-01 quotes the raw grep output verbatim into phi-audit.md §2 and uses §Classification Taxonomy as the rubric source."
      applies_to: ["claim-grep-baseline", "claim-prop-inheritance-phi-classification"]
      must_surface: true
      required_actions: ["read", "cite", "quote-classification-rubric"]
    - id: "ref-phase56-close"
      kind: "prior-artifact"
      locator: "derivations/paper5-peirce-preservation/alfsen-shultz-notes.md:426-533 (§'Phase 56 CLOSE' + §'Phase 57 inheritance note')"
      role: "primary-phi-audit-target-specifier"
      why_it_matters: "Explicitly names prop:inheritance as THE primary phi-audit target for Phase 57; establishes the append-only discipline Plan 57-01 must preserve (READ-ONLY in this plan; Plan 57-03 appends)."
      applies_to: ["claim-prop-inheritance-phi-classification", "claim-derv-00-01-phase57-slice-plan01"]
      must_surface: true
      required_actions: ["read", "cite-with-line-numbers", "preserve-append-only"]
    - id: "ref-phase54-result"
      kind: "prior-artifact"
      locator: ".gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md"
      role: "phi-independence-source"
      why_it_matters: "Establishes Cor cor:S4-phi-indep (S4 holds for any f with f(0,x)=0) — the target citation for every role-(d) classification in Plans 57-01/02; Plan 57-01 cites when verifying prop:inheritance re-routing."
      applies_to: ["claim-prop-inheritance-phi-classification"]
      must_surface: true
      required_actions: ["read", "cite"]
    - id: "ref-phase55-result"
      kind: "prior-artifact"
      locator: ".gpd/phases/55-s4-facial-structure-lemma/55-RESULT.md"
      role: "phi-independence-sympy-verification"
      why_it_matters: "SymPy Test 3 (runtime 0.3s exact) φ-independence verified under f=λμ; Plan 57-01 cites as the computational confirmation of Phase 54's corollary — NO new SymPy needed."
      applies_to: ["claim-prop-inheritance-phi-classification"]
      must_surface: true
      required_actions: ["read", "cite", "do-not-rederive"]
    - id: "ref-pitfalls-r8"
      kind: "prior-artifact"
      locator: ".gpd/research/PITFALLS.md:245-273 (§R8 Phi Overloading)"
      role: "taxonomy-source"
      why_it_matters: "Four-section role hypothesis + equivocation-detection methodology + recovery strategy; Plan 57-01 inherits the taxonomy and heuristic verbatim."
      applies_to: ["claim-prop-inheritance-phi-classification", "claim-derv-00-01-phase57-slice-plan01"]
      must_surface: true
      required_actions: ["read", "cite", "apply-heuristic"]
    - id: "ref-roadmap-p57-forbidden"
      kind: "prior-artifact"
      locator: ".gpd/ROADMAP.md:775 (Phase 57 Forbidden proxies list)"
      role: "forbidden-proxy-specifier"
      why_it_matters: "Defines the 4 forbidden proxies Plan 57-01 must explicitly reject: (i) fixing one occurrence without checking all (R8); (ii) assuming Φ is globally identity; (iii) Chiribella purification invocation; (iv) renaming without classification-exhaustive check."
      applies_to: ["claim-derv-00-01-phase57-slice-plan01"]
      must_surface: true
      required_actions: ["read", "cite", "explicit-rejection-in-prose"]
  acceptance_tests:
    - id: "test-grep-reconciliation"
      subject: "claim-grep-baseline"
      kind: "count-reconciliation"
      procedure: "Run rg -nH --no-heading -e '\\\\varphi' -e '\\\\phi' -e '\\\\Phi' -e '\\\\widehat' -e '\\\\hat' main.tex sections/*.tex preamble.sty; count rows per file; compare to research preflight baseline (main.tex:16; appendix-proofs.tex:7; axiom-verification.tex:2; composite-lt.tex:7; discussion.tex:13; type-exclusion.tex:1; appendix-numerical.tex:2; preamble.sty:0 = 48 total)."
      pass_condition: "Per-file counts MATCH research preflight exactly (48 total \\varphi, 0 \\phi/\\Phi/\\widehat). If any count deviates, stop and investigate before §2 is written — either the paper drifted or the grep pattern missed something."
      evidence_required: ["deliv-phi-audit-s1-s2", "ref-paper5-living", "ref-research-preflight"]
    - id: "test-preamble-scan"
      subject: "claim-grep-baseline"
      kind: "macro-definition-scan"
      procedure: "Run grep -nE '\\\\newcommand.*[Pp]hi|\\\\def.*[Pp]hi|\\\\DeclareMathOperator.*[Pp]hi' preamble.sty; capture full output (including zero-match confirmation)."
      pass_condition: "Zero custom phi-family macros defined in preamble.sty (matching research preflight). If any are found, the single-macro-source assumption is broken; Plan 57-02 scope expands to include those macros and Plan 57-01 must be re-scoped before proceeding."
      evidence_required: ["deliv-phi-audit-s1-s2", "ref-preamble"]
    - id: "test-prop-inheritance-lineaudit"
      subject: "claim-prop-inheritance-phi-classification"
      kind: "per-line-audit"
      procedure: "For prop:inheritance (composite-lt.tex:66 statement + proof body, locate proof span via 'begin{proof}'/'end{proof}' markers) and its 3 consumer sites (appendix-proofs.tex:235; composite-lt.tex:217; type-exclusion.tex:127, 246): enumerate every \\varphi usage; for each, fill (file:line, local text excerpt, role hypothesis (a/b/c/d), minimal property of \\varphi used, inherited phi-independence cite if role (d)); identify any step that requires a \\varphi-specific property not re-routable through lem:peirce-preservation or cor:S4-phi-indep."
      pass_condition: "100% of prop:inheritance internal \\varphi usages classified with all 5 columns populated; explicit verdict 'Outcome (B) feasible — prop:inheritance re-routable' OR 'Outcome (C-i) required — \\varphi-specific step at line LL'; if the latter, the verdict names the exact line and the specific property used."
      evidence_required: ["deliv-phi-audit-s3", "ref-paper5-living", "ref-phase56-close", "ref-phase54-result", "ref-phase55-result"]
    - id: "test-outcome-pivot-gate"
      subject: "claim-prop-inheritance-phi-classification"
      kind: "gate-check"
      procedure: "Compare the Task 3 verdict against Phase 56 CLOSE expectation (prop:inheritance should be re-routable per Phase 54 corollary). If Task 3 verdict confirms re-routable → Plan 57-02 proceeds with classification table; if Task 3 surfaces a \\varphi-specific step, phi-audit.md §3 records an ESCALATION MARKER and the next-action note 'Plan 57-02 paused pending /gpd:discuss-phase 57 — outcome may shift to (C-i)' (Phase 57 halts rather than silently expanding scope)."
      pass_condition: "Verdict is unambiguous (no 'maybe' / 'partial' classification); if escalation required, the escalation marker is explicit and Plan 57-02 is NOT started within this same plan run."
      evidence_required: ["deliv-phi-audit-s3"]
    - id: "test-forbidden-proxy-absence-plan01"
      subject: "claim-derv-00-01-phase57-slice-plan01"
      kind: "keyword-absence"
      procedure: "grep -ni 'purification\\|chiribella\\|CDP\\|V_M = V_B\\|\\\\varphi = id' derivations/paper5-peirce-preservation/phi-audit.md (Plan 57-01 output). For every hit, confirm the surrounding prose is an explicit REJECTION (e.g., 'Paper 5 does NOT use purification') rather than an invocation."
      pass_condition: "Zero hits that are invocations (not rejections); all hits (if any) are explicit rejections with 'NOT', 'forbidden', or 'do not use' adjacent."
      evidence_required: ["deliv-phi-audit-s1-s2", "deliv-phi-audit-s3", "ref-roadmap-p57-forbidden"]
    - id: "test-frozen-file-untouched"
      subject: "claim-derv-00-01-phase57-slice-plan01"
      kind: "file-state-assertion"
      procedure: "Run git -C ~/repos/blog diff --stat paper5-jmp-submitted -- landing/papers/qm-from-self-modeling/main-jmp-submitted.tex AND git -C ~/repos/blog status --porcelain landing/papers/qm-from-self-modeling/main-jmp-submitted.tex."
      pass_condition: "diff --stat returns 0 lines changed AND status --porcelain returns empty (frozen baseline untouched throughout Plan 57-01)."
      evidence_required: []
  forbidden_proxies:
    - id: "fp-r8-single-fix-plan01"
      subject: "claim-grep-baseline"
      proxy: "Writing revision text or partial classification for a single \\varphi occurrence before the full grep baseline is committed to §2"
      reason: "Violates Phase 57 anti-pattern R8; Plan 57-02 classification table is the INPUT for any revision work, not an output; Plan 57-01 must deliver only §1-§3 (evidentiary + prop:inheritance pivot), NOT partial classification."
    - id: "fp-chiribella-purification-plan01"
      subject: "claim-prop-inheritance-phi-classification"
      proxy: "Classifying any prop:inheritance internal \\varphi as role (c) 'ancilla' by invoking Chiribella-D'Ariano-Perinotti 2011 purification postulate"
      reason: "Paper 5 uses faithful self-modeling, strictly weaker than CDP purification; importing CDP is a forbidden heavier-assumption proxy (ROADMAP.md:775; 57-RESEARCH §'Common Pitfalls' R8b)."
    - id: "fp-global-identity-plan01"
      subject: "claim-prop-inheritance-phi-classification"
      proxy: "Concluding prop:inheritance is re-routable by assuming '\\varphi = id globally' in its proof"
      reason: "\\varphi = id is a WLOG local specialization (cor:S4-phi-indep-justified), NOT a global claim; a proof re-routing that depends on global identity is not a valid re-routing (57-RESEARCH §Anti-Patterns R8c)."
    - id: "fp-rederive-cor-s4-phi-indep-plan01"
      subject: "claim-prop-inheritance-phi-classification"
      proxy: "Re-proving or re-verifying cor:S4-phi-indep inside phi-audit.md §3"
      reason: "Phase 54 sealed the corollary; Phase 55 SymPy-verified it; Plan 57-01 cites only (57-RESEARCH §Pitfalls R8e)."
  links:
    - id: "link-grep-to-baseline"
      source: "claim-grep-baseline"
      target: "deliv-phi-audit-s1-s2"
      relation: "supports"
      verified_by: ["test-grep-reconciliation", "test-preamble-scan"]
    - id: "link-deep-dive-to-pivot"
      source: "claim-prop-inheritance-phi-classification"
      target: "deliv-phi-audit-s3"
      relation: "supports"
      verified_by: ["test-prop-inheritance-lineaudit", "test-outcome-pivot-gate"]
    - id: "link-hand-off-to-plan-57-02"
      source: "deliv-phi-audit-s3"
      target: "claim-grep-baseline"
      relation: "enables"
      verified_by: ["test-outcome-pivot-gate"]
  uncertainty_markers:
    weakest_anchors:
      - "Verdict on prop:inheritance re-routability (Phase 56 CLOSE named it as primary target but did not pre-classify; Q2 in 57-RESEARCH §Open Questions — can flip outcome (B)→(C-i))"
      - "Completeness of grep patterns (if a hidden \\def or \\DeclareMathOperator defines a phi-alias not caught by the 4 listed patterns, the baseline is incomplete; Task 1 preamble scan is the mitigation)"
    disconfirming_observations:
      - "Any prop:inheritance internal \\varphi requires a property strictly stronger than 'faithful order iso' (e.g., normality, specific tracial property) — would force outcome (C-i) structural pivot and HALT Plan 57-02"
      - "grep pattern returns >= 1 match for \\phi or \\Phi (non-\\varphi form) anywhere in the 7 source files — would indicate drift in the paper since research preflight and REQUIRES re-scoping the 4-role taxonomy"
      - "preamble.sty contains a custom phi-family \\newcommand — would expand the macro space Plan 57-02 must classify and invalidate the 'single-macro' simplification"
---

<objective>
Establish the evidentiary baseline for Paper 5's phi-family notation audit and resolve the single outcome-pivot uncertainty (prop:inheritance re-routability, the Phase 56-named primary phi-audit target) BEFORE classification begins in Plan 57-02.

Purpose: DERV-57-01 (grep-reconciliation) and DERV-57-02 (equivocation-site identification) require a committed raw grep baseline and a resolved verdict on prop:inheritance. Without this plan, Plan 57-02 would iterate over 48 occurrences without knowing whether the phase can close at outcome (B) notation-audit or must escalate to outcome (C-i) structural revision. 57-RESEARCH §Open Questions Q2 names this as "the highest-uncertainty node"; Phase 56 CLOSE entry (alfsen-shultz-notes.md:500-511) explicitly flags prop:inheritance (composite-lt.tex:66 factor-level S5/S6/S7 inheritance chain) as the primary phi-audit target still outstanding.

Output:
- derivations/paper5-peirce-preservation/phi-audit.md §1 Methodology
- derivations/paper5-peirce-preservation/phi-audit.md §2 Raw Grep Output (evidentiary baseline; 48 \\varphi rows + preamble.sty scan)
- derivations/paper5-peirce-preservation/phi-audit.md §3 prop:inheritance Deep-Dive (per-line audit + verdict)
- Commit via gpd commit

Boundary: NO classification table (Plan 57-02); NO revision drafts (Plan 57-03); NO edits to any Paper 5 .tex file (Plan 57-03); NO append to alfsen-shultz-notes.md (Plan 57-03). All five of those are downstream.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/REQUIREMENTS.md
@.gpd/phases/57-phi-inert-wrapper-resolution/57-RESEARCH.md
@.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md
@.gpd/phases/55-s4-facial-structure-lemma/55-RESULT.md
@.gpd/phases/56-establish-w-product-form-closure-thm-5-8/56-RESULT.md
@derivations/paper5-peirce-preservation/alfsen-shultz-notes.md
@.gpd/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phi-audit.md scaffold and write §1 Methodology</name>
  <files>derivations/paper5-peirce-preservation/phi-audit.md</files>
  <action>
    Create `derivations/paper5-peirce-preservation/phi-audit.md` with the full scaffold mandated by 57-RESEARCH §"phi-audit.md Structure (DERV-57-01 DELIVERABLE)":

    1. Write the top-of-file header:
       ```
       # Phi Audit — Paper 5 `\varphi` Occurrence Classification

       **Consumed by:** Phases 54, 58, 59 (per DERV-57-01)
       **Produced in:** Phase 57 (in progress; Plan 57-01)
       **Frozen reference:** `~/repos/blog/landing/papers/qm-from-self-modeling/main-jmp-submitted.tex` @ tag paper5-jmp-submitted (READ-ONLY; NEVER EDIT)
       **Living editable copy:** `~/repos/blog/landing/papers/qm-from-self-modeling/main.tex` + sections/*.tex
       **Shared-artifact sibling:** `derivations/paper5-peirce-preservation/alfsen-shultz-notes.md` (append-only; Plan 57-03 appends Phase 57 CLOSE entry)
       ```

    2. Write §1 Methodology containing ALL of the following subsections in order:
       - **§1.1 Grep command used:** Quote the exact command: `rg -nH --no-heading -e '\\varphi' -e '\\phi' -e '\\Phi' -e '\\widehat' -e '\\hat' main.tex sections/*.tex preamble.sty` (run from `~/repos/blog/landing/papers/qm-from-self-modeling/`).
       - **§1.2 Preamble scan:** The fallback `grep -nE '\\\\newcommand.*[Pp]hi|\\\\def.*[Pp]hi|\\\\DeclareMathOperator.*[Pp]hi' preamble.sty` (must report zero matches per research preflight).
       - **§1.3 4-Role Classification Taxonomy:** Inline the four operational criteria from 57-RESEARCH §"Classification Taxonomy":
         * Role (a) Self-Modeling Tracking Map B→M (load-bearing) — 3 textual cues + operational criterion
         * Role (b) Inert Auxiliary Wrapper / Ancilla (likely empty) — note "Paper 5 grep for 'ancilla|wrapper' returns zero; role (b) predicted empty"
         * Role (c) State-Preparation Notation (likely empty) — note "Paper 5 uses \\varphi exclusively as a MAP, never as a state"
         * Role (d) Exposition Shorthand (expected dominant) — operational criterion + textual cues + citation-adjacency requirement
       - **§1.4 R8 Equivocation Detection Heuristic:** Inline the three-clause heuristic from 57-RESEARCH §"R8 Equivocation Detection Heuristic" (within-subsection role-shift without reintroduction; role (a)↔(d) adjacency without subsection boundary; missing/stale phi-independence cite).
       - **§1.5 Forbidden Proxies (explicit rejection):** List and REJECT each of:
         * (i) R8 single-fix — "This audit will not revise any \\varphi occurrence until the full classification table (Plan 57-02) is committed."
         * (ii) Chiribella/CDP purification — "Paper 5 uses faithful self-modeling, which is strictly weaker than CDP 2011 purification; no role-(c) classification in this audit will invoke purification."
         * (iii) Global \\varphi = id assumption — "Every \\varphi = id specialization is a WLOG local claim citable to cor:S4-phi-indep, NOT a global identification; Def. def:self-modeling-system is preserved generically."
         * (iv) Rename-without-classification — "The classification table IS the rename rule; Plan 57-03 revision drafts consume, not produce, the table."
       - **§1.6 Scope boundary (Plan 57-01 ONLY):** "This plan produces §1-§3 only. §4 classification table = Plan 57-02; §5/§6 revision drafts + §7 pivot decision + §8 consistency check + §9 hand-off = Plan 57-03."

    3. Do NOT begin §2 or §3 yet — those are Tasks 2 and 3. Leave placeholder anchors:
       ```
       ## §2. Raw Grep Output (Evidentiary Baseline)
       _Populated in Task 2._

       ## §3. prop:inheritance Deep-Dive (Phase 56 CLOSE primary target)
       _Populated in Task 3._

       ## §4-§9
       _Populated in Plans 57-02 and 57-03._
       ```

    Do NOT touch any file outside `derivations/paper5-peirce-preservation/phi-audit.md`. Do NOT open main-jmp-submitted.tex. Do NOT append to alfsen-shultz-notes.md.
  </action>
  <verify>
    1. File exists at `derivations/paper5-peirce-preservation/phi-audit.md`.
    2. §1 contains ALL six subsections (§1.1 through §1.6).
    3. §1.5 explicitly REJECTS all four forbidden proxies verbatim (grep -c 'Paper 5 uses faithful self-modeling, which is strictly weaker' phi-audit.md → 1; grep -c 'global identification' phi-audit.md → ≥ 1; grep -c 'classification table IS the rename rule' phi-audit.md → 1; grep -c 'will not revise any \\\\varphi occurrence until the full classification table' phi-audit.md → 1).
    4. §2 and §3 placeholders are present with `_Populated in Task N._` text (NOT real content).
    5. `git status --porcelain ~/repos/blog/landing/papers/qm-from-self-modeling/` returns empty (no blog-repo edits).
  </verify>
  <done>
    phi-audit.md exists with complete §1 (methodology) and §2/§3 placeholders. Forbidden proxies (R8 single-fix, Chiribella purification, global-identity assumption, rename-without-classification) are explicitly listed and rejected in §1.5. No other files modified. No Paper 5 source edits. (Rejects R8 single-fix forbidden proxy: scaffold task writes NO revision text; rejects rename-without-classification forbidden proxy: §1.6 explicitly pins rename work to Plan 57-03.)
  </done>
</task>

<task type="auto">
  <name>Task 2: Run grep baseline and populate §2 Raw Grep Output</name>
  <files>derivations/paper5-peirce-preservation/phi-audit.md</files>
  <action>
    Execute the grep and preamble scan, then commit the raw output verbatim into phi-audit.md §2.

    1. From `/Users/ehrlich/repos/blog/landing/papers/qm-from-self-modeling/`, run:
       ```
       rg -nH --no-heading -e '\\varphi' -e '\\phi' -e '\\Phi' -e '\\widehat' -e '\\hat' main.tex sections/*.tex preamble.sty
       ```
       Capture the complete stdout. If `rg` is unavailable, fall back to:
       ```
       grep -nHE '\\\\varphi|\\\\phi|\\\\Phi|\\\\widehat|\\\\hat' main.tex sections/*.tex preamble.sty
       ```

    2. Also run the per-file count reconciliation:
       ```
       rg -cn '\\varphi' main.tex sections/*.tex preamble.sty
       rg -cn '\\phi|\\Phi|\\widehat|\\hat' main.tex sections/*.tex preamble.sty
       ```
       Record per-file counts and the grand total.

    3. Run the preamble macro-definition scan:
       ```
       grep -nE '\\\\newcommand.*[Pp]hi|\\\\def.*[Pp]hi|\\\\DeclareMathOperator.*[Pp]hi' preamble.sty
       ```
       Capture the complete output (expected: zero-match, i.e., empty stdout).

    4. Open phi-audit.md and REPLACE the §2 placeholder with the following populated structure:
       ```
       ## §2. Raw Grep Output (Evidentiary Baseline)

       **Run date:** 2026-04-17 (or execution date)
       **Working directory:** `/Users/ehrlich/repos/blog/landing/papers/qm-from-self-modeling/`

       ### §2.1 Per-file \\varphi count reconciliation

       | File | \\varphi count | Research-preflight baseline | Match? |
       |------|-----------------|------------------------------|--------|
       | main.tex | [N] | 16 | ✓/✗ |
       | sections/appendix-proofs.tex | [N] | 7 | ✓/✗ |
       | sections/axiom-verification.tex | [N] | 2 | ✓/✗ |
       | sections/composite-lt.tex | [N] | 7 | ✓/✗ |
       | sections/discussion.tex | [N] | 13 | ✓/✗ |
       | sections/type-exclusion.tex | [N] | 1 | ✓/✗ |
       | sections/appendix-numerical.tex | [N] | 2 | ✓/✗ |
       | preamble.sty | [N] | 0 | ✓/✗ |
       | **TOTAL** | [N] | **48** | ✓/✗ |

       ### §2.2 Raw \\varphi occurrence list (verbatim rg -nH output)

       ```
       [Paste the full stdout of the rg command, verbatim, line-for-line]
       ```

       ### §2.3 Non-\\varphi phi-family pattern scan

       **Command:** `rg -cn '\\phi|\\Phi|\\widehat|\\hat' main.tex sections/*.tex preamble.sty`
       **Expected:** 0 matches across all files (per research preflight).
       **Actual:**
       ```
       [Paste output; expected empty or all-zero]
       ```

       ### §2.4 preamble.sty macro-definition scan

       **Command:** `grep -nE '\\\\newcommand.*[Pp]hi|\\\\def.*[Pp]hi|\\\\DeclareMathOperator.*[Pp]hi' preamble.sty`
       **Expected:** zero-match (empty stdout).
       **Actual:**
       ```
       [Paste output; expected empty]
       ```

       ### §2.5 Reconciliation verdict

       - [ ] Per-file \\varphi counts match research preflight baseline exactly (48 total).
       - [ ] Non-\\varphi phi-family scan returns zero matches.
       - [ ] preamble.sty scan returns zero custom phi macros.
       - [ ] If ANY deviation, Plan 57-02 is blocked pending re-scope.

       **Verdict:** [MATCHES / DEVIATIONS SURFACED — DETAILS BELOW]
       ```

    5. If any count deviates from 16/7/2/7/13/1/2/0 = 48 per file, STOP. Write a "Deviation details" subsection in §2.5 naming the exact file and count mismatch, and do NOT proceed to Task 3 until the deviation is explained (either research preflight was wrong, or paper drifted, or grep pattern missed something). Append an explicit instruction: "Plan 57-02 is BLOCKED; escalate via /gpd:discuss-phase 57."

    6. If counts match, write "Verdict: MATCHES — baseline established; proceed to Task 3."

    Do NOT open main-jmp-submitted.tex. Do NOT edit main.tex or any sections/*.tex file (read-only inspection only; all writes are to phi-audit.md).
  </action>
  <verify>
    1. §2 is fully populated (placeholder replaced).
    2. §2.1 table has exactly 8 rows + TOTAL; every file's count column is numerical (not "[N]").
    3. §2.2 raw output contains exactly 48 \\varphi lines (unless deviation surfaced).
    4. §2.3 shows zero matches for non-\\varphi patterns.
    5. §2.4 shows empty preamble scan output.
    6. §2.5 Verdict is one of {MATCHES, DEVIATIONS SURFACED}; if DEVIATIONS, the blocking instruction is present.
    7. `git -C ~/repos/blog status --porcelain landing/papers/qm-from-self-modeling/` returns empty (no blog-repo edits).
    8. `git -C ~/repos/blog diff --stat paper5-jmp-submitted -- landing/papers/qm-from-self-modeling/main-jmp-submitted.tex` returns "0 files changed" or similar zero-diff.
  </verify>
  <done>
    phi-audit.md §2 populated with verbatim grep outputs + count reconciliation + preamble scan + explicit verdict. If the verdict is MATCHES (expected), Task 3 is unblocked; if DEVIATIONS, Task 3 is NOT executed and the plan halts for escalation. (Rejects R8 single-fix forbidden proxy: only raw data committed, zero classification attempted; rejects rename-without-classification forbidden proxy: §2 is data-only, no rename prose.)
  </done>
</task>

<task type="auto">
  <name>Task 3: prop:inheritance deep-dive and outcome-pivot verdict (§3)</name>
  <files>derivations/paper5-peirce-preservation/phi-audit.md</files>
  <action>
    Conduct the per-line \\varphi audit of prop:inheritance (Phase 56 CLOSE's primary phi-audit target) and commit the verdict into phi-audit.md §3. This task resolves 57-RESEARCH Q2 and gates Plan 57-02.

    1. Locate prop:inheritance in `~/repos/blog/landing/papers/qm-from-self-modeling/sections/composite-lt.tex`:
       - Proposition statement: line 66 (and surrounding context — read lines 50-90 for full context of the proposition + its statement).
       - Proof body: locate via `begin{proof}` / `end{proof}` markers adjacent to line 66. If the proof is in-line rather than standalone, read from line 66 forward until the proposition's argument terminates (typically 10-50 lines).

    2. Locate the 3 consumer sites (per 57-RESEARCH §Active Anchor References):
       - `appendix-proofs.tex:235` — read ±10 lines of context.
       - `composite-lt.tex:217` — read ±10 lines of context.
       - `type-exclusion.tex:127, 246` — read ±10 lines of context at each.

    3. Enumerate every \\varphi occurrence in the prop:inheritance statement + proof body + 4 consumer contexts. For each occurrence, produce a classification row using this table structure (populated into §3.2 of phi-audit.md):

       | # | file:line | Local text excerpt (±1 line) | Role hypothesis (a/b/c/d) | Minimal property of \\varphi used here | Phi-indep cite if role (d) | Re-routable through lem:peirce-preservation / cor:S4-phi-indep? |
       |---|-----------|------------------------------|---------------------------|----------------------------------------|-----------------------------|---------------------------------------------------------------|

       For each row, fill every column. For the "minimal property" column, use one of:
       - "order isomorphism V_B → V_M" (full definition used)
       - "positive unital map" (weaker than order iso — if used here, the argument is phi-INDEPENDENT modulo regularity)
       - "faithful order iso" (faithful used specifically — check whether faithfulness is actually exercised or whether it's a carried property unused at this step)
       - "\\varphi^{-1} exists" (invertibility used — role (a) marker)
       - "generic map tag (no specific property)" (role (d) marker)

       For the "Re-routable?" column, use one of:
       - "YES — via lem:peirce-preservation"
       - "YES — via cor:S4-phi-indep"
       - "YES — unused at this step (pure tag)"
       - "NO — requires property X at step Y" (this is the smoking gun for outcome C-i)

    4. Read Phase 54 RESULT.md (`.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md`) §2 and §5 to confirm the exact statement and proof shape of lem:peirce-preservation and cor:S4-phi-indep. Cite these by section number in the table's "Phi-indep cite" column (NOT re-derive; NOT re-verify; pure citation).

    5. Read Phase 55 RESULT.md (`.gpd/phases/55-s4-facial-structure-lemma/55-RESULT.md`) §6 to confirm the SymPy Test 3 φ-independence verification under f=λμ. Cite in §3 intro as the computational confirmation (NO new SymPy in this plan).

    6. Read Phase 56 CLOSE entry (`derivations/paper5-peirce-preservation/alfsen-shultz-notes.md:500-533`) verbatim. Do NOT modify this file. Quote the "Phase 57 inheritance note" section into phi-audit.md §3.1 with line-number citation.

    7. Author §3 of phi-audit.md with this structure (REPLACE the §3 placeholder):
       ```
       ## §3. prop:inheritance Deep-Dive (Phase 56 CLOSE primary target)

       ### §3.1 Background and inheritance
       [Quote Phase 56 CLOSE "Phase 57 inheritance note" from alfsen-shultz-notes.md:500-533, naming prop:inheritance (composite-lt.tex:66) as THE primary phi-audit target. Cite Phase 55 SymPy Test 3 as the computational confirmation of cor:S4-phi-indep. Cite Phase 54 lem:peirce-preservation and cor:S4-phi-indep as the re-routing anchors.]

       ### §3.2 Per-line \\varphi classification of prop:inheritance statement + proof + 4 consumer sites
       [Insert the 7-column table populated per Task step 3]

       ### §3.3 Verdict
       [ONE of:]

       **VERDICT A (outcome (B) feasible):** Every prop:inheritance \\varphi usage is re-routable through lem:peirce-preservation or cor:S4-phi-indep. No \\varphi-specific (non-re-routable) step exists. Plan 57-02 may proceed with the full 48-occurrence classification.

       [OR]

       **VERDICT B (outcome pivot to (C-i) required):** The prop:inheritance proof contains a \\varphi-specific step at [exact file:line], using property [name the property], which cannot be re-routed through Phase 54's phi-independent results. Plan 57-02 is HALTED pending `/gpd:discuss-phase 57` — outcome may shift from (B) notation-audit to (C-i) structural revision (mirroring Phase 54 C-i precedent). Estimated additional effort: 1-2 days per 57-RESEARCH §"Alternative Approaches if Primary Fails".

       ### §3.4 Escalation marker (if Verdict B)
       [If Verdict B, write a 3-line escalation marker: (1) exact line + property; (2) note that the consumer sites at appendix-proofs.tex:235, composite-lt.tex:217, type-exclusion.tex:127,246 may inherit the issue; (3) "Plan 57-02 is NOT to be started within this run."]

       ### §3.5 Hand-off to Plan 57-02
       [If Verdict A: "Proceed to Plan 57-02 with the 4-role taxonomy (§1.3) and R8 heuristic (§1.4). prop:inheritance classification rows above (§3.2) pre-populate 4-10 rows of the Plan 57-02 §4 table."]
       [If Verdict B: omit this section; write "See §3.4 — Plan 57-02 blocked."]
       ```

    8. Forbidden-proxy self-check: `grep -i 'purification\|chiribella\|CDP' phi-audit.md` must return zero matches that are NOT inside an explicit rejection phrase. `grep -i 'V_M = V_B\|\\\\varphi = id' phi-audit.md` — if any match exists, verify it is followed within 50 characters by a cor:S4-phi-indep citation or a WLOG-local qualifier.

    Do NOT touch main-jmp-submitted.tex. Do NOT edit any Paper 5 source .tex file (read-only inspection only). Do NOT append to alfsen-shultz-notes.md (Plan 57-03 handles the append).
  </action>
  <verify>
    1. §3 is fully populated (placeholder replaced).
    2. §3.1 contains a verbatim quote from alfsen-shultz-notes.md:500-533 with line-number citation.
    3. §3.2 table has ≥ 7 rows (prop:inheritance statement + proof body ≥ 3 rows; consumer sites ≥ 4 rows at lines 235, 217, 127, 246); every row's 7 columns are populated (no "[?]" or NULL).
    4. §3.3 Verdict is exactly one of {VERDICT A, VERDICT B} (not "VERDICT UNCLEAR" / "partial" / "maybe").
    5. If VERDICT B, §3.4 escalation marker exists with (exact file:line, specific property, block instruction for Plan 57-02).
    6. Keyword-absence sanity checks:
       - `grep -i 'purification\\|chiribella\\|CDP' derivations/paper5-peirce-preservation/phi-audit.md` — if any hits, each is inside an explicit "does NOT use" / "is forbidden" / "NOT invoked" rejection phrase.
       - `grep -E 'V_M = V_B|\\\\varphi = id' derivations/paper5-peirce-preservation/phi-audit.md` — each hit (if any) is followed within 50 characters by `cor:S4-phi-indep` OR the phrase "WLOG" or "local specialization".
    7. `git -C ~/repos/blog status --porcelain landing/papers/qm-from-self-modeling/` returns empty (no blog-repo edits).
    8. `git status --porcelain derivations/paper5-peirce-preservation/alfsen-shultz-notes.md` returns empty (shared artifact untouched in this plan).
  </verify>
  <done>
    phi-audit.md §3 populated with prop:inheritance deep-dive + 7-column per-line classification + unambiguous VERDICT A or VERDICT B. If VERDICT A, Plan 57-02 is unblocked (hand-off note in §3.5); if VERDICT B, Plan 57-02 is explicitly blocked (§3.4 escalation marker) and an outcome-pivot marker is committed. No revision text, no classification beyond prop:inheritance + consumers (Plan 57-02 scope), no Paper 5 source edits, no alfsen-shultz-notes.md edits. (Rejects all four forbidden proxies: R8 single-fix — no revision text; Chiribella purification — explicit keyword-absence check; global-identity assumption — every \\varphi = id has adjacent cor:S4-phi-indep or WLOG qualifier; rename-without-classification — only prop:inheritance rows classified, not renames proposed.)
  </done>
</task>

<task type="auto">
  <name>Task 4: Commit Plan 57-01 deliverable via gpd commit</name>
  <files>derivations/paper5-peirce-preservation/phi-audit.md</files>
  <action>
    Stage and commit the Plan 57-01 deliverable (phi-audit.md §1-§3) via gpd commit.

    1. Verify the plan delivered exactly one new file:
       ```
       git status --porcelain
       ```
       Expected: exactly one new file `derivations/paper5-peirce-preservation/phi-audit.md` (Untracked: `??`). If any other file is modified or untracked, STOP and investigate.

    2. Add the file to tracking with git add -f (per project instruction feedback_gpd_gitignore.md — .gpd/phases is git-aware but derivations/ may inherit broader .gitignore patterns):
       ```
       git add derivations/paper5-peirce-preservation/phi-audit.md
       ```

    3. Commit using gpd commit with a single-sentence message per user coding style:
       ```
       /Users/ehrlich/.gpd/venv/bin/python -m gpd.runtime_cli --runtime claude-code --config-dir ./.claude --install-scope local commit "phase(57-01): establish phi-audit evidentiary baseline + prop:inheritance deep-dive verdict" --files derivations/paper5-peirce-preservation/phi-audit.md
       ```

    4. Verify the commit landed:
       ```
       git log --oneline -1
       git status
       ```
       Expected: clean working tree; latest commit message matches; no co-author tags added (per user global instruction).

    5. Do NOT edit STATE.md or ROADMAP.md in this plan (Plan 57-03 handles phase close). Do NOT push to any remote. Do NOT run latexdiff or tectonic.
  </action>
  <verify>
    1. `git log --oneline -1` shows the phase(57-01) commit.
    2. `git status --porcelain` returns empty.
    3. `git show --stat HEAD` lists exactly one file: `derivations/paper5-peirce-preservation/phi-audit.md`.
    4. Commit message is a single sentence with no co-author tag, no emoji, no multi-line body.
    5. `git -C ~/repos/blog status --porcelain` returns empty (blog repo untouched).
  </verify>
  <done>
    Plan 57-01 deliverable committed as a single atomic commit. phi-audit.md §1-§3 lives in the repo; §4-§9 remain placeholders for Plans 57-02/03. Working tree is clean. (Rejects R8 single-fix forbidden proxy: only §1-§3 committed, which is pure evidence + deep-dive, not revision; rejects frozen-file-edit forbidden proxy: blog repo untouched per §4 verify.)
  </done>
</task>

</tasks>

<verification>
Plan-level checks across all 4 tasks:

- **Append-only shared-artifact discipline:** `git status --porcelain derivations/paper5-peirce-preservation/alfsen-shultz-notes.md` returns empty throughout and at plan close (this plan READS but does NOT WRITE the shared artifact; Plan 57-03 appends).
- **Frozen-file zero-diff:** `git -C ~/repos/blog diff --stat paper5-jmp-submitted -- landing/papers/qm-from-self-modeling/main-jmp-submitted.tex` returns "0 files changed" throughout and at plan close.
- **No Paper 5 source edits:** `git -C ~/repos/blog status --porcelain landing/papers/qm-from-self-modeling/` returns empty throughout (Plan 57-01 is a meta-notation plan, not a paper-editing plan).
- **No new SymPy:** grep -ri 'sympy' derivations/paper5-peirce-preservation/phi-audit.md returns zero hits (Phase 55 Test 3 is CITED, not RE-EXECUTED).
- **Forbidden proxies explicitly rejected:** §1.5 enumerates all 4 forbidden proxies; keyword-absence checks in Tasks 2-3 verify no silent invocation.
- **Outcome gate enforced:** If Task 3 produces VERDICT B, the plan returns a clean deliverable with the escalation marker, and the ORCHESTRATOR is the one that decides to halt Plan 57-02 (this plan still closes cleanly with the verdict; downstream gating is not this plan's responsibility).
- **Contract coverage:** Claims claim-grep-baseline, claim-prop-inheritance-phi-classification, claim-derv-00-01-phase57-slice-plan01 all have at least one acceptance test each; every test has evidence_required listing deliverables + references; forbidden proxies and uncertainty markers are populated.
- **R8 audit-ordering invariant (57-RESEARCH §R8):** Classification table is NOT produced in this plan (Plan 57-02 scope). Only prop:inheritance-scoped rows (§3.2) are classified — justified by the Phase 56 CLOSE designation of prop:inheritance as THE primary audit target, which must be resolved BEFORE full classification begins.
</verification>

<success_criteria>
- phi-audit.md exists at derivations/paper5-peirce-preservation/phi-audit.md with §1 (methodology), §2 (raw grep baseline), §3 (prop:inheritance deep-dive + verdict), and placeholders for §4-§9.
- Per-file \\varphi counts match research preflight (48 total; 16+2+7+2+7+13+1+0 per file).
- prop:inheritance verdict is unambiguous (A or B, not partial).
- If VERDICT A: Plan 57-02 is unblocked with a pre-populated chunk of its future §4 table (the prop:inheritance rows).
- If VERDICT B: Plan 57-02 is explicitly blocked with an escalation marker naming the exact file:line and property, ready for `/gpd:discuss-phase 57` human decision.
- Single git commit via gpd commit with single-sentence message and no co-author tag.
- All four forbidden proxies explicitly rejected in prose AND verified by keyword-absence checks.
- No modifications to: main-jmp-submitted.tex (frozen), Paper 5 living .tex files (Plan 57-03 edit target), alfsen-shultz-notes.md (Plan 57-03 append target).
</success_criteria>

<output>
After completion, create `.gpd/phases/57-phi-inert-wrapper-resolution/57-01-SUMMARY.md` (per template `./.claude/get-physics-done/templates/summary.md`).
</output>
