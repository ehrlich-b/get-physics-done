---
phase: 57-phi-inert-wrapper-resolution
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - derivations/paper5-peirce-preservation/phi-audit.md
interactive: true
researcher_setup: []

context_note: "Classification iterates over 48 occurrences; per-occurrence entries live in the phi-audit.md §4 table, NOT inlined into the plan body. This plan's atomic units are per-file batches (7 batches × ~7 rows each), plus 3 audit tasks (\\varphi=id specializations, R8 equivocation detection, split-vs-standing pivot) + 1 decision checkpoint. Total: 4 tasks, but Task 1 is the large one — treat as one atomic deliverable with sub-batches enumerated in the action."

conventions:
  units: "N/A (notation audit)"
  metric: "N/A"
  coordinates: "N/A"
  tracking_macro: "\\varphi (single existing macro)"
  four_role_taxonomy: "(a) self-modeling tracking B→M load-bearing; (b) inert wrapper/ancilla — likely empty; (c) state-preparation — likely empty; (d) exposition shorthand phi-independent — expected dominant"
  r8_heuristic: "within-subsection role-shift without reintroduction; role-(a)↔role-(d) adjacency without subsection boundary; missing/stale phi-independence cite"
  append_only_shared_artifact: "alfsen-shultz-notes.md mutations = append only (Plan 57-03; NOT this plan)"
  frozen_submission_tag: "paper5-jmp-submitted (NEVER edit main-jmp-submitted.tex)"

dimensional_check:
  classification_row_count: "[dimensionless; expected = 48 (reconciled with Plan 57-01 §2 grand total)]"
  r8_equivocation_site_count: "[dimensionless; predicted 3-6 per 57-RESEARCH §Open Questions Q1]"
  phi_id_wlog_count: "[dimensionless; predicted 4 per 57-RESEARCH §Summary — main.tex:92, 407, 410; discussion.tex:249]"
  role_a_count: "[dimensionless; predicted 8-12]"
  role_b_count: "[dimensionless; predicted 0]"
  role_c_count: "[dimensionless; predicted 0]"
  role_d_count: "[dimensionless; predicted 30-40]"
  sections_affected_count: "[dimensionless; drives split-vs-standing pivot: > 5 ⇒ Approach 1 standing-def]"

approximations:
  - name: "48-occurrence classification exhaustiveness"
    parameter: "Every \\varphi occurrence enumerated in Plan 57-01 §2 is classified with 5 columns (role, minimal property, phi-indep cite if (d), R8 flag, within-subsection context)"
    validity: "100% classification rate targeted; research caveat allows ≤ 2 'author-judgment needed' residuals (57-RESEARCH §Validation Strategies row 'Role-coverage exhaustiveness')"
    breaks_when: "≥ 3 rows with no defensible classification — indicates taxonomy is under-specified and escalation required"
    check: "Plan 57-02 Task 2 explicit residual itemization; if residuals > 2, HALT and return for /gpd:discuss-phase 57"

  - name: "\\varphi = id specialization adjacency test"
    parameter: "Every \\varphi = id occurrence cites cor:S4-phi-indep (or lem:peirce-preservation, or equivalent) in the SAME paragraph"
    validity: "Strict adjacency = role (d) clean; non-adjacent = R8 equivocation flag"
    breaks_when: "A \\varphi = id occurrence has no phi-independence cite anywhere in the surrounding proof/remark — this is the highest-probability bug site per 57-RESEARCH §Open Questions Q4"
    check: "Task 2 dedicated \\varphi = id audit subtask; 4 expected occurrences per 57-RESEARCH (main.tex:92, 407, 410; discussion.tex:249)"

  - name: "Diff-cost pivot threshold"
    parameter: "Count distinct sections requiring revision-text edits (boxed def insertion, role qualifier insertion, or split-notation search-and-replace)"
    validity: "> 5 distinct sections ⇒ Approach 1 (standing-def) minimizes JMP-referee surface; ≤ 5 ⇒ either approach viable"
    breaks_when: "Author explicitly prefers macro-level clarity even at larger diff-cost — checkpoint:decision task surfaces this tradeoff"
    check: "Task 3 produces numeric section-count estimate BEFORE drafts are authored in Plan 57-03"

contract:
  scope:
    question: "Given the prop:inheritance pivot verdict (Plan 57-01 §3.3) and the 48-occurrence grep baseline (Plan 57-01 §2), what is the complete role classification of every Paper 5 \\varphi occurrence, what is the exhaustive list of R8 equivocation sites (including \\varphi = id WLOG specializations without adjacent cor:S4-phi-indep cite), and which revision approach — Approach 1 (standing-def + qualifier) or Approach 2 (split notation) — does the diff-cost pivot rule recommend?"
  claims:
    - id: "claim-full-classification"
      statement: "Every one of the 48 Paper 5 \\varphi occurrences enumerated in Plan 57-01 §2 is classified into exactly one of {a, b, c, d} with a populated 5-column table row (role, minimal property of \\varphi used, phi-independence cite if role (d), R8 flag, subsection context); ≤ 2 rows may be flagged 'author-judgment needed' per 57-RESEARCH §Level of Rigor (DERV-57-01 completeness requirement)."
      deliverables: ["deliv-phi-audit-s4"]
      acceptance_tests: ["test-classification-exhaustiveness", "test-grep-count-reconciliation-full"]
      references: ["ref-plan-57-01-output", "ref-paper5-living", "ref-phase54-result", "ref-phase55-result", "ref-pitfalls-r8"]
    - id: "claim-wlog-phi-id-audit"
      statement: "Every \\varphi = id WLOG specialization in Paper 5 (predicted 4: main.tex:92, 407, 410; discussion.tex:249) is audited for adjacent cor:S4-phi-indep (or lem:peirce-preservation) citation; any specialization without adjacent citation is flagged as R8 equivocation (DERV-57-02; 57-RESEARCH §Open Questions Q4)."
      deliverables: ["deliv-phi-audit-s4-wlog-subsection"]
      acceptance_tests: ["test-wlog-adjacency"]
      references: ["ref-paper5-living", "ref-phase54-result"]
    - id: "claim-r8-equivocation-roster"
      statement: "An exhaustive list of R8 equivocation sites is produced by applying the three-clause R8 heuristic (within-subsection role-shift without reintroduction; role-(a)↔role-(d) adjacency without subsection boundary; missing/stale phi-independence cite) to the full 48-row classification; predicted 3-6 sites (57-RESEARCH §Open Questions Q1) with at minimum the four pre-identified hypothesis sites examined: rem:phi-inert-objection scope (discussion.tex:471-487), §3.4 mixing-function-selection block (main.tex:~696), §5 local-tomography with \\varphi^{-1} (composite-lt.tex:131-157), and the Plan 57-01 §3 prop:inheritance chain (DERV-57-02)."
      deliverables: ["deliv-phi-audit-s4-r8-subsection"]
      acceptance_tests: ["test-r8-heuristic-applied", "test-rem-phi-inert-objection-global-soundness"]
      references: ["ref-paper5-living", "ref-plan-57-01-output", "ref-pitfalls-r8"]
    - id: "claim-pivot-recommendation"
      statement: "The split-vs-standing-definition pivot recommendation is produced with a numeric estimate of distinct sections affected (affected-section-count), and the recommendation follows the diff-cost rule: > 5 ⇒ Approach 1 (standing-def); ≤ 5 ⇒ either viable, checkpoint:decision presents tradeoffs to user. The recommendation is committed BEFORE Plan 57-03 authors any draft, preserving the backtracking gate."
      deliverables: ["deliv-phi-audit-s7-pivot"]
      acceptance_tests: ["test-pivot-numeric-estimate", "test-checkpoint-decision-presented"]
      references: ["ref-roadmap-p57-backtracking", "ref-research-approach-1", "ref-research-approach-2"]
    - id: "claim-derv-00-01-phase57-slice-plan02"
      statement: "Plan 57-02 preserves append-only shared-artifact discipline (alfsen-shultz-notes.md NOT modified), frozen-file zero-diff (main-jmp-submitted.tex NOT opened), no Paper 5 source edits (all classification is meta; edits happen in Plan 57-03), and the four forbidden proxies are explicitly not invoked (DERV-00-01 Phase 57 slice, Plan 57-02 portion)."
      deliverables: ["deliv-phi-audit-s4", "deliv-phi-audit-s4-wlog-subsection", "deliv-phi-audit-s4-r8-subsection", "deliv-phi-audit-s7-pivot"]
      acceptance_tests: ["test-forbidden-proxy-absence-plan02", "test-frozen-file-untouched-plan02"]
      references: ["ref-roadmap-p57-forbidden", "ref-pitfalls-r8"]
  deliverables:
    - id: "deliv-phi-audit-s4"
      kind: "audit-document"
      path: "derivations/paper5-peirce-preservation/phi-audit.md"
      description: "phi-audit.md §4 Per-Occurrence Classification Table: 48-row table with columns (#, file:line, local text excerpt, role (a/b/c/d), minimal property of \\varphi used, phi-independence cite if role (d), R8 equivocation flag)."
      must_contain:
        - "## §4. Per-Occurrence Classification Table"
        - "| # | File:line |"
        - "main.tex:"
        - "composite-lt.tex:"
        - "appendix-proofs.tex:"
        - "discussion.tex:"
        - "48 rows"
    - id: "deliv-phi-audit-s4-wlog-subsection"
      kind: "audit-document"
      path: "derivations/paper5-peirce-preservation/phi-audit.md"
      description: "phi-audit.md §4.A \\varphi = id WLOG Specialization Audit: dedicated subsection listing every \\varphi = id occurrence (predicted main.tex:92, 407, 410; discussion.tex:249) with adjacent-citation verdict per occurrence."
      must_contain:
        - "## §4.A \\\\varphi = id WLOG Specialization Audit"
        - "main.tex:92"
        - "main.tex:407"
        - "main.tex:410"
        - "discussion.tex:249"
        - "adjacent cite"
    - id: "deliv-phi-audit-s4-r8-subsection"
      kind: "audit-document"
      path: "derivations/paper5-peirce-preservation/phi-audit.md"
      description: "phi-audit.md §4.B R8 Equivocation Roster: bullet list of every R8 equivocation site detected with (file:line range, heuristic clause triggered, remediation hint for Plan 57-03)."
      must_contain:
        - "## §4.B R8 Equivocation Roster"
        - "discussion.tex:471"
        - "rem:phi-inert-objection"
    - id: "deliv-phi-audit-s7-pivot"
      kind: "audit-document"
      path: "derivations/paper5-peirce-preservation/phi-audit.md"
      description: "phi-audit.md §7 Author Decision Pivot: numeric estimate of affected-section-count + diff-cost comparison + explicit recommendation (Approach 1 standing-def vs Approach 2 split-notation); committed BEFORE Plan 57-03 drafts authored."
      must_contain:
        - "## §7. Author Decision Pivot"
        - "affected sections"
        - "Recommendation:"
  references:
    - id: "ref-plan-57-01-output"
      kind: "prior-artifact"
      locator: "derivations/paper5-peirce-preservation/phi-audit.md §1-§3 (Plan 57-01 output)"
      role: "plan-input"
      why_it_matters: "§1 methodology + §2 grep baseline + §3 prop:inheritance verdict are the direct inputs for Plan 57-02; grep rows populate §4 table; prop:inheritance rows pre-populate 4-10 §4 table rows; §3 verdict determines whether §4 proceeds at all (VERDICT A required)."
      applies_to: ["claim-full-classification", "claim-r8-equivocation-roster"]
      must_surface: true
      required_actions: ["read", "consume-grep-rows", "pre-populate-table"]
    - id: "ref-paper5-living"
      kind: "prior-artifact"
      locator: "~/repos/blog/landing/papers/qm-from-self-modeling/main.tex + sections/*.tex"
      role: "classification-source"
      why_it_matters: "Source text for the 'Local text excerpt' column of §4 table; read-only inspection for Plan 57-02."
      applies_to: ["claim-full-classification", "claim-wlog-phi-id-audit", "claim-r8-equivocation-roster"]
      must_surface: true
      required_actions: ["read-with-line-numbers", "quote-verbatim-excerpt"]
    - id: "ref-phase54-result"
      kind: "prior-artifact"
      locator: ".gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md"
      role: "phi-independence-cite-target"
      why_it_matters: "cor:S4-phi-indep + lem:peirce-preservation are the target citations for every role-(d) classification's 'Phi-indep cite' column; also the target for \\varphi = id adjacency audit."
      applies_to: ["claim-full-classification", "claim-wlog-phi-id-audit"]
      must_surface: true
      required_actions: ["read", "cite-in-table-column"]
    - id: "ref-phase55-result"
      kind: "prior-artifact"
      locator: ".gpd/phases/55-s4-facial-structure-lemma/55-RESULT.md"
      role: "phi-independence-cite-target"
      why_it_matters: "rem:S4-phi-independent (axiom-verification.tex:179-182) is the reader-facing paraphrase; cite alongside cor:S4-phi-indep where appropriate."
      applies_to: ["claim-full-classification"]
      must_surface: true
      required_actions: ["read", "cite"]
    - id: "ref-pitfalls-r8"
      kind: "prior-artifact"
      locator: ".gpd/research/PITFALLS.md:245-273 (§R8)"
      role: "heuristic-source"
      why_it_matters: "R8 equivocation detection methodology; Plan 57-02 applies the three-clause heuristic."
      applies_to: ["claim-r8-equivocation-roster", "claim-derv-00-01-phase57-slice-plan02"]
      must_surface: true
      required_actions: ["read", "apply-heuristic"]
    - id: "ref-roadmap-p57-backtracking"
      kind: "prior-artifact"
      locator: ".gpd/ROADMAP.md:785 (Phase 57 Backtracking rule)"
      role: "pivot-rule-source"
      why_it_matters: "'If the split-notation approach forces renaming in > 5 sections, prefer the standing-definition approach to minimize LaTeX diff.' Plan 57-02 Task 3 enforces this numerically."
      applies_to: ["claim-pivot-recommendation"]
      must_surface: true
      required_actions: ["read", "apply-rule"]
    - id: "ref-research-approach-1"
      kind: "prior-artifact"
      locator: ".gpd/phases/57-phi-inert-wrapper-resolution/57-RESEARCH.md §'Approach 1: Standing Definition + Per-Section Role Qualifier'"
      role: "revision-approach-spec"
      why_it_matters: "Defines Approach 1 with diff-cost envelope (≈ 6 qualifiers + 1 remark; ~30 min); Task 3 pivot recommendation cites this for diff-cost comparison."
      applies_to: ["claim-pivot-recommendation"]
      must_surface: true
      required_actions: ["read", "cite", "quote-diff-cost"]
    - id: "ref-research-approach-2"
      kind: "prior-artifact"
      locator: ".gpd/phases/57-phi-inert-wrapper-resolution/57-RESEARCH.md §'Approach 2: Split Notation'"
      role: "revision-approach-spec"
      why_it_matters: "Defines Approach 2 with diff-cost envelope (≈ 48 search-and-replace + preamble edit; 1-2 hours); Task 3 pivot recommendation cites this for diff-cost comparison."
      applies_to: ["claim-pivot-recommendation"]
      must_surface: true
      required_actions: ["read", "cite", "quote-diff-cost"]
    - id: "ref-roadmap-p57-forbidden"
      kind: "prior-artifact"
      locator: ".gpd/ROADMAP.md:775 (Phase 57 Forbidden proxies list)"
      role: "forbidden-proxy-specifier"
      why_it_matters: "Plan 57-02 must explicitly reject all four forbidden proxies in §4/§7 prose."
      applies_to: ["claim-derv-00-01-phase57-slice-plan02"]
      must_surface: true
      required_actions: ["read", "reject-in-prose"]
  acceptance_tests:
    - id: "test-classification-exhaustiveness"
      subject: "claim-full-classification"
      kind: "row-count-audit"
      procedure: "Count rows in phi-audit.md §4 classification table (excluding header). Compare to Plan 57-01 §2 grand total (expected 48). Verify every row has all 7 columns populated (no blank or '[?]' entries) except for the 'Phi-indep cite if role (d)' column which is naturally blank for roles (a)/(b)/(c). Count 'author-judgment needed' flags; verify ≤ 2 per 57-RESEARCH §Validation row 'Role-coverage exhaustiveness'."
      pass_condition: "§4 table has exactly 48 rows; 7 columns populated per row (with the role-(d) cite column valid blank for non-(d) roles); ≤ 2 'author-judgment needed' flags."
      evidence_required: ["deliv-phi-audit-s4", "ref-plan-57-01-output"]
    - id: "test-grep-count-reconciliation-full"
      subject: "claim-full-classification"
      kind: "per-file-count-match"
      procedure: "Sum §4 rows by file. Compare per-file count to Plan 57-01 §2.1 per-file baseline (main.tex:16, appendix-proofs.tex:7, axiom-verification.tex:2, composite-lt.tex:7, discussion.tex:13, type-exclusion.tex:1, appendix-numerical.tex:2)."
      pass_condition: "Per-file row counts MATCH §2.1 baseline exactly. Any mismatch indicates a missed or duplicated occurrence — must be reconciled before §4 is considered complete."
      evidence_required: ["deliv-phi-audit-s4", "ref-plan-57-01-output"]
    - id: "test-wlog-adjacency"
      subject: "claim-wlog-phi-id-audit"
      kind: "citation-adjacency"
      procedure: "For each \\varphi = id occurrence listed in §4.A (predicted 4 at main.tex:92, 407, 410; discussion.tex:249): read ±15 lines of context in the source .tex file; verify whether cor:S4-phi-indep, lem:peirce-preservation, or rem:S4-phi-independent appears in the same paragraph. Flag any non-adjacent occurrence as 'R8 equivocation — missing phi-independence cite'."
      pass_condition: "§4.A lists each \\varphi = id occurrence with its 'adjacent cite?' verdict (YES/NO + cite name if YES); every NO verdict is added to §4.B R8 equivocation roster."
      evidence_required: ["deliv-phi-audit-s4-wlog-subsection", "ref-paper5-living", "ref-phase54-result"]
    - id: "test-r8-heuristic-applied"
      subject: "claim-r8-equivocation-roster"
      kind: "heuristic-application-audit"
      procedure: "For each of the 4 pre-identified hypothesis sites (rem:phi-inert-objection at discussion.tex:471-487; §3.4 mixing-function at main.tex:~696; §5 \\varphi^{-1} at composite-lt.tex:131-157; prop:inheritance chain from Plan 57-01 §3), apply the three-clause R8 heuristic and record the verdict in §4.B. Additionally, scan the §4 table for any adjacent-row pairs (within-subsection) where 'minimal property' columns differ AND no reintroduction occurs — flag these as newly-discovered equivocation sites."
      pass_condition: "§4.B contains at minimum 4 verdict entries (for the 4 hypothesis sites, each marked CONFIRMED / NOT-CONFIRMED / DOWNGRADED-TO-ROLE-(d)); §4.B also lists any newly-discovered equivocation sites from the table scan."
      evidence_required: ["deliv-phi-audit-s4-r8-subsection", "deliv-phi-audit-s4", "ref-pitfalls-r8"]
    - id: "test-rem-phi-inert-objection-global-soundness"
      subject: "claim-r8-equivocation-roster"
      kind: "global-scope-check"
      procedure: "Given the role-(a) roster (§4 rows classified (a)), verify whether rem:phi-inert-objection's claim '\\varphi does not appear as a free variable' is globally sound. If role (a) includes ANY occurrence that appears in the proof of the main theorem (\\ref{thm:main} at main.tex:~84, via §sec:sp + §sec:self-modeling), the remark's scope is overclaimed and §4.B flags a required revision."
      pass_condition: "§4.B contains an explicit 'rem:phi-inert-objection scope verdict': (a) SURVIVES if no role-(a) occurrence enters the main-theorem proof; (b) NARROW-TO-STATEMENT-LEVEL if role-(a) occurrences do enter the proof chain, with specific line references identifying which."
      evidence_required: ["deliv-phi-audit-s4-r8-subsection", "deliv-phi-audit-s4"]
    - id: "test-pivot-numeric-estimate"
      subject: "claim-pivot-recommendation"
      kind: "numeric-estimate"
      procedure: "Count distinct sections/subsections affected by Approach 1 revision: every subsection containing a role-(d) first-occurrence (needing a qualifier) + the subsection containing Def. def:self-modeling-system (needing a standing definition). Also count sections affected by Approach 2: every subsection containing any \\varphi occurrence (search-and-replace scope). Commit both counts in §7."
      pass_condition: "§7 contains: (i) Approach 1 affected-section count (numeric); (ii) Approach 2 affected-section count (numeric); (iii) explicit recommendation following the backtracking rule (>5 Approach-1-affected ⇒ Approach 1; ≤5 ⇒ both viable, checkpoint:decision presents tradeoffs)."
      evidence_required: ["deliv-phi-audit-s7-pivot", "ref-roadmap-p57-backtracking", "ref-research-approach-1", "ref-research-approach-2"]
    - id: "test-checkpoint-decision-presented"
      subject: "claim-pivot-recommendation"
      kind: "checkpoint-artifact"
      procedure: "Task 4 checkpoint:decision presents the §7 recommendation with both options (Approach 1 pros/cons, Approach 2 pros/cons, affected-section counts, JMP-referee surface considerations). User response (option-a OR option-b) is captured and committed to §7 as 'User decision: Approach N' before plan completes."
      pass_condition: "§7 ends with an explicit 'User decision: Approach [1|2]' line; Plan 57-03 inherits this decision as its primary-draft indicator (both drafts still required per DERV-57-03 but the user-chosen one is the integration target)."
      evidence_required: ["deliv-phi-audit-s7-pivot"]
    - id: "test-forbidden-proxy-absence-plan02"
      subject: "claim-derv-00-01-phase57-slice-plan02"
      kind: "keyword-absence"
      procedure: "grep -ni 'purification\\|chiribella\\|CDP\\|ancilla' phi-audit.md §4-§7 portions; grep -E 'V_M = V_B|\\\\varphi = id' phi-audit.md §4-§7; for every hit, verify the prose is explicit rejection OR the \\varphi = id occurrence has adjacent cor:S4-phi-indep cite with WLOG qualifier."
      pass_condition: "Zero forbidden-proxy invocations in §4-§7 (rejections OK; flagged equivocations OK if marked R8)."
      evidence_required: ["deliv-phi-audit-s4", "deliv-phi-audit-s4-wlog-subsection", "deliv-phi-audit-s4-r8-subsection", "deliv-phi-audit-s7-pivot"]
    - id: "test-frozen-file-untouched-plan02"
      subject: "claim-derv-00-01-phase57-slice-plan02"
      kind: "file-state-assertion"
      procedure: "git -C ~/repos/blog diff --stat paper5-jmp-submitted -- landing/papers/qm-from-self-modeling/main-jmp-submitted.tex; git -C ~/repos/blog status --porcelain landing/papers/qm-from-self-modeling/; git status --porcelain derivations/paper5-peirce-preservation/alfsen-shultz-notes.md."
      pass_condition: "All three commands return empty / zero-diff output (frozen file untouched; blog repo untouched; shared artifact untouched)."
      evidence_required: []
  forbidden_proxies:
    - id: "fp-r8-single-fix-plan02"
      subject: "claim-full-classification"
      proxy: "Starting revision text authoring within Plan 57-02 (e.g., drafting a role qualifier for a single section while the classification table is partial)"
      reason: "Plan 57-02 produces classification + pivot recommendation; Plan 57-03 authors drafts. Mixing the two violates R8 ordering invariant."
    - id: "fp-chiribella-purification-plan02"
      subject: "claim-full-classification"
      proxy: "Classifying any occurrence as role (c) 'ancilla' or role (b) 'inert wrapper' by invoking purification or CDP 2011"
      reason: "Paper 5 grep for 'ancilla|wrapper' returns zero (57-RESEARCH §Missing anchors); role (b)/(c) classifications must come from OPERATIONAL criteria, not from importing heavier purification assumptions."
    - id: "fp-global-identity-plan02"
      subject: "claim-wlog-phi-id-audit"
      proxy: "Classifying a \\varphi = id occurrence as role (d) 'harmless' without checking paragraph-adjacency of a phi-independence cite"
      reason: "Global-identity assumption is a forbidden proxy (ROADMAP.md:775); the adjacency test is specifically what distinguishes safe role-(d) from R8 equivocation (57-RESEARCH Q4)."
    - id: "fp-rename-without-classification-plan02"
      subject: "claim-pivot-recommendation"
      proxy: "Recommending Approach 2 (split-notation) without committing the classification table first"
      reason: "The classification table IS the rename rule; Approach 2 without a populated table produces macro-choice guesswork (57-RESEARCH §Pitfalls R8d)."
    - id: "fp-rederive-phi-indep-plan02"
      subject: "claim-full-classification"
      proxy: "Running new SymPy tests or re-proving cor:S4-phi-indep inside §4 justifications"
      reason: "Phase 54 sealed the corollary; Phase 55 SymPy-verified; Plan 57-02 cites only (57-RESEARCH §Pitfalls R8e)."
  links:
    - id: "link-grep-to-classification"
      source: "deliv-phi-audit-s4"
      target: "claim-full-classification"
      relation: "establishes"
      verified_by: ["test-classification-exhaustiveness", "test-grep-count-reconciliation-full"]
    - id: "link-classification-to-r8"
      source: "deliv-phi-audit-s4"
      target: "claim-r8-equivocation-roster"
      relation: "enables"
      verified_by: ["test-r8-heuristic-applied", "test-rem-phi-inert-objection-global-soundness"]
    - id: "link-r8-to-pivot"
      source: "claim-r8-equivocation-roster"
      target: "claim-pivot-recommendation"
      relation: "informs"
      verified_by: ["test-pivot-numeric-estimate"]
    - id: "link-pivot-to-plan-57-03"
      source: "deliv-phi-audit-s7-pivot"
      target: "claim-pivot-recommendation"
      relation: "gates"
      verified_by: ["test-checkpoint-decision-presented"]
  uncertainty_markers:
    weakest_anchors:
      - "'Minimal property' column judgments (subjective for ambiguous rows — e.g., is \\varphi in Def. def:self-modeling-system 'full definition' or 'order iso'? Both true; mitigation: ≤ 2 rows flagged 'author-judgment needed')"
      - "Whether role (b) 'inert wrapper' is truly empty (preflight predicts zero, but classification may surface a role-(b) candidate in discussion.tex; if role (b) count > 0, Approach 2 macro set expands)"
      - "rem:phi-inert-objection scope verdict (test-rem-phi-inert-objection-global-soundness may force revision of the remark itself, which is a separate Plan 57-03 revision-text item)"
    disconfirming_observations:
      - "Role (a) count > ~20 occurrences — signals \\varphi is load-bearing in many more places than pre-audit predicted; outcome may shift from (B) notation-audit to (C) structural (57-RESEARCH §Alternative Approaches)"
      - "≥ 3 rows flagged 'author-judgment needed' — taxonomy may be under-specified; escalate via /gpd:discuss-phase 57"
      - "§4.B R8 roster contains the same \\varphi = id specialization in both §4.A and §4.B with contradictory verdicts — internal-consistency failure; halt and reconcile before Task 3 pivot"
      - "rem:phi-inert-objection scope verdict = NARROW-TO-STATEMENT-LEVEL AND user chooses Approach 2 — Approach 2 alone cannot fix the remark's text; Plan 57-03 must author a remark-revision regardless of split-vs-standing"
---

<objective>
Produce the complete 48-occurrence \\varphi role classification, exhaustive R8 equivocation roster (including \\varphi = id WLOG specialization audit), and the split-vs-standing-definition pivot recommendation — in that order — so Plan 57-03 can author the two revision drafts with a pre-committed rename rule and user-approved primary approach.

Purpose: DERV-57-01 (classification completeness) and DERV-57-02 (equivocation-site identification) require this plan's full table and R8 roster. The ROADMAP backtracking rule ("> 5 sections ⇒ Approach 1 standing-def") can only be applied after counting affected sections, and the count can only be produced after the classification table is committed. 57-RESEARCH Q1 predicts 3-6 equivocation sites; Q4 predicts the \\varphi = id audit as the highest-probability bug location. Plan 57-02 is where both are resolved.

Precondition: Plan 57-01 Task 3 must have committed VERDICT A ("Outcome (B) feasible — prop:inheritance re-routable"). If Plan 57-01 committed VERDICT B (escalation required), Plan 57-02 MUST NOT START; re-planning via /gpd:discuss-phase 57 is required first. Task 1 verifies this precondition before iterating.

Output:
- derivations/paper5-peirce-preservation/phi-audit.md §4 (48-row classification table)
- derivations/paper5-peirce-preservation/phi-audit.md §4.A (\\varphi = id WLOG specialization audit)
- derivations/paper5-peirce-preservation/phi-audit.md §4.B (R8 equivocation roster)
- derivations/paper5-peirce-preservation/phi-audit.md §7 (pivot recommendation + user decision captured)
- Commit via gpd commit

Boundary: NO revision drafts (Plan 57-03); NO edits to any Paper 5 .tex file (Plan 57-03); NO append to alfsen-shultz-notes.md (Plan 57-03); NO new SymPy; NO re-derivation of Phase 54/55 results.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/REQUIREMENTS.md
@.gpd/phases/57-phi-inert-wrapper-resolution/57-RESEARCH.md
@.gpd/phases/57-phi-inert-wrapper-resolution/57-01-PLAN.md
@.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md
@.gpd/phases/55-s4-facial-structure-lemma/55-RESULT.md
@derivations/paper5-peirce-preservation/phi-audit.md
@derivations/paper5-peirce-preservation/alfsen-shultz-notes.md
@.gpd/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Plan 57-01 VERDICT A precondition and populate §4 classification table (48 rows, 7 per-file batches)</name>
  <files>derivations/paper5-peirce-preservation/phi-audit.md</files>
  <action>
    **Precondition check:** Read phi-audit.md §3.3 Verdict. If the verdict is VERDICT B (outcome pivot required), STOP immediately and write a §4 placeholder stating "Plan 57-02 halted pending /gpd:discuss-phase 57 — see §3.4 escalation marker." Skip to Task 4 commit (committing the halt marker). Do NOT populate §4/§4.A/§4.B/§7. If the verdict is VERDICT A, proceed.

    **Populate §4 classification table:**

    1. Open phi-audit.md and replace the §4 placeholder with:
       ```
       ## §4. Per-Occurrence Classification Table

       **Source:** §2 grep baseline (48 total \\varphi rows); Plan 57-01 §3.2 pre-populates 7-10 rows for prop:inheritance statement + proof + consumers.

       **Table structure:**

       | # | File:line | Local text excerpt (±1 line) | Role (a/b/c/d) | Minimal property of \\varphi used | Phi-indep cite if role (d) | R8 equivocation flag | Subsection context |
       |---|-----------|------------------------------|----------------|------------------------------------|-----------------------------|----------------------|---------------------|
       ```

    2. Iterate through the 7 per-file batches (classify one file at a time to preserve context budget — do NOT re-read the whole paper for each row):

       **Batch 2.1 — main.tex (16 rows):**
       - Read main.tex line-by-line at each \\varphi occurrence (use the §2 line numbers); for each, capture ±1 line of local text, classify into (a)/(b)/(c)/(d), fill "minimal property" and "phi-indep cite" columns, apply R8 heuristic.
       - Expected: role (a) cluster at Def. def:self-modeling-system (main.tex:330, 343, 349); role (d) cluster at main.tex:92, 407, 410 (\\varphi = id specializations — adjacency test in Task 2); role (a) cluster at §3.4 mixing-function (main.tex:~696+); role (a) or (d) at main.tex:400, 792 (tracking-property statements).

       **Batch 2.2 — sections/appendix-proofs.tex (7 rows):**
       - Expected: role (a) at \\varphi^{-1} in trace form (appendix-proofs.tex:186-200); role (d) at phi-independence theorem citations (appendix-proofs.tex:137+ contains cor:S4-phi-indep statement).

       **Batch 2.3 — sections/axiom-verification.tex (2 rows):**
       - Expected: role (d) at rem:S4-phi-independent (axiom-verification.tex:179-182).

       **Batch 2.4 — sections/composite-lt.tex (7 rows):**
       - Expected: role (a) at \\varphi^{-1} trace form (composite-lt.tex:131-157); prop:inheritance rows already classified in Plan 57-01 §3.2 — COPY those rows into §4 with row numbers adjusted, add source note "(from Plan 57-01 §3.2 — prop:inheritance deep-dive)".

       **Batch 2.5 — sections/discussion.tex (13 rows):**
       - Expected: role (a) or (d) at rem:phi-inert-objection (discussion.tex:471-487); \\varphi = id at discussion.tex:249 (adjacency test in Task 2); other occurrences scattered through discussion.

       **Batch 2.6 — sections/type-exclusion.tex (1 row):**
       - Expected: role (d) definition reminder (type-exclusion.tex:216 per research).

       **Batch 2.7 — sections/appendix-numerical.tex (2 rows):**
       - Expected: role (d) (numerical appendix quotes \\varphi generically).

    3. For each batch, verify the batch's row count matches the §2.1 per-file count EXACTLY before moving to the next batch. If mismatch, halt and reconcile (either re-read the file or flag as 'row-count deviation' in a new §4 footnote).

    4. At batch completion, append a §4 summary subsection:
       ```
       ### §4.summary Role Count Summary

       | Role | Count | Predicted (57-RESEARCH) | Match? |
       |------|-------|-------------------------|--------|
       | (a) self-modeling tracking | [N_a] | 8-12 | ✓/✗ |
       | (b) inert wrapper | [N_b] | 0 | ✓/✗ |
       | (c) state-preparation | [N_c] | 0 | ✓/✗ |
       | (d) exposition shorthand | [N_d] | 30-40 | ✓/✗ |
       | **Total** | [48] | 48 | ✓/✗ |

       **Author-judgment-needed residuals:** [list any rows flagged; target ≤ 2]
       ```

    5. Forbidden-proxy sanity check (in-prose): the §4 intro text explicitly states that classification is meta-level and no revision text, no renames, no Paper 5 source edits, no new SymPy, and no purification / CDP / ancilla classifications are present.

    Do NOT touch main-jmp-submitted.tex. Do NOT edit any Paper 5 source .tex file (read-only inspection). Do NOT append to alfsen-shultz-notes.md. Do NOT run SymPy or lake build or latexdiff.
  </action>
  <verify>
    1. If Plan 57-01 VERDICT was B: §4 contains only the halt placeholder; Tasks 2-3 skipped; Task 4 commits halt-marker.
    2. If VERDICT A: §4 contains 48 rows in the table (row-count exactly matching §2.1 grand total); §4.summary role count table is populated; residuals count ≤ 2.
    3. Per-file row counts in §4 MATCH §2.1 per-file counts exactly (main.tex:16, appendix-proofs.tex:7, axiom-verification.tex:2, composite-lt.tex:7, discussion.tex:13, type-exclusion.tex:1, appendix-numerical.tex:2).
    4. Every row has 7 columns populated; 'Phi-indep cite if role (d)' is populated for every role-(d) row (not left blank); 'R8 equivocation flag' is YES or NO (not blank).
    5. `git status --porcelain ~/repos/blog/landing/papers/qm-from-self-modeling/` returns empty.
    6. `git status --porcelain derivations/paper5-peirce-preservation/alfsen-shultz-notes.md` returns empty.
    7. `grep -ni 'purification\\|chiribella\\|CDP' derivations/paper5-peirce-preservation/phi-audit.md` — any hits are inside explicit rejection prose.
  </verify>
  <done>
    §4 classification table is populated with all 48 rows (or halt-placeholder if VERDICT B), per-file counts match §2.1 baseline, ≤ 2 author-judgment-needed residuals. Role count summary committed. (Rejects R8 single-fix forbidden proxy: classification table covers all 48 occurrences BEFORE any revision begins; rejects rename-without-classification forbidden proxy: §4 is the rename rule input, not the rename itself; rejects Chiribella/CDP forbidden proxy: keyword-absence verified.)
  </done>
</task>

<task type="auto">
  <name>Task 2: \\varphi = id WLOG audit (§4.A) + R8 equivocation roster (§4.B) + rem:phi-inert-objection global-soundness verdict</name>
  <files>derivations/paper5-peirce-preservation/phi-audit.md</files>
  <action>
    Produce the dedicated \\varphi = id adjacency audit (§4.A) and the R8 equivocation roster (§4.B), applying the three-clause heuristic from 57-RESEARCH. If Plan 57-01 committed VERDICT B, skip to Task 4.

    **§4.A \\varphi = id WLOG Specialization Audit:**

    1. Identify every \\varphi = id occurrence from the §4 table (predicted 4: main.tex:92, 407, 410; discussion.tex:249). If the classification surfaces additional \\varphi = id specializations not predicted, include them.

    2. For each occurrence, read ±15 lines of context in the source .tex file. Check for presence of cor:S4-phi-indep, lem:peirce-preservation, rem:S4-phi-independent, or equivalent phi-independence citation in the SAME paragraph.

    3. Write §4.A:
       ```
       ## §4.A \\varphi = id WLOG Specialization Audit

       **Operational criterion:** A \\varphi = id specialization is a safe role-(d) classification ONLY IF cor:S4-phi-indep (or lem:peirce-preservation, or rem:S4-phi-independent) appears in the SAME paragraph as the specialization. Non-adjacent citations or missing citations constitute R8 equivocation.

       | # | File:line | \\varphi = id specialization context | Adjacent phi-indep cite? | Cite name (if YES) | R8 flag |
       |---|-----------|--------------------------------------|--------------------------|--------------------|---------|
       | 1 | main.tex:92 | [context] | YES/NO | cor:S4-phi-indep / ... | R8-EQUIV / CLEAN |
       | 2 | main.tex:407 | [context] | YES/NO | ... | ... |
       | 3 | main.tex:410 | [context] | YES/NO | ... | ... |
       | 4 | discussion.tex:249 | [context] | YES/NO | ... | ... |
       | ... | (any additional) | ... | ... | ... | ... |
       ```

    4. Every R8-EQUIV row is appended to §4.B (below) automatically.

    **§4.B R8 Equivocation Roster:**

    1. Write §4.B intro:
       ```
       ## §4.B R8 Equivocation Roster

       **Heuristic (from 57-RESEARCH §R8 Equivocation Detection):** An R8 equivocation site is any of:
       1. Two rows within the same subsection, both role (d), with incompatible "minimal property" columns, with no explicit \\varphi reintroduction between them.
       2. A role-(a) row followed by a role-(d) row within the same subsection, with no subsection boundary or phi-independence citation between them.
       3. A role-(d) row whose "phi-independence cite" column is missing or stale (cites \\ref{thm:S4} instead of \\ref{cor:S4-phi-indep}).

       **Detection procedure:** Apply the three heuristic clauses to the §4 table; apply adjacency audit from §4.A; check the 4 pre-identified hypothesis sites.
       ```

    2. Section-by-section scan of §4: for each adjacent pair of rows within the same subsection, apply clauses 1 and 2 of the heuristic. For each role-(d) row, apply clause 3.

    3. Audit the 4 pre-identified hypothesis sites:
       - **rem:phi-inert-objection** (discussion.tex:471-487): Quote the remark's claim verbatim; confirm whether it is consistent with the §4 role-(a) roster. Use test-rem-phi-inert-objection-global-soundness procedure.
       - **§3.4 mixing-function selection** (main.tex:~696+): Confirm every \\varphi in this block is role (a); any role (d) here = category error, flag as R8.
       - **§5 local-tomography \\varphi^{-1}** (composite-lt.tex:131-157): Confirm role (a) classification; flag any surrounding role (d) without subsection boundary.
       - **prop:inheritance chain** (Plan 57-01 §3): Inherit the verdict from §3.3; if VERDICT A confirmed re-routing, no new R8 flag added; otherwise this task skipped entirely.

    4. Write §4.B table:
       ```
       | # | Site (file:line range) | Heuristic clause triggered | Severity | Remediation hint (for Plan 57-03) |
       |---|------------------------|----------------------------|----------|-------------------------------------|
       | 1 | [e.g., main.tex:92] | Clause 3: \\varphi = id without adjacent cite | HIGH | Insert \\ref{cor:S4-phi-indep} in the same paragraph |
       | 2 | [e.g., discussion.tex:471-487] | Global soundness of rem:phi-inert-objection scope claim | [HIGH/MEDIUM] | Narrow scope to "main-theorem statement level" or confirm SURVIVES |
       | ... | ... | ... | ... | ... |
       ```

    5. Write the rem:phi-inert-objection verdict subsection §4.B.rem:
       ```
       ### §4.B.rem:phi-inert-objection global-soundness verdict

       **Claim under test:** "the tracking map \\varphi does not appear as a free variable in the derivation of the main theorem" (discussion.tex:471-487).

       **Main theorem proof chain:** \\ref{thm:main} (main.tex:~84) → §sec:sp → §sec:self-modeling → §5 local-tomography (composite-lt.tex:131-157).

       **Role-(a) roster inspection:** [list every role-(a) occurrence from §4 that appears inside the main-theorem proof chain above]

       **Verdict:** [ONE of:]
       - **SURVIVES** — No role-(a) occurrence enters the main-theorem proof chain; rem:phi-inert-objection's claim is globally sound as stated.
       - **NARROW-TO-STATEMENT-LEVEL** — Role-(a) occurrences at [specific lines, e.g., composite-lt.tex:131-157] DO enter the main-theorem proof chain; rem:phi-inert-objection's scope must be narrowed to "main-theorem statement level, not proof level" in Plan 57-03 revision. Specific narrow wording: "In the STATEMENT of the main theorem (\\ref{thm:main}), the tracking map \\varphi does not appear as a free variable; in its PROOF (via \\ref{sec:sp} and \\ref{sec:self-modeling}), \\varphi^{-1} appears as a non-trivial free variable in the trace-form non-degeneracy argument (composite-lt.tex:131-157)."
       ```

    6. Summary count:
       ```
       ### §4.B.summary R8 equivocation count

       - Total R8 equivocation sites detected: [N]
       - Predicted range (57-RESEARCH Q1): 3-6
       - Match? ✓/✗
       - If N > 6: disconfirming observation triggered; note in §4.B.summary and carry to §7 pivot reasoning.
       ```

    Do NOT touch main-jmp-submitted.tex. Do NOT edit Paper 5 source files. Do NOT append to alfsen-shultz-notes.md.
  </action>
  <verify>
    1. §4.A contains exactly one row per \\varphi = id occurrence (≥ 4 predicted, possibly more); every row's "Adjacent phi-indep cite?" column is YES or NO (not blank); every YES row names the cite; every NO row is added to §4.B.
    2. §4.B contains at minimum 4 verdict entries for the 4 hypothesis sites (rem:phi-inert-objection, §3.4 mixing-function, §5 \\varphi^{-1}, prop:inheritance); each has (site, clause triggered, severity, remediation).
    3. §4.B.rem:phi-inert-objection has an explicit verdict (SURVIVES or NARROW-TO-STATEMENT-LEVEL) with supporting role-(a) roster evidence.
    4. §4.B.summary count is populated; any N > 6 has a disconfirming-observation note.
    5. `git status --porcelain derivations/paper5-peirce-preservation/alfsen-shultz-notes.md` returns empty.
    6. `git -C ~/repos/blog status --porcelain landing/papers/qm-from-self-modeling/` returns empty.
  </verify>
  <done>
    §4.A \\varphi = id adjacency audit populated with per-occurrence verdict; §4.B R8 equivocation roster populated with heuristic-clause attribution per site; rem:phi-inert-objection global-soundness verdict explicit (SURVIVES or NARROW). R8 count committed with match/deviation vs 57-RESEARCH Q1 prediction. (Rejects R8 single-fix forbidden proxy: equivocation roster covers ALL detected sites, not one; rejects global-identity forbidden proxy: every \\varphi = id has explicit adjacency verdict.)
  </done>
</task>

<task type="checkpoint:decision">
  <name>Task 3: Split-vs-standing-definition pivot recommendation (§7) + checkpoint:decision on Approach</name>
  <files>derivations/paper5-peirce-preservation/phi-audit.md</files>
  <action>
    **Gate:** If Plan 57-01 VERDICT was B, skip this task entirely (Task 4 commits the halt marker).

    Produce the affected-section-count estimate and the pivot recommendation BEFORE Plan 57-03 authors any draft, then present the decision to the user via checkpoint:decision.

    1. Count affected sections for each approach:

       **Approach 1 (standing-def + per-section role qualifier) affected-section count:**
       - Count distinct subsections in §4 that contain at least one role-(d) first-occurrence (needs a qualifier). A "subsection" here is a ``\subsection{...}`` block in the LaTeX source.
       - Add 1 for Def. def:self-modeling-system (main.tex block containing the boxed standing definition).
       - Add 1 for each R8 equivocation site from §4.B that requires a remediation outside the above subsections (e.g., rem:phi-inert-objection revision is a separate discussion.tex subsection).
       - Approach 1 affected-section count = [N_a1]

       **Approach 2 (split-notation) affected-section count:**
       - Count distinct subsections in §4 that contain at least one \\varphi occurrence (search-and-replace scope is every occurrence, but the section count is distinct subsections).
       - Add 1 for preamble.sty (new \\newcommand definitions).
       - Approach 2 affected-section count = [N_a2]

    2. Apply the backtracking rule from ROADMAP.md:785:
       - IF N_a1 > 5: recommend Approach 1 (standing-def) to minimize LaTeX diff and JMP-referee surface.
       - IF N_a1 ≤ 5 AND role (b)/(c) counts both = 0 (from §4.summary): recommend Approach 1 (Approach 2 degenerates to \\varphitrack alone, which is equivalent to a boxed standing def but with 48 s-and-r lines).
       - IF N_a1 ≤ 5 AND role (b) or role (c) counts > 0: recommend Approach 2 (macro-level clarity justified by non-empty role sets).
       - Reference the 57-RESEARCH §"Approach 1" and §"Approach 2" diff-cost envelopes:
         * Approach 1: ~6 role-qualifier insertions + 1 boxed def + ≤ 3 R8 remediation edits ≈ 10 edits ≈ 30 min
         * Approach 2: 48 search-and-replace lines + preamble.sty edit ≈ 49 edits ≈ 1-2 hours

    3. Write §7 Author Decision Pivot:
       ```
       ## §7. Author Decision Pivot

       ### §7.1 Affected-section counts

       | Approach | Affected sections | Edit count estimate | Referee-surface risk | Time estimate |
       |----------|-------------------|---------------------|----------------------|---------------|
       | Approach 1 (standing-def + qualifiers) | [N_a1] | [~10] | LOW (minimum diff) | ~30 min |
       | Approach 2 (split-notation) | [N_a2] | [~49] | MEDIUM (large diff may re-open §3.3/§S4) | 1-2 hours |

       ### §7.2 Recommendation

       **Recommendation: Approach [1 / 2] / either-viable**

       **Reasoning:** [Cite the backtracking rule clause that applies; cite §4.summary role counts; cite §4.B R8 count; cite JMP-referee-surface consideration from 57-RESEARCH Q3.]

       ### §7.3 Approach 1 pros/cons

       **Pros:** [quote 57-RESEARCH §"Approach 1" track record: vdW 2019 Def. 2 single-symbol + per-section qualifiers; A-S 2003 compression c_p precedent]
       **Cons:** Relies on reader remembering the standing definition; no macro-level visual cue.

       ### §7.4 Approach 2 pros/cons

       **Pros:** Per-occurrence explicit role marker; flip-draft ergonomics via preamble macro bodies.
       **Cons:** ~49 search-and-replace edits; may re-open already-closed Phase 54/55 sections on notation grounds.

       ### §7.5 DERV-57-03 requirement

       **Regardless of user decision, BOTH drafts are authored in Plan 57-03** (DERV-57-03: "Two revision-text drafts produced: split-notation AND standing-definition"). The user-chosen approach is the INTEGRATION TARGET (gets committed to the living Paper 5 working copy); the other is authored but not integrated.

       ### §7.6 User decision

       [Populated by checkpoint:decision in this task]
       ```

    4. **Present checkpoint:decision to the user:**

       Show the user:
       - §7.1 counts table
       - §7.2 recommendation with reasoning
       - §7.3 and §7.4 pros/cons
       - §7.5 DERV-57-03 both-drafts requirement
       - Explicit prompt: "Which approach should be the INTEGRATION TARGET for Plan 57-03? Options: option-a (Approach 1 standing-def + qualifiers), option-b (Approach 2 split-notation), option-c (I want to revisit the counts first — do not proceed)."

    5. On user response:
       - **option-a:** Append "User decision: Approach 1 (standing-def + per-section role qualifier) is the integration target for Plan 57-03. Approach 2 draft will be authored in phi-audit.md §6 but NOT integrated into Paper 5 living copy." to §7.6.
       - **option-b:** Append "User decision: Approach 2 (split-notation \\varphitrack / \\varphiinert / \\varphianc) is the integration target for Plan 57-03. Approach 1 draft will be authored in phi-audit.md §5 but NOT integrated." to §7.6.
       - **option-c:** HALT — write "User requested count revisit; Plan 57-02 pauses pending counts re-review. Plan 57-03 will NOT start." Return from this task without calling Task 4.

    Do NOT start authoring drafts (Plan 57-03). Do NOT edit Paper 5 source files. Do NOT append to alfsen-shultz-notes.md.
  </action>
  <verify>
    1. §7 contains §7.1 through §7.6 subsections.
    2. §7.1 table has numeric N_a1 and N_a2 (not "[N]").
    3. §7.2 recommendation is one of {Approach 1, Approach 2, either-viable} with backtracking-rule citation.
    4. §7.5 explicitly names DERV-57-03 both-drafts requirement.
    5. §7.6 ends with a concrete user decision (option-a, option-b, or option-c-halt).
    6. If option-c: the file notes the halt and NO Task 4 call is made in the same plan run.
  </verify>
  <done>
    §7 pivot recommendation committed with numeric affected-section counts, backtracking-rule application, Approach 1/2 pros-cons, DERV-57-03 both-drafts reminder, and an explicit user decision (option-a, option-b, or option-c halt). Plan 57-03 now knows its integration target. (Rejects rename-without-classification forbidden proxy: recommendation is based on §4 classification + §4.B R8 roster; rejects R8 single-fix forbidden proxy: recommendation addresses all R8 sites collectively, not piecemeal.)
  </done>
</task>

<task type="auto">
  <name>Task 4: Commit Plan 57-02 deliverable via gpd commit</name>
  <files>derivations/paper5-peirce-preservation/phi-audit.md</files>
  <action>
    Stage and commit the Plan 57-02 deliverable (phi-audit.md §4 + §4.A + §4.B + §7 with user decision, OR halt marker if VERDICT B was inherited, OR option-c halt if user requested count revisit).

    1. Verify the plan delivered exactly one modification:
       ```
       git status --porcelain
       ```
       Expected: exactly one modified file `derivations/paper5-peirce-preservation/phi-audit.md`. If any other file is modified or untracked, STOP and investigate.

    2. Commit via gpd commit with a single-sentence message (match the variant to the outcome path):
       - VERDICT B inherited: `phase(57-02): halt — Plan 57-01 VERDICT B escalation; see phi-audit.md §3.4`
       - Option-c halt: `phase(57-02): pause — user requested pivot count revisit; decision pending`
       - option-a (Approach 1 chosen): `phase(57-02): classification table + R8 roster + Approach 1 standing-def chosen as Plan 57-03 integration target`
       - option-b (Approach 2 chosen): `phase(57-02): classification table + R8 roster + Approach 2 split-notation chosen as Plan 57-03 integration target`

       Command template:
       ```
       /Users/ehrlich/.gpd/venv/bin/python -m gpd.runtime_cli --runtime claude-code --config-dir ./.claude --install-scope local commit "[message per above]" --files derivations/paper5-peirce-preservation/phi-audit.md
       ```

    3. Verify the commit landed:
       ```
       git log --oneline -1
       git status --porcelain
       ```
       Expected: clean working tree; latest commit is the phase(57-02) commit; single-sentence message; no co-author tag.

    4. Do NOT edit STATE.md or ROADMAP.md. Do NOT push. Do NOT run tectonic or latexdiff.
  </action>
  <verify>
    1. `git log --oneline -1` shows the phase(57-02) commit.
    2. `git status --porcelain` returns empty.
    3. `git show --stat HEAD` lists exactly one file: `derivations/paper5-peirce-preservation/phi-audit.md`.
    4. Commit message is single-sentence; no co-author tag; matches the outcome path.
    5. `git -C ~/repos/blog status --porcelain` returns empty.
  </verify>
  <done>
    Plan 57-02 deliverable committed. phi-audit.md now contains §1-§4.B and §7 (or halt marker). Plan 57-03 has its integration target locked. (Rejects frozen-file-edit forbidden proxy: blog repo untouched.)
  </done>
</task>

</tasks>

<verification>
Plan-level checks across all 4 tasks:

- **Classification-before-revision invariant (R8):** §4 classification is fully committed BEFORE §7 pivot is written; §7 pivot is committed BEFORE any Plan 57-03 draft begins. Enforced by task ordering (Task 1 → Task 2 → Task 3 → Task 4) and by §7.5 which states DERV-57-03 drafts are a Plan 57-03 deliverable.
- **Append-only shared-artifact discipline:** alfsen-shultz-notes.md NOT modified (enforced per-task by git status check).
- **Frozen-file zero-diff:** main-jmp-submitted.tex NOT opened; blog repo untouched.
- **No new SymPy / no re-derivation:** grep -ri 'sympy' phi-audit.md returns zero; Phase 54/55 results cited by \\ref, not re-verified.
- **Forbidden-proxy rejection:** §4, §4.A, §4.B, §7 prose does not invoke purification, Chiribella, CDP, global-identity, or single-fix patterns (keyword-absence check test-forbidden-proxy-absence-plan02).
- **R8 audit ordering:** §4.B roster is produced AFTER §4 classification and §4.A adjacency audit (within Task 2 sub-ordering).
- **Outcome pivot gate:** If Plan 57-01 committed VERDICT B, Plan 57-02 halts at Task 1 precondition check and commits a halt marker only.
- **User-decision capture:** §7.6 contains an explicit option-a / option-b / option-c-halt decision; if option-c, Plan 57-03 does NOT start.
- **DERV-57-03 both-drafts requirement surfaced:** §7.5 explicitly states that regardless of user decision, BOTH drafts are authored in Plan 57-03.
- **Contract coverage:** 5 claims (claim-full-classification, claim-wlog-phi-id-audit, claim-r8-equivocation-roster, claim-pivot-recommendation, claim-derv-00-01-phase57-slice-plan02) each with ≥ 1 acceptance test and ≥ 2 references; all 5 forbidden proxies listed; uncertainty markers include weakest anchors + disconfirming observations.
</verification>

<success_criteria>
- phi-audit.md §4 populated with 48-row classification table OR halt marker.
- phi-audit.md §4.A populated with \\varphi = id adjacency audit; every row's verdict explicit.
- phi-audit.md §4.B populated with R8 equivocation roster; rem:phi-inert-objection global-soundness verdict explicit (SURVIVES or NARROW-TO-STATEMENT-LEVEL).
- phi-audit.md §7 populated with numeric affected-section counts + pivot recommendation + user decision captured.
- Single git commit via gpd commit; message matches outcome path (VERDICT-B-halt, option-a, option-b, or option-c-pause).
- All five forbidden proxies explicitly rejected in prose AND verified by keyword-absence checks.
- No modifications to: main-jmp-submitted.tex, Paper 5 living .tex files, alfsen-shultz-notes.md.
- User decision (option-a or option-b) stored in §7.6 as Plan 57-03's integration-target indicator. option-c halt blocks Plan 57-03 until re-discussed.
</success_criteria>

<output>
After completion, create `.gpd/phases/57-phi-inert-wrapper-resolution/57-02-SUMMARY.md` (per template `./.claude/get-physics-done/templates/summary.md`).
</output>
