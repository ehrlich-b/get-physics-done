---
phase: 76-phase-a-5-berry-curvature-same-wall-gate-soft-kill
plan: 02
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - code/cartan_phaseA5_berry.py
  - derivations/76-berry-same-wall.tex
interactive: true

conventions:
  units: "natural (hbar=c=k_B=1, dimensionless)"
  metric: "(+,-,-,-) mostly-minus, timelike-positive; soldered (1,3) Lorentz block on the C_u^2 survivors {11,18,19,26} = the FORCED SO(3,1) Lorentz structure (Phase 75 SURVIVES); det_2 = x0^2 - x1^2 - x2^2 - x3^2, G = diag(+1,-1,-1,-1)"
  det_ssot: "ring_lemma_verification.py det_3, cross-term 2Re(x2* x0* x1) (F_4-invariant, CH + 324/324); off-switch numerator = bulk_geometry_verification.inv_det_X_block (V_0<->V_{1/2} triple 2Re((x2 x1)x3) dropped)"
  octonion_mul: "Fano table, e1 e2 = e4"
  complex_structure: "u = e_7 (C_u = span{1, e_7}); slice_to_complex maps e_7 -> i (sympy.I) SYMBOLICALLY over Q(i)"
  primitive_idempotent: "E_11 = diag(1,0,0); matter M in V_{1/2} (engine indices {11..26}); off-center via X_bg = I/3 + M (rho_J / _offcenter_subs)"
  qgt: "F_B = -2 Im Tr(P dP dP) (the v18.0 Lie/antisymmetric sector); decisive verdicts on GAUGE-INVARIANT scalars (rank-1 P => F_B abelian, but the Lorentz-block F_B^F_B contraction is tested under an SO(3,1) frame rotation)"
  stress_energy: "T[M] = the INDEPENDENTLY-FROZEN V_{1/2} stress-energy from the v17.0 matter-on-flat pipeline: h(x;M) := H_source(bg+M) - H_source(bg-only) (B1), spacetime_curvature_of_g + the inv_det_X_block off-switch + ricci_decomposition_n4; kappa frozen FIRST, T[M] built WITHOUT reference to F_B (non-circular)"
  exactness: "EXACT over Q(i) on every decisive verdict (sympy diff/re/im/series/Matrix.rank/.eigenvals); the i is symbolic (sympy.I), NEVER a float; the matter M-series is exact per order; numpy/float = triage only"
  vacuum: "Lambda = 0; the M=0 spacetime vacuum is flat KKT eta; matter F_B must VANISH as M->0 (subtract the plan-76-01 vacuum F_B); do NOT reintroduce Lambda<0 / R x H^3"

dimensional_check:
  matter_F_B: "[dimensionless] the matter part of F_B (vacuum subtracted), leading nonzero order in ||M||; must VANISH as M->0"
  M_power: "[integer] the leading M-power of the matter F_B-source; must MATCH the M-power of T[M] at the matched order (v17.0: g entered at O(||M||^2), curvature at O(||M||^4))"
  FwedgeF_contraction: "[dimensionless] the epsilon-contraction of F_B^F_B onto the FORCED SO(3,1) Lorentz block (the MacDowell-Mansouri-style diagnostic); gauge-invariant scalar, frame-rotation invariant"
  T_trace: "[dimensionless] T^mu_mu of the effective F_B stress vs T[M]: traceless (T^mu_mu=0, ~F^2, conformal) => EM-shaped => SOFT KILL; non-traceless + matchable => Einstein-shaped => SURVIVES"

approximations:
  - name: "small-M series of the matter F_B and T[M]"
    parameter: "||M|| (rho_J off-center-ness of X_bg = I/3 + M)"
    validity: "leading nonzero matter order -- the DECISIVE order (v17.0 used O(||M||^2) for g, O(||M||^4) for curvature); compute the next order to confirm the leading one"
    breaks_when: "symbolic blow-up / watchdog stall on a long F_B(x;M) simplify (mitigation: rational-M spot evaluations as exact triage; commit per task; cancel not full simplify)"
    check: "the M->0 limit returns the plan-76-01 vacuum F_B exactly; the off-switch (cross-term OFF) gives the v17.0 decisive reduction; power-counting confirmed by the next order"

contract:
  schema_version: 1
  scope:
    question: "With M in V_{1/2} turned ON: is the matter-sourced Berry curvature F_B (and the epsilon-contraction of F_B^F_B onto the FORCED SO(3,1) Lorentz block) TRANSVERSE / EINSTEIN-SHAPED -- its order (M-power), tensor structure, AND support matchable to an INDEPENDENTLY-FROZEN V_{1/2} stress-energy T[M] (=> SURVIVES to Phase B) -- OR EM-SHAPED (traceless T^mu_mu=0, ~F^2, conformal; support disjoint from T[M]; no M-power match) reproducing the v17.0 cone-Hessian same-wall mismatch (=> the Lie sector inherits the symmetric-sector failure: SOFT KILL, recommend STOP before Phase B)? Decided on GAUGE-INVARIANT scalars, exact over Q(i)."
  claims:
    - id: claim-berry-same-wall
      statement: "SOFT KILL gate. The canonical Berry curvature F_B (imaginary part of the QGT; its real part is the Fubini-Study pullback along E(x), a SOFT sanity check of the same non-Einstein character as the cone-Hessian -- established PART 1, plan 76-01) has a vacuum structure at M=0 (flat / pure-Lambda / other -- plan 76-01) and a matter-sourced part at M!=0 that is EITHER transverse/Einstein-shaped (order, tensor structure, and support matchable to a V_{1/2} stress-energy T[M]) OR EM-shaped and reproducing the v17.0 cone-Hessian same-wall mismatch (support disjoint from T[M], no order-matching) -- in which case the Lie sector inherits the symmetric-sector failure and a STOP is recommended before Phase B. This plan decides the matter-on VALD-04 clause: build the matter-on idempotent E(x;M) over C_u (rank-1 + C_u-faithful + M->0 limit), compute the matter F_B small-M series and the F_B^F_B Lorentz-block contraction, build the independently-frozen T[M], and render the Einstein-vs-EM SURVIVES/SOFT-KILL verdict on gauge-invariant scalars. CONJUNCTIVE with Phase 75 (✓ SURVIVES): BOTH must SURVIVE to greenlight Phase B (77)."
      observables: [obs-berry-curvature]
      deliverables: [deliv-phaseA5, deliv-phaseA5-code]
      acceptance_tests: [test-berry-matter-family, test-berry-shape]
      references: [ref-provost-vallee, ref-wise, ref-mm-1977, ref-faraut-koranyi, ref-bulk-geometry-prior, ref-peirce-coupling, ref-52-kkt, ref-orbit-gate]
  observables:
    - id: obs-berry-curvature
      name: "Canonical Berry curvature F_B = imaginary part of the QGT"
      kind: proof_obligation
      definition: "The Berry curvature F_B = Im(QGT) = -2 Im Tr(P dP dP) of the rank-1 idempotent field E(x) -- in this plan: the tensor SHAPE of its matter-sourced part at M!=0 (transverse/Einstein-shaped vs EM-shaped traceless ~F^2), the epsilon-contraction F_B^F_B onto the FORCED SO(3,1) Lorentz block, and the M-power/tensor-structure/support match against an independently-frozen V_{1/2} stress-energy T[M]; the decisive SOFT-KILL clause of claim-berry-same-wall."
      regime: "M in V_{1/2} turned ON (X_bg = I/3 + M), leading nonzero order in ||M||; gauge-invariant scalars tested under an SO(3,1) frame rotation; C_u reduction e_7 -> i over Q(i)"
  deliverables:
    - id: deliv-phaseA5
      kind: derivation
      path: derivations/76-berry-same-wall.tex
      description: "Phase A.5 Berry-curvature same-wall verdict derivation (this plan APPENDS PART 2: the matter-on E(x;M) C_u construction; the matter F_B small-M series; the F_B^F_B Lorentz-block epsilon-contraction; the independently-frozen T[M]; the VALD-04 Einstein-vs-EM verdict on gauge-invariant scalars; and the EXPLICIT SOFT KILL / SURVIVES line -- conjunctive with Phase 75 for the Phase-77 greenlight). Plan 76-01 wrote PART 1 (recipe + CP^1 pin, VALD-03, CALC-03, CALC-04). Backed by exact-over-Q(i) SymPy in code/cartan_phaseA5_berry.py."
      must_contain:
        - "the matter-on idempotent E(x;M) over C_u built explicitly to leading order in M and VERIFIED: rank-1 (E^2-E=0) + C_u-faithful (NO e_1..e_6 leak under M in V_{1/2}) + the M->0 limit returns the plan-76-01 vacuum F_B"
        - "the matter part of F_B (vacuum subtracted) as a small-M series, with the leading M-power identified (and the next order computed to confirm it)"
        - "the independently-frozen V_{1/2} stress-energy T[M] (h(x;M)=H_source(bg+M)-H_source(bg-only), spacetime_curvature_of_g + the inv_det_X_block off-switch + ricci_decomposition_n4), kappa frozen FIRST, built WITHOUT reference to F_B"
        - "the VALD-04 verdict on GAUGE-INVARIANT scalars (the F_B^F_B Lorentz-block epsilon-contraction tested under an SO(3,1) frame rotation): Einstein-shaped (M-power + tensor structure + support matchable to T[M]) => SURVIVES, OR EM-shaped (traceless T^mu_mu=0 / ~F^2 / support-disjoint / no M-power match) => SOFT KILL"
        - "an EXPLICIT SOFT KILL / SURVIVES verdict line, reported FLAT at true strength (negative-result-is-success), with the conjunctive-with-Phase-75 consequence for the Phase-77 greenlight stated"
    - id: deliv-phaseA5-code
      kind: code
      path: code/cartan_phaseA5_berry.py
      description: "Exact-over-Q(i) SymPy driver (PART 2, appended to the plan-76-01 driver): the matter-on E(x;M) over C_u (rank-1 + C_u-faithfulness + M->0 verification); the matter F_B small-M series; the F_B^F_B Lorentz-block epsilon-contraction onto the SO(3,1) block; the independently-frozen T[M] via spacetime_curvature_of_g + inv_det_X_block off-switch + ricci_decomposition_n4; the SO(3,1) frame-rotation gauge-invariance test; the VALD-04 Einstein-vs-EM verdict. Reuses the plan-76-01 QGT/berry_F helpers + vacuum F_B; octonion_algebra source guard active; sympy only."
      must_contain:
        - "matter-on E(x;M): rank-1 check E^2-E==0 over Q(i) + C_u-faithfulness assertion (no e_1..e_6 survive pi_u) + M->0 limit == plan-76-01 vacuum F_B"
        - "off-switch comparison: spacetime_curvature_of_g(norm_potential=BG.inv_det_X_block) gives the decisive cross-term-OFF reduction (the v17.0 ~94% result)"
        - "frame-rotation test: a generic SO(3,1) rotation of the 4 base dirs leaves the decisive scalar UNCHANGED over Q(i)"
        - "the VALD-04 decision: traceless/~F^2/support-disjoint/no-M-power-match => SOFT KILL; matchable in M-power + tensor structure + support => SURVIVES"
  references:
    - id: ref-provost-vallee
      kind: paper
      locator: "Provost & Vallee, Comm. Math. Phys. 76 (1980) 289"
      aliases: ["Provost-Vallee 1980", "QGT"]
      role: definition
      why_it_matters: "The QGT split Re=FS / Im=Berry: F_B = Im(QGT) is the object whose matter shape VALD-04 tests. The load-bearing convention (the Im sector, distinct from the dead Re sector)."
      applies_to: [claim-berry-same-wall]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-wise
      kind: paper
      locator: "Wise, gr-qc/0611154 (2010), MacDowell-Mansouri gravity and Cartan geometry"
      aliases: ["Wise 2006", "Wise gr-qc/0611154"]
      role: definition
      why_it_matters: "The MM epsilon-contraction F^F -> EH + Lambda structure: the F_B^F_B Lorentz-block contraction is the A.5(b) DIAGNOSTIC and the EM-vs-Einstein distinction lives here. Cited for the contraction STRUCTURE only (the diagnostic), NOT the forced-vs-posited audit (Phase C)."
      applies_to: [claim-berry-same-wall]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-mm-1977
      kind: paper
      locator: "MacDowell & Mansouri, Phys. Rev. Lett. 38 (1977) 739"
      aliases: ["MacDowell-Mansouri 1977", "MM 1977"]
      role: definition
      why_it_matters: "The original MM unification of the Lorentz connection and the coframe into a single curvature whose F^F gives EH+Lambda. The structural template for the F_B^F_B Lorentz-block contraction VALD-04 computes as the Einstein-vs-EM diagnostic."
      applies_to: [claim-berry-same-wall]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [cite]
    - id: ref-52-kkt
      kind: prior_artifact
      locator: "derivations/52-kkt-spacetime.tex, derivations/52-observer-uniqueness.tex; 75-01-SUMMARY.md (soldered (1,3) Lorentz target, SO(3,1) FORCED)"
      aliases: ["52-kkt-spacetime", "52-observer-uniqueness"]
      role: benchmark
      why_it_matters: "h_2(C_u) ~= R^{3,1}, det_2 (1,3), G=diag(+1,-1,-1,-1); the soldered (1,3) Lorentz block + the FORCED SO(3,1) (Phase 75 SURVIVES) is the target the F_B^F_B contraction lands on and the frame group for the SO(3,1) frame-rotation gauge test."
      applies_to: [claim-berry-same-wall]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [read, compare, cite]
    - id: ref-bulk-geometry-prior
      kind: prior_artifact
      locator: "code/bulk_geometry_verification.py (spacetime_curvature_of_g, inv_det_X_block off-switch, ricci_decomposition_n4, _matterless_reference_hessian, _offcenter_subs); v17.0 cone-Hessian NONE (kappa T ~10^3 < G, support-disjoint, S!=0/Weyl!=0)"
      aliases: ["bulk_geometry_verification.py", "v17.0 cone-Hessian"]
      role: benchmark
      why_it_matters: "Warm engine for the INDEPENDENTLY-FROZEN T[M] (the v17.0 matter-on-flat B1 pipeline h=H(bg+M)-H(bg) + the cross-term off-switch) and the trace/traceless/Weyl decomposition. The v17.0 NONE is the same-wall FAILURE PATTERN VALD-04 tests for -- it binds ONLY the Re sector (fp-reuse-cone-hessian), so the cone-Hessian is NEVER load-bearing for this Im verdict; it builds T[M], which F_B is compared AGAINST."
      applies_to: [claim-berry-same-wall]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [read, use]
    - id: ref-peirce-coupling
      kind: prior_artifact
      locator: "code/embedding_under_E_verification.py (E, proj_u_exact, slice_to_complex, cu_to_complex, h3o_from_coords, jordan)"
      aliases: ["peirce_indices_under_E11", "embedding_under_E_verification.py"]
      role: benchmark
      why_it_matters: "slice_to_complex (e_7 -> i) is THE C_u->C bridge for the matter-on E(x;M); proj_u_exact tests C_u-faithfulness (no e_1..e_6 leak). The single key tool of the matter construction."
      applies_to: [claim-berry-same-wall]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [read, use]
    - id: ref-orbit-gate
      kind: prior_artifact
      locator: "code/orbit_dimension_gate.py (infinitesimal_action, exact_qq_rank, span_rank_over_QQ -- import-only; NEVER run its slow __main__)"
      aliases: ["orbit_dimension_gate.py"]
      role: benchmark
      why_it_matters: "Exact-rank-over-QQ helpers for the SO(3,1) frame-rotation gauge-invariance test on the F_B^F_B Lorentz-block contraction (the decisive scalar must be frame-rotation invariant). Import the helpers ONLY -- its __main__ is the ~19-min RING gate that exits nonzero by design."
      applies_to: [claim-berry-same-wall]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [use]
    - id: ref-faraut-koranyi
      kind: paper
      locator: "Faraut & Koranyi, Analysis on Symmetric Cones (1994)"
      aliases: ["Faraut-Koranyi", "FK94"]
      role: background
      why_it_matters: "The cone metric g_X = Hess(-log det) underlying the T[M] cubic-norm pipeline (the cross-terms C_{(V_0)(V_1)(V_{1/2})} the off-switch toggles). Cited for the T[M] construction; the cone-Hessian itself is the REAL-PART object, never load-bearing for the Im verdict (fp-reuse-cone-hessian)."
      applies_to: [claim-berry-same-wall]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [cite]
  acceptance_tests:
    - id: test-berry-matter-family
      subject: claim-berry-same-wall
      kind: existence
      procedure: "Q6 / the biggest modeling gap. Build the matter-on idempotent E(x;M) over C_u explicitly to leading order in M in V_{1/2} (X_bg = I/3 + M; the off-center basepoint move, parallel to _offcenter_subs/rho_J). Verify EXACTLY over Q(i): (1) rank-1 idempotent (E(x;M)^2 - E(x;M) == 0 to the working order, Tr == 1); (2) C_u-FAITHFULNESS -- after pi_u, NO octonion comp e_1..e_6 survives on the decisive path (M in V_{1/2} must not leak e_1..e_6 that pi_u then kills, changing rank); (3) the M->0 limit of the matter F_B returns the plan-76-01 vacuum F_B exactly."
      pass_condition: "E(x;M) is a clean rank-1 C_u projector to the working M-order (E^2-E==0, Tr==1), C_u-faithful (no e_1..e_6 leak), and the M->0 limit recovers the plan-76-01 vacuum F_B EXACTLY over Q(i). If the matter family is NOT a clean rank-1 C_u projector (rank changes / e_1..e_6 leak that alters the verdict): report the construction obstruction flat and route to the Approach-2 octonionic cross-check before trusting VALD-04. No float."
      evidence_required: [deliv-phaseA5, deliv-phaseA5-code, ref-peirce-coupling, ref-bulk-geometry-prior]
      automation: automated
    - id: test-berry-shape
      subject: claim-berry-same-wall
      kind: oracle
      procedure: "THE DECISIVE SOFT-KILL CLAUSE (VALD-04), MEASURED not assumed. (1) Compute the matter part of F_B (vacuum subtracted) as a small-M series; identify its leading M-power (compute the next order to confirm). (2) Compute the epsilon-contraction of F_B^F_B onto the FORCED SO(3,1) Lorentz block (the MM-style diagnostic, ref-wise/ref-mm-1977) as a GAUGE-INVARIANT scalar; TEST it under a generic SO(3,1) frame rotation (must be unchanged over Q(i)). (3) Build the INDEPENDENTLY-FROZEN T[M] (h=H_source(bg+M)-H_source(bg-only) via spacetime_curvature_of_g; freeze kappa FIRST; the inv_det_X_block off-switch confirms T[M] is genuinely cross-term-sourced; ricci_decomposition_n4 for trace/traceless/Weyl) -- built WITHOUT reference to F_B. (4) TEST three matches of the matter F_B-source against T[M] on gauge-invariant scalars: M-power AND tensor structure (traceless T^mu_mu=0 / ~F^2 => EM-shaped) AND support."
      pass_condition: "SURVIVES iff the matter F_B-source is EINSTEIN-SHAPED: M-power, tensor structure, AND support matchable to the independently-frozen T[M] (non-traceless effective stress; the F_B^F_B contraction frame-rotation invariant). SOFT KILL iff EM-SHAPED: traceless T^mu_mu=0 / ~F^2 / conformal, OR support disjoint from T[M], OR M-power mismatch (the v17.0 ~10^3 magnitude pattern) -- 'Lie sector inherits the symmetric-sector mismatch', recommend STOP before Phase B. Reported FLAT either way (negative-result-is-success), on gauge-invariant scalars, exact over Q(i). NO assumed Einstein form; NO float."
      evidence_required: [deliv-phaseA5, deliv-phaseA5-code, ref-wise, ref-mm-1977, ref-provost-vallee, ref-bulk-geometry-prior, ref-52-kkt, ref-orbit-gate]
      automation: hybrid
  forbidden_proxies:
    - id: fp-relabel
      subject: claim-berry-same-wall
      proxy: "Relabeling 'F_B satisfies some 2-form equation' or 'F_B != 0 and matter turns it on' as 'F_B satisfies Einstein / gravity' WITHOUT an independent stress-energy T[M] matched in magnitude, tensor structure, AND M-power; OR letting T[M] / kappa reference F_B (circular)."
      reason: "The v17.0 Ph73 lesson: a nonzero antisymmetric 2-form is a FIELD STRENGTH (EM-shaped, source side), not an Einstein structure -- a tensor appearing is not Einstein structure. The match must be in order (M-power), shape (tensor structure: non-traceless, not ~F^2), AND support, against an INDEPENDENTLY-FROZEN T[M] (built without F_B, kappa frozen first). This is the central same-wall trap."
    - id: fp-reuse-cone-hessian
      subject: claim-berry-same-wall
      proxy: "Using the v17.0 cone-Hessian symmetric-sector Riemann tensor (the REAL part of the QGT) as load-bearing for the Lie-sector / Im verdict -- e.g. importing the v17.0 Riemann and calling its non-Einstein-ness the A.5 verdict (re-binding to the v17.0 NONE by sector confusion)."
      reason: "This route computes the antisymmetric/connection curvature Im(QGT)=F_B -- a DIFFERENT tensor. The cone-Hessian is the real-part consistency check ONLY (plan 76-01 CALC-03) and the BUILDER of T[M] (which F_B is compared AGAINST), never the load-bearing verdict object. The v17.0 NONE binds only Re."
    - id: fp-relabel-softkill
      subject: claim-berry-same-wall
      proxy: "Relabeling an EM-shaped / support-disjoint / M-power-mismatched matter F_B as 'approximately Einstein' or 'morally gravitational'; OR deflating a genuine Einstein-shaped signal to look like a failure."
      reason: "Negative-result-is-success: a clean SOFT KILL ('the Lie sector inherits the symmetric-sector mismatch') is a valuable, publishable closure that must be reported FLAT at true strength. Softening an EM-shaped result to 'approximately Einstein' destroys the gate's value and misleads the Phase-77 greenlight; equally, an honest Einstein-shaped SURVIVES must not be deflated (claims at true strength in BOTH directions)."
    - id: fp-nonabelian-gauge
      subject: claim-berry-same-wall
      proxy: "Deciding the verdict on a frame-DEPENDENT (gauge-covariant) F_B component or a connection component / open Wilson line, instead of a gauge-INVARIANT scalar (trace, tr(F_B^F_B), Wilson loop) tested under an SO(3,1) frame rotation."
      reason: "Even though the rank-1 primitive idempotent gives an ABELIAN F_B (the projector form is genuinely gauge-invariant), the Lorentz-block F_B^F_B contraction and any frame statement must use gauge-invariant scalars tested under an SO(3,1) frame rotation (the Wilczek-Zee covariance F->UFU^-1 lives in the frame bundle, Phase B). A verdict that changes under a frame rotation is a gauge artifact."
    - id: fp-float-decisive
      subject: claim-berry-same-wall
      proxy: "Any decisive verdict (the matter F_B M-series, the M-power, the F_B^F_B contraction, the trace/traceless/Weyl split, the T[M] match, the frame-rotation invariance) resting on a float i, numpy rank, or floating-point eigenvalues."
      reason: "The i of Q(i) is carried SYMBOLICALLY (sympy.I); the matter M-series is exact per order; ranks/eigenvalues/Re/Im via SymPy. A float 'approximately Einstein' is explicitly forbidden -- the verdict rests on exact cancellations (e.g. a traceless T^mu_mu=0 must be exactly 0). Float is triage-only."
    - id: fp-octonion-algebra
      subject: claim-berry-same-wall
      proxy: "Importing octonion_algebra.py (buggy associator, 0.67 gap, float) on any decisive path; OR using the Jordan product ∘ inside the QGT P dP dP instead of the associative complex matrix product on slice_to_complex(P)."
      reason: "octonion_algebra.py is BANNED. det/trace/Jordan come from ring_lemma_verification.py det_3 (SSOT) + bulk_geometry_verification + embedding_under_E_verification. The QGT uses the ASSOCIATIVE operator product on C_u (forced by u=e_7), NOT the Jordan product."
  links:
    - id: link-berry
      source: claim-berry-same-wall
      target: deliv-phaseA5
      relation: supports
      verified_by: [test-berry-matter-family, test-berry-shape]
    - id: link-berry-code-02
      source: claim-berry-same-wall
      target: deliv-phaseA5-code
      relation: computes
      verified_by: [test-berry-matter-family, test-berry-shape]
    - id: link-tm-diagnostic
      source: ref-bulk-geometry-prior
      target: claim-berry-same-wall
      relation: benchmarks
      verified_by: [test-berry-shape]
  uncertainty_markers:
    weakest_anchors:
      - "VALD-04 (test-berry-shape) is the genuinely-open SOFT-KILL clause (MEDIUM confidence; designed to be MEASURED, not assumed). The honest base rate leans SOFT KILL -- the symmetric sector FAILED the analogous v17.0 test (NONE, kappa T ~10^3 < G, support-disjoint, S!=0/Weyl!=0) -- but the Lie sector is a DIFFERENT tensor (the v17.0 NONE does not bind it), so it genuinely might not inherit the mismatch. This is exactly why it is computed; report the outcome flat either way."
      - "The matter-on E(x;M) C_u-faithfulness (test-berry-matter-family) is THE single biggest modeling gap (Q6): only the M=0 vacuum family is de-risked exact over Q(i); whether the rank-1 idempotent of the perturbed (X_bg=I/3+M) configuration stays a clean rank-1 C_u projector (no e_1..e_6 leak under M in V_{1/2}) must be built and verified explicitly BEFORE the VALD-04 verdict. If it does not, route to the Approach-2 octonionic cross-check."
    disconfirming_observations:
      - "Matter F_B traceless (T^mu_mu=0 on the F_B^F_B contraction) and ~F^2 / ~(dM)^2 -> EM-shaped -> SOFT KILL (recommend STOP before Phase B)."
      - "Matter-source support disjoint from T[M] support, or magnitude mismatch (the v17.0 ~10^3 pattern) -> same-wall mismatch -> SOFT KILL."
      - "M-power of the matter F_B-source != M-power of T[M] at the matched order -> EM-shaped, not Einstein -> SOFT KILL."
      - "The F_B^F_B Lorentz-block contraction / verdict scalar CHANGES under an SO(3,1) frame rotation -> gauge artifact (fp-nonabelian-gauge); switch to an invariant scalar and re-run."
      - "The matter-on E(x;M) is NOT a clean rank-1 C_u projector (rank changes / e_1..e_6 leak alters the verdict) -> the modeling gap bites; report flat and route to the Approach-2 octonionic cross-check before VALD-04."
      - "T[M] or kappa found to reference F_B -> circular (fp-relabel); rebuild T[M] independently, kappa frozen first."

estimated_execution:
  total_minutes: 90
  breakdown:
    - task: 1
      minutes: 30
      note: "Matter-on E(x;M) over C_u: rank-1 + C_u-faithfulness + M->0 limit (THE biggest modeling gap; symbolic M-perturbation of the rank-1 idempotent; the 1.5x unfamiliar-formalism multiplier applies)"
    - task: 2
      minutes: 35
      note: "VALD-04 decisive: matter F_B M-series + F_B^F_B Lorentz-block contraction + independently-frozen T[M] (off-switch) + SO(3,1) frame-rotation test + Einstein-vs-EM match (the watchdog-risk task -- foreground python3 -u, commit immediately)"
    - task: 3
      minutes: 25
      note: "Verdict synthesis -> derivation PART 2 + the SOFT KILL/SURVIVES line; human ratification checkpoint of the milestone-gating verdict (mirror Phase 75 Task 4)"
---

<objective>
PART 2 of the v18.0 SOFT-KILL gate (Phase A.5), the DECISIVE/MATTER half. With the vacuum
baseline fixed by plan 76-01 (F_B well-defined, nonzero, born-from-breaking; M=0 vacuum
classified), turn matter M in V_{1/2} ON and decide the genuinely-open VALD-04 clause:
is the matter-sourced Berry curvature F_B (and the epsilon-contraction of F_B^F_B onto the
FORCED SO(3,1) Lorentz block of Phase 75) TRANSVERSE / EINSTEIN-SHAPED -- M-power, tensor
structure, AND support matchable to an INDEPENDENTLY-FROZEN V_{1/2} stress-energy T[M]
(=> SURVIVES to Phase B) -- OR EM-SHAPED (traceless T^mu_mu=0, ~F^2, conformal; support
disjoint; no M-power match) reproducing the v17.0 cone-Hessian same-wall mismatch (=> the
Lie sector inherits the symmetric-sector failure: SOFT KILL, recommend STOP before Phase B)?
Decided on GAUGE-INVARIANT scalars, exact over Q(i). The final task is the blocking
human ratification of the milestone-gating SURVIVES/SOFT-KILL verdict (Bryan ratifies
milestone-gating verdicts; same class as Ph70.1/Ph73/Ph75 Task 4).

Purpose: This is the decisive half of the conjunctive A.5 gate. The single biggest modeling
gap (the matter-on E(x;M) C_u-faithfulness) gets its OWN verified task (rank-1 + no e_1..e_6
leak + M->0 limit) BEFORE the verdict. The verdict is rendered against an INDEPENDENTLY-FROZEN
T[M] (the v17.0 cross-term pipeline, kappa frozen first, built WITHOUT reference to F_B), so a
match is non-circular. A.5 is CONJUNCTIVE with Phase 75 (✓ SURVIVES): BOTH must SURVIVE to
greenlight Phase B (77); a SOFT KILL here closes the route as a publishable negative.

THE DESIGN POINTS (do not get these wrong):
  1. The verdict is the M-power + tensor structure + support match to an INDEPENDENTLY-FROZEN
     T[M] -- "some 2-form appears" is forbidden (fp-relabel). The v17.0 lesson: power-counting
     AND support must match, not just "some tensor appears" (kappa T was ~10^3 < G).
  2. Decisive verdicts on GAUGE-INVARIANT scalars (rank-1 P => F_B abelian, but the Lorentz-block
     F_B^F_B contraction and any frame statement are tested under an SO(3,1) frame rotation;
     fp-nonabelian-gauge). The Wilczek-Zee non-abelian structure lives in the frame bundle (Phase B).
  3. The cone-Hessian / Re(QGT) is NEVER load-bearing for the Im verdict (fp-reuse-cone-hessian);
     it BUILDS T[M] (which F_B is compared AGAINST). The v17.0 NONE binds only the Re sector.
  4. Negative-result-is-success: an EM-shaped result is reported FLAT as a SOFT KILL, never
     "approximately Einstein"; an honest Einstein-shaped SURVIVES is not deflated (fp-relabel-softkill).

Output:
- code/cartan_phaseA5_berry.py -- APPEND PART 2: the matter-on E(x;M) over C_u (rank-1 +
  C_u-faithfulness + M->0); the matter F_B small-M series; the F_B^F_B Lorentz-block contraction;
  the independently-frozen T[M] (off-switch); the SO(3,1) frame-rotation test; the VALD-04 verdict.
- derivations/76-berry-same-wall.tex -- APPEND PART 2: the matter construction, the VALD-04
  Einstein-vs-EM verdict, and the EXPLICIT SOFT KILL / SURVIVES line (conjunctive with Phase 75).
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/CONVENTIONS.md
@.gpd/phases/76-phase-a-5-berry-curvature-same-wall-gate-soft-kill/76-RESEARCH.md
@.gpd/phases/76-phase-a-5-berry-curvature-same-wall-gate-soft-kill/76-01-SUMMARY.md
@.gpd/phases/75-phase-a-coframe-reduction-dealbreaker-the-kill-gate/75-01-SUMMARY.md
@code/cartan_phaseA5_berry.py
@code/bulk_geometry_verification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and VERIFY the matter-on idempotent E(x;M) over C_u (rank-1 + C_u-faithful + M->0 limit) -- the biggest modeling gap, BEFORE the verdict</name>
  <files>code/cartan_phaseA5_berry.py</files>
  <action>
    Append to code/cartan_phaseA5_berry.py (reuse the plan-76-01 QGT/berry_F/fs_metric helpers, the
    source guard, and the emitted vacuum F_B). Run foreground UNBUFFERED (`python3 -u`); COMMIT this
    task IMMEDIATELY on completion (the watchdog discipline -- a long symbolic M-expansion can stall).

    THE SINGLE BIGGEST MODELING GAP (Q6 / Open Risk #3): only the M=0 vacuum family is de-risked. Build
    the matter-on idempotent E(x;M) explicitly and verify it is a clean rank-1 C_u projector BEFORE any
    verdict.

    1. TURN ON M in V_{1/2}: set X_bg = I/3 + M with M a symbolic element of V_{1/2} (engine indices
       {11..26}); use the off-center basepoint machinery (BG._offcenter_subs / rho_J) as the precedent.
       Keep M symbolic to leading order (a single small scalar t scaling a fixed rational M-direction in
       V_{1/2}, plus -- for power-counting -- the next order). Build E(x;M) as the rank-1 idempotent of the
       perturbed configuration over C_u (the Veronese chart v=[1,z1,z2] deformed by M, OR the conjugation
       E(x;M)=g(x;M) E_11 g(x;M)^-1; pick the rational/exact route, cross-check with the other if cheap).
    2. RANK-1 + IDEMPOTENT (exact over Q(i), to the working M-order): assert E(x;M)^2 - E(x;M) == 0 and
       Tr(E(x;M)) == 1 to the working order (cancel/series, not full simplify).
    3. C_u-FAITHFULNESS (the decisive modeling check): apply pi_u = EMB.E()/proj_u_exact and assert NO
       octonion comp e_1..e_6 survives on the decisive path (M in V_{1/2} must not leak e_1..e_6 that pi_u
       then kills and changes the rank). If e_1..e_6 DO leak in a way that alters rank/the verdict, this is
       the modeling gap biting -- _report it flat and route to the Approach-2 octonionic cross-check before
       trusting VALD-04 (do NOT silently proceed).
    4. M->0 LIMIT: compute the matter F_B = berry_F(slice_to_complex(E(x;M)), base dirs), subtract the
       plan-76-01 vacuum F_B, and confirm the matter part -> 0 as M->0 (t->0) EXACTLY over Q(i) (smooth
       limit; the vacuum is correctly recovered). Emit the leading matter F_B(x;M) for Task 2.
  </action>
  <verify>
    1. E(x;M) rank-1 idempotent EXACT over Q(i) to the working M-order: E^2 - E == 0, Tr(E) == 1.
    2. C_u-FAITHFUL: no octonion comp e_1..e_6 survives pi_u on the decisive path (the matter family stays
       a clean rank-1 C_u projector). If it leaks in a verdict-altering way -> report flat + route to Approach 2.
    3. M->0 limit EXACT: matter F_B (vacuum subtracted) -> 0 as M->0; the plan-76-01 vacuum F_B is recovered.
    4. The leading matter F_B(x;M) is emitted for Task 2 (with the next order available for power-counting).
    5. Source guard still active (0 octonion_algebra on the decisive path; no Jordan ∘ in the QGT); no fp-float-decisive.
    6. Order-of-expectation: the matter family is a clean rank-1 C_u projector and the matter F_B vanishes at M=0
       (the expected smooth, C_u-faithful construction); a leak/rank-change is the disconfirming outcome, reported flat.
  </verify>
  <done>
    The matter-on idempotent E(x;M) over C_u is built explicitly (X_bg = I/3 + M, M in V_{1/2}) and VERIFIED
    a clean rank-1 C_u projector to the working M-order (E^2-E==0, Tr==1, C_u-faithful: no e_1..e_6 leak), with
    the M->0 limit recovering the plan-76-01 vacuum F_B exactly over Q(i) and the leading matter F_B(x;M) emitted
    for the VALD-04 verdict. If the matter family is NOT clean (rank-change / verdict-altering e_1..e_6 leak), the
    obstruction is reported flat and routed to the Approach-2 octonionic cross-check. Task committed immediately.
  </done>
</task>

<task type="auto">
  <name>Task 2: VALD-04 (the decisive SOFT-KILL clause) -- matter F_B M-series + F_B^F_B Lorentz-block contraction vs the INDEPENDENTLY-FROZEN T[M], on gauge-invariant scalars</name>
  <files>code/cartan_phaseA5_berry.py</files>
  <action>
    THE DECISIVE CLAUSE. Append to code/cartan_phaseA5_berry.py. MEASURE the Einstein-vs-EM verdict; do NOT
    assume an Einstein form. Run foreground UNBUFFERED (`python3 -u`); COMMIT IMMEDIATELY (the watchdog-risk
    task -- a long F_B(x;M) / T[M] symbolic simplify can stall; recover via git log, not re-run).

    (A) MATTER F_B + ITS M-POWER: take the leading matter F_B(x;M) from Task 1 as a small-M series; identify
        the leading M-power; compute the NEXT order to confirm the leading one (power-counting is decisive --
        v17.0: g at O(||M||^2), curvature at O(||M||^4)). Use cancel, not full simplify, to control blow-up.

    (B) THE F_B^F_B LORENTZ-BLOCK epsilon-CONTRACTION (the MM-style diagnostic, ref-wise/ref-mm-1977): contract
        F_B^F_B with the epsilon tensor onto the FORCED SO(3,1) Lorentz block (the soldered (1,3) target of
        Phase 75; G=diag(+1,-1,-1,-1)). This is a GAUGE-INVARIANT scalar. Compute it exactly over Q(i).

    (C) FRAME-ROTATION GAUGE TEST (fp-nonabelian-gauge guard): apply a generic SO(3,1) rotation to the 4 base
        dirs (use orbit_dimension_gate.exact_qq_rank / a rational Lorentz boost+rotation over QQ) and confirm
        the decisive scalar (the F_B^F_B contraction) is UNCHANGED over Q(i). A verdict that changes under a
        frame rotation is a gauge artifact -> switch to an invariant scalar.

    (D) THE INDEPENDENTLY-FROZEN T[M] (built WITHOUT reference to F_B; kappa frozen FIRST):
        - h(x;M) := H_source(bg+M) - H_source(bg-only) via BG.spacetime_curvature_of_g (the v17.0 B1
          matter-on-flat pipeline; stay on the 4d slice -- the dim-10 V_0 inverse is the >200s cliff).
        - OFF-SWITCH: call BG.spacetime_curvature_of_g(norm_potential=BG.inv_det_X_block) and confirm the
          decisive cross-term-OFF reduction (the v17.0 ~94% cross-term-sourced result) -- this certifies T[M]
          is genuinely V_0<->V_{1/2}-cross-term-sourced and independent of F_B.
        - Freeze kappa as a single global constant FIRST (Phase-73 style); decompose T[M] (and the effective
          F_B stress) into trace/traceless/Weyl via BG.ricci_decomposition_n4.

    (E) THE THREE MATCHES (on gauge-invariant scalars, exact over Q(i)) -- the verdict:
        - M-POWER: leading M-power of the matter F_B-source == M-power of T[M] at the matched order?
        - TENSOR STRUCTURE: is the effective F_B stress TRACELESS (T^mu_mu == 0) and ~F^2 / conformal? If yes
          => EM-SHAPED. Is it non-traceless and structurally matchable to T[M]? If yes => Einstein-shaped.
        - SUPPORT: does the matter F_B-source support coincide with T[M] support, or is it disjoint (the v17.0
          pattern)?
        VERDICT: SURVIVES iff Einstein-shaped (all three match: M-power + non-traceless tensor structure +
        support, frame-rotation invariant). SOFT KILL iff EM-shaped (traceless ~F^2 / support-disjoint /
        M-power mismatch -- "Lie sector inherits the symmetric-sector mismatch"). _report the verdict FLAT.
        GUARD: do NOT relabel an EM-shaped result "approximately Einstein" (fp-relabel-softkill); do NOT use the
        cone-Hessian/Re(QGT) as load-bearing (fp-reuse-cone-hessian -- it BUILDS T[M], it is not the verdict);
        do NOT let T[M]/kappa reference F_B (fp-relabel).
  </action>
  <verify>
    1. Matter F_B M-power identified EXACT over Q(i), confirmed by the next order (power-counting decisive).
    2. F_B^F_B Lorentz-block epsilon-contraction computed as a gauge-invariant scalar over Q(i) (onto the Phase-75
       SO(3,1) block); UNCHANGED under a generic SO(3,1) frame rotation (fp-nonabelian-gauge guard PASSED).
    3. T[M] INDEPENDENTLY frozen: built via spacetime_curvature_of_g WITHOUT reference to F_B; kappa frozen first;
       the inv_det_X_block off-switch gives the decisive cross-term-OFF reduction (the v17.0 ~94% result).
    4. THE THREE MATCHES decided exactly: M-power match?, tensor structure (traceless ~F^2 EM-shaped vs non-traceless
       matchable Einstein-shaped)?, support (coincident vs disjoint)?
    5. VALD-04 VERDICT rendered: SURVIVES (Einstein-shaped, all three match, frame-invariant) OR SOFT KILL (EM-shaped /
       support-disjoint / M-power mismatch), reported FLAT, on gauge-invariant scalars.
    6. Guards: NO fp-relabel (T[M]/kappa independent of F_B, the three-way match required); NO fp-reuse-cone-hessian
       (cone-Hessian only BUILDS T[M]); NO fp-relabel-softkill (EM reported flat, Einstein not deflated); NO
       fp-float-decisive (all decisive numbers exact over Q(i), e.g. T^mu_mu == 0 exactly); NO fp-octonion-algebra.
  </verify>
  <done>
    VALD-04 is DECIDED exactly over Q(i) on gauge-invariant scalars: the matter F_B M-power (confirmed by the next
    order), the F_B^F_B Lorentz-block epsilon-contraction (frame-rotation invariant), and the three-way match
    (M-power + tensor structure + support) against the INDEPENDENTLY-FROZEN T[M] (kappa frozen first, off-switch
    confirmed cross-term-sourced, built without F_B). The verdict is rendered FLAT: SURVIVES (Einstein-shaped,
    matchable) OR SOFT KILL (EM-shaped: traceless ~F^2 / support-disjoint / M-power mismatch -- "Lie sector inherits
    the symmetric-sector mismatch"). Every forbidden proxy (fp-relabel, fp-reuse-cone-hessian, fp-relabel-softkill,
    fp-nonabelian-gauge, fp-float-decisive, fp-octonion-algebra) is explicitly guarded. Task committed immediately.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verdict synthesis -> deliv-phaseA5 PART 2 + human ratification of the SOFT-KILL/SURVIVES verdict</name>
  <files>derivations/76-berry-same-wall.tex</files>
  <action>
    APPEND PART 2 to derivations/76-berry-same-wall.tex synthesizing the matter-on construction and the VALD-04
    decision into the Phase A.5 verdict (the file already carries the plan-76-01 PART 1: recipe+CP^1 pin, VALD-03,
    CALC-03, CALC-04). PART 2 MUST contain, at true strength, no softening:
      (a) THE MATTER-ON E(x;M): the explicit C_u construction (X_bg=I/3+M, M in V_{1/2}) VERIFIED rank-1 +
          C_u-faithful (no e_1..e_6 leak) + M->0 limit recovering the PART-1 vacuum F_B.
      (b) THE MATTER F_B: its small-M series, the leading M-power (confirmed by the next order).
      (c) THE F_B^F_B LORENTZ-BLOCK CONTRACTION: the MM-style epsilon-contraction onto the FORCED SO(3,1) block
          (Phase 75), a gauge-invariant scalar, frame-rotation invariant (cite ref-wise/ref-mm-1977 for the
          contraction STRUCTURE only -- the diagnostic, NOT the Phase-C action).
      (d) THE INDEPENDENTLY-FROZEN T[M]: h=H_source(bg+M)-H_source(bg-only), the off-switch cross-term-OFF
          reduction, kappa frozen first, built WITHOUT reference to F_B; trace/traceless/Weyl.
      (e) THE VALD-04 VERDICT on gauge-invariant scalars: either "Phase A.5 SURVIVES: the matter Berry curvature
          is Einstein-shaped -- M-power + tensor structure + support matchable to T[M] -- greenlight Phase 77
          (conjunctive with Phase 75 ✓)" OR "Phase A.5: SOFT KILL -- the matter Berry curvature is EM-shaped
          (traceless ~F^2 / support-disjoint / M-power mismatch); the Lie sector inherits the symmetric-sector
          same-wall mismatch; recommend STOP before Phase B" -- a flat, publishable verdict.
      (f) THE EXPLICIT SOFT KILL / SURVIVES LINE and the conjunctive-with-Phase-75 consequence for the Phase-77
          greenlight.
    Explicitly REJECT each forbidden proxy in the verdict statement: NO fp-relabel (the verdict is the three-way
    M-power + tensor-structure + support match to an INDEPENDENTLY-FROZEN T[M], never "some 2-form appears"); NO
    fp-reuse-cone-hessian (the cone-Hessian/Re(QGT) only BUILDS T[M] and is the SOFT real-part check, never the Im
    verdict); NO fp-relabel-softkill (an EM-shaped result is reported FLAT as a SOFT KILL, never "approximately
    Einstein"; an Einstein-shaped SURVIVES is not deflated); NO fp-nonabelian-gauge (decided on gauge-invariant
    scalars, frame-rotation tested); NO fp-float-decisive; NO fp-octonion-algebra. Negative-result-is-success.
  </action>
  <verify>
    Present to the researcher (Bryan) for RATIFICATION of the milestone-gating verdict (the math is DECIDABLE from
    the exact-over-Q(i) numbers in code/cartan_phaseA5_berry.py -- this checkpoint ratifies the SOFT-KILL/SURVIVES
    CALL and its consequence for the Phase-77 greenlight, not the arithmetic). Surface for inspection:
      1. The matter-on E(x;M) verification (rank-1 + C_u-faithful + M->0) -- the biggest modeling gap, resolved or
         routed to Approach 2.
      2. The three decisive comparisons against the INDEPENDENTLY-FROZEN T[M]: M-power match, tensor structure
         (traceless ~F^2 EM-shaped vs non-traceless matchable Einstein-shaped), support (coincident vs disjoint) --
         all on gauge-invariant scalars, the F_B^F_B contraction frame-rotation invariant.
      3. The honest base rate: the symmetric sector FAILED the analogous v17.0 test (NONE); the Lie sector is a
         DIFFERENT tensor -- whether it inherits the same-wall mismatch (SOFT KILL) or not (SURVIVES) is the open call.
      4. The overall verdict line (SURVIVES -> greenlight 77 conjunctively with Phase 75 ✓; or SOFT KILL -> STOP
         before B as a publishable closure) and that it is reported at true strength (neither inflated nor deflated).
    Note prior precedent: Bryan ratified the decisive verdicts at Ph70.1, Ph73, and Ph75 (Task 4); this is the same
    class of milestone SOFT-KILL/greenlight gate.
  </verify>
  <resume-signal>Type "approved" to ratify the verdict (SURVIVES greenlights Phase 77 conjunctively with Phase 75 ✓; SOFT KILL stops the route before Phase B as a publishable closure), or describe required corrections.</resume-signal>
  <done>
    derivations/76-berry-same-wall.tex PART 2 written with the matter-on E(x;M) construction, the matter F_B M-series,
    the F_B^F_B Lorentz-block contraction, the independently-frozen T[M], and a DECISIVE VALD-04 verdict (SURVIVES ->
    greenlight Phase 77 [conjunctive with Phase 75 ✓], OR SOFT KILL -> STOP before Phase B as a publishable closure),
    every forbidden proxy explicitly rejected, the verdict on gauge-invariant scalars reported at true strength, and
    the researcher has ratified the SOFT-KILL/SURVIVES call.
  </done>
</task>

</tasks>

<verification>
- EXACT over Q(i) on EVERY decisive number (sympy diff/re/im/series/Matrix.rank/.eigenvals; the i is sympy.I,
  symbolic; the matter M-series exact per order; never numpy/float) -- fp-float-decisive guard.
- octonion_algebra.py ABSENT on the decisive path (source guard); the QGT uses the ASSOCIATIVE complex matrix
  product on slice_to_complex(P), NEVER the Jordan product ∘ -- fp-octonion-algebra guard.
- The matter-on E(x;M) C_u construction (the biggest modeling gap) is built and VERIFIED rank-1 + C_u-faithful
  (no e_1..e_6 leak) + M->0 limit BEFORE the VALD-04 verdict; a verdict-altering leak/rank-change is reported flat
  and routed to the Approach-2 octonionic cross-check.
- T[M] is INDEPENDENTLY FROZEN: built via spacetime_curvature_of_g WITHOUT reference to F_B, kappa frozen FIRST,
  the inv_det_X_block off-switch confirming it is genuinely cross-term-sourced -- non-circular (fp-relabel guard).
- The VALD-04 verdict is the THREE-WAY match (M-power + tensor structure + support) on GAUGE-INVARIANT scalars,
  the F_B^F_B Lorentz-block contraction tested under an SO(3,1) frame rotation -- "some 2-form appears" is forbidden
  (fp-relabel); a frame-dependent verdict is a gauge artifact (fp-nonabelian-gauge).
- The cone-Hessian / Re(QGT) is NEVER load-bearing for the Im verdict -- it only BUILDS T[M] (fp-reuse-cone-hessian);
  the v17.0 NONE binds only the Re sector.
- Lambda is NOT reintroduced negative; the matter F_B VANISHES at M=0 (the PART-1 vacuum is subtracted).
- Negative-result-is-success: an EM-shaped result is reported FLAT as a SOFT KILL ("Lie sector inherits the
  symmetric-sector mismatch"), never "approximately Einstein"; an Einstein-shaped SURVIVES is not deflated
  (fp-relabel-softkill) -- claims at true strength in BOTH directions.
- Order-of-expectation stated for each decisive quantity (matter F_B vanishes at M=0; the M-power confirmed by the
  next order; the honest base rate leans SOFT KILL since the symmetric sector failed, but the Lie sector is a
  different tensor) and decided exactly, never assumed.
- The final task is the blocking human-verify ratification of the milestone-gating SOFT-KILL/SURVIVES verdict
  (conjunctive with Phase 75 for the Phase-77 greenlight; mirror Phase 75 Task 4).
</verification>

<success_criteria>
PART 2 of Phase A.5 -- the decisive SOFT-KILL clause -- is DECIDED exactly over Q(i) on gauge-invariant scalars,
at true strength, with the researcher's ratification:

1. The matter-on E(x;M) over C_u (THE biggest modeling gap) is built explicitly and VERIFIED a clean rank-1 C_u
   projector (E^2-E==0, Tr==1, C_u-faithful: no e_1..e_6 leak) with the M->0 limit recovering the plan-76-01 vacuum
   F_B exactly -- or the obstruction is reported flat and routed to the Approach-2 octonionic cross-check.
2. VALD-04 is MEASURED (not assumed): the matter F_B M-power (confirmed by the next order); the F_B^F_B Lorentz-block
   epsilon-contraction (gauge-invariant, frame-rotation invariant); and the three-way match (M-power + tensor
   structure + support) against the INDEPENDENTLY-FROZEN T[M] (kappa frozen first, off-switch cross-term-sourced,
   built without F_B). 
3. THE VERDICT is rendered FLAT: SURVIVES iff Einstein-shaped (all three match, frame-invariant) -> greenlight
   Phase 77 conjunctively with Phase 75 ✓; SOFT KILL iff EM-shaped (traceless T^mu_mu=0 / ~F^2 / support-disjoint /
   M-power mismatch) -> STOP before Phase B as a publishable closure ("Lie sector inherits the symmetric-sector
   mismatch"). Reported at true strength -- neither inflated nor deflated.
4. deliv-phaseA5 (PART 2) + deliv-phaseA5-code carry the matter construction, the VALD-04 results, and the EXPLICIT
   SOFT KILL / SURVIVES line, with every forbidden proxy (fp-relabel, fp-reuse-cone-hessian, fp-relabel-softkill,
   fp-nonabelian-gauge, fp-float-decisive, fp-octonion-algebra) explicitly rejected.
5. The researcher has RATIFIED the milestone-gating SOFT-KILL/SURVIVES verdict (the blocking human-verify checkpoint;
   same class as Ph70.1/Ph73/Ph75). Negative-result-is-success: a SOFT KILL is a valuable, publishable closure
   reported flat; a SURVIVES is the conjunctive greenlight (with Phase 75 ✓) for Phase B (77).
</success_criteria>

<output>
After completion, create `.gpd/phases/76-phase-a-5-berry-curvature-same-wall-gate-soft-kill/76-02-SUMMARY.md`.
</output>
