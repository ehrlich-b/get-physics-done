---
phase: 76-phase-a-5-berry-curvature-same-wall-gate-soft-kill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - code/cartan_phaseA5_berry.py
  - derivations/76-berry-same-wall.tex
interactive: false

conventions:
  units: "natural (hbar=c=k_B=1, dimensionless)"
  metric: "(+,-,-,-) mostly-minus, timelike-positive; soldered (1,3) Lorentz block on the C_u^2 survivors {11,18,19,26}; det_2 = x0^2 - x1^2 - x2^2 - x3^2, G = diag(+1,-1,-1,-1) (Phase 75 SURVIVES target)"
  bulk_op2_metric: "Riemannian / positive-definite Fubini-Study metric of OP^2=F_4/Spin(9); bare trace form on pi_u(V_{1/2}) = diag(2,2,2,2) = (4,0) Euclidean (the DIAGNOSTIC FOIL, not a verdict metric)"
  det_ssot: "ring_lemma_verification.py det_3, cross-term 2Re(x2* x0* x1) (F_4-invariant, CH + 324/324)"
  octonion_mul: "Fano table, e1 e2 = e4"
  complex_structure: "u = e_7 (C_u = span{1, e_7}); slice_to_complex maps e_7 -> i (sympy.I) SYMBOLICALLY over Q(i)"
  primitive_idempotent: "E_11 = diag(1,0,0)"
  peirce_layout: "engine 27-coord flat layout; V_1={0}, V_0={1..10}, V_{1/2}={11..26}; survivors under pi_u: {11,18,19,26} = C_u^2 (the 4 reduced coframe directions, Phase 75)"
  qgt: "projector form Q_{mu nu} = Tr(P d_mu P d_nu P), gauge-invariant for rank-1 P (no phase fixing); g = Re Q (Fubini-Study metric); F_B = i Tr(P[d_mu P, d_nu P]) = -2 Im Q (Berry curvature). Sign/factor PINNED by the CP^1 anchor (F_B = -sin theta/2, g_theta theta = 1/4)."
  exactness: "EXACT over Q(i) on every decisive verdict (sympy diff/re/im/Matrix.rank/.eigenvals over QQ or Q(i)); the i is symbolic (sympy.I), NEVER a float; numpy/float = triage only"
  vacuum: "Lambda = 0; the M=0 spacetime vacuum is flat KKT eta; do NOT reintroduce Lambda<0 / R x H^3 (the FALSIFIED v17.0 cone-Hessian source-field geometry)"

dimensional_check:
  F_B: "[dimensionless] antisymmetric 4x4 matrix of rational functions over Q(i); generically NONZERO (expect block-diagonal Kahler form, e.g. F_B[a,b]=F_B[c,d]=-2 at the base point)"
  Re_QGT: "[dimensionless] symmetric positive-definite Fubini-Study metric (round CP^2-over-C_u gave diag(1,1,1,1)); SOFT-compared to cone-Hessian diag(9,9,18,18) by CHARACTER only"
  CP1_anchor: "[dimensionless] F_B(CP^1) = -sin theta/2 (int = -2pi), g_theta theta = 1/4 (the convention pin, exact over Q(i))"
  vacuum_F_B_level: "[classification] flat / pure-Lambda (~ e ^ e Kahler form) / other; expected pure-Lambda/Kahler (do NOT reintroduce Lambda<0)"

approximations:
  - name: "single base point + rational chart for the vacuum 2-form"
    parameter: "none (M=0 is exact; OP^2 is a homogeneous symmetric space so the vacuum class is base-point-independent)"
    validity: "the vacuum (M=0) F_B and Re(QGT) are EXACT over Q(i) -- no truncation"
    breaks_when: "n/a for the vacuum side (matter-on M-series is in plan 76-02)"
    check: "verify rank-1 idempotent P^2 - P = 0 exactly over Q(i); reproduce the CP^1/CP^2 anchors"

contract:
  schema_version: 1
  scope:
    question: "BEFORE any matter is turned on: is the canonical Berry curvature F_B = Im(QGT) of the rank-1 C_u-idempotent family WELL-DEFINED and generically NONZERO on the 4d slice (born from the C_u breaking of the isotropy-irreducible OP^2, which carries no invariant 2-form) -- and what is its M=0 vacuum class (flat / pure-Lambda / other)? Plus the SOFT real-part sanity check: is Re(QGT) a sensible Fubini-Study metric of the same non-Einstein CHARACTER as the v17.0 cone-Hessian (a discrepancy is INFORMATIVE, never a KILL)?"
  claims:
    - id: claim-berry-welldef-vacuum
      statement: "PART 1 of the SOFT-KILL gate (well-definedness + vacuum). After the C_u reduction (e_7 -> i) of the rank-1 primitive-idempotent family E(x), the canonical Berry curvature F_B = Im(QGT) = -2 Im Tr(P d P d P) is (i) well-defined and generically NONZERO on the 4d slice {11,18,19,26}=C_u^2, BORN FROM THE C_u BREAKING (OP^2=F_4/Spin(9) is isotropy-irreducible => no invariant 2-form => round Berry = 0); (ii) at M=0 has a definite vacuum class -- flat / pure-Lambda (~ e^e Kahler) / other -- decomposed trace/traceless/Weyl, with Lambda not reintroduced negative. The SOFT real-part anchor: Re(QGT) is a positive-definite Fubini-Study metric whose CHARACTER (Ricci-eigenvalue pattern / non-Einstein-ness) is compared -- informatively, NOT as a KILL -- to the cone-Hessian. An identically-zero F_B (degenerate, worse-than-SOFT) is detected and reported flat. Decided exactly over Q(i)."
      observables: [obs-berry-curvature]
      deliverables: [deliv-phaseA5, deliv-phaseA5-code]
      acceptance_tests: [test-berry-real-part, test-berry-vacuum, test-berry-nonzero]
      references: [ref-provost-vallee, ref-baez-octonions, ref-faraut-koranyi, ref-bulk-geometry-prior, ref-peirce-coupling, ref-ring-lemma-engine]
  observables:
    - id: obs-berry-curvature
      name: "Canonical Berry curvature F_B = imaginary part of the QGT"
      kind: proof_obligation
      definition: "The Berry curvature F_B = Im(QGT) = -2 Im Tr(P dP dP) of the rank-1 idempotent field E(x) (curvature of the canonical/tautological connection on the eigenbundle) -- in this plan: its well-definedness and generic non-zero-ness (born from the C_u breaking), its M=0 vacuum structure (zero/flat, pure-Lambda ~ e^e, or other), and whether its real-part counterpart Re(QGT) is a Fubini-Study metric of the same non-Einstein character as the cone-Hessian (a SOFT check). The matter-sourced shape at M!=0 (Einstein-vs-EM) is decided in plan 76-02."
      regime: "M=0 vacuum; canonical Grassmann/tautological connection (no metric-compatibility equation); C_u reduction e_7 -> i over Q(i)"
  deliverables:
    - id: deliv-phaseA5
      kind: derivation
      path: derivations/76-berry-same-wall.tex
      description: "Phase A.5 Berry-curvature same-wall verdict derivation (this plan writes PART 1: the QGT recipe + CP^1 convention pin; the VALD-03 born-from-breaking + generically-nonzero proof; the CALC-03 SOFT Re(QGT) same-character anchor [informative, not a KILL]; the CALC-04 M=0 vacuum classification). The decisive VALD-04 Einstein-vs-EM verdict + the explicit SOFT KILL / SURVIVES line are appended by plan 76-02. Backed by exact-over-Q(i) SymPy in code/cartan_phaseA5_berry.py."
      must_contain:
        - "the QGT recipe Q = Tr(P dP dP) with P = slice_to_complex(E(x)) (e_7 -> i, i symbolic over Q(i)) and the CP^1 convention pin F_B = -sin theta/2, g_theta theta = 1/4 reproduced exactly"
        - "VALD-03: F_B = -2 Im Q is well-defined and generically NONZERO on the 4d slice, BORN FROM the C_u breaking (OP^2 isotropy-irreducible => no invariant 2-form => round Berry = 0); an identically-zero F_B is flagged degenerate (worse-than-SOFT) and would be reported flat"
        - "CALC-03: Re(QGT) is a positive-definite Fubini-Study metric; its non-Einstein CHARACTER is compared to the cone-Hessian diag(9,9,18,18) / round H^3 K=-1 as a SOFT, INFORMATIVE diagnostic (FS-pullback != cone-Hessian restriction) -- explicitly NOT a KILL (fp-reuse-cone-hessian)"
        - "CALC-04: the M=0 vacuum class of F_B (flat / pure-Lambda ~ e^e Kahler / other) via the trace/traceless/Weyl decomposition; Lambda NOT reintroduced negative (no R x H^3)"
    - id: deliv-phaseA5-code
      kind: code
      path: code/cartan_phaseA5_berry.py
      description: "Exact-over-Q(i) SymPy driver (PART 1): copies the Phase-75 _report/_source_guard harness; reuses warm engines (embedding_under_E_verification slice_to_complex/E/proj_u_exact, ring_lemma_verification det_3/Tr/jordan, bulk_geometry_verification cone_hessian_at_center/h3_constant_curvature/ricci_decomposition_n4); builds the rank-1 C_u idempotent family, the projector-form QGT over Q(i), the Re/Im split; the CP^1 pin; VALD-03 nonzero/born-from-breaking; CALC-03 SOFT Re-anchor; CALC-04 vacuum classification. octonion_algebra source guard active; sympy ranks only."
      must_contain:
        - "_source_guard(): assert octonion_algebra absent on the decisive path (fp-octonion-algebra)"
        - "CP^1 anchor reproduced over Q(i): F_B = -sin theta/2, g_theta theta = 1/4"
        - "rank-1 idempotent check P^2 - P == 0 over Q(i) on the C_u chart v=[1,z1,z2]"
        - "F_B and Re(QGT) computed as the literal sympy im/re of Q = Tr(P dP dP); F_B generically nonzero; Re(QGT) positive-definite"
  references:
    - id: ref-provost-vallee
      kind: paper
      locator: "Provost & Vallee, Comm. Math. Phys. 76 (1980) 289"
      aliases: ["Provost-Vallee 1980", "QGT"]
      role: definition
      why_it_matters: "The QGT split: Re = Fubini-Study metric, Im = Berry curvature. THE load-bearing convention -- the dead route was Re, this route is Im. Confirms Re(QGT) = FS pullback (CALC-03) and that Im(QGT) is the canonical Berry 2-form (VALD-03)."
      applies_to: [claim-berry-welldef-vacuum]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-baez-octonions
      kind: paper
      locator: "Baez, The Octonions, math/0105155 (2002), Sec. 3.4"
      aliases: ["Baez 2002"]
      role: definition
      why_it_matters: "OP^2 = F_4/Spin(9), isotropy = the unique irreducible 16-dim Spin(9) spinor rep => isotropy-irreducible => unique invariant symmetric form (FS metric) AND NO invariant 2-form => round Berry curvature vanishes. THE core VALD-03 born-from-breaking fact."
      applies_to: [claim-berry-welldef-vacuum]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-faraut-koranyi
      kind: paper
      locator: "Faraut & Koranyi, Analysis on Symmetric Cones (1994)"
      aliases: ["Faraut-Koranyi", "FK94"]
      role: background
      why_it_matters: "The cone metric g_X = Hess(-log det) underlying the cone-Hessian diag(9,9,18,18) that CALC-03 SOFT-compares Re(QGT) to. Cited for the REAL-PART character check ONLY (the Re-anchor is never load-bearing for the Im verdict; fp-reuse-cone-hessian)."
      applies_to: [claim-berry-welldef-vacuum]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [cite]
    - id: ref-bulk-geometry-prior
      kind: prior_artifact
      locator: "code/bulk_geometry_verification.py (cone_hessian_at_center, h3_constant_curvature, ricci_decomposition_n4); v17.0 cone-Hessian NONE"
      aliases: ["bulk_geometry_verification.py", "v17.0 cone-Hessian"]
      role: benchmark
      why_it_matters: "Warm engine for the CALC-03 Re-anchor (cone_hessian_at_center -> diag(9,9,18,18); h3_constant_curvature -> round K=-1; Phase-71 cone-Hessian slice K=-1/2) and the CALC-04 trace/traceless/Weyl decomposition (ricci_decomposition_n4). The v17.0 NONE binds ONLY the Re sector, NOT this Im verdict."
      applies_to: [claim-berry-welldef-vacuum]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [read, use]
    - id: ref-peirce-coupling
      kind: prior_artifact
      locator: "code/embedding_under_E_verification.py (E, proj_u_exact, slice_to_complex, cu_to_complex, h3o_from_coords, jordan); peirce_indices_under_E11"
      aliases: ["peirce_indices_under_E11", "embedding_under_E_verification.py"]
      role: benchmark
      why_it_matters: "slice_to_complex maps an h_3(C_u) element -> a 3x3 complex matrix with e_7 -> i: THE bridge that makes Im the literal SymPy imaginary part and carries i symbolically over Q(i). E/proj_u_exact give the C_u reduction. The single key tool of the whole phase."
      applies_to: [claim-berry-welldef-vacuum]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [read, use]
    - id: ref-ring-lemma-engine
      kind: prior_artifact
      locator: "code/ring_lemma_verification.py (det_3 SSOT, Tr, jordan)"
      aliases: ["ring_lemma_verification.py", "det SSOT", "warm engine"]
      role: benchmark
      why_it_matters: "The det SSOT cubic norm + trace form for any Jordan-product / trace step and the Re-anchor; correct octonion association (never octonion_algebra.py)."
      applies_to: [claim-berry-welldef-vacuum]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [use]
  acceptance_tests:
    - id: test-berry-nonzero
      subject: claim-berry-welldef-vacuum
      kind: existence
      procedure: "VALD-03 (do FIRST). (i) Group theory: cite Baez/Berger -- OP^2=F_4/Spin(9) isotropy-irreducible (isotropy = irreducible 16-dim Spin(9) spinor) => no invariant 2-form => the ROUND Berry curvature is identically 0 => any nonzero F_B is BORN FROM THE C_u BREAKING. (ii) Concrete: build the rank-1 C_u idempotent P = vv^dagger/(v^dagger v), v=[1,z1,z2], z1,z2 in C_u (z1=a+ib, z2=c+id); M_C = slice_to_complex(P); Q = Tr(M_C dM_C dM_C) over Q(i); F_B = -2 Im Q. Show F_B is generically NONZERO (not identically the zero matrix) exactly over Q(i) (expect block-diagonal Kahler form, base point F_B[a,b]=F_B[c,d]=-2)."
      pass_condition: "F_B is generically NONZERO over Q(i) AND its nonzero-ness is consistent with born-from-breaking (round Berry = 0 by isotropy-irreducibility; C_u-broken nonzero). If F_B is IDENTICALLY 0 on the slice: report the DEGENERATE (worse-than-SOFT-KILL) outcome flat -- no antisymmetric content at all. Exact over Q(i), no float."
      evidence_required: [deliv-phaseA5, deliv-phaseA5-code, ref-baez-octonions, ref-provost-vallee]
      automation: automated
    - id: test-berry-real-part
      subject: claim-berry-welldef-vacuum
      kind: consistency
      procedure: "CALC-03 (SOFT, NOT a hard stop). Compute Re(QGT) = ½ Tr(dP dP) on the C_u chart over Q(i) (round CP^2-over-C_u gave diag(1,1,1,1)); confirm it is a positive-definite Fubini-Study metric. Compare its non-Einstein CHARACTER (Ricci-eigenvalue PATTERN / non-Einstein-ness / sign structure via ricci_decomposition_n4) to the cone-Hessian (cone_hessian_at_center -> diag(9,9,18,18); h3_constant_curvature -> round K=-1; Phase-71 slice K=-1/2). 'Character' is operational (pattern/sign), NOT numerical identity."
      pass_condition: "Re(QGT) is a sensible positive-definite FS metric (greenlight to trust the QGT object). A character match/mismatch to the cone-Hessian is reported as an INFORMATIVE diagnostic. A DISCREPANCY IS EXPECTED (FS-pullback != cone-Hessian restriction) and is NEVER a KILL/HALT on this anchor alone (fp-reuse-cone-hessian). The ONLY failure here is Re(QGT) NOT positive-definite / not a sensible FS metric (=> the QGT construction is wrong => STOP and re-examine before trusting Im)."
      evidence_required: [deliv-phaseA5, deliv-phaseA5-code, ref-provost-vallee, ref-faraut-koranyi, ref-bulk-geometry-prior]
      automation: automated
    - id: test-berry-vacuum
      subject: claim-berry-welldef-vacuum
      kind: limiting_case
      procedure: "CALC-04. Classify F_B at M=0 exactly over Q(i): zero/flat, pure-Lambda (F_B ~ e^e, the maximally-symmetric Kahler/cosmological form), or other. Decompose into trace/traceless/Weyl (ricci_decomposition_n4 on the associated effective object). The round CP^2-over-C_u Berry form (block-diagonal F_B[a,b]=F_B[c,d]=-2, g=I_4) is the expected pure-Kahler/pure-Lambda character."
      pass_condition: "The M=0 vacuum level is DECIDED exactly (flat / pure-Lambda-Kahler / other) and reported at true strength. Expected pure-Lambda/Kahler (the CP^2 Kahler form). Lambda is NOT reintroduced negative; R x H^3 is NOT reintroduced (the FALSIFIED v17.0 vacuum -- Pitfall 4). A constant ~ e^e form is Lambda/Kahler (vacuum), NOT matter."
      evidence_required: [deliv-phaseA5, deliv-phaseA5-code, ref-provost-vallee]
      automation: automated
  forbidden_proxies:
    - id: fp-reuse-cone-hessian
      subject: claim-berry-welldef-vacuum
      proxy: "Using the v17.0 cone-Hessian symmetric-sector Riemann tensor (the REAL part of the QGT) -- or the CALC-03 Re(QGT) anchor -- as LOAD-BEARING for the Berry / Im-sector verdict: e.g. importing the v17.0 Riemann and calling its non-Einstein-ness the A.5 verdict, or KILLing/HALTing because Re(QGT) differs from the cone-Hessian character."
      reason: "This phase computes the antisymmetric/connection curvature Im(QGT)=F_B -- a DIFFERENT tensor from the cone-Hessian. The cone-Hessian / Re(QGT) is the real-part SOFT consistency check ONLY (FS-pullback != cone-Hessian restriction; v17.0 NONE binds only Re). Treating it as load-bearing re-binds to the v17.0 NONE by sector confusion."
    - id: fp-relabel-vacuum
      subject: claim-berry-welldef-vacuum
      proxy: "Relabeling an identically-zero F_B as 'effectively present', relabeling the constant pure-Lambda/Kahler vacuum form as 'matter curvature', or reintroducing Lambda<0 / R x H^3 as the expected M=0 vacuum."
      reason: "Negative-result-is-success: an identically-zero F_B is a degenerate (worse-than-SOFT-KILL) outcome and must be reported flat. The maximally-symmetric ~ e^e Kahler form is the Lambda/Kahler VACUUM, not matter (matter must vanish as M->0, plan 76-02). Lambda<0 / R x H^3 is the FALSIFIED v17.0 source-field geometry (CONVENTIONS §6)."
    - id: fp-float-decisive
      subject: claim-berry-welldef-vacuum
      proxy: "Any decisive verdict (F_B nonzero-ness, Re(QGT) positive-definiteness, the vacuum classification, the CP^1 pin) resting on a float i, numpy rank, or floating-point eigenvalues."
      reason: "The i of Q(i) is carried SYMBOLICALLY (sympy.I); ranks/eigenvalues/Re/Im via SymPy over Q(i). A float 'approximately nonzero' or float Re/Im split is explicitly forbidden -- exact cancellations (e.g. the e_7->i map producing exact rationals) demand exact arithmetic. Float is triage-only."
    - id: fp-octonion-algebra
      subject: claim-berry-welldef-vacuum
      proxy: "Importing octonion_algebra.py (buggy associator, wrong (x1 x2) x3 order, float, 0.67 associator gap) on any decisive path; OR using the Jordan product ∘ inside the QGT P dP dP instead of the associative complex matrix product on slice_to_complex(P)."
      reason: "octonion_algebra.py is BANNED. det/trace/Jordan come from ring_lemma_verification.py det_3 (SSOT) + embedding_under_E_verification. The QGT uses the ASSOCIATIVE operator product (complex matrix multiply on C_u, forced because C_u≅C is the unique associative completion u=e_7 provides), NOT the Jordan product -- using ∘ gives a different, wrong object."
  links:
    - id: link-berry-vacuum
      source: claim-berry-welldef-vacuum
      target: deliv-phaseA5
      relation: supports
      verified_by: [test-berry-nonzero, test-berry-real-part, test-berry-vacuum]
    - id: link-berry-code
      source: claim-berry-welldef-vacuum
      target: deliv-phaseA5-code
      relation: computes
      verified_by: [test-berry-nonzero, test-berry-vacuum]
  uncertainty_markers:
    weakest_anchors:
      - "The judgment that the C_u-complex realization of P dP dP (slice_to_complex, e_7->i) is THE physically correct QGT (vs an octonionic refinement). Defended: C_u≅C is the unique associative completion u=e_7 provides (so the associative product is forced, not chosen); the CP^1 anchor (F_B=-sin theta/2, g=1/4) pins sign/factor; Approach 2 (octonionic trace-form QGT) is available as a cross-check (low risk -- verified vacuum-side this research)."
      - "CALC-03 'same non-Einstein character': operationally a Ricci-eigenvalue PATTERN / non-Einstein-ness comparison, NOT numerical identity. A discrepancy is EXPECTED (FS-pullback != cone-Hessian restriction) and is INFORMATIVE only -- this anchor is deliberately SOFT and never carries a KILL (fp-reuse-cone-hessian)."
    disconfirming_observations:
      - "F_B identically zero on the slice -> DEGENERATE, worse-than-SOFT-KILL (no antisymmetric content at all); VALD-03 must detect and report it flat."
      - "Re(QGT) NOT positive-definite / not a sensible Fubini-Study metric -> the QGT construction or the family is wrong -> STOP and re-examine before trusting Im(QGT)."
      - "Re(QGT) character wildly different from the cone-Hessian -> DIAGNOSE (FS-pullback != cone-Hessian restriction), do NOT KILL (soft anchor)."
      - "Any nonzero octonion comp e_1..e_6 surviving pi_u, or Im(QGT) nonzero where the family should be real -> the C_u projection / slice_to_complex map is mis-implemented; re-check before trusting any verdict."
      - "M=0 vacuum classified as Lambda<0 / R x H^3 -> a vacuum mis-read (the FALSIFIED v17.0 object); subtract correctly and reclassify (the expected vacuum is flat / pure-Lambda-Kahler)."

estimated_execution:
  total_minutes: 75
  breakdown:
    - task: 1
      minutes: 25
      note: "Harness + source guard + CP^1 convention pin + rank-1 C_u idempotent family + projector-form QGT over Q(i) (warm engines; <5s compute verified this research, time is in wiring/verification)"
    - task: 2
      minutes: 30
      note: "VALD-03 (born-from-breaking + generically-nonzero) FIRST, then CALC-03 (SOFT Re-anchor vs cone-Hessian character) -- symbolic Re/Im split + ricci_decomposition_n4; the 1.3x symbolic+numerical-mixed multiplier applies"
    - task: 3
      minutes: 20
      note: "CALC-04 vacuum classification (trace/traceless/Weyl) + write PART 1 of the derivation"
---

<objective>
PART 1 of the v18.0 SOFT-KILL gate (Phase A.5), the CHEAP/VACUUM half: establish, exactly
over Q(i) and BEFORE any matter is turned on, that the canonical Berry curvature
F_B = Im(QGT) = -2 Im Tr(P dP dP) of the rank-1 C_u-idempotent family E(x) is (VALD-03)
well-defined and generically NONZERO on the 4d slice {11,18,19,26}=C_u^2, BORN FROM the
C_u breaking of the isotropy-irreducible OP^2=F_4/Spin(9) (which carries no invariant
2-form, so the round Berry curvature vanishes); run the SOFT real-part anchor (CALC-03,
Re(QGT) = a Fubini-Study metric of the same non-Einstein CHARACTER as the cone-Hessian --
INFORMATIVE, never a KILL); and classify the M=0 vacuum (CALC-04: flat / pure-Lambda-Kahler
/ other). The decisive matter-on VALD-04 Einstein-vs-EM same-wall verdict + the explicit
SOFT KILL / SURVIVES line live in plan 76-02 (depends_on this plan).

Purpose: This is the cheapest part of the conjunctive A.5 gate (the vacuum side is verified
<5s this research). It (a) PINS the QGT convention against the CP^1 anchor before any
h_3(O) verdict, (b) confirms the Berry 2-form even EXISTS and is nonzero (a degenerate
F_B ≡ 0 would be a worse-than-SOFT failure to catch HERE), and (c) fixes the vacuum baseline
that 76-02's matter part must be measured AGAINST. A.5 is CONJUNCTIVE with Phase 75 (✓
SURVIVES): BOTH must SURVIVE to greenlight Phase B (77).

THE TWO DESIGN POINTS (do not get these wrong):
  1. The QGT uses the ASSOCIATIVE complex matrix product on slice_to_complex(P) (e_7 -> i),
     NOT the Jordan product ∘ -- C_u ≅ C is the unique associative completion u=e_7 provides,
     so the associative product is FORCED. F_B = Im(QGT) is the literal SymPy imaginary part.
  2. The Re(QGT) = cone-Hessian comparison (CALC-03) is a SOFT, INFORMATIVE diagnostic ONLY
     (FS-pullback != cone-Hessian restriction). A discrepancy is EXPECTED and is NEVER a KILL.
     The v17.0 NONE binds the REAL part; this phase mines the IMAGINARY part (fp-reuse-cone-hessian).

Output:
- code/cartan_phaseA5_berry.py -- exact-over-Q(i) SymPy driver (PART 1): the Phase-75 harness
  (_report/_source_guard), the CP^1 pin, the rank-1 C_u idempotent family + projector QGT,
  the Re/Im split, VALD-03, CALC-03, CALC-04. (Plan 76-02 APPENDS the matter-on machinery.)
- derivations/76-berry-same-wall.tex -- PART 1: the recipe + CP^1 pin, VALD-03, CALC-03 (soft),
  CALC-04. (Plan 76-02 appends VALD-04 + the SOFT KILL / SURVIVES verdict.)
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/CONVENTIONS.md
@.gpd/phases/76-phase-a-5-berry-curvature-same-wall-gate-soft-kill/76-RESEARCH.md
@.gpd/phases/75-phase-a-coframe-reduction-dealbreaker-the-kill-gate/75-01-SUMMARY.md
@code/cartan_phaseA_coframe.py
@code/embedding_under_E_verification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harness + source guard + CP^1 convention pin + rank-1 C_u idempotent family + projector-form QGT over Q(i)</name>
  <files>code/cartan_phaseA5_berry.py</files>
  <action>
    Create the exact-over-Q(i) driver by COPYING the harness pattern from code/cartan_phaseA_coframe.py
    (the _report(label, ok) latch, the _source_guard(), the unbuffered/print-each-check style,
    V_HALF_IDX = list(range(11, 27)), and the C_u survivors {11,18,19,26}). Reuse the warm engines
    -- do NOT rebuild octonion arithmetic:
      import bulk_geometry_verification as BG   # _standard_basis_27, _flat27, cone_hessian_at_center, h3_constant_curvature, ricci_decomposition_n4, peirce_indices_under_E11
      import embedding_under_E_verification as EMB   # E, proj_u_exact(a, u_index=7), slice_to_complex, cu_to_complex, h3o_from_coords, jordan
      import ring_lemma_verification as RL   # Tr, jordan, det_3 (det SSOT)
    SOURCE GUARD (inherit Phase 0/75): _source_guard() asserts NO `import octonion_algebra` anywhere
    on the decisive path (grep the imported modules + assert 'octonion_algebra' not in sys.modules
    after import). fp-octonion-algebra.

    Run foreground UNBUFFERED (`python3 -u code/cartan_phaseA5_berry.py`); commit task-by-task
    (the v17.0/Phase-75 watchdog discipline -- a long no-output symbolic simplify can be killed).

    1. CP^1 CONVENTION PIN (do this BEFORE any h_3(O) object -- defeats the Pitfall-5 normalization slip):
       build the 2x2 CP^1 projector P(theta,phi) = ½[[1+cos th, sin th e^{-i phi}],[sin th e^{i phi}, 1-cos th]]
       with theta, phi real sympy symbols and sympy.I for i. Compute Q_{mu nu} = Tr(P d_mu P d_nu P)
       exactly; verify EXACTLY (over the symbolic field): Q_theta theta = 1/4, Q_phi phi = sin^2 th /4,
       Q_theta phi = i sin th /4, and F_B = i Tr(P[d_theta P, d_phi P]) = -2 Im Q_theta phi = -sin th /2,
       and g_theta theta = Re Q_theta theta = 1/4. This PINS the sign and factor-2 of the whole recipe.
       Define helper functions QGT(P, params) -> Matrix, berry_F(P, params) -> antisymmetric Matrix,
       fs_metric(P, params) -> symmetric Matrix, using berry_F = -2*im(QGT) and fs_metric = re(QGT),
       and assert they reproduce the CP^1 anchor before any reuse.

    2. RANK-1 C_u IDEMPOTENT FAMILY (the vacuum chart, de-risked this research):
       v(z1,z2) = [1, z1, z2] with z1 = a + I*b, z2 = c + I*d (a,b,c,d real sympy symbols);
       P_C = v v^dagger / (v^dagger v)  -- a 3x3 sympy COMPLEX matrix (this IS slice_to_complex of
       the C_u rank-1 idempotent; the i is sympy.I = e_7 symbolic over Q(i)).
       Verify rank-1 idempotent EXACTLY over Q(i): simplify(P_C*P_C - P_C) == zero 3x3 and
       Tr(P_C) == 1 (use sympy cancel/simplify; this was verified clean this research).
       Cross-check the bridge: build the corresponding h_3(C_u) element via EMB.h3o_from_coords with
       C_u entries and confirm EMB.slice_to_complex of it equals P_C (the e_7 -> i map is faithful;
       assert no octonion comp e_1..e_6 survives -- if any do, the C_u projection is mis-implemented, STOP).

    3. THE 4 BASE DIRECTIONS: the differentiation parameters mu, nu are (a,b,c,d) = the 4 reduced
       coframe directions, the C_u^2 survivors {11,18,19,26} from Phase 75 (the pi_u-image of V_{1/2}).
       Build the 4x4 QGT Q(a,b,c,d) = [Tr(P_C d_mu P_C d_nu P_C)]_{mu,nu in (a,b,c,d)} over Q(i),
       using sympy diff. Keep it symbolic; use cancel (not full simplify) to control blow-up.

    Use rational basepoints throughout. Float is permitted ONLY as pre-simplify triage (which entries
    are nonzero before exact simplify), NEVER as a decisive value (fp-float-decisive).
  </action>
  <verify>
    1. CP^1 pin EXACT: Q_theta theta == 1/4, Q_phi phi == sin^2 th/4, Q_theta phi == I*sin th/4,
       F_B == -sin th/2, g_theta theta == 1/4 (sign/factor pinned; the SymPy re/im split works).
    2. Rank-1 idempotent EXACT over Q(i): P_C^2 - P_C == 0 (3x3 zero) and Tr(P_C) == 1.
    3. Bridge faithful: EMB.slice_to_complex(h_3(C_u) element) == P_C; zero octonion comps e_1..e_6 survive pi_u.
    4. The 4x4 QGT Q(a,b,c,d) assembles over Q(i) (antisymmetric Im part, symmetric Re part by construction).
    5. Source guard: 0 octonion_algebra imports on the decisive path; 0 numpy/float ranks (sympy only);
       NO Jordan product ∘ inside the QGT (associative complex matrix multiply only).
    6. Order-of-expectation met: the CP^1 monopole F_B = -sin th/2 (int = -2pi) reproduced; the recipe is calibrated.
  </verify>
  <done>
    Driver code/cartan_phaseA5_berry.py created from the Phase-75 harness with the octonion_algebra source
    guard active; the CP^1 convention pin (F_B = -sin theta/2, g_theta theta = 1/4) reproduced EXACTLY over
    Q(i); the rank-1 C_u idempotent family P_C = vv^dagger/(v^dagger v) verified (P_C^2-P_C==0, Tr==1) with the
    slice_to_complex (e_7 -> i) bridge confirmed faithful (no e_1..e_6 leak); the 4x4 projector-form QGT
    Q(a,b,c,d) assembled over Q(i) on the 4 C_u^2 base directions. No fp-float-decisive, no fp-octonion-algebra,
    no Jordan product in the QGT.
  </done>
</task>

<task type="auto">
  <name>Task 2: VALD-03 (well-definedness: born-from-breaking + generically NONZERO) FIRST, then CALC-03 (SOFT Re(QGT) same-character anchor)</name>
  <files>code/cartan_phaseA5_berry.py</files>
  <action>
    Append to code/cartan_phaseA5_berry.py. ORDER MATTERS: VALD-03 (well-definedness) is done FIRST,
    BEFORE any shape test; CALC-03 is the SOFT real-part anchor.

    (A) VALD-03 -- F_B WELL-DEFINED AND GENERICALLY NONZERO, BORN FROM THE C_u BREAKING:
      (i) BORN-FROM-BREAKING (group theory, state in the derivation, cite Baez/Berger): OP^2 = F_4/Spin(9)
          is isotropy-irreducible -- the isotropy rep is the unique irreducible 16-dim Spin(9) spinor =>
          there is a UNIQUE invariant symmetric form (the Fubini-Study metric) AND NO invariant 2-form =>
          the ROUND Berry curvature is identically 0. Therefore any nonzero F_B MUST be born from the C_u
          breaking (the C_u-reduced isotropy DOES admit the Kahler 2-form). This is the load-bearing fact.
      (ii) GENERICALLY NONZERO (compute, exact over Q(i)): F_B = berry_F(P_C, (a,b,c,d)) -- the 4x4
          antisymmetric matrix = -2 Im Q. Evaluate at the base point a=b=c=d=0 and confirm it is NONZERO
          (expect the block-diagonal CP^2 Kahler form F_B[a,b] = F_B[c,d] = -2, off-blocks 0 -- verified
          this research). Also confirm F_B is not identically 0 as a symbolic matrix (some entry is a
          nonzero rational function). 
      (iii) DEGENERACY GUARD: if F_B IS identically the zero 4x4 matrix on the slice, this is the
          DEGENERATE (worse-than-SOFT-KILL) outcome -- _report it FLAT ("VALD-03: F_B identically zero ->
          degenerate, no antisymmetric content"). Do NOT relabel it (fp-relabel-vacuum).

    (B) CALC-03 -- THE SOFT REAL-PART ANCHOR (INFORMATIVE, NOT A KILL):
      - Compute Re(QGT) = fs_metric(P_C, (a,b,c,d)) = ½ Tr(dP_C dP_C); evaluate at the base point
        (round CP^2-over-C_u gave diag(1,1,1,1)) and confirm it is a POSITIVE-DEFINITE Fubini-Study metric
        (sympy eigenvals over Q(i) all > 0). This positive-definiteness IS a real check: if Re(QGT) is NOT
        a sensible positive-definite FS metric, the QGT construction/family is wrong -> STOP and re-examine
        before trusting Im (this is the ONLY hard failure in CALC-03).
      - CHARACTER comparison to the cone-Hessian (SOFT): get the cone-Hessian via BG.cone_hessian_at_center()
        (expect diag(9,9,18,18)) and the round H^3 curvature via BG.h3_constant_curvature() (expect K=-1;
        Phase-71 cone-Hessian slice K=-1/2). Compare the non-Einstein CHARACTER -- the Ricci-eigenvalue
        PATTERN / non-Einstein-ness / sign structure (use BG.ricci_decomposition_n4 on the FS metric's
        curvature if a curvature is needed; otherwise compare the eigenvalue-multiplicity pattern) -- NOT
        numerical identity. _report it as a DIAGNOSTIC.
      - GUARD (fp-reuse-cone-hessian): state explicitly in the print/_report and the derivation that a
        discrepancy between Re(QGT) and the cone-Hessian character is EXPECTED (FS-pullback != cone-Hessian
        restriction) and INFORMATIVE, and is NEVER a KILL/HALT on this anchor alone. Type-guard: label this
        block "Re / symmetric-sector SOFT anchor", distinct from the Im verdict.
  </action>
  <verify>
    1. VALD-03 born-from-breaking stated and cited (Baez/Berger): OP^2 isotropy-irreducible => no invariant
       2-form => round Berry = 0 => F_B born from C_u breaking. Consistent (round vanishes; C_u-broken nonzero).
    2. VALD-03 nonzero EXACT over Q(i): F_B at the base point is NONZERO (expect F_B[a,b]=F_B[c,d]=-2,
       off-blocks 0); F_B not identically the zero matrix. Degeneracy guard wired (would report flat if F_B≡0).
    3. CALC-03 Re(QGT) positive-definite FS metric (eigenvals > 0 over Q(i); round base diag(1,1,1,1)). If NOT
       positive-definite -> STOP (the only hard failure here).
    4. CALC-03 character comparison to cone-Hessian (diag(9,9,18,18); round K=-1) reported as an INFORMATIVE
       SOFT diagnostic, with the explicit fp-reuse-cone-hessian guard ("discrepancy expected, NOT a KILL").
    5. No fp-float-decisive: F_B, Re(QGT), eigenvalues all from sympy over Q(i), never numpy/float.
    6. Order-of-expectation met: F_B generically nonzero (VALD-03 PASS); Re(QGT) a sensible positive-definite
       FS metric of broadly non-Einstein character (CALC-03 SOFT diagnostic).
  </verify>
  <done>
    VALD-03 (done FIRST) reports F_B = -2 Im Q is well-defined and generically NONZERO on the 4d slice
    (base point F_B[a,b]=F_B[c,d]=-2), BORN FROM the C_u breaking (OP^2 isotropy-irreducible => round Berry=0),
    with the F_B≡0 degeneracy guard wired to report flat. CALC-03 reports Re(QGT) as a positive-definite
    Fubini-Study metric and compares its non-Einstein CHARACTER to the cone-Hessian as an explicitly SOFT,
    INFORMATIVE diagnostic (fp-reuse-cone-hessian guard active -- a discrepancy is expected and NEVER a KILL).
    Exact over Q(i); the Re (symmetric) block is type-separated from the Im (antisymmetric) verdict.
  </done>
</task>

<task type="auto">
  <name>Task 3: CALC-04 (M=0 vacuum classification of F_B) + write PART 1 of the derivation</name>
  <files>code/cartan_phaseA5_berry.py, derivations/76-berry-same-wall.tex</files>
  <action>
    (A) CALC-04 -- M=0 VACUUM CLASSIFICATION (append to code/cartan_phaseA5_berry.py):
      Classify F_B at M=0 exactly over Q(i): is it zero/flat, pure-Lambda (F_B ~ e^e -- the maximally-
      symmetric Kahler/cosmological form), or other? The round CP^2-over-C_u Berry form is block-diagonal
      and maximally symmetric (F_B[a,b]=F_B[c,d]=-2, g=I_4) -- the expected pure-Kahler/pure-Lambda character.
      - Decompose the vacuum F_B (and/or its associated effective curvature object) into trace/traceless/Weyl
        via BG.ricci_decomposition_n4 (or, for the 2-form, test proportionality to the Kahler form e^e built
        from the FS metric). Report whether it is pure ~ e^e (=> pure-Lambda/Kahler) or carries structure beyond.
      - GUARD (fp-relabel-vacuum + Pitfall 4): a constant ~ e^e form is the Lambda/Kahler VACUUM, NOT matter.
        Do NOT call it "matter curvature" (matter must vanish as M->0 -- that is plan 76-02). Do NOT reintroduce
        Lambda<0 / R x H^3 (the FALSIFIED v17.0 source-field geometry, CONVENTIONS §6). Report the vacuum level
        at true strength.
      - Emit the vacuum F_B (the base-point value AND the symbolic slice form) so plan 76-02 can SUBTRACT it to
        isolate the matter part (the matter F_B must vanish as M->0). Pre-register this hand-off.

    (B) WRITE PART 1 OF THE DERIVATION (derivations/76-berry-same-wall.tex):
      Start with an ASSERT_CONVENTION header mirroring 52-kkt-spacetime / 75-coframe-reduction (natural units;
      metric_signature = mostly_minus (+,-,-,-); det SSOT = ring_lemma det_3; u = e_7; E_11 = diag(1,0,0);
      QGT over Q(i) with i symbolic; Lambda = 0). The derivation MUST contain, at true strength:
      (a) THE RECIPE + CP^1 PIN: P = slice_to_complex(E(x)) (e_7 -> i, i symbolic over Q(i)); Q = Tr(P dP dP);
          g = Re Q; F_B = -2 Im Q; the CP^1 anchor F_B = -sin theta/2, g_theta theta = 1/4 reproduced exactly.
          State the two design points: associative complex product (NOT Jordan ∘); F_B is the literal Im.
      (b) VALD-03: F_B well-defined and generically NONZERO, BORN FROM the C_u breaking (OP^2 isotropy-irreducible
          => no invariant 2-form => round Berry=0; cite Baez/Berger); the F_B≡0 degeneracy outcome named.
      (c) CALC-03 (SOFT): Re(QGT) a positive-definite FS metric; its non-Einstein CHARACTER vs the cone-Hessian
          (diag(9,9,18,18); round K=-1) reported as an INFORMATIVE diagnostic, EXPLICITLY not a KILL
          (fp-reuse-cone-hessian; FS-pullback != cone-Hessian restriction; v17.0 NONE binds only Re).
      (d) CALC-04: the M=0 vacuum class (flat / pure-Lambda-Kahler / other), trace/traceless/Weyl; Lambda not
          reintroduced negative; the constant ~ e^e form named as the Lambda/Kahler VACUUM (not matter).
      (e) A "PART 2 (plan 76-02)" stub stating that the decisive matter-on VALD-04 Einstein-vs-EM same-wall
          verdict and the explicit SOFT KILL / SURVIVES line are appended there (conjunctive with Phase 75 for
          the Phase-77 greenlight). Do NOT pre-empt the VALD-04 verdict here.
      Explicitly REJECT each forbidden proxy in the PART-1 statement: NO fp-reuse-cone-hessian (the Re-anchor is
      SOFT, never load-bearing for the Im verdict); NO fp-relabel-vacuum (F_B≡0 reported flat; the ~e^e form is
      Lambda/Kahler vacuum not matter; no Lambda<0/R x H^3); NO fp-float-decisive; NO fp-octonion-algebra.
  </action>
  <verify>
    1. CALC-04 vacuum DECIDED exactly over Q(i): flat / pure-Lambda-Kahler / other, reported at true strength
       (expected pure-Lambda/Kahler -- the CP^2 Kahler form). trace/traceless/Weyl decomposition done.
    2. fp-relabel-vacuum guard: the ~e^e constant form is named as the Lambda/Kahler VACUUM, NOT matter; NO
       Lambda<0 / R x H^3 reintroduced (CONVENTIONS §6 honored).
    3. Vacuum F_B emitted (base-point + symbolic) and the subtraction hand-off to plan 76-02 pre-registered.
    4. Derivation written with ASSERT_CONVENTION header and all four PART-1 results (recipe+CP^1 pin, VALD-03,
       CALC-03 soft, CALC-04) + the PART-2 stub; every forbidden proxy explicitly rejected; no VALD-04 verdict
       pre-empted.
    5. No fp-float-decisive (all decisive numbers rational/symbolic over Q(i)); no fp-octonion-algebra.
  </verify>
  <done>
    CALC-04 classifies the M=0 vacuum F_B exactly over Q(i) (expected pure-Lambda/Kahler, the CP^2 Kahler form),
    decomposed trace/traceless/Weyl, with the ~e^e form named as the Lambda/Kahler VACUUM (not matter) and no
    Lambda<0/R x H^3 reintroduced; the vacuum F_B is emitted for plan 76-02 to subtract. derivations/76-berry-same-wall.tex
    PART 1 is written (recipe + CP^1 pin; VALD-03 born-from-breaking + nonzero; CALC-03 SOFT Re-anchor; CALC-04
    vacuum) with the PART-2 stub and every forbidden proxy explicitly rejected, no VALD-04 verdict pre-empted.
  </done>
</task>

</tasks>

<verification>
- EXACT over Q(i) on EVERY decisive number (sympy diff/re/im/Matrix.rank/.eigenvals over QQ or Q(i); the i is
  sympy.I, symbolic; never numpy/float) -- fp-float-decisive guard.
- octonion_algebra.py ABSENT on the decisive path (source guard inherited from Phase 0/75); the QGT uses the
  ASSOCIATIVE complex matrix product on slice_to_complex(P), NEVER the Jordan product ∘ -- fp-octonion-algebra guard.
- The CP^1 anchor (F_B = -sin theta/2, g_theta theta = 1/4) is reproduced exactly BEFORE any h_3(O) verdict -- the
  sign/factor convention pin (defeats the Pitfall-5 normalization slip).
- VALD-03 done FIRST (well-definedness before any shape test): F_B generically NONZERO and BORN FROM the C_u
  breaking (OP^2 isotropy-irreducible => round Berry=0); a degenerate F_B≡0 is detected and reported flat.
- CALC-03 is SOFT: Re(QGT) positive-definite FS metric is a real check, but the cone-Hessian character comparison
  is INFORMATIVE only and NEVER a KILL on this anchor alone (FS-pullback != cone-Hessian restriction;
  fp-reuse-cone-hessian). The Re (symmetric) block is type-separated from the Im (antisymmetric) verdict.
- CALC-04 vacuum: the constant ~e^e Kahler form is the Lambda/Kahler VACUUM, NOT matter; Lambda is NOT
  reintroduced negative; R x H^3 is NOT reintroduced (the FALSIFIED v17.0 vacuum) -- fp-relabel-vacuum guard.
- This plan does NOT pre-empt the decisive VALD-04 verdict (matter-on Einstein-vs-EM) -- that is plan 76-02.
- Order-of-expectation stated for each decisive quantity (CP^1 pin; F_B generically nonzero; Re(QGT) positive-
  definite; M=0 vacuum pure-Lambda/Kahler) and decided exactly, never assumed.
</verification>

<success_criteria>
PART 1 of Phase A.5 is DECIDED exactly over Q(i), at true strength, with the vacuum baseline fixed for plan 76-02:

1. The QGT recipe is CALIBRATED: the CP^1 anchor F_B = -sin theta/2, g_theta theta = 1/4 is reproduced exactly
   over Q(i) (sign/factor pinned), the rank-1 C_u idempotent family is verified (P_C^2 - P_C == 0, Tr == 1), and
   the slice_to_complex (e_7 -> i) bridge is confirmed faithful (no e_1..e_6 leak).
2. VALD-03 (done FIRST): F_B = -2 Im Q is well-defined and generically NONZERO on the 4d slice (base point
   F_B[a,b]=F_B[c,d]=-2), BORN FROM the C_u breaking (OP^2 isotropy-irreducible => round Berry=0). A degenerate
   F_B≡0 (worse-than-SOFT-KILL) would be detected and reported flat (it is not expected).
3. CALC-03 (SOFT): Re(QGT) is a positive-definite Fubini-Study metric (a real check); its non-Einstein character
   vs the cone-Hessian (diag(9,9,18,18); round K=-1) is reported as an INFORMATIVE diagnostic, NEVER a KILL
   (fp-reuse-cone-hessian). The only hard failure here is Re(QGT) not positive-definite.
4. CALC-04: the M=0 vacuum class of F_B is decided exactly (expected pure-Lambda/Kahler, the CP^2 Kahler form),
   trace/traceless/Weyl; the ~e^e form is named the Lambda/Kahler VACUUM (not matter); no Lambda<0 / R x H^3.
5. deliv-phaseA5 (PART 1) + deliv-phaseA5-code carry all four PART-1 results with every forbidden proxy
   (fp-reuse-cone-hessian, fp-relabel-vacuum, fp-float-decisive, fp-octonion-algebra) explicitly rejected, the
   vacuum F_B emitted for 76-02's subtraction, and NO VALD-04 verdict pre-empted. Negative-result-is-success: a
   degenerate or vacuum-mis-read outcome is reported flat, never relabeled.
</success_criteria>

<output>
After completion, create `.gpd/phases/76-phase-a-5-berry-curvature-same-wall-gate-soft-kill/76-01-SUMMARY.md`.
</output>
