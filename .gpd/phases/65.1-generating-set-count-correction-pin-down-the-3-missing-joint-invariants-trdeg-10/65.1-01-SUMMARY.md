---
phase: 65.1-generating-set-count-correction-pin-down-the-3-missing-joint-invariants-trdeg-10
plan: 01
depth: full
one-liner: "Confirmed by exact computation over Q that {6 pointwise, c, Tr(X^2 o Y), Tr(X o Y^2), Tr(X^2 o Y^2)} realizes Jacobian rank 10 = the Phase-65 orbit-derived trdeg (two-route agreement); the joint F_4-invariant generating set is FIELD-level complete at trdeg 10."
subsystem: [formalism, validation, computation]
tags: [invariant-theory, F4, albert-algebra, jacobian-criterion, transcendence-degree, exact-arithmetic, derksen-kemper]

requires:
  - phase: 65-f-4-construction-orbit-dimension-gate
    provides: "Pair orbit dim 44 -> orbit-derived trdeg 54-44=10 (the number the candidate Jacobian rank must match); certified f_4 builder + exact-rank machinery (exact_qq_rank, PAIR_POINTS, _select_independent_basis)"
  - phase: 64-setup-conventions-exact-engine
    provides: "Frozen exact-SymPy engine (jordan, corrected det_3, Tr, Tr2, c, polarize_d, X_from_symbols, 54-symbol layout, 7 base invariants)"
provides:
  - "The corrected trdeg-10 joint generating set with bidegree table: 6 pointwise (1,0)(2,0)(3,0)(0,1)(0,2)(0,3) + c (1,1) + Tr(X^2 o Y)(2,1) + Tr(X o Y^2)(1,2) + Tr(X^2 o Y^2)(2,2)"
  - "Two-route confirmation trdeg=10 (invariant-Jacobian rank here == orbit-tangent rank in Phase 65)"
  - "FIELD-level functional completeness (NOT ring generation -- that is Phase 68)"
  - "Tier ladder (6,7,8,9,10): 6-pointwise==6, +c==7 (Phase-66 SPINE preview), each mixed adds +1"
affects: [66, 67, 68]

methods:
  added: ["Derksen-Kemper char-0 Jacobian criterion as the DUAL of the orbit-dimension computation (generic Jacobian rank of f_1..f_k = trdeg of the field they generate)"]
  patterns: ["substitute-first 10x54 candidate Jacobian -> DomainMatrix-over-QQ exact rank (MAX over >=3 generic integer pairs)", "F_4-invariance gate (D_M f=0 for all 52 generators) as a CORRECTNESS precondition before any rank", "fine-grained prefix-rank tier ladder to localize independence", "pre-registered trdeg target as a CHECK constant, never an input"]

key-files:
  created: [code/ring_generating_set.py]
  modified: []

key-decisions:
  - "The 3 NATURAL mixed trace monomials Tr(X^2 o Y), Tr(X o Y^2), Tr(X^2 o Y^2) complete the set to trdeg 10 -- the deterministic fallback ladder was a recorded NO-OP (rank already 10)."
  - "Established FIELD-level (trdeg) completeness only; ring generation (Hilbert series / Krull / minimal generators) is explicitly OUT OF SCOPE and deferred to Phase 68."

patterns-established:
  - "Candidate-Jacobian rank as the exact DUAL of the certified orbit-tangent rank: two independent routes cross-check trdeg."
  - "F_4-invariance (D_M f=0, all 52 generators, >=3 octonionic points) gated BEFORE any rank -- a non-invariant candidate is rejected as not an invariant at all."

conventions:
  - "Arithmetic: EXACT over Q (sympy.Rational / DomainMatrix-over-QQ); ranks via exact_qq_rank, NEVER numpy.linalg.matrix_rank on the decisive path"
  - "jordan(A,B) = (1/2)(AB+BA); X^2 := jordan(X,X); octonion Fano e1.e2=e4"
  - "det_3 cross term 2*Re((x2.x1).x3) (Phase-64.1 corrected generic norm)"
  - "F_4 = Aut(h_3(O)), 52-dim; 54-symbol pair layout xs=x0:26 (X), ys=y0:26 (Y), per-copy [alpha,beta,gamma,x1(8),x2(8),x3(8)]"
  - "Jacobian criterion (Derksen-Kemper char-0): trdeg of the subfield generated by f_1..f_k = generic rank over Q of the k x 54 Jacobian"

plan_contract_ref: ".gpd/phases/65.1-generating-set-count-correction-pin-down-the-3-missing-joint-invariants-trdeg-10/65.1-01-PLAN.md#/contract"
contract_results:
  claims:
    claim-mixed-complete:
      status: passed
      summary: "The 3 natural mixed trace monomials Tr(X^2 o Y) (2,1), Tr(X o Y^2) (1,2), Tr(X^2 o Y^2) (2,2) are functionally independent of {6 pointwise + c} and complete the candidate set to transcendence degree 10. No fallback was needed (the tier ladder is (6,7,8,9,10), every increment +1)."
      linked_ids: [deliv-harness, deliv-generating-set, test-full-rank-10, test-tier-increments, test-grouping-unambiguous, test-bidegree, test-f4-invariance]
      evidence:
        - verifier: gpd-executor
          method: exact Jacobian rank over Q (substitute-first, DomainMatrix-over-QQ), MAX over 4 generic integer pairs
          confidence: high
          claim_id: claim-mixed-complete
          deliverable_id: deliv-harness
          acceptance_test_id: test-full-rank-10
          reference_id: ref-derksen-kemper
          evidence_path: "code/ring_generating_set.py"
    claim-two-route-agreement:
      status: passed
      summary: "Full candidate Jacobian rank (MAX over 4 generic integer pairs, exact over Q) = 10 == the Phase-65 orbit-derived transcendence degree 54-44 = 10. Two INDEPENDENT routes (orbit-tangent rank in Phase 65 vs invariant-Jacobian rank here) agree on trdeg = 10."
      linked_ids: [deliv-harness, test-full-rank-10, test-trdeg-match, test-three-domain, test-exact-only]
      evidence:
        - verifier: gpd-executor
          method: two-route cross-check (candidate-Jacobian trdeg vs orbit-derived trdeg) + three-exact-domain rank cross-check at width 54
          confidence: high
          claim_id: claim-two-route-agreement
          deliverable_id: deliv-harness
          acceptance_test_id: test-trdeg-match
          reference_id: ref-phase65-orbit
          evidence_path: "code/ring_generating_set.py"
    claim-field-not-ring:
      status: passed
      summary: "What is established is FIELD-LEVEL functional completeness (the invariant field R(27+27)^{F_4} is algebraic over Q(f_1..f_10), trdeg-saturating). This is NOT ring generation -- no Hilbert-series / Krull / minimal-generator certificate is claimed; ring generation is Phase 68. The boundary is stated explicitly in both the harness handoff print and this SUMMARY."
      linked_ids: [deliv-generating-set, test-scope-boundary]
  deliverables:
    deliv-harness:
      status: passed
      path: code/ring_generating_set.py
      summary: "Exact-over-Q harness: builds the 10 candidate invariants as SymPy polynomials in the 54 engine symbols; runs the grouping/F_4-invariance/bidegree correctness gates; computes the full 10x54 Jacobian rank (substitute-first -> exact_qq_rank, MAX over 4 generic pairs); the tier increments (6->7->8->9->10); the two-route trdeg cross-check; the three-exact-domain width-54 cross-check; the module-local exact-only guard; the conditional fallback ladder (no-op here); and the handoff print. Imports the FROZEN engine + certified gate machinery; touches NO octonion_algebra.py. main() exits 0 on the clean pass."
      linked_ids: [claim-mixed-complete, claim-two-route-agreement, test-full-rank-10, test-exact-only]
    deliv-generating-set:
      status: passed
      path: code/ring_generating_set.py
      summary: "The harness prints the confirmed trdeg-10 generating set as a structured handoff: ordered invariant list + bidegree table, realized tier ladder (6,7,8,9,10), two-route agreement statement, explicit FIELD-vs-RING boundary note, and Phase 66/67/68 wiring. Carried verbatim in this SUMMARY (see Next Phase Readiness)."
      linked_ids: [claim-mixed-complete, claim-field-not-ring, test-tier-increments, test-scope-boundary]
  acceptance_tests:
    test-full-rank-10:
      status: passed
      summary: "MAX exact Jacobian rank over Q across 4 generic integer pairs == 10 (each pair individually = 10: P1xP2=10, P2xP3=10, P1xP3=10, P4xP5=10). Not 9 (no fallback), not >10 (no bug). The success path."
      linked_ids: [claim-two-route-agreement, deliv-harness, ref-derksen-kemper, ref-phase65-orbit]
    test-trdeg-match:
      status: passed
      summary: "Candidate-Jacobian trdeg (10) == Phase-65 orbit-derived trdeg (54-44=10) == pre-registered TRDEG_TARGET (10). Two independent routes agree."
      linked_ids: [claim-two-route-agreement, deliv-harness, ref-phase65-orbit, ref-derksen-kemper]
    test-tier-increments:
      status: passed
      summary: "Fixed-order prefix ranks (MAX over pairs): r6=6 (6-pointwise = 3 X-block + 3 Y-block, single-copy trdeg-3 cross-check), r7=7 (+c, previews Phase-66 SPINE), r8=8, r9=9, r10=10. Every increment in {0,1}; ladder (6,7,8,9,10), all four increments +1 (no redundant candidate). r10 == MAX_RANK from test-full-rank-10."
      linked_ids: [claim-mixed-complete, deliv-harness, ref-phase65-orbit]
    test-grouping-unambiguous:
      status: passed
      summary: "Tr((X o X) o Y) - Tr(X o (X o Y)) == 0 exactly over Q at a generic octonionic point (trace bilinear form associative even though jordan is not) -- the mixed-monomial builders have no grouping/ordering ambiguity."
      linked_ids: [claim-mixed-complete, deliv-harness, ref-engine]
    test-bidegree:
      status: passed
      summary: "All 10 candidates verified homogeneous of the claimed bidegree by s,t scaling: the 7 base reproduce (1,0)(2,0)(3,0)(0,1)(0,2)(0,3)(1,1); the 3 mixed give Tr(X^2 o Y)~s^2 t^1, Tr(X o Y^2)~s^1 t^2, Tr(X^2 o Y^2)~s^2 t^2."
      linked_ids: [claim-mixed-complete, deliv-harness]
    test-f4-invariance:
      status: passed
      summary: "STRONG correctness gate, run BEFORE any rank: every one of the 52-independent f_4 basis generators annihilates each of f_21, f_12, f_22 (D_M f = 0 exactly over Q; worst-case killed = 52/52) at >=3 genuinely-octonionic rational points + 2 independent (X!=Y) pairs. The 3 mixed are certified genuine F_4 invariants."
      linked_ids: [claim-mixed-complete, deliv-harness, ref-engine, ref-gate-machinery]
    test-three-domain:
      status: passed
      summary: "Three-exact-domain cross-check at P1xP2: candidate 10x54 Jacobian QQ-frac / QQ-int / ZZ-int = (10,10,10) all agree; the gate f_4-tangent route cross-check = (44,44,44) all agree. Certifies exact_qq_rank is EXACT over Q at width 54, not a float proxy."
      linked_ids: [claim-two-route-agreement, deliv-harness, ref-gate-machinery]
    test-exact-only:
      status: passed
      summary: "Module-local exact-only guard scanning code/ring_generating_set.py's own source: 0 live numpy/numpy.linalg float-rank CALLS and 0 `import octonion_algebra` on the decisive path. Independently re-confirmed by orchestrator grep (only matches are comments/docstrings + the guard's own detector regex)."
      linked_ids: [claim-two-route-agreement, deliv-harness, ref-gate-machinery]
    test-scope-boundary:
      status: passed
      summary: "Both the harness handoff print and this SUMMARY state in plain language that rank 10 establishes FIELD-LEVEL functional completeness (trdeg-saturating) and does NOT establish ring generation (Phase 68). No sentence claims the 10 invariants 'generate the ring'. (Acceptance kind: human_review -- final human sign-off occurs at phase verification.)"
      linked_ids: [claim-field-not-ring, deliv-generating-set]
  references:
    ref-derksen-kemper:
      status: completed
      completed_actions: [cite, use]
      summary: "The char-0 Jacobian criterion (generic Jacobian rank of f_1..f_k = trdeg of the field they generate) is THE decisive method here -- used as the exact DUAL of the Phase-65 orbit-dimension computation."
    ref-iltyakov:
      status: completed
      completed_actions: [cite]
      summary: "Artin-Procesi-Iltyakov equality (split Albert algebra, char 0, m<=2 elements: invariant ring = trace algebra) warrants the trace-monomial candidate FAMILY. Family-level warrant only; the verdict is COMPUTED."
    ref-phase65-orbit:
      status: completed
      completed_actions: [read, compare]
      summary: "Phase-65 orbit-derived trdeg 54-44=10 read from 65-03-SUMMARY.md and pre-registered as the CHECK the candidate Jacobian rank must match. The decisive two-route benchmark."
    ref-engine:
      status: completed
      completed_actions: [read, use]
      summary: "code/ring_lemma_verification.py reused verbatim (jordan, corrected det_3, Tr, Tr2, c, X_from_symbols, xs/ys, octonionic_points, the 7 base invariants). The 3 mixed built on top of jordan+Tr. octonion_algebra.py never touched."
    ref-gate-machinery:
      status: completed
      completed_actions: [read, use]
      summary: "code/orbit_dimension_gate.py reused (exact_qq_rank, PAIR_POINTS, _pair_not_proportional, exact_rank_route_crosscheck, _select_independent_basis, _is_genuinely_octonionic_integer). Same substitute-first exact-rank recipe; the f_4-invariance pair action extended the gate's single-copy infinitesimal_action to the 54-vector diagonal tangent."
    ref-schwarz-fallback:
      status: completed
      completed_actions: [cite]
      summary: "Schwarz math/0609078 (2-polarization can fail generically in char 0) motivated the COMPUTE-not-assume deterministic fallback ladder. Not exercised here (the natural three completed the set), but implemented and guarded."
  forbidden_proxies:
    fp-float-rank:
      status: rejected
      notes: "Decisive rank is exact_qq_rank (DomainMatrix-over-QQ) only; module-local guard confirms 0 float-rank calls; three exact domains agree at width 54. numpy not imported on the decisive path."
    fp-nongeneric-point:
      status: rejected
      notes: "Only generic PAIR_POINTS used; _pair_not_proportional asserted on every pair and X!=Y; rank taken as MAX over >=3 points (rank lower-semicontinuous -> generic value = MAX). No X=Y, proportional, or real-only/diagonal points."
    fp-assume-complete:
      status: rejected
      notes: "Completeness is the COMPUTED Jacobian rank (10), not assumed; TRDEG_TARGET=10 is a pre-registered CHECK, not an input to the computation."
    fp-force-count:
      status: rejected
      notes: "Rank came out of the engine at 10 with no point-picking or invariant redefinition. NEEDS_FALLBACK=False; had the natural three given 9, the deterministic fallback ladder (computed, not forced) would have run. No reward-hacking."
    fp-conflate-field-ring:
      status: rejected
      notes: "Field-level (trdeg) completeness stated; ring generation explicitly deferred to Phase 68. No sentence claims ring generation."
  uncertainty_markers:
    weakest_anchors:
      - "Mutual functional independence of the three natural mixed monomials was the a priori weakest point (one could have been dependent via a Newton-type identity -> rank 9). RESOLVED: computed tier ladder (6,7,8,9,10) shows each adds +1; no dependence; fallback unused."
    unvalidated_assumptions: []
    competing_explanations: []
    disconfirming_observations:
      - "rank > 10 (would contradict the orbit-derived trdeg) -- NOT observed (MAX = 10, sanity bound held). r6 != 6 or r7 != 7 (engine/builder bug) -- NOT observed (r6=6, r7=7)."

comparison_verdicts:
  - subject_id: claim-two-route-agreement
    subject_kind: claim
    subject_role: decisive
    reference_id: ref-phase65-orbit
    comparison_kind: cross_method
    metric: exact_integer_match (candidate-Jacobian trdeg vs orbit-derived trdeg)
    threshold: "== 10"
    verdict: pass
    recommended_action: "Propagate corrected trdeg=10 to Phases 66/68; proceed to Phase 66 (THE SPINE)."
    notes: "Invariant-Jacobian rank (this phase) = orbit-tangent rank (Phase 65) = 10, two independent exact-over-Q routes. Also corroborated by the three-exact-domain width-54 cross-check (candidate (10,10,10), gate-tangent (44,44,44))."

duration: ~10min
completed: 2026-05-25
---

# Phase 65.1: Generating-set count correction (trdeg-10) Summary

**Confirmed by exact computation over Q that the 10-candidate set {6 pointwise, c, Tr(X^2 o Y), Tr(X o Y^2), Tr(X^2 o Y^2)} realizes Jacobian rank 10 = the Phase-65 orbit-derived transcendence degree (two independent routes agree); the joint diagonal-F_4-invariant generating set of h_3(O) (+) h_3(O) is FIELD-level functionally complete at trdeg 10.**

## Performance

- **Duration:** ~10 min (decisive compute ~2.5 min; dominant cost = the 52-generator F_4-invariance gate with simplify())
- **Completed:** 2026-05-25
- **Tasks:** 6 (3 in a bounded first segment to the first-result gate, 3 after gate clearance)
- **Files modified:** 1 (`code/ring_generating_set.py`, new)

## Key Results

- **Full 10x54 candidate Jacobian MAX rank over Q = 10** (each of 4 generic integer pairs individually = 10; substitute-first -> `exact_qq_rank` over QQ). The 3 NATURAL mixed trace monomials complete the set -- the fallback ladder was a recorded no-op.
- **Two-route agreement: candidate-Jacobian trdeg 10 == Phase-65 orbit-derived trdeg 54 - 44 == 10.** The invariant-Jacobian route (this phase, Derksen-Kemper criterion) and the orbit-tangent route (Phase 65) independently fix trdeg = 10.
- **Tier ladder (r6,r7,r8,r9,r10) = (6,7,8,9,10)**, every increment +1: 6-pointwise = 6 (single-copy 3+3, Garibaldi-Guralnick), +c = 7 (functional independence of c -- previews the Phase-66 SPINE), and each of Tr(X^2 o Y), Tr(X o Y^2), Tr(X^2 o Y^2) adds one more independent direction.
- **All 3 mixed invariants certified genuine F_4 invariants** of bidegree (2,1),(1,2),(2,2): D_M f = 0 over Q for all 52 f_4 generators at >=3 genuinely-octonionic points + 2 independent pairs (gated BEFORE any rank).
- **Scope:** this is FIELD-level (trdeg) completeness, NOT ring generation (Phase 68).

## Task Commits

1. **Tasks 1-3 (build + gate 3 mixed invariants; full 10x54 rank; tier ladder)** - `c07b7603` (bounded first segment to the first-result gate)
2. **Tasks 4-6 (fallback no-op; main() + two-route + three-exact-domain cross-checks; trdeg-10 handoff)** - `785d8346`

_Recovery note: the executor completed the Task 4-6 CODE but its agent process was killed by the stream watchdog (no token stream during the multi-minute F_4-invariance computation) before committing or writing this SUMMARY. The orchestrator independently re-ran the harness end-to-end (exit 0, full CLEAN PASS), then committed Tasks 4-6 and authored this SUMMARY. No code was modified by the orchestrator beyond what the executor wrote._

## Files Created/Modified

- `code/ring_generating_set.py` - Exact-over-Q harness (1065 lines): builds the 10 candidate invariants, runs grouping/F_4-invariance/bidegree gates, the full 10x54 Jacobian rank (MAX over generic pairs), tier ladder, two-route + three-exact-domain cross-checks, exact-only guard, conditional fallback ladder, and the trdeg-10 generating-set handoff. `main()` exits 0 on the clean pass.

## Validations Completed

- **Exact over Q end-to-end:** decisive rank is `exact_qq_rank` (DomainMatrix-over-QQ); module-local exact-only guard confirms 0 float-rank calls and 0 `octonion_algebra` imports; three exact domains (QQ-frac/QQ-int/ZZ-int) agree at width 54.
- **Genericity:** only `PAIR_POINTS` with `_pair_not_proportional` and X!=Y asserted; MAX over 4 points.
- **Single-copy cross-check:** 6-pointwise prefix rank = 6 = 3(X) + 3(Y), consistent with the Garibaldi-Guralnick / Faraut-Koranyi single-copy trdeg-3 result.
- **Orchestrator-independent re-confirmation:** the full 10x54 rank was re-computed at a FRESH generic octonionic pair (not in `PAIR_POINTS`; distinct diagonals, X!=Y, non-proportional) -> rank 10, r6=6, r7=7. Mirrors the orchestrator-independent confirmation that hardened the Phase-65 GATE.

## Decisions Made

- The three natural mixed trace monomials complete the set; the deterministic fallback ladder (polarized cubics, then alternative (2,2), then higher-degree trace monomials, capped at total degree 5) was implemented and guarded but recorded as a no-op.
- Established FIELD/trdeg-level completeness only; ring generation deferred to Phase 68 (no Hilbert series / Krull / minimal generators claimed).

## Deviations from Plan

None in the computation -- the plan was executed exactly as written and reached the success path (rank 10 via the natural three). The only deviation was procedural recovery: the executor's agent process stalled (stream watchdog) after writing the Task 4-6 code; the orchestrator verified the harness (exit 0), committed the code, and wrote this SUMMARY. No physics or code content changed.

## Issues Encountered

- **Executor stream-watchdog stall (procedural, resolved):** the gpd-executor agent was killed after 600s of no token stream while the harness's 52-generator F_4-invariance gate (with `simplify()`) ran. The harness itself is correct and completes with exit 0; the orchestrator re-ran it independently to confirm before committing. For downstream phases that re-run this harness, run with `python -u` (unbuffered) so output streams, or note that the F_4-invariance gate takes ~2-3 min.

## Open Questions

- Are the trace-monomial completing invariants Tr(X^2 o Y), Tr(X o Y^2), Tr(X^2 o Y^2) genuine ring GENERATORS, or reducible in the ring (products/relations)? -> Phase 68 (Hilbert series / Krull dimension at the COMPUTED trdeg 10).

## Next Phase Readiness

The corrected **trdeg-10 generating set** is the decisive handoff for Phases 66/67/68. Verbatim from the harness:

```
############################################################################
# DOWNSTREAM HANDOFF (deliv-generating-set) -- the corrected trdeg-10
# generating set. Phases 66/67/68 build on THIS (not the naive 7).
############################################################################
#
# CONFIRMED / REALIZED trdeg-10 generating set (FIELD-level complete):
#   full candidate Jacobian rank (MAX over >=3 generic integer pairs, exact over Q) = 10
#
#   ordered invariant list + BIDEGREE TABLE:
#   ---------------------------------------------------------------------
#   #   name              builder                                          (deg_X, deg_Y)
#   ---------------------------------------------------------------------
#   1   Tr X              Tr(Xsym)                                         (1, 0)
#   2   Tr X^2            Tr2(Xsym)                                        (2, 0)
#   3   det X             det_3(Xsym)                                      (3, 0)
#   4   Tr Y              Tr(Ysym)                                         (0, 1)
#   5   Tr Y^2            Tr2(Ysym)                                        (0, 2)
#   6   det Y             det_3(Ysym)                                      (0, 3)
#   7   c = Tr(X o Y)     Tr(jordan(Xsym,Ysym))                            (1, 1)
#   8   Tr(X^2 o Y)       Tr(jordan(jordan(Xsym,Xsym),Ysym))               (2, 1)
#   9   Tr(X o Y^2)       Tr(jordan(Xsym,jordan(Ysym,Ysym)))               (1, 2)
#   10  Tr(X^2 o Y^2)     Tr(jordan(jordan(Xsym,Xsym),jordan(Ysym,Ysym)))  (2, 2)
#   ---------------------------------------------------------------------
#   (rows 1-6 = the six POINTWISE generators R_pt = R[..X] (x) R[..Y];
#    row 7 = the coupling c; rows 8-10 = the 3 mixed completing invariants.)
#
#   NOTE: the three natural mixed monomials Tr(X^2 o Y) (2,1), Tr(X o Y^2) (1,2),
#         Tr(X^2 o Y^2) (2,2) complete the set; NO fallback was needed.
#
#   REALIZED TIER LADDER (fixed order): (6, 7, 8, 9, 10)
#     6-pointwise prefix = 6  (= 3 X-block + 3 Y-block; single-copy trdeg-3 decomposition, Garibaldi-Guralnick / Faraut-Koranyi)
#     +c prefix          = 7  (c independent of the pointwise sextet; previews the Phase-66 SPINE)
#     +Tr(X^2 o Y)       = 8  (increment +1)
#     +Tr(X o Y^2)       = 9  (increment +1)
#     +Tr(X^2 o Y^2)     = 10 (increment +1)
#
#   TWO-ROUTE AGREEMENT: candidate-Jacobian trdeg 10 == Phase-65 orbit-derived trdeg 54 - 44 == 10.
#     (Independent routes: orbit-tangent rank in Phase 65 vs invariant-Jacobian rank here.)
#
#   FIELD-vs-RING BOUNDARY (test-scope-boundary) -- READ CAREFULLY:
#     Jacobian rank 10 establishes FIELD-LEVEL functional completeness:
#     the invariant field R(27(+)27)^{F_4} is algebraic over Q(f_1..f_10);
#     trdeg-saturating. This does NOT establish ring generation (Hilbert
#     series / Krull dimension / minimal generators & relations), which is
#     Phase 68's job. Do NOT read this as 'the 10 invariants generate the ring'.
#
#   DOWNSTREAM WIRING:
#     Phase 66 (THE SPINE): the c-independence rank-7 test for {6 pointwise + c}
#       is UNCHANGED, but rank 7 no longer SATURATES trdeg (there are 3 more
#       independent invariants); (b) is unaffected / strengthened.
#     Phase 67: the unique-degree-2-coupling question is unchanged.
#     Phase 68: the Krull-dimension / Hilbert-series target is the COMPUTED
#       trdeg 10. The trace-monomial completing invariants Tr(X^2 o Y),
#       Tr(X o Y^2), Tr(X^2 o Y^2) (or the realized minimal set) are the
#       joint-generator candidates to test for ring reducibility.
############################################################################
```

## Contract Coverage

- Claim IDs advanced: claim-mixed-complete -> passed; claim-two-route-agreement -> passed; claim-field-not-ring -> passed
- Deliverable IDs produced: deliv-harness -> code/ring_generating_set.py (passed); deliv-generating-set -> handoff printed + carried verbatim (passed)
- Acceptance test IDs run: test-full-rank-10, test-trdeg-match, test-tier-increments, test-grouping-unambiguous, test-bidegree, test-f4-invariance, test-three-domain, test-exact-only -> all passed; test-scope-boundary -> passed (human_review, final sign-off at verification)
- Reference IDs surfaced: ref-derksen-kemper (cite,use), ref-iltyakov (cite), ref-phase65-orbit (read,compare), ref-engine (read,use), ref-gate-machinery (read,use), ref-schwarz-fallback (cite) -> all completed
- Forbidden proxies rejected: fp-float-rank, fp-nongeneric-point, fp-assume-complete, fp-force-count, fp-conflate-field-ring -> all rejected
- Decisive comparison verdicts: claim-two-route-agreement -> pass (candidate-Jacobian trdeg == orbit-derived trdeg == 10)

## Structured Return

```yaml
gpd_return:
  status: completed
  files_written:
    - code/ring_generating_set.py
    - .gpd/phases/65.1-generating-set-count-correction-pin-down-the-3-missing-joint-invariants-trdeg-10/65.1-01-SUMMARY.md
  issues:
    - "Executor agent stalled (stream watchdog, ~600s with no token stream while the 52-generator F_4-invariance gate ran) AFTER writing the Task 4-6 code but BEFORE committing or writing the SUMMARY. Orchestrator independently re-ran the harness end-to-end (exit 0, full CLEAN PASS, ~150s), committed Tasks 4-6 (785d8346), and authored this SUMMARY. No code or physics content changed."
  phase: "65.1-generating-set-count-correction-pin-down-the-3-missing-joint-invariants-trdeg-10"
  plan: "01"
  tasks_completed: 6
  tasks_total: 6
  duration_seconds: 600
  commits:
    - c07b7603
    - 785d8346
  decisions:
    - phase: "65.1"
      summary: "Candidate 10-set {6 pointwise, c, Tr(X^2 o Y), Tr(X o Y^2), Tr(X^2 o Y^2)} realizes EXACT Jacobian rank 10 over Q (MAX over 4 generic integer pairs, each=10), == Phase-65 orbit-derived trdeg 54-44=10 (two-route agreement, Derksen-Kemper char-0 dual). The 3 natural mixed monomials complete the set; the fallback ladder was a recorded no-op."
      rationale: "All 3 mixed certified genuine F_4 invariants (D_M f=0, all 52 generators, >=3 octonionic points) of bidegree (2,1),(1,2),(2,2) BEFORE any rank; tier ladder (6,7,8,9,10) every increment +1; 6-pointwise==6 and +c==7 cross-checks hold; exact over Q (exact-only guard PASS, 3 exact domains agree at width 54); TRDEG_TARGET=10 pre-registered as a CHECK, not an input (rank COMPUTED, not forced)."
  key_results:
    - "Full 10x54 candidate Jacobian MAX rank over Q = 10 (each of 4 generic integer pairs = 10; exact_qq_rank) [CONFIDENCE: HIGH]"
    - "Two-route agreement: candidate-Jacobian trdeg 10 == Phase-65 orbit-derived trdeg 54-44 = 10 [CONFIDENCE: HIGH]"
    - "Tier ladder (6,7,8,9,10), all increments +1; 6-pointwise==6 and +c==7 cross-checks PASS [CONFIDENCE: HIGH]"
    - "F_4-invariance 52/52 for each of f_21,f_12,f_22; grouping-unambiguous; all 10 bidegrees correct; 3 exact domains agree at width 54 [CONFIDENCE: HIGH]"
    - "FIELD-level (trdeg) completeness only; ring generation deferred to Phase 68 [CONFIDENCE: HIGH]"
  next_actions:
    - "Orchestrator: state advance + record-metric for 65.1-01; phase verification; rapid consistency check; mark Phase 65.1 complete; route to Phase 66 (THE SPINE)."
    - "Phase 66: c-independence rank-7 test for {6 pointwise + c} UNCHANGED, but rank 7 no longer saturates trdeg (3 more independent invariants) -- (b) unaffected/strengthened."
    - "Phase 68: Krull-dimension / Hilbert-series target = COMPUTED trdeg 10; the 3 mixed trace monomials are the joint-generator candidates to test for ring reducibility."
  state_updates:
    current_plan: "01 complete (1/1)"
    last_activity: "2026-05-25 Phase 65.1 Plan 01 complete: trdeg-10 joint generating set FIELD-complete (Jacobian rank 10 == orbit-derived trdeg 10, two-route agreement, Derksen-Kemper dual); corrected count propagates to Phases 66/68; NEGATIVE-RESULT-IS-SUCCESS framing preserved (this is the corrective confirmation the GATE backtrack required)."
    intermediate_results:
      - "code/ring_generating_set.py: full 10x54 Jacobian rank 10 (4 generic pairs all 10); tier ladder (6,7,8,9,10); f_21=-19, f_12=71, f_22=1523 at P1xP2 (sanity nonzero); three-exact-domain candidate (10,10,10) + gate-tangent (44,44,44)."
  metrics:
    label: "Phase 65.1 P65.1-01"
    tasks: 6
    files: 1
    harness_runtime_sec: 150
    full_jacobian_rank: 10
    tier_ladder: "(6,7,8,9,10)"
    f4_invariance: "52/52 x 3 mixed invariants"
    needs_fallback: false
    exact_only_guard: "PASS (0 float-rank, 0 octonion_algebra)"
```

---

_Phase: 65.1-generating-set-count-correction-pin-down-the-3-missing-joint-invariants-trdeg-10_
_Completed: 2026-05-25_
