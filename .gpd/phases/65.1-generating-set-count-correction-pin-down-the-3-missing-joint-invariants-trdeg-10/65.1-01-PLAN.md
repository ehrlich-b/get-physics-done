---
phase: 65.1-generating-set-count-correction-pin-down-the-3-missing-joint-invariants-trdeg-10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [code/ring_generating_set.py]
interactive: false

conventions:
  arithmetic: "EXACT over Q (sympy.Rational / QQ); NO float64, NO numpy.linalg.matrix_rank on the decisive path"
  decisive_rank: "exact_qq_rank(A) = DomainMatrix.from_Matrix(A).convert_to(QQ).rank() (EXACT over QQ; sympy.Matrix.rank() STALLS at width 54)"
  jordan_product: "jordan(A,B) = (1/2)(AB+BA) (the 1/2 is LOAD-BEARING)"
  power_notation: "X^2 := jordan(X,X) (unambiguous); octonion Fano table e1.e2=e4"
  cubic_norm_cross: "2*Re((x2.x1).x3) (Phase-64.1 CORRECTED det_3; was (x1.x2).x3)"
  symbol_layout: "xs=x0:26 (X), ys=y0:26 (Y); 54 symbols total; per-copy [alpha,beta,gamma, x1(8), x2(8), x3(8)]; Xsym=X_from_symbols(xs), Ysym=X_from_symbols(ys)"
  generic_point: "INTEGER-cleared, genuinely octonionic (>=4 nonzero imaginary comps per off-diag octonion), distinct diagonals, X != Y, X NOT proportional to Y; substitute BEFORE the rank"
  jacobian_criterion: "Derksen-Kemper char-0 DUAL: trdeg of the subfield generated by f_1..f_k = generic rank over Q of the k x 54 Jacobian [d f_i / d z_j]"

dimensional_check:
  # "Dimensional consistency" maps to BIDEGREE (deg_X, deg_Y) consistency for each invariant.
  inv_Tr_X: "(1,0)"
  inv_Tr2_X: "(2,0)"
  inv_det_X: "(3,0)"
  inv_Tr_Y: "(0,1)"
  inv_Tr2_Y: "(0,2)"
  inv_det_Y: "(0,3)"
  inv_c: "(1,1)"
  f_21_TrX2Y: "(2,1)"
  f_12_TrXY2: "(1,2)"
  f_22_TrX2Y2: "(2,2)"
  full_jacobian_rank: "10 (exact integer; == Phase-65 orbit-derived trdeg 54-44)"
  tier_increments: "each of {6->7, 7->8, 8->9, 9->10} in {0,1}; rank <= 10 always"

approximations: []  # NONE. Exact finite linear algebra over Q end to end (sympy diff + DomainMatrix-over-QQ rank). No small parameter, no truncation, no convergence, no tolerance.

contract:
  schema_version: 1
  scope:
    question: "Do the 3 named mixed trace monomials {Tr(X^2 o Y), Tr(X o Y^2), Tr(X^2 o Y^2)} complete {6 pointwise + c} to a field-level functionally-complete set of transcendence degree 10 (matching the Phase-65 orbit-derived trdeg), and if not, what is the COMPUTED minimal completing set?"
  claims:
    - id: claim-mixed-complete
      statement: "The 3 named mixed trace monomials Tr(X^2 o Y) (2,1), Tr(X o Y^2) (1,2), Tr(X^2 o Y^2) (2,2) are functionally independent of {6 pointwise + c} and complete the candidate set to transcendence degree 10 -- OR, if a natural candidate is functionally dependent (full rank 9), the COMPUTED minimal completing set (from the deterministic fallback ladder) that reaches trdeg 10, reported with its realized bidegrees."
      deliverables: [deliv-harness, deliv-generating-set]
      acceptance_tests: [test-full-rank-10, test-tier-increments, test-grouping-unambiguous, test-bidegree, test-f4-invariance]
      references: [ref-derksen-kemper, ref-iltyakov, ref-phase65-orbit, ref-engine, ref-gate-machinery, ref-schwarz-fallback]
    - id: claim-two-route-agreement
      statement: "The full candidate Jacobian rank (MAX over >=3 generic integer pairs, exact over Q) equals 10, which equals the Phase-65 orbit-derived transcendence degree 54-44=10. Two INDEPENDENT routes (orbit-tangent rank in Phase 65 vs invariant-Jacobian rank here) agree on trdeg=10."
      deliverables: [deliv-harness]
      acceptance_tests: [test-full-rank-10, test-trdeg-match, test-three-domain, test-exact-only]
      references: [ref-derksen-kemper, ref-phase65-orbit, ref-gate-machinery]
    - id: claim-field-not-ring
      statement: "What is established is FIELD-LEVEL functional completeness (the invariant field R(27+27)^{F_4} is algebraic over Q(f_1..f_10), trdeg-saturating). This is NOT ring generation (no Hilbert-series / Krull / minimal-generator certificate) -- ring generation is Phase 68's job and is explicitly OUT OF SCOPE here."
      deliverables: [deliv-generating-set]
      acceptance_tests: [test-scope-boundary]
      references: [ref-derksen-kemper, ref-iltyakov]
  deliverables:
    - id: deliv-harness
      kind: code
      path: code/ring_generating_set.py
      description: "Exact-over-Q harness that builds the 10 candidate invariants as SymPy polynomials in the 54 engine symbols, computes the full 10x54 candidate Jacobian rank (substitute-first -> stack -> exact_qq_rank) MAX over >=3 generic integer pairs, the fine-grained tier increments (6->7->8->9->10), the F_4-invariance gate, the grouping-unambiguity check, the three-exact-domain cross-check, the exact-only guard, and (conditionally) the fallback ladder. Imports the FROZEN engine + certified gate machinery; touches NO octonion_algebra.py."
      must_contain: ["exact_qq_rank", "10", "54", "Jacobian", "tier", "trdeg"]
    - id: deliv-generating-set
      kind: report
      path: code/ring_generating_set.py
      description: "The confirmed/realized trdeg-10 generating set printed by the harness as a structured handoff for Phases 66/68: the ordered invariant list with a bidegree table, the realized tier increments, the explicit FIELD-completeness-vs-RING-generation boundary note, and (if the natural three were deficient) which candidate(s) the fallback replaced."
      must_contain: ["bidegree", "trdeg = 10", "field", "ring", "Phase 66", "Phase 68"]
  references:
    - id: ref-derksen-kemper
      kind: paper
      locator: "Derksen & Kemper, Computational Invariant Theory, Sec. 4 (char-0 Jacobian criterion: generic Jacobian rank of f_1..f_k = trdeg of the field they generate)"
      role: method
      why_it_matters: "THE decisive criterion: rank 10 == orbit-derived trdeg 10 ⇒ field-level functional completeness. This is the exact DUAL of the certified Phase-65 orbit-dimension computation."
      applies_to: [claim-mixed-complete, claim-two-route-agreement, claim-field-not-ring]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [cite, use]
    - id: ref-iltyakov
      kind: paper
      locator: "Iltyakov 1995 + Polikarpov 1991 (Artin-Procesi-Iltyakov equality): for the split Albert algebra in char 0, the invariant ring of m in {1,2} elements EQUALS the trace algebra"
      role: benchmark
      why_it_matters: "Warrants the candidate FAMILY -- the missing pair generators ARE trace monomials, so the natural completing candidates are the mixed-bidegree trace monomials (motivated, not a guess). Family-level warrant only; the answer is COMPUTED."
      applies_to: [claim-mixed-complete, claim-field-not-ring]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [cite]
    - id: ref-phase65-orbit
      kind: prior_artifact
      locator: ".gpd/phases/65-f-4-construction-orbit-dimension-gate/65-03-SUMMARY.md (pair orbit dim 44 ⇒ trdeg 54-44=10; triple-confirmed)"
      aliases: ["Phase 65 GATE", "orbit-derived trdeg 10"]
      role: benchmark
      why_it_matters: "The number the candidate Jacobian rank MUST MATCH (rank == 10). The 3(X)+3(Y)+4-mixed split says 4 of the 10 are mixed (c + 3). The decisive two-route cross-check."
      applies_to: [claim-mixed-complete, claim-two-route-agreement]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-engine
      kind: prior_artifact
      locator: "code/ring_lemma_verification.py (Phase-64.1 frozen engine: jordan, corrected det_3, Tr, Tr2, c, polarize_d, X_from_symbols, _flat27, Xsym, Ysym, xs, ys, octonionic_points, and the 7 base invariants inv_Tr_X..inv_c)"
      role: method
      why_it_matters: "Supplies the certified algebra + the 7 base invariants verbatim. Build the 3 mixed on top of jordan+Tr; reuse the 7 inv_*. NEVER touch octonion_algebra.py (float64, cross-term bug)."
      applies_to: [claim-mixed-complete]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use]
    - id: ref-gate-machinery
      kind: prior_artifact
      locator: "code/orbit_dimension_gate.py (Phase-65 certified: exact_qq_rank, _gradients_in_X substitute-first pattern, infinitesimal_action, PAIR_POINTS, _pair_not_proportional, exact_rank_route_crosscheck, _select_independent_basis, exact_only_guard)"
      role: method
      why_it_matters: "Supplies the exact-rank recipe, the generic PAIR_POINTS, the three-domain cross-check, and the exact-only guard. Build the Jacobian the SAME substitute-first way."
      applies_to: [claim-mixed-complete, claim-two-route-agreement]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use]
    - id: ref-schwarz-fallback
      kind: paper
      locator: "Schwarz, math/0609078 (2-polarization can fail generically in char 0)"
      role: must_consider
      why_it_matters: "A natural trace-monomial candidate MAY be functionally dependent ⇒ motivates the COMPUTE-not-assume deterministic fallback ladder (polarized cubics first, then alternative (2,2), then higher-degree trace monomials)."
      applies_to: [claim-mixed-complete]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [cite]
  acceptance_tests:
    - id: test-full-rank-10
      subject: claim-two-route-agreement
      kind: benchmark
      procedure: "Build the 10 candidate invariants as SymPy polys in the 54 symbols (7 base reused verbatim + 3 mixed built via Tr(jordan(...))). For each, take the 54 partials (sympy diff). At each generic integer pair in PAIR_POINTS, substitute the point into the 54 partials FIRST, assemble the 10x54 rational matrix, take exact_qq_rank. Take MAX over >=3 generic integer pairs."
      pass_condition: "MAX exact Jacobian rank over Q == 10. (If == 9, the natural three are deficient -> NOT a failure: trigger the fallback ladder, then this test passes on the COMPUTED minimal completing set that reaches 10. If > 10: impossible, STOP -- Jacobian/point bug.)"
      evidence_required: [deliv-harness, ref-derksen-kemper, ref-phase65-orbit]
      automation: automated
    - id: test-trdeg-match
      subject: claim-two-route-agreement
      kind: cross_method
      procedure: "Compare the candidate-Jacobian-derived trdeg (the full rank from test-full-rank-10) against the Phase-65 orbit-derived trdeg (54 - 44 = 10) read from the Phase-65 SUMMARY. Pre-register 10 as a CHECK, not an input."
      pass_condition: "candidate-Jacobian trdeg == orbit-derived trdeg == 10 (two independent routes agree)."
      evidence_required: [deliv-harness, ref-phase65-orbit, ref-derksen-kemper]
      automation: automated
    - id: test-tier-increments
      subject: claim-mixed-complete
      kind: limiting_case
      procedure: "In the FIXED order [Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y, c, f_21, f_12, f_22], compute the prefix Jacobian rank for k = 6, 7, 8, 9, 10 at each generic pair, MAX. Record which increments bump the rank (independent addition) vs not (redundant candidate)."
      pass_condition: "6-pointwise prefix rank == 6 (= 3 X + 3 Y, Garibaldi-Guralnick single-copy cross-check); +c prefix rank == 7 (previews Phase-66 SPINE); each subsequent increment in {0,1}; final == the full rank from test-full-rank-10. On the success path the ladder is (6,7,8,9,10)."
      evidence_required: [deliv-harness, ref-phase65-orbit]
      automation: automated
    - id: test-grouping-unambiguous
      subject: claim-mixed-complete
      kind: consistency
      procedure: "At a generic octonionic pair, evaluate Tr(jordan(jordan(Xsym,Xsym),Ysym)) - Tr(jordan(Xsym,jordan(Xsym,Ysym))) exactly over Q (trace associativity: the trace bilinear form is associative even though jordan is not)."
      pass_condition: "Tr((X o X) o Y) - Tr(X o (X o Y)) == 0 exactly over Q at the generic point (so the mixed monomial builders have no grouping/ordering ambiguity). If != 0: engine reuse is wrong, STOP."
      evidence_required: [deliv-harness, ref-engine]
      automation: automated
    - id: test-bidegree
      subject: claim-mixed-complete
      kind: dimensional_analysis
      procedure: "For each of the 3 mixed invariants, verify the claimed bidegree (deg_X, deg_Y) by scaling: substitute X -> s*X (s a fresh rational/symbol scaling all 27 X-coords) and Y -> t*Y, and check the invariant scales as s^{deg_X} * t^{deg_Y}. (Or verify by construction degree-count.)"
      pass_condition: "f_21 scales as s^2 t^1, f_12 as s^1 t^2, f_22 as s^2 t^2; the 7 base reproduce their tabulated bidegrees."
      evidence_required: [deliv-harness]
      automation: automated
    - id: test-f4-invariance
      subject: claim-mixed-complete
      kind: symmetry
      procedure: "For each of the 3 mixed invariants f, verify D_M f == 0 over Q for every f_4 generator M (the 52-independent basis from _select_independent_basis, faithful to the full 324), at >=3 genuinely-octonionic rational points, reusing the check_invariance / infinitesimal_action machinery (gradient substituted FIRST). A strong CORRECTNESS gate BEFORE any rank: a non-invariant candidate is not an invariant at all."
      pass_condition: "Every f_4 generator annihilates each of f_21, f_12, f_22 at all >=3 generic octonionic points (D_M f = 0 exactly over Q). If any survives: the builder is wrong, STOP before the rank."
      evidence_required: [deliv-harness, ref-engine, ref-gate-machinery]
      automation: automated
    - id: test-three-domain
      subject: claim-two-route-agreement
      kind: consistency
      procedure: "Run exact_rank_route_crosscheck (or the equivalent three-exact-domain pattern) on the decisive 10x54 candidate Jacobian at a generic pair: QQ-on-fractional == QQ-on-integer-cleared == ZZ-on-integer-cleared."
      pass_condition: "All three exact domains agree on the rank (certifies exact_qq_rank is EXACT over Q at width 54, not a float proxy)."
      evidence_required: [deliv-harness, ref-gate-machinery]
      automation: automated
    - id: test-exact-only
      subject: claim-two-route-agreement
      kind: reproducibility
      procedure: "Run a module-local exact-only guard (mirroring exact_only_guard, scanning THIS module's __file__ source) that asserts 0 live numpy/numpy.linalg float-rank CALLS and 0 `import octonion_algebra` on the decisive path."
      pass_condition: "0 float-rank calls AND 0 octonion_algebra imports in code/ring_generating_set.py (a non-decisive numpy float pre-screen, if present, is prose-distinguishable / never asserted on)."
      evidence_required: [deliv-harness, ref-gate-machinery]
      automation: automated
    - id: test-scope-boundary
      subject: claim-field-not-ring
      kind: human_review
      procedure: "The harness output and the SUMMARY must state, in plain language, that rank 10 establishes FIELD-LEVEL functional completeness (trdeg-saturating; the invariant field is algebraic over Q(f_1..f_10)) and does NOT establish ring generation (Hilbert series / Krull / minimal generators = Phase 68)."
      pass_condition: "The field-completeness-vs-ring-generation boundary is stated explicitly in both the harness handoff print and the SUMMARY; no sentence claims the 10 invariants 'generate the ring'."
      evidence_required: [deliv-generating-set]
      automation: human
  forbidden_proxies:
    - id: fp-float-rank
      subject: claim-two-route-agreement
      proxy: "numpy.linalg.matrix_rank / lstsq / SVD-tolerance for the decisive Jacobian rank"
      reason: "GLOBAL FORBIDDEN PROXY -- float rank is not exact over Q; the verdict must be exact_qq_rank (DomainMatrix-over-QQ). numpy is permissible ONLY for a non-decisive pre-screen."
    - id: fp-nongeneric-point
      subject: claim-two-route-agreement
      proxy: "Evaluating the Jacobian at a non-generic point (X=Y, X proportional to Y, repeated spectra, discriminant locus, real-only/diagonal)"
      reason: "Rank is lower-semicontinuous and DROPS on special loci -> a non-generic point gives a false LOW rank. Use the certified generic PAIR_POINTS + _pair_not_proportional; take MAX over >=3 points."
    - id: fp-assume-complete
      subject: claim-mixed-complete
      proxy: "Asserting the candidate set is complete (trdeg 10) without actually computing the exact Jacobian rank"
      reason: "The deliverable is the COMPUTED rank, not an assumption from the literature or the bidegree count."
    - id: fp-force-count
      subject: claim-mixed-complete
      proxy: "Redefining invariants, hand-picking a point, or otherwise FORCING the rank to hit 10 if the natural three give 9"
      reason: "Reward-hacking guard. On a deficit, REPORT it and walk the deterministic fallback ladder to the actual minimal completing set. The honest computed minimal set is the deliverable."
    - id: fp-conflate-field-ring
      subject: claim-field-not-ring
      proxy: "Reporting 'the 10 invariants generate the invariant RING' from rank 10"
      reason: "Rank 10 proves FIELD completeness only (trdeg-saturating). Ring generation (Hilbert series, Krull dim, minimal generators/relations) is Phase 68 -- overclaiming it here poaches Phase 68's result and is false."
  links:
    - id: link-jacobian-supports-completeness
      source: claim-mixed-complete
      target: deliv-harness
      relation: supports
      verified_by: [test-full-rank-10, test-tier-increments, test-f4-invariance, test-grouping-unambiguous, test-bidegree]
    - id: link-harness-computes-rank
      source: deliv-harness
      target: claim-two-route-agreement
      relation: computes
      verified_by: [test-full-rank-10, test-trdeg-match, test-three-domain, test-exact-only]
    - id: link-orbit-benchmarks-trdeg
      source: ref-phase65-orbit
      target: claim-two-route-agreement
      relation: benchmarks
      verified_by: [test-trdeg-match]
    - id: link-handoff-to-downstream
      source: claim-mixed-complete
      target: deliv-generating-set
      relation: supports
      verified_by: [test-tier-increments, test-scope-boundary]
  uncertainty_markers:
    weakest_anchors:
      - "Whether all three natural mixed monomials {Tr(X^2 o Y), Tr(X o Y^2), Tr(X^2 o Y^2)} are MUTUALLY functionally independent. One could be dependent on the others + the pointwise set (e.g. via a Newton-type identity expressing Tr(X^2 o Y^2) in lower monomials) -> full rank 9 -> the fallback ladder fills the slot. MITIGATED: the phase COMPUTES the rank + fine-grained tier increments and has a deterministic terminating fallback; the verdict does not depend on the natural three being the answer."
      - "The candidate-FAMILY warrant (Iltyakov/Polikarpov, trace algebra = pair invariant ring) is for the split Albert algebra over an alg-closed field; the in-engine computation is over Q (char 0), which is the regime the Jacobian criterion needs (consistent). The family warrant carries no load on the FINAL verdict (which is computed)."
    disconfirming_observations:
      - "Full candidate Jacobian rank != 10 in a way the fallback cannot fix (i.e. > 10, or the fallback ladder caps out below 10 within the stated total-degree bound). rank > 10 would CONTRADICT the Phase-65 orbit-derived trdeg and signal a builder/engine/point inconsistency -> STOP and investigate, do NOT force. A fallback cap-out below 10 (unexpected, since the trace algebra is finitely generated and trdeg<=10) would mean the trace-monomial family is insufficient at the capped degree -> report the maximal rank achieved honestly."
      - "6-pointwise prefix rank != 6, OR an f_4 generator fails to annihilate a mixed candidate (D_M f != 0): either is an engine/layout/builder bug -> STOP before trusting any completeness verdict (the pointwise sextet independence and the f_4 builder are certified upstream)."
---

<objective>
Pin down the 3 missing functionally-independent mixed invariants that complete the joint invariant generating set of the diagonal F_4 action on h_3(O) ⊕ h_3(O) from {6 pointwise + c} to transcendence degree 10, by EXACT computation over Q. Phase 65's GATE computed (triple-confirmed) that the generic pair orbit dimension is 44, so trdeg = 54 − 44 = 10 — three MORE than the naive-anchor 7. This phase confirms the 10-candidate set {Tr X, Tr X², det X, Tr Y, Tr Y², det Y, c, Tr(X²∘Y), Tr(X∘Y²), Tr(X²∘Y²)} realizes exact Jacobian rank 10 over Q (matching the orbit-derived trdeg via the Derksen-Kemper char-0 criterion, the DUAL of the Phase-65 orbit computation), pinpoints which tier increments add independence (6→7→8→9→10), and — if a natural candidate is dependent — walks a deterministic fallback ladder to the COMPUTED minimal completing set.

Purpose: This corrective phase fixes a generating-set COUNT that gates Phases 66/67/68. The corrected trdeg-10 set (with bidegree table) is the decisive handoff: Phase 66 (THE SPINE) re-derives its target context from 10 (not 7); Phase 68 (Krull dimension) targets 10. The (b) c-independence rank-7 test is unchanged but no longer "saturates" trdeg.

Output: `code/ring_generating_set.py` — an exact-over-Q harness reusing the FROZEN engine + certified gate machinery; it builds the 10 candidates, runs the F_4-invariance + grouping + bidegree correctness gates, computes the full 10×54 Jacobian rank (MAX over ≥3 generic integer pairs, exact_qq_rank), the fine-grained tier increments, the two-route trdeg cross-check, the three-exact-domain cross-check, the exact-only guard, the conditional fallback ladder, and prints the corrected trdeg-10 generating set + bidegree table + the FIELD-vs-RING boundary note as the downstream handoff.

SCOPE BOUNDARY (state explicitly in the SUMMARY): this phase proves FIELD/trdeg-level functional completeness (Jacobian rank 10), NOT ring generation (Hilbert series / Krull dimension / minimal generators = Phase 68). EXACT over Q only; float rank forbidden on the decisive path.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/65.1-generating-set-count-correction-pin-down-the-3-missing-joint-invariants-trdeg-10/65.1-RESEARCH.md
@.gpd/phases/65-f-4-construction-orbit-dimension-gate/65-03-SUMMARY.md
@code/ring_lemma_verification.py
@code/orbit_dimension_gate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + register the 3 mixed invariants; gate them (grouping-unambiguity, F_4-invariance, bidegree) BEFORE any rank</name>
  <files>code/ring_generating_set.py</files>
  <action>
    Create `code/ring_generating_set.py` as a NEW module that imports the frozen engine and the certified gate machinery (do NOT edit those files; do NOT import `octonion_algebra.py`):
      - `import code.ring_lemma_verification as E` (or the project's existing import idiom — match how `orbit_dimension_gate.py` imports the engine as `E`). Reuse: `E.jordan`, `E.Tr`, `E.Tr2`, `E.c`, `E.det_3`, `E.polarize_d`, `E.Xsym`, `E.Ysym`, `E.xs`, `E.ys`, `E.X_from_symbols`, `E._flat27`, `E.octonionic_points`, and the 7 base invariants `E.inv_Tr_X, E.inv_Tr2_X, E.inv_det_X, E.inv_Tr_Y, E.inv_Tr2_Y, E.inv_det_Y, E.inv_c`.
      - `from code.orbit_dimension_gate import (exact_qq_rank, infinitesimal_action, PAIR_POINTS, _pair_not_proportional, exact_rank_route_crosscheck, _select_independent_basis, cached_L_matrices, inner_derivations)` (match the actual names/locations; `inner_derivations`/`cached_L_matrices` are in the engine vs gate — confirm at import time and adjust).

    Build the 3 mixed invariants as SymPy polynomials in the 54 symbols, with the CANONICAL groupings (X² := jordan(X,X) is unambiguous; the outer grouping is irrelevant under Tr by trace associativity — but PIN the canonical form):
      - `f_21 = E.Tr(E.jordan(E.jordan(E.Xsym, E.Xsym), E.Ysym))`        # Tr(X²∘Y),  bidegree (2,1)
      - `f_12 = E.Tr(E.jordan(E.Xsym, E.jordan(E.Ysym, E.Ysym)))`        # Tr(X∘Y²),  bidegree (1,2)
      - `f_22 = E.Tr(E.jordan(E.jordan(E.Xsym, E.Xsym), E.jordan(E.Ysym, E.Ysym)))`  # Tr(X²∘Y²), bidegree (2,2)

    Define the ORDERED 10-candidate list (FIXED order — load-bearing for the tier increments in Task 3):
      CANDIDATES = [inv_Tr_X, inv_Tr2_X, inv_det_X, inv_Tr_Y, inv_Tr2_Y, inv_det_Y, inv_c, f_21, f_12, f_22]
      with a parallel NAMES list and a BIDEGREES list [(1,0),(2,0),(3,0),(0,1),(0,2),(0,3),(1,1),(2,1),(1,2),(2,2)].

    Then run THREE correctness gates (all BEFORE any Jacobian rank — a non-invariant or mis-grouped candidate is not an invariant at all):

    (a) GROUPING-UNAMBIGUITY (test-grouping-unambiguous): at one generic octonionic point (use `E.octonionic_points()[0]`, substitute into the 54 X+Y symbols), assert
        `(E.Tr(E.jordan(E.jordan(E.Xsym,E.Xsym),E.Ysym)) - E.Tr(E.jordan(E.Xsym,E.jordan(E.Xsym,E.Ysym)))).subs(point) == 0` exactly over Q.
        (Trace bilinear form is associative; this confirms the engine reuse is correct. If != 0: STOP, engine/jordan/Tr reuse is wrong.)

    (b) F_4-INVARIANCE (test-f4-invariance) — the STRONG correctness gate: for each mixed invariant f in {f_21, f_12, f_22}, verify D_M f == 0 over Q for every f_4 generator M, at >=3 genuinely-octonionic rational points. Build the gradient of f in ALL 54 symbols (`grad_f = [diff(f, z) for z in list(E.xs)+list(E.ys)]`), substitute each generic point into the gradient FIRST, and check `simplify(infinitesimal_action(grad_at, M, v)) == 0` for every M in the f_4 basis. CRITICAL: the f_4 generators act on the 27-dim copy; the DIAGONAL action means the same M acts on each copy. So the orbit tangent of the PAIR is concat(M·v_x, M·v_y) and D_M f = grad_X(f)·(M·v_x) + grad_Y(f)·(M·v_y). Mirror `check_invariance` in `orbit_dimension_gate.py` but extend the gradient to all 54 symbols and the tangent to the 54-vector concat(M·v_x, M·v_y). Use the 52-independent basis from `_select_independent_basis(inner_derivations())` (faithful to the full 324; reuse Phase-65's selection). Assert every generator annihilates each mixed invariant at all >=3 points (worst-case killed == number of generators). If any survives: STOP, the builder is wrong.

    (c) BIDEGREE (test-bidegree): verify each invariant's claimed bidegree. Introduce fresh scalars s, t (sympy symbols); substitute X-coords -> s*x_i (all 27 X-symbols) and Y-coords -> t*y_j (all 27 Y-symbols) in each candidate, expand, and assert the result is s^{deg_X} * t^{deg_Y} times the original (i.e. homogeneous of the claimed bidegree). Check all 10 (the 7 base reproduce their tabulated bidegrees; the 3 mixed give (2,1),(1,2),(2,2)).

    Structure these as `check_mixed_builders()` returning (ok, detail) with clear per-check booleans, mirroring the engine's `_report(label, ok)` style. Cache the symbolic gradients of all 10 candidates at module scope (they are reused in Tasks 2-3) — but do NOT `simplify()` the symbolic 54-var gradients (expression swell; substitute the integer point FIRST per `_gradients_in_X`).
  </action>
  <verify>
    - test-grouping-unambiguous: Tr((X∘X)∘Y) − Tr(X∘(X∘Y)) == 0 exactly over Q at a generic octonionic point.
    - test-f4-invariance: every one of the 52-basis f_4 generators annihilates each of f_21, f_12, f_22 (D_M f = 0 over Q) at all ≥3 generic octonionic points.
    - test-bidegree: f_21 ~ s²t¹, f_12 ~ s¹t², f_22 ~ s²t²; the 7 base reproduce (1,0),(2,0),(3,0),(0,1),(0,2),(0,3),(1,1).
    - The 3 mixed builders compose without error and evaluate to sane nonzero rationals at a generic point (RESEARCH intermediate result).
  </verify>
  <done>`code/ring_generating_set.py` imports the frozen engine + certified gate machinery (no octonion_algebra.py); the 3 mixed invariants are built with canonical groupings and registered in the FIXED-order 10-candidate list with a bidegree table; grouping-unambiguity, F_4-invariance (D_M f = 0 for all 52 generators at ≥3 points), and bidegree all PASS — the candidates are certified genuine F_4 invariants of the claimed bidegree BEFORE any rank.</done>
</task>

<task type="auto">
  <name>Task 2: Build the candidate-Jacobian rank routine; compute the full 10×54 rank (MAX over ≥3 generic pairs), pre-register 10</name>
  <files>code/ring_generating_set.py</files>
  <action>
    Implement the candidate-Jacobian rank computation, the DUAL of the Phase-65 orbit-tangent rank (same substitute-first / DomainMatrix-over-QQ recipe, on a 10×54 matrix instead of 52×54 — strictly cheaper):

    1. `candidate_jacobian_rows_at(point_pair)`: for a generic integer pair (X27, Y27) from `PAIR_POINTS`, build the substitution dict `{E.xs[i]: X27[i] for i}` ∪ `{E.ys[j]: Y27[j] for j}`, then for each of the 10 candidates substitute the point into its CACHED 54-symbol gradient FIRST (rationals), producing a length-54 rational row. Stack into a 10×54 `sympy.Matrix`. (Substitute-first controls det-X (deg 3) and f_22 (deg 4) expression swell — do NOT simplify the symbolic gradient.)
    2. `full_candidate_rank(point_pair) = exact_qq_rank(candidate_jacobian_matrix_at(point_pair))` — exact rank over QQ via DomainMatrix-over-QQ (NEVER numpy/float; `sympy.Matrix.rank()` stalls at width 54).
    3. PRE-REGISTER the target: define `TRDEG_TARGET = 10` (= 54 − 44 from the Phase-65 SUMMARY) as a CHECK constant, NOT an input to the computation.
    4. `check_full_jacobian_rank()`: compute `full_candidate_rank` at EACH generic integer pair in `PAIR_POINTS` (>=3), take `MAX_RANK = max(...)`. Use ONLY generic pairs — assert `_pair_not_proportional(X,Y)` for each. Print each per-point rank + the MAX. Assert MAX_RANK <= 10 (sanity; > 10 is impossible). Record MAX_RANK as the computed full Jacobian rank for the downstream handoff and the tiering (Task 3).

    Make the assertion structure explicit and loud (mirror Phase 65's pre-registered-anchor pattern): the PASS condition for test-full-rank-10 is MAX_RANK == 10. If MAX_RANK == 9, do NOT assert-fail hard — set a flag `NEEDS_FALLBACK = True` and let Task 4 walk the ladder (the fine-grained increments from Task 3 pinpoint the dependent slot). If MAX_RANK > 10, that IS a hard stop (Jacobian/point/builder bug — contradicts the orbit-derived trdeg).

    PERF: cache the 10 symbolic gradients once (from Task 1); re-substitute per point. The dominant cost is the symbolic diff of det X and f_22 (seconds), not the rank (~0.01–0.1 s per 10×54 exact rank).
  </action>
  <verify>
    - test-full-rank-10 (the primary): MAX exact Jacobian rank over Q across ≥3 generic integer pairs == 10 (the success path). Each per-point rank printed; MAX is the verdict (rank lower-semicontinuous → generic value = MAX).
    - MAX_RANK <= 10 always (sanity bound; > 10 impossible — would exceed the orbit-derived trdeg).
    - The rank routine uses `exact_qq_rank` (DomainMatrix-over-QQ), substitute-first, generic `PAIR_POINTS` only (`_pair_not_proportional` asserted) — NO numpy float rank on the decisive path.
    - If MAX_RANK == 9: NEEDS_FALLBACK flag set (NOT a failure — Task 4 completes the set). If > 10: hard STOP.
  </verify>
  <done>The candidate-Jacobian rank routine is implemented as the exact DUAL of the Phase-65 orbit computation (substitute-first → 10×54 → exact_qq_rank); the full candidate Jacobian rank is computed as the MAX over ≥3 generic integer pairs, pre-registered against TRDEG_TARGET=10. On the success path MAX_RANK == 10; otherwise NEEDS_FALLBACK is set (rank 9) or a hard STOP fires (rank > 10).</done>
</task>

<task type="auto">
  <name>Task 3: Fine-grained tier increments 6→7→8→9→10; the 6-pointwise==6 and +c==7 cross-checks</name>
  <files>code/ring_generating_set.py</files>
  <action>
    Compute the fine-grained PREFIX Jacobian ranks in the FIXED candidate order, so the executor pinpoints EXACTLY which candidate adds independence vs is redundant (essential for the fallback in Task 4):

    1. `prefix_rank(k, point_pair)`: exact_qq_rank of the first k rows (k candidates) of the 10×54 Jacobian at that pair.
    2. `check_tier_increments()`: for k in [6, 7, 8, 9, 10], compute `MAX over PAIR_POINTS of prefix_rank(k, .)`. This gives the tier ladder (r6, r7, r8, r9, r10). Print the ladder and the per-step increments (r7−r6, r8−r7, r9−r8, r10−r9), labeling each addition {independent if +1, redundant if +0}.
    3. Assert the two FREE cross-checks that fall out:
       - 6-POINTWISE sub-rank r6 == 6 (= 3 X-block + 3 Y-block; the Garibaldi-Guralnick / Faraut-Korányi single-copy trdeg-3 result, decomposing 3+3). If r6 != 6: STOP — engine/layout/base-invariant bug (the pointwise sextet independence is certified upstream).
       - +c sub-rank r7 == 7 (c functionally independent of the pointwise sextet — PREVIEWS the Phase-66 SPINE). If r7 != 7 (i.e. +c does not bump 6→7): this is a SURPRISING Phase-66-relevant finding — report it loudly (it is NOT the expected branch; do not silently continue).
    4. Assert each increment r_{k+1} − r_k ∈ {0, 1} (a single new generator can add at most 1 to the trdeg) and r10 == MAX_RANK from Task 2 (consistency between the prefix-10 and the full rank).
    5. Record the ladder + which of {f_21, f_12, f_22} bumped the rank (independent) vs did not (redundant) — this is the input to Task 4's fallback (it names the slot to fill).

    Expected success path: ladder (6, 7, 8, 9, 10), all four increments == 1 → all three natural mixed monomials are independent → no fallback needed.
  </action>
  <verify>
    - test-tier-increments: 6-pointwise prefix rank == 6 (single-copy 3+3 cross-check); +c prefix rank == 7 (Phase-66 SPINE preview); each increment in {0,1}; r10 == the full rank from Task 2.
    - On the success path the ladder is exactly (6,7,8,9,10) with every increment == 1.
    - If any mixed increment is 0, the ladder names the dependent candidate (input to Task 4).
    - r6 != 6 or r7 != 7 are loud, reported findings (r6 != 6 → STOP bug; r7 != 7 → surprising Phase-66 finding to report).
  </verify>
  <done>The fine-grained tier increments (6→7→8→9→10) are computed as MAX-over-points prefix ranks in the fixed order; the 6-pointwise==6 and +c==7 cross-checks pass (or are reported as loud findings); each increment is in {0,1}; r10 matches the full rank; the dependent slot (if any) is named for the fallback.</done>
</task>

<task type="auto">
  <name>Task 4: Conditional FALLBACK ladder — if full rank < 10, walk to the COMPUTED minimal completing set</name>
  <files>code/ring_generating_set.py</files>
  <action>
    GUARD: this task only does real work if `NEEDS_FALLBACK` (MAX_RANK == 9 from Task 2). If MAX_RANK == 10, record "fallback not needed (natural three complete the set)" and skip to the handoff. COMPUTE — do NOT assume which candidate fills the slot.

    If a natural candidate is dependent (the tier increments from Task 3 name which), walk this DETERMINISTIC ladder, adding candidates in order, recomputing the MAX exact Jacobian rank of the (independent prefix + new candidate) after EACH addition, and STOPPING at the first augmented set that reaches rank 10 (the minimal completing set). Build each fallback candidate as a SymPy poly in the 54 symbols, and re-run the F_4-invariance + bidegree gates from Task 1 on it BEFORE counting it (a fallback candidate must also be a genuine invariant of its claimed bidegree):
      - F1: `E.polarize_d(E.Xsym, E.Xsym, E.Ysym)`  — polarized mixed cubic f(X,X,Y), bidegree (2,1); a DIFFERENT (2,1) invariant from Tr(X²∘Y) (cubic-norm polarization vs trace monomial). Schwarz: polarizations can be independent where trace monomials are dependent.
      - F2: `E.polarize_d(E.Xsym, E.Ysym, E.Ysym)`  — polarized mixed cubic f(X,Y,Y), bidegree (1,2); the (1,2) partner of F1.
      - F3: `E.Tr2(E.jordan(E.Xsym, E.Ysym))` = Tr((X∘Y)²), bidegree (2,2); a DIFFERENT (2,2) invariant from Tr(X²∘Y²) (square of the coupling vs the mixed-square monomial).
      - F5 (only if F1–F3 do not complete): higher mixed trace monomials, increasing total degree, with a STATED total-degree cap (total degree ≤ 5): `E.Tr(E.jordan(E.jordan(E.Xsym,E.Xsym), E.jordan(E.Xsym,E.Ysym)))` (3,1), `E.Tr(E.jordan(E.jordan(E.Xsym,E.Ysym), E.jordan(E.Ysym,E.Ysym)))` (1,3), etc.

    Termination: the trace algebra of the Albert pair is finitely generated (Iltyakov) and trdeg ≤ 10, so the ladder terminates at rank 10 within finitely many candidates. STOP at the first augmented set with rank 10 — report it explicitly with the realized bidegrees and note which natural candidate(s) it replaced. If the ladder caps out below 10 within total degree ≤ 5 (unexpected), report the maximal rank achieved and the stall — do NOT loop indefinitely, do NOT force the count.

    Reward-hacking guard (fp-force-count): the minimal completing set need NOT be unique; report the one the deterministic order finds. Never hand-pick a point or redefine an invariant to reach 10.

    Implement as `run_fallback_ladder(independent_prefix)` returning the minimal completing set + the realized bidegrees + a flag (completed-at-10 vs capped-below-10). When NEEDS_FALLBACK is False, this is a no-op that records the natural three as the answer.
  </action>
  <verify>
    - If NEEDS_FALLBACK: each fallback candidate passes F_4-invariance + bidegree BEFORE being counted; the MAX exact Jacobian rank is recomputed after each addition; the ladder STOPS at the first rank-10 set (the minimal completing set), reported with realized bidegrees and which natural candidate it replaced.
    - If not NEEDS_FALLBACK: recorded as "natural three complete the set; no fallback" (no-op).
    - Termination respected: ladder capped at total degree ≤ 5; a cap-out below 10 is reported honestly (maximal rank), never forced.
    - fp-force-count rejected: no redefinition / point-picking to hit 10.
  </verify>
  <done>The fallback ladder is implemented and (conditionally) executed: on a deficit it walks F1→F2→F3→(F5) deterministically — gating each candidate's invariance + bidegree first — recomputing the MAX exact rank after each, stopping at the first rank-10 minimal completing set (reported with bidegrees and the replaced candidate); on the success path it is a recorded no-op. The honest computed minimal set (whatever it is) is the deliverable.</done>
</task>

<task type="auto">
  <name>Task 5: Decisive cross-checks — two-route trdeg agreement (==10), three-exact-domain width-54 cross-check, exact-only guard</name>
  <files>code/ring_generating_set.py</files>
  <action>
    Wire the decisive cross-checks that certify the verdict is exact and two-route-consistent:

    1. TWO-ROUTE trdeg agreement (test-trdeg-match) — THE decisive cross-check: assert the candidate-Jacobian-derived trdeg (the full/completed MAX rank == 10) EQUALS the Phase-65 orbit-derived trdeg (54 − 44 = 10). Pull the orbit-derived 10 from the Phase-65 SUMMARY as a pre-registered CHECK constant `ORBIT_DERIVED_TRDEG = 54 - 44`. Two INDEPENDENT routes (orbit-tangent rank in Phase 65 vs invariant-Jacobian rank here) agreeing on trdeg=10 is the strongest internal evidence the candidate set is field-complete. Print both numbers and the agreement.

    2. THREE-EXACT-DOMAIN cross-check (test-three-domain): run `exact_rank_route_crosscheck` (reused from the gate module) — or the equivalent pattern (QQ-on-fractional == QQ-on-integer-cleared == ZZ-on-integer-cleared) — on the decisive 10×54 candidate Jacobian at a generic pair. Assert all three exact domains agree on the rank. This certifies `exact_qq_rank` (DomainMatrix-over-QQ) is EXACT over Q at width 54, not a float proxy.

    3. EXACT-ONLY guard (test-exact-only): implement a MODULE-LOCAL `exact_only_guard()` that scans THIS module's `__file__` source (mirror `orbit_dimension_gate.py`'s `exact_only_guard` — it scans its OWN `__file__`, so importing the gate's guard would scan the WRONG file; copy the pattern, point it at `code/ring_generating_set.py`). Assert 0 live `(np|numpy).linalg.matrix_rank(` CALLS and 0 `import octonion_algebra` on the decisive path. A non-decisive numpy float pre-screen (if any) must be prose-distinguishable and never asserted on.

    4. Add a `main()` that runs the full harness in order: `check_mixed_builders()` (Task 1 gates) → `check_full_jacobian_rank()` (Task 2) → `check_tier_increments()` (Task 3) → `run_fallback_ladder(...)` (Task 4, conditional) → the three cross-checks here → the handoff print (Task 6). Use an exit classifier mirroring Phase 65: distinguish a CLEAN PASS (rank 10 reached — natural or via fallback — all cross-checks green) from a BUG (engine/layout/invariance failure, rank > 10, or fallback cap-out). Exit 0 on a clean pass.
  </action>
  <verify>
    - test-trdeg-match: candidate-Jacobian trdeg == orbit-derived trdeg == 10 (printed; two independent routes agree).
    - test-three-domain: QQ-frac == QQ-int == ZZ-int on the decisive 10×54 Jacobian (exact, not float).
    - test-exact-only: 0 numpy float-rank calls AND 0 octonion_algebra imports in code/ring_generating_set.py (module-local guard scanning the correct __file__).
    - `main()` runs the full ordered harness with a clean-pass-vs-bug exit classifier; exits 0 on the rank-10 clean pass.
  </verify>
  <done>The decisive cross-checks pass: the candidate-Jacobian trdeg equals the Phase-65 orbit-derived trdeg (both 10, two-route agreement); the three exact domains agree at width 54; the module-local exact-only guard confirms 0 float-rank calls and 0 octonion_algebra imports on the decisive path; `main()` orchestrates the full harness with a clean-pass exit classifier.</done>
</task>

<task type="auto">
  <name>Task 6: Emit the corrected trdeg-10 generating-set handoff for Phases 66/68 (bidegree table + field-vs-ring boundary)</name>
  <files>code/ring_generating_set.py</files>
  <action>
    Implement `print_generating_set_handoff(minimal_set, bidegrees, tier_ladder, used_fallback)` — the decisive downstream handoff (deliv-generating-set). It must print, as a clearly-delimited structured block the SUMMARY will quote:

    1. The CONFIRMED / REALIZED trdeg-10 generating set: the ordered invariant list (names + builder expressions) with a BIDEGREE TABLE (one row per generator: name, builder, bidegree (deg_X, deg_Y)). On the success path: the 7 base + {Tr(X²∘Y) (2,1), Tr(X∘Y²) (1,2), Tr(X²∘Y²) (2,2)}. If the fallback ran: the realized minimal completing set with its bidegrees, and an explicit note of which natural candidate(s) were replaced and by what.
    2. The realized tier ladder (6,7,8,9,10) and which candidate added each unit of independence.
    3. The TWO-ROUTE agreement statement: candidate-Jacobian trdeg 10 == orbit-derived trdeg 54−44 == 10.
    4. The FIELD-vs-RING BOUNDARY NOTE (test-scope-boundary) — explicit, plain language: "Jacobian rank 10 establishes FIELD-LEVEL functional completeness (the invariant field R(27⊕27)^{F_4} is algebraic over Q(f_1..f_10); trdeg-saturating). This does NOT establish ring generation (Hilbert series / Krull dimension / minimal generators & relations), which is Phase 68's job. Do NOT read this as 'the 10 invariants generate the ring'."
    5. The downstream wiring: "Phase 66 (THE SPINE): the c-independence rank-7 test for {6 pointwise + c} is UNCHANGED, but rank 7 no longer saturates trdeg (there are 3 more independent invariants); (b) is unaffected/strengthened. Phase 67: the unique-degree-2-coupling question is unchanged. Phase 68: the Krull-dimension / Hilbert-series target is the COMPUTED trdeg 10 (the trace-monomial candidates Tr(X²∘Y), Tr(X∘Y²), Tr(X²∘Y²) — or the realized minimal set — are the joint-generator candidates to test for ring reducibility)."

    The SUMMARY (`65.1-01-SUMMARY.md`) must carry this handoff verbatim in its Key Results / Next Phase Readiness, and must state the FIELD-vs-RING boundary explicitly (do NOT overclaim ring generation anywhere). Include the bidegree table and the realized tier ladder in the SUMMARY.
  </action>
  <verify>
    - test-scope-boundary: the handoff print AND the SUMMARY state the FIELD-completeness-vs-RING-generation boundary in plain language; no sentence claims the 10 invariants "generate the ring".
    - The handoff includes: the trdeg-10 generating set with a bidegree table; the realized tier ladder; the two-route agreement; the Phase 66/67/68 wiring.
    - If the fallback ran, the handoff names the realized minimal completing set + which natural candidate(s) it replaced.
  </verify>
  <done>The harness prints a structured trdeg-10 generating-set handoff (ordered list + bidegree table + tier ladder + two-route agreement + explicit field-vs-ring boundary + Phase 66/67/68 wiring); the SUMMARY carries it verbatim and states the field-vs-ring boundary without overclaiming ring generation.</done>
</task>

</tasks>

<verification>
- EXACT over Q end to end: the decisive 10×54 Jacobian rank is `exact_qq_rank` (DomainMatrix-over-QQ); 0 numpy float-rank calls and 0 octonion_algebra imports on the decisive path (module-local exact-only guard); three exact domains agree at width 54.
- Correctness gates BEFORE the rank: the 3 mixed invariants are genuine F_4 invariants (D_M f = 0 for all 52 generators at ≥3 generic octonionic points), grouping-unambiguous (Tr((X∘X)∘Y) = Tr(X∘(X∘Y))), and of the claimed bidegrees (2,1),(1,2),(2,2).
- The DECISIVE result: the full candidate Jacobian rank (MAX over ≥3 generic integer pairs) == 10 == the Phase-65 orbit-derived trdeg 54−44 (two independent routes agree). If the natural three give 9, the deterministic fallback ladder reaches a COMPUTED minimal completing set of rank 10 (reported with bidegrees).
- The fine-grained tier ladder (6→7→8→9→10) is computed and reported; 6-pointwise==6 (single-copy 3+3 cross-check) and +c==7 (Phase-66 SPINE preview) hold; each increment ∈ {0,1}.
- Genericity: only `PAIR_POINTS` with `_pair_not_proportional` asserted; MAX over ≥3 points (rank lower-semicontinuous); no X=Y, no proportional, no real-only/diagonal points.
- Reward-hacking guards: the rank is COMPUTED (target 10 pre-registered as a CHECK, not an input); on a deficit the minimal set is COMPUTED via the fallback, never forced by redefinition or point-picking.
- Scope boundary stated: FIELD-level functional completeness (trdeg-saturating), NOT ring generation (Phase 68); no overclaim.
</verification>

<success_criteria>
- `code/ring_generating_set.py` exists, imports the frozen engine + certified gate machinery (no octonion_algebra.py), and its `main()` runs deterministically to a clean-pass exit (exit 0) with the rank-10 verdict reached (natural three OR a computed minimal completing set).
- The 3 mixed invariants Tr(X²∘Y), Tr(X∘Y²), Tr(X²∘Y²) are certified genuine F_4 invariants (D_M f = 0 over Q for all 52 generators at ≥3 generic octonionic points), grouping-unambiguous, and of bidegrees (2,1),(1,2),(2,2).
- The full candidate Jacobian rank (MAX over ≥3 generic integer pairs, exact_qq_rank over QQ) == 10 == the Phase-65 orbit-derived trdeg (two-route agreement); the three exact domains agree at width 54.
- The fine-grained tier ladder is (6,7,8,9,10) on the success path (or the actual ladder with the dependent slot named + filled by the fallback to a computed minimal rank-10 set); 6-pointwise==6 and +c==7 cross-checks hold.
- The corrected trdeg-10 generating set + bidegree table + tier ladder + two-route agreement + explicit FIELD-vs-RING boundary + Phase 66/67/68 wiring are printed by the harness and carried verbatim into the SUMMARY, with NO overclaim of ring generation.
</success_criteria>

<output>
After completion, create `.gpd/phases/65.1-generating-set-count-correction-pin-down-the-3-missing-joint-invariants-trdeg-10/65.1-01-SUMMARY.md`.
</output>
