---
phase: 75-phase-a-coframe-reduction-dealbreaker-the-kill-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - code/cartan_phaseA_coframe.py
  - derivations/75-coframe-reduction.tex
interactive: true

conventions:
  units: "natural (hbar=1, dimensionless)"
  metric: "(+,-,-,-) mostly-minus; det_2 = x0^2 - x1^2 - x2^2 - x3^2; G = diag(+1,-1,-1,-1); signature (1,3)"
  det_ssot: "ring_lemma_verification.py det_3, cross-term 2Re(x2* x0* x1) (F_4-invariant, CH + 324/324 verified)"
  octonion_mul: "Fano table, e1 e2 = e4"
  complex_structure: "u = e_7 (C_u = span{1, e_7})"
  primitive_idempotent: "E_11 = diag(1,0,0)"
  peirce_layout: "engine 27-coord flat layout [alpha,beta,gamma, x1(8), x2(8), x3(8)]; V_1={0}, V_0={1..10}, V_{1/2}={11..26}; survivors under pi_u: {11,18,19,26} = C_u^2"
  spacetime_subslice: "h_2(C_u) ~= R^{3,1}; Minkowski coords x0=(beta+gamma)/2, x1=Re(x1), x2=<x1,u>, x3=(beta-gamma)/2 (52-kkt-spacetime Eq.46.4)"
  exactness: "EXACT over Q on every decisive verdict (sympy.Matrix.rank/.eigenvals/.nullspace over QQ); NEVER numpy/float (float = triage only)"

dimensional_check:
  image_dim_pi_u_Vhalf: "[dimensionless integer] expect exactly 4 (= dim C_u^2)"
  coframe_signature: "[signature pair] expect Lorentzian (1,3) on the R^{3,1} soldering target"
  bare_trace_gram: "[signature pair] expect Euclidean (4,0) = compact OP^2 Fubini-Study metric (DIAGNOSTIC FOIL, NOT the verdict)"
  soldering_bilinear_rank: "[dimensionless integer] expect exactly 4 (B surjects R^{3,1}; OD3 rank-4-on-pi_u(V_0))"
  residual_structure_group_dim: "[dimensionless integer] residual algebra contains so(3,1) (dim 6) with NO extra free parameters; expect exactly so(3,1) on the Lorentz block"

approximations: []
# NONE. This phase is exact linear algebra over Q. No small parameter, no truncation,
# no approximation. Any "approximately 4d / approximately Lorentzian" is fp-relabel-approx-4d.

contract:
  schema_version: 1
  scope:
    question: "With (E_11,u) fixed, does the C_u bottleneck reduce the 16-dim V_{1/2} soldering form e=dE to a 4-dim coframe that is Lorentzian (1,3) and carries a residual structure group containing SO(3,1) FORCED by (E_11,u) -- or is the route DEAD (image not 4-dim, or not Lorentzian, or requiring an arbitrary extra choice)?"
  claims:
    - id: claim-coframe-reduction
      statement: "THE KILL GATE. With (E_11,u) fixed, the C_u bottleneck reduces the 16-dim V_{1/2} soldering form e=dE to a 4-dim coframe that is Lorentzian (1,3) and carries a residual structure group containing SO(3,1), FORCED by (E_11,u) -- OR it does not (image not 4-dim, or not Lorentzian, or requiring an arbitrary extra choice), in which case the route is DEAD and the KILL is reported flat. Decided by exact image dimension, exact Gram-eigenvalue signature, and a forced-vs-chosen structure-group analysis."
      deliverables: [deliv-phaseA]
      acceptance_tests: [test-coframe-dim, test-coframe-signature, test-coframe-forced]
      references: [ref-52-kkt, ref-52-uniqueness, ref-phase74, ref-sharpe, ref-baez, ref-mccrimmon]
  deliverables:
    - id: deliv-phaseA
      kind: derivation
      path: derivations/75-coframe-reduction.tex
      description: "Phase A coframe-reduction verdict: exact image dim of pi_u(V_{1/2}(16)) with the reduction made explicit; exact Gram-eigenvalue signature of the induced (soldering-form) coframe pairing; forced-vs-arbitrary residual-structure-group analysis; a DECISIVE 4d-Lorentzian-and-forced verdict OR a flat 'Phase A: coframe reduction fails [clause]' KILL and STOP. Backed by exact-over-Q SymPy in code/cartan_phaseA_coframe.py."
      must_contain:
        - "exact image dimension of pi_u(V_{1/2}(16)) over Q (expect 4) with the C_u reduction made explicit"
        - "exact Gram-eigenvalue signature of the induced coframe pairing (expect Lorentzian (1,3)), read on the R^{3,1} soldering target, with the bare trace-form Gram diag(2,2,2,2) reported as the Euclidean OP^2 diagnostic foil (NOT the verdict)"
        - "forced-vs-arbitrary analysis: the residual structure group contains SO(3,1) and is FORCED by (E_11,u), not chosen by hand (exact nullspace/rank over QQ, calibration anchors 61/45 reproduced first)"
        - "if not 4-dim / not Lorentzian / not forced: an explicit 'Phase A: coframe reduction fails [clause]' KILL and STOP"
  references:
    - id: ref-52-kkt
      kind: prior_artifact
      locator: "derivations/52-kkt-spacetime.tex"
      aliases: ["52-kkt", "P46"]
      role: benchmark
      why_it_matters: "The validated V_0 = h_2(O) -> h_2(C_u) ~= R^{3,1} precedent (det_2 Lorentzian (1,3), G=diag(+1,-1,-1,-1) Eq.46.4; SO(3,1) boosts B_i=L_{sigma_i} in Str_0, [B_i,B_j]=-eps_ijk J_k, Lorentz {B_i,J_i} Killing sig (3,3)). The SAME pi_u, REUSED for V_{1/2}; the (1,3) det_2 is the soldering target the coframe maps onto."
      applies_to: [claim-coframe-reduction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-52-uniqueness
      kind: prior_artifact
      locator: "derivations/52-observer-uniqueness.tex"
      aliases: ["52-uniqueness", "OD3", "Uniqueness Theorem"]
      role: benchmark
      why_it_matters: "OD3: V_{1/2} x V_{1/2} -> V_0 surjective, rank 10 (full) on V_0 / rank 4 on pi_u(V_0) -- this IS the soldering bilinear B and the CALC-02 surjectivity check. Uniqueness Theorem: pi_u(V_0)=h_2(C_u) is the UNIQUE 4-dim JSpin(3) subalgebra, FORCED by u -- the V_0 analog VALD-02 must parallel (NOT blindly copy) for V_{1/2}."
      applies_to: [claim-coframe-reduction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-phase74
      kind: prior_artifact
      locator: "code/cartan_phase0_tangent.py and .gpd/phases/74-*/74-01-SUMMARY.md"
      aliases: ["Phase 74", "Phase 0"]
      role: method
      why_it_matters: "Phase 74 (DONE/verified 9/9 HIGH) established the warm-engine reuse pattern, the Peirce layout V_{1/2}=idx 11..26 (V_HALF_IDX=range(11,27)), the verified V_{1/2}-valued soldering form dE (kernel==span{11..26}), the det SSOT source guard, and the _report/_run_engine harness. Copy the driver pattern; do NOT redo the tangent identity."
      applies_to: [claim-coframe-reduction]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [read, use]
    - id: ref-sharpe
      kind: paper
      locator: "R. W. Sharpe, Differential Geometry: Cartan's Generalization of Klein's Erlangen Program (1997)"
      aliases: ["Sharpe 1997"]
      role: definition
      why_it_matters: "The rigorous soldering-form / Cartan-geometry definition g = e^* eta (metric = pullback of the model-space R^{3,1} metric through the soldering form). This is WHY the induced coframe pairing is the soldering-form metric (candidate b), NOT the bare trace form -- the single most important design point of CALC-02."
      applies_to: [claim-coframe-reduction]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [cite]
    - id: ref-baez
      kind: paper
      locator: "Baez, The Octonions, math/0105155 (2002)"
      aliases: ["Baez 2002"]
      role: definition
      why_it_matters: "OP^2 = F_4/Spin(9), T_E OP^2 = V_{1/2}; OP^2 is a compact Riemannian symmetric space (no invariant Lorentzian form) -- the reason the bare trace form is Euclidean (4,0) and the Lorentzian structure must be SOLDERED, not intrinsic to V_{1/2}."
      applies_to: [claim-coframe-reduction]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [cite]
    - id: ref-mccrimmon
      kind: paper
      locator: "McCrimmon, A Taste of Jordan Algebras (2004)"
      aliases: ["McCrimmon"]
      role: definition
      why_it_matters: "Peirce decomposition, E o delta = (1/2)delta, primitive idempotents. Cite for the Peirce identity defining V_{1/2} (already verified Phase 74; do not re-derive)."
      applies_to: [claim-coframe-reduction]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [cite]
  acceptance_tests:
    - id: test-coframe-dim
      subject: claim-coframe-reduction
      kind: oracle
      procedure: "Build the 16 V_{1/2}(E_11) basis elements (engine idx 11..26) via bulk_geometry_verification._standard_basis_27(); apply the literal Phase-46 C_u map pi_u = embedding_under_E_verification.E()/proj_u_exact(a,u_index=7) entrywise to each; assemble the image matrix M (columns = pi_u(b_k) in 27-flat coords); compute rank_M = M.rank() over QQ (DomainMatrix-over-QQ exact path). Independently confirm via the image(B)==pi_u(V_0) subspace-equality identity: rank[ image(B) | pi_u(V_0) ] == 4 (the V_{1/2}-analog of Phase 74's rank[ker | V_HALF_IDX])."
      pass_condition: "Decisive integer over Q: image dimension is EXACTLY 4 (greenlight; survivors {11,18,19,26} = C_u^2) or != 4 (KILL clause (a)). No softening, no 'approximately 4', no selecting a 4-dim sub-piece if the image is not 4."
      evidence_required: [deliv-phaseA, ref-52-uniqueness, ref-phase74]
      automation: automated
    - id: test-coframe-signature
      subject: claim-coframe-reduction
      kind: oracle
      procedure: "VERDICT pairing = the soldering-form metric (Sharpe g=e^*eta), realized exact over Q via the Peirce soldering bilinear B(delta,delta')=(delta o delta')|_{V_0} (OD3 map) into V_0 ~= R^{3,1}: (1) confirm rank(B)=4 onto R^{3,1} (= OD3 rank-4-on-pi_u(V_0)); (2) compute det_2 = x0^2-x1^2-x2^2-x3^2 signature on the R^{3,1} TARGET via Gram eigenvals over QQ -> read (1,3); (3) confirm n(delta)=B(delta,delta) is null (det_2(delta o delta)==0 identically) with forward x0=(1/2)||delta||^2>=0. FOIL (transparency, NOT the verdict): also compute and REPORT the bare trace Gram Tr(jordan(cof_i,cof_j)) over QQ = diag(2,2,2,2), signature (4,0) = the compact OP^2 Fubini-Study metric, stating explicitly it is the diagnostic foil."
      pass_condition: "Soldering target signature is EXACTLY Lorentzian (1,3)/mostly-minus over Q AND B is rank 4 onto R^{3,1} (greenlight) -- OR not (1,3), or B not rank 4 (KILL clause (b)). NO Wick rotation to rescue a non-Lorentzian Gram. The (4,0) trace-form Gram is reported as the foil and is NEVER the verdict; det_2(delta o delta)==0 is the null cone, NEVER mis-read as a degenerate metric."
      evidence_required: [deliv-phaseA, ref-52-kkt, ref-sharpe, ref-baez]
      automation: automated
    - id: test-coframe-forced
      subject: claim-coframe-reduction
      kind: oracle
      procedure: "MEASURE (do not assume): reproduce the calibration anchors Stab_{E_6}(E_11)=61, Stab_{V_0}=45=Spin(9,1) via orbit_dimension_gate (import infinitesimal_action/exact_qq_rank/span_rank_over_QQ ONLY -- do NOT run its slow __main__) BEFORE any new count. Then compute the residual structure group preserving the reduced coframe as the exact nullspace over QQ of the joint action {fix E_11, commute with u=e_7 (preserve C_u), preserve pi_u(V_{1/2})} inside the Spin(9,1) Levi of Stab_{E_6(-26)}(E_11). Show it contains so(3,1) by matching the V_0 boosts B_i=L_{sigma_i} ([B_i,B_j]=-eps_ijk J_k, Lorentz {B_i,J_i} Killing sig (3,3)). Confirm the surviving 4-space = the eigenvalue-1 space of the C_u projector proj_u (canonical, determined by u alone) and V_{1/2} by E_11 alone -- not a hand-picked subspace. Land on the Spin(3,1) 4d vector of the Spin(9,1) ⊃ Spin(3,1) x Spin(6) split, NOT the Spin(6) internal directions."
      pass_condition: "The residual algebra over QQ contains so(3,1) (dim 6) FORCED by (E_11,u) with NO extra free parameters tracing to an arbitrary frame choice, and the 4-space is the C_u-projector eigenspace (greenlight) -- OR the SO(3,1)/4d reduction requires an arbitrary extra choice / has extra free parameters (KILL = fp-arbitrary-reduction). MEASURED via exact rank, never assumed by analogy to V_0."
      evidence_required: [deliv-phaseA, ref-52-kkt, ref-52-uniqueness]
      automation: automated
  forbidden_proxies:
    - id: fp-arbitrary-reduction
      subject: claim-coframe-reduction
      proxy: "A 4-dim coframe or an SO(3,1) obtained by ANY choice not forced by (E_11,u): picking 4 of 16 directions, a non-canonical projection, a Wick rotation, or an arbitrary SO(3,1) frame."
      reason: "'4-dim Lorentzian spacetime' is the hoped-for answer; the V_0 success tempts assuming V_{1/2} obviously parallels it. '4' and the Lorentz group must be EARNED by the exact rank / forced residual-group computation, not smuggled in. A coframe smuggled in by hand is not a result -- it is the KILL."
    - id: fp-relabel-approx-4d
      subject: claim-coframe-reduction
      proxy: "Relabeling a non-4d / non-Lorentzian / not-forced reduction as 'approximately 4d' or 'approximately Lorentzian' or 'morally Einstein'."
      reason: "Negative-result-is-success: a clean Phase A KILL is a valuable, publishable closure. Softening a failed clause to look like a partial success destroys the value of the gate and misleads the downstream Phase 77 greenlight."
    - id: fp-float-decisive
      subject: claim-coframe-reduction
      proxy: "Any decisive verdict (image dimension, Gram signature, soldering-map rank, residual-group dimension) resting on numpy/float rank or floating-point eigenvalues."
      reason: "The ranks and signatures involve exact cancellations (e.g. det_2(delta o delta)==0 identically). A float 'approximately 4' or 'approximately Lorentzian' is explicitly forbidden; every decisive number must be a rational/integer from sympy over QQ. Float is triage-only, never decisive."
    - id: fp-octonion-algebra
      subject: claim-coframe-reduction
      proxy: "Importing octonion_algebra.py (buggy associator, wrong (x1 x2) x3 cross-term order, float, 0.67 associator gap) on any decisive path."
      reason: "octonion_algebra.py is BANNED. The det SSOT is ring_lemma_verification.py det_3; octonion/Jordan primitives come from bulk_geometry_verification.py. Importing the banned engine reintroduces the exact arithmetic-hygiene failures the Phase-0 source guard exists to prevent."
  links:
    - id: link-coframe
      source: claim-coframe-reduction
      target: deliv-phaseA
      relation: supports
      verified_by: [test-coframe-dim, test-coframe-signature, test-coframe-forced]
  uncertainty_markers:
    weakest_anchors:
      - "VALD-02 forced-vs-arbitrary SO(3,1) (test-coframe-forced) is the genuinely-open clause (MEDIUM confidence per research). It must be MEASURED via an exact residual-group rank over QQ, NOT assumed by analogy to the V_0 Uniqueness Theorem. This is where a fp-arbitrary-reduction KILL would trigger."
      - "The judgment that the soldering bilinear B=(delta o delta')|_{V_0} is THE canonical coframe pairing (not merely one defensible choice). Defended by OD3 (the unique Peirce map to the validated R^{3,1}) and the OP^2-Riemannian-symmetric impossibility of an intrinsic Lorentzian form on V_{1/2}; both readings (foil + verdict) are reported so the verifier can audit the reasoning."
    disconfirming_observations:
      - "Image dimension != 4 (e.g. 6, 8, or 16) -> KILL clause (a)."
      - "Soldering bilinear B rank < 4 onto R^{3,1} -> the coframe does not surject the spacetime -> KILL (degenerate soldering)."
      - "det_2 target signature != (1,3) (e.g. (2,2) split, (4,0) Euclidean, or degenerate) -> KILL clause (b)."
      - "Residual structure group has extra free parameters beyond so(3,1) tracing to a frame choice -> fp-arbitrary-reduction KILL clause (c)."
      - "image(B) != pi_u(V_0) (the coframe solders to a look-alike 4-space, not THE validated spacetime) -> framing must be revisited."

estimated_execution:
  total_minutes: 95
  breakdown:
    - task: 1
      minutes: 20
      note: "CALC-01: exact image dim over QQ + image(B)==pi_u(V_0) identity (small matrices, warm engines)"
    - task: 2
      minutes: 30
      note: "CALC-02: soldering bilinear B rank + det_2 (1,3) target signature + null-cone + the (4,0) trace-form foil"
    - task: 3
      minutes: 30
      note: "VALD-02: reproduce 61/45 anchors, then residual-group nullspace over QQ, so(3,1)-forced verdict"
    - task: 4
      minutes: 15
      note: "Verdict synthesis -> derivation; human ratification checkpoint of KILL/greenlight"
---

<objective>
THE KILL GATE for v18.0. Decide, exactly over Q, whether the fixed pair (E_11, u=e_7)
ALONE reduces the 16-dim Peirce half-eigenspace V_{1/2}(E_11) -- the values of the
soldering form e=dE on OP^2=F_4/Spin(9) -- to a 4-dim coframe that is Lorentzian (1,3)
and carries a residual structure group containing SO(3,1) FORCED by (E_11,u). If yes on
all three clauses (dim=4, Lorentzian, forced), Phase A SURVIVES and greenlights Phase 77.
If the image is not 4-dim, or the coframe is not Lorentzian, or the 4d reduction needs an
arbitrary extra choice, the route is DEAD: report "Phase A: coframe reduction fails
[clause]" flat and STOP.

Purpose: This is the cheapest, most decisive gate of the milestone -- pure exact linear
algebra over Q on small matrices, no calculus, no curvature. It either licenses the entire
Cartan/MM connection-curvature gravity route or closes it as a clean, publishable negative.
A conjunctive gate with Phase 76 (A.5): BOTH must SURVIVE to greenlight Phase B (77).

Output:
- code/cartan_phaseA_coframe.py -- exact-over-Q SymPy driver reusing the warm engines,
  with a _report harness printing each decisive integer/signature and an OVERALL verdict line.
- derivations/75-coframe-reduction.tex -- the Phase A verdict derivation (image dim, soldering
  signature with the Euclidean foil, forced-vs-arbitrary residual group, KILL-or-greenlight).

THE SINGLE MOST IMPORTANT DESIGN POINT (do not get this wrong): the bare Jordan trace form
Tr(delta o delta') on pi_u(V_{1/2}) is diag(2,2,2,2) = Euclidean (4,0), because that IS the
compact Fubini-Study metric of the Riemannian symmetric space OP^2=F_4/Spin(9) -- it carries
NO invariant Lorentzian form. Reading the signature verdict THERE manufactures a FALSE
"not Lorentzian" KILL. The verdict pairing is the soldering-form metric (Sharpe g=e^*eta),
realized exact over Q via the Peirce soldering bilinear B(delta,delta')=(delta o delta')|_{V_0}
into V_0 ~= R^{3,1}: read the (1,3) signature on the R^{3,1} TARGET (the validated det_2
benchmark) and confirm B is rank 4 (surjective). Report BOTH the (4,0) trace-form foil and the
(1,3) soldering verdict. Also: det_2(delta o delta)==0 identically is the forward LIGHT CONE
(x0=(1/2)||delta||^2>=0), NOT a degenerate metric -- never mis-read it as a KILL.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/75-phase-a-coframe-reduction-dealbreaker-the-kill-gate/75-RESEARCH.md
@derivations/52-kkt-spacetime.tex
@derivations/52-observer-uniqueness.tex
@code/cartan_phase0_tangent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: CALC-01 -- exact image dimension of pi_u(V_{1/2}(16)) over Q (KILL if != 4)</name>
  <files>code/cartan_phaseA_coframe.py</files>
  <action>
    Create the exact-over-Q driver by COPYING the harness pattern from code/cartan_phase0_tangent.py
    (the _report(label, ok), _run_engine(rel_path) helpers, the unbuffered/print-each-check style, and
    V_HALF_IDX = list(range(11, 27))). Reuse the warm engines -- do NOT rebuild octonion arithmetic:
      import bulk_geometry_verification as BG   # _standard_basis_27, _flat27, _coord_from_octmat, h3o_from_coords, jordan, oct_zero, peirce_indices_under_E11
      import embedding_under_E_verification as EMB   # proj_u_exact(a, u_index=7), E(X), slice_to_complex
      import ring_lemma_verification as RL   # Tr, jordan, X_from_symbols, det_3 (det SSOT)
    SOURCE GUARD (inherit Phase 0): assert NO `import octonion_algebra` anywhere on the decisive path
    (grep the module's own imports + assert it is not in sys.modules after import); fp-octonion-algebra.

    1. Build E_11 = BG.h3o_from_coords(1,0,0, BG.oct_zero(), BG.oct_zero(), BG.oct_zero()) (= diag(1,0,0)).
    2. Build the 16 canonical V_{1/2}(E_11) basis elements: from BG._standard_basis_27(), take the 16
       basis matrices whose nonzero flat-coord is in V_HALF_IDX = range(11,27) (the (1,3)=x2 entry idx
       11..18 and the (1,2)=x3 entry idx 19..26). Confirm via BG.peirce_indices_under_E11() that
       V_{1/2} == list(range(11,27)) (Phase-74 regression; KILL the run if the layout drifted).
    3. Apply pi_u = the LITERAL Phase-46 C_u map EMB.E() (entrywise EMB.proj_u_exact(a, u_index=7),
       which keeps octonion comps 0 and 7 of each entry and zeros comps 1..6) to each of the 16 basis
       elements. Assemble M = sympy.Matrix whose 16 COLUMNS are BG._flat27(EMB.E(b_k)) (27-dim each).
    4. CALC-01 VERDICT: rank_M = M.rank() over QQ (use sympy DomainMatrix/QQ exact path; NEVER numpy).
       Expect EXACTLY 4. Also extract which flat-coords survive (nonzero rows of M) -- expect {11,18,19,26}
       = {Re(x2), <x2,e_7>, Re(x3), <x3,e_7>} = C_u^2. Assert no octonion comp 1..6 survives (if any
       nonzero comp 1..6 appears, the C_u projection is mis-implemented -- STOP).
    5. image(B)==pi_u(V_0) SUBSPACE-EQUALITY IDENTITY (the V_{1/2}-analog of Phase 74's rank[ker|V_HALF]):
       build pi_u(V_0) by applying EMB.E() to a basis of V_0 = idx 1..10 (= h_2(O)); the soldering
       bilinear image image(B) is computed in Task 2, but here pre-register the check column block
       so Task 2 can assert rank[ image(B) | pi_u(V_0) ] == 4 (image solders to THE validated spacetime,
       not a look-alike). In Task 1, just verify dim pi_u(V_0) == 4 over QQ (the V_0 limit: 10 -> 4).

    Use rational basepoints throughout. Float is permitted ONLY as pre-simplify triage (e.g. to see which
    M-rows are nonzero before .rank()), NEVER as the decisive rank (fp-float-decisive).
  </action>
  <verify>
    1. EXACT over Q: M.rank() over QQ == 4 (integer, zero tolerance). KILL clause (a) if != 4 -- do NOT
       pick a 4-dim sub-piece (fp-arbitrary-reduction).
    2. Survivor coords == {11,18,19,26} = C_u^2; zero octonion comps 1..6 survive pi_u (C_u projection correct).
    3. Peirce-layout regression: BG.peirce_indices_under_E11() gives V_{1/2}==range(11,27) (matches Phase 74).
    4. V_0 limit / dimensional cross-check: dim pi_u(V_0) over QQ == 4 (the validated 10 -> 4 reduction; 52-kkt).
    5. Source guard: 0 octonion_algebra imports on the decisive path; 0 numpy/float rank calls (sympy only).
    6. Order-of-expectation met: dim 4 (= dim C_u^2 = 2 entries x 2 C_u comps), matching the V_0 analog (1 entry x 2).
  </verify>
  <done>
    Driver code/cartan_phaseA_coframe.py created from the Phase-74 harness; reuses the warm engines with the
    octonion_algebra source guard active. CALC-01 prints the EXACT image dimension of pi_u(V_{1/2}(16)) over Q
    (expect 4) with the surviving C_u^2 coords {11,18,19,26}, the V_0-limit cross-check dim pi_u(V_0)==4, and the
    Peirce-layout regression -- a decidable integer (4 = greenlight clause (a), or != 4 = flat KILL clause (a)),
    no softening, no fp-arbitrary-reduction sub-piece selection, no fp-float-decisive rank.
  </done>
</task>

<task type="auto">
  <name>Task 2: CALC-02 -- soldering-form-metric signature (verdict) + the Euclidean trace-form foil (KILL if target not (1,3) or B not rank 4)</name>
  <files>code/cartan_phaseA_coframe.py</files>
  <action>
    THE CRUX. Compute the induced coframe signature via the SOLDERING-FORM METRIC (Sharpe g=e^*eta),
    realized exact over Q by the Peirce soldering bilinear B into the R^{3,1} target -- NOT the bare trace
    form. Read the research's resolved Q1 (75-RESEARCH.md "The induced coframe pairing"): candidate (b)
    realized via candidate (c). Append to code/cartan_phaseA_coframe.py.

    (i) FOIL (transparency, NOT the verdict): let cof_1..cof_4 be the 4 surviving coframe directions from
        CALC-01 (the pi_u-images spanning {11,18,19,26}). Build the bare trace Gram
        Gram_ij = RL.Tr(BG.jordan(cof_i, cof_j)) over QQ. Compute Gram.eigenvals() over QQ. EXPECT
        diag(2,2,2,2), signature (4,0) Euclidean. REPORT it explicitly as "the compact OP^2=F_4/Spin(9)
        Fubini-Study metric -- the DIAGNOSTIC FOIL, NOT the coframe signature verdict" (cite Provost-Vallee:
        Re(QGT)=FS metric; OP^2 Riemannian symmetric carries no invariant Lorentzian form). Reading the
        verdict here is Pitfall 1 (the Euclidean trap) -> a FALSE "not Lorentzian" KILL. Do NOT do it.

    (ii) VERDICT -- the Peirce soldering bilinear B(delta,delta') = (delta o delta')|_{V_0}, the OD3 map
         V_{1/2} x V_{1/2} -> V_0 ~= R^{3,1} (52-observer-uniqueness OD3: surjective rank 10 on V_0,
         rank 4 on pi_u(V_0)):
         - For delta, delta' ranging over the 4 reduced coframe dirs (and, for the rank computation, over a
           basis of pi_u(V_{1/2})), compute BG.jordan(delta, delta') and project onto V_0 (flat-coords 1..10).
         - Map the V_0 part to the Minkowski R^{3,1} coords (x0=(beta+gamma)/2, x1=Re(x1), x2=<x1,u>,
           x3=(beta-gamma)/2; 52-kkt Eq.46.4) -- or equivalently apply EMB.E()/EMB.slice_to_complex and read
           the h_2(C_u) ~= R^{3,1} entries.
         - SURJECTIVITY: assemble the soldering map as a matrix onto the 4 R^{3,1} coords; confirm
           rank(B onto R^{3,1}) == 4 over QQ (= OD3 rank-4-on-pi_u(V_0)). KILL if rank < 4 (degenerate soldering).
         - image(B)==pi_u(V_0) identity (from Task 1 pre-registration): rank[ image(B) | pi_u(V_0) ] == 4 over QQ
           (the coframe solders to THE validated spacetime, not a look-alike).
         - TARGET SIGNATURE: the metric on the R^{3,1} target is det_2's polarization
           G = diag(+1,-1,-1,-1), x0^2-x1^2-x2^2-x3^2; its eigenvalues over QQ give signature (1,3). Read it.
           KILL clause (b) if the target signature is not Lorentzian (1,3) -- do NOT Wick-rotate (fp-arbitrary-reduction).
         - NULL-CONE consistency: n(delta) = B(delta,delta); confirm det_2(n(delta)) == 0 IDENTICALLY over QQ
           (Brahmagupta-Fibonacci) with forward x0 = (1/2)||delta||^2 >= 0. This is the LIGHT CONE (correct
           physics: coframe dirs solder to the forward null cone), Pitfall 3 -- do NOT mis-read det_2(delta o delta)==0
           as a degenerate/undefined Gram and KILL.

    Optional cross-check (Approach 2, confirmation only, NOT load-bearing): the +i-eigenspace of the complex
    structure J=u on V_{1/2} should give the same 4-dim image -- report agreement if computed, but do not let it
    carry the verdict (a non-Phase-46 J convention is an fp-arbitrary-reduction foot-gun).
  </action>
  <verify>
    1. FOIL reported: bare trace Gram == diag(2,2,2,2) over QQ, signature (4,0), labeled the Euclidean OP^2
       Fubini-Study FOIL and explicitly NOT the verdict.
    2. VERDICT, EXACT over Q: soldering bilinear B is rank 4 onto R^{3,1}; det_2 target signature == (1,3)
       Lorentzian/mostly-minus. KILL clause (b) if not (1,3) or B not rank 4. No Wick rotation.
    3. NULL-CONE: det_2(B(delta,delta)) == 0 identically over QQ, x0 = (1/2)||delta||^2 >= 0 (forward) -- the
       light cone, NOT a degeneracy (Pitfall 3 guard explicit).
    4. image(B)==pi_u(V_0): rank[ image(B) | pi_u(V_0) ] == 4 over QQ (solders to THE spacetime).
    5. V_0 limit / OD3 cross-check: the soldering map IS the OD3 map (rank-4-on-pi_u(V_0)); reproduces 52-uniqueness.
    6. Order-of-expectation met: signature (1,3) on the target (matches the 52-kkt det_2 (1,3) benchmark, G=diag(+1,-1,-1,-1)).
    7. No fp-float-decisive: all signatures/ranks from sympy.eigenvals/.rank over QQ, never numpy.
  </verify>
  <done>
    CALC-02 reports BOTH (i) the Euclidean trace-form foil diag(2,2,2,2) [(4,0), the OP^2 FS metric, explicitly
    NOT the verdict] and (ii) the VERDICT: the soldering bilinear B is rank 4 (surjective) onto R^{3,1}, the
    det_2 target signature is EXACTLY Lorentzian (1,3), the diagonal n(delta) lands on the forward light cone
    (det_2(delta o delta)==0, x0>=0), and image(B)==pi_u(V_0). A decidable signature ((1,3)+rank-4 = greenlight
    clause (b), or not-(1,3)/B-not-rank-4 = flat KILL clause (b)) -- read on the soldering target, with the
    Euclidean trap (Pitfall 1) and the null-cone trap (Pitfall 3) both explicitly avoided; no Wick rotation,
    no fp-float-decisive.
  </done>
</task>

<task type="auto">
  <name>Task 3: VALD-02 -- forced-vs-arbitrary residual structure group over Q (KILL = fp-arbitrary-reduction if SO(3,1) not forced)</name>
  <files>code/cartan_phaseA_coframe.py</files>
  <action>
    THE GENUINELY-OPEN CLAUSE (MEDIUM confidence -- MEASURE, do not assume by analogy to the V_0 Uniqueness
    Theorem). Decide whether the 4-dim reduction and its SO(3,1) are FORCED by (E_11, u) ALONE, with NO arbitrary
    extra choice. Append to code/cartan_phaseA_coframe.py. Reuse orbit_dimension_gate machinery -- import ONLY the
    helpers, do NOT run its slow __main__ (the ~19-min v16.0 RING pair-orbit gate that exits NONZERO BY DESIGN):
      from orbit_dimension_gate import infinitesimal_action, exact_qq_rank, span_rank_over_QQ

    1. CALIBRATION ANCHORS FIRST (trust nothing until these reproduce; the v16.0 "naive 7 refuted by orbit method"
       precedent): reproduce Stab_{E_6}(E_11) = 61 and Stab_{V_0} = 45 = Spin(9,1) exactly over QQ via the
       orbit_dimension_gate exact-rank path. If either anchor fails to reproduce, STOP -- the residual-group count
       is untrustworthy (do not estimate, do not proceed).
    2. CANONICALITY of the 4-space (no basis selection): show the surviving 4-space pi_u(V_{1/2}) = span{11,18,19,26}
       is exactly the eigenvalue-1 space of the C_u projector proj_u acting on V_{1/2} -- proj_u is determined by
       u=e_7 ALONE, and V_{1/2} by E_11 ALONE. Demonstrate the image was computed by RANK (Task 1), not by choosing
       4 of 16 directions. (If the 4-space required picking a sub-piece, that is fp-arbitrary-reduction.)
    3. RESIDUAL STRUCTURE GROUP: compute the Lie algebra preserving the reduced coframe as the exact NULLSPACE over QQ
       of the joint infinitesimal action {fix E_11; commute with u=e_7 multiplication (preserve C_u); preserve the
       4-space pi_u(V_{1/2})} inside the Spin(9,1) Levi of Stab_{E_6(-26)}(E_11) (dim 45). Use infinitesimal_action /
       span_rank_over_QQ on the 45-dim Levi generators. Report dim of the residual algebra.
    4. SO(3,1)-FORCED test: show the residual algebra CONTAINS so(3,1) by matching the V_0 Lorentz generators --
       the boosts B_i = L_{sigma_i} in Str_0 with [B_i,B_j] = -eps_ijk J_k (NEGATIVE sign = non-compact) and the
       rotations J_i, the Lorentz block {B_i,J_i} having Killing signature (3,3) = so(3,1) (52-kkt "Boost identification").
       Connect to the Spin(9,1) ⊃ Spin(3,1) x Spin(6) split (Phase 48 so(3)xso(6)): the reduction must land on the
       Spin(3,1) 4d VECTOR (Lorentz), NOT the Spin(6) internal directions.
    5. FORCED-VS-ARBITRARY VERDICT: SO(3,1) is FORCED iff the residual algebra contains so(3,1) with NO extra free
       parameters tracing to an arbitrary frame choice. KILL = fp-arbitrary-reduction (clause (c)) if the residual
       group has extra free parameters beyond so(3,1) attributable to a frame/chart choice, or if SO(3,1) only
       appears after an arbitrary extra input. Report the verdict FLAT either way (negative-result-is-success).
  </action>
  <verify>
    1. Calibration anchors reproduced EXACTLY over QQ FIRST: Stab_{E_6}(E_11)==61, Stab_{V_0}==45=Spin(9,1)
       (matches Phase 74 VALD-01). STOP if either fails.
    2. Canonicality: the 4-space == eigenvalue-1 space of the C_u projector proj_u (u-determined); V_{1/2} E_11-determined;
       image computed by rank, not selection (no fp-arbitrary-reduction in the construction).
    3. Residual algebra dim from an EXACT nullspace over QQ (infinitesimal_action / span_rank_over_QQ), never float.
    4. so(3,1) CONTAINED: residual algebra ⊇ span{B_i, J_i}, Killing signature (3,3), [B_i,B_j]=-eps_ijk J_k matched to
       the 52-kkt boosts; lands on the Spin(3,1) 4d vector of Spin(9,1) ⊃ Spin(3,1) x Spin(6), not Spin(6) internal.
    5. FORCED verdict decided: so(3,1) forced by (E_11,u) with NO extra free parameters (greenlight) OR extra free
       parameters / arbitrary input required (KILL = fp-arbitrary-reduction). MEASURED, not assumed.
    6. Order-of-expectation: residual Lorentz block dim == 6 = dim so(3,1) (the V_0 analog target).
    7. No fp-float-decisive: every rank/nullspace dim over QQ.
  </verify>
  <done>
    VALD-02 reproduces the calibration anchors (61/45) FIRST, then reports the residual-structure-group dimension as
    an EXACT nullspace over QQ of {fix E_11, commute with u, preserve pi_u(V_{1/2})} in the Spin(9,1) Levi, the
    canonicality of the 4-space (C_u-projector eigenspace, not a hand-picked sub-piece), and a DECIDED forced-vs-arbitrary
    verdict: so(3,1) FORCED by (E_11,u) with no extra free parameters (greenlight clause (c)) OR SO(3,1)/the 4d
    reduction requires an arbitrary extra choice (flat KILL clause (c) = fp-arbitrary-reduction) -- matched to the
    52-kkt boosts (sig (3,3)) and the Spin(3,1) 4d vector of the Spin(9,1) ⊃ Spin(3,1) x Spin(6) split, measured by
    exact rank and never assumed by analogy to V_0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verdict synthesis -> deliv-phaseA derivation + human ratification of the KILL/greenlight</name>
  <files>derivations/75-coframe-reduction.tex</files>
  <action>
    Write derivations/75-coframe-reduction.tex synthesizing the three exact-over-Q computations into the Phase A
    verdict (start with an ASSERT_CONVENTION header mirroring 52-kkt-spacetime: natural units, metric_signature=mostly_minus,
    det SSOT = ring_lemma det_3, u=e_7, E_11=diag(1,0,0)). The derivation MUST contain, at true strength, no softening:
      (a) CALC-01: the exact image dimension of pi_u(V_{1/2}(16)) over Q with the C_u reduction made explicit
          (the survivors {11,18,19,26}=C_u^2; the V_0-limit cross-check dim pi_u(V_0)=4).
      (b) CALC-02: the exact Gram-eigenvalue signature of the induced coframe pairing, read on the R^{3,1}
          SOLDERING TARGET as Lorentzian (1,3) via the Peirce bilinear B (rank 4, surjective; n(delta) on the
          forward light cone), WITH the bare trace-form Gram diag(2,2,2,2) reported as the Euclidean OP^2
          Fubini-Study FOIL (explicitly NOT the verdict; cite Sharpe for the soldering-form metric g=e^*eta and
          Provost-Vallee/Baez for the OP^2 FS-metric foil).
      (c) VALD-02: the forced-vs-arbitrary residual-structure-group analysis (anchors 61/45 reproduced; residual
          algebra ⊇ so(3,1) FORCED by (E_11,u) with no extra free parameters, matched to the 52-kkt boosts (sig (3,3))
          and the Spin(3,1) 4d vector) -- or the fp-arbitrary-reduction KILL.
      (d) THE VERDICT: either "Phase A SURVIVES: (E_11,u) forces a 4-dim Lorentzian (1,3) coframe carrying SO(3,1)
          -- greenlight Phase 77" (only if ALL THREE clauses pass) OR "Phase A: coframe reduction fails [clause a/b/c]"
          -- a flat, publishable KILL that STOPS the route. Cite the conjunctive gate with Phase 76.
    Explicitly REJECT each forbidden proxy in the success statement: NO fp-arbitrary-reduction (the 4 and SO(3,1) are
    EARNED by exact rank / forced residual group, never smuggled by picking directions or Wick-rotating); NO
    fp-relabel-approx-4d (a failed clause is reported flat as a KILL, never "approximately 4d/Lorentzian"); NO
    fp-float-decisive (every decisive number is rational/integer from sympy over QQ); NO fp-octonion-algebra
    (octonion_algebra.py absent on the decisive path). Negative-result-is-success.
  </action>
  <verify>
    Present to the researcher (Bryan) for RATIFICATION of the milestone-gating verdict (the math is DECIDABLE from the
    exact integers/signature in code/cartan_phaseA_coframe.py -- this checkpoint ratifies the KILL/greenlight CALL and
    its consequence for the Phase 77 greenlight, not the arithmetic). Surface for inspection:
      1. The three decidable numbers: image dim (expect 4), soldering target signature (expect (1,3)) + B rank (expect 4),
         residual-group forced-vs-arbitrary verdict (the MEDIUM-confidence clause (c) -- the one that can KILL).
      2. The both-readings transparency: the (4,0) trace-form foil printed ALONGSIDE the (1,3) soldering verdict, so the
         claim "4-dim Lorentzian" is shown as a SOLDERED (not intrinsic-to-V_{1/2}) property -- the honest framing that
         defeats fp-relabel.
      3. Whether the VALD-02 residual group is genuinely forced (greenlight) or hides an arbitrary choice (KILL).
      4. The overall verdict line (SURVIVES->greenlight 77, or KILL [clause]->STOP) and that it is reported at true strength.
    Note prior precedent: Bryan ratified the decisive verdicts at Ph70.1 and Ph73; this is the same class of milestone
    KILL/greenlight gate.
  </verify>
  <resume-signal>Type "approved" to ratify the verdict (SURVIVES greenlights Phase 77 ∥ closes with Phase 76's gate; KILL stops the route as a publishable closure), or describe required corrections.</resume-signal>
  <done>
    derivations/75-coframe-reduction.tex written with the three exact-over-Q results and a DECISIVE verdict (SURVIVES
    greenlight OR flat "Phase A: coframe reduction fails [clause]" KILL), every forbidden proxy explicitly rejected in
    the success statement, the soldering-vs-trace-form design point baked in (both readings reported; verdict on the
    soldering target), and the researcher has ratified the KILL/greenlight call.
  </done>
</task>

</tasks>

<verification>
- EXACT over Q on EVERY decisive number (sympy.Matrix.rank/.eigenvals/.nullspace over QQ; never numpy/float) -- fp-float-decisive guard.
- octonion_algebra.py ABSENT on the decisive path (source guard, inherited from Phase 0) -- fp-octonion-algebra guard.
- The induced coframe signature is read on the R^{3,1} SOLDERING TARGET (the validated det_2 (1,3)), NOT off the bare trace form (which is the Euclidean (4,0) OP^2 FS foil, reported for transparency only) -- the single most important design point; Pitfall 1 (Euclidean trap) avoided.
- det_2(delta o delta) == 0 identically is the forward LIGHT CONE, NOT a degenerate metric -- Pitfall 3 avoided.
- The "4" and SO(3,1) are EARNED by exact rank / forced residual-group computation, never smuggled by selecting directions, a non-canonical projection, or a Wick rotation -- fp-arbitrary-reduction guard.
- V_0 LIMIT consistency: the SAME pi_u on V_0 reproduces the validated 10 -> 4 det_2 (1,3) reduction (52-kkt); the soldering bilinear B IS the OD3 map (rank-4-on-pi_u(V_0), 52-uniqueness); image(B) == pi_u(V_0) (solders to THE spacetime).
- Calibration anchors (61/45) reproduced BEFORE any new VALD-02 stabilizer count (the v16.0 "naive 7 refuted" precedent).
- Order-of-expectation: dim 4, signature (1,3), residual Lorentz block ⊇ so(3,1) (dim 6) -- each stated as the EXPECTED value and decided exactly, never assumed.
- A failed clause is a flat KILL ("Phase A: coframe reduction fails [clause]"), reported at true strength; negative-result-is-success -- fp-relabel-approx-4d guard.
</verification>

<success_criteria>
Phase A is DECIDED exactly over Q on all three clauses, with the verdict reported at true strength and the researcher's ratification:

1. CALC-01: the exact image dimension of pi_u(V_{1/2}(16)) over Q is computed as a decidable integer -- EXACTLY 4 (greenlight clause (a), survivors {11,18,19,26}=C_u^2) or != 4 (flat KILL clause (a)). No fp-arbitrary-reduction sub-piece selection.
2. CALC-02: the induced coframe pairing's exact signature is read on the R^{3,1} soldering target -- Lorentzian (1,3) with B rank 4 surjective (greenlight clause (b)) or non-(1,3)/B-not-rank-4 (flat KILL clause (b)). The Euclidean (4,0) trace-form foil is reported transparently and is NEVER the verdict; det_2(delta o delta)==0 is the light cone, never a KILL. No Wick rotation.
3. VALD-02: the residual structure group is MEASURED as an exact nullspace over QQ (anchors 61/45 reproduced first) -- so(3,1) FORCED by (E_11,u) with no extra free parameters, matched to the 52-kkt boosts (sig (3,3)) and the Spin(3,1) 4d vector (greenlight clause (c)) or an arbitrary extra choice required (flat KILL clause (c) = fp-arbitrary-reduction).
4. deliv-phaseA (derivations/75-coframe-reduction.tex) carries all three results and a DECISIVE verdict (SURVIVES -> greenlight Phase 77 [conjunctive with Phase 76], or KILL [clause] -> STOP as a publishable closure), with every forbidden proxy (fp-arbitrary-reduction, fp-relabel-approx-4d, fp-float-decisive, fp-octonion-algebra) explicitly rejected.
5. Forbidden proxies rejected: NO float-decisive verdict; NO octonion_algebra import; NO smuggled 4/SO(3,1); NO "approximately 4d/Lorentzian" relabel of a KILL.
</success_criteria>

<output>
After completion, create `.gpd/phases/75-phase-a-coframe-reduction-dealbreaker-the-kill-gate/75-01-SUMMARY.md`.
</output>
