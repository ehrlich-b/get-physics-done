---
phase: 61-slice-satisfies-clause-iii
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - code/slice_clause_iii_verification.py
  - tests/test_slice_clause_iii.py
interactive: false

conventions:
  units: "natural (hbar=1, k_B=1); pure algebra — no physical dimensions"
  metric: "riemannian_fisher (N/A this plan); type/category consistency is the dimensional-analysis analog"
  algebra: "Jordan product a o b = (1/2)(ab+ba); sequential product a&b = sqrt(a) b sqrt(a) (Luders)"
  slice: "A = h_3(C_u) ~ M_3(C)^sa; n = 3; complex structure u = e_7 (any u in S^6 equivalent under G_2)"
  arithmetic: "exact symbolic/rational (SymPy); NO floating point for the decisive assertions"

dimensional_check:
  # 'Dimensions' here = real vector-space / category dimensions (the pure-algebra analog).
  dim_M3C_sa: "[real dim] = 9 (Jordan rank 3)"
  dim_M9C_sa: "[real dim] = 81"
  dim_minimal_composite: "[real dim] = dim(M_3(C)^sa) * dim(M_3(C)^sa) = 9*9 = 81 (local tomography)"
  dim_maximal_composite: "[real dim] = 2*81 = 162 (universal C_3 box-tilde C_3 = M_9(C)^sa (+) M_9(C)^sa; extra classical bit) — NOT the clause (iii) object"

approximations:
  - name: "none (exact symbolic verification)"
    parameter: "N/A — pure algebra, exact rational/symbolic arithmetic"
    validity: "always exact; assertions are exact-equality / exact-zero, NOT float-tolerance"
    breaks_when: "N/A; if any numeric (eigenvalue) check is unavoidable, fix tolerance to symbolic zero or |x| < 1e-12 and justify"
    check: "Every assertion uses SymPy .equals(zeros)/== 0 / Rational equality; no np.isclose on decisive results"

estimated_execution:
  total_minutes: 50
  breakdown:
    - task: 1
      minutes: 30
      note: "Extend 2x2 helpers to 3x3; verify rank 3, three orthogonal rank-1 projective units summing to I_3, simplicity (no nontrivial central idempotents), composite dim 81 (Symbolic + matrix, 1.3x multiplier applied)"
    - task: 2
      minutes: 20
      note: "Associative product-form sequential product factorizes on M_9(C)^sa = M_3(C)^sa (x) M_3(C)^sa; pytest harness mirroring test_octonion_h3o.py"

contract:
  schema_version: 1
  scope:
    question: "Does explicit exact-symbolic matrix computation confirm that A = M_3(C)^sa has Jordan rank 3 with three mutually orthogonal rank-1 projective units summing to I_3, is simple (no nontrivial central idempotents), has standard composite of real dimension 81 = 9*9, and carries an associative product-form sequential product a&b = sqrt(a) b sqrt(a) factorizing on M_3(C)^sa (x) M_3(C)^sa ~ M_9(C)^sa?"
  claims:
    - id: claim-slice-clause-iii
      statement: "The C*-bottleneck slice A = h_3(C_u) ~ M_3(C)^sa satisfies all four clauses of Paper 5 Definition 1 as a self-modeler in its own right; this plan supplies the hard computational (SymPy/matrix) evidence backing clauses (i), (iv) and the clause-(iii) product-form sequential product / composite-dimension structure."
      deliverables: [deliv-slice-clause-iii]
      acceptance_tests: [test-slice-clause-iii, test-sympy-rank-units, test-sympy-simplicity, test-sympy-composite-dim, test-sympy-seqprod-factorize]
      references: [ref-paper5-def1, ref-lem-bottleneck, ref-bgw]
  deliverables:
    - id: deliv-slice-clause-iii
      kind: code
      path: derivations/p5-basin-restriction/
      description: "VALD-61-01 computational evidence: code/slice_clause_iii_verification.py + tests/test_slice_clause_iii.py — exact-symbolic confirmation of rank 3, three orthogonal rank-1 projective units summing to I_3, simplicity, composite real-dim 81, and associative product-form sequential-product factorization on M_3(C)^sa (x) M_3(C)^sa. This is the hard-evidence portion of the clause-by-clause deliverable that 61-01's prose cites."
      must_contain:
        - "rank 3 (three mutually orthogonal nontrivial projective units summing to I_3)"
        - "no nontrivial central idempotents (simplicity / clause iv)"
        - "dim(M_3(C)^sa (x) M_3(C)^sa) = dim(M_9(C)^sa) = 81 = 9*9"
        - "associative product-form sequential product a&b = sqrt(a) b sqrt(a) factorizes on the associative composite M_9(C)^sa"
        - "exact-symbolic assertions (no float tolerance on decisive checks); runtime < 5 s"
        - "ASSERT_CONVENTION header tagging slice A = M_3(C)^sa, n=3, exact arithmetic"
  references:
    - id: ref-paper5-def1
      kind: spec
      locator: "~/repos/blog/landing/papers/qm-from-self-modeling/main.tex def:self-modeling-system (line ~342): clause (i) sms:finite (~346), (iii) sms:minimal (351-353), (iv) sms:simple (~354)"
      aliases: ["Paper 5 Def 1", "def:self-modeling-system"]
      role: definition
      why_it_matters: "Clause (i) demands a finite-dim spectral OUS with >=2 orthogonal nontrivial projective units; clause (iv) demands simplicity; clause (iii) demands a minimal composite with product-form sequential product. This plan computes the exact-matrix witnesses for each. LIVE paper only — not the stale repo papers/ copy."
      applies_to: [claim-slice-clause-iii]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-lem-bottleneck
      kind: prior_artifact
      locator: "~/repos/blog/landing/papers/sm-from-self-modeling/sections/complexification.tex lem:bottleneck (line 409). rem:converse is prompt-inline (p5-basin-restriction-prompt.md), NOT yet labeled in the live paper (grep-verified absent, Phase 60)."
      aliases: ["lem:bottleneck", "Paper 7 bottleneck lemma"]
      role: method
      why_it_matters: "Supplies the slice A = h_3(C_u) ~ M_3(C)^sa (n=3) whose structure this plan verifies. The associative product-form sequential-product factorization is the Phase 60 OPEN ITEM (60-02 unvalidated_assumptions): asserted from the Luders form, this plan re-derives it by exact matrix computation on M_3(C)^sa (x) M_3(C)^sa."
      applies_to: [claim-slice-clause-iii]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-bgw
      kind: paper
      locator: "Barnum-Graydon-Wilce, Quantum 4, 359 (2020); arXiv:1606.09331v3. Grounded in Phase 60 (rem-converse-bgw.md). Composite-dimension baseline: minimal/standard composite M_{n^2}(C)^sa real-dim n^4; maximal/universal C_n box-tilde C_n = M_{n^2}(C)^sa (+) M_{n^2}(C)^sa real-dim 2n^4."
      aliases: ["BGW", "BGW 2020", "Barnum-Graydon-Wilce"]
      role: benchmark
      why_it_matters: "Fixes the EXACT composite-dimension targets and the corrected minimal != maximal fact. The dim-81 (minimal, n^4=81) vs dim-162 (maximal, 2n^4=162) computation is the quantitative signature of the extra classical bit. This plan's composite-dimension check must reproduce the MINIMAL composite (81), the clause (iii) object, and must NOT silently treat the maximal composite as the clause (iii) one."
      applies_to: [claim-slice-clause-iii]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, compare, cite]
  acceptance_tests:
    - id: test-slice-clause-iii
      subject: claim-slice-clause-iii
      kind: human_review
      procedure: "Run code/slice_clause_iii_verification.py and pytest tests/test_slice_clause_iii.py. Confirm all SymPy assertions pass exactly (rank 3; three orthogonal rank-1 projective units summing to I_3; no nontrivial central idempotents; composite real-dim 81; product-form sequential-product factorization). Confirm runtime < 5 s and that decisive assertions are symbolic/exact (no float tolerance). Confirm the script reproduces standard M_3(C)^sa structure (a failure here is the backtracking trigger)."
      pass_condition: "All assertions pass exactly; runtime < 5 s; rank 3, simplicity, composite dim 81, and seq-product factorization confirmed by exact arithmetic; standard structure reproduced."
      evidence_required: [deliv-slice-clause-iii, ref-paper5-def1, ref-bgw]
      automation: hybrid
    - id: test-sympy-rank-units
      subject: claim-slice-clause-iii
      kind: existence
      procedure: "Construct M_3(C)^sa; exhibit three mutually orthogonal rank-1 projections E_11, E_22, E_33 (and verify on a non-diagonal F_4-rotated/random unitary-conjugated frame too); check E_ii^2 = E_ii (idempotent / projective unit), E_ii o E_jj = 0 for i != j (orthogonal), each rank 1 and nontrivial (!= 0, != I_3), and E_11 + E_22 + E_33 = I_3. Confirm no fourth nonzero orthogonal projective unit exists (rank caps at 3)."
      pass_condition: "Exactly three mutually orthogonal nontrivial rank-1 projective units found, summing to I_3; rank(A) = 3 >= 2 (clause (i) ingredient); holds in both standard and a rotated frame."
      evidence_required: [deliv-slice-clause-iii]
      automation: automated
    - id: test-sympy-simplicity
      subject: claim-slice-clause-iii
      kind: consistency
      procedure: "Verify M_3(C)^sa is simple: compute the center of the associated complex algebra M_3(C) and confirm it is C*I_3 (scalars only) — equivalently, no nontrivial central idempotent z (z^2 = z, z != 0, z != I, z commutes with all of M_3(C)). Solve for central elements symbolically (X commutes with a generating set of M_3(C)) and confirm only multiples of I_3 survive."
      pass_condition: "Center = scalar multiples of I_3; no nontrivial central idempotent exists; M_3(C)^sa admits no nontrivial direct-sum decomposition into order-unit subspaces (clause (iv) — simple)."
      evidence_required: [deliv-slice-clause-iii]
      automation: automated
    - id: test-sympy-composite-dim
      subject: claim-slice-clause-iii
      kind: dimensional_analysis
      procedure: "Compute real dimensions: dim_R(M_3(C)^sa) = 9 (rank 3 Hermitian); dim_R(M_3(C)^sa (x) M_3(C)^sa) via the standard tensor product = dim_R(M_9(C)^sa) = 81; verify 81 = 9*9 (local tomography / product-dimension bookkeeping for the MINIMAL composite). Separately record the MAXIMAL/universal composite real-dim 2*81 = 162 and assert 162 != 81 (the extra classical bit, BGW) — confirming the clause (iii) object is the minimal composite, NOT the maximal."
      pass_condition: "dim_R(minimal composite) = 81 = 9*9 (matches dim M_9(C)^sa); dim_R(maximal composite) = 162 != 81; minimal composite confirmed as the local-tomography-respecting clause (iii) object."
      evidence_required: [deliv-slice-clause-iii, ref-bgw]
      automation: automated
    - id: test-sympy-seqprod-factorize
      subject: claim-slice-clause-iii
      kind: consistency
      procedure: "On the ASSOCIATIVE composite M_9(C)^sa = M_3(C)^sa (x) M_3(C)^sa, verify the product-form (Luders) sequential product factorizes across the body-model tensor split: for product effects a = a_B (x) a_M and b = b_B (x) b_M, check (a & b) = (sqrt(a) b sqrt(a)) equals (sqrt(a_B) b_B sqrt(a_B)) (x) (sqrt(a_M) b_M sqrt(a_M)) = (a_B & b_B) (x) (a_M & b_M), using matrix_sqrt generalized to 3x3 / 9x9 and exact rational test effects. Also confirm 1 & a = a (S3 unitality) and a&b is an effect (0 <= a&b <= I) on a sample. Stay on the ASSOCIATIVE slice — do NOT reach into h_3(O) (that non-associative computation is Phase 62)."
      pass_condition: "Product-form sequential product factorizes exactly across the tensor split on M_9(C)^sa for the tested product effects; S3 holds; result is an effect. Phase 60 open item (asserted-from-Luders) now confirmed by explicit matrix computation."
      evidence_required: [deliv-slice-clause-iii, ref-lem-bottleneck]
      automation: automated
  forbidden_proxies:
    - id: fp-redefine-iii
      subject: claim-slice-clause-iii
      proxy: "Treating clause (iii) as 'automatic because A = M_3(C)^sa' without an explicit projective-unit / simplicity / composite-dimension computation, or weakening 'minimal' or dropping any of the four data (product states, product effects, non-signaling, product-form sequential product)."
      reason: "Clause (iii) is the complex-field-forcing condition. The whole point of VALD-61-01 is to produce the EXPLICIT computational witnesses (rank 3, summation to I_3, simplicity, dim 81, factorizing sequential product) rather than asserting them. 'Looks right' without the computation is exactly the roadmap-flagged proxy."
    - id: fp-conflate-composites
      subject: claim-slice-clause-iii
      proxy: "Computing the dimension/structure of the maximal/universal composite M_9(C)^sa (+) M_9(C)^sa (real-dim 162) and reporting it as the clause (iii) object, or otherwise identifying the observer's internal composite V_BM with the BGW universe-tensoring of h_3(O)."
      reason: "Per Phase 60 (CONFIRMED-WITH-CAVEAT), the clause (iii) object is the MINIMAL/standard composite M_9(C)^sa (real-dim 81), a direct summand of the maximal one; minimal != maximal. Reporting 162 as 'the composite' would reintroduce the stale 'minimal=maximal' error from the wrong direction and conflate the two composites."
    - id: fp-reach-into-h3o
      subject: claim-slice-clause-iii
      proxy: "Letting the Phase 61 sequential-product SymPy reach into the non-associative h_3(O) ambient (e.g. importing H3O / jordan_product on octonionic entries) to 'verify' factorization."
      reason: "Phase 61 verifies the ASSOCIATIVE slice M_3(C)^sa and its associative composite M_9(C)^sa in its own right. The non-associative h_3(O) computation and the induced-by-E question are Phase 62's job (NON-ASSOCIATIVITY guard). Mixing them here would pre-empt Phase 62 and is a scope violation."
    - id: fp-float-pass
      subject: claim-slice-clause-iii
      proxy: "Passing the decisive rank/orthogonality/simplicity/dimension assertions via np.isclose / floating-point tolerance instead of exact symbolic equality."
      reason: "The phase requires symbolic/numeric-EXACT assertions (pure algebra, exact rational arithmetic). A float-tolerance pass on structural integer facts (rank 3, dim 81, idempotency) is unjustified and could mask an error."
  links:
    - id: link-slice-clause-iii
      source: claim-slice-clause-iii
      target: deliv-slice-clause-iii
      relation: supports
      verified_by: [test-slice-clause-iii, test-sympy-rank-units, test-sympy-simplicity, test-sympy-composite-dim, test-sympy-seqprod-factorize]
    - id: link-composite-dim-bgw
      source: ref-bgw
      target: deliv-slice-clause-iii
      relation: benchmarks
      verified_by: [test-sympy-composite-dim]
    - id: link-seqprod-openitem
      source: ref-lem-bottleneck
      target: deliv-slice-clause-iii
      relation: depends_on
      verified_by: [test-sympy-seqprod-factorize]
  uncertainty_markers:
    weakest_anchors:
      - "rem:converse remains prompt-authoritative / not-yet-in-live-paper (FUTR-01); this plan does not depend on its wording, only on the standard structure of M_3(C)^sa and the BGW-grounded composite dimensions (Phase 60). The minimal-composite dim target (81) is the load it carries."
      - "The associative product-form sequential-product factorization was asserted from the Luders form in Phase 60 (60-02 unvalidated_assumptions) and is re-derived here; until this plan runs, it is the least-certain computational claim."
    disconfirming_observations:
      - "If rank(M_3(C)^sa) != 3, or the three rank-1 projections fail to sum to I_3, or a nontrivial central idempotent is found (simplicity fails), or dim(M_3(C)^sa (x) M_3(C)^sa) != 81 — any of these contradicts standard structure and is the BACKTRACKING TRIGGER (revisit Phase 60 framing / rem:converse confirmation)."
      - "If the product-form sequential product does NOT factorize across the tensor split on the associative composite M_9(C)^sa, clause (iii)'s 'product-form sequential product' datum fails for the slice in its own right — a genuine obstruction to be reported honestly, NOT papered over."
---

<objective>
Produce the hard, exact-symbolic computational evidence (VALD-61-01) that the C*-bottleneck slice A = h_3(C_u) ~ M_3(C)^sa has the structure clauses (i), (iii), (iv) of Paper 5 Definition 1 require, AS AN ASSOCIATIVE ALGEBRA IN ITS OWN RIGHT.

Purpose: This plan is the evidence foundation for Phase 61. The clause-by-clause derivation (61-02 PLAN, wave 2) cites THIS plan's computed values rather than asserting them — so the rank-3 / projective-unit / simplicity / composite-dimension / sequential-product-factorization claims are backed by explicit matrix computation, defeating the roadmap-flagged "looks right" and "automatic because M_3(C)^sa" proxies. It also closes the Phase 60 open item: the product-form sequential product, asserted from the Luders form in 60-02, is here re-derived by exact matrix computation on the associative composite M_9(C)^sa = M_3(C)^sa (x) M_3(C)^sa.

Output: code/slice_clause_iii_verification.py (standalone, runtime < 5 s, exact arithmetic) and tests/test_slice_clause_iii.py (pytest, mirroring tests/test_octonion_h3o.py patterns).

SCOPE GUARD: This is the ASSOCIATIVE slice only. Do NOT touch the non-associative h_3(O) ambient or the induced-by-E question — those are Phase 62 (fp-reach-into-h3o). Clause (iii) is checked AS STATED; do NOT weaken "minimal" or drop any of its four data (fp-redefine-iii). The clause (iii) composite is the MINIMAL composite M_9(C)^sa (real-dim 81), NOT the maximal/universal one (real-dim 162, extra classical bit) — do NOT conflate them (fp-conflate-composites).
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/CONVENTIONS.md
@derivations/p5-basin-restriction/rem-converse-bgw.md
@derivations/p5-basin-restriction/STATE.md
@code/sp_verification.py
@tests/test_octonion_h3o.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exact-symbolic verification of rank 3, projective units, simplicity, and composite dimension on M_3(C)^sa</name>
  <files>code/slice_clause_iii_verification.py</files>
  <action>
    Create code/slice_clause_iii_verification.py — a standalone SymPy script verifying the structural facts clauses (i) and (iv) need, plus the composite-dimension fact clause (iii) needs, for A = M_3(C)^sa.

    REUSE existing infrastructure: import from code/sp_verification.py where possible. The 2x2 helpers (hermitian_2x2, matrix_sqrt, is_positive_semidefinite, is_effect) are hardwired for 2x2 — GENERALIZE the patterns to 3x3 (and 9x9 for the composite in Task 2). Use SymPy Matrix with exact Rational / symbolic entries throughout. Do NOT use numpy floats for decisive assertions.

    Put at the top of the file an ASSERT_CONVENTION header comment:
    "# ASSERT_CONVENTION: slice A = M_3(C)^sa (n=3); Jordan product a o b = (1/2)(ab+ba); sequential product a&b = sqrt(a) b sqrt(a); exact symbolic/rational arithmetic; pure algebra (no physical dimensions); LIVE-paper provenance."

    Implement and ASSERT (exact, via .equals(zeros)/== 0 / Rational equality):

    1. hermitian_3x3(real params) builder: construct a general 3x3 Hermitian matrix (9 real params: 3 real diagonal, 3 complex off-diagonal). Confirm Hermiticity M = M.H symbolically. Confirm dim_R(M_3(C)^sa) = 9 by counting independent real parameters.

    2. RANK 3 / three orthogonal rank-1 projective units (clause (i) ingredient — DERV-61-01):
       - Define E_11, E_22, E_33 = the three standard diagonal rank-1 projections (e_i e_i^dag).
       - Assert each is a projective unit: E_ii^2 == E_ii (idempotent), E_ii Hermitian, E_ii != 0 and E_ii != I_3 (nontrivial), and rank(E_ii) == 1.
       - Assert mutual orthogonality via the Jordan product: E_ii o E_jj = (1/2)(E_ii E_jj + E_jj E_ii) == 0 for i != j (and equivalently E_ii E_jj == 0).
       - Assert completeness: E_11 + E_22 + E_33 == I_3.
       - Assert rank caps at 3: there is no nonzero projective unit orthogonal to all three (their orthocomplement in M_3(C)^sa is {0}).
       - ROTATED-FRAME check (robustness): pick a fixed unitary U in M_3(C) (e.g. a rational/√2 entry unitary, or a representative of an F_4-type rotation acting on the slice) and verify the conjugated projections U E_ii U^dag remain three mutually orthogonal rank-1 projective units summing to I_3 — so the result is frame-independent (>= 2 orthogonal nontrivial projective units holds intrinsically, clause (i) AS STATED).

    3. SIMPLICITY / no nontrivial central idempotents (clause (iv) — DERV-61-02):
       - Compute the center of M_3(C): solve symbolically for X (3x3 complex) such that X commutes with a generating set of M_3(C) (e.g. the matrix units E_12, E_23, E_13 and their adjoints, or the standard generators). Assert the only solutions are scalar multiples of I_3 (center = C*I_3).
       - Equivalently, assert no nontrivial central idempotent: the only idempotents z with z^2 = z that are central (commute with all of M_3(C)) are z = 0 and z = I_3. Hence M_3(C)^sa admits no nontrivial direct-sum decomposition into nonzero order-unit subspaces — it is SIMPLE.

    4. COMPOSITE DIMENSION (clause (iii) ingredient — DERV-61-03, BGW-grounded):
       - Assert dim_R(M_3(C)^sa) = 9 and dim_R(M_9(C)^sa) = 81.
       - Assert the MINIMAL/standard composite M_3(C)^sa (x) M_3(C)^sa has real dimension 81 = 9*9 (local tomography / product-dimension bookkeeping). Build it concretely if cheap: the standard tensor product of two 3x3 Hermitian spaces is the 9x9 Hermitian space M_9(C)^sa (dim 81); a kron-product spot check on a couple of basis elements suffices, plus the dimension count 9*9 == 81.
       - Assert the MAXIMAL/universal composite C_3 box-tilde C_3 = M_9(C)^sa (+) M_9(C)^sa has real dimension 2*81 = 162, and assert 162 != 81 — the quantitative signature of the extra classical bit (BGW Table 2 / Cor 4.16, per Phase 60 rem-converse-bgw.md). Record (comment + printed line) that the clause (iii) object is the MINIMAL composite (81), NOT the maximal — minimality SELECTS the standard summand. Do NOT report 162 as "the composite."

    Print a clear PASS/FAIL line per check and a final summary. Guard the runtime: the whole script must finish in < 5 s (use exact rationals and small fixed test matrices; avoid expensive fully-symbolic eigen-decompositions where a constructive witness suffices). Make the script exit nonzero if any assertion fails.

    CRITICAL framing notes to embed as comments:
    - These computations REPRODUCE the standard, known structure of M_3(C)^sa (rank 3, simple). They are the "known results" the phase must recover; a FAILURE is the backtracking trigger (revisit Phase 60), not something to force-pass.
    - Clause (iii) is checked AS STATED — minimality in full force; the composite is the minimal one (fp-redefine-iii, fp-conflate-composites rejected).
    - STALE-TEXT GUARD: ROADMAP Phase 61 Success Criterion 3 still literally says "minimal = maximal per BGW" — this is STALE (pre-Phase-60). The corrected fact (Phase 60) is minimal != maximal; the minimal composite is a direct summand of the maximal. This script's dim 81 vs 162 check IS the witness of that correction. Do NOT reproduce "minimal = maximal coincide" anywhere.
  </action>
  <verify>
    Run: python3 code/slice_clause_iii_verification.py ; echo "exit: $?"
    1. rank(M_3(C)^sa) = 3; E_11, E_22, E_33 are three mutually orthogonal nontrivial rank-1 projective units; E_11+E_22+E_33 = I_3 (exact). Holds in the rotated frame too.
    2. center(M_3(C)) = C*I_3; no nontrivial central idempotent; M_3(C)^sa simple (exact).
    3. dim_R(M_3(C)^sa) = 9; dim_R(minimal composite) = 81 = 9*9 = dim_R(M_9(C)^sa); dim_R(maximal composite) = 162 != 81 (exact integer checks).
    4. All decisive assertions are exact (no float tolerance); script exits 0; runtime < 5 s (time it).
    5. Type/category consistency: every object tagged (M_3(C)^sa special simple FRJA; minimal composite simple FRJA; maximal composite non-simple FRJA). "minimal vs maximal" is a same-category comparison with answer NOT-equal.
  </verify>
  <done>code/slice_clause_iii_verification.py exists, runs in < 5 s, exits 0, and proves by exact symbolic arithmetic: rank 3 with three orthogonal rank-1 projective units summing to I_3 (clause i), simplicity / no nontrivial central idempotent (clause iv), and the minimal-composite real-dim 81 = 9*9 with the maximal composite real-dim 162 != 81 (clause iii composite, BGW-corrected). Standard M_3(C)^sa structure reproduced; no "minimal=maximal" phrasing.</done>
</task>

<task type="auto">
  <name>Task 2: Associative product-form sequential product factorizes on M_9(C)^sa = M_3(C)^sa (x) M_3(C)^sa; pytest harness</name>
  <files>tests/test_slice_clause_iii.py</files>
  <action>
    Close the Phase 60 open item: verify by exact matrix computation that the product-form (Luders) sequential product a&b = sqrt(a) b sqrt(a) factorizes across the body-model tensor split on the ASSOCIATIVE composite M_9(C)^sa = M_3(C)^sa (x) M_3(C)^sa. Then wrap the Task-1 structural checks and this factorization check in a pytest harness mirroring tests/test_octonion_h3o.py.

    Add the factorization computation to code/slice_clause_iii_verification.py (functions) and the assertions to tests/test_slice_clause_iii.py (pytest test functions). REUSE matrix_sqrt from sp_verification.py generalized to 3x3 and 9x9 (the 2x2 closed-form in sp_verification will NOT work for 3x3 — use SymPy's eigendecomposition-based positive square root, or .sqrt() on the diagonalized form, with exact entries; verify sqrt(a)^2 == a as a self-check).

    Implement and ASSERT (exact symbolic):

    1. matrix_sqrt_nxn(M): positive square root of an n x n PSD Hermitian matrix via spectral decomposition (eigenprojectors with sqrt of eigenvalues). Self-check: matrix_sqrt_nxn(M)^2 == M for a couple of exact 3x3 PSD test effects; result is Hermitian and PSD.

    2. PRODUCT-FORM FACTORIZATION (the Phase 60 open item — DERV-61-03 datum 4):
       - Take product effects a = kron(a_B, a_M) and b = kron(b_B, b_M) where a_B, a_M, b_B, b_M are exact 3x3 effects (rational entries, eigenvalues in [0,1]); include at least one NON-diagonal (off-diagonal) factor so the test is non-trivial.
       - Compute LHS = a & b = sqrt(a) b sqrt(a) on the full 9x9 composite (using matrix_sqrt_nxn on the 9x9 a).
       - Compute RHS = kron(a_B & b_B, a_M & b_M) = kron(sqrt(a_B) b_B sqrt(a_B), sqrt(a_M) b_M sqrt(a_M)) on the 3x3 factors.
       - Assert LHS == RHS exactly (uses sqrt(kron(a_B,a_M)) = kron(sqrt(a_B), sqrt(a_M)) for PSD factors — confirm this identity too as an intermediate assertion). This is the product-form sequential product factorizing across the tensor split.
       - S3 unitality: assert I_9 & a = a for a sample product effect (and 1 & a = a on a 3x3 sample). Effect range: assert 0 <= a&b <= I_9 (PSD and I_9 - (a&b) PSD) on a sample.

    3. SCOPE GUARD (assert by construction, and state in a comment): this computation stays entirely within the ASSOCIATIVE algebras M_3(C)^sa and M_9(C)^sa. It does NOT import H3O / octonion_algebra non-associative machinery; it does NOT compute on h_3(O). The non-associativity of the GENERAL (non-product) sequential product on M_n(C)^sa (already established for 2x2 in sp_verification.py, required by Westerbaan-Westerbaan-vdW) may be noted but the DECISIVE Phase 61 claim is the product-FORM FACTORIZATION on the associative composite, which is what clause (iii) datum 4 asks for. (fp-reach-into-h3o rejected.)

    4. pytest harness tests/test_slice_clause_iii.py: mirror tests/test_octonion_h3o.py structure (clear test_* functions, exact assertions, fast). Cover: rank 3 + three orthogonal projective units summing to I_3; simplicity (no nontrivial central idempotent); composite dim 81 (and maximal 162 != 81); matrix_sqrt_nxn self-check; product-form factorization; S3; effect range. Each test must use exact assertions (assert simplify(...).equals(...) / == 0 / == expected integer), NOT float tolerance.

    Keep total runtime (script + pytest) under ~5 s for the decisive checks (the < 5 s budget is for VALD-61-01's core assertions; the pytest file may include a couple extra robustness cases but should stay fast — prefer small exact matrices).
  </action>
  <verify>
    Run: python3 code/slice_clause_iii_verification.py ; echo "exit: $?"
    Run: python3 -m pytest tests/test_slice_clause_iii.py -q ; echo "exit: $?"
    1. matrix_sqrt_nxn self-check passes (sqrt(M)^2 == M, Hermitian, PSD) for 3x3 test effects (exact).
    2. Product-form factorization: sqrt(a) b sqrt(a) == kron(a_B & b_B, a_M & b_M) exactly for product effects with at least one off-diagonal factor; the intermediate sqrt(kron) = kron(sqrt) identity holds.
    3. S3: I_9 & a = a (and 1 & a = a on 3x3 sample); effect range 0 <= a&b <= I_9 on sample.
    4. No import of H3O / non-associative octonion machinery (grep the file: it must NOT import octonion_algebra's H3O/jordan_product for the seq-product check). Stays on the associative slice.
    5. pytest: all tests pass; exact assertions (no np.isclose on decisive checks); fast.
  </verify>
  <done>The product-form sequential product a&b = sqrt(a) b sqrt(a) is proven by exact matrix computation to factorize across the body-model tensor split on the associative composite M_9(C)^sa = M_3(C)^sa (x) M_3(C)^sa (Phase 60 open item CLOSED), with S3 and effect-range confirmed; tests/test_slice_clause_iii.py (pytest) passes all structural + factorization checks with exact assertions; the computation stays entirely on the associative slice (no h_3(O) reach, fp-reach-into-h3o rejected).</done>
</task>

</tasks>

<verification>
- EXACTNESS: every decisive assertion is symbolic/rational-exact (.equals(zeros)/== 0/integer equality), NOT float-tolerance. Error budget is N/A (exact arithmetic); any unavoidable numeric eigenvalue check uses exact-zero / |x| < 1e-12 with justification.
- STRUCTURAL (clauses i, iv): rank 3; three mutually orthogonal nontrivial rank-1 projective units summing to I_3 (frame-independent); simple (center = C*I_3, no nontrivial central idempotent). These REPRODUCE standard M_3(C)^sa structure — the phase's "known results"; failure = backtracking trigger.
- COMPOSITE (clause iii ingredient): minimal composite real-dim 81 = 9*9 = dim(M_9(C)^sa); maximal/universal composite real-dim 162 != 81 (extra classical bit, BGW-corrected). Clause (iii) object = MINIMAL composite (fp-conflate-composites rejected); minimality selects the standard summand; NO "minimal=maximal" phrasing.
- SEQUENTIAL PRODUCT (clause iii datum 4): product-form a&b factorizes across the tensor split on the ASSOCIATIVE M_9(C)^sa (Phase 60 open item closed); S3 + effect range hold.
- SCOPE: associative slice only; NO h_3(O) / non-associative reach (fp-reach-into-h3o rejected) — that is Phase 62.
- TYPE/CATEGORY: every object tagged (special simple FRJA M_3(C)^sa; simple FRJA minimal composite; non-simple FRJA maximal composite); "minimal vs maximal" is a same-category comparison, answer NOT-equal.
- RUNTIME: VALD-61-01 core assertions complete in < 5 s; script exits 0; pytest passes.
</verification>

<success_criteria>
VALD-61-01 satisfied by exact symbolic computation: (1) Jordan rank 3 with three mutually orthogonal rank-1 projective units summing to I_3 (clause i ingredient, frame-independent); (2) simplicity — no nontrivial central idempotent (clause iv); (3) minimal composite real-dim 81 = 9*9, maximal composite real-dim 162 != 81 (clause iii composite, BGW-corrected — minimal != maximal); (4) product-form sequential product a&b = sqrt(a) b sqrt(a) factorizes on the associative composite M_9(C)^sa (Phase 60 open item closed). Runtime < 5 s; all assertions exact; standard M_3(C)^sa structure reproduced. Explicitly NOT done by redefining clause (iii) (fp-redefine-iii rejected — every datum checked, minimality in full force), NOT by conflating minimal with maximal composite (fp-conflate-composites rejected — 81 reported as the clause (iii) object, 162 flagged as the extra-classical-bit maximal), NOT by reaching into the non-associative h_3(O) (fp-reach-into-h3o rejected — that is Phase 62), and NOT by float-tolerance passes (fp-float-pass rejected). STALE-TEXT GUARD honored: the corrected minimal != maximal fact is the dim 81 vs 162 witness; no "minimal=maximal coincide" wording reproduced.
</success_criteria>

<output>
After completion, create `.gpd/phases/61-slice-satisfies-clause-iii/61-02-SUMMARY.md`.
</output>
