---
phase: 65-f-4-construction-orbit-dimension-gate
plan: 03
type: execute
wave: 3
depends_on: ["65-01", "65-02"]
files_modified: [code/orbit_dimension_gate.py]
interactive: true

conventions:
  arithmetic: "exact symbolic over Q (SymPy 1.14, Rational/QQ); NO float64 on any decisive path"
  rank_operator: "sympy.Matrix(...).rank() over QQ; numpy.linalg.matrix_rank / np.linalg.matrix_rank FORBIDDEN on the decisive path"
  jordan_product: "X∘Y = (1/2)(XY + YX) (frozen engine `jordan`)"
  octonion_table: "Fano e1*e2 = e4 (frozen engine)"
  cubic_norm: "det_3 generic norm, cross 2*Re((x2*x1)*x3) (Phase-64.1 corrected); frozen engine det_3 verbatim"
  coordinatization: "engine-native 27-per-copy layout; 54-symbol pair (xs for X, ys for Y); _flat27 / X_from_symbols"
  f4_builder: "f_4 = span{[L_a,L_b]} (dim 52, Plan 01); builder CERTIFIED by single-copy orbit 24 (Plan 02)"
  diagonal_action: "F_4 acts DIAGONALLY on h_3(O) (+) h_3(O): the SAME 27x27 matrix M_xi acts on X and on Y. The xi-th pair-orbit-tangent row is the 54-vector (M_xi . x*, M_xi . y*) at a generic rational pair (x*, y*) = (_flat27(X*), _flat27(Y*)). Substitute the pair BEFORE forming the matrix."
  orbit_dim_criterion: "Derksen-Kemper char-0: pair generic orbit dim = rank over Q of the (generators x 54) infinitesimal-action matrix at a generic point; trdeg of R[27(+)27]^{F_4} = 54 - orbit_dim"
  basis_selection: "to de-risk the pair rank, FIRST select 52 independent generators from the 324 brackets (greedy incremental rank growth on the 729-flattenings, ~19s measured); the orbit rank of a 52-row basis equals that of the 324-row spanning set but is far lighter to rank"

dimensional_check:
  pair_orbit_dim: 'exact rank over QQ of the (52 x 54) infinitesimal-action matrix at a generic rational pair (substitute first) == 47 (expected); orbit_dim <= 47'
  pair_stabilizer: 'dim f_4 - pair_orbit_dim = 52 - 47 = 5 (expected); pair-stabilizer dim >= 5'
  pair_trdeg: '54 - pair_orbit_dim == 7 (THE milestone anchor: six pointwise + c)'

approximations: []
# NONE. Exact finite linear algebra over Q. The ONLY non-exactness budget is the
# numpy float pre-screen (NON-DECISIVE scaffold) which never enters the verdict.

environment_requirements:
  python: ">=3.11"
  libraries:
    - name: "sympy"
      used_for: "exact rank over QQ of the 52x54 pair infinitesimal-action matrix (the decisive GATE value)"
      version: "1.14.0 (present)"
    - name: "numpy"
      used_for: "OPTIONAL non-decisive float pre-screen of the pair rank ONLY (never the verdict); planner spike showed exact rank of the FULL 324-row pair stack stalls (>9 min) so the selected-52-basis route is required for the decisive computation"
      version: "2.4.2 (present)"
  external_tools: []  # SageMath NOT needed for Phase 65

estimated_execution:
  total_minutes: 60
  breakdown:
    - task: 1
      minutes: 40
      note: "Select 52 independent generators (greedy incremental QQ rank on 729-flattenings, ~19s measured) THEN build the (52 x 54) diagonal-action tangent matrix at a generic rational pair (substitute first), exact rank over QQ -> pair_orbit_dim. Recompute at >=3 integer points, take MAX. Exact-rank x1.5; the full 324-row pair stack STALLS (planner spike >9 min) so the 52-basis route is mandatory."
    - task: 2
      minutes: 15
      note: "Derive trdeg = 54 - orbit_dim; assert THE anchor 54 - orbit_dim == 7 (orbit 47, stab >= 5); wire the milestone GATE/backtracking; full-harness ALL_PASS + exact-only guard. Then the milestone go/no-go checkpoint."
    - task: 3
      minutes: 5
      note: "checkpoint:human-verify -- the milestone go/no-go (54 - orbit_dim = 7). Chain-critical: gates Phases 66/67/68."

patterns_consulted:
  insights: []
  error_patterns: []
  adjustments_made:
    - "PERF (planner spike, MEASURED): the exact rank of the FULL 324-row pair (324x54) stack at small integer points STALLS (>9 min, timed out at 540s). MITIGATION baked into Task 1: select 52 independent generators FIRST (greedy incremental QQ rank, ~19s), then rank the (52 x 54) matrix -- the orbit rank of a basis equals that of the spanning set. Substitute the pair point BEFORE forming the matrix; tangents as matrix*vector; small integer points (avoid denominator blowup)."
    - "ANCHOR is a GATE (not a soft check): 54 - orbit_dim = 7 is the milestone go/no-go. balanced autonomy + chain-critical result -> one checkpoint:human-verify at the end (Task 3). If != 7, STOP the milestone (do not proceed to Phases 66/67/68)."

contract:
  schema_version: 1

  scope:
    question: "What is the EXACT generic orbit dimension of F_4 acting diagonally on h_3(O) (+) h_3(O), computed as the rank over Q of the 52x54 infinitesimal-action matrix at a generic rational point -- and does the resulting transcendence degree 54 - orbit_dim equal 7 (the milestone go/no-go anchor: six pointwise generators + the coupling c)? This value (forbidden to look up; the Spin(8)-triality back-of-envelope is a trap) fixes the target rank for Phase 66 (THE SPINE) and the Krull/Hilbert dimension for Phase 68."

  claims:
    - id: claim-pair-orbit-dim
      statement: "The generic orbit dimension of F_4 on h_3(O) (+) h_3(O) (DIAGONAL action: the same M_xi acts on X and Y) is the exact rank over Q of the 52x54 infinitesimal-action matrix (row xi = (M_xi . x*, M_xi . y*)) at a generic rational pair, substituting the point BEFORE the rank, recomputed at >=3 integer points (MAX taken). It is COMPUTED, not looked up; the selected-52-independent-generator basis is used (its orbit rank equals the 324-row spanning set's)."
      deliverables: [deliv-basis-selection, deliv-pair-rank]
      acceptance_tests: [test-pair-rank-exact, test-pair-multipoint, test-pair-basis-equiv]
      references: [ref-engine, ref-derksen-kemper]
    - id: claim-trdeg-7-anchor
      statement: "The transcendence degree of R[h_3(O)(+)h_3(O)]^{F_4} is trdeg = 54 - pair_orbit_dim, and the milestone consistency anchor 54 - orbit_dim == 7 holds (orbit_dim == 47, pair-stabilizer dim = 52 - 47 = 5 >= 5). This = six pointwise generators + the coupling c, fixing the target Jacobian rank for Phase 66 and the Krull dimension for Phase 68."
      deliverables: [deliv-trdeg-anchor]
      acceptance_tests: [test-anchor-7, test-orbit-le-47, test-stab-ge-5]
      references: [ref-engine, ref-derksen-kemper, ref-project-scope]

  deliverables:
    - id: deliv-basis-selection
      kind: code
      path: code/orbit_dimension_gate.py
      description: "Selection of 52 independent f_4 generators from the 324 inner derivations: greedy incremental rank growth on the 729-flattenings over QQ until rank 52 (perf de-risking; ~19s measured). Asserts exactly 52 selected and that they span the full f_4."
      must_contain: ["52", "rank"]
    - id: deliv-pair-rank
      kind: code
      path: code/orbit_dimension_gate.py
      description: "Pair orbit-dimension computation: at a generic rational pair (x*, y*), the (52 x 54) matrix with row (M_xi . x*, M_xi . y*) for each selected generator; exact rank over QQ == pair_orbit_dim. Evaluated at >=3 generic integer points, MAX taken. Substitute the pair BEFORE forming the matrix."
      must_contain: ["rank", "54", "orbit_dim"]
    - id: deliv-trdeg-anchor
      kind: code
      path: code/orbit_dimension_gate.py
      description: "trdeg = 54 - pair_orbit_dim; the assertion of the milestone anchor 54 - orbit_dim == 7 (orbit 47, stab 5); the GATE/backtracking comment (STOP the milestone if != 7); and a recorded handoff of (pair_orbit_dim, trdeg) for Phases 66/67/68."
      must_contain: ["54", "7", "trdeg"]

  references:
    - id: ref-engine
      kind: prior_artifact
      locator: "code/ring_lemma_verification.py (frozen engine) + code/orbit_dimension_gate.py Plans 01-02 (f_4 dim 52 + single-copy orbit 24 CERTIFIED)"
      aliases: ["frozen engine", "certified f_4 builder"]
      role: definition
      why_it_matters: "Provides the CERTIFIED f_4 generators (dim 52 in Plan 01; single-copy orbit 24 in Plan 02), _flat27, X_from_symbols + the 54-symbol pair layout, octonionic points, and the exact-only/rank-routing convention. The pair tangents reuse the SAME generators; the diagonal action applies each M to both x* and y*."
      applies_to: [claim-pair-orbit-dim, claim-trdeg-7-anchor]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use]
    - id: ref-derksen-kemper
      kind: paper
      locator: "Derksen, H.; Kemper, G. -- Computational Invariant Theory, Section 4"
      aliases: ["Derksen-Kemper", "DK 2002"]
      role: method
      why_it_matters: "Char-0 orbit-dimension criterion: generic orbit dim = rank of the infinitesimal action at a generic point; trdeg of the invariant field = ambient dim (54) - orbit dim. THE justification for computing the pair value as an exact rank and reading trdeg = 54 - orbit_dim."
      applies_to: [claim-pair-orbit-dim, claim-trdeg-7-anchor]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [cite, use]
    - id: ref-project-scope
      kind: spec
      locator: ".gpd/PROJECT.md / .gpd/ROADMAP.md ((RING) milestone; the anchor 54 - orbit_dim = 7 = six pointwise + c; GATE for Phases 66/67/68)"
      aliases: ["PROJECT.md", "ROADMAP", "RING scope"]
      role: must_consider
      why_it_matters: "The anchor 54 - orbit_dim = 7 is the milestone go/no-go: 7 = the six pointwise generators {Tr,Tr^2,det of X and Y} + the coupling c, the expected trdeg / target Jacobian rank (Phase 66) / Krull dimension (Phase 68). A value != 7 means the generating-set picture is wrong -> STOP."
      applies_to: [claim-trdeg-7-anchor]
      carry_forward_to: [planning]
      must_surface: true
      required_actions: [use]

  acceptance_tests:
    - id: test-pair-rank-exact
      subject: claim-pair-orbit-dim
      kind: consistency
      procedure: "Select 52 independent generators from the 324 brackets (greedy incremental QQ rank on 729-flattenings). Pick a generic rational/integer pair (x*, y*) (both genuinely octonionic, distinct, X* not proportional to Y*). Build the (52 x 54) matrix: row for each selected M is the 54-vector concat(M . v_x, M . v_y), v_x = Matrix(_flat27(X*)), v_y = Matrix(_flat27(Y*)); matrix*vector tangents; substitute the pair BEFORE forming the matrix. Compute .rank() over QQ."
      pass_condition: "rank over QQ == pair_orbit_dim, a definite integer <= 47 (expected exactly 47). The full 324-row spanning set is NOT required (test-pair-basis-equiv justifies the basis)."
      evidence_required: [deliv-basis-selection, deliv-pair-rank, ref-engine, ref-derksen-kemper]
      automation: automated
    - id: test-pair-multipoint
      subject: claim-pair-orbit-dim
      kind: consistency
      procedure: "Recompute the (52 x 54) rank at >=3 independent generic integer pairs and take the MAX (rank lower-semicontinuous; the generic orbit dim is the max over sampled points). Use small integer coordinates to avoid denominator blowup."
      pass_condition: "The MAX over >=3 integer pairs == pair_orbit_dim (stable; no pair gives a larger rank)."
      evidence_required: [deliv-pair-rank, ref-derksen-kemper]
      automation: automated
    - id: test-pair-basis-equiv
      subject: claim-pair-orbit-dim
      kind: consistency
      procedure: "Justify the selected-52-basis route: confirm the 52 selected generators span f_4 (their 729-flattening rank over QQ == 52, = the full span). Since the orbit-tangent map is linear in the generator, the row space of the (52 x 54) basis-tangent matrix equals that of the (324 x 54) spanning-set matrix; hence equal rank. (Optionally spot-check at ONE point that a modest random sub-sample of the extra brackets adds no rank.)"
      pass_condition: "The 52 selected generators have 729-flattening rank 52 over QQ (a genuine basis of f_4), so the basis pair-rank equals the spanning-set pair-rank."
      evidence_required: [deliv-basis-selection]
      automation: automated
    - id: test-anchor-7
      subject: claim-trdeg-7-anchor
      kind: consistency
      procedure: "Compute trdeg = 54 - pair_orbit_dim and assert == 7. THIS IS THE MILESTONE GO/NO-GO."
      pass_condition: "54 - pair_orbit_dim == 7 (orbit_dim == 47): trdeg of R[27(+)27]^{F_4} = 7 = six pointwise + c."
      evidence_required: [deliv-trdeg-anchor, ref-project-scope, ref-derksen-kemper]
      automation: automated
    - id: test-orbit-le-47
      subject: claim-trdeg-7-anchor
      kind: consistency
      procedure: "Assert pair_orbit_dim <= 47 (a generic pair cannot have orbit dim exceeding 47 if trdeg >= 7; the bound is the sanity guard on the rank)."
      pass_condition: "pair_orbit_dim <= 47."
      evidence_required: [deliv-pair-rank]
      automation: automated
    - id: test-stab-ge-5
      subject: claim-trdeg-7-anchor
      kind: consistency
      procedure: "Compute pair-stabilizer dim = 52 - pair_orbit_dim and assert >= 5."
      pass_condition: "52 - pair_orbit_dim >= 5 (= 5 when orbit_dim = 47; the generic pair stabilizer is at least 5-dimensional)."
      evidence_required: [deliv-trdeg-anchor]
      automation: automated

  forbidden_proxies:
    - id: fp-lookup-pair-orbit
      subject: claim-pair-orbit-dim
      proxy: "Looking up the pair orbit dimension in the literature, or asserting trdeg = 7 from the 'six pointwise + c' expectation without computing the 52x54 rank."
      reason: "COMPUTE pair_orbit_dim as the exact rank over Q of the 52x54 infinitesimal-action matrix at >=3 generic points; the anchor 54 - orbit_dim = 7 is a CHECK on the computed value, not an input. Tempting because the expected answer (7) is stated in the roadmap, inviting one to assert rather than compute and let the anchor 'confirm' a hardcoded value."
    - id: fp-triality-backofenvelope
      subject: claim-pair-orbit-dim
      proxy: "Deriving the pair stabilizer from the single-copy Spin(8) by a triality back-of-envelope (the three 8's are permuted, so a generic element of one copy is NOT trivially stabilized by the other's Spin(8))."
      reason: "COMPUTE the pair principal isotropy via the 52x54 rank; the pair stabilizer (dim 5 expected) is NOT the single-copy Spin(8) (dim 28) and is NOT obtainable by elementary triality counting. Tempting because Spin(8) triality is a famous structure that invites a slick-looking shortcut that is wrong for the pair."
    - id: fp-float-rank
      subject: claim-pair-orbit-dim
      proxy: "numpy.linalg.matrix_rank / np.linalg.matrix_rank (or float SVD tolerance) on the DECISIVE 52x54 rank. A float tolerance fabricates the 47-vs-other / trdeg-7-vs-6 verdict."
      reason: "Use sympy.Matrix(...).rank() over QQ for the verdict; numpy float rank is allowed ONLY as a clearly-labelled NON-DECISIVE pre-screen that never enters the assertion. Tempting because the planner spike showed the exact pair rank is the heaviest step and numpy is instant, but the verdict must be exact."
    - id: fp-full-stack-rank
      subject: claim-pair-orbit-dim
      proxy: "Ranking the full 324-row pair stack symbolically or on fraction-heavy points -- the planner spike confirmed this STALLS (>9 min, timed out)."
      reason: "Select 52 independent generators FIRST (basis), substitute small INTEGER pair points BEFORE forming the matrix, and rank the (52 x 54) integer matrix over QQ; test-pair-basis-equiv justifies that the basis rank equals the spanning-set rank. Tempting because using all 324 brackets 'to be safe' seems more rigorous, but it is computationally infeasible and unnecessary (basis rank == span rank)."
    - id: fp-nongeneric-pair
      subject: claim-pair-orbit-dim
      proxy: "Using a non-generic pair point: X*=Y*, X* proportional to Y*, real-only/diagonal points, or repeated spectra -- these can DROP the rank below the generic value and corrupt the orbit dim."
      reason: "Use generic rational pairs: both genuinely octonionic, distinct, X* not proportional to Y*, distinct diagonals; take the MAX over >=3 such points (rank lower-semicontinuous). Tempting because simple/symmetric points are easier to type and faster to rank, but they live on special loci where the rank drops."

  uncertainty_markers:
    weakest_anchors:
      - "The pair value 47 / trdeg 7 is the one number NOT independently confirmed by the planner spike (the full-stack pair rank timed out); it is computed fresh in this plan via the 52-basis route. The single-copy 24 (Plan 02, MEASURED at exit 0) is the strongest cross-check that the action and rank machinery are correct."
      - "The anchor 7 = six pointwise + c is an EXPECTATION from the generating-set picture; it is a GATE, so a computed value != 7 is a real milestone-level disconfirmation, not a tuning target."
    disconfirming_observations:
      - "54 - orbit_dim != 7 (e.g. trdeg 6 or 8) -> the 'six pointwise + c' generating-set picture is WRONG. STOP the milestone; do NOT proceed to Phases 66/67/68 (roadmap GATE). Consider /gpd:research-phase only on this failure. Do NOT tune the point or the basis to force 7."
      - "the 52-basis pair rank disagrees with a (feasible) 324-row spot check at a shared point -> the basis selection is not a true basis (rank < 52) or the diagonal action is mis-applied. Reconcile before trusting."
      - "rank unstable across the >=3 integer pairs and MAX != a clean 47 -> a sampled pair is non-generic (re-pick distinct, octonionic, non-proportional pairs) before doubting the engine."
---

<objective>
Compute the EXACT generic orbit dimension of F_4 acting diagonally on h_3(O) (+) h_3(O) as the rank over Q of the 52x54 infinitesimal-action matrix at a generic rational point, derive transcendence degree = 54 - orbit_dim, and verify the milestone anchor 54 - orbit_dim = 7. This is the chain-critical BASE-02 GATE value.

Purpose: This single number fixes the target transcendence degree (hence the target Jacobian rank for Phase 66 THE SPINE, and the Hilbert-series Krull dimension for Phase 68) for the entire (RING) milestone. The anchor 54 - orbit_dim = 7 (= six pointwise generators + the coupling c) is the milestone's early go/no-go: if it fails, the generating-set picture is wrong and the milestone STOPS before Phase 66. The value is forbidden to look up and the Spin(8)-triality back-of-envelope is a trap -- it must be COMPUTED.
Output: The selected 52-independent-generator basis, the pair orbit-dimension (exact QQ rank of the 52x54 matrix at >=3 generic integer pairs, MAX taken), trdeg = 54 - orbit_dim, and the asserted anchor 54 - orbit_dim = 7, in code/orbit_dimension_gate.py. A milestone go/no-go checkpoint closes the phase.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/ROADMAP.md
@.gpd/phases/65-f-4-construction-orbit-dimension-gate/65-RESEARCH.md
@.gpd/phases/65-f-4-construction-orbit-dimension-gate/65-02-SUMMARY.md
@code/ring_lemma_verification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select a 52-generator basis and compute the pair orbit dimension (exact 52x54 rank over QQ, >=3 points)</name>
  <files>code/orbit_dimension_gate.py</files>
  <action>
    Extend code/orbit_dimension_gate.py with the pair (diagonal) orbit-dimension computation. Reuse the CERTIFIED f_4 generators (Plan 01 dim 52; Plan 02 single-copy 24). The full 324-row pair stack STALLS (planner spike: >9 min, timed out), so the 52-basis route is MANDATORY for the decisive rank.

    1. Select 52 independent generators from the 324 brackets: iterate the brackets, flatten each 27x27 to a 729-vector, greedily keep a bracket iff it increases the running rank over QQ; stop at rank 52. (Planner spike: ~19s.) Assert exactly 52 selected and that their 729-flattening rank over QQ == 52 (a genuine basis of f_4). This is deliverable deliv-basis-selection and test-pair-basis-equiv.
    2. Generic rational pair: pick (X*, Y*) BOTH genuinely octonionic, DISTINCT, with X* NOT proportional to Y* and distinct diagonals. Small INTEGER coordinates (avoid denominator blowup). v_x = Matrix(E._flat27(X*)), v_y = Matrix(E._flat27(Y*)), each a 27-vector over Q.
    3. Pair tangent matrix (DIAGONAL action -- same M acts on both copies): for each selected generator M, the row is the 54-vector concat([ (M*v_x)[k] for k in 0..26 ], [ (M*v_y)[k] for k in 0..26 ]). Stack into a (52 x 54) SymPy Matrix. Substitute the pair point BEFORE forming the matrix (entries are integers).
    4. pair_orbit_dim = matrix.rank() over QQ.
    5. Multipoint: recompute at >=3 independent generic integer pairs; pair_orbit_dim = MAX over the points (rank lower-semicontinuous). Assert the value is stable (same MAX) and record it.
    6. OPTIONAL non-decisive pre-screen: a numpy float rank of the 52x54 (clearly labelled NON-DECISIVE, never asserted on) may be printed to anticipate the value; the VERDICT is the exact QQ rank only.

    PERF: select the basis first; substitute integer pairs before forming matrices; tangents matrix*vector; small integers. Do NOT rank the 324-row stack on the decisive path. Do NOT use numpy rank for the verdict. Do NOT use non-generic pairs (X*=Y*, proportional, real-only).
  </action>
  <verify>
    1. Exactly 52 independent generators selected; their 729-flattening rank over QQ == 52 (genuine f_4 basis) -- contract test-pair-basis-equiv.
    2. Pair orbit dim == exact QQ rank of the (52 x 54) matrix at a generic integer pair, a definite integer <= 47 -- contract test-pair-rank-exact.
    3. MAX over >=3 generic integer pairs is stable == pair_orbit_dim -- contract test-pair-multipoint.
    4. Forbidden proxies rejected: NO full-324-stack decisive rank (fp-full-stack-rank), NO float/numpy verdict rank (fp-float-rank), NO non-generic pair (fp-nongeneric-pair), value COMPUTED not looked up (fp-lookup-pair-orbit).
  </verify>
  <done>52 independent f_4 generators selected (basis, rank 52 over QQ); pair orbit dimension COMPUTED as the exact rank over QQ of the 52x54 diagonal-action matrix at >=3 generic integer octonionic pairs (MAX taken), a definite integer <= 47 (expected 47). Forbidden proxies (lookup, triality back-of-envelope, float verdict rank, full-stack rank, non-generic pair) all rejected.</done>
</task>

<task type="auto">
  <name>Task 2: Derive trdeg = 54 - orbit_dim and assert the milestone anchor 54 - orbit_dim = 7</name>
  <files>code/orbit_dimension_gate.py</files>
  <action>
    Add the transcendence-degree derivation and the milestone-anchor GATE to code/orbit_dimension_gate.py.

    1. trdeg = 54 - pair_orbit_dim (Derksen-Kemper: trdeg of R[27(+)27]^{F_4} = ambient dim - orbit dim).
    2. Assert THE ANCHOR: 54 - pair_orbit_dim == 7 (equivalently pair_orbit_dim == 47). Record that 7 = six pointwise generators {Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y} + the coupling c = Tr(X o Y).
    3. Assert pair_orbit_dim <= 47 (test-orbit-le-47) and pair-stabilizer dim = 52 - pair_orbit_dim >= 5 (test-stab-ge-5; = 5 when orbit_dim = 47).
    4. Wire the milestone GATE/backtracking (comment + hard assert): IF 54 - orbit_dim != 7, fail the harness with a message that the 'six pointwise + c' generating-set picture is wrong and the milestone STOPS -- do NOT proceed to Phases 66/67/68 (consider /gpd:research-phase only on this failure). Do NOT tune the point/basis to force 7.
    5. Record the decisive handoff for downstream phases: pair_orbit_dim, trdeg = 54 - orbit_dim = 7, single-copy orbit 24 (Plan 02), dim f_4 = 52 (Plan 01) -- as a module-level summary block (Phase 66 target Jacobian rank = 7; Phase 68 Krull dimension = 7).
    6. Finalize the assert-harness: ALL_PASS across Plans 01-03 checks; re-run the exact-only guard self-check; sys.exit(0 iff ALL_PASS).
  </action>
  <verify>
    1. trdeg = 54 - pair_orbit_dim == 7 -- contract test-anchor-7 (THE milestone go/no-go).
    2. pair_orbit_dim <= 47 (test-orbit-le-47) and 52 - pair_orbit_dim >= 5 (test-stab-ge-5).
    3. Milestone GATE/backtracking wired (STOP-the-milestone-if-not-7); handoff block records (orbit_dim, trdeg=7) for Phases 66/67/68.
    4. exact-only guard passes; full harness ALL_PASS; sys.exit(0).
  </verify>
  <done>trdeg = 54 - orbit_dim derived and the milestone anchor 54 - orbit_dim == 7 ASSERTED (orbit 47, stab 5 >= 5, orbit <= 47). 7 = six pointwise + c recorded; milestone GATE/backtracking wired; decisive handoff (orbit_dim, trdeg=7, single-copy 24, dim f_4 52) recorded for Phases 66/67/68. exact-only guard passes; harness exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Milestone GO/NO-GO checkpoint -- confirm trdeg = 54 - orbit_dim = 7</name>
  <files>code/orbit_dimension_gate.py</files>
  <action>
    HUMAN VERIFICATION (chain-critical milestone go/no-go -- gates Phases 66/67/68). No new computation; the researcher inspects the completed BASE-02 GATE.

    What was built (in code/orbit_dimension_gate.py): f_4 as the 52-dim span of inner derivations (dim computed, bracket-closed, infinitesimally F_4-invariant on {Tr,Tr^2,det_3}); single-copy orbit dimension 24 reproducing the Garibaldi-Guralnick Spin(8) anchor (builder certified); and the DECISIVE pair value -- generic orbit dimension of F_4 on h_3(O)(+)h_3(O) as the exact 52x54 rank over Q at >=3 generic integer pairs, with trdeg = 54 - orbit_dim asserted == 7.

    How to verify (the researcher confirms):
    1. `python3 code/orbit_dimension_gate.py` exits 0 with ALL_PASS (every lock + the anchor).
    2. THE DECISIVE value: pair_orbit_dim and trdeg = 54 - orbit_dim. Inspect the printed value -- is trdeg exactly 7 (six pointwise + c)?
    3. Single-copy sanity: orbit 24, stabilizer 52-24=28 (Spin(8)), trdeg 27-24=3 (Garibaldi-Guralnick anchor reproduced).
    4. Exactness: all decisive ranks via sympy.Matrix.rank() over QQ; no numpy/float rank, no octonion_algebra det_3 on the decisive path; points genuinely octonionic and generic.
    5. The value was COMPUTED, not looked up, and not derived by Spin(8)-triality.

    GATE: if trdeg != 7, the milestone STOPS here (the generating-set picture is wrong) -- do NOT proceed to Phase 66; consider /gpd:research-phase only on this failure. If trdeg = 7, Phase 66 (THE SPINE) inherits target Jacobian rank 7.
  </action>
  <what-built>
    The complete BASE-02 GATE in code/orbit_dimension_gate.py: f_4 built as the 52-dim span of inner derivations (dim computed, bracket-closed, infinitesimally F_4-invariant on {Tr,Tr^2,det_3}); single-copy orbit dimension 24 reproducing the Garibaldi-Guralnick Spin(8) anchor (builder certified); and the DECISIVE pair value -- generic orbit dimension of F_4 on h_3(O)(+)h_3(O) computed as the exact 52x54 rank over Q at >=3 generic integer pairs, with trdeg = 54 - orbit_dim asserted == 7.
  </what-built>
  <how-to-verify>
    This is the milestone GO/NO-GO. Confirm:
    1. The harness `python3 code/orbit_dimension_gate.py` exits 0 with ALL_PASS (every lock + the anchor).
    2. The DECISIVE value: pair_orbit_dim and trdeg = 54 - orbit_dim = 7 (six pointwise + c). Inspect the printed value -- is it exactly 7?
    3. Single-copy sanity: orbit 24, stabilizer 52-24=28 (Spin(8)), trdeg 27-24=3 (the Garibaldi-Guralnick anchor reproduced).
    4. Exactness: all decisive ranks via sympy.Matrix.rank() over QQ; no numpy/float rank, no octonion_algebra det_3 on the decisive path; points genuinely octonionic and generic.
    5. The value was COMPUTED, not looked up, and not derived by Spin(8)-triality.
    GATE: if trdeg != 7, the milestone STOPS here (the generating-set picture is wrong) -- do NOT proceed to Phase 66. If 7, Phase 66 (THE SPINE) inherits target Jacobian rank 7.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm the gate (trdeg = 7, milestone proceeds to Phase 66), or describe the discrepancy (e.g. trdeg != 7 -> milestone backtrack).</resume-signal>
</task>

</tasks>

<verification>
- Pair generic orbit dimension COMPUTED as the exact rank over QQ of the 52x54 diagonal-action matrix at >=3 generic integer pairs (MAX taken) -- NOT looked up, NOT from triality.
- 52 selected generators are a genuine f_4 basis (729-flattening rank 52) so the basis pair-rank equals the 324-row spanning-set rank (perf-justified).
- trdeg = 54 - orbit_dim == 7 (the milestone anchor: six pointwise + c); orbit_dim <= 47; pair-stabilizer dim >= 5.
- Single-copy orbit 24 / Spin(8) (Plan 02) cross-checks the action+rank machinery.
- Exact over QQ for every decisive rank; numpy float only as a labelled non-decisive pre-screen; no octonion_algebra on the decisive path.
- Milestone GATE/backtracking wired: trdeg != 7 STOPS the milestone.
</verification>

<success_criteria>
The generic orbit dimension of F_4 on h_3(O)(+)h_3(O) is COMPUTED as the exact rank over Q of the 52x54 infinitesimal-action matrix at >=3 generic integer points (substitute-first, MAX taken), via a verified 52-independent-generator basis; trdeg = 54 - orbit_dim is derived and the milestone anchor 54 - orbit_dim == 7 is ASSERTED (orbit 47, stab 5). The decisive handoff (orbit_dim, trdeg=7, single-copy 24, dim f_4 52) is recorded for Phases 66/67/68. Forbidden proxies rejected: NO lookup, NO Spin(8)-triality back-of-envelope, NO float/numpy verdict rank, NO full-324-stack decisive rank, NO non-generic pair. code/orbit_dimension_gate.py harness exits 0; the milestone go/no-go checkpoint confirms trdeg = 7.
</success_criteria>

<output>
After completion, create `.gpd/phases/65-f-4-construction-orbit-dimension-gate/65-03-SUMMARY.md`.
Decisive handoff for downstream phases: pair_orbit_dim (expected 47), trdeg = 54 - orbit_dim = 7 (= six pointwise + c) -> Phase 66 target Jacobian rank = 7 (THE SPINE: rank 7 = c independent; rank 6 = decisive NEGATIVE); Phase 68 Hilbert-series Krull dimension = 7. The CERTIFIED f_4 builder + the 52 generators in code/orbit_dimension_gate.py are reused by Phase 66 (Jacobian + orbit-derivative routes) and Phase 67.
</output>
