# Phase 65 Plan 03 — Research Log (pair orbit-dimension GATE)

**Conventions in effect (from state.json convention_lock + plan frontmatter):**
- Arithmetic: EXACT over Q. Ranks via exact rational linear algebra over QQ; numpy/float rank FORBIDDEN on the decisive path.
- Jordan product `X∘Y = (1/2)(XY+YX)`; Fano `e1 e2 = e4`; det_3 cross `2 Re((x2 x1) x3)` (Phase-64.1 corrected; frozen engine E.det_3).
- 27-per-copy layout `[α,β,γ, x1(8), x2(8), x3(8)]`; 54-symbol pair (X, Y).
- DIAGONAL action: the SAME f_4 generator M acts on both copies; pair-tangent row = concat(M·v_x, M·v_y), a 54-vector. Substitute the pair point BEFORE forming the matrix.
- Derksen-Kemper char-0: generic orbit dim = rank of infinitesimal action at a generic point; trdeg = ambient (54) − orbit_dim.

---

## Task 1 — Select 52-basis + compute pair orbit dimension

### First-result gate (observe BEFORE pre-registering the anchor)

**Engine functions verified present:** `X_from_symbols`, `_flat27`, `_coord_from_octmat`, `inner_derivations`, `_standard_basis_27`, `jordan_L_matrix`, `octonionic_points`. Roundtrip `_flat27(X_from_symbols(v)) == v` holds; layout confirmed.

**Performance finding (deviation Rule 1 — exact-method substitution):**
- `inner_derivations()` builds 324 brackets in 0.7s; `_select_independent_basis()` (exact rref over QQ) selects 52 in 0.2s.
- **`sympy.Matrix(...).rank()` is pathologically slow at 54-column width**: single-copy 52×27 took 55.1s; the pair 52×54 did NOT return after 5+ min CPU even with integer-cleared entries (generic dense symbolic Gaussian elimination, NOT fraction blowup — column width is the killer).
- **Resolution:** use `DomainMatrix.from_Matrix(A).convert_to(QQ).rank()` — EXACT rational linear algebra (SymPy polys module, exact MPQ/fmpq; NOT numpy, NOT float, NOT SVD). Validated equivalent to `Matrix.rank()`:
  - single-copy 52×27 at P1: `Matrix.rank()` = 24 (55.1s) **==** DomainMatrix-QQ = 24 (0.003s) — reproduces the CERTIFIED Plan-02 single-copy orbit 24.
  - pair 52×54 at P1×P2: three independent exact domains all agree — QQ-on-fractional = 44, QQ-on-integer-cleared = 44, ZZ-on-integer-cleared = 44 (pure-integer exact domain). 

### DECISIVE VALUE

**pair_orbit_dim = 44** (exact QQ rank of the 52×54 diagonal-action matrix), stable across **5 distinct generic integer pairs** (P1×P2, P2×P3, P1×P3, P4×P5, P3×P1 — all = 44; MAX = 44). Points genuinely octonionic, distinct diagonals, X not proportional to Y.

### Reconciliation (mandated by disconfirming_observations BEFORE trusting 44)

- **(A) basis vs full stack:** 52-basis pair rank = 44 **==** full 324-row pair rank = 44 (AGREE). Basis reduction is faithful; 44 is not a basis artifact.
- **(B) genuine f_4 basis:** 52-basis 729-flatten rank = 52; full 324-span = 52. The 52 generators are a true basis of f_4.
- **(C) block structure:** left block (M·v_x) rank = 24, right block (M·v_y) rank = 24 — **each independently reproduces the certified single-copy orbit 24** at X and at Y. Combined 54-col rank = 44 ⇒ the two 24-dim row-images overlap in exactly 24+24−44 = **4** dimensions. Diagonal action correctly applied.

### Self-critique checkpoint (first-result)
1. CONSTRUCTION: diagonal action = concat(M·v_x, M·v_y); left/right blocks each = 24 (certified). ✓
2. CONVENTION: exact over Q; 3 exact domains agree; no float/numpy. ✓
3. GENERICITY: 5 distinct generic pairs, stable 44 (clean MAX, not a deficient single point). ✓
4. PLAUSIBILITY: pair-stabilizer dim = 52−44 = 8 = dim(Stab(X) ∩ Stab(Y)) for two generic Spin(8)'s (dim 28 each) in F_4 (dim 52). This is exactly the principal-isotropy computation the plan forbids GUESSING (triality back-of-envelope); it was COMPUTED. 8-dim intersection is plausible.

**No disconfirming observation fired against 44.** It is the honest computed value.

---

## Task 2 — trdeg + milestone anchor (PRE-REGISTERED)

**Pre-registered assertion (written BEFORE the verdict can be rerolled):** `54 − pair_orbit_dim == 7` (i.e. pair_orbit_dim == 47), with orbit_dim ≤ 47 and pair-stabilizer ≥ 5.

**Computed:** trdeg = 54 − 44 = **10**. 

**ANCHOR RESULT: FAIL.** 10 ≠ 7. orbit_dim = 44 ≠ 47. pair-stabilizer = 8 (≥ 5 holds, but the equality-to-5 expectation fails).

**GATE INTERPRETATION (per binding_constraints + disconfirming_observations):** trdeg = 10 ≠ 7 ⇒ the "six pointwise + c" generating-set picture is INCOMPLETE. The full invariant field has trdeg 10, i.e. **3 additional** functionally independent joint invariants beyond {Tr X, Tr X², det X, Tr Y, Tr Y², det Y, c=Tr(X∘Y)}. (Mathematically sensible: the higher mixed trace monomials Tr(X²∘Y), Tr(X∘Y²), Tr(X²∘Y²) flagged as open in STATE.md Phase 68 are candidates for the missing 3.)

**Per the GATE rule: do NOT fudge the rank, do NOT force 7.** The honest value 44/trdeg-10 is reported; the pre-registered anchor assertion FAILS loudly; the harness exits nonzero. This is the milestone go/no-go STOP — the verdict (backtrack vs proceed) is the HUMAN's call at the authored checkpoint (Task 3).

**Single-copy sanity (still consistent):** orbit 24 / stabilizer 28 = Spin(8) / single-state trdeg 3 — unchanged (Plan 02 GATE still PASSES; the builder is still certified). The pair anchor failure is about the *generating-set count*, not the builder.
