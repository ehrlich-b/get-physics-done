---
phase: 65-f-4-construction-orbit-dimension-gate
plan: 02
type: execute
wave: 2
depends_on: ["65-01"]
files_modified: [code/orbit_dimension_gate.py]
interactive: false

conventions:
  arithmetic: "exact symbolic over Q (SymPy 1.14, Rational/QQ); NO float64 on any decisive path"
  rank_operator: "sympy.Matrix(...).rank() over QQ; numpy.linalg.matrix_rank / np.linalg.matrix_rank FORBIDDEN on the decisive path"
  jordan_product: "X∘Y = (1/2)(XY + YX) (frozen engine `jordan`)"
  octonion_table: "Fano e1*e2 = e4, e_i*e_i = -1 (frozen engine)"
  cubic_norm: "det_3 = generic norm, cross 2*Re((x2*x1)*x3) (Phase-64.1 corrected); frozen engine det_3 verbatim"
  coordinatization: "engine-native 27-per-copy layout; X_from_symbols / _flat27 / _standard_basis_27"
  f4_builder: "f_4 = span{[L_a,L_b]} (inner_derivations(), 324 nonzero, dim 52 -- established in Plan 01)"
  single_copy_action: "F_4 acting on ONE copy of h_3(O): the xi-th orbit-tangent row is M_xi . v (matrix*vector) for v = _flat27(X*); orbit_dim = rank over Q of the (generators x 27) tangent matrix at a generic point; substitute the INTEGER point BEFORE taking the rank"
  orbit_dim_criterion: "Derksen-Kemper char-0: generic orbit dim = rank of the infinitesimal action at a generic point; trdeg of the invariant field = (ambient dim) - orbit_dim. Single copy: 27 - orbit_dim"

dimensional_check:
  single_copy_orbit_dim: 'exact rank over QQ of the (324 x 27) [or (52 x 27)] infinitesimal-action matrix at a generic integer point == 24'
  single_copy_stabilizer: 'dim f_4 - orbit_dim = 52 - 24 = 28 (= dim Spin(8))'
  single_copy_trdeg: '27 - orbit_dim = 27 - 24 = 3 (single-state ring R[Tr,Tr^2,det], trdeg 3)'

approximations: []
# NONE. Exact finite linear algebra over Q.

environment_requirements:
  python: ">=3.11"
  libraries:
    - name: "sympy"
      used_for: "exact rank over QQ of the single-copy infinitesimal-action matrix"
      version: "1.14.0 (present)"
  external_tools: []

estimated_execution:
  total_minutes: 40
  breakdown:
    - task: 1
      minutes: 35
      note: "Single-copy orbit rank: build the (generators x 27) tangent matrix M_xi . v at a generic integer octonionic point, exact rank over QQ. Planner spike measured ~137s for the full 324-row spanning set at a small integer point -> recompute at >=2 integer points (take MAX). Exact-rank x1.5; the heavy step is the QQ rank."
    - task: 2
      minutes: 5
      note: "Assert the single-copy GATE: orbit 24, stab 52-24=28 (Spin(8)), trdeg 27-24=3; record the Garibaldi-Guralnick anchor match; wire the STOP-if-not-24 backtracking."

patterns_consulted:
  insights: []
  error_patterns: []
  adjustments_made:
    - "PERF (planner spike, MEASURED): single-copy rank via the FULL 324-row spanning set at a small INTEGER point = 24 in ~137s. This is acceptable in-phase; substitute the integer point BEFORE forming the matrix, tangents as matrix*vector. Re-check at >=2 integer points and take the MAX (rank lower-semicontinuous). If 324-row is too slow at a second point, use the 52 cached basis generators (rank of a basis == rank of the spanning set)."

contract:
  schema_version: 1

  scope:
    question: "Does the Plan-01 f_4 builder reproduce the Garibaldi-Guralnick single-copy anchor EXACTLY -- F_4 acting on ONE copy of h_3(O) has generic orbit dimension 24 (stabilizer Spin(8), dim 28; single-state trdeg 27-24=3)? This is the builder-correctness GATE that must pass BEFORE the pair orbit value is computed/trusted."

  claims:
    - id: claim-single-copy-orbit-24
      statement: "The generic orbit dimension of F_4 on a single copy of h_3(O) is EXACTLY 24, computed as the rank over Q of the infinitesimal-action matrix (rows M_xi . v over the f_4 generators, v = _flat27(X*) the flattened coordinates) at a generic integer octonionic point. Recomputed at >=2 integer points (take MAX; rank is lower-semicontinuous)."
      deliverables: [deliv-single-copy-rank]
      acceptance_tests: [test-single-copy-24, test-single-copy-multipoint]
      references: [ref-engine, ref-garibaldi-guralnick, ref-derksen-kemper]
    - id: claim-single-copy-stabilizer-trdeg
      statement: "The single-copy stabilizer dimension is 52 - 24 = 28 = dim Spin(8) (the Garibaldi-Guralnick generic stabilizer), and the single-state transcendence degree is 27 - 24 = 3 (= the single-state ring R[Tr, Tr^2, det] confirmed in Phase 64). The f_4 builder is therefore correct and the pair value may be computed."
      deliverables: [deliv-single-copy-gate]
      acceptance_tests: [test-stabilizer-28, test-trdeg-3]
      references: [ref-garibaldi-guralnick, ref-faraut-koranyi]

  deliverables:
    - id: deliv-single-copy-rank
      kind: code
      path: code/orbit_dimension_gate.py
      description: "Single-copy orbit-dimension computation: a function that, given an h_3(O) point X*, forms the (generators x 27) tangent matrix whose xi-th row is M_xi . _flat27(X*) (matrix*vector), and returns its exact rank over QQ. Evaluated at >=2 generic integer octonionic points; MAX taken."
      must_contain: ["rank", "_flat27", "24"]
    - id: deliv-single-copy-gate
      kind: code
      path: code/orbit_dimension_gate.py
      description: "The single-copy GATE assertions: orbit_dim == 24, stabilizer 52 - orbit_dim == 28 (Spin(8)), trdeg 27 - orbit_dim == 3; plus a recorded note that this reproduces the Garibaldi-Guralnick anchor and is the precondition for the pair value (STOP-if-not-24 backtracking)."
      must_contain: ["28", "Spin(8)", "trdeg"]

  references:
    - id: ref-engine
      kind: prior_artifact
      locator: "code/ring_lemma_verification.py (frozen engine) + code/orbit_dimension_gate.py Plan-01 (cached L-matrices, 324 f_4 generators, dim 52)"
      aliases: ["frozen engine", "Plan-01 f_4 generators"]
      role: definition
      why_it_matters: "Provides the f_4 generators (inner_derivations(), dim 52 verified in Plan 01), _flat27, _standard_basis_27, octonionic_points/h3o_from_coords for generic integer points, and the exact-only/rank-routing convention. The single-copy tangent rows reuse the SAME generators built in Plan 01."
      applies_to: [claim-single-copy-orbit-24]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use]
    - id: ref-garibaldi-guralnick
      kind: paper
      locator: "Garibaldi, S.; Guralnick, R. -- arXiv:2308.08214 (F_4 on 26 -> generic stabilizer Spin(8) dim 28, orbit 24, trdeg 3); corroborated by Lawther arXiv:1508.02918"
      aliases: ["Garibaldi-Guralnick", "GG 2308.08214", "Lawther 2015"]
      role: benchmark
      why_it_matters: "THE single-copy anchor. orbit 24 / stabilizer Spin(8) (28) / trdeg 3 is the exact target this GATE must reproduce. A match certifies the f_4 builder is Der(h_3(O)) = f_4 and not a wrong subalgebra; a mismatch means the builder is broken (do NOT compute the pair value)."
      applies_to: [claim-single-copy-orbit-24, claim-single-copy-stabilizer-trdeg]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [cite, use]
    - id: ref-derksen-kemper
      kind: paper
      locator: "Derksen, H.; Kemper, G. -- Computational Invariant Theory, Section 4"
      aliases: ["Derksen-Kemper", "DK 2002"]
      role: method
      why_it_matters: "Char-0 orbit-dimension criterion: generic orbit dim = rank of the infinitesimal action at a generic point; trdeg = ambient dim - orbit dim. Justifies computing orbit_dim as the exact rank and reading trdeg = 27 - orbit_dim."
      applies_to: [claim-single-copy-orbit-24, claim-single-copy-stabilizer-trdeg]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [cite, use]
    - id: ref-faraut-koranyi
      kind: paper
      locator: "Faraut, J.; Koranyi, A. -- Analysis on Symmetric Cones, Ch. II-IV (single-state ring R[Tr, Tr^2, det], trdeg 3)"
      aliases: ["Faraut-Koranyi", "FK 1994"]
      role: benchmark
      why_it_matters: "The single-state ring R[h_3(O)]^{F_4} = R[Tr, Tr^2, det] has trdeg 3 -- exactly the 27 - 24 = 3 read off here. Confirmed by citation in Phase 64 (Ch. II-IV; the Ch. V -> II-IV correction is recorded there)."
      applies_to: [claim-single-copy-stabilizer-trdeg]
      carry_forward_to: [verification]
      must_surface: false
      required_actions: [cite]

  acceptance_tests:
    - id: test-single-copy-24
      subject: claim-single-copy-orbit-24
      kind: limiting_case
      procedure: "Pick a generic integer octonionic point X* (rational/integer diagonal, integer octonion off-diagonals with several nonzero imaginary components, e.g. via h3o_from_coords / X_from_symbols on an integer 27-vector). Form the tangent matrix whose row for each f_4 generator M is M . v (matrix*vector), v = Matrix(_flat27(X*)); a (generators x 27) matrix over Q. Substitute the integer point BEFORE forming the matrix. Compute .rank() over QQ. (Planner spike: full 324-row spanning set at a small integer point gives 24 in ~137s; the 52 cached basis generators give the same rank faster.)"
      pass_condition: "rank over QQ == 24 EXACTLY (the single-copy generic orbit dimension)."
      evidence_required: [deliv-single-copy-rank, ref-engine, ref-garibaldi-guralnick]
      automation: automated
    - id: test-single-copy-multipoint
      subject: claim-single-copy-orbit-24
      kind: consistency
      procedure: "Recompute the single-copy rank at a SECOND independent generic integer octonionic point and take the MAX of the two ranks (rank is lower-semicontinuous -- it can only drop on special loci, so the generic value is the max over sampled points)."
      pass_condition: "The MAX over >=2 integer points == 24 (no point gives a larger rank; the generic orbit dim is 24)."
      evidence_required: [deliv-single-copy-rank, ref-derksen-kemper]
      automation: automated
    - id: test-stabilizer-28
      subject: claim-single-copy-stabilizer-trdeg
      kind: consistency
      procedure: "Compute stabilizer dim = dim f_4 - orbit_dim = 52 - 24 and compare to dim Spin(8) = 28."
      pass_condition: "52 - 24 == 28 == dim Spin(8) (the Garibaldi-Guralnick generic stabilizer)."
      evidence_required: [deliv-single-copy-gate, ref-garibaldi-guralnick]
      automation: automated
    - id: test-trdeg-3
      subject: claim-single-copy-stabilizer-trdeg
      kind: consistency
      procedure: "Compute single-state trdeg = 27 - orbit_dim = 27 - 24 and compare to the single-state ring trdeg (R[Tr, Tr^2, det], degrees 1/2/3)."
      pass_condition: "27 - 24 == 3 (matches the single-state ring transcendence degree confirmed in Phase 64)."
      evidence_required: [deliv-single-copy-gate, ref-faraut-koranyi]
      automation: automated

  forbidden_proxies:
    - id: fp-float-rank
      subject: claim-single-copy-orbit-24
      proxy: "numpy.linalg.matrix_rank / np.linalg.matrix_rank (or float SVD-tolerance rank) on the single-copy rank. A float tolerance fabricates the 24 verdict."
      reason: "Use sympy.Matrix(...).rank() over QQ; substitute the integer point first so entries are exact integers. Tempting because the exact QQ rank takes minutes (~137s measured) while numpy rank is instant."
    - id: fp-rank-before-substitution
      subject: claim-single-copy-orbit-24
      proxy: "Forming the symbolic tangent matrix in 27 variables and ranking it symbolically (or simplify()-ing det_3-laden symbolic tangents) -- expression swell."
      reason: "Substitute the generic INTEGER point FIRST, then rank the resulting integer matrix over QQ. Tempting because a symbolic rank looks more general, but in practice it stalls."
    - id: fp-skip-single-copy-gate
      subject: claim-single-copy-stabilizer-trdeg
      proxy: "Skipping the single-copy check and going straight to the pair (52x54) value, trusting dim 52 alone as builder correctness."
      reason: "The single-copy orbit 24 / Spin(8) is the builder GATE: it must reproduce the Garibaldi-Guralnick anchor BEFORE the pair value (Plan 03) is computed; dim 52 alone does not certify the builder is the RIGHT 52-dim algebra acting correctly. Tempting because dim 52 already passed in Plan 01 and the single-copy rank is the slow step."

  uncertainty_markers:
    weakest_anchors:
      - "Garibaldi-Guralnick PDF not directly quotable; mitigated by standard-fact status + Lawther corroboration + the planner-spike in-engine reproduction (orbit 24 measured at exit 0). This GATE makes the in-engine reproduction the decisive evidence."
    disconfirming_observations:
      - "single-copy orbit_dim != 24 (e.g. 23 or 25) at the generic point -> the f_4 builder is BROKEN (wrong subalgebra, mis-flattened L-columns, or wrong action convention). STOP and fix the builder; do NOT compute the pair value (GATE/Backtracking from the roadmap)."
      - "rank differs between the two integer points and the MAX is not 24 -> a sampled point is non-generic (re-pick a more generic octonionic integer point) OR the action is mis-implemented. Reconcile before trusting."
---

<objective>
Reproduce the Garibaldi-Guralnick single-copy anchor EXACTLY over Q: F_4 acting on ONE copy of h_3(O) has generic orbit dimension 24 (stabilizer Spin(8), dim 28; single-state trdeg 27-24=3). This is the builder-correctness GATE.

Purpose: Before the pair orbit-dimension value is computed (Plan 03), the f_4 builder from Plan 01 must be proven to be the RIGHT 52-dimensional algebra acting correctly -- not merely 52-dimensional. The single-copy orbit 24 / Spin(8) is the literature anchor that certifies this. The roadmap GATE is explicit: if the single-copy sanity does not reproduce, the builder is broken and the pair value must NOT be computed.
Output: The single-copy orbit-dimension computation (exact rank over QQ of the (generators x 27) tangent matrix at >=2 generic integer points, MAX taken) and the GATE assertions orbit 24 / stab 28 (Spin(8)) / trdeg 3, in code/orbit_dimension_gate.py.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/ROADMAP.md
@.gpd/phases/65-f-4-construction-orbit-dimension-gate/65-RESEARCH.md
@.gpd/phases/65-f-4-construction-orbit-dimension-gate/65-01-SUMMARY.md
@code/ring_lemma_verification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute the single-copy orbit dimension (exact rank over QQ, >=2 integer points)</name>
  <files>code/orbit_dimension_gate.py</files>
  <action>
    Extend code/orbit_dimension_gate.py with the single-copy orbit-dimension computation. Reuse the f_4 generators and cached L-matrices from Plan 01 (same module).

    1. Generic integer octonionic point: build X* with integer diagonal and integer octonion off-diagonals carrying SEVERAL nonzero imaginary components (genuinely octonionic, not on the real/commutative subalgebra). Use E.X_from_symbols on an integer 27-vector, or E.h3o_from_coords with E.oct([...]) octonions. A concrete known-good first point (planner spike, gives 24): the 27-vector [1,-2,3, 1,0,-1,2,0,1,0,-1, 0,2,0,-1,1,0,1,1, 0,-1,1,0,2,-1,1,0] (diagonal 1,-2,3 then x1,x2,x3 octonions).
    2. Tangent matrix: v = Matrix(E._flat27(X*)) (a 27-vector over Q). For each f_4 generator M (the 324 from inner_derivations(), OR the 52-independent basis if you select it for speed), the orbit-tangent row is (M * v) -- matrix*vector, a 27-vector over Q. Stack into a (generators x 27) SymPy Matrix. Substitute/evaluate the integer point BEFORE forming the matrix (the generators are already numeric; v is numeric; so the matrix is integer-valued).
    3. orbit_dim = matrix.rank() over QQ. (Planner spike measured: full 324-row spanning set at the point above -> rank 24 in ~137s. The 52 cached basis generators give the SAME rank faster -- a basis spans the same row space.)
    4. Second point: repeat with a DIFFERENT generic integer octonionic point and take orbit_dim = max(rank_point1, rank_point2) (rank lower-semicontinuous: the generic value is the max over sampled points).
    5. Record orbit_dim in a module-level result (the value Plan 03 will compare its trdeg bound against).

    PERF: substitute the integer point FIRST; tangents are matrix*vector; if the 324-row rank is too slow at the second point, use the 52 cached basis generators. Do NOT rank a symbolic 27-variable matrix. Do NOT use numpy rank.
  </action>
  <verify>
    1. Single-copy orbit rank over QQ == 24 at the first generic integer octonionic point -- contract test-single-copy-24.
    2. MAX over >=2 integer points == 24 (no point gives a larger rank) -- contract test-single-copy-multipoint.
    3. The point is genuinely octonionic (non-real off-diagonals), the rank is exact over QQ, and the integer point is substituted BEFORE the rank (forbidden proxies fp-float-rank, fp-rank-before-substitution rejected).
  </verify>
  <done>Single-copy orbit dimension computed as the exact rank over QQ of the (generators x 27) infinitesimal-action matrix at >=2 generic integer octonionic points (MAX taken) == 24. Forbidden proxies (float/numpy rank, symbolic-rank-before-substitution) rejected.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the single-copy GATE -- orbit 24 / Spin(8) (28) / trdeg 3 (builder correctness)</name>
  <files>code/orbit_dimension_gate.py</files>
  <action>
    Add the single-copy GATE assertions and the Garibaldi-Guralnick anchor record to code/orbit_dimension_gate.py.

    1. Assert orbit_dim == 24.
    2. Compute and assert stabilizer dim = dim_f4 - orbit_dim = 52 - 24 == 28, and record that 28 = dim Spin(8) (the Garibaldi-Guralnick generic stabilizer).
    3. Compute and assert single-state trdeg = 27 - orbit_dim = 27 - 24 == 3, and record that this matches the single-state ring R[Tr, Tr^2, det] (degrees 1/2/3) confirmed in Phase 64.
    4. Record (as a _report line and a short module note) that this reproduces the Garibaldi-Guralnick / Lawther single-copy anchor in-engine and therefore CERTIFIES the f_4 builder is Der(h_3(O)) = f_4.
    5. Wire the GATE backtracking explicitly in a comment + a hard assert: IF orbit_dim != 24, raise / fail the harness with a message that the builder is broken and the pair value (Plan 03) must NOT be computed until fixed (roadmap GATE/Backtracking).

    Keep everything in the assert-harness (_report / ALL_PASS / sys.exit). The harness exits 0 only if the single-copy GATE passes.
  </action>
  <verify>
    1. stabilizer dim 52 - 24 == 28 == dim Spin(8) -- contract test-stabilizer-28.
    2. trdeg 27 - 24 == 3 (matches single-state ring) -- contract test-trdeg-3.
    3. The GATE records the Garibaldi-Guralnick anchor match and the STOP-if-not-24 backtracking (forbidden proxy fp-skip-single-copy-gate rejected -- the GATE is not skipped).
    4. Harness exits 0 with the single-copy GATE PASS; orbit_dim is available to Plan 03.
  </verify>
  <done>Single-copy GATE asserted and PASSED: orbit 24, stabilizer 52-24=28 (= Spin(8)), trdeg 27-24=3 (= single-state ring). Garibaldi-Guralnick anchor reproduced in-engine; f_4 builder CERTIFIED. STOP-if-not-24 backtracking wired. Harness exits 0; the pair value may now be computed in Plan 03.</done>
</task>

</tasks>

<verification>
- Single-copy generic orbit dimension == 24 (exact rank over QQ at >=2 generic integer octonionic points, MAX taken).
- Stabilizer dim = 52 - 24 = 28 = dim Spin(8) (Garibaldi-Guralnick generic stabilizer reproduced in-engine).
- Single-state trdeg = 27 - 24 = 3 (matches R[Tr, Tr^2, det], Phase 64).
- The f_4 builder is therefore CERTIFIED correct -- the precondition for computing the pair value in Plan 03.
- Exact over QQ throughout; no float/numpy rank; integer point substituted before the rank.
</verification>

<success_criteria>
F_4 on a single copy of h_3(O) gives generic orbit dimension 24 EXACTLY over Q (stabilizer Spin(8) dim 28, single-state trdeg 3), reproducing the Garibaldi-Guralnick anchor in-engine and certifying the Plan-01 f_4 builder. The GATE backtracking (STOP-if-not-24) is wired. Forbidden proxies rejected: NO float/numpy rank, NO symbolic-rank-before-substitution, NO skipping the single-copy GATE. Harness exits 0.
</success_criteria>

<output>
After completion, create `.gpd/phases/65-f-4-construction-orbit-dimension-gate/65-02-SUMMARY.md`.
Hand off to Plan 03: the CERTIFIED f_4 builder (single-copy orbit 24 confirms it) -- Plan 03 may now compute the pair (52x54) orbit value. Carry forward orbit_dim_single = 24 and the recipe (integer point, substitute-first, matrix*vector tangents, exact QQ rank).
</output>
