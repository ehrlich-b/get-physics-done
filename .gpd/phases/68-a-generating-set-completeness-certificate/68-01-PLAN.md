---
phase: 68-a-generating-set-completeness-certificate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - code/molien_bigraded.py
interactive: true
researcher_setup: []

conventions:
  arithmetic: "exact SymPy over Q (Rational); ranks/nullspaces via exact_qq_rank = DomainMatrix over QQ; NEVER float64 / numpy.linalg.matrix_rank on the decisive path"
  group: "compact F_4 = Aut(h_3(O)); complexification F_4(C) reductive"
  jordan_product: "jordan(A,B) = (1/2)(AB+BA)"
  coupling: "c = Tr(X o Y) = Tr(jordan(X,Y)); c(X,X) = Tr(X^2) (NOT (Tr X)^2)"
  rep_27: "27 = 1 (+) 26 (trivial Tr-direction (+) trace-free irreducible 26)"
  weight_multiset_27: "{zero weight x3} union {24 short roots = all perms of (+/-1,+/-1,0,0), norm^2=2}"
  roots_F4: "48 roots: 24 short = perms(+/-1,+/-1,0,0) norm^2=2; 24 long = +/-e_i and (+/-1/2)^4 norm^2=1"
  weyl_order: "|W(F_4)| = 1152 (validated structurally: CT_z[prod_{48 roots}(1 - z^a)] = 1152)"
  gen_func_vars: "s = X-copy degree marker, t = Y-copy degree marker; H(s,t) bigraded"
  krull_target: "10 (= 54 - orbit_dim 44, Phase 65); the SUPERSEDED 7 (Spin(8)-triality trap) is FORBIDDEN"

dimensional_check:
  hilbert_coefficients: "every d_{a,b} is a non-negative INTEGER (any non-integer = bug)"
  d_1_1: "= 2 exactly (Phase 67 anchor)"
  H_s_0: "= 1/((1-s)(1-s^2)(1-s^3)) = [1,1,2,3,4,5,6] through s^6 (single-copy anchor)"
  weyl_measure_CT: "CT_z[prod_{48 roots}(1 - z^a)] = 1152 = |W(F_4)|"
  krull_pole_order: "order of pole of H(s,t) at s=t=1 = 10 (Phase 65)"
  symmetry: "d_{a,b} = d_{b,a} for all a,b (X<->Y diagonal-action symmetry)"

approximations:
  - name: "degree-<=6 series truncation"
    parameter: "total degree a+b"
    validity: "sufficient for the contract (genuinely-new generators live at degree <=4: single-copy <=3, mixed <=4); degrees 5,6 only probe for surprise generators"
    breaks_when: "a generator-degree bound > 6 is suspected (it is not, per Phase 65.1)"
    check: "exact within the truncation; NO error inside total degree 6"
  - name: "NONE on the decisive path"
    parameter: "exact-integer arithmetic over Q"
    validity: "every Hilbert coefficient and every rank is an exact integer"
    breaks_when: "n/a"
    check: "any non-integer coefficient or any float-rank on the decisive path = bug; error budget is EXACT (no tolerance)"

contract:
  schema_version: 1
  scope:
    question: "What is the bigraded Hilbert/Molien series H(s,t) of R[27(+)27]^{F_4} to total degree <= 6, computed exactly (no Sage), calibration-gated against the single-copy and (1,1) anchors, and cross-confirmed bidegree-by-bidegree at a+b<=4 by an independent exact-over-Q route -- i.e. the certified bigraded dimension table d_{a,b} that Plan 02 turns into a generating-set completeness certificate?"
  claims:
    - id: claim-molien-series
      statement: "The bigraded Hilbert series H(s,t) = sum d_{a,b} s^a t^b of R[27(+)27]^{F_4} is computed exactly for all total degrees a+b <= 6 via the pure-SymPy Molien-Weyl iterated symbolic residue (F_4 27-weight multiset {0^3} U {24 short roots}, Weyl-measure numerator prod_{48 roots}(1-z^a), normalization 1/|W| with |W|=1152), as a truncated bivariate polynomial of exact non-negative integer coefficients."
      deliverables: [deliv-molien-code, deliv-dim-table]
      acceptance_tests: [test-integer-coeffs, test-weyl-ct]
      references: [ref-derksen-kemper, ref-hanany, ref-single-copy, ref-frozen-orbit]
    - id: claim-calibration-gates
      statement: "H(s,t) passes both mandatory calibration gates BEFORE any two-copy coefficient is trusted: (G1) the t=0 specialization H(s,0) = 1/((1-s)(1-s^2)(1-s^3)) = [1,1,2,3,4,5,6] through s^6 (and s=0 symmetric); (G2) the (1,1) coefficient d_{1,1} = 2 (Phase 67). A gate failure means the weight set or normalization is wrong and execution STOPS to fix it."
      deliverables: [deliv-molien-code, deliv-dim-table]
      acceptance_tests: [test-gate-singlecopy, test-gate-11]
      references: [ref-single-copy, ref-frozen-degree2, ref-derksen-kemper]
    - id: claim-tworoute-agreement
      statement: "At every feasible low bidegree (a+b <= 4, at minimum (1,1),(2,0),(0,2),(2,1),(1,2) and (2,2) if it completes within the runtime budget), the Molien coefficient d_{a,b} equals the EXACT-over-Q f_4-infinitesimal-kernel dimension d_{a,b} = dim Sym^a(27)(x)Sym^b(27) - exact_qq_rank(stacked Leibniz-lift f_4-derivation operators) computed by generalizing the Phase-67 Route B engine. This two-route agreement is the reward-hacking tripwire; disagreement at any bidegree means at least one route is wrong and execution localizes it before proceeding."
      deliverables: [deliv-molien-code, deliv-f4kernel-table]
      acceptance_tests: [test-tworoute, test-f4kernel-exact]
      references: [ref-frozen-degree2, ref-frozen-orbit, ref-derksen-kemper]
    - id: claim-krull-symmetry
      statement: "H(s,t) is consistent with the prior-phase structural anchors: the order of the pole of H(s,t) at s=t=1 (the Krull dimension read from the series) = 10 (Phase 65, NOT the superseded 7), and H(s,t) = H(t,s) (the X<->Y diagonal-action symmetry, d_{a,b} = d_{b,a})."
      deliverables: [deliv-dim-table, deliv-molien-code]
      acceptance_tests: [test-krull, test-symmetry]
      references: [ref-frozen-orbit, ref-derksen-kemper]
  deliverables:
    - id: deliv-molien-code
      kind: code
      path: code/molien_bigraded.py
      description: "Pure-SymPy Molien-Weyl iterated-residue computation of H(s,t) to total degree <=6, the two calibration gates (single-copy specialization, (1,1)=2), the generalized Route-B exact f_4-kernel cross-check at feasible bidegrees, and the Krull-pole-order and symmetry checks. Imports the frozen engine; exact over Q; no Sage; no float on the decisive path."
      must_contain:
        - "Molien-Weyl iterated symbolic residue (NOT a numerical torus grid)"
        - "z_i = w_i^2 doubling substitution to clear half-integer exponents from the (+/-1/2)^4 long roots"
        - "G1 single-copy gate: H(s,0) == 1/((1-s)(1-s^2)(1-s^3))"
        - "G2 (1,1) gate: d_{1,1} == 2"
        - "generalized Route-B f_4-kernel dim = dim Sym^a(27)(x)Sym^b(27) - exact_qq_rank(stacked rho(M) operators)"
        - "Krull pole order at s=t=1 == 10"
        - "H(s,t) == H(t,s) symmetry check"
        - "ASSERT_CONVENTION line"
    - id: deliv-dim-table
      kind: data
      path: code/molien_bigraded.py
      description: "The certified bigraded dimension table {d_{a,b} : a+b <= 6} emitted by the harness (printed/returned as exact integers), which is the decisive handoff consumed by Plan 02 for the candidate-product comparison and minimality certificate."
      must_contain:
        - "d_{a,b} for all a+b <= 6 as exact integers"
        - "d_{1,1} = 2"
        - "single-copy column/row d_{a,0} = d_{0,a} = [1,1,2,3,4,5,6]"
    - id: deliv-f4kernel-table
      kind: data
      path: code/molien_bigraded.py
      description: "The exact-over-Q f_4-kernel dimensions at the cross-checked low bidegrees (a+b<=4) used to confirm the Molien coefficients independently."
      must_contain:
        - "exact f_4-kernel dim at (1,1) (must reproduce Phase 67 value 2)"
        - "exact f_4-kernel dim at (2,0),(0,2),(2,1),(1,2) over QQ"
        - "(2,2) exact dim OR an explicit recorded fallback to Molien-only at (2,2) with the runtime-budget reason"
  references:
    - id: ref-derksen-kemper
      kind: paper
      locator: "Derksen, H.; Kemper, G. Computational Invariant Theory, 2nd ed., Springer (2015), Molien-Weyl formula ~Thm 4.6.5; plethystic log; Reynolds redundancy"
      aliases: ["Derksen-Kemper 2015"]
      role: method
      why_it_matters: "Canonical Molien-Weyl (Weyl-integration / constant-term) formula for the Hilbert series of a reductive-group invariant ring; supplies the integrand form and the all-roots numerator with 1/|W| normalization."
      applies_to: [claim-molien-series, claim-calibration-gates, claim-tworoute-agreement, claim-krull-symmetry]
      carry_forward_to: [planning, execution, verification]
      must_surface: true
      required_actions: [use, cite]
    - id: ref-hanany
      kind: paper
      locator: "Hanany et al., 'Standard Model Plethystics' arXiv:1902.10550; 'Highest Weight Generating Functions for Hilbert Series' arXiv:1408.4690"
      aliases: ["Hanany plethystics"]
      role: method
      why_it_matters: "The Molien-Weyl + plethystic-exponential/log workflow for gauge-rep invariant rings is the exact computational analogue; practical iterated-residue evaluation recipe."
      applies_to: [claim-molien-series]
      carry_forward_to: [execution]
      must_surface: false
      required_actions: [read, use]
    - id: ref-single-copy
      kind: paper
      locator: "Springer 1962 (Indag. Math. 24) / Faraut-Koranyi Analysis on Symmetric Cones Ch. II-IV / Garibaldi-Guralnick: single-copy R[27]^{F_4} = R[Tr, Tr^2, det], Hilbert series 1/((1-s)(1-s^2)(1-s^3))"
      aliases: ["single-copy series", "Springer 1962"]
      role: benchmark
      why_it_matters: "THE calibration gate G1: the t=0 (and s=0) specialization of H(s,t) MUST reduce to this free single-copy series before any two-copy coefficient can be trusted."
      applies_to: [claim-calibration-gates, claim-molien-series]
      carry_forward_to: [execution, verification]
      must_surface: true
      required_actions: [use, compare, cite]
    - id: ref-frozen-degree2
      kind: prior_artifact
      locator: "code/degree2_uniqueness.py (Phase 67): Route B Leibniz-lift f_4-kernel machinery; bidegree-(1,1) trivial mult = 2 over QQ (729 - 727 = 2)"
      aliases: ["Phase 67 (1,1)=2", "Route B engine"]
      role: must_consider
      why_it_matters: "The (1,1) Hilbert coefficient MUST equal 2 (gate G2). The Route B Leibniz-lift kernel machinery (rho(M)=M(x)I+I(x)M, invariance <=> M^T C + C M = 0) is the reusable engine generalized here to all low bidegrees for the exact-over-Q cross-check."
      applies_to: [claim-calibration-gates, claim-tworoute-agreement]
      carry_forward_to: [planning, execution, verification]
      must_surface: true
      required_actions: [use, compare]
    - id: ref-frozen-orbit
      kind: prior_artifact
      locator: "code/orbit_dimension_gate.py (Phase 65): inner_derivations() = 52-generator f_4 basis, exact_qq_rank; orbit_dim 44 => Krull dim 54-44 = 10"
      aliases: ["Phase 65 Krull=10", "f_4 basis"]
      role: must_consider
      why_it_matters: "Supplies the 52-generator f_4 basis and exact_qq_rank for the Route-B kernel route, and the Krull-dimension target 10 the Hilbert-series pole order must match."
      applies_to: [claim-tworoute-agreement, claim-krull-symmetry]
      carry_forward_to: [execution, verification]
      must_surface: true
      required_actions: [use, compare]
    - id: ref-schwarz
      kind: paper
      locator: "Schwarz, G. W. 'When Polarizations Generate,' arXiv:math/0609078; Transform. Groups 12 (2007): 2-polarization fails generically in char 0"
      aliases: ["Schwarz 2006"]
      role: must_consider
      why_it_matters: "THE headline forbidden-proxy guard for sub-claim (a): polarizing single-copy generators need NOT generate the pair ring even in char 0, so the Hilbert match (this plan's series) is the actual certificate, not polarization. Surfaced here because this plan PRODUCES the certificate that supersedes the polarization hand-wave."
      applies_to: [claim-molien-series]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [cite, avoid]
    - id: ref-blind
      kind: paper
      locator: "Blind, B. J. Lie Theory 21 (2011), arXiv:0906.5525: C[27(+)27]^{E_6} is FREE on 4 det-polarizations (CONTRAST, NOT the F_4 target)"
      aliases: ["Blind 2011"]
      role: must_consider
      why_it_matters: "The E_6 pair ring is free; the F_4 pair ring is strictly LARGER (contains c, F_4- but not E_6-invariant) and is NOT free. H(s,t) must NOT have a clean free-algebra product denominator; expecting one is the E_6-import proxy."
      applies_to: [claim-molien-series, claim-krull-symmetry]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [cite, compare]
  acceptance_tests:
    - id: test-integer-coeffs
      subject: claim-molien-series
      kind: existence
      procedure: "Compute H(s,t) to total degree <=6 by the symbolic iterated residue; assert every coefficient d_{a,b} is a non-negative integer (SymPy Rational with denominator 1)."
      pass_condition: "All d_{a,b}, a+b<=6, are non-negative integers; any non-integer aborts with a STOP (residue extraction or normalization bug)."
      evidence_required: [deliv-molien-code, deliv-dim-table]
      automation: automated
    - id: test-weyl-ct
      subject: claim-molien-series
      kind: consistency
      procedure: "Independently recompute the Weyl-measure constant term CT_z[prod_{48 roots}(1 - z^a)] (numerator-only, all 48 roots) via the same symbolic machinery (with the doubling substitution)."
      pass_condition: "CT == 1152 = |W(F_4)| exactly, confirming the numerator + 1/|W| normalization structure."
      evidence_required: [deliv-molien-code]
      automation: automated
    - id: test-gate-singlecopy
      subject: claim-calibration-gates
      kind: limiting_case
      procedure: "Set t=0 in H(s,t) and compare coefficient-by-coefficient against 1/((1-s)(1-s^2)(1-s^3)) through s^6; repeat for s=0 by symmetry."
      pass_condition: "H(s,0) coefficients == [1,1,2,3,4,5,6] (and H(0,t) the same). If this gate fails, STOP and fix the weight set / normalization BEFORE reporting any two-copy number."
      evidence_required: [deliv-molien-code, deliv-dim-table, ref-single-copy]
      automation: automated
    - id: test-gate-11
      subject: claim-calibration-gates
      kind: consistency
      procedure: "Read d_{1,1} from H(s,t) and compare against the Phase-67 anchor value 2."
      pass_condition: "d_{1,1} == 2 exactly. If not, the integrand is wrong -- STOP."
      evidence_required: [deliv-molien-code, deliv-dim-table, ref-frozen-degree2]
      automation: automated
    - id: test-f4kernel-exact
      subject: claim-tworoute-agreement
      kind: cross_method
      procedure: "Generalize the Phase-67 Route B engine: build the stacked Leibniz-lift f_4-derivation operators rho(M)=M(x)I+I(x)M on the Sym^a(27)(x)Sym^b(27) trivial-isotypic test space and compute d_{a,b} = dim(space) - exact_qq_rank(stacked operators) over QQ at (1,1),(2,0),(0,2),(2,1),(1,2) [and (2,2) if feasible]. Reuse the CORRECTNESS GUARD that rho(M) annihilates the known invariant c (52/52) while the wrong lift M(x)M annihilates 0/52."
      pass_condition: "Exact-over-Q f_4-kernel dim computed at each feasible bidegree via exact_qq_rank (NEVER float); the Leibniz-lift correctness guard fires (rho(M) kills c for 52/52)."
      evidence_required: [deliv-molien-code, deliv-f4kernel-table, ref-frozen-degree2, ref-frozen-orbit]
      automation: automated
    - id: test-tworoute
      subject: claim-tworoute-agreement
      kind: cross_method
      procedure: "Compare the Molien coefficient d_{a,b} against the exact f_4-kernel dimension at every feasible bidegree a+b<=4."
      pass_condition: "Molien d_{a,b} == f_4-kernel d_{a,b} as IDENTICAL exact integers at every cross-checked bidegree. Disagreement at any bidegree => NO VERDICT + localize the discrepancy + STOP (reward-hacking tripwire)."
      evidence_required: [deliv-dim-table, deliv-f4kernel-table]
      automation: automated
    - id: test-krull
      subject: claim-krull-symmetry
      kind: consistency
      procedure: "Read the Krull dimension from H(s,t) as the order of the pole at s=t=1 (degree of the denominator after writing H as a rational function, or the leading growth order of d_{a,b}); compare against Phase 65."
      pass_condition: "Krull dimension == 10 (NOT 7). A pole order of 7 would contradict the Phase-65 orbit computation."
      evidence_required: [deliv-dim-table, ref-frozen-orbit]
      automation: hybrid
    - id: test-symmetry
      subject: claim-krull-symmetry
      kind: symmetry
      procedure: "Compare d_{a,b} against d_{b,a} for all a+b<=6."
      pass_condition: "d_{a,b} == d_{b,a} for all a,b (the X<->Y diagonal-action symmetry); any asymmetry is an integrand bug."
      evidence_required: [deliv-dim-table]
      automation: automated
  forbidden_proxies:
    - id: fp-numerical-grid
      subject: claim-molien-series
      proxy: "Evaluating the Molien-Weyl constant term by averaging the integrand over a dense product grid of roots of unity (numerical torus grid)."
      reason: "Infeasible in pure Python (validated timeout -- the prod over 48 roots has Laurent support ~degree 48 in the doubled lattice) AND a forbidden float path. The decisive route must be the symbolic iterated residue (exact, grid-free)."
    - id: fp-float-rank
      subject: claim-tworoute-agreement
      proxy: "Using numpy.linalg.matrix_rank / SVD tolerance for the f_4-kernel nullspace dimension."
      reason: "Float rank fabricates the dimension verdict (the GLOBAL forbidden proxy). The f_4-kernel dim must be exact_qq_rank (DomainMatrix over QQ) only."
    - id: fp-noninteger
      subject: claim-molien-series
      proxy: "Reporting a Hilbert coefficient that is not an exact non-negative integer (e.g. a residual rational from a normalization error) as if it were the dimension."
      reason: "Every d_{a,b} is a vector-space dimension = exact non-negative integer; a non-integer is a residue/normalization bug, not a result. No tolerance -- error budget is exact."
    - id: fp-krull7
      subject: claim-krull-symmetry
      proxy: "Targeting or accepting Krull dimension 7 (the Spin(8)-triality back-of-envelope) for the pair ring."
      reason: "Phase 65 computed orbit_dim 44 => Krull dim 54-44 = 10, triple-confirmed. The 7 is the SUPERSEDED trap value scattered through older docs; the pole order must be 10."
    - id: fp-skip-gate
      subject: claim-calibration-gates
      proxy: "Reporting two-copy coefficients d_{a,b} (a,b>=1) before the single-copy gate (G1) and the (1,1) gate (G2) have both passed."
      reason: "The Molien normalization has competing literature conventions (|Delta| vs |Delta|^2, all-roots vs positive-roots, 1/|W| placement); the gates are the only thing pinning the normalization. Un-gated two-copy numbers are untrustworthy."
    - id: fp-e6-free
      subject: claim-molien-series
      proxy: "Assuming H(s,t) has a clean free-algebra product denominator prod 1/(1-s^a t^b) because the E_6 pair ring (Blind 2011) is free."
      reason: "The F_4 pair ring is strictly larger (contains c) and is NOT free; H(s,t) will differ from the free-algebra form above the first relation. Importing the E_6 free answer is the contrast-anchor confusion."
  links:
    - id: link-series-table
      source: claim-molien-series
      target: deliv-dim-table
      relation: computes
      verified_by: [test-integer-coeffs, test-gate-singlecopy, test-gate-11]
    - id: link-gates-series
      source: claim-calibration-gates
      target: deliv-molien-code
      relation: depends_on
      verified_by: [test-gate-singlecopy, test-gate-11]
    - id: link-tworoute
      source: claim-tworoute-agreement
      target: deliv-f4kernel-table
      relation: benchmarks
      verified_by: [test-tworoute, test-f4kernel-exact]
    - id: link-krull
      source: claim-krull-symmetry
      target: deliv-dim-table
      relation: evaluated_by
      verified_by: [test-krull, test-symmetry]
    - id: link-handoff-to-plan02
      source: deliv-dim-table
      target: claim-molien-series
      relation: supports
      verified_by: [test-tworoute, test-gate-singlecopy]
  uncertainty_markers:
    weakest_anchors:
      - "The exact Molien-Weyl normalization (|Delta| vs |Delta|^2, 1/|W| placement, per-variable residue sign) is validated only structurally (CT=1152); it is PINNED operationally by the two calibration gates, not proven a priori -- if a gate fails, the normalization is the prime suspect."
      - "The f_4-kernel exact cross-check at (2,2) (142,884-dim space, ~7.4M-row stacked matrix) may exceed the executor ~600s stream-watchdog; if so, (2,2) falls back to Molien-only and the exact two-route anchor is restricted to a+b<=3 plus (2,1),(1,2)."
    disconfirming_observations:
      - "t=0 specialization != 1/((1-s)(1-s^2)(1-s^3)) -> wrong weight set or normalization (STOP)."
      - "d_{1,1} != 2 -> contradicts Phase 67; integrand is wrong (STOP)."
      - "Molien and f_4-kernel disagree at any cross-checked bidegree -> at least one route is wrong; localize before any verdict (the reward-hacking tripwire)."
      - "Krull pole order != 10 at s=t=1 -> contradicts the Phase-65 orbit computation."
      - "H(s,t) != H(t,s) -> the X<->Y symmetry is broken; integrand bug."
---

<objective>
Compute the bigraded Hilbert/Molien series H(s,t) = sum_{a,b} dim R[27(+)27]^{F_4}_{(a,b)} s^a t^b of the diagonal two-copy F_4-invariant ring, exactly and without Sage, for all total degrees a+b <= 6 -- then gate it against the single-copy and (1,1) anchors and cross-confirm it bidegree-by-bidegree (a+b<=4) by an independent exact-over-Q route.

Purpose: This is the SERIES half of sub-claim (a) of the (RING) Lemma. Phase 65 fixed the Krull dimension (10), Phase 65.1 proved FIELD-level (trdeg) completeness of the 10-candidate set, Phase 67 fixed the (1,1) coefficient (=2). This plan produces the certified bigraded dimension table d_{a,b} that supplies the completeness CERTIFICATE polarization cannot (Schwarz guard). Plan 02 consumes this table to assemble the generating set and run the minimality/completeness certificate. This is a CERTIFICATION computation: every decisive number is an exact integer over Q, cross-confirmed by two independent routes in the low-bidegree window where the genuinely-new generators live.

Output: code/molien_bigraded.py (the Molien-Weyl iterated-residue engine, both calibration gates, the generalized Route-B exact f_4-kernel cross-check, the Krull-pole-order and symmetry checks) and the certified bigraded dimension table {d_{a,b} : a+b <= 6}.

SCOPE METHOD DECISION (resolved IN-PLAN, not deferred to the executor): the bigraded Molien step is computed by a PRIMARY pure-SymPy Molien-Weyl iterated symbolic residue (self-contained, reaches all a+b<=6), CORROBORATED by the exact-over-Q frozen-f_4-kernel route at every feasible low bidegree (a+b<=4). Sage WeylCharacterRing('F4') is an OPTIONAL third witness ONLY (it breaks self-containment and is NOT used here); do NOT make it primary, do NOT stage a Sage fixture.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/68-a-generating-set-completeness-certificate/68-RESEARCH.md
@code/degree2_uniqueness.py
@code/orbit_dimension_gate.py
@code/ring_generating_set.py
@code/embedding_under_E_verification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the F_4 Molien ingredients + validate the Weyl-measure constant term, then assemble the iterated-residue H(s,t) engine (exact, symbolic, no grid)</name>
  <files>code/molien_bigraded.py</files>
  <action>
    Create code/molien_bigraded.py. Start with an ASSERT_CONVENTION line mirroring degree2_uniqueness.py (jordan=(1/2)(AB+BA); coupling c=Tr(X o Y); 27=1(+)26; weight multiset {0^3} U {24 short roots}; arithmetic exact-SymPy-over-Q; ranks=exact_qq_rank; NEVER float64/numpy rank on the decisive path; Krull target 10).

    1. F_4 ROOT/WEIGHT DATA (exact rational 4-vectors -- NO algebra engine needed):
       - Build the 48 roots: 24 SHORT = all sign/coordinate permutations of (1,1,0,0) -> (+/-1,+/-1,0,0), norm^2 = 2; 24 LONG = the 8 vectors +/-e_i and the 16 vectors (+/-1/2,+/-1/2,+/-1/2,+/-1/2), norm^2 = 1. Assert exactly 24 short + 24 long = 48 distinct roots.
       - Build the 27-WEIGHT MULTISET: the zero weight with multiplicity 3, PLUS the 24 short roots (the 26's nonzero weights). Assert the multiset has 27 entries total (3 + 24).
       - These are the crux ingredients. The 26's nonzero weights are unambiguously the perms-of-(+/-1,+/-1,0,0) set in the rep-theory normalization; the calibration gate (Task 2) is the loud check if the short/long label is swapped.

    2. THE DOUBLING SUBSTITUTION: the long roots contribute half-integer exponents (from (+/-1/2)^4). Before ANY coefficient extraction, substitute z_i = w_i^2 so every monomial z^alpha = prod_i w_i^(2*alpha_i) has integer exponents in the w-variables. Work entirely in w_1..w_4 with integer Laurent exponents thereafter. (Alternatively a cyclotomic field, but the doubling substitution is simpler -- use it.)

    3. VALIDATE THE WEYL MEASURE (mandatory structural gate BEFORE building H): compute the constant term CT_w[ prod_{alpha in 48 roots} (1 - w^(2*alpha)) ] by iterated symbolic Laurent-coefficient extraction over the 4 torus variables (expand as a Laurent polynomial in w_1..w_4 via SymPy Poly over the Laurent ring, or via successive `series`/coefficient picks; extract the w_1^0 w_2^0 w_3^0 w_4^0 term). Assert CT == 1152 = |W(F_4)|. This confirms the all-48-roots numerator and the 1/|W| normalization structure (validated in research at a grid; reproduce it here symbolically). If CT != 1152, the root set is wrong -- STOP.

    4. ASSEMBLE THE MOLIEN INTEGRAND and the ITERATED-RESIDUE EXTRACTOR:
       - One-copy characteristic factor D(u,w) = (1-u)^3 * prod_{mu in 24 short roots} (1 - u * w^(2*mu)), with u a degree marker (u=s for the X copy, u=t for the Y copy). The (1-u)^3 is the zero weight of multiplicity 3.
       - Integrand = N(w) / (D_X(s,w) * D_Y(t,w)) where N(w) = prod_{alpha in 48 roots}(1 - w^(2*alpha)).
       - H(s,t) = (1/|W|) * CT_w[ integrand ], truncated to total degree a+b <= 6 in (s,t).
       - IMPLEMENTATION (exact, grid-free): expand 1/D_X and 1/D_Y as bivariate power series in (s,t) to total degree 6 FIRST (each 1/(1 - u*w^(2*mu)) = sum_{k>=0} u^k w^(2*k*mu); (1-u)^{-3} = sum binom(k+2,2) u^k); multiply by N(w); collect as a Laurent polynomial in w_1..w_4 with coefficients that are polynomials in (s,t) of total degree <= 6; extract the w^0 (constant-in-w) coefficient by iterated single-variable Laurent-coefficient extraction (one torus variable at a time, using SymPy `Poly`/`as_poly`/coefficient dict on the Laurent expansion). Divide by 1152.
       - Hold everything as exact SymPy Rational/Poly. Use `python -u` and chunk progress prints (one line per torus-variable elimination, one per (s,t) total-degree shell) to avoid the long-no-output stream-watchdog stall.

    DO NOT use a dense numerical torus grid (infeasible AND a forbidden float path -- fp-numerical-grid). DO NOT import numpy/mpmath on this path. Symbolic iterated residue ONLY.
  </action>
  <verify>
    1. Exactly 48 roots (24 short norm^2=2 + 24 long norm^2=1), 27-weight multiset = 3 zeros + 24 short.
    2. Weyl-measure constant term CT_w[prod_{48 roots}(1 - w^(2*alpha))] == 1152 = |W(F_4)| EXACTLY (test-weyl-ct).
    3. The integrand assembly and iterated-residue extractor run to completion and return H(s,t) as a truncated bivariate polynomial; every coefficient is an exact non-negative integer (test-integer-coeffs) -- no non-integer (would signal a residue/normalization bug).
    4. No float / numpy / mpmath anywhere on the H(s,t) path (grep the file; exact SymPy only).
  </verify>
  <done>code/molien_bigraded.py builds the exact F_4 root/weight data, reproduces CT=1152 symbolically, assembles the Molien-Weyl integrand with the z_i=w_i^2 doubling substitution, and computes H(s,t) to total degree <=6 by iterated symbolic residue (no grid, no float) with all-integer coefficients. fp-numerical-grid and fp-noninteger rejected.</done>
</task>

<task type="auto">
  <name>Task 2: Run BOTH calibration gates (single-copy specialization G1, (1,1)=2 gate G2) and the symmetry check; report the bigraded dimension table</name>
  <files>code/molien_bigraded.py</files>
  <action>
    With H(s,t) in hand from Task 1, run the mandatory calibration gates BEFORE trusting any two-copy coefficient (fp-skip-gate). These pin the normalization, which has competing literature conventions.

    1. GATE G1 (single-copy specialization): set t=0 in H(s,t). Assert H(s,0) == 1/((1-s)(1-s^2)(1-s^3)) coefficient-by-coefficient through s^6, i.e. the coefficient list [1,1,2,3,4,5,6]. By symmetry also set s=0 and assert H(0,t) == [1,1,2,3,4,5,6] in t. If G1 FAILS: the weight set or normalization is wrong -- the most likely culprits are (a) the short/long root label swapped, (b) a |Delta| vs |Delta|^2 numerator, or (c) a per-variable residue-sign error. STOP, fix the normalization, re-run CT=1152 and G1. Do NOT report any two-copy number until G1 passes (test-gate-singlecopy).

    2. GATE G2 ((1,1) coefficient): read d_{1,1} from H(s,t). Assert d_{1,1} == 2 (Phase 67 anchor; = span{Tr(X)Tr(Y), c}). If d_{1,1} != 2, the integrand is wrong -- STOP (test-gate-11).

    3. SYMMETRY: assert d_{a,b} == d_{b,a} for all a+b <= 6 (the X<->Y diagonal-action symmetry). Any asymmetry is an integrand bug (test-symmetry).

    4. EMIT THE DIMENSION TABLE: print/return the full table {d_{a,b} : a+b <= 6} as exact integers, formatted as a clear grid (rows a=0..6, cols b=0..6-a). This is the decisive handoff to Plan 02. Record it in the harness output and (for the SUMMARY) note the (2,2) value explicitly -- it is the key diagnostic bidegree for the "(2,2) generator or product?" question Plan 02 resolves.

    Cross-reference the FREE-algebra series prod 1/(1-s^a t^b) over the 10 candidate bidegrees ONLY as a sanity comparison (it must reproduce d_{1,1}=2 and the t=0 single-copy series -- both validated in research) -- but DO NOT treat the free series as H_true (fp-e6-free); the F_4 ring is non-free, so they diverge above the first relation. The plethystic-log generator/relation separation is Plan 02's job.
  </action>
  <verify>
    1. G1: H(s,0) coefficients == [1,1,2,3,4,5,6] and H(0,t) == [1,1,2,3,4,5,6] (test-gate-singlecopy) -- the single-copy anchor 1/((1-s)(1-s^2)(1-s^3)).
    2. G2: d_{1,1} == 2 exactly (test-gate-11) -- matches Phase 67.
    3. Symmetry: d_{a,b} == d_{b,a} for all a+b<=6 (test-symmetry).
    4. The dimension table {d_{a,b} : a+b<=6} prints as exact non-negative integers; d_{2,0}=d_{0,2}=2, d_{2,2} reported explicitly.
  </verify>
  <done>Both calibration gates pass (G1 single-copy specialization == 1/((1-s)(1-s^2)(1-s^3)); G2 d_{1,1}==2), the H(s,t)=H(t,s) symmetry holds, and the certified bigraded dimension table {d_{a,b}: a+b<=6} is emitted as exact integers. fp-skip-gate and fp-e6-free rejected. If a gate failed, execution STOPPED and fixed the normalization before reporting any two-copy coefficient.</done>
</task>

<task type="auto">
  <name>Task 3: Exact-over-Q two-route cross-check (generalized Phase-67 Route B f_4-kernel) at every feasible low bidegree, plus the Krull-pole-order read-off</name>
  <files>code/molien_bigraded.py</files>
  <action>
    Independently confirm the Molien coefficients at low bidegree by the EXACT-over-Q f_4-infinitesimal-kernel route, generalizing the Phase-67 Route B engine (degree2_uniqueness.py). This two-route agreement is the reward-hacking tripwire.

    1. GENERALIZE ROUTE B from (1,1) to general (a,b): the dimension of the F_4-invariants at bidegree (a,b) = dim Sym^a(27) (x) Sym^b(27) - exact_qq_rank(stacked f_4-derivation operators), where the f_4 action is the Leibniz lift rho(M) = M(x)I + I(x)M of each of the 52 f_4 generators M (from inner_derivations() / _select_independent_basis in the frozen engine) onto the Sym^a(27)(x)Sym^b(27) monomial basis. Reuse the Phase-67 _flatten ordering convention and exact_qq_rank. The invariant space is the JOINT kernel of all 52 lifted operators; its dim = (space dim) - rank(vertical stack of the 52 operator matrices).
       - CORRECTNESS GUARD (mandatory, reused from Phase 67): rho(M) must annihilate the KNOWN invariant c at (1,1) for all 52/52 generators, while the WRONG lift M(x)M annihilates 0/52. Run this guard before trusting any kernel dim (test-f4kernel-exact).
       - Build the symmetric-power monomial basis for Sym^a(27) (dimension C(26+a,a)) and form the lift on the tensor-product basis. Use exact SymPy/QQ throughout.

    2. COMPUTE the exact f_4-kernel dim at: (1,1) [must reproduce 2, re-validating against Phase 67 verbatim], (2,0), (0,2), (2,1), (1,2). These spaces are 729, 378, 378, 10206, 10206 -- all feasible. ATTEMPT (2,2) (142,884-dim space; ~7.4M-row stacked matrix) with structured/sparse exact rank if it fits the runtime budget; if it risks the ~600s stream-watchdog, RECORD an explicit fallback to Molien-only at (2,2) (the exact two-route anchor then covers a+b<=2 plus (2,1),(1,2)) and note this in the output and SUMMARY. Use `python -u` and chunk prints (one line per bidegree, progress within the big ranks) to avoid the watchdog stall.

    3. TWO-ROUTE AGREEMENT: at every bidegree where the f_4-kernel route completed, assert Molien d_{a,b} == f_4-kernel d_{a,b} as IDENTICAL exact integers (test-tworoute). If they DISAGREE at any bidegree: emit NO VERDICT, localize the discrepancy (most likely the Molien normalization or the symmetric-power basis ordering), and STOP -- do not paper over it (the reward-hacking tripwire).

    4. KRULL-POLE-ORDER read-off: determine the Krull dimension from H(s,t) -- the order of the pole at s=t=1 (equivalently the degree of the denominator when H is written as a rational function, or the leading polynomial growth order of d_{a,b} along a ray). Assert == 10 (Phase 65), NOT the superseded 7 (fp-krull7). If the truncated series alone cannot cleanly fix the pole order, corroborate with the dimension-10 growth of the cross-checked coefficients and cite Phase 65 explicitly (test-krull).

    EXACT-OVER-Q ONLY: all kernel dims via exact_qq_rank (DomainMatrix over QQ); NEVER numpy.linalg.matrix_rank or any tolerance (fp-float-rank). No octonion_algebra.py (float64) on the decisive path.
  </action>
  <verify>
    1. Leibniz-lift correctness guard fires: rho(M)=M(x)I+I(x)M annihilates c at (1,1) for 52/52 generators (the wrong M(x)M would give 0/52).
    2. Exact f_4-kernel dim at (1,1) == 2 (reproduces Phase 67), and computed exactly over QQ at (2,0),(0,2),(2,1),(1,2) [and (2,2) if feasible].
    3. Two-route agreement: Molien d_{a,b} == f_4-kernel d_{a,b} as identical integers at every feasible bidegree a+b<=4 (test-tworoute) -- or a recorded, reasoned (2,2) Molien-only fallback.
    4. Krull dimension from H(s,t) == 10 (test-krull), NOT 7.
    5. exact_qq_rank only -- no float rank, no octonion_algebra on the decisive path (grep confirms).
  </verify>
  <done>The generalized Route-B exact f_4-kernel dims are computed over QQ at every feasible low bidegree and AGREE with the Molien coefficients as identical exact integers (two-route tripwire satisfied; (2,2) either confirmed or recorded as a reasoned Molien-only fallback). The Leibniz-lift correctness guard fires (52/52 for c). The Krull dimension read from H(s,t) is 10 (Phase 65), not the superseded 7. fp-float-rank and fp-krull7 rejected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Calibration + two-route adjudication checkpoint (the make-or-break gate before the Plan 02 handoff)</name>
  <files>code/molien_bigraded.py</files>
  <action>
    Present for human verification the decisive structural facts of the bigraded series before it is handed to Plan 02. This checkpoint exists because the Molien normalization is the single most error-prone ingredient (competing literature conventions) and a silently-wrong normalization would propagate undetected into the completeness verdict. Surface:
    - CT_w[prod_{48 roots}] = 1152 (Weyl-measure structural check).
    - G1: H(s,0) = [1,1,2,3,4,5,6] = 1/((1-s)(1-s^2)(1-s^3)) (and s=0 symmetric).
    - G2: d_{1,1} = 2 (Phase 67 anchor).
    - The two-route agreement table: Molien vs exact f_4-kernel at (1,1),(2,0),(0,2),(2,1),(1,2) [and (2,2) status: confirmed or Molien-only fallback].
    - Krull pole order = 10 (Phase 65) and H(s,t)=H(t,s).
    - The full bigraded dimension table {d_{a,b}: a+b<=6}, with d_{2,2} highlighted.
    Note the NEGATIVE-RESULT-IS-SUCCESS framing: if a gate or the two-route check forced a STOP-and-fix, that is reported honestly here.
  </action>
  <verify>
    Human confirms: both gates passed, the two routes agree at every feasible bidegree (no unlocalized disagreement), the Krull dimension is 10 (not 7), the symmetry holds, and the dimension table is exact-integer. If any gate failed or the routes disagreed without localization, the human directs the fix (likely a normalization or basis-ordering correction) before Plan 02 runs.
  </verify>
  <done>Human has verified the calibrated, two-route-confirmed bigraded dimension table {d_{a,b}: a+b<=6} and approved it as the decisive handoff to Plan 02 (generating-set assembly + completeness/minimality certificate). Resume signal: type "approved" or describe the normalization/basis issue to fix.</done>
</task>

</tasks>

<verification>
- Weyl-measure constant term CT == 1152 = |W(F_4)| (numerator + normalization structure).
- G1 single-copy specialization: H(s,0) = H(0,t) = 1/((1-s)(1-s^2)(1-s^3)) = [1,1,2,3,4,5,6].
- G2: d_{1,1} = 2 (Phase 67).
- Two-route agreement: Molien d_{a,b} == exact f_4-kernel d_{a,b} at every feasible a+b<=4 (the reward-hacking tripwire).
- Krull dimension from H(s,t) = 10 (Phase 65), NOT 7.
- H(s,t) = H(t,s) symmetry.
- Every Hilbert coefficient and every rank is an EXACT integer over Q -- no float, no numpy rank, no octonion_algebra on the decisive path; no tolerance (error budget exact).
</verification>

<success_criteria>
H(s,t) computed exactly to total degree <=6 by the pure-SymPy Molien-Weyl iterated symbolic residue (no Sage, no numerical grid), passing BOTH calibration gates (single-copy specialization == 1/((1-s)(1-s^2)(1-s^3)); d_{1,1}=2) and cross-confirmed bidegree-by-bidegree (a+b<=4) against the exact-over-Q generalized Route-B f_4-kernel dimensions as identical integers. The Krull dimension read from the series is 10 (Phase 65, not the superseded 7) and H(s,t)=H(t,s). The certified bigraded dimension table {d_{a,b}: a+b<=6} is emitted as the decisive handoff to Plan 02. Every decisive number is an exact integer over Q. All Plan-01 forbidden proxies are explicitly rejected: numerical torus grid, float/numpy rank, non-integer coefficients, Krull-dim 7, ungated two-copy numbers, and the E_6 free-algebra import. NEGATIVE-RESULT-IS-SUCCESS: any gate failure or two-route disagreement is reported honestly and fixed (not papered over) before the handoff.
</success_criteria>

<output>
After completion, create `.gpd/phases/68-a-generating-set-completeness-certificate/68-01-SUMMARY.md`. Record the full bigraded dimension table {d_{a,b}: a+b<=6} (exact integers), the gate results (CT=1152, G1, G2), the two-route agreement table (including the (2,2) status), the Krull dimension (10), and the symmetry confirmation -- this is the decisive input Plan 02 consumes.
</output>
