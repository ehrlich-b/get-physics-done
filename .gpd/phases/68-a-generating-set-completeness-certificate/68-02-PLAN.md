---
phase: 68-a-generating-set-completeness-certificate
plan: 02
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - code/generating_set_certificate.py
interactive: false
researcher_setup: []

conventions:
  arithmetic: "exact SymPy over Q (Rational); ranks/nullspaces via exact_qq_rank = DomainMatrix over QQ; NEVER float64 / numpy.linalg.matrix_rank on the decisive path"
  group: "compact F_4 = Aut(h_3(O))"
  jordan_product: "jordan(A,B) = (1/2)(AB+BA)"
  coupling: "c = Tr(X o Y) = Tr(jordan(X,Y)); c(X,X) = Tr(X^2) (NOT (Tr X)^2)"
  candidate_set: "10 candidates {Tr X (1,0), Tr X^2 (2,0), det X (3,0), Tr Y (0,1), Tr Y^2 (0,2), det Y (0,3), c (1,1), Tr(X^2 o Y) (2,1), Tr(X o Y^2) (1,2), Tr(X^2 o Y^2) (2,2)} -- verbatim from Phase 65.1 ring_generating_set.py"
  R_pt: "FROZEN pointwise subring = R-subalgebra gen by {Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y}; Tr(X)Tr(Y) in R_pt; c NOT in R_pt"
  gen_func_vars: "s = X-copy degree marker, t = Y-copy degree marker; H(s,t) bigraded; H_free = prod 1/(1 - s^a t^b) over candidate bidegrees"
  free_vs_true: "the F_4 ring is NOT free (E_6 is, Blind 2011 -- CONTRAST); H_free over-counts H_true above the first relation"

dimensional_check:
  candidate_product_dims: "d_candidate(a,b) = exact integer = rank of the value-matrix of all candidate monomials of bidegree (a,b) at generic rational points"
  generator_test: "for a genuine generator at (a,b): its value-vector is NOT in the span of products of strictly-lower-degree candidates (exact_qq_rank increases by 1)"
  plog_signs: "plethystic-log coefficient > 0 at (a,b) = generator(s); < 0 = relation(s); the F_4 ring being non-free means negatives WILL appear"
  completeness: "d_true(a,b) (Plan 01 Molien) vs d_candidate(a,b): d_true > d_candidate => MISSING generator at (a,b) (NEGATIVE-RESULT-IS-SUCCESS, must report)"

approximations:
  - name: "degree-<=6 series truncation (inherited from Plan 01)"
    parameter: "total degree a+b"
    validity: "the contract scope; genuinely-new generators live at degree <=4 (single-copy <=3, mixed <=4); degrees 5,6 probe for surprise generators only"
    breaks_when: "a generator-degree bound > 6 is suspected (it is not, per Phase 65.1)"
    check: "exact within the truncation; no error inside total degree 6"
  - name: "NONE on the decisive path"
    parameter: "exact-integer arithmetic over Q"
    validity: "every dimension, rank, and plog coefficient is an exact integer/rational"
    breaks_when: "n/a"
    check: "any float-rank on the decisive path = bug; error budget EXACT (no tolerance)"

contract:
  schema_version: 1
  scope:
    question: "Is the assembled candidate F_4 two-copy generating set COMPLETE -- does it generate R[27(+)27]^{F_4} to total degree <= 6 -- and is it MINIMAL (each generator genuinely new, not a product of lower-degree ones), certified by matching the Plan-01 bigraded Hilbert series bidegree-by-bidegree (NOT by assuming polarization generates), with the (2,2) 'generator or product?' question decided and an honest completeness verdict (certified complete OR a specific missing-generator bidegree)?"
  claims:
    - id: claim-candidate-set
      statement: "The candidate F_4 two-copy generating set is assembled with explicit bidegrees: six pointwise generators at (1,0),(2,0),(3,0),(0,1),(0,2),(0,3); c=Tr(X o Y) at (1,1); the mixed trace monomials Tr(X^2 o Y) at (2,1), Tr(X o Y^2) at (1,2), Tr(X^2 o Y^2) at (2,2) (reusing ring_generating_set.py verbatim; polarize_d produces the mixed-cubic candidates). Polarization is used ONLY to PRODUCE candidates -- it is NOT assumed to generate the ring (Schwarz guard)."
      deliverables: [deliv-cert-code, deliv-candidate-table]
      acceptance_tests: [test-candidate-bidegrees, test-no-polarization-assumption]
      references: [ref-ring-gen-set, ref-iltyakov, ref-schwarz, ref-polarize]
    - id: claim-minimality
      statement: "Each proposed generator is GENUINELY NEW (minimal generating, not merely spanning): for each candidate generator G at bidegree (a,b), evaluating G and ALL products of strictly-lower-degree candidates landing at (a,b) at enough generic rational octonionic points (PAIR_POINTS) and forming the exact value-matrix shows, via exact_qq_rank, that G's value-vector is NOT in the span of the lower-product value-vectors. Spanning is explicitly distinguished from minimal generating (Pitfall 8)."
      deliverables: [deliv-cert-code, deliv-minimality-table]
      acceptance_tests: [test-minimality, test-spanning-vs-minimal]
      references: [ref-ring-gen-set, ref-frozen-degree2]
    - id: claim-22-decision
      statement: "The key diagnostic bidegree (2,2) is decided: whether Tr(X^2 o Y^2) is a GENUINE generator or a PRODUCT of strictly-lower candidates {c^2, c*Tr(X)Tr(Y), Tr(X^2)Tr(Y^2), Tr(X^2 o Y)*Tr(Y), Tr(X o Y^2)*Tr(X), ...} is resolved by comparing d_true(2,2) (Plan 01 Molien) against the candidate-product dimension d_candidate(2,2) AND running the in-span minimality test for Tr(X^2 o Y^2). The outcome is reported honestly either way."
      deliverables: [deliv-cert-code, deliv-minimality-table]
      acceptance_tests: [test-22-decision]
      references: [ref-ring-gen-set, ref-frozen-degree2]
    - id: claim-completeness
      statement: "The completeness certificate is produced by matching the Plan-01 bigraded Hilbert series H(s,t) against the candidate set degree-by-degree to total degree <= 6: compute the plethystic log plog H(s,t) = sum_k (mu(k)/k) log H(s^k,t^k) to separate generators (positive coefficients) from relations (negative); at each bidegree compare d_true(a,b) (Molien) against d_candidate(a,b) (products of candidates). The verdict is stated honestly: either CERTIFIED COMPLETE to total degree <= 6 (with the explicit note that polarization was NOT assumed -- the Hilbert match is the certificate), OR the specific bidegree with d_true > d_candidate where a generator is MISSING (NEGATIVE-RESULT-IS-SUCCESS)."
      deliverables: [deliv-cert-code, deliv-verdict]
      acceptance_tests: [test-hilbert-match, test-plog, test-verdict-honest]
      references: [ref-plan01-table, ref-derksen-kemper, ref-hanany, ref-schwarz, ref-blind]
  deliverables:
    - id: deliv-cert-code
      kind: code
      path: code/generating_set_certificate.py
      description: "The generating-set assembly, the plethystic-log generator/relation separation, the candidate-product dimension per bidegree, the d_true-vs-d_candidate match, the in-span-of-lower-products minimality test per generator, the (2,2) generator-vs-product decision, and the honest completeness verdict. Consumes the Plan-01 bigraded dimension table; reuses ring_generating_set.py (candidates, PAIR_POINTS) and exact_qq_rank. Exact over Q; no float on the decisive path."
      must_contain:
        - "10-candidate set with bidegrees (reused verbatim from ring_generating_set.py)"
        - "d_candidate(a,b) = exact_qq_rank of the candidate-monomial value-matrix at PAIR_POINTS"
        - "plethystic log plog H(s,t) = sum_k (mu(k)/k) log H(s^k,t^k)"
        - "d_true(a,b) (Plan 01 Molien) vs d_candidate(a,b) comparison per bidegree a+b<=6"
        - "in-span-of-lower-products minimality test per generator (value-vector NOT in lower-product span, exact_qq_rank)"
        - "(2,2) generator-or-product decision for Tr(X^2 o Y^2)"
        - "explicit 'polarization NOT assumed' note in the verdict"
        - "ASSERT_CONVENTION line"
    - id: deliv-candidate-table
      kind: data
      path: code/generating_set_certificate.py
      description: "The assembled candidate generating set with explicit bidegrees and the builder expressions, plus the candidate-product dimension d_candidate(a,b) per bidegree a+b<=6."
      must_contain:
        - "all 10 candidates with bidegrees (1,0),(2,0),(3,0),(0,1),(0,2),(0,3),(1,1),(2,1),(1,2),(2,2)"
        - "d_candidate(a,b) for all a+b<=6 as exact integers"
    - id: deliv-minimality-table
      kind: data
      path: code/generating_set_certificate.py
      description: "Per-generator minimality results: for each candidate generator, whether its value-vector lies in the span of products of strictly-lower-degree candidates (exact_qq_rank), with the (2,2) Tr(X^2 o Y^2) result called out explicitly."
      must_contain:
        - "minimality (genuine-generator) PASS/FAIL per candidate generator"
        - "the (2,2) Tr(X^2 o Y^2) generator-or-product verdict"
    - id: deliv-verdict
      kind: report
      path: code/generating_set_certificate.py
      description: "The honest completeness verdict: CERTIFIED COMPLETE to total degree <=6 (with the polarization-NOT-assumed note and the Hilbert-match as the certificate), OR the specific missing-generator bidegree(s) where d_true > d_candidate. States the relations (negative plog coefficients) found."
      must_contain:
        - "verdict: certified-complete OR missing-generator-at-(a,b)"
        - "the polarization-was-NOT-assumed statement"
        - "the bidegrees where relations (syzygies) appear (negative plog)"
    - id: deliv-summary-handoff
      kind: note
      path: .gpd/phases/68-a-generating-set-completeness-certificate/68-02-SUMMARY.md
      description: "The phase-closing sub-claim (a) result: the minimal F_4 two-copy generating set + the degree-<=6 completeness certificate (or the missing-generator bidegree), cross-consistent with Phases 65 (Krull 10), 65.1 (trdeg-10 set), 67 ((1,1)=2)."
      must_contain:
        - "the certified generating set (or missing-generator report)"
        - "cross-phase consistency: trdeg 10 (65/65.1) >= rank 7 (66) >= quotient 1 (67); Krull 10; (1,1)=2"
  references:
    - id: ref-plan01-table
      kind: prior_artifact
      locator: ".gpd/phases/68-a-generating-set-completeness-certificate/68-01-SUMMARY.md and code/molien_bigraded.py -- the certified bigraded dimension table {d_{a,b}: a+b<=6} (d_true), gate-passed and two-route-confirmed"
      aliases: ["Plan 01 dimension table", "d_true"]
      role: must_consider
      why_it_matters: "This is the decisive INPUT: d_true(a,b) for the candidate match and the missing-generator test. The completeness certificate IS the comparison of this table against the candidate-product dimensions. Plan 02 cannot run without it (depends_on 68-01)."
      applies_to: [claim-completeness, claim-22-decision]
      carry_forward_to: [execution, verification]
      must_surface: true
      required_actions: [read, use, compare]
    - id: ref-ring-gen-set
      kind: prior_artifact
      locator: "code/ring_generating_set.py (Phase 65.1): the 10-candidate list, NAMES, BIDEGREES, builder expressions, PAIR_POINTS, value-matrix exact_qq_rank scaffolding; trdeg-10 (field-level) completeness"
      aliases: ["Phase 65.1 candidate set"]
      role: must_consider
      why_it_matters: "Supplies the candidate generating set verbatim (with bidegrees) and the PAIR_POINTS + value-matrix machinery for the candidate-product dimension and the minimality test. Phase 65.1 is FIELD-level (trdeg) complete; THIS plan upgrades to ring generation."
      applies_to: [claim-candidate-set, claim-minimality, claim-22-decision]
      carry_forward_to: [execution, verification]
      must_surface: true
      required_actions: [read, use]
    - id: ref-frozen-degree2
      kind: prior_artifact
      locator: "code/degree2_uniqueness.py (Phase 67): (1,1) trivial mult = 2 = span{Tr(X)Tr(Y), c}; the genuine-coupling quotient mod the reducible product Tr(X)Tr(Y) in R_pt is 1 = span{c}"
      aliases: ["Phase 67 (1,1)=2"]
      role: must_consider
      why_it_matters: "Anchors the (1,1) cell of the completeness analysis: d_true(1,1)=2 with the basis {Tr(X)Tr(Y), c}, where Tr(X)Tr(Y) is a product (in R_pt) and c is the genuine new generator -- the prototype of the minimality / generator-vs-product reasoning applied here at every bidegree."
      applies_to: [claim-minimality, claim-22-decision]
      carry_forward_to: [execution, verification]
      must_surface: true
      required_actions: [read, use, compare]
    - id: ref-schwarz
      kind: paper
      locator: "Schwarz, G. W. 'When Polarizations Generate,' arXiv:math/0609078; Transform. Groups 12 (2007): 2-polarization fails generically in char 0"
      aliases: ["Schwarz 2006"]
      role: must_consider
      why_it_matters: "THE headline forbidden-proxy guard for (a): polarizing single-copy generators need NOT generate the pair ring even in char 0. The bigraded Hilbert match is the actual certificate; polarization only PRODUCES candidates. Must be cited in EVERY completeness claim."
      applies_to: [claim-completeness, claim-candidate-set]
      carry_forward_to: [planning, execution, verification]
      must_surface: true
      required_actions: [cite, avoid]
    - id: ref-blind
      kind: paper
      locator: "Blind, B. J. Lie Theory 21 (2011), arXiv:0906.5525: C[27(+)27]^{E_6} FREE on 4 det-polarizations (CONTRAST -- F_4 ring is larger, contains c, NOT free)"
      aliases: ["Blind 2011"]
      role: must_consider
      why_it_matters: "The CONTRAST/lower-bound anchor: the E_6 pair ring is free; the F_4 ring is strictly larger and NOT free, so H_free != H_true and relations (negative plog) are expected. Importing the E_6 free answer as the F_4 ring is a forbidden proxy."
      applies_to: [claim-completeness]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [cite, compare]
    - id: ref-iltyakov
      kind: paper
      locator: "Iltyakov, A. V. J. Algebra 207 (1998): F_4 several-copy invariants = trace polynomials + Laplace invariants"
      aliases: ["Iltyakov 1998"]
      role: must_consider
      why_it_matters: "The authority that the candidate generators ARE trace polynomials (c, Tr(X^2 o Y), Tr(X o Y^2), Tr(X^2 o Y^2)) -- the candidate set is the right FORM, so a missing generator (if any) is the lowest-degree trace monomial at that bidegree (backtracking guidance)."
      applies_to: [claim-candidate-set]
      carry_forward_to: [planning]
      must_surface: true
      required_actions: [cite]
    - id: ref-polarize
      kind: prior_artifact
      locator: "polarize_d (frozen engine, embedding_under_E_verification.py): d(X,X,X) = 6*det_3; produces the polarized mixed-cubic candidates f(X,X,Y), f(X,Y,Y)"
      aliases: ["polarize_d"]
      role: method
      why_it_matters: "Produces the (2,1) and (1,2) mixed-cubic CANDIDATES; these are candidate-producers ONLY (Schwarz guard), supplied to the Hilbert match, never assumed to generate."
      applies_to: [claim-candidate-set]
      carry_forward_to: [execution]
      must_surface: false
      required_actions: [use]
    - id: ref-derksen-kemper
      kind: paper
      locator: "Derksen, H.; Kemper, G. Computational Invariant Theory, 2nd ed., Springer (2015): plethystic log, generating-set extraction from the Hilbert series, Reynolds redundancy"
      aliases: ["Derksen-Kemper 2015"]
      role: method
      why_it_matters: "Canonical reference for the plethystic-log generator/relation extraction and for distinguishing a minimal generating set from a spanning set via the Hilbert series."
      applies_to: [claim-completeness]
      carry_forward_to: [execution]
      must_surface: false
      required_actions: [use, cite]
    - id: ref-hanany
      kind: paper
      locator: "Hanany et al. 'Standard Model Plethystics' arXiv:1902.10550; 'Highest Weight Generating Functions for Hilbert Series' arXiv:1408.4690"
      aliases: ["Hanany plethystics"]
      role: method
      why_it_matters: "Practical plethystic-exponential / plethystic-log formulas (PE/plog) for reading generators and relations off a Hilbert series -- the workflow analogue."
      applies_to: [claim-completeness]
      carry_forward_to: [execution]
      must_surface: false
      required_actions: [use]
  acceptance_tests:
    - id: test-candidate-bidegrees
      subject: claim-candidate-set
      kind: existence
      procedure: "Load the 10 candidates from ring_generating_set.py; assert each candidate is present with its tabulated bidegree (1,0),(2,0),(3,0),(0,1),(0,2),(0,3),(1,1),(2,1),(1,2),(2,2) and is a certified F_4 invariant (the Phase-65.1 D_M f=0 check)."
      pass_condition: "All 10 candidates assembled with correct bidegrees and confirmed F_4-invariant; the mixed cubics produced via polarize_d as candidates."
      evidence_required: [deliv-cert-code, deliv-candidate-table, ref-ring-gen-set]
      automation: automated
    - id: test-no-polarization-assumption
      subject: claim-candidate-set
      kind: proxy
      procedure: "Audit the code and verdict: confirm polarization is used ONLY to construct candidate expressions and is NOWHERE used as evidence of completeness; the completeness claim rests solely on the Hilbert match."
      pass_condition: "No code path or verdict sentence treats 'we polarized the cubic norm' as proof of a complete generating set (Schwarz guard); the verdict explicitly states polarization was NOT assumed to generate."
      evidence_required: [deliv-cert-code, deliv-verdict, ref-schwarz]
      automation: hybrid
    - id: test-minimality
      subject: claim-minimality
      kind: consistency
      procedure: "For each candidate generator G at (a,b), build the value-matrix of G together with ALL products of strictly-lower-degree candidates landing at (a,b), evaluated at the PAIR_POINTS generic rational octonionic points; compute exact_qq_rank with and without G's row."
      pass_condition: "For each GENUINE generator, exact_qq_rank increases by 1 when G is added (G's value-vector NOT in the lower-product span) -- proven, not asserted; any generator whose value-vector IS in the lower-product span is reported as reducible (a product)."
      evidence_required: [deliv-cert-code, deliv-minimality-table]
      automation: automated
    - id: test-spanning-vs-minimal
      subject: claim-minimality
      kind: proxy
      procedure: "Confirm the certificate distinguishes spanning from minimal generating: a redundant (reducible) candidate is NOT counted as a generator just because it appears in a spanning set."
      pass_condition: "Minimality rests on the in-span-of-lower-products test (Pitfall 8), not on membership in a Reynolds spanning set; H_free is never substituted for H_true."
      evidence_required: [deliv-cert-code, deliv-minimality-table]
      automation: hybrid
    - id: test-22-decision
      subject: claim-22-decision
      kind: cross_method
      procedure: "At (2,2): compute d_candidate(2,2) = exact_qq_rank of the value-matrix of all candidate monomials of bidegree (2,2) {c^2, c*Tr(X)Tr(Y), Tr(X^2)Tr(Y^2), Tr(X^2)Tr(Y)Tr(Y)??, Tr(X^2 o Y)*Tr(Y), Tr(X o Y^2)*Tr(X), (Tr X)^2(Tr Y)^2, ..., and Tr(X^2 o Y^2)}; compare against d_true(2,2) from Plan 01; run the in-span test for Tr(X^2 o Y^2) specifically."
      pass_condition: "The (2,2) verdict is decided exactly: Tr(X^2 o Y^2) is a GENERATOR iff its value-vector is not in the span of the strictly-lower products AND d_true(2,2) requires it; otherwise it is a PRODUCT/relation. Reported honestly either way."
      evidence_required: [deliv-cert-code, deliv-minimality-table, ref-plan01-table]
      automation: automated
    - id: test-hilbert-match
      subject: claim-completeness
      kind: benchmark
      procedure: "At every bidegree a+b<=6 compare d_true(a,b) (Plan 01 Molien) against d_candidate(a,b) (the exact_qq_rank dimension reachable by products of the candidate generators at PAIR_POINTS)."
      pass_condition: "If d_true(a,b) <= d_candidate(a,b) for ALL a+b<=6 (candidate products reach the true dimension), the set is candidate-complete to degree 6. If d_true(a,b) > d_candidate(a,b) at any bidegree, a generator is MISSING there -- report the bidegree (NEGATIVE-RESULT-IS-SUCCESS)."
      evidence_required: [deliv-cert-code, deliv-verdict, ref-plan01-table]
      automation: automated
    - id: test-plog
      subject: claim-completeness
      kind: cross_method
      procedure: "Compute the plethystic log plog H(s,t) = sum_{k>=1} (mu(k)/k) log H(s^k,t^k) to total degree <=6 (SymPy mobius, exact rational series); read off generators (positive coefficients) and relations (first/lowest negative coefficients) per bidegree."
      pass_condition: "The plog positive coefficients at low bidegree match the candidate generator bidegrees (1,0),(2,0),(3,0),(0,1),(0,2),(0,3),(1,1),(2,1),(1,2)[,(2,2)?]; any positive plog coefficient at a bidegree NOT in the candidate set flags a genuinely missing generator (must report); negative coefficients identify the relations (the ring is non-free)."
      evidence_required: [deliv-cert-code, deliv-verdict]
      automation: automated
    - id: test-verdict-honest
      subject: claim-completeness
      kind: human_review
      procedure: "State the completeness verdict per the NEGATIVE-RESULT-IS-SUCCESS rule, cross-checked against Phases 65/65.1/67."
      pass_condition: "Verdict is one of: (i) CERTIFIED COMPLETE to total degree <=6 with the explicit polarization-NOT-assumed note (the Hilbert match is the certificate), or (ii) the specific missing-generator bidegree(s); cross-consistent with trdeg 10, Krull 10, (1,1)=2. No hand-wave; both outcomes are full passes."
      evidence_required: [deliv-verdict, deliv-summary-handoff, ref-schwarz]
      automation: human
  forbidden_proxies:
    - id: fp-polarization-generates
      subject: claim-completeness
      proxy: "Declaring the generating set complete because the candidates were obtained by polarizing the single-copy generators (cubic norm) -- 'we polarized, so we have all generators.'"
      reason: "THE headline forbidden proxy (Schwarz): 2-polarization fails generically in char 0. Polarization only PRODUCES candidates; the degree-by-degree Hilbert match is the only valid completeness certificate. Stopping after building f(X,X,Y), f(X,Y,Y) without the Molien match is false progress."
    - id: fp-spanning-as-minimal
      subject: claim-minimality
      proxy: "Counting a candidate as a 'generator' because it appears in a Reynolds spanning set, without the in-span-of-lower-products test (e.g. listing Tr(X^2 o Y^2) at (2,2) as a generator without checking it is not a product)."
      reason: "Spanning != minimal generating (Pitfall 8). A Reynolds spanning set is redundant by design; minimality requires the exact in-span test. The ring is non-free, so reducible candidates exist."
    - id: fp-float-rank
      subject: claim-minimality
      proxy: "Using numpy.linalg.matrix_rank / SVD tolerance for the candidate-product value-matrix rank or the in-span test."
      reason: "Float rank fabricates the generator/product verdict (the GLOBAL forbidden proxy). All value-matrix ranks must be exact_qq_rank (DomainMatrix over QQ); no octonion_algebra.py (float64) on the decisive path."
    - id: fp-e6-free-form
      subject: claim-completeness
      proxy: "Assuming H_true(s,t) has the free-algebra product form prod 1/(1-s^a t^b) (no relations) because the E_6 pair ring is free (Blind 2011)."
      reason: "The F_4 pair ring contains c and is strictly larger than the E_6 ring and NOT free; H_free over-counts H_true above the first relation. The plog WILL have negative coefficients (relations). Treating H_free as H_true mis-certifies completeness."
    - id: fp-suppress-missing
      subject: claim-completeness
      proxy: "Papering over a bidegree where d_true > d_candidate (or a positive plog coefficient outside the candidate set) by assuming polarization or a syzygy closes the gap."
      reason: "NEGATIVE-RESULT-IS-SUCCESS: a missing generator is a valid, must-report outcome. The backtracking rule is to ADD the lowest-degree trace monomial at that bidegree and re-match -- not to suppress the gap."
  links:
    - id: link-input-from-plan01
      source: claim-completeness
      target: deliv-verdict
      relation: depends_on
      verified_by: [test-hilbert-match, test-plog]
    - id: link-candidate-table
      source: claim-candidate-set
      target: deliv-candidate-table
      relation: computes
      verified_by: [test-candidate-bidegrees]
    - id: link-minimality
      source: claim-minimality
      target: deliv-minimality-table
      relation: supports
      verified_by: [test-minimality, test-spanning-vs-minimal]
    - id: link-22
      source: claim-22-decision
      target: deliv-minimality-table
      relation: evaluated_by
      verified_by: [test-22-decision]
    - id: link-verdict
      source: claim-completeness
      target: deliv-verdict
      relation: supports
      verified_by: [test-hilbert-match, test-plog, test-verdict-honest]
  uncertainty_markers:
    weakest_anchors:
      - "The candidate-product dimension d_candidate(a,b) depends on enumerating ALL products of lower candidates at each bidegree; an incomplete product enumeration would falsely flag a missing generator. Mitigation: enumerate products systematically by bidegree partition and validate the (1,1) and (2,0) cells against the known Phase-67/65.1 structure before trusting higher bidegrees."
      - "The (2,2) decision inherits the Plan-01 (2,2) status: if Plan 01 fell back to Molien-only at (2,2) (no exact f_4-kernel cross-check), the d_true(2,2) value rests on the gate-passed Molien series alone -- still decisive, but with one fewer independent confirmation than the a+b<=2 cells."
    disconfirming_observations:
      - "d_true(a,b) > d_candidate(a,b) at some bidegree -> a generator is MISSING there (report it; do NOT assume polarization closes it)."
      - "A positive plethystic-log coefficient at a bidegree NOT in the candidate set -> a genuinely missing generator (the NEGATIVE-result-is-success outcome)."
      - "A candidate generator whose value-vector IS in the span of lower products -> it is reducible (a product), not a generator (Pitfall 8); the minimal set is smaller than the candidate set."
      - "plog with NO negative coefficients through degree 6 would contradict the non-free expectation (Blind contrast); a clean free form is a red flag for the E_6-import proxy."
---

<objective>
Assemble the candidate F_4 two-copy generating set and CERTIFY its completeness (and minimality) by matching the Plan-01 bigraded Hilbert series H(s,t) degree-by-degree to total degree <= 6 -- NOT by assuming polarization generates (Schwarz guard). Decide the key diagnostic bidegree (2,2) (is Tr(X^2 o Y^2) a generator or a product?), and state an honest completeness verdict: certified complete to degree <= 6, OR the specific bidegree where a generator is missing.

Purpose: This is the CERTIFICATE half of sub-claim (a) of the (RING) Lemma and the LAST proof phase of v16.0 (it does NOT gate (b)/(c), which are already done in Phases 66/67). Phase 65.1 proved FIELD-level (trdeg) completeness; this plan upgrades to RING generation via the Hilbert match. The decisive guard is that polarization (Weyl's theorem needs many copies; the single-copy 2-polarization property fails generically in char 0) is used ONLY to produce candidates -- the bigraded Hilbert match supplies the completeness certificate polarization cannot.

Output: code/generating_set_certificate.py (candidate assembly; plethystic-log generator/relation separation; candidate-product dimension per bidegree; d_true-vs-d_candidate match; per-generator in-span-of-lower-products minimality test; the (2,2) generator-vs-product decision; the honest verdict) and the phase-closing (a) result.

This plan CONSUMES the Plan-01 deliverable: the certified bigraded dimension table {d_{a,b}: a+b<=6} (= d_true), which is gate-passed (single-copy + (1,1)=2) and two-route-confirmed. depends_on 68-01.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/68-a-generating-set-completeness-certificate/68-RESEARCH.md
@.gpd/phases/68-a-generating-set-completeness-certificate/68-01-SUMMARY.md
@code/molien_bigraded.py
@code/ring_generating_set.py
@code/degree2_uniqueness.py
@code/embedding_under_E_verification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the candidate generating set; compute the candidate-product dimension d_candidate(a,b) per bidegree and the plethystic log of the Plan-01 series</name>
  <files>code/generating_set_certificate.py</files>
  <action>
    Create code/generating_set_certificate.py with an ASSERT_CONVENTION line mirroring ring_generating_set.py / degree2_uniqueness.py (jordan=(1/2)(AB+BA); c=Tr(X o Y); 10-candidate set with bidegrees; R_pt frozen; exact-SymPy-over-Q; ranks=exact_qq_rank; NEVER float64/numpy rank on the decisive path; H_free != H_true since F_4 ring non-free).

    1. LOAD THE CANDIDATE SET verbatim from ring_generating_set.py: CANDIDATES, NAMES, BIDEGREES, the builder expressions, PAIR_POINTS, exact_qq_rank, octonionic points. The 10 candidates with bidegrees (1,0),(2,0),(3,0),(0,1),(0,2),(0,3),(1,1),(2,1),(1,2),(2,2). The three mixed cubics are produced by polarize_d (candidates only -- Schwarz guard). Re-confirm each is a certified F_4 invariant (reuse the Phase-65.1 D_M f=0 check). State explicitly in a comment that polarization is used ONLY to construct candidate EXPRESSIONS, never as evidence of completeness (fp-polarization-generates).

    2. LOAD d_true from Plan 01: read the certified bigraded dimension table {d_{a,b}: a+b<=6} from code/molien_bigraded.py (import the table or its producing function; the Plan-01 SUMMARY records the values). These are the gate-passed, two-route-confirmed Molien coefficients. Assert the (1,1) cell = 2 and the single-copy row/col = [1,1,2,3,4,5,6] as a sanity re-check of the handoff.

    3. CANDIDATE-PRODUCT DIMENSION d_candidate(a,b): for each bidegree (a,b) with a+b<=6, enumerate ALL monomials in the candidate generators whose total bidegree is (a,b) (systematically, by integer partitions of (a,b) over the candidate bidegrees), evaluate each at the PAIR_POINTS generic rational octonionic points, build the exact rational value-matrix, and set d_candidate(a,b) = exact_qq_rank of that matrix. This is the dimension of the degree-(a,b) piece REACHABLE by products of the candidate generators. Validate the (1,1) cell (should be 2 = {Tr(X)Tr(Y), c}) and the single-copy cells against the known free single-copy structure before trusting higher bidegrees (weakest-anchor mitigation). Use enough PAIR_POINTS that the value-matrix rank saturates (add fresh generic points until the rank stops increasing).

    4. PLETHYSTIC LOG: compute plog H(s,t) = sum_{k>=1} (mu(k)/k) log H(s^k,t^k) to total degree <=6 (SymPy mobius for mu(k); exact rational power-series log via series composition). Read off, per bidegree: positive coefficients = generators, negative = relations. The F_4 ring is NON-free (Blind contrast), so negative coefficients WILL appear -- do NOT treat their absence as expected (fp-e6-free-form).

    Use `python -u` and chunked prints (one line per bidegree for d_candidate; the value-matrix ranks can be the heaviest step) to avoid the stream-watchdog stall.
    EXACT-OVER-Q ONLY: all value-matrix ranks via exact_qq_rank; no numpy rank, no octonion_algebra float64 on the decisive path (fp-float-rank).
  </action>
  <verify>
    1. All 10 candidates loaded with correct bidegrees and re-confirmed F_4-invariant (D_M f=0); the mixed cubics produced via polarize_d as candidates only (test-candidate-bidegrees).
    2. d_true loaded from Plan 01; (1,1)=2 and single-copy row/col [1,1,2,3,4,5,6] re-checked at the handoff.
    3. d_candidate(a,b) computed via exact_qq_rank for all a+b<=6; (1,1) cell = 2 and single-copy cells validated against the known structure.
    4. plog H(s,t) computed to degree <=6 with exact rational coefficients (mu(k) via SymPy mobius).
    5. No float / numpy rank / octonion_algebra on the decisive path (grep confirms); polarization used only to build candidate expressions (code comment + audit).
  </verify>
  <done>The 10-candidate generating set is assembled (bidegrees confirmed, F_4-invariance re-checked, polarization used only to PRODUCE candidates per the Schwarz guard), d_true is loaded from the Plan-01 table (handoff sanity-checked), the candidate-product dimensions d_candidate(a,b) are computed exactly over Q for all a+b<=6, and plog H(s,t) is computed to degree <=6. fp-polarization-generates, fp-float-rank, and fp-e6-free-form rejected on this task.</done>
</task>

<task type="auto">
  <name>Task 2: Per-generator minimality (in-span-of-lower-products), the (2,2) generator-vs-product decision, the d_true-vs-d_candidate completeness match, and the honest verdict</name>
  <files>code/generating_set_certificate.py</files>
  <action>
    Produce the minimality certificate, decide (2,2), match the full series, and state the verdict.

    1. MINIMALITY (in-span-of-lower-products) per generator: for each candidate generator G at bidegree (a,b), build the value-matrix of {all products of strictly-lower-degree candidates landing at (a,b)} and compare exact_qq_rank with and without G's value-vector (evaluated at PAIR_POINTS, enough points to saturate). G is a GENUINE (minimal) generator iff adding G increases the rank by 1 (its value-vector is NOT in the lower-product span). The six pointwise generators and c are generators by construction/Phase-67; the decisive cases are the mixed monomials at (2,1),(1,2),(2,2). Distinguish spanning from minimal generating explicitly (Pitfall 8; test-minimality, test-spanning-vs-minimal). Report PASS (genuine generator) / reducible (product) per candidate.

    2. THE (2,2) DECISION: at (2,2), enumerate the strictly-lower candidate products landing at (2,2): {c^2, c*Tr(X)Tr(Y), Tr(X^2)*Tr(Y^2), Tr(X^2 o Y)*Tr(Y), Tr(X o Y^2)*Tr(X), (Tr X)^2*(Tr Y)^2, Tr(X^2)*(Tr Y)^2, (Tr X)^2*Tr(Y^2), Tr(X)Tr(Y)*c, ... (all bidegree-(2,2) products of lower candidates)}. Compute d_candidate(2,2) = exact_qq_rank of their value-matrix; compare to d_true(2,2) from Plan 01; run the in-span test for Tr(X^2 o Y^2) specifically. VERDICT: Tr(X^2 o Y^2) is a GENERATOR iff its value-vector is not in the lower-product span AND d_true(2,2) > (rank without it); else it is a PRODUCT/relation. Report honestly either way (test-22-decision). (Recall: field-completeness in Phase 65.1 put Tr(X^2 o Y^2) in the trdeg-10 set, but ring-generation is a STRICTER question -- this decides it.)

    3. COMPLETENESS MATCH: at every bidegree a+b<=6, compare d_true(a,b) (Plan 01 Molien) against d_candidate(a,b). If d_true(a,b) <= d_candidate(a,b) for ALL bidegrees (candidate products reach the true dimension), the candidate set is COMPLETE to total degree 6. If d_true(a,b) > d_candidate(a,b) at any bidegree, a generator is MISSING there -- per the backtracking rule, identify the lowest-degree trace monomial at that bidegree (Iltyakov: generators are trace polynomials), note it as the missing generator, and report the bidegree. Cross-check the plog: positive coefficients at low bidegree should match the candidate generator bidegrees; a positive plog coefficient at a non-candidate bidegree independently flags a missing generator; negative coefficients catalogue the relations (the non-free structure). (test-hilbert-match, test-plog.)

    4. HONEST VERDICT (test-verdict-honest, NEGATIVE-RESULT-IS-SUCCESS): state one of -- (i) CERTIFIED COMPLETE to total degree <=6, with the EXPLICIT note that polarization was NOT assumed to generate (the bigraded Hilbert match is the certificate; Schwarz), the minimal generating set listed with bidegrees, and the relations (negative plog) catalogued; or (ii) the specific missing-generator bidegree(s). Cross-check consistency with the prior phases: trdeg 10 (65/65.1) >= rank 7 (66) >= quotient 1 (67); Krull dim 10; (1,1)=2. BOTH outcomes are full passes -- report whichever is true.

    EXACT-OVER-Q ONLY throughout (exact_qq_rank; no float/numpy/octonion_algebra on the decisive path -- fp-float-rank). Do NOT suppress a missing generator by appealing to polarization or an assumed syzygy (fp-suppress-missing).
  </action>
  <verify>
    1. Minimality: for each genuine generator, exact_qq_rank increases by 1 when its value-vector is added to the lower-product span (proven, not asserted); any reducible candidate reported as a product (test-minimality); spanning explicitly distinguished from minimal generating (test-spanning-vs-minimal).
    2. (2,2) decided exactly: Tr(X^2 o Y^2) generator-or-product verdict with d_candidate(2,2) vs d_true(2,2) and the in-span test (test-22-decision).
    3. Completeness match: d_true(a,b) vs d_candidate(a,b) at every a+b<=6; any d_true>d_candidate reported as a missing generator at that bidegree (test-hilbert-match). plog positive/negative coefficients consistent with the candidate generators and the (non-free) relations (test-plog).
    4. Verdict stated honestly (certified-complete with the polarization-NOT-assumed note, OR missing-generator bidegree); cross-consistent with trdeg 10, Krull 10, (1,1)=2 (test-verdict-honest).
    5. exact_qq_rank only -- no float rank / octonion_algebra on the decisive path (grep confirms); no missing generator suppressed.
  </verify>
  <done>The per-generator in-span-of-lower-products minimality certificate is produced (genuine generators proven, reducible candidates reported as products; spanning distinguished from minimal generating), the (2,2) Tr(X^2 o Y^2) generator-vs-product question is decided exactly, the d_true-vs-d_candidate completeness match is run at every bidegree a+b<=6 with the plog cross-check, and the honest completeness verdict is stated (certified complete to <=6 with the explicit polarization-NOT-assumed note, OR the specific missing-generator bidegree) -- cross-consistent with Phases 65/65.1/67. fp-spanning-as-minimal, fp-float-rank, and fp-suppress-missing rejected.</done>
</task>

</tasks>

<verification>
- Candidate set assembled with correct bidegrees; F_4-invariance re-confirmed; polarization used ONLY to produce candidates (Schwarz guard; verdict explicitly says polarization NOT assumed to generate).
- Minimality by the in-span-of-lower-products test (exact_qq_rank), spanning distinguished from minimal generating (Pitfall 8).
- The (2,2) Tr(X^2 o Y^2) generator-vs-product question decided exactly via d_candidate(2,2) vs d_true(2,2) + the in-span test.
- Completeness by the d_true (Plan 01 Molien) vs d_candidate match at every a+b<=6, corroborated by the plethystic log (generators = positive coeffs, relations = negative; the F_4 ring is non-free, so relations appear).
- Honest verdict per NEGATIVE-RESULT-IS-SUCCESS: certified complete OR specific missing-generator bidegree.
- Cross-phase consistency: trdeg 10 (65/65.1) >= rank 7 (66) >= quotient 1 (67); Krull 10; (1,1)=2.
- Every dimension, rank, and plog coefficient is EXACT over Q -- no float, no numpy rank, no octonion_algebra on the decisive path; no tolerance (error budget exact).
</verification>

<success_criteria>
The candidate F_4 two-copy generating set is assembled with explicit bidegrees (six pointwise + c + the three mixed trace monomials, the mixed cubics produced via polarize_d as candidates only). Each generator's minimality is PROVEN by the exact-over-Q in-span-of-lower-products test (spanning distinguished from minimal generating). The (2,2) Tr(X^2 o Y^2) generator-vs-product question is decided exactly. Completeness is certified by matching the Plan-01 bigraded Hilbert series d_true(a,b) against the candidate-product dimensions d_candidate(a,b) at every total degree <=6, corroborated by the plethystic log -- with the EXPLICIT statement that polarization was NOT assumed to generate (the Hilbert match is the certificate; Schwarz). The verdict is honest per NEGATIVE-RESULT-IS-SUCCESS: either CERTIFIED COMPLETE to total degree <=6 (minimal generating set listed, relations catalogued) OR the specific missing-generator bidegree (with the lowest-degree trace monomial to add). The result is cross-consistent with Phases 65/65.1 (trdeg 10), 66 (rank 7), 67 ((1,1)=2). All Plan-02 forbidden proxies are explicitly rejected: polarization-generates, spanning-as-minimal, float/numpy rank, E_6-free-form, and suppressing a missing generator. Every decisive number is exact over Q.
</success_criteria>

<output>
After completion, create `.gpd/phases/68-a-generating-set-completeness-certificate/68-02-SUMMARY.md`. Record the minimal generating set (or the missing-generator bidegree), the (2,2) decision, the full d_true-vs-d_candidate match table, the relations found (negative plog), the honest completeness verdict with the polarization-NOT-assumed note, and the cross-phase consistency (trdeg 10 / Krull 10 / (1,1)=2). This closes sub-claim (a) of the (RING) Lemma.
</output>
