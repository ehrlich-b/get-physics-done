---
phase: 08-locality-formalization
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - derivations/08-lr-self-modeling.md
  - code/self_modeling_lr_velocity.py
interactive: false

conventions:
  units: "natural (hbar = c = k_B = 1)"
  metric: "(-,+,+,+) Wald convention"
  sequential_product: "a & b (non-commutative, vdW arXiv:1803.11139 Def. 2)"
  composite_product: "(a tensor b) & (c tensor d) = (a & c) tensor (b & d)"
  local_algebra: "A_x = M_n(C), self-adjoint part A_x^sa = M_n(C)^sa"
  hamiltonian_sign: "H = sum h_{xy}, ground state minimizes energy"
  energy_scale: "||h_{xy}|| = J"
  lattice_spacing: "a_lat = 1"
  entropy_base: "nats (ln)"

dimensional_check:
  v_LR: "[energy * lattice_spacing] = [velocity in natural units]"
  C_lr: "[dimensionless] (prefactor in LR bound)"
  mu_lr: "[1/lattice_spacing] (decay rate in LR bound)"

approximations:
  - name: "nearest-neighbor truncation"
    parameter: "interaction range / system size"
    validity: "exact if self-modeling coupling is strictly nearest-neighbor"
    breaks_when: "self-modeling produces longer-range coupling"
    check: "inherited from Plan 01"
  - name: "exponential F-function for LR bound"
    parameter: "decay rate a > 0"
    validity: "standard for finite-range interactions"
    breaks_when: "never for finite-range"
    check: "inherited from Plan 02"

estimated_execution:
  total_minutes: 50
  breakdown:
    - task: 1
      minutes: 25
      note: "Compute v_LR for self-modeling Hamiltonian + verify Paper 5 consistency"
    - task: 2
      minutes: 25
      note: "Verify composite OUS compatibility + document effective causal structure"

patterns_consulted:
  insights: []
  error_patterns: []
  adjustments_made: []

contract:
  scope:
    question: "What is the Lieb-Robinson velocity for the self-modeling Hamiltonian, and does the resulting lattice system reproduce the Paper 5 composite OUS structure?"
  claims:
    - id: claim-lr-velocity
      statement: "The Lieb-Robinson velocity v_LR is computed for the self-modeling interaction Hamiltonian with explicit constants C, mu, v_LR"
      deliverables: ["deliv-lr-computation"]
      acceptance_tests: ["test-lr-explicit", "test-lr-dimensional", "test-lr-limits"]
      references: ["ref-nachtergaele-sims-phase8", "ref-lieb-robinson-phase8"]
    - id: claim-paper5-compatibility
      statement: "The self-modeling lattice two-site structure reproduces the Paper 5 composite OUS axioms (C1-C4) and product-form SP"
      deliverables: ["deliv-compatibility-check"]
      acceptance_tests: ["test-c1c4", "test-product-sp"]
      references: ["ref-paper5-phase8"]
    - id: claim-causal-structure
      statement: "The self-modeling lattice has an effective causal structure: information propagation is bounded by v_LR, establishing a discrete light cone"
      deliverables: ["deliv-causal-structure"]
      acceptance_tests: ["test-light-cone"]
      references: ["ref-nachtergaele-sims-phase8"]
  deliverables:
    - id: deliv-lr-computation
      kind: derivation
      path: "derivations/08-lr-self-modeling.md"
      description: "Lieb-Robinson velocity computed for the self-modeling Hamiltonian with explicit C, mu, v_LR"
      must_contain: ["||Phi||_a for self-modeling h_{xy}", "v_LR = 2||Phi||_a C_a / a with optimized a", "explicit numerical value of v_LR/J", "C and mu constants"]
    - id: deliv-compatibility-check
      kind: derivation
      path: "derivations/08-lr-self-modeling.md"
      description: "Verification that the lattice two-site system satisfies Paper 5 composite OUS axioms"
      must_contain: ["C1 verification", "C2 verification", "C3 verification", "C4 verification", "product-form SP verification"]
    - id: deliv-causal-structure
      kind: derivation
      path: "derivations/08-lr-self-modeling.md"
      description: "Statement of the effective causal structure with explicit light cone"
      must_contain: ["light cone inequality", "exponential decay outside cone", "interpretation for downstream phases"]
    - id: deliv-lr-code
      kind: code
      path: "code/self_modeling_lr_velocity.py"
      description: "Computation of v_LR for the self-modeling Hamiltonian and verification of Paper 5 compatibility"
      must_contain: ["compute_self_modeling_lr_velocity()", "verify_composite_ous()", "verify_product_sp()", "verify_light_cone()"]
  acceptance_tests:
    - id: test-lr-explicit
      subject: claim-lr-velocity
      kind: benchmark
      procedure: "Compute v_LR for the self-modeling h_{xy} derived in Plan 01. Report C, mu, v_LR as explicit numbers (not just 'finite')."
      pass_condition: "v_LR is a finite positive number, reported as v_LR = [number] * J with explicit numerical coefficient"
      evidence_required: ["deliv-lr-computation", "deliv-lr-code"]
    - id: test-lr-dimensional
      subject: claim-lr-velocity
      kind: dimensional_analysis
      procedure: "Verify [v_LR] = [energy * length] in natural units with a_lat = 1, consistent with velocity"
      pass_condition: "Dimensions correct"
      evidence_required: ["deliv-lr-computation"]
    - id: test-lr-limits
      subject: claim-lr-velocity
      kind: limiting_case
      procedure: "Verify: (a) J -> 0 gives v_LR -> 0, (b) v_LR > 0 for J > 0 (non-trivial propagation), (c) v_LR scales linearly with J"
      pass_condition: "All three limits correct"
      evidence_required: ["deliv-lr-code"]
    - id: test-c1c4
      subject: claim-paper5-compatibility
      kind: consistency
      procedure: "For the lattice two-site system {x,y}: verify C1 (V_{xy} contains V_x tensor V_y), C2 (1_{xy} = 1_x tensor 1_y), C3 (product states exist), C4 (non-signaling: marginals well-defined)"
      pass_condition: "All four axioms verified"
      evidence_required: ["deliv-compatibility-check"]
    - id: test-product-sp
      subject: claim-paper5-compatibility
      kind: benchmark
      procedure: "For the lattice two-site system, verify (a tensor b) & (c tensor d) = (a & c) tensor (b & d) numerically for 20+ random effect quadruples using the Luders product"
      pass_condition: "All products match to relative error < 1e-10"
      evidence_required: ["deliv-lr-code"]
    - id: test-light-cone
      subject: claim-causal-structure
      kind: benchmark
      procedure: "For the self-modeling Hamiltonian on a chain of N=8 sites, time-evolve a local perturbation at site 1 and measure the commutator ||[A_1(t), B_r]|| for r = 2,...,8 at various times. Verify the wavefront propagates at speed <= v_LR."
      pass_condition: "Commutator is exponentially small for r > v_LR * t (within the LR bound)"
      evidence_required: ["deliv-lr-code"]
  references:
    - id: ref-paper5-phase8
      kind: prior_artifact
      locator: "Paper 5 (v2.0): composite OUS axioms C1-C4, product-form SP"
      role: benchmark
      why_it_matters: "The lattice two-site system must reproduce Paper 5's composite structure"
      applies_to: ["claim-paper5-compatibility"]
      must_surface: true
      required_actions: ["read", "compare"]
    - id: ref-nachtergaele-sims-phase8
      kind: paper
      locator: "Nachtergaele-Sims-Young (2019), arXiv:1810.02428"
      role: method
      why_it_matters: "Provides the LR bound formula applied to our specific Hamiltonian"
      applies_to: ["claim-lr-velocity", "claim-causal-structure"]
      must_surface: true
      required_actions: ["read", "cite"]
    - id: ref-lieb-robinson-phase8
      kind: paper
      locator: "Lieb-Robinson (1972), Commun. Math. Phys. 28, 251"
      role: benchmark
      why_it_matters: "Original finite group velocity result; historical anchor"
      applies_to: ["claim-lr-velocity"]
      must_surface: true
      required_actions: ["cite"]
  forbidden_proxies:
    - id: fp-cite-without-compute-phase8
      subject: claim-lr-velocity
      proxy: "Citing Lieb-Robinson bounds without computing v_LR for the specific self-modeling Hamiltonian"
      reason: "Contract requires v_LR computed with explicit constants C, mu, v_LR -- not just 'LR bounds apply'"
    - id: fp-assume-compatibility
      subject: claim-paper5-compatibility
      proxy: "Assuming the lattice structure is compatible with Paper 5 without checking axioms C1-C4"
      reason: "Compatibility is a non-trivial verification, not an assumption"
  uncertainty_markers:
    weakest_anchors:
      - "v_LR is an upper bound; the actual propagation speed may be slower by a constant factor"
      - "If Plan 01 finds a family of compatible Hamiltonians (not unique h_{xy}), v_LR depends on which member is chosen; must report the range"
    unvalidated_assumptions:
      - "The exponential F-function gives a near-optimal LR bound for the self-modeling interaction (Wang-Hazzard improved bounds may be tighter)"
    disconfirming_observations:
      - "If v_LR is infinite, the interaction norm ||Phi|| is unbounded -- something is wrong with the Hamiltonian construction"
      - "If the Paper 5 composite axioms C1-C4 are NOT satisfied by the lattice two-site system, the lattice construction is inconsistent with the self-modeling framework"
---

<objective>
Compute the Lieb-Robinson velocity for the self-modeling Hamiltonian constructed in Plan 01, verify compatibility with Paper 5's composite OUS, and establish the effective causal structure of the self-modeling lattice.

Purpose: This completes the Phase 8 deliverables (LOCL-01 and LOCL-02). The v_LR computation uses the framework validated in Plan 02 (Heisenberg benchmark). The Paper 5 compatibility check ensures the lattice construction is internally consistent.
Output: v_LR with explicit constants, Paper 5 compatibility verification, effective causal structure documentation.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/08-locality-formalization/08-01-SUMMARY.md
@.gpd/phases/08-locality-formalization/08-02-SUMMARY.md
@derivations/08-lattice-definition.md
@derivations/08-hamiltonian-construction.md
@derivations/08-lr-framework.md
@code/self_modeling_hamiltonian.py
@code/lieb_robinson_benchmark.py
@paper/sections/composite-lt.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute Lieb-Robinson velocity for self-modeling Hamiltonian</name>
  <files>derivations/08-lr-self-modeling.md, code/self_modeling_lr_velocity.py</files>
  <action>
    Apply the Nachtergaele-Sims LR framework (from Plan 02) to the self-modeling Hamiltonian (from Plan 01) and compute v_LR with explicit constants.

    **Part A: Interaction Norm**

    1. Read h_{xy} from Plan 01. Compute ||h_{xy}|| = J (this defines the energy scale).

    2. If Plan 01 found a family of compatible Hamiltonians (not unique h_{xy}):
       - Compute ||h_{xy}|| for each member of the family
       - Report the range of J values
       - v_LR will be computed for each J (since v_LR scales linearly with J)

    3. For the specific h_{xy} constructed in Plan 01 (n = 2), compute ||h_{xy}|| explicitly. This is the operator norm (largest singular value, which equals the largest absolute eigenvalue since h_{xy} is self-adjoint).

    4. Compute ||Phi||_a = z * ||h_{xy}|| * e^a / F_a(1) for the self-modeling interaction, where z is the coordination number and F_a(1) = e^{-a}.

    **Part B: LR Velocity**

    1. Using the C_a values computed in Plan 02, compute v_LR = 2 * ||Phi||_a * C_a / a for d = 1, 2, 3.

    2. Optimize over a to get the tightest bound for each dimension.

    3. Report: v_LR = [coefficient] * J for each lattice dimension, with explicit numerical coefficients.

    4. Report the full LR bound constants: C_LR (prefactor), mu_LR (decay rate), v_LR (velocity):
       ||[A(t), B]|| <= C_LR * ||A|| * ||B|| * e^{-mu_LR (d(X,Y) - v_LR |t|)}

    **Part C: Comparison with Heisenberg Benchmark**

    1. Compare v_LR for the self-modeling Hamiltonian with v_LR for the Heisenberg model (from Plan 02). Report the ratio v_LR^{SM} / v_LR^{Heisenberg}.

    2. If the self-modeling Hamiltonian turns out to be Heisenberg-like (possible outcome of Plan 01), the ratio should be close to ||h_{xy}^{SM}|| / ||h_{xy}^{Heisenberg}||.

    **Part D: Write Up Effective Causal Structure**

    1. State the effective light cone: for the self-modeling lattice, information propagation between sites X and Y is exponentially suppressed when d(X,Y) > v_LR * |t|.

    2. Interpret for downstream phases:
       - Phase 9 (area law): The LR bound + causal structure provides the "locality" input needed for area-law arguments
       - Phase 10 (Jacobson): v_LR provides the emergent speed of light (the maximum speed of information propagation in the lattice)
       - Phase 11 (numerics): v_LR sets the timescale for simulations (thermalization time ~ N / v_LR)

    3. State the connection to effective spacetime geometry: the LR light cone defines an effective causal structure on the lattice. In the continuum limit (Phase 10), this causal structure should match the light cone of the emergent spacetime metric.
  </action>
  <verify>
    1. **Explicit constants:** v_LR, C_LR, mu_LR are all reported as explicit numbers (not "finite" or "exists").

    2. **Dimensional analysis:** [v_LR] = [J] * [dimensionless] = [energy] = [1/time] (natural units, a_lat = 1). Consistent with velocity.

    3. **Limiting cases:**
       - J -> 0: v_LR -> 0 (no interaction, no propagation). Verify numerically.
       - v_LR > 0 for J > 0 (non-trivial propagation). Verify.
       - v_LR scales linearly with J. Verify: v_LR(2J) = 2 * v_LR(J).

    4. **Positivity:** C_LR > 0, mu_LR > 0, v_LR > 0 for J > 0.

    5. **Finiteness:** v_LR is finite (not divergent). If infinite, STOP -- the interaction norm is unbounded.

    6. **Consistency with Plan 02 pipeline:** The same code infrastructure (convolution constants, optimization) is reused, ensuring methodological consistency.
  </verify>
  <done>v_LR computed for the self-modeling Hamiltonian with explicit constants C_LR, mu_LR, v_LR. Values reported for Z^1, Z^2, Z^3. Comparison with Heisenberg benchmark documented. Effective causal structure described with downstream interpretation.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Paper 5 composite OUS compatibility and document complete Phase 8 results</name>
  <files>code/self_modeling_lr_velocity.py, derivations/08-lr-self-modeling.md</files>
  <action>
    Verify that the self-modeling lattice two-site system reproduces Paper 5's composite OUS axioms, and assemble the complete Phase 8 deliverables.

    **Part A: Composite OUS Axiom Verification (C1-C4)**

    For the lattice two-site system {x,y} with A_x = A_y = M_n(C), n = 2:

    1. **C1 (Vector space):** Verify V_{xy} = M_4(C)^sa contains V_x tensor V_y = M_2(C)^sa tensor M_2(C)^sa as a subspace. Since M_2(C) tensor M_2(C) = M_4(C), the self-adjoint parts satisfy this embedding. Check that dim(V_{xy}) = 16 = 4 * 4 = dim(V_x) * dim(V_y) (local tomography dimension counting).

    2. **C2 (Order unit):** Verify 1_{xy} = 1_x tensor 1_y = I_2 tensor I_2 = I_4.

    3. **C3 (Product states):** For random states rho_x, rho_y (density matrices on C^2), verify that rho_x tensor rho_y is a valid state on C^4 (positive, trace 1). Compute expectations: (rho_x tensor rho_y)(a tensor b) = rho_x(a) * rho_y(b) = Tr(rho_x a) * Tr(rho_y b).

    4. **C4 (Non-signaling):** For random joint state rho_{xy} on C^4, verify that the marginals are well-defined: rho_x = Tr_y(rho_{xy}), rho_y = Tr_x(rho_{xy}). Verify non-signaling: measuring at site x (applying effect a tensor I at site x) does not change the marginal state at site y.

    Perform all checks numerically with 20+ random inputs.

    **Part B: Product-Form SP Verification**

    For the lattice two-site system, verify the product-form SP:
    (a tensor b) & (c tensor d) = (a & c) tensor (b & d)

    where a & c = a^{1/2} c a^{1/2} (Luders product) on M_2(C)^sa.

    1. Generate 20 random effect quadruples (a, b, c, d) with 0 <= a, b, c, d <= I.
    2. Compute LHS: (a tensor b) & (c tensor d) using the composite Luders product on M_4(C)^sa.
    3. Compute RHS: (a^{1/2} c a^{1/2}) tensor (b^{1/2} d b^{1/2}).
    4. Verify ||LHS - RHS|| < 1e-10 for all quadruples.

    **Part C: Light Cone Numerical Verification**

    On a small chain (N = 8 sites, n = 2):

    1. Construct the full Hamiltonian H = sum_{i=1}^{7} h_{i,i+1} using the self-modeling h_{xy} from Plan 01.
    2. Prepare a local perturbation at site 1: A = sigma_z tensor I tensor ... tensor I.
    3. Time-evolve A(t) = e^{iHt} A e^{-iHt} for t = 0, 0.1, 0.2, ..., 2.0 (in units of 1/J).
    4. Measure the commutator ||[A(t), B_r]|| for B_r = I tensor ... tensor sigma_z tensor ... tensor I (sigma_z at site r, I elsewhere) for r = 2, ..., 8.
    5. Verify: for each (t, r), the commutator is bounded by the LR bound C_LR * e^{-mu_LR(r - v_LR t)}.
    6. Plot the "light cone": commutator magnitude as a function of (t, r). The wavefront should propagate at speed <= v_LR.

    Note: the full Hilbert space has dimension 2^8 = 256, so exact diagonalization is trivial.

    **Part D: Assemble Phase 8 Results Summary**

    In the derivation file, include a final section summarizing all Phase 8 results in a form ready for Phase 9:

    1. Lattice definition: G = (V, E), A_x = M_n(C), H = sum h_{xy}
    2. h_{xy} form and parameter family (from Plan 01)
    3. Locality mapping (from Plan 01)
    4. v_LR with explicit constants (this plan)
    5. Paper 5 compatibility: verified
    6. Downstream inputs for Phase 9: the Hamiltonian H, the causal structure (v_LR, light cone), and the relevant state(s) of H
    7. Open questions for Phase 9: which state has area-law entanglement, whether H is gapped, whether the family of compatible h_{xy} affects area-law robustness
  </action>
  <verify>
    1. **C1-C4 all pass:** Each axiom verified numerically with 20+ random inputs.

    2. **Product-form SP:** All 20+ random quadruples give ||LHS - RHS|| < 1e-10.

    3. **Light cone:** Commutator magnitudes are bounded by the LR bound for all (t, r) pairs. Wavefront speed <= v_LR.

    4. **Dimension counting:** dim(V_{xy}) = 16 = dim(V_x) * dim(V_y) = 4 * 4 (local tomography).

    5. **Non-signaling:** Marginal states are independent of remote measurements to numerical precision.

    6. **Complete summary:** All 5 success criteria from the ROADMAP are addressed:
       (a) Lattice defined with G, A_x, H -- CHECK
       (b) Self-modeling locality mapped to Hamiltonian locality -- CHECK (Plan 01)
       (c) v_LR computed with explicit C, mu, v_LR -- CHECK
       (d) Dimensional consistency and Bratteli-Robinson compatibility -- CHECK
       (e) Paper 5 local tomography compatibility -- CHECK

    7. **No forbidden proxies:** v_LR is computed for the specific self-modeling Hamiltonian, not cited generically. Compatibility is verified, not assumed.
  </verify>
  <done>Paper 5 composite OUS axioms C1-C4 and product-form SP verified numerically. Light cone simulation confirms information propagation bounded by v_LR. Complete Phase 8 summary assembled with all deliverables documented and downstream inputs for Phase 9 identified.</done>
</task>

</tasks>

<verification>
- v_LR computed with explicit numerical constants for d = 1, 2, 3
- Paper 5 composite OUS axioms C1-C4 all verified
- Product-form SP verified numerically to high precision
- Light cone simulation confirms LR bound
- All 5 Phase 8 success criteria from ROADMAP addressed
- Complete downstream summary for Phase 9
</verification>

<success_criteria>
v_LR computed with explicit constants C_LR, mu_LR, v_LR for the self-modeling Hamiltonian. Paper 5 compatibility verified (C1-C4 + product-form SP). Light cone confirmed numerically. Phase 8 deliverables complete (LOCL-01 lattice definition + LOCL-02 Lieb-Robinson bounds).
</success_criteria>

<output>
After completion, create `.gpd/phases/08-locality-formalization/08-03-SUMMARY.md`
</output>
