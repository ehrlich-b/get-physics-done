---
phase: 72-b-matter-sourcing
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified: [code/bulk_geometry_verification.py, derivations/72-matter-sourcing.tex]
interactive: true

conventions:
  units: "natural (hbar=c=k_B=1); EXACT rational arithmetic over Q on all decisive verdicts (NO float)"
  metric_slice: "mostly-minus (-,+,+,+) Lorentzian on the dim-4 h_2(C_u) sub-slice via det_2"
  metric_bulk: "Riemannian positive-definite; cone metric g_X = Hess(-log det)"
  potential: "Phi = -log det_3 (full) vs Phi_off = -log det_3_block (cross-term off)"
  det_SSOT: "engine det_3 (code/bulk_geometry_verification.py), cross-term 2*Re((x2*x1)*x3); octonion_algebra.py BANNED. det_3_block = det_3 with the 2*Re((x2 x1) x3) triple set to 0"
  curvature_engine: "Totaro closed form R_ijkl = -(1/4) g^{pq}(C_jlp C_ikq - C_ilp C_jkq); hand-rolled Christoffel/Riemann (NOT sympy.diffgeom)"
  riemann_ricci_sign: "Ric_jl = g^{ik} R_ijkl; R = g^{jl} Ric_jl. Sign pinned NEGATIVE at center (R(center)=-3); H^3 cone-Hessian K_sect=-1/2"
  coordinates: "spacetime point on dim-4 h_2(C_u); engine slice indices {1,2,3,10}=(beta,gamma,p,q) kept symbolic; FIXED V_0-background X_bg = I/3 + (fixed V_0-internal x1 in {4..9}); matter M in V_1={0} + V_{1/2}={11..26}"
  genuine_modulus: "det_2 (Stab_{V_0}=Spin(9,1) invariant); report scaling vs det_2 and NOTE the rho_J coincidence for single-direction perturbations"

dimensional_check:
  Delta_R_full: "[dimensionless]; exact rational over Q; real (imaginary part 0)"
  Delta_R_off: "[dimensionless]; exact rational over Q; the cross-term-OFF matter response"
  Delta_R_series_coeff: "[dimensionless]; exact rational per order in amplitude t"
  leading_power_k: "integer >= 1 (read EMPIRICALLY from the series; do NOT assume k=2)"

approximations:
  - name: "matter rational before symbolic inverse"
    parameter: "matter substituted to small rationals; only the 4 slice coords symbolic"
    validity: "always (controlled exact regime, ~0.5-3s 4x4 rational inverse; on/off pair ~40s)"
    breaks_when: "matter carried symbolic through Matrix.inv() => >200s, trips ~150s watchdog"
    check: "Matrix.inv() returns in <60s; foreground python -u with progress prints"
  - name: "series in matter amplitude t (M = t*M0)"
    parameter: "t = ||M|| along a fixed V_{1/2} octonionic direction M0"
    validity: "small-matter neighborhood of the fixed V_0-background; det cubic => the t-jet TERMINATES (finite, exact per order)"
    breaks_when: "using t to extrapolate to large M (out of scope) or reading the leading power without checking the next order"
    check: "each series coefficient is an exact rational; Delta R(t)->0 as t->0; leading power k read empirically (compute >= 2 orders)"
  - name: "representative M + off-switch + ||M||->0, not all-M"
    parameter: "decisive verdict on representative non-vacuous M with the cross-term off-switch and the t->0 limit"
    validity: "sufficient for SURVIVES / honest-negative (exact over Q); an all-M general theorem is Phase C / out of scope"
    breaks_when: "claiming a general Einstein theorem for all M -- defer to Phase C"
    check: "report 'decisive on representative M', not 'proved for all M'"

contract:
  schema_version: 1
  scope:
    question: "Is the M-dependent slice curvature Delta R(M) (at a FIXED V_0-background, all three octonion slots populated) SOURCED by the cubic-norm cross-term 2*Re((x2 x1) x3) -- i.e. does Delta R_full(M) != 0 VANISH (or change decisively) when the cross-term is switched off (det_3_block), does Delta R(M)->0 as ||M||->0, and does Delta R carry traceless-Ricci/Weyl structure beyond a pure-Lambda shift? Decisive: SURVIVES (Phase 73 greenlit) vs honest-negative 'position-dependent but not matter-sourced' (STOP)."
  claims:
    - id: claim-matter-sourcing
      statement: "Conditional on Phase A showing genuine x-dependence (Phase 71: SURVIVES, VERIFIED): the slice curvature is SOURCED by matter M in V_1+V_{1/2} via the cubic-norm cross-terms -- M=0 gives flat or pure-Lambda, and switching off the cross-terms (block-diagonal det) removes the M-sourced curvature; the curvature scale relates to ||M|| and rho_J(X_bg). THIS PLAN (72-02) delivers the DECISIVE cross-term ON/OFF test and the ||M||/det_2 scaling, consuming the matter-curvature pipeline + ricci_decomposition + chosen M from 72-01."
      deliverables: [deliv-phaseB]
      acceptance_tests: [test-cross-term-onoff, test-lambda-vs-matter]
      references: [ref-faraut-koranyi, ref-mccrimmon, ref-peirce-coupling, ref-totaro, ref-prompt]
  observables:
    - id: obs-matter-sourcing
      name: "Cross-term-isolated M-dependent slice curvature and its ||M||/det_2 scaling"
      kind: proof_obligation
      definition: "Delta R(M) = R(X_bg+M)-R(X_bg) computed with the FULL cubic norm vs the block-diagonal det_3_block (cross-term off) at the SAME M != 0 and fixed V_0-background; plus the series Delta R(t), M=t*M0, giving the leading power in ||M|| and the V_1-only / V_{1/2}-only / both decomposition; scaling reported vs det_2."
      regime: "linear and beyond in ||M||; off-center det_2; representative non-vacuous rational M on the dim-4 h_2(C_u) slice; exact over Q"
  deliverables:
    - id: deliv-phaseB
      kind: derivation
      path: derivations/72-matter-sourcing.tex
      description: "Matter-sourcing of the inherited slice curvature (engine extension + writeup). 72-02 portion: the cross-term ON/OFF isolation (full det vs det_3_block), the Lambda/traceless-Ricci/Weyl decomposition of Delta R, the ||M|| leading-power and det_2 scaling, the V_1-vs-V_{1/2} channel decomposition, and the decisive SURVIVES / honest-negative verdict."
      must_contain:
        - "cross-term on/off comparison (full det vs block-diagonal det(V_1)*det(V_0))"
        - "quantified curvature scale vs ||M|| and off-center rho_J/det_2"
        - "M=0 baseline (flat or pure Lambda) established"
        - "Riemann/Ricci tensor of the inherited slice metric as a function of M"
        - "decisive verdict: matter-sourced (SURVIVES, Phase 73 greenlit) OR position-dependent-but-not-matter-sourced (honest negative, STOP)"
    - id: deliv-engine-onoff
      kind: code
      path: code/bulk_geometry_verification.py
      description: "Engine extension: det_3_block (cross-term off) + its M=0 reduction proof; the decisive Delta R_full vs Delta R_off comparison; the series Delta R(t) scaling + V_1/V_{1/2} decomposition; all registered as _report() gates keeping main() ALL_PASS exit 0."
      must_contain:
        - "det_3_block"
        - "_report"
  references:
    - id: ref-prompt
      kind: spec
      locator: "~/scratch/get-physics-done/paper6-bulk-geometry-prompt.md"
      aliases: ["ref-prompt", "paper6-bulk-geometry-prompt"]
      role: must_consider
      why_it_matters: "Authoritative milestone spec: Phase B(b) (cross-term off-switch removes M-curvature), B(c) (scale vs ||M|| and rho_J), and the reporting discipline ('do not soften'; honest negative if not sourced)."
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [planning, execution, verification]
      must_surface: true
      required_actions: [read]
    - id: ref-faraut-koranyi
      kind: paper
      locator: "Faraut & Koranyi, Analysis on Symmetric Cones (1994), Oxford"
      aliases: ["ref-faraut-koranyi", "Faraut-Koranyi"]
      role: definition
      why_it_matters: "Cone metric g_X=Hess(-log det); g^{pq}=P(X); E_{6(-26)}/F_4 Cartan/Einstein-negative baseline (the M=0 pure-Lambda reference Delta R is differenced from). det_3_block reduction to det(V_0) at M=0 rests on the same det structure."
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-mccrimmon
      kind: paper
      locator: "McCrimmon, A Taste of Jordan Algebras (2004)"
      aliases: ["ref-mccrimmon", "McCrimmon"]
      role: background
      why_it_matters: "Peirce decomposition + cubic norm structure: the triple 2*Re((x2 x1) x3) as the unique V_0<->V_{1/2} coupling whose removal defines det_3_block."
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [read, cite]
    - id: ref-peirce-coupling
      kind: prior_artifact
      locator: "~/scratch/get-physics-done/rho_directional_derivatives.py (exact SymPy off-center / amplitude-expansion STRUCTURE for CALC-04) + peirce_coupling.py (numpy/float intuition: x1 couples to E_11 only through the cubic trilinear)"
      aliases: ["ref-peirce-coupling"]
      role: must_consider
      why_it_matters: "The off-center / amplitude expansion strategy for the Delta R(t) scaling law (series in t). CAUTION: rho_directional_derivatives.py uses a real-only cross-term 2*d1*d2*d3 -- reuse the EXPANSION STRATEGY only; recompute the octonionic cross-term from the engine det_3 (the decisive path)."
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [use]
    - id: ref-totaro
      kind: paper
      locator: "Totaro, The curvature of a Hessian metric, arXiv:math/0401381, Cor 2.3"
      aliases: ["ref-totaro", "Totaro"]
      role: method
      why_it_matters: "The Hessian-curvature closed form (engine totaro_riemann) used for both Delta R_full and Delta R_off; det_3_block is also cubic so f_ijkl=0 still holds (no 4th-derivative blowup)."
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [planning, execution]
      must_surface: false
      required_actions: [cite]
    - id: ref-h3o-tower
      kind: prior_artifact
      locator: "~/repos/blog/research/qualia-fixed-point/h3o_tower.py (corrected cubic-norm benchmark)"
      aliases: ["ref-h3o-tower"]
      role: benchmark
      why_it_matters: "Optional provenance cross-check of the engine det_3 cross-term association used in det_3_block."
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [verification]
      must_surface: false
      required_actions: [use]
  acceptance_tests:
    - id: test-cross-term-onoff
      subject: claim-matter-sourcing
      kind: consistency
      procedure: "Compute the slice Riemann/Ricci with the FULL cubic norm (Phi=-log det_3) vs the block-diagonal det_3_block (Phi_off=-log det_3_block, the 2*Re((x2 x1) x3) triple set to 0) at the SAME M != 0 and the SAME FIXED V_0-background, all three octonion slots populated (non-vacuity gate 2*Re((x2 x1) x3) != 0 asserted first). Compare Delta R_full(M)=R_full(X_bg+M)-R_full(X_bg) against Delta R_off(M)=R_off(X_bg+M)-R_off(X_bg), exact over Q. First verify det_3_block reduces to the slice norm det(V_0) at M=0 (x2=x3=alpha-matter=0). Decompose Delta R_full into scalar(Lambda)/traceless-Ricci/Weyl (ricci_decomposition from 72-01) to confirm it is matter STRUCTURE, not a Lambda shift."
      pass_condition: "det_3_block reduces to det(V_0) at M=0 exactly over Q; M-sourced curvature present with the full norm (Delta R_full != 0) VANISHES (Delta R_off = 0) or changes DECISIVELY when the cross-term is switched off, at the same M != 0 => matter is cross-term-sourced (SURVIVES). If Delta R_off = Delta R_full (off-switch makes no difference) OR Delta R_full is pure-Lambda (Delta S_munu = 0 and Delta Weyl = 0) => honest negative 'position-dependent but NOT matter-sourced' (STOP, do NOT proceed to Phase 73). Exact over Q; non-vacuity gate passed."
      evidence_required: [deliv-phaseB, deliv-engine-onoff, ref-prompt, ref-faraut-koranyi]
      automation: automated
    - id: test-lambda-vs-matter
      subject: claim-matter-sourcing
      kind: limiting_case
      procedure: "Series Delta R(t) with M = t*M0 (a fixed V_{1/2} octonionic direction): confirm Delta R(t) -> 0 as t -> 0 (recovers the matterless baseline) and read the leading power k in ||M|| EMPIRICALLY from >= 2 exact-per-order coefficients (do NOT assume k=2). Report the curvature scale vs det_2 (the genuine Stab_{V_0}-invariant modulus) at fixed direction, noting the rho_J coincidence. Decompose Delta R into V_1-only, V_{1/2}-only, and both, and explain the Phase-71 V_1-inertness structurally (alpha index 0 is ABSENT from the octonion triple 2*Re((x2 x1) x3))."
      pass_condition: "Delta R(t) -> 0 as t -> 0 (M->0 recovers the matterless geometry, exact per order over Q); leading power k >= 1 read empirically with exact rational coefficient; scaling reported vs det_2 (rho_J coincidence noted); V_1-only Delta R = 0 (inert, structurally explained) while V_{1/2} contributes -- so the M=0/pure-Lambda part is NOT counted as matter-sourcing (fp-lambda-as-sourcing rejected). Exact over Q."
      evidence_required: [deliv-phaseB, deliv-engine-onoff, ref-peirce-coupling]
      automation: automated
  forbidden_proxies:
    - id: fp-lambda-as-sourcing
      subject: claim-matter-sourcing
      proxy: "Counting a pure-Lambda (M=0) curvature -- OR, per Phase 71, the matterless det_2-variation -- as matter-sourcing; OR reading a Delta R that SURVIVES the cross-term off-switch as cross-term-sourced."
      reason: "Only the M-dependent part sourced by the V_0<->V_1/V_{1/2} cross-term counts. A Delta R proportional to g_munu with constant coefficient is a Lambda shift (not matter structure); a Delta R that survives the off-switch is intrinsic, not cross-term-sourced. Both are the forbidden proxy => honest negative."
    - id: fp-ensemble-gravity
      subject: claim-matter-sourcing
      proxy: "Any observers-make-gravity / thermodynamic (Jacobson-style) ensemble argument for the matter-curvature link."
      reason: "The curvature must come from the algebra's own cubic-norm geometry -- one observer, one off-center point. No ensemble/entropy/holographic-screen argument."
    - id: fp-float-decisive
      subject: claim-matter-sourcing
      proxy: "Computing the decisive cross-term on/off comparison or the scaling-law coefficients in float (numpy ranks, float curvature)."
      reason: "Exact over Q. The off-switch verdict (Delta R_off = 0 vs != 0) and each series coefficient are exact rationals; float only for explicitly-labeled NON-decisive triage."
  links:
    - id: link-onoff-decisive
      source: claim-matter-sourcing
      target: deliv-phaseB
      relation: supports
      verified_by: [test-cross-term-onoff, test-lambda-vs-matter]
    - id: link-engine-onoff-computes
      source: deliv-phaseB
      target: deliv-engine-onoff
      relation: computes
      verified_by: [test-cross-term-onoff]
    - id: link-prompt-discipline
      source: ref-prompt
      target: deliv-phaseB
      relation: supports
      verified_by: [test-cross-term-onoff]
    - id: link-peirce-scaling
      source: ref-peirce-coupling
      target: deliv-engine-onoff
      relation: supports
      verified_by: [test-lambda-vs-matter]
    - id: link-fk-reduction
      source: ref-faraut-koranyi
      target: deliv-engine-onoff
      relation: benchmarks
      verified_by: [test-cross-term-onoff]
  uncertainty_markers:
    weakest_anchors:
      - "No external literature anchor for the matter-sourcing CLAIM (novel; off-diagonal-Peirce content sourcing Hessian curvature on a symmetric cone). Validation is internal: the off-switch must EXACTLY kill Delta R, ||M||->0 must EXACTLY recover the matterless geometry, reality EXACT over Q. Confidence on the OUTCOME is MEDIUM (this is the phase's open question)."
      - "Open Question 2: 'cross-terms off' is precise for the V_0<->V_{1/2} TRIPLE (2*Re((x2 x1) x3)); the |x2|^2,|x3|^2 norm terms (V_{1/2} self-norms) remain and still couple x2,x3 to the diagonal a,b,g. det_3_block does NOT fully decouple the matter norms -- it removes only the named triple channel. State explicitly; do not overclaim full decoupling."
      - "rho_J vs det_2: the contract wording is rho_J(X_bg); the genuine Stab_{V_0}=Spin(9,1)-invariant modulus is det_2 (they coincide for single-direction perturbations). Report scaling vs det_2 and NOTE the coincidence."
      - "Leading power k in ||M|| is unknown a priori (Open Question 3): METHODS suggested O(||M||^2) from C*C, but the cross-term may give a linear leading term. Read empirically; do NOT assume."
    disconfirming_observations:
      - "Delta R SURVIVES the cross-term off-switch (Delta R_off = Delta R_full, off-switch makes no difference) => curvature is intrinsic/Lambda-like, NOT cross-term-sourced => honest negative 'position-dependent but not matter-sourced', STOP (do NOT proceed to Phase 73). This is fp-lambda-as-sourcing avoided -- a SUCCESS (NEGATIVE-RESULT-IS-SUCCESS)."
      - "Delta R_full is pure-Lambda (Delta S_munu = 0 and Delta Weyl = 0, i.e. Delta R proportional to g_munu with constant coefficient) => a Lambda shift, not matter structure => honest negative."
      - "Delta R(t) does NOT vanish as t->0 => isolation broken (background not actually fixed, or matter leaked into V_0) => re-fix the V_0-background; the verdict is not yet decidable."
      - "det_3_block does NOT reduce to det(V_0) at M=0 => the off-switch is implemented wrong (wrong term dropped) => fix before any on/off verdict."
      - "Delta R has a nonzero imaginary part => reality/Wick-contamination or wrong cross-term association => block until exactly real over Q."
---

<objective>
Run the DECISIVE matter-sourcing test: does the M-dependent slice curvature Delta R(M) come from the cubic-norm cross-term 2*Re((x2 x1) x3) (the unique V_0<->V_{1/2} channel)? This is the phase verdict gate.

Consume the 72-01 pipeline: the matter-aware Delta R(M) = R(X_bg+M) - R(X_bg) at a FIXED V_0-background, the ricci_decomposition (scalar/traceless-Ricci/Weyl), and the chosen non-vacuous M (fixed V_0-internal x1 in {4..9}; matter in V_1={0} and V_{1/2}={11..26}; all three octonion slots populated with e_4..e_7 content; cross-term verified != 0). Build det_3_block (the cross-term off-switch), run the ON/OFF comparison, the ||M||/det_2 scaling, and the V_1-vs-V_{1/2} channel decomposition.

Decisive logic (per the backtracking trigger): matter SOURCES the curvature (SURVIVES; Phase 73 greenlit) iff Delta R_full(M) != 0 VANISHES (or changes decisively) under the off-switch AND Delta R(M) -> 0 as ||M|| -> 0 AND Delta R carries traceless-Ricci/Weyl structure beyond a pure-Lambda shift. If Delta R SURVIVES the off-switch, OR is pure-Lambda, OR does not vanish as ||M||->0, then matter does NOT source the slice curvature -- report "position-dependent but pure-Lambda / not matter-sourced" HONESTLY and do NOT proceed to Phase 73 (NEGATIVE-RESULT-IS-SUCCESS).

Purpose: CALC-03 (the decisive cross-term ON/OFF) and CALC-04 (the ||M|| leading-power + det_2 scaling + V_1/V_{1/2} channel decomposition).
Output: engine extension (det_3_block + the on/off + scaling gates) and the 72-02 portion of derivations/72-matter-sourcing.tex with the explicit verdict.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/CONVENTIONS.md
@.gpd/phases/72-b-matter-sourcing/72-RESEARCH.md
@.gpd/phases/72-b-matter-sourcing/72-01-SUMMARY.md
@code/bulk_geometry_verification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: CALC-03a -- build det_3_block (cross-term off) and verify its M=0 reduction + non-vacuity gate</name>
  <files>code/bulk_geometry_verification.py</files>
  <action>
    Build the cross-term off-switch as a one-line variant of det_3, and verify it is implemented correctly BEFORE any on/off verdict. Foreground `python3 -u`; progress prints (watchdog).

    1. det_3_block(X). Copy det_3 (line 351) but set the triple to zero:
       det_3_block(X) = a*b*g - a*|x1|^2 - b*|x2|^2 - g*|x3|^2   (the `+ 2*cross[0]` term DROPPED, where cross = oct_mul(oct_mul(x2,x1),x3)).
       Implement as a flag on det_3 (`cross_off=True`) OR a separate det_3_block. This removes the UNIQUE V_0<->V_{1/2} octonion-triple coupling; the |x2|^2,|x3|^2 norm terms remain (Open Question 2 -- state explicitly: this is the named triple channel off-switch, NOT full matter-norm decoupling).

    2. M=0 REDUCTION GATE. Verify det_3_block reduces to the V_0 slice norm at M=0 (x2 = x3 = alpha-matter = 0): on a representative point with matter off (x2=x3=0, alpha=beta=gamma + V_0 x1), assert det_3_block(X) == det_3(X) (they agree exactly when the triple is already zero) and == the block-diagonal a*b*g - a*|x1|^2 (det(V_0) form with x2=x3=0). Exact over Q. Document that "det(V_1)*det(V_0)" in the prompt is schematic; the operational definition is "det_3 with the 2*Re((x2 x1) x3) term set to 0".

    3. NON-VACUITY GATE (Pitfall 3). Re-assert at the chosen M (from 72-01: fixed V_0-internal x1 in {4..9}; matter x2 in {11..18}, x3 in {19..26}; e_4..e_7 content) that 2*Re((x2 x1) x3) != 0 exactly over Q (so the off-switch genuinely changes det). If it were 0, the on/off test would be hollow (full and block identical for trivial reasons). All three octonion slots must be populated.

    4. Phi_off PIPELINE. Confirm Phi_off = -log det_3_block flows through the SAME exact curvature path: build a det_3_block-based variant of _curvature_invariants_at (or parametrize it by the norm function), so R_off, Ric_off, Delta R_off are computed identically to the full case but with Phi_off. det_3_block is still cubic => f_ijkl = 0 still holds (totaro_riemann still valid, no 4th-derivative blowup). Spot-check riemann_symmetry_ok on R_off.

    Register the M=0 reduction gate, the non-vacuity gate, and a riemann_symmetry_ok(R_off) gate as _report() in main(). Exact over Q; matter rational.
  </action>
  <verify>
    1. det_3_block = det_3 with the 2*Re((x2 x1) x3) triple dropped; at M=0 (x2=x3=alpha-matter=0) it reduces to det(V_0) = a*b*g - a*|x1|^2 exactly over Q, and equals det_3 there.
    2. Non-vacuity gate: 2*Re((x2 x1) x3) != 0 at the chosen M (exact over Q; all three octonion slots populated, e_4..e_7) -- the off-switch genuinely changes det.
    3. Phi_off = -log det_3_block flows through the exact rational-inverse Totaro path; R_off is real over Q, det_g_off != 0, riemann_symmetry_ok(R_off,4) = True.
    4. det_3_block is cubic (degree 3 in the coords) => f_ijkl = 0 holds, Totaro form valid.
    5. Open Question 2 documented: the off-switch removes the named triple channel only; |x2|^2,|x3|^2 norm terms remain (no overclaim of full decoupling).
    6. Engine main() ALL_PASS exit 0.
  </verify>
  <done>det_3_block (cross-term off) built and verified to reduce to det(V_0) at M=0 exactly over Q; non-vacuity gate re-asserted (2*Re((x2 x1) x3) != 0 at the chosen M, all three slots); Phi_off flows through the exact Totaro path with R_off real, nondegenerate, riemann_symmetry_ok True; Open Question 2 documented; engine ALL_PASS exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: CALC-03b -- the DECISIVE cross-term ON/OFF test (Delta R_full vs Delta R_off, with Lambda/structure decomposition)</name>
  <files>code/bulk_geometry_verification.py, derivations/72-matter-sourcing.tex</files>
  <action>
    Run the decisive matter-sourcing comparison at the SAME M != 0 and the SAME FIXED V_0-background, full norm vs block norm. Foreground `python3 -u`; progress prints between the four heavy curvature builds (R_full at X_bg, R_full at X_bg+M, R_off at X_bg, R_off at X_bg+M).

    1. THE FOUR CURVATURES. Using delta_R_at_fixed_bg (72-01) for the FULL norm and its det_3_block twin for the OFF norm, at the chosen non-vacuous M and the common slice point, compute:
       - Delta R_full(M) = R_full(X_bg+M) - R_full(X_bg)
       - Delta R_off(M)  = R_off(X_bg+M)  - R_off(X_bg)
       all exact over Q, with reality and det_g != 0 checks. (72-01 smoke confirmed Delta R_full = 16118528604125200/3374980547075787 != 0 at one such M.)

    2. THE DECISIVE COMPARISON. Evaluate, exact over Q:
       - Is Delta R_full(M) != 0? (matter changes the full-norm curvature)
       - Is Delta R_off(M) = 0, or decisively smaller / structurally different from Delta R_full? (the off-switch removes the matter effect)
       Decisive SURVIVES iff Delta R_full != 0 AND Delta R_off vanishes (or changes decisively). If Delta R_off == Delta R_full (off-switch makes NO difference) => the curvature is NOT cross-term-sourced => honest negative.

    3. STRUCTURE NOT LAMBDA. Decompose Delta R_full via the 72-01 ricci_decomposition into Delta(scalar/Lambda), Delta(traceless-Ricci S_munu), Delta(Weyl): confirm Delta R_full carries Delta S_munu != 0 and/or Delta Weyl != 0 (genuine matter STRUCTURE beyond a pure-Lambda shift). If Delta R_full is pure-Lambda (Delta S_munu = 0 AND Delta Weyl = 0) => honest negative (it is just a cosmological-constant shift, fp-lambda-as-sourcing).

    4. Register the decisive on/off comparison and the Lambda-vs-structure decomposition of Delta R as _report() gates in main() (the gate PASSES on a clean decisive outcome -- either a clean SURVIVES or a clean, honestly-reported negative; it FAILS only on an inconsistent/contaminated computation, e.g. non-real Delta R, det_g=0, or det_3_block not reducing at M=0). Write the CALC-03 portion of derivations/72-matter-sourcing.tex: the four curvatures, the on/off verdict, the Delta-decomposition. Surface ref-prompt (B(b) discipline), ref-faraut-koranyi.

    Performance: four curvature builds, matter rational => ~40-80s total. If any inverse runs >60s, matter leaked symbolic -- substitute to rationals. Progress prints between builds (watchdog).
  </action>
  <verify>
    1. Delta R_full(M) and Delta R_off(M) both exact over Q, real (imaginary part 0), det_g != 0 (full and off, M-on and M-off).
    2. Decisive on/off: either (SURVIVES) Delta R_full != 0 AND Delta R_off = 0 / decisively changed; or (honest negative) Delta R_off = Delta R_full. The verdict is read from exact-over-Q values, NOT float (fp-float-decisive).
    3. Lambda-vs-structure: Delta R_full decomposed into Delta(scalar), Delta(S_munu), Delta(Weyl) exact over Q; SURVIVES requires Delta S_munu != 0 and/or Delta Weyl != 0 (matter structure, not a constant Lambda shift).
    4. Consistency: the M=0 column R_full(X_bg) and R_off(X_bg) agree where the cross-term is already zero at the background (sanity on the fixed reference); det_3_block reduced correctly at M=0 (Task 1).
    5. Disconfirming-observation discipline: if Delta R survives the off-switch OR is pure-Lambda, the gate records the HONEST NEGATIVE (not a softened positive) -- this is a SUCCESS outcome of the test, routed to the Task 3 verdict checkpoint.
    6. derivations/72-matter-sourcing.tex CALC-03 portion written; ref-prompt, ref-faraut-koranyi surfaced. Engine ALL_PASS exit 0.
  </verify>
  <done>The decisive cross-term ON/OFF test executed exact over Q: Delta R_full(M) vs Delta R_off(M) at the same M and fixed V_0-background, with Delta R_full decomposed into Lambda/traceless-Ricci/Weyl. The outcome (SURVIVES iff Delta R_full != 0 vanishes under the off-switch AND carries non-Lambda structure; else honest negative) is recorded exact over Q and routed to the Task 3 verdict checkpoint. CALC-03 writeup done; engine ALL_PASS exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: CALC-04 -- ||M|| leading-power + det_2 scaling + V_1/V_{1/2} channel decomposition</name>
  <files>code/bulk_geometry_verification.py, derivations/72-matter-sourcing.tex</files>
  <action>
    Quantify the matter-curvature scale and decompose the channels. Use the series-in-amplitude strategy from rho_directional_derivatives.py (ref-peirce-coupling) -- STRATEGY ONLY; recompute the octonionic cross-term from the engine det_3 (the real-only 2*d1*d2*d3 in that file is NOT the decisive path). Foreground `python3 -u`; progress prints.

    1. SCALING LAW Delta R(t). Set M = t*M0 with M0 a FIXED V_{1/2} octonionic direction (e.g. the x2,x3 content from the chosen M, scaled by t; keep the fixed V_0-internal x1 in the background). Compute Delta R(t) = R(X_bg + t*M0) - R(X_bg) at a sequence of small rational t (e.g. t in {1/10, 1/5, 1/3}) exact over Q, AND/OR as a low-order series in t (each coefficient an exact rational; the det-cubic jet TERMINATES). Read the LEADING POWER k in ||M|| EMPIRICALLY (compute >= 2 orders; do NOT assume O(||M||^2) -- Open Question 3). Confirm Delta R(t) -> 0 as t -> 0 (recovers the matterless baseline, exact per order).

    2. det_2 SCALING. Report the curvature scale vs det_2 (the genuine Stab_{V_0}=Spin(9,1)-invariant modulus, Phase-71): vary the fixed V_0-background across >= 2 det_2 leaves and report how the Delta R(M) magnitude tracks det_2 at fixed matter direction. NOTE the rho_J coincidence (rho_J and det_2 agree for single-direction perturbations; det_2 is the strictly-correct invariant).

    3. V_1 vs V_{1/2} CHANNEL DECOMPOSITION. Compute Delta R separately for (a) V_1-only matter (alpha, index 0), (b) V_{1/2}-only matter (indices 11..26), (c) both, at the fixed V_0-background. Confirm/explain the Phase-71 V_1-inertness: V_1-only Delta R = 0 (alpha is ABSENT from the octonion triple 2*Re((x2 x1) x3) -- it only multiplies |x1|^2 etc. as a diagonal coefficient), while V_{1/2} contributes. This identifies V_{1/2} as the live matter channel and structurally explains why (Open Question 1).

    4. Register the t->0 limit, the leading-power read, the det_2 scaling, and the V_1-inertness gates as _report() in main(). Write the CALC-04 portion of derivations/72-matter-sourcing.tex. Surface ref-peirce-coupling (expansion strategy), ref-totaro.
  </action>
  <verify>
    1. Delta R(t) -> 0 as t -> 0 exact per order over Q (M->0 recovers the matterless geometry).
    2. Leading power k >= 1 in ||M|| read EMPIRICALLY from >= 2 exact-per-order coefficients (not assumed); coefficient exact rational.
    3. det_2 scaling reported across >= 2 det_2 leaves at fixed matter direction; rho_J coincidence noted (det_2 the genuine modulus).
    4. V_1-only Delta R = 0 exactly (inert; structurally explained: alpha absent from the triple); V_{1/2} contributes -- V_{1/2} is the live channel.
    5. Order-of-magnitude/leading-power sanity: the scaling is a genuine power law in ||M|| (k>=1), vanishing at t=0; consistent with the cross-term being bilinear-in-matter coupled to the fixed x1.
    6. derivations/72-matter-sourcing.tex CALC-04 portion written; ref-peirce-coupling, ref-totaro surfaced. Engine ALL_PASS exit 0.
  </verify>
  <done>Delta R(t) scaling law computed exact over Q with the leading power in ||M|| read empirically (>=1) and Delta R->0 as t->0; det_2 scaling reported across >=2 leaves (rho_J coincidence noted); V_1-only Delta R=0 (inert, structurally explained via the triple) vs V_{1/2} live -- the channel decomposition done; CALC-04 writeup done; engine ALL_PASS exit 0.</done>
</task>

<task type="checkpoint:decision">
  <name>Task 4: Phase-72 verdict gate -- SURVIVES (Phase 73 greenlit) vs honest negative (STOP)</name>
  <files>derivations/72-matter-sourcing.tex</files>
  <action>
    Assemble the decisive evidence (Tasks 1-3) and present the Phase-72 matter-sourcing verdict for the researcher's confirmation. This is a genuine physics decision point (the backtracking trigger): it determines whether the milestone proceeds to Phase 73 (Einstein structure) or stops with an honest negative. Per the ROADMAP backtracking trigger and the decision criteria in 72-RESEARCH.md.

    Present the exact-over-Q evidence:
    - Cross-term ON/OFF (CALC-03): Delta R_full(M) vs Delta R_off(M); did the off-switch kill (or decisively change) the M-curvature?
    - Lambda-vs-structure: does Delta R_full carry Delta S_munu / Delta Weyl (matter structure) beyond a pure-Lambda shift?
    - ||M||->0 (CALC-04): does Delta R(t) -> 0 as t -> 0?
    - V_1/V_{1/2} channel: V_1 inert, V_{1/2} live (structural explanation).

    Verdict logic:
    - SURVIVES (Phase 73 greenlit): Delta R_full != 0 VANISHES (or changes decisively) under the off-switch AND Delta R -> 0 as ||M|| -> 0 AND Delta R carries traceless-Ricci/Weyl structure (not pure-Lambda).
    - HONEST NEGATIVE (STOP): Delta R SURVIVES the off-switch, OR Delta R is pure-Lambda (Delta S_munu = 0 and Delta Weyl = 0), OR Delta R does not vanish as ||M||->0. Report "position-dependent but pure-Lambda / not matter-sourced" and do NOT proceed to Phase 73 (NEGATIVE-RESULT-IS-SUCCESS; fp-lambda-as-sourcing rejected, not softened).

    Record the verdict in derivations/72-matter-sourcing.tex with the supporting exact-over-Q values. Either verdict is a valid, publishable outcome -- do NOT bias toward SURVIVES.
  </action>
  <verify>
    Evidence assembled is exact over Q (no float on the verdict); the three forbidden proxies (fp-lambda-as-sourcing, fp-ensemble-gravity, fp-float-decisive) are explicitly addressed; the verdict follows the decision logic without softening a negative or forcing a positive; the engine is ALL_PASS exit 0.
  </verify>
  <done>The Phase-72 matter-sourcing verdict (SURVIVES => Phase 73 greenlit, OR honest negative => STOP) is presented with exact-over-Q supporting evidence and confirmed by the researcher. The verdict is recorded in derivations/72-matter-sourcing.tex.</done>
</task>

</tasks>

<verification>
- EXACT over Q on every decisive value (Delta R_full, Delta R_off, the decomposition, the series coefficients, the channel decomposition). No float on any verdict (fp-float-decisive). Ranks/zeros via sympy.
- det_3_block reduces to det(V_0) at M=0 exactly over Q (the off-switch is implemented correctly).
- Non-vacuity (Pitfall 3): 2*Re((x2 x1) x3) != 0 at the chosen M (all three octonion slots, e_4..e_7) -- re-asserted before the on/off verdict.
- Reality: Delta R_full, Delta R_off, the Delta-decomposition all real over Q (a nonzero imaginary part flags a cross-term-association or Wick-contamination bug).
- Riemann algebraic symmetries: riemann_symmetry_ok(R_off,4) = True (and R_full from 72-01).
- Decisive on/off (CALC-03): SURVIVES iff Delta R_full != 0 vanishes (or changes decisively) under the off-switch AND carries non-Lambda structure; else honest negative. Read exact over Q.
- Limiting case (CALC-04): Delta R(t) -> 0 as t -> 0 (M->0 recovers the matterless geometry); leading power k >= 1 read empirically.
- det_2 scaling reported (rho_J coincidence noted); V_1-only Delta R = 0 (inert, structurally explained), V_{1/2} live.
- Isolation discipline (Pitfall 1 / fp-lambda-as-sourcing): Delta from a FIXED V_0-background; a Delta R that survives the off-switch or is pure-Lambda => honest negative, NOT a softened positive.
- Engine regression: python3 code/bulk_geometry_verification.py => OVERALL: ALL_PASS, exit 0.
- Error budget: exact over Q => 0 numerical error. Dominant risk = the >200s symbolic-matter inverse cliff + ~150s watchdog; mitigations: matter rational before inv (smoke-tested), foreground python -u, progress prints between the four curvature builds.
- Consistency: the on/off and series paths agree on the V_{1/2} contribution where they overlap; Delta R_full from 72-01's delta_R_at_fixed_bg matches the recomputation here.
</verification>

<success_criteria>
The decisive cross-term ON/OFF test (CALC-03) executed exact over Q: Delta R_full(M) computed with the full cubic norm vs Delta R_off(M) with det_3_block (cross-term off) at the same M != 0 and fixed V_0-background, all three octonion slots populated (non-vacuity gate passed), det_3_block verified to reduce to det(V_0) at M=0. Delta R_full decomposed into Lambda/traceless-Ricci/Weyl (matter structure vs a pure-Lambda shift). The ||M|| leading-power read empirically with Delta R(t)->0 as t->0, the det_2 scaling reported (rho_J coincidence noted), and the V_1-inert / V_{1/2}-live channel decomposition done with a structural explanation (CALC-04). The Phase-72 verdict -- matter SOURCES the curvature (SURVIVES, Phase 73 greenlit) iff Delta R_full vanishes under the off-switch AND -> 0 as ||M||->0 AND carries non-Lambda structure; else honest negative "position-dependent but not matter-sourced" (STOP) -- is presented exact over Q and confirmed. fp-lambda-as-sourcing, fp-ensemble-gravity, fp-float-decisive all rejected. derivations/72-matter-sourcing.tex completed (CALC-03 + CALC-04 + verdict); ref-prompt, ref-faraut-koranyi, ref-peirce-coupling, ref-totaro surfaced. Engine main() ALL_PASS exit 0.
</success_criteria>

<output>
After completion, create `.gpd/phases/72-b-matter-sourcing/72-02-SUMMARY.md`.
Record the Phase-72 verdict (SURVIVES => Phase 73 greenlit, OR honest negative => STOP), the decisive Delta R_full vs Delta R_off values, the Lambda/structure decomposition, the ||M|| leading power, the det_2 scaling, and the V_1/V_{1/2} channel result -- all exact over Q. If SURVIVES, note the inputs Phase 73 (Einstein structure G_munu = kappa T_munu + Lambda g_munu) will need (kappa, Lambda fitted globally there).
</output>
