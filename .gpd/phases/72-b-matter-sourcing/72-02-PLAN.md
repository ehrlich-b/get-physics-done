---
phase: 72-b-matter-sourcing
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified: [code/bulk_geometry_verification.py, derivations/72-matter-sourcing.tex]
interactive: true

conventions:
  units: "natural (hbar=c=k_B=1); dimensionless differential geometry; EXACT over Q on all decisive verdicts"
  metric: "mostly-minus (-,+,+,+) Lorentzian on the slice; eta = pullback of diag(+1,-1,-1,-1) via the 52-kkt frame; timelike x_0 = beta+gamma"
  coordinates: "engine slice coords (beta,gamma,p,q) = engine-native indices {1,2,3,10} of h_2(C_u)"
  spacetime_metric: "g_munu(x) = eta_munu + h_munu(x;M); M=0 => g=eta (R=0, DERIVED from KKT det_2, NOT inserted)"
  source_structure: "cone-Hessian g_X = Hess(-log det_3) is the matter SOURCE; matter enters ONLY via the det_3 cross-term 2*Re((x2*x1)*x3)"
  det_norm: "Freudenthal det_3, cross-term 2*Re((x2*x1)*x3); SSOT = code/bulk_geometry_verification.py det_3; octonion_algebra.py BANNED"
  cross_term_offswitch: "det_block(X) = a*b*g - a*|x1|^2 - b*|x2|^2 - g*|x3|^2 (the 2*Re((x2 x1)x3) triple DROPPED -- the unique V_0<->V_{1/2} channel); reduces to det(V_0) when matter (x2,x3,alpha)=0"
  basepoint_modulus: "det_2 (Stab_{V_0}=Spin(9,1) invariant) is the genuine modulus; rho_J coincides for single-direction perturbations"

dimensional_check:
  R_g_full_vs_off: "R[g_full](M) vs R[g_off](M) both scalars exact over Q; decisive: full != 0 (curved), off == 0 (flat) or decisively less"
  curvature_scaling: "curvature(t) for M=t*M0 is an exact rational per order in t; curvature(0)=0 (flat); leading power k>=1 read EMPIRICALLY (not assumed O(t^2))"
  h1_linear: "h^{(1)}_munu = d/dt h(x; t*M0)|_{t=0}, symmetric (0,2); emitted for the Phase-73 linearized-Einstein test"

approximations:
  - name: "matter-amplitude series (small t = ||M||) -- exact per order"
    parameter: "t = ||M|| (deviation from the flat eta vacuum); M = t*M0, M0 a fixed V_{1/2}+V_0-partner direction"
    validity: "the -log det jet in the matter directions TERMINATES (finite rational jet, det cubic) -> exact per order; the smallness is ||M||, NEVER the spacetime coord x (Phase-71 lesson)"
    breaks_when: "expanding in x instead of ||M|| (wrong variable); or ||M|| large enough to flip g's signature off (1,3)"
    check: "curvature(t=0)=0 exact (flat); each Taylor coefficient an exact rational; g keeps signature (1,3) at each finite t used"
  - name: "matter-rational-before-inverse computational guard"
    parameter: "matter substituted to small rationals BEFORE Matrix.inv() of g=eta+h"
    validity: "~3s inverse, ~19s full curvature, ~40s for the ON vs OFF pair; keep ONLY the 4 slice coords symbolic"
    breaks_when: "matter + coords ALL symbolic through .inv() => >200s cliff, trips the ~150s executor stream-watchdog"
    check: ".inv() returns <60s; foreground python3 -u with progress prints between heavy steps"

contract:
  schema_version: 1
  scope:
    question: "DECISIVE: is the M!=0 curvature of g=eta+h SOURCED by the det_3 cross-term 2Re((x2 x1)x3)? I.e. does switching the cross-term OFF (block-diagonal norm) at the SAME M!=0 remove the M-curvature of g, AND does the curvature vanish as ||M||->0 (recovering flat eta), AND does the M!=0 curvature carry genuine traceless-Ricci/Weyl structure? The conjunction is the matter-sourcing verdict; any failure (survives off-switch / doesn't vanish as ||M||->0 / pure coordinate artifact) is the honest negative result -> HALT before Phase 73 (negative-result-is-success)."
  claims:
    - id: claim-matter-sourcing
      statement: "Conditional on Phase A (71) showing genuine x-dependence: the slice curvature of g=eta+h is SOURCED by matter M in V_1+V_{1/2} via the cubic-norm cross-terms -- M=0 gives flat eta (R=0, DERIVED), and switching off the cross-terms removes the M-sourced curvature; the curvature scale relates to ||M|| and rho_J(X_bg)/det_2. THIS PLAN delivers the DECISIVE cross-term ON/OFF off-switch (test-cross-term-onoff), the ||M||->0 flat limit + det_2 scaling, emits the linearized-in-M h^{(1)} for Phase 73, and pronounces the matter-sourcing verdict (SURVIVES => Phase 73 greenlit; OR honest negative => HALT)."
      deliverables: [deliv-phaseB]
      acceptance_tests: [test-cross-term-onoff, test-lambda-vs-matter]
      references: [ref-70.1-verdict, ref-prompt, ref-totaro, ref-faraut-koranyi, ref-warm-engine, ref-h3o-tower]
  deliverables:
    - id: deliv-phaseB
      kind: derivation
      path: derivations/72-matter-sourcing.tex
      description: "Phase B (matter-on-flat), Plan 02: the DECISIVE cross-term ON/OFF test (full det_3 vs block-diagonal det_block at the SAME M!=0) on the curvature of g=eta+h; the ||M||->0 flat-eta limiting case and the curvature scaling vs ||M|| and det_2 (series in amplitude t, exact per order); the emitted linearized-in-M perturbation h^{(1)} for the Phase-73 linearized-Einstein test; and the matter-sourcing VERDICT with the explicit negative-result-is-success backtracking gate."
      must_contain:
        - "cross-term ON/OFF: R[g_full](M)!=0 vs R[g_off](M) (det_block = det_3 with 2Re((x2 x1)x3) dropped) at the SAME M!=0, all three slots populated (non-vacuous triple); decisive if g_off is flat (or decisively less curved)"
        - "||M||->0 limiting case: curvature of g=eta+h -> 0 (recovering flat eta) as ||M||->0, exact per order in t (the DECISIVE limit -- NOT a Lambda baseline subtraction)"
        - "curvature scaling vs ||M|| (leading power read EMPIRICALLY) and vs det_2 (the genuine modulus; rho_J coincidence NOTED)"
        - "emitted h^{(1)}_munu (linearized-in-M) and the linearized curvature R^{(1)} for the Phase-73 box h_bar ~ kappa T test"
        - "the matter-sourcing VERDICT: SURVIVES (off-switch kills the M-curvature AND ||M||->0 recovers flat eta AND traceless-Ricci/Weyl present) => Phase 73 greenlit; OR honest NEGATIVE (survives off-switch / doesn't vanish as ||M||->0 / pure coordinate artifact) => 'position-dependent source but NOT matter-sourced spacetime curvature', HALT before Phase 73"
        - "explicit statement that the negative result is a SUCCESS (negative-result-is-success), NOT relabeled as survival (fp-relabel), and that a pure-Lambda/matterless piece is NOT sourcing (fp-lambda-as-sourcing)"
  references:
    - id: ref-70.1-verdict
      kind: prior_artifact
      locator: ".gpd/phases/70.1-revise-a0-select-the-physical-spacetime-metric-cone-hessian-vs-eta-h-bridge/70.1-01-SUMMARY.md -- human-ratified verdict: g=eta+h is the physical metric; M=0 flat DERIVED from KKT; Phase 73 = linearized-Einstein can-fail test; NO Lambda tripwire"
      aliases: ["70.1 verdict", "70.1-01-SUMMARY"]
      role: must_consider
      why_it_matters: "AUTHORITATIVE re-scope; fixes the metric, the flat baseline, and that Phase 72 establishes only matter-SOURCING (not Einstein structure)"
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [execution, verification]
      must_surface: true
      required_actions: [read]
    - id: ref-prompt
      kind: spec
      locator: "~/scratch/get-physics-done/paper6-bulk-geometry-prompt.md -- Phase B targets (cross-term off-switch removes M-curvature; scale vs ||M||; ||M||->0 flat); negative-result-is-success discipline is BINDING"
      aliases: ["bulk-geometry prompt", "paper6-bulk-geometry-prompt"]
      role: must_consider
      why_it_matters: "Defines the decisive Phase B controls and the 'do not soften / curved-but-not-Einstein is acceptable' reporting discipline"
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [execution, verification]
      must_surface: true
      required_actions: [read]
    - id: ref-totaro
      kind: paper
      locator: "Totaro, arXiv:math/0401381, Cor 2.3 -- Hessian-curvature closed form; applied to g=eta+h (validated against hand-rolled Riemann in Plan 72-01)"
      aliases: ["Totaro 2004", "TOT04"]
      role: method
      why_it_matters: "The curvature engine reused for the ON/OFF and scaling computations"
      applies_to: [claim-matter-sourcing, deliv-phaseB]
      carry_forward_to: [execution]
      must_surface: true
      required_actions: [cite]
    - id: ref-faraut-koranyi
      kind: paper
      locator: "Faraut & Koranyi, Analysis on Symmetric Cones (1994) -- cone metric and det_2 modulus; grounds the scaling-vs-det_2 reading"
      aliases: ["Faraut-Koranyi", "FK94"]
      role: definition
      why_it_matters: "Grounds the SOURCE structure and the det_2 basepoint modulus for CALC-04"
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [execution]
      must_surface: false
      required_actions: [cite]
    - id: ref-warm-engine
      kind: prior_artifact
      locator: "code/bulk_geometry_verification.py -- warm engine, EXTENDED by Plan 72-01 with spacetime_curvature_of_g + hand-rolled cross-check + ricci_decomposition_n4; add the det_block off-switch + the amplitude-series scaling here"
      aliases: ["warm engine", "bulk_geometry_verification.py"]
      role: benchmark
      why_it_matters: "The decisive computation surface; the curvature-of-g routine from 72-01 is reused for the ON/OFF and scaling"
      applies_to: [claim-matter-sourcing, deliv-phaseB]
      carry_forward_to: [execution, verification]
      must_surface: true
      required_actions: [use]
    - id: ref-h3o-tower
      kind: prior_artifact
      locator: "~/repos/blog/research/qualia-fixed-point/h3o_tower.py -- corrected cubic-norm benchmark; provenance cross-check for det_3 / det_block"
      aliases: ["h3o_tower.py"]
      role: benchmark
      why_it_matters: "Confirms the det_3 cross-term association underlying the off-switch"
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [execution]
      must_surface: false
      required_actions: [use]
  acceptance_tests:
    - id: test-cross-term-onoff
      subject: claim-matter-sourcing
      kind: consistency
      procedure: "Compute the curvature of g=eta+h with the FULL det_3 vs with det replaced by the block-diagonal det_block (the 2*Re((x2 x1)x3) triple set to 0), at the SAME M!=0 with all three octonion slots populated (non-vacuous). Build Phi_full=-log det_3 and Phi_off=-log det_block, h_full/h_off, g_full=eta+h_full, g_off=eta+h_off, and compare their curvatures."
      pass_condition: "The M-sourced curvature present with the full norm VANISHES (or changes decisively) when the V_0<->V_{1/2} cross-term is switched off: R[g_full](M)!=0 but R[g_off](M)=0 (g_off flat) or decisively less curved. Exact over Q. (If the off-switch leaves the curvature UNCHANGED => NOT cross-term-sourced => honest negative.)"
      evidence_required: [deliv-phaseB]
      automation: automated
    - id: test-lambda-vs-matter
      subject: claim-matter-sourcing
      kind: limiting_case
      procedure: "Confirm the curvature of g=eta+h vanishes as ||M||->0 (g->eta, R[g]->0, recovering flat eta), via the amplitude series M=t*M0 with curvature(t=0)=0; distinguish this from genuine M-sourced curvature. RE-FRAMED per 70.1: M=0 is flat eta DERIVED from KKT, NOT a pure-Lambda baseline; only the M-dependent part counts."
      pass_condition: "curvature(||M||->0) -> 0 (flat eta, DERIVED) and is NOT counted as sourcing; only the M-dependent part counts. NO Lambda tripwire, NO R=-3 / pure-Lambda baseline subtraction."
      evidence_required: [deliv-phaseB]
      automation: automated
  forbidden_proxies:
    - id: fp-lambda-as-sourcing
      subject: claim-matter-sourcing
      proxy: "Counting a pure cosmological-constant / matterless / M=0 piece as matter-sourcing, OR subtracting an R=-3 / 'center is Einstein' / Cartan / R_time x H^3 baseline (the FALSIFIED pre-70.1 cone-Hessian framing)"
      reason: "Per Phase 70.1 the M=0 spacetime baseline is FLAT eta (R=0), DERIVED from KKT. Only the M-dependent curvature sourced by the V_0<->V_{1/2} cross-terms counts; a constant background or the matterless cone-Hessian R is NOT matter."
    - id: fp-relabel
      subject: claim-matter-sourcing
      proxy: "Relabeling a flat/unchanged M!=0 result (curvature survives the off-switch, or does not vanish as ||M||->0, or g stays flat with matter on) as 'survival' / Phase-73-greenlit to avoid the honest negative"
      reason: "A flat-under-matter or off-switch-insensitive result is the honest NEGATIVE (negative-result-is-success) and must be reported as such and HALT before Phase 73; softening it fakes survival of an unsourced route."
    - id: fp-ensemble-gravity
      subject: claim-matter-sourcing
      proxy: "Any observers-make-gravity ensemble / thermodynamic (Jacobson-style) argument to explain the sourcing"
      reason: "Explicitly rejected; the curvature must come from the algebra's own cubic-norm geometry (the det_3 cross-term), one observer, one off-center point -- not an ensemble."
    - id: fp-coordinate-curvature
      subject: claim-matter-sourcing
      proxy: "Reading a coordinate/embedding artifact (pure-trace, removable, no traceless-Ricci/Weyl) as matter-sourced spacetime curvature, or computing curvature with H_bg^{-1} instead of (eta+h)^{-1}"
      reason: "Only curvature-scalar invariants of g=eta+h decide; the M!=0 'curvature' must carry genuine traceless-Ricci/Weyl structure (not removable by a coordinate change) to count as gravity."
    - id: fp-float-decisive
      subject: claim-matter-sourcing
      proxy: "Computing the cross-term ON/OFF verdict or the ||M||->0 limit in float (numpy float rank / float curvature)"
      reason: "The off-switch and limit are derivative-cancellation-sensitive; float tolerance fabricates the verdict. The decisive ON/OFF and limit are EXACT over Q."
    - id: fp-assume-einstein
      subject: claim-matter-sourcing
      proxy: "Inserting a factor to force Ric proportional to g, or testing/fitting an Einstein form in Phase 72"
      reason: "The Einstein test is Phase 73 (linearized-Einstein, can-fail). Phase 72 emits h^{(1)} for that test but establishes only matter-SOURCING here; no Einstein-fitting."
  links:
    - id: link-matter-sourcing-decisive
      source: claim-matter-sourcing
      target: deliv-phaseB
      relation: supports
      verified_by: [test-cross-term-onoff, test-lambda-vs-matter]
  uncertainty_markers:
    weakest_anchors:
      - "The matter-sourcing OUTCOME is NOVEL (no external literature); the SURVIVES/negative verdict rests on INTERNAL exact-over-Q controls (cross-term off-switch, ||M||->0 flat limit, traceless-Ricci/Weyl structure, two-route curvature cross-check from 72-01). Rigor is 'exact on representative M', NOT a general theorem for all M (that is Phase C / future)."
      - "The off-switch det_block removes the TRIPLE coupling but not the V_{1/2} self-norm terms |x2|^2,|x3|^2 (which still multiply a,b,g); det_block is the prompt's named V_0<->V_{1/2} channel, stated explicitly as schematic 'det(V_1)*det(V_0)'."
    disconfirming_observations:
      - "The cross-term off-switch leaves the M-curvature of g UNCHANGED => the curvature is NOT sourced via the V_0<->V_{1/2} cross-terms => honest negative, HALT (fp-lambda-as-sourcing avoided)."
      - "The curvature of g does NOT vanish as ||M||->0 (does not recover flat eta) => not matter-sourced (something other than M curves g) => honest negative, HALT."
      - "The M!=0 'curvature' is a pure coordinate/embedding artifact (no traceless-Ricci/Weyl, removable) => not intrinsic gravity => honest negative, HALT (fp-coordinate-curvature)."
      - "g=eta+h(M) stays flat (R=0) with matter on => the honest negative result; report it and do NOT relabel as survival (fp-relabel)."
---

<objective>
Run the DECISIVE matter-sourcing controls on the validated curvature-of-g engine from Plan 72-01, and pronounce the Phase-72 verdict.

The decisive question (matter-on-flat): does turning on matter M in V_1+V_{1/2} produce a NONZERO curvature of g=eta+h through the det_3 cross-term 2Re((x2 x1)x3), and does switching the cross-term OFF (block-diagonal norm) remove it, with the curvature vanishing as ||M||->0 (recovering flat eta) and carrying genuine traceless-Ricci/Weyl structure? The conjunction is the matter-sourcing verdict; any failure is the honest negative result (negative-result-is-success) -> HALT before Phase 73.

Purpose: delivers test-cross-term-onoff (the DECISIVE gate), the ||M||->0 flat limit + det_2 scaling, the emitted linearized-in-M h^{(1)} for the Phase-73 linearized-Einstein test, and the phase-boundary verdict (greenlight Phase 73 or HALT). Establishes (CALC-03) the cross-term off-switch and (CALC-04) the scaling + flat limit.
Output: an extended code/bulk_geometry_verification.py (det_block off-switch + amplitude-series scaling), the verdict section of derivations/72-matter-sourcing.tex, and the matter-sourcing verdict at a human checkpoint.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/CONVENTIONS.md
@.gpd/phases/70.1-revise-a0-select-the-physical-spacetime-metric-cone-hessian-vs-eta-h-bridge/70.1-01-SUMMARY.md
@.gpd/phases/72-b-matter-sourcing/72-RESEARCH.md
@.gpd/phases/72-b-matter-sourcing/72-01-SUMMARY.md
@code/bulk_geometry_verification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: CALC-03 -- the DECISIVE cross-term ON/OFF off-switch on the curvature of g=eta+h</name>
  <files>code/bulk_geometry_verification.py</files>
  <action>
    Implement the block-diagonal off-switch and run the decisive comparison. Reuse spacetime_curvature_of_g from Plan 72-01 (indices raised with g=eta+h). Foreground `python3 -u` with progress prints between heavy steps.

    1. Add `det_3_block(X)` (or a `cross_off=True` flag on det_3) = a*b*g - a*|x1|^2 - b*|x2|^2 - g*|x3|^2 (the `2*Re((x2*x1)*x3)` triple DROPPED -- the unique V_0<->V_{1/2} octonion channel). VERIFY it reduces to det(V_0) when matter (x2,x3,alpha)=0, exactly over Q. Document that the prompt's "det(V_1)*det(V_0)" product is schematic; the operational definition is "det_3 with the triple set to 0" (the |x2|^2,|x3|^2 self-norms remain; only the triple coupling is removed -- state this explicitly, research Open Q4).
    2. NON-VACUITY gate (Pitfall 3): use the Task-1(72-01) known-good representative M with all three slots populated and e_4..e_7 content (cross-term -13/315 != 0). Assert `2*Re((x2 x1)x3) != 0` at the chosen M BEFORE running curvature (else the ON/OFF test is hollow / trivially equal).
    3. Build Phi_full = -log det_3 and Phi_off = -log det_3_block at the SAME M!=0 (matter rational, slice symbolic-then-evaluated). Build h_full, h_off, g_full=eta+h_full, g_off=eta+h_off. Compute the curvature of each via spacetime_curvature_of_g (Totaro C from each potential, indices raised with the respective g=eta+h): R[g_full], R[g_off], and their n=4 decompositions (S, Weyl from ricci_decomposition_n4).
    4. DECISIVE comparison, exact over Q: report R[g_full](M) (expect !=0, curved) vs R[g_off](M) (decisive if ==0 flat, or decisively less curved). If R[g_off](M)==R[g_full](M) (off-switch makes no difference) that is the honest NEGATIVE (not cross-term-sourced). Also confirm g_off keeps a valid signature over Q.

    Watchdog: the ON vs OFF pair is ~40s total; print after each potential build, each .inv(), each curvature.
  </action>
  <verify>
    1. det_3_block reduces to det(V_0) when matter=0 exactly over Q; the off-switch drops ONLY the 2Re((x2 x1)x3) triple (documented as the schematic V_0<->V_{1/2} channel; self-norms remain).
    2. Non-vacuity: 2Re((x2 x1)x3) != 0 at the chosen M (all three slots, e_4 content) -- the ON/OFF test is NOT hollow.
    3. Decisive ON/OFF exact over Q: R[g_full](M) vs R[g_off](M) reported; if full is curved and off is flat (or decisively less) => cross-term-sourced; if equal => honest negative (fp-lambda-as-sourcing avoided).
    4. Index/signature discipline maintained (curvature raised with g=eta+h, NOT H_bg^{-1}; g_off signature checked over Q); no float on the verdict (fp-float-decisive).
  </verify>
  <done>The DECISIVE cross-term ON/OFF off-switch implemented and run exactly over Q on a non-vacuous representative M: R[g_full](M) vs R[g_off](M) reported, establishing whether the M-curvature is cross-term-sourced; no forbidden proxy (fp-float-decisive, fp-coordinate-curvature, fp-lambda-as-sourcing) triggered.</done>
</task>

<task type="auto">
  <name>Task 2: CALC-04 -- ||M||->0 flat limit + curvature scaling vs ||M|| and det_2; emit h^{(1)} for Phase 73</name>
  <files>code/bulk_geometry_verification.py, derivations/72-matter-sourcing.tex</files>
  <action>
    Establish the limiting case and the scaling law, and emit the linearized-in-M data for Phase 73. Foreground `python3 -u`.

    1. AMPLITUDE SERIES: pick a single matter direction M0 (one V_{1/2} component with octonionic e_4..e_7 content + a V_0 x1 partner via the slice p,q so the triple is non-vacuous). Set M = t*M0 and expand h(t) and the curvature of g=eta+h(t) as a series in t to low order (exact per order -- the -log det jet in the matter directions TERMINATES; reuse the rho_directional_derivatives.py expansion STRATEGY only, recompute the octonionic cross-term from the engine det_3). Confirm curvature(t=0)=0 EXACTLY (flat eta -- the DECISIVE limiting case, recovering the KKT vacuum; NOT a Lambda baseline subtraction). [test-lambda-vs-matter]
    2. LEADING POWER (empirical): read the leading nonzero power k of curvature(t) in t EMPIRICALLY from the series (do NOT assume O(t^2)); report k and the leading exact-rational coefficient.
    3. SCALING vs det_2: at a fixed matter direction, report the curvature scale as a function of det_2 (the genuine basepoint modulus, Stab_{V_0}=Spin(9,1) invariant) and NOTE that rho_J coincides with det_2 for single-direction perturbations. Use rho_J_squared(delta) from the engine for the rho_J value.
    4. EMIT h^{(1)} FOR PHASE 73: compute and record h^{(1)}_munu = d/dt h(x; t*M0)|_{t=0} (the linearized-in-M perturbation) and the linearized curvature R^{(1)}. State that Phase 73 will build h_bar = h^{(1)} - (1/2) eta tr_eta(h^{(1)}) and apply box h_bar ~ kappa T (the linearized-Einstein can-fail test). Phase 72 does NOT do the Einstein test -- only emit the data (fp-assume-einstein guard).
    5. Confirm g=eta+h(t) keeps signature (1,3) at each finite t used (eigenvalue-sign over Q; small-||M|| regime, 70.1 splice-consistency).

    Update derivations/72-matter-sourcing.tex with the ON/OFF result (Task 1), the ||M||->0 limit, the scaling law, and the emitted h^{(1)} (the Phase-73 handoff). Exact over Q throughout.
  </action>
  <verify>
    1. curvature(t=0)=0 exactly over Q (flat eta recovered as ||M||->0 -- the DECISIVE limit; explicitly NOT a Lambda baseline). [test-lambda-vs-matter]
    2. Leading power k>=1 read empirically (not assumed O(t^2)); leading coefficient an exact rational; each series coefficient exact over Q.
    3. Scaling reported vs det_2 (genuine modulus) with the rho_J coincidence noted.
    4. h^{(1)}_munu and R^{(1)} emitted (symmetric (0,2)); the Phase-73 handoff (box h_bar ~ kappa T) stated; no Einstein-fitting done here (fp-assume-einstein).
    5. g keeps signature (1,3) at each finite t over Q.
  </verify>
  <done>The ||M||->0 flat limit (curvature->0, exact) confirmed as the decisive limiting case; the curvature scaling vs ||M|| (empirical leading power) and det_2 reported exactly over Q; the linearized-in-M h^{(1)} emitted for the Phase-73 linearized-Einstein test; .tex updated; no forbidden proxy (fp-lambda-as-sourcing, fp-assume-einstein, fp-float-decisive) triggered.</done>
</task>

<task type="checkpoint:decision">
  <name>Task 3: Matter-sourcing VERDICT -- greenlight Phase 73 or HALT (negative-result-is-success)</name>
  <files>derivations/72-matter-sourcing.tex</files>
  <action>
    Assemble the Phase-72 matter-sourcing verdict from the decisive evidence (Tasks 1-2 of this plan + the curvature-of-g + decomposition from Plan 72-01) and present it for the researcher's ratification. This is a phase-boundary decision: the verdict greenlights Phase 73 (the linearized-Einstein can-fail test) or HALTS the route.

    Write the verdict into derivations/72-matter-sourcing.tex as the closing section, stating the level reached and the decisive evidence, then surface it to the researcher.

    The VERDICT is the CONJUNCTION (all three required for SURVIVES):
      (i)  cross-term OFF kills (or decisively reduces) the M-curvature of g  [test-cross-term-onoff, Task 1];
      (ii) curvature of g -> 0 as ||M||->0, recovering flat eta              [test-lambda-vs-matter, Task 2];
      (iii) the M!=0 curvature carries genuine traceless-Ricci/Weyl structure (not a pure coordinate artifact)  [72-01 decomposition].

    - If ALL THREE hold: VERDICT = SURVIVES (matter cross-term-sources the spacetime curvature). Phase 73 greenlit; the emitted h^{(1)} feeds the linearized-Einstein test box h_bar ~ kappa T.
    - If ANY fails (curvature survives the off-switch, OR does not vanish as ||M||->0, OR is a pure coordinate/embedding artifact, OR g stays flat with matter on): VERDICT = honest NEGATIVE = "position-dependent SOURCE but NOT matter-sourced spacetime curvature". Report it as a SUCCESS (negative-result-is-success), do NOT relabel as survival (fp-relabel), and HALT before Phase 73.

    Present to the researcher: the headline numbers (R[g_full](M), R[g_off](M), the ||M||->0 boundary value, the leading power, the S/Weyl structure), the three-part conjunction result, and the recommended action (greenlight Phase 73 OR halt). Do NOT force a positive verdict; "curved-but-not-Einstein" is acceptable downstream, and a clean negative here is a valuable result.
  </action>
  <verify>
    Assemble before asking: all three conjunction inputs are computed exact over Q (ON/OFF from Task 1; ||M||->0 from Task 2; S/Weyl structure from 72-01); the headline numbers are exact rationals; no float on any decisive quantity (fp-float-decisive); the negative-result-is-success framing is explicit (fp-relabel guarded); the verdict statement matches the computed evidence (no overclaim).
  </verify>
  <done>The matter-sourcing verdict (SURVIVES => Phase 73 greenlit, OR honest NEGATIVE => HALT) is assembled from the exact-over-Q decisive evidence, written into derivations/72-matter-sourcing.tex, and ratified by the researcher; the negative-result-is-success discipline is honored (no fp-relabel, no fp-lambda-as-sourcing, no fp-assume-einstein).</done>
  <resume-signal>Confirm the verdict (e.g. "SURVIVES, greenlight Phase 73" or "NEGATIVE, halt") or describe corrections.</resume-signal>
</task>

</tasks>

<verification>
- The DECISIVE cross-term ON/OFF (full det_3 vs det_block at the SAME non-vacuous M!=0) computed on the curvature of g=eta+h, exact over Q; the off-switch result decides cross-term sourcing.
- The ||M||->0 flat limit (curvature->0, recovering flat eta) confirmed exactly per order -- the decisive limiting case, NOT a Lambda baseline subtraction.
- Curvature scaling vs ||M|| (empirical leading power) and det_2 (genuine modulus, rho_J coincidence noted) reported exactly over Q.
- The linearized-in-M h^{(1)} emitted for the Phase-73 linearized-Einstein test (the explicit handoff); no Einstein-fitting in Phase 72.
- The matter-sourcing VERDICT is the three-part conjunction; the negative-result-is-success backtracking gate is explicit (HALT before Phase 73 on any failure; do NOT relabel).
- All decisive quantities EXACT over Q; det_3 SSOT used; octonion_algebra.py banned; indices raised with g=eta+h (not the bare cone-Hessian); g signature (1,3) maintained.
</verification>

<success_criteria>
The cross-term ON/OFF off-switch (test-cross-term-onoff) and the ||M||->0 flat limit (test-lambda-vs-matter) are computed exactly over Q on a non-vacuous representative M; the curvature scaling vs ||M|| and det_2 is reported; the linearized-in-M h^{(1)} is emitted for Phase 73; and the matter-sourcing verdict (SURVIVES => Phase 73 greenlit, OR honest NEGATIVE => HALT, negative-result-is-success) is assembled from the decisive evidence and ratified by the researcher. No forbidden proxy (fp-lambda-as-sourcing, fp-relabel, fp-ensemble-gravity, fp-coordinate-curvature, fp-float-decisive, fp-assume-einstein) is triggered; NO Lambda tripwire / NO pure-Lambda baseline anywhere.
</success_criteria>

<output>
After completion, create `.gpd/phases/72-b-matter-sourcing/72-02-SUMMARY.md`.
</output>
