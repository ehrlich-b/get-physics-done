---
phase: 72-b-matter-sourcing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [code/bulk_geometry_verification.py, derivations/72-matter-sourcing.tex]
interactive: false

conventions:
  units: "natural (hbar=c=k_B=1); EXACT rational arithmetic over Q on all decisive verdicts (NO float)"
  metric_slice: "mostly-minus (-,+,+,+) Lorentzian on the dim-4 h_2(C_u) sub-slice via det_2"
  metric_bulk: "Riemannian positive-definite; cone metric g_X = Hess(-log det)"
  potential: "Phi = -log det_3 (FIXED; do NOT mix with bare det)"
  det_SSOT: "engine det_3 (code/bulk_geometry_verification.py), cross-term 2*Re((x2*x1)*x3); octonion_algebra.py BANNED"
  curvature_engine: "Totaro closed form R_ijkl = -(1/4) g^{pq}(C_jlp C_ikq - C_ilp C_jkq); hand-rolled Christoffel/Riemann (NOT sympy.diffgeom)"
  riemann_ricci_sign: "Ric_jl = g^{ik} R_ijkl; R = g^{jl} Ric_jl. Sign pinned NEGATIVE at center: R(center)=-3; H^3 cone-Hessian K_sect=-1/2 (round -1, exact factor 2)"
  coordinates: "spacetime point on dim-4 h_2(C_u); engine slice indices {1,2,3,10}=(beta,gamma,p,q) kept symbolic; background X_bg = I/3 + (fixed V_0 off-center) + M"
  peirce: "under E_11=diag(1,0,0): V_1={0}(alpha), V_0={1..10}=h_2(O), V_{1/2}={11..26}; matter M in V_1+V_{1/2}"
  genuine_modulus: "det_2 (Stab_{V_0}=Spin(9,1) invariant); rho_J coincides for single-direction perturbations"

dimensional_check:
  R_ricci_scalar: "[dimensionless] (geometric, natural units); exact rational over Q"
  Delta_R: "[dimensionless]; exact rational over Q; real (imaginary part identically 0)"
  g_munu: "[dimensionless] slice metric; det_g != 0 (nondegenerate at positive-cone basepoints)"
  S_munu_tracelessRicci: "[dimensionless]; g^{munu} S_munu = 0 identically (trace removed)"

approximations:
  - name: "matter rational before symbolic inverse"
    parameter: "matter components substituted to small rationals; only 4 slice coords {beta,gamma,p,q} symbolic"
    validity: "always (this is the controlled exact regime: ~0.5-3s 4x4 rational inverse)"
    breaks_when: "matter carried symbolic through Matrix.inv() => expression swell >200s, trips ~150s watchdog"
    check: "Matrix.inv() returns in <60s; if it hangs, matter is still symbolic -- substitute to rationals"
  - name: "decisive arena = dim-4 h_2(C_u) sub-slice"
    parameter: "n=4 slice {1,2,3,10}; dim-10 V_0 off critical path"
    validity: "Phase-71 established the 4-dim h_2(C_u) is the decisive spacetime sub-slice (non-geodesic off-center)"
    breaks_when: "attempting dim-10 V_0 symbolic inverse (TIMEOUT >200s)"
    check: "stay on the n=4 slice; dim-10 is mpmath-only NON-decisive triage"
  - name: "representative M, not all-M"
    parameter: "decisive verdict on representative rational M (+ ||M||->0 limit in 72-02)"
    validity: "sufficient for the SURVIVES/honest-negative verdict (exact over Q)"
    breaks_when: "claiming a general theorem for all M -- that is Phase C / out of scope"
    check: "report 'decisive on representative M', not 'proved for all M'"

contract:
  schema_version: 1
  scope:
    question: "At a FIXED V_0-background (fixed det_2 leaf), does matter M in V_1+V_{1/2} enter the inherited dim-4 h_2(C_u) slice curvature -- i.e. is Delta R(M)=R(X_bg+M)-R(X_bg) a well-posed, real, exact-over-Q function of M, and is the M=0 baseline pure-Lambda (Einstein, negative Cartan) at the center and the matterless Phase-71 R(det_2) off-center?"
  claims:
    - id: claim-matter-sourcing
      statement: "Conditional on Phase A showing genuine x-dependence (Phase 71: SURVIVES, VERIFIED): the slice curvature is SOURCED by matter M in V_1+V_{1/2} via the cubic-norm cross-terms -- M=0 gives flat or pure-Lambda, and switching off the cross-terms (block-diagonal det) removes the M-sourced curvature; the curvature scale relates to ||M|| and rho_J(X_bg). THIS PLAN (72-01) establishes the matter-aware Riemann/Ricci engine and the M=0 Lambda baseline; the decisive cross-term off-switch and scaling are 72-02."
      deliverables: [deliv-phaseB]
      acceptance_tests: [test-lambda-vs-matter]
      references: [ref-faraut-koranyi, ref-totaro, ref-mccrimmon, ref-peirce-coupling, ref-prompt]
  observables:
    - id: obs-matter-sourcing
      name: "Riemann tensor of the inherited slice metric as a function of M"
      kind: proof_obligation
      definition: "The Riemann tensor of g_mu_nu(x) as a function of M in V_1+V_{1/2}: M=0 gives flat or pure-Lambda; turning on M generates curvature through the det cross-terms coupling V_0 to V_1/V_{1/2}, with the difference isolated by the cross-term on/off test. 72-01 delivers the M-explicit Riemann/Ricci and the M=0 baseline; the on/off isolation is 72-02."
      regime: "linear and beyond in ||M||; off-center det_2; representative rational M on the dim-4 h_2(C_u) slice; exact over Q"
  deliverables:
    - id: deliv-phaseB
      kind: derivation
      path: derivations/72-matter-sourcing.tex
      description: "Matter-sourcing of the inherited slice curvature (engine extension + writeup). 72-01 portion: matter-aware Riemann/Ricci as an explicit function of M, the n=4 Ricci decomposition (scalar/traceless-Ricci/Weyl), and the M=0 baseline (center pure-Lambda Einstein R=-3<0 Cartan; off-center matterless Phase-71 R(det_2))."
      must_contain:
        - "Riemann/Ricci tensor of the inherited slice metric as a function of M"
        - "M=0 baseline (flat or pure Lambda) established"
        - "n=4 Ricci decomposition routine (Ricci scalar, traceless Ricci S_munu, Weyl C_munu)"
        - "Phase-71 matterless R anchors reproduced exactly over Q (regression)"
        - "non-vacuity preflight: 2*Re((x2 x1) x3) != 0 at the chosen M (all three octonion slots populated, e_4..e_7 content)"
    - id: deliv-engine-matter
      kind: code
      path: code/bulk_geometry_verification.py
      description: "Engine extension: matter-aware delta plumbing (V_1={0}, V_{1/2}={11..26}); Delta R(M)=R(X_bg+M)-R(X_bg) at fixed V_0-background via the existing exact rational-inverse path; ricci_decomposition(R,g,ginv,n=4); all registered as _report() gates keeping main() ALL_PASS exit 0."
      must_contain:
        - "ricci_decomposition"
        - "_report"
  references:
    - id: ref-prompt
      kind: spec
      locator: "~/scratch/get-physics-done/paper6-bulk-geometry-prompt.md"
      aliases: ["ref-prompt", "paper6-bulk-geometry-prompt"]
      role: must_consider
      why_it_matters: "Authoritative milestone spec for Phase B targets (M=0 flat/pure-Lambda; cross-term off-switch; scale vs ||M|| and rho_J) and the reporting discipline ('do not soften'; 'curved-but-not-Einstein is acceptable')."
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [planning, execution, verification]
      must_surface: true
      required_actions: [read]
    - id: ref-faraut-koranyi
      kind: paper
      locator: "Faraut & Koranyi, Analysis on Symmetric Cones (1994), Oxford"
      aliases: ["ref-faraut-koranyi", "Faraut-Koranyi"]
      role: definition
      why_it_matters: "Cone metric g_X=Hess(-log det); inverse g^{pq}=P(X); det=1 hypersurface = E_{6(-26)}/F_4 symmetric space. The M=0 center Einstein-negative (Cartan) baseline rests on this -- the load-bearing anchor for VALD-04."
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-totaro
      kind: paper
      locator: "Totaro, The curvature of a Hessian metric, arXiv:math/0401381, Cor 2.3"
      aliases: ["ref-totaro", "Totaro"]
      role: method
      why_it_matters: "The Hessian-curvature closed form R_ijkl = -(1/4) g^{pq}(C_jlp C_ikq - C_ilp C_jkq); det cubic => f_ijkl=0 (no 4th-derivative blowup); rank-1 slice constant curvature -d^2/4. Pins the Riemann sign and is the engine totaro_riemann."
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [cite]
    - id: ref-mccrimmon
      kind: paper
      locator: "McCrimmon, A Taste of Jordan Algebras (2004)"
      aliases: ["ref-mccrimmon", "McCrimmon"]
      role: background
      why_it_matters: "Peirce decomposition under E_11, cubic norm, quadratic representation P(X). Grounds the V_1/V_{1/2}/V_0 split and the cross-term coupling structure used in the index audit."
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [read, cite]
    - id: ref-peirce-coupling
      kind: prior_artifact
      locator: "~/scratch/get-physics-done/peirce_coupling.py (numpy/float, intuition only) + ~/scratch/get-physics-done/rho_directional_derivatives.py (exact SymPy off-center expansion structure)"
      aliases: ["ref-peirce-coupling"]
      role: must_consider
      why_it_matters: "Establishes that x1 (V_0) couples to E_11 ONLY through the cubic trilinear (never bilinear) -- exactly the cross-term mechanism the index audit confirms. NUMPY/float REFERENCE only, NOT the decisive path (which is the exact engine det_3)."
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [use]
    - id: ref-h3o-tower
      kind: prior_artifact
      locator: "~/repos/blog/research/qualia-fixed-point/h3o_tower.py (corrected cubic-norm benchmark)"
      aliases: ["ref-h3o-tower"]
      role: benchmark
      why_it_matters: "Optional provenance cross-check of the engine det_3 cross-term association."
      applies_to: [claim-matter-sourcing]
      carry_forward_to: [verification]
      must_surface: false
      required_actions: [use]
  acceptance_tests:
    - id: test-lambda-vs-matter
      subject: claim-matter-sourcing
      kind: limiting_case
      procedure: "Compute the slice curvature at M=0 at the FIXED V_0-background and confirm: (a) at the center X_bg=I/3, R(center)=-3<0 and the slice is Einstein (R_munu proportional to g_munu, i.e. traceless Ricci S_munu = R_munu-(R/4)g_munu = 0 exactly over Q) = pure cosmological constant Lambda<0 (Cartan); (b) at a fixed off-center det_2 leaf, R reproduces the matterless Phase-71 R(det_2) anchor exactly over Q. Build the n=4 ricci_decomposition (scalar + traceless-Ricci + Weyl) and verify g^{munu} S_munu = 0 identically. Confirm the matter-aware Riemann obeys riemann_symmetry_ok and Delta R(M) is real over Q at a representative non-vacuous M."
      pass_condition: "M=0 center is Einstein with R=-3<0 (S_munu=0 exact over Q) = pure-Lambda; M=0 off-center reproduces the Phase-71 matterless R(det_2) exactly; ricci_decomposition reconstructs R_ijkl = scalar + traceless-Ricci + Weyl parts exactly over Q (residual 0); the M=0/pure-Lambda part is NOT counted as matter-sourcing -- only the M-dependent Delta is. riemann_symmetry_ok = True; Delta R(M) real over Q at a non-vacuous M. Engine main() ALL_PASS exit 0."
      evidence_required: [deliv-phaseB, deliv-engine-matter, ref-faraut-koranyi, ref-totaro]
      automation: automated
  forbidden_proxies:
    - id: fp-lambda-as-sourcing
      subject: claim-matter-sourcing
      proxy: "Counting a pure-Lambda (M=0) curvature -- OR, per Phase 71, the matterless det_2-variation -- as matter-sourcing."
      reason: "Only the M-dependent part sourced by the V_0<->V_1/V_{1/2} cross-terms counts. The M=0 baseline (center pure-Lambda OR off-center matterless R(det_2)) is the reference to subtract, not a result. Always work with Delta R(M)=R(X_bg+M)-R(X_bg) at a FIXED V_0-background."
    - id: fp-ensemble-gravity
      subject: claim-matter-sourcing
      proxy: "Any observers-make-gravity / thermodynamic (Jacobson-style) ensemble argument."
      reason: "The curvature must come from the algebra's own cubic-norm geometry -- one observer, one off-center point. No ensemble, no entropy bound, no holographic-screen argument."
    - id: fp-float-decisive
      subject: claim-matter-sourcing
      proxy: "Computing the decisive Riemann/Ricci/Delta R/baseline verdicts in float (numpy ranks, float curvature)."
      reason: "Exact over Q. Ranks via sympy.Matrix.rank(); curvature via sympy.Rational. Float (mpmath) only for explicitly-labeled NON-decisive triage."
  links:
    - id: link-baseline-supports
      source: claim-matter-sourcing
      target: deliv-phaseB
      relation: supports
      verified_by: [test-lambda-vs-matter]
    - id: link-engine-computes
      source: deliv-phaseB
      target: deliv-engine-matter
      relation: computes
      verified_by: [test-lambda-vs-matter]
    - id: link-totaro-method
      source: ref-totaro
      target: deliv-engine-matter
      relation: supports
      verified_by: [test-lambda-vs-matter]
    - id: link-fk-baseline
      source: ref-faraut-koranyi
      target: deliv-phaseB
      relation: benchmarks
      verified_by: [test-lambda-vs-matter]
  uncertainty_markers:
    weakest_anchors:
      - "No external literature anchor for the matter-sourcing CLAIM (novel); literature only corroborates the matterless M=0 Cartan/Einstein-negative baseline (arXiv:2309.02682). Validation is internal/exact-over-Q, not literature-benchmarked."
      - "rho_J vs det_2: the contract says rho_J(X_bg); Phase-71 verifier pinned det_2 (Stab_{V_0}=Spin(9,1) invariant) as the strictly-correct modulus. They coincide for single-direction perturbations. 72-01 sets up the engine; the scaling vs det_2 is reported in 72-02."
      - "Totaro totaro_riemann correctness (possible engine bug). Mitigated by riemann_symmetry_ok + the H^3 K=-1/2 benchmark already in the harness + a few-component hand-rolled cross-check."
    disconfirming_observations:
      - "Delta R(M) has a nonzero imaginary part => reality/Wick-contamination error (wrong cross-term association or float leak). Must be exactly real over Q."
      - "ricci_decomposition residual (R_ijkl - scalar - tracelessRicci - Weyl) != 0 over Q => decomposition or sign-convention bug; block downstream until fixed."
      - "M=0 center is NOT Einstein (S_munu != 0 at the center) => construction-(ii) bridge contaminated or wrong baseline => would invalidate the pure-Lambda reference (revisit Phase 70 before any matter verdict)."
      - "2*Re((x2 x1) x3) = 0 at the chosen M (single-slot / accidentally-associative) => hollow non-vacuity; repopulate all three octonion slots with e_4..e_7 content."
---

<objective>
Build the matter-aware curvature engine for the dim-4 h_2(C_u) spacetime sub-slice and establish the M=0 baseline (pure-Lambda at the center; matterless Phase-71 R(det_2) off-center) -- the foundation the decisive cross-term ON/OFF test (72-02) consumes.

This is an ISOLATION phase, not a method-selection phase. The method (Totaro closed-form curvature on the certified det_3 SSOT) is LOCKED and the engine (code/bulk_geometry_verification.py, ALL_PASS exit 0) is warm. Phase 71 established (VERIFIED, exact over Q) that the matterless V_0 geometry is ALREADY position-dependent: R = R(det_2) varies across det_2 leaves with ZERO matter, and is pure-Lambda (Einstein, R(center)=-3) ONLY at the F_4-symmetric center I/3. So matter is NOT the origin of homogeneity-breaking; the dominant failure mode is misattributing the matterless det_2-variation to matter. The fix: isolate matter as Delta R(M) = R(X_bg+M) - R(X_bg) at a FIXED V_0-background.

Purpose: DERV-02 (Riemann/Ricci as an explicit function of M) and VALD-04 (n=4 Ricci decomposition + M=0 pure-Lambda baseline). These give 72-02 a verified matter-curvature pipeline and a verified Lambda reference to difference against.
Output: engine extension (matter-aware Delta R + ricci_decomposition, registered as ALL_PASS gates) and the 72-01 portion of derivations/72-matter-sourcing.tex.

Honest-negative discipline: a pure-Lambda Delta R, or one that survives the off-switch (tested in 72-02), is the forbidden proxy fp-lambda-as-sourcing and must be reported as "position-dependent but NOT matter-sourced" (NEGATIVE-RESULT-IS-SUCCESS).
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/CONVENTIONS.md
@.gpd/phases/72-b-matter-sourcing/72-RESEARCH.md
@.gpd/phases/71-a-homogeneity-kill-gate/71-VERIFICATION.md
@code/bulk_geometry_verification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanism + index audit, non-vacuity preflight, and Phase-71 regression anchors</name>
  <files>code/bulk_geometry_verification.py</files>
  <action>
    Confirm the matter mechanism and the engine index map BEFORE adding any matter, and reproduce the Phase-71 matterless anchors exactly over Q (regression). Add these as _report() gates in main() (placed after the existing CALC-02 II gates). Run foreground `python3 -u code/bulk_geometry_verification.py` with progress prints between heavy steps (watchdog discipline -- the ~150s stream-watchdog kills quiet runs).

    1. PEIRCE INDEX MAP. Call `peirce_indices_under_E11()` and assert the eigenvalue->index split is EXACTLY V_1 (eigenvalue 1) = {0}, V_0 (eigenvalue 0) = {1..10}, V_{1/2} (eigenvalue 1/2) = {11..26}, and L_{E_11} is diagonal. Cross-check against the module constants SPACETIME_SLICE_IDX=[1,2,3,10], V_HALF_IDX=range(11,27), H2CU_SLICE_IDX=[1,2,3,10]. (Confirmed in smoke test: groups = {1:[0], 0:[1..10], 1/2:[11..26]}, diagonal=True.)

    2. CROSS-TERM IS THE ONLY V_0<->V_{1/2} OCTONION CHANNEL. From `_coord_from_octmat`: a,b,g diagonal; x1=X[2][1] (V_0 octonion, engine indices {3..10}; slice uses p=idx3, q=idx10, internal W-sector = {4..9}); x2=X[0][2] (indices {11..18}); x3=X[1][0] (indices {19..26}). In det_3 (line 351), the ONLY term mixing x1 with x2,x3 is `cross = oct_mul(oct_mul(x2,x1),x3)`, contributing `2*cross[0] = 2*Re((x2 x1) x3)`. Assert (symbolically or on a representative octonionic point) that det_3 has no other x1-x2 or x1-x3 octonion-product term: the diagonal a,b,g multiply |x1|^2,|x2|^2,|x3|^2 (norm couplings, NOT the triple), and the triple `2*Re((x2 x1) x3)` is the unique V_0<->V_{1/2} mixing channel. Document that "cross-terms off" precisely means dropping this triple (the |x2|^2,|x3|^2 norm terms remain -- Open Question 2; state explicitly, do not overclaim full decoupling).

    3. POLARIZATION BLOCK NONZERO ON OCTONIONIC M. Using `polarize_d` on the Peirce basis, confirm the (V_{1/2}, V_{1/2}, V_0) trilinear block d(x2-dir, x3-dir, x1-dir) is NONZERO when the directions carry genuinely octonionic (e_4..e_7) content -- i.e. the cubic vertex coupling matter to V_0 is genuinely present (not vacuously zero on quaternionic e_0..e_3 inputs only). This guards Pitfall 2 (octonion non-associativity) and Pitfall 3 (hollow triple product).

    4. NON-VACUITY PREFLIGHT (the chosen M). Define the representative non-vacuous matter placement for this phase: a FIXED V_0-internal x1 component in {4..9} (so x1 != 0 at the background -- resolves the center-I/3 x1=0 hollowness; e.g. idx4=1/4, idx7=1/5 with e_1,e_4 content), x2 matter in {11..18} (e.g. idx11=1/3, idx15=1/6), x3 matter in {19..26} (e.g. idx19=1/3, idx23=1/7), all with e_4..e_7 octonionic content. Build X_bg+M and assert `2*Re((x2 x1) x3) != 0` exactly over Q (smoke test gave -13/315 != 0). This is the M used for the DERV-02 matter-Riemann (Task 2) and handed to 72-02.

    5. PHASE-71 MATTERLESS REGRESSION ANCHORS. Using the existing `_curvature_invariants_at(delta, slice_pt, simp=cancel)` with ZERO matter (delta in V_0-internal {4..9} only), reproduce EXACTLY over Q at least: R(center)=-3 (delta={}, evaluated at the center slice), and one 71-VERIFICATION basepoint, e.g. delta={4:Rational(1,3)} => R = -73041507/21967969, and delta={4:Rational(1,5),5:Rational(1,7)} => R = -521269105/154700283 (71-VERIFICATION CHECK A(i)). Assert exact equality. This proves the matter-aware path (Task 2) is a faithful extension of the verified matterless engine.

    Use `from sympy import cancel`; pass simp=cancel in hot loops (NOT simplify). Keep matter rational; only the 4 slice coords symbolic.
  </action>
  <verify>
    1. peirce_indices_under_E11() returns {1:[0], 0:[1..10], 1/2:[11..26]} with L_{E_11} diagonal=True (exact over Q); matches SPACETIME_SLICE_IDX / V_HALF_IDX / H2CU_SLICE_IDX.
    2. det_3 mixes x1 with x2,x3 ONLY through 2*Re((x2 x1) x3); the diagonal a,b,g couple only the squared norms |x1|^2,|x2|^2,|x3|^2 (documented; the off-switch target is unambiguous).
    3. polarize_d (V_{1/2},V_{1/2},V_0) trilinear block != 0 on e_4..e_7 octonionic directions (cubic vertex genuinely present).
    4. Non-vacuity: 2*Re((x2 x1) x3) != 0 at the chosen M (exact over Q; e.g. -13/315). All three octonion slots populated with e_4..e_7 content.
    5. Regression: R(center) = -3 exactly; R(delta={4:1/3}) = -73041507/21967969 exactly; R(delta={4:1/5,5:1/7}) = -521269105/154700283 exactly (matches 71-VERIFICATION over Q).
    6. Engine `python3 code/bulk_geometry_verification.py` => OVERALL: ALL_PASS, exit 0 (new gates pass; no regression in the 71 gates).
  </verify>
  <done>Peirce index map confirmed (V_1={0}, V_0={1..10}, V_{1/2}={11..26}); cross-term established as the unique V_0<->V_{1/2} octonion channel; polarization block nonzero on octonionic M; non-vacuous M chosen and verified (cross-term != 0, all three slots, e_4..e_7); Phase-71 matterless R anchors reproduced exactly over Q; engine ALL_PASS exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: DERV-02 -- matter-aware Riemann/Ricci as an explicit function of M (Delta R at fixed V_0-background)</name>
  <files>code/bulk_geometry_verification.py</files>
  <action>
    Extend the engine so matter enters the background and the inherited slice Riemann/Ricci become explicit functions of M, isolated at a FIXED V_0-background via the difference Delta R(M) = R(X_bg+M) - R(X_bg). Reuse the verified exact rational-inverse path `_curvature_invariants_at` (lines 1546-1578): it puts the perturbation into `delta` (rational), keeps the 4 slice coords symbolic for g and the 3rd-derivative tensor C, evaluates at a rational slice point, inverts the 4x4 RATIONAL g (the ~0.5-3s path, NOT the >200s symbolic-matter cliff), and contracts via totaro_riemann. Matter indices {0} U {11..26} go into the SAME `delta`; the V_0-internal x1 content {4..9} is part of the FIXED background (carried in BOTH the X_bg and X_bg+M calls so it cancels in Delta R).

    1. ISOLATION PROTOCOL helper. Add `delta_R_at_fixed_bg(bg_delta, matter_delta, slice_pt, simp=cancel)` that:
       - computes R_bg = _curvature_invariants_at(bg_delta, slice_pt)            # fixed V_0-background only (matter off)
       - computes R_full = _curvature_invariants_at({**bg_delta, **matter_delta}, slice_pt)   # background + matter
       - returns (R_bg, R_full, Delta R = cancel(R_full - R_bg)), plus reality flags and det_g != 0 for both.
       bg_delta holds the FIXED V_0-internal x1 content (indices {4..9}); matter_delta holds M in V_1 {0} and/or V_{1/2} {11..26}. The matterless det_2-variation cancels in the difference (same V_0-background both sides), so Delta R is purely the M-effect (defeats fp-lambda-as-sourcing).

    2. EXPLICIT-FUNCTION-OF-M Riemann/Ricci. For the representative non-vacuous M from Task 1, build the FULL lower-index Riemann R[i][j][k][l] (totaro_riemann), Ricci tensor Ric_jl = g^{ik}R_ijkl, and Ricci scalar (ricci_scalar) at X_bg+M, evaluated at the common slice point. Also retain them at X_bg (M=0, same fixed background) so the per-component Delta R_ijkl, Delta Ric_jl, Delta R are all explicit. Assert `riemann_symmetry_ok(R, 4, simp=cancel)` = True for BOTH the M-on and M-off Riemann tensors.

    3. CONSISTENCY CROSS-CHECK (totaro vs hand-rolled). Cross-check a handful of Riemann components against an INDEPENDENT hand-rolled Christoffel/Riemann computation of the SAME 4x4 slice metric at the SAME rational point (the engine already has this path in h3_constant_curvature / the H^3 benchmark; mirror it for one matter background, a few components). This guards against a totaro_riemann bug (Approach 2 fallback / disconfirming-observation discipline). Exact over Q.

    4. Register all of the above as _report() gates in main(): Delta R(M) is real over Q and det_g != 0 (M-on and M-off); riemann_symmetry_ok True (both); the few-component totaro-vs-hand-rolled agreement is exact. Print progress between the Hessian build, the inverse, the Riemann contraction, and each decomposition step (watchdog). Foreground `python3 -u`.

    Performance: matter is rational => the 4x4 inverse is ~0.5-3s; full Riemann ~19s; the M-on + M-off pair ~40s total. If anything runs >60s, matter has leaked symbolic -- substitute to rationals. Do NOT carry matter symbolic through Matrix.inv() (the >200s cliff, trips the watchdog).
  </action>
  <verify>
    1. delta_R_at_fixed_bg returns Delta R(M) exact over Q at the fixed V_0-background; matterless det_2-variation cancels (R_bg is the same fixed reference in both calls).
    2. Delta R(M) is REAL over Q (imaginary part identically 0) and det_g != 0 for both M-on and M-off (nondegenerate at the positive-cone basepoint). (Smoke test: with x1 fixed {4,7}, M in {11,15,19,23}, Delta R = 16118528604125200/3374980547075787 != 0, real.)
    3. riemann_symmetry_ok(R,4) = True for BOTH the M-on and M-off lower-index Riemann tensors (antisymmetry i<->j, k<->l; pair-swap (ij)<->(kl)).
    4. Dimensional/grading check: R, Ric_jl, Delta R all dimensionless exact rationals; Ric_jl symmetric; the slice metric g is the inherited construction-(ii) metric (g(center,M=0)=eta).
    5. Cross-check: the few totaro_riemann components match the independent hand-rolled Christoffel/Riemann to exact equality over Q (no totaro engine bug).
    6. Limiting case: with matter_delta = {} (M=0), Delta R = 0 exactly (the difference vanishes when no matter is added) -- the trivial M->0 sanity at fixed background.
    7. Engine main() ALL_PASS exit 0; no regression.
  </verify>
  <done>Matter-aware Riemann/Ricci built as explicit functions of M via delta_R_at_fixed_bg; Delta R(M)=R(X_bg+M)-R(X_bg) real and exact over Q at a fixed V_0-background; riemann_symmetry_ok True for M-on and M-off; totaro vs hand-rolled cross-check exact; M->0 (no matter added) gives Delta R=0; engine ALL_PASS exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: VALD-04 -- n=4 Ricci decomposition + M=0 pure-Lambda baseline (center Einstein, off-center matterless R(det_2))</name>
  <files>code/bulk_geometry_verification.py, derivations/72-matter-sourcing.tex</files>
  <action>
    Build the standard n=4 GR Ricci decomposition and establish the M=0 baseline that separates the maximally-symmetric (pure-Lambda) piece from genuine M-structure. Use the EXACT formulas from 72-RESEARCH.md (lines 368-380), indices raised by the slice metric g_munu (mostly-minus; the decomposition is a signature-agnostic algebraic identity in the curvature tensor). n=4.

    1. ricci_decomposition(R, g, ginv, n=4, simp=cancel) routine returning a dict:
       - Ricci tensor Ric_jl = g^{ik} R_ijkl; Ricci scalar Rs = g^{jl} Ric_jl.
       - Traceless Ricci S_munu = Ric_munu - (Rs/n) g_munu = Ric_munu - (Rs/4) g_munu. Assert g^{munu} S_munu = 0 identically over Q (trace removed).
       - Scalar part of Riemann: Scal_ijkl = (Rs/(n(n-1)))(g_il g_jk - g_ik g_jl) = (Rs/12)(g_il g_jk - g_ik g_jl).
       - Traceless-Ricci part of Riemann: with Z_jk = S_jk, E_ijkl = (1/(n-2))(Z_il g_jk - Z_jl g_ik - Z_ik g_jl + Z_jk g_il) = (1/2)(...) for n=4.
       - Weyl tensor (residual): Weyl_ijkl = R_ijkl - Scal_ijkl - E_ijkl.
       - RECONSTRUCTION GATE: assert R_ijkl - (Scal_ijkl + E_ijkl + Weyl_ijkl) = 0 for all i,j,k,l exactly over Q (the decomposition is complete and consistent; a nonzero residual flags a sign/index bug -- disconfirming observation).

    2. M=0 CENTER BASELINE (pure-Lambda, Cartan). At X_bg = I/3 (delta={}), M=0, compute the slice curvature and assert: R(center) = -3 < 0 (negative => Lambda < 0 Cartan); the slice is EINSTEIN, i.e. traceless Ricci S_munu = 0 exactly over Q (R_munu proportional to g_munu). This is the maximally-symmetric pure-cosmological-constant reference (test-lambda-vs-matter (a)). Cite ref-faraut-koranyi (E_{6(-26)}/F_4 symmetric space, Cartan/Einstein-negative baseline) and ref-totaro (sign). Confirm consistency with the H^3 benchmark already in the harness (cone-Hessian K_sect = -1/2 constant, sign pinned).

    3. M=0 OFF-CENTER BASELINE (matterless, NOT Einstein in general). At a fixed off-center V_0-internal background (e.g. delta={4:Rational(1,3)}), M=0, confirm R reproduces the Phase-71 matterless R(det_2) anchor (-73041507/21967969) exactly, and compute its decomposition -- generically S_munu != 0 (matterlessly non-Einstein off-center, the Phase-71 finding). Document that the M=0 baseline is pure-Lambda ONLY at the center; off-center it is the matterless det_2-geometry. THIS is why matter must be isolated as Delta from the SAME fixed V_0-background, not against a global "flat" reference (Pitfall 1 / fp-lambda-as-sourcing).

    4. WRITEUP. Create derivations/72-matter-sourcing.tex (72-01 portion): state the isolation protocol (Delta R at fixed V_0-background), the matter mechanism (cross-term 2*Re((x2 x1) x3) as the unique V_0<->V_{1/2} channel; cite ref-mccrimmon, ref-peirce-coupling), the matter-aware Riemann/Ricci (DERV-02, Task 2), the n=4 Ricci decomposition, and the M=0 baseline (center pure-Lambda Einstein R=-3<0 Cartan; off-center matterless R(det_2)). Surface ref-prompt (Phase B targets + reporting discipline), ref-faraut-koranyi (cone metric + Cartan baseline), ref-totaro (curvature engine + sign). State the conventions block (mostly-minus slice; Phi=-log det_3; engine det_3 SSOT; Riemann sign R<0 at center). Mark the cross-term off-switch and scaling as DEFERRED to 72-02 (the decisive matter verdict), and note the honest-negative branch (fp-lambda-as-sourcing).

    Register the decomposition reconstruction gate, the M=0 center Einstein gate, and the off-center matterless anchor gate as _report() in main(). Foreground `python3 -u`; progress prints.
  </action>
  <verify>
    1. ricci_decomposition reconstruction: R_ijkl - (Scal + E + Weyl) = 0 for all components exactly over Q (complete, consistent decomposition).
    2. Traceless Ricci is traceless: g^{munu} S_munu = 0 identically over Q.
    3. M=0 center: R(center) = -3 < 0 (Lambda < 0 Cartan); S_munu = 0 exactly => slice Einstein (pure-Lambda). Consistent with the H^3 K=-1/2 benchmark (sign pinned negative).
    4. M=0 off-center (delta={4:1/3}): R = -73041507/21967969 exactly (Phase-71 anchor); S_munu generically != 0 (matterlessly non-Einstein) => the M=0 baseline is pure-Lambda ONLY at the center.
    5. fp-lambda-as-sourcing guard documented: matter isolated as Delta from the SAME fixed V_0-background; the M=0/pure-Lambda part is the reference, not a matter result.
    6. Order-of-magnitude/sign sanity: R<0 at the center and along the matterless family (negative scalar curvature, Cartan), matching the symmetric-space expectation (ref-faraut-koranyi); decomposition residual 0.
    7. derivations/72-matter-sourcing.tex contains the DERV-02 Riemann/Ricci-of-M, the M=0 baseline, the n=4 Ricci decomposition, the Phase-71 regression anchors, and the non-vacuity preflight; surfaces ref-prompt, ref-faraut-koranyi, ref-totaro. Engine ALL_PASS exit 0.
  </verify>
  <done>ricci_decomposition(R,g,ginv,n=4) built and reconstruction-gated exact over Q; M=0 center baseline is Einstein with R=-3<0 (pure-Lambda Cartan, S_munu=0); M=0 off-center reproduces the matterless Phase-71 R(det_2) and is generically non-Einstein; isolation protocol (Delta from fixed V_0-background) documented to defeat fp-lambda-as-sourcing; derivations/72-matter-sourcing.tex (72-01 portion) written and anchors surfaced; engine ALL_PASS exit 0.</done>
</task>

</tasks>

<verification>
- EXACT over Q on every decisive value (Riemann, Ricci, Delta R, decomposition, baseline). No float on any verdict (fp-float-decisive). Ranks/zeros via sympy, never numpy.
- Reality: Delta R(M), Ric_jl, S_munu, Weyl all real over Q (imaginary part identically 0) -- a nonzero imaginary part flags a cross-term-association or Wick-contamination bug.
- Riemann algebraic symmetries: riemann_symmetry_ok(R,4) = True for M-on and M-off.
- Decomposition completeness: R_ijkl = Scal + E + Weyl exactly over Q (residual 0); g^{munu} S_munu = 0.
- Limiting cases: M=0 center => Einstein R=-3<0 (pure-Lambda Cartan); M=0 off-center => matterless Phase-71 R(det_2) reproduced exactly; matter_delta={} => Delta R = 0.
- Sign pinned negative (R<0 at center / on the matterless family), consistent with the H^3 cone-Hessian K=-1/2 benchmark.
- Consistency cross-check: totaro_riemann few-component vs independent hand-rolled Christoffel/Riemann exact over Q.
- Isolation discipline (Pitfall 1 / fp-lambda-as-sourcing): matter is the Delta at a FIXED V_0-background; the matterless det_2-variation cancels.
- Non-vacuity (Pitfall 3): 2*Re((x2 x1) x3) != 0 at the chosen M (all three octonion slots, e_4..e_7).
- Engine regression: python3 code/bulk_geometry_verification.py => OVERALL: ALL_PASS, exit 0.
- Error budget: exact over Q => 0 numerical error. Dominant risk = the >200s symbolic-matter inverse cliff + ~150s watchdog; mitigations: matter rational before inv (smoke-tested ~0.5-3s), foreground python -u, progress prints between heavy steps.
</verification>

<success_criteria>
Matter-aware Riemann/Ricci of the inherited dim-4 h_2(C_u) slice metric built as an explicit function of M (DERV-02), isolated as Delta R(M)=R(X_bg+M)-R(X_bg) at a FIXED V_0-background, real and exact over Q, riemann_symmetry_ok True, totaro cross-checked against a hand-rolled few-component computation. n=4 ricci_decomposition (scalar/traceless-Ricci/Weyl) built and reconstruction-gated. M=0 baseline established and decomposed (VALD-04): the center is Einstein with negative Lambda (Cartan, R=-3<0, S_munu=0 = pure-Lambda); the off-center matterless baseline reproduces the Phase-71 R(det_2) and is generically non-Einstein. The pure-Lambda/matterless baseline is the reference to difference against, NOT counted as matter-sourcing (fp-lambda-as-sourcing rejected). derivations/72-matter-sourcing.tex (72-01 portion) written; ref-prompt, ref-faraut-koranyi, ref-totaro surfaced. Engine main() ALL_PASS exit 0. The matter-curvature pipeline + ricci_decomposition + the chosen non-vacuous M are handed to 72-02 for the decisive cross-term ON/OFF test.
</success_criteria>

<output>
After completion, create `.gpd/phases/72-b-matter-sourcing/72-01-SUMMARY.md`.
Record for the 72-02 handoff: the chosen non-vacuous M (bg_delta for the fixed V_0-internal x1, matter_delta for V_1/V_{1/2}), the M=0 center and off-center baseline R values + decompositions, the delta_R_at_fixed_bg and ricci_decomposition signatures, and the engine line numbers of the new functions/gates.
</output>
