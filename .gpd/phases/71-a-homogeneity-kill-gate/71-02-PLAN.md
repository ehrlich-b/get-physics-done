---
phase: 71-a-homogeneity-kill-gate
plan: 02
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - code/bulk_geometry_verification.py
  - derivations/71-homogeneity-stabilizer.tex
interactive: false

conventions:
  units: "natural (hbar=c=k_B=1); decisive arithmetic EXACT over Q (sympy.Rational/Matrix, DomainMatrix-over-QQ via exact_qq_rank); NEVER numpy float rank"
  jordan_product: "(1/2)(XY+YX)"
  octonion: "Fano e1*e2=e4; complex structure u=e7; C_u = span{1,e_7}"
  cubic_norm: "Freudenthal det WITH cross-terms, corrected triple order 2Re((x2 x1) x3); engine det_3 (inv_det_X) is SSOT; NEVER import octonion_algebra.py"
  group: "F_4 = Aut(h_3(O)) (compact, 52-dim) = inner_derivations(); E_6 = Stab(det), e_6 = f_4 + L(h_3(O)_traceless), dim 52 + 26 = 78 (Koecher-Tits, ref-baez-octonions)"
  E_11: "diag(1,0,0); Peirce h_3(O)=V_1(1)+V_{1/2}(16)+V_0(10)=h_2(O); spacetime sub-slice engine indices {x1,x2,x3,x10}={17,18,19,26}"
  potential: "Phi = -log det (FIXED); cone metric g_X = Hess(-log det), positive-definite (Riemannian)"
  signature: "mostly-minus (-,+,+,+) Lorentzian on h_2(C_u) via det_2; ambient h_3(O) cone RIEMANNIAN. Signature bridge = construction (ii) (Phase 70 LOCKED)"
  center: "I/3 (rho_J=0); det(I/3)=1/27"
  expansion_variable: "rho_J(X_bg) = off-center-ness of the BASEPOINT, NOT the spacetime coordinate x"
  orbit_stabilizer: "dim(orbit of v) = rank of [M . v]_{M in basis} at a generic v (char 0, Derksen-Kemper); dim Stab_{E_6}(E_11) = 78 - dim(orbit of E_11) = dim ker{D -> D . E_11}; reproduce single-copy F_4 anchor (orbit 24 / Spin(8) stabilizer 28) FIRST as calibration"
  gauss_equation: "R^slice = R^ambient|_slice + (II_ik II_jl - II_il II_jk); II(X,Y) = (nabla_X Y)^perp; II = 0 <=> totally geodesic => R^slice = ambient CONSTANT curvature => homogeneous => KILL (ref-kollross-rodriguez-vazquez)"

dimensional_check:
  dim_e6: "78 = 52 (f_4) + 26 (L traceless) — exact span rank over Q"
  dim_stab_E11: "expected ~45 (parabolic; Levi ~ Spin(9,1)) — exact-over-Q kernel dimension; structural sanity only, the computed value is decisive"
  single_copy_orbit: "orbit 24 / Spin(8) stabilizer 28 / trdeg 3 — the calibration anchor (must reproduce exactly)"
  II_components: "second fundamental form of V_0 in the cone — exact over Q; II = 0 (totally geodesic) vs II != 0 (non-geodesic)"

approximations:
  - name: "Generic-basepoint orbit dimension via rank of the infinitesimal action (char 0)"
    parameter: "none (exact rational/octonionic-integer points)"
    validity: "rank is lower-semicontinuous => use >= 2 generic points and take the MAX (the gate's established pattern); _is_genuinely_octonionic_integer validates each point (>= 2 imaginary comps, distinct diag)"
    breaks_when: "a non-generic basepoint UNDERESTIMATES the orbit (rank drops on a measure-zero subvariety) — mitigated by MAX over >= 2 generic points"
    check: "the single-copy anchor (orbit 24 / Spin(8) 28 / trdeg 3) reproduced FIRST; if not, the builder is untrustworthy (backtracking trigger)"
  - name: "II computed on the V_0 slice at a generic rational basepoint"
    parameter: "none (exact)"
    validity: "the totally-geodesic question is algebraic; II operates on the FULL V_0 family (not just the dim-4 sub-slice), addressing the dim-4-suffices gap in Plan 71-01"
    breaks_when: "a degenerate/light-cone basepoint — choose a generic point"
    check: "Gauss equation consistency: if II = 0 then R^slice = ambient constant (cross-check against Plan 71-01's H^3 benchmark and verdict)"

contract:
  schema_version: 1
  scope:
    question: "Does Stab_{E_6}(E_11) act transitively enough on the (basepoint, slice) family to make all pairs isometric (orbit dim = family dim => homogeneous => KILL) or not (orbit dim < family dim => SURVIVES)? Is the V_0 slice totally geodesic (II = 0 => KILL)? And do these two FULL-V_0-family routes AGREE with Plan 71-01's dim-4 curvature verdict (the mandatory two-route cross-check that is part of the pass condition)?"
  claims:
    - id: claim-homogeneity
      statement: "With E_11 fixed, the inherited slice metric g_mu_nu(x) is EITHER genuinely position-dependent (SURVIVES) OR x-independent/homogeneous (DEAD). This plan advances the claim via Route 2 (stabilizer transitivity, the mandatory cross-check) + CALC-02 (the totally-geodesic II shortcut), BOTH on the FULL V_0 (basepoint, slice) family, and emits the FINAL reconciled two-route verdict: dim Stab_{E_6}(E_11) and the (basepoint, slice)-family dimension modulo it (exact ranks over Q), plus the second fundamental form II of V_0; cross-checked against Plan 71-01's curvature verdict (the routes MUST AGREE). Reported honestly either way, a homogeneous result a clean valuable KILL."
      deliverables: [deliv-phaseA, deliv-stabilizer-note]
      acceptance_tests: [test-homogeneity, test-single-copy-anchor, test-e6-dim, test-two-route-agreement]
      references: [ref-warm-engine, ref-baez-octonions, ref-kollross-rodriguez-vazquez, ref-faraut-koranyi, ref-52-kkt]
  observables:
    - id: obs-position-dependence
      name: "Stabilizer transitivity and totally-geodesic II of the V_0 slice"
      kind: classification
      definition: "Whether Stab_{E_6}(E_11) acts transitively on the (basepoint, slice) family (orbit dim = family dim => KILL) or not (orbit dim < family dim => SURVIVES), and whether II of V_0 vanishes (II = 0 => totally geodesic => KILL) — both cross-checked against Plan 71-01's curvature-invariant verdict."
      regime: "full V_0 (basepoint, slice) family, exact ranks over Q"
  deliverables:
    - id: deliv-phaseA
      kind: derivation
      path: "derivations/"
      description: "SHARED Phase 70+71 deliverable. Phase 70 completed the A0 signature-bridge portion; Plan 71-01 advanced the curvature portion. This plan advances the remaining must_contain items: dim Stab_{E_6}(E_11) and the dimension of the (basepoint, slice) family modulo that stabilizer; the FINAL reconciled two-route verdict (curvature vs stabilizer-transitivity, plus the II totally-geodesic shortcut); if homogeneous, the explicit 'route dead' STOP; and the Cayley-Hamilton + multiplicativity verification of the corrected cubic-norm cross-term reaffirmed BEFORE the group/geometry build (reuse the certified engine's LOCK 7a/7b, do NOT re-derive)."
      must_contain:
        - "dim Stab_{E_6}(E_11) and the dimension of the (basepoint, slice) family modulo that stabilizer (exact over Q)"
        - "the FINAL reconciled two-route verdict (curvature-invariant Route 1 AND stabilizer-transitivity Route 2 AGREE), plus the totally-geodesic II shortcut"
        - "if homogeneous: an explicit 'Phase A homogeneous -- route dead. STOP.' statement"
        - "Cayley-Hamilton + multiplicativity reaffirmation of the corrected cubic-norm cross-term (engine LOCK 7a/7b) BEFORE the group/geometry build"
    - id: deliv-stabilizer-note
      kind: note
      path: "derivations/71-homogeneity-stabilizer.tex"
      description: "Route 2 + CALC-02 write-up: the e_6 = f_4 + L(traceless) build (dim 78); the single-copy calibration anchor (orbit 24 / Spin(8) 28); dim Stab_{E_6}(E_11) = ker{D -> D.E_11} and the (basepoint,slice)-family dim modulo it; the second fundamental form II of V_0 (II=0? totally geodesic?) with the Kollross-Rodriguez-Vazquez Table-7 inference (V_0 absent => leans non-geodesic) confirmed/refuted by the DIRECT II computation; and the FINAL reconciled two-route verdict."
      must_contain:
        - "e_6 = f_4 + L(h_3(O)_traceless), dim 78 = 52 + 26"
        - "dim Stab_{E_6}(E_11) = ker{D -> D . E_11}"
        - "second fundamental form II"
        - "two-route agreement"
  references:
    - id: ref-warm-engine
      kind: prior_artifact
      locator: "code/orbit_dimension_gate.py (single_copy_orbit_rank, check_single_copy_orbit_dim, check_single_copy_gate, infinitesimal_action, exact_qq_rank, span_rank_over_QQ, _is_genuinely_octonionic_integer) + code/bulk_geometry_verification.py (det_3 SSOT, jordan_L_matrix, inner_derivations, _standard_basis_27)"
      aliases: ["warm engine", "orbit_dimension_gate.py"]
      role: method
      why_it_matters: "The CERTIFIED exact-over-Q orbit/stabilizer rank machinery REUSED VERBATIM for Route 2: single_copy_orbit_rank + infinitesimal_action + exact_qq_rank reproduce the calibration anchor (orbit 24 / Spin(8) 28) and compute dim Stab_{E_6}(E_11) as a kernel over Q. e_6 assembles in ~15 lines from jordan_L_matrix (f_4 via inner_derivations) + L(traceless). REUSE primitives; do NOT rebuild; do NOT import octonion_algebra.py."
      applies_to: [claim-homogeneity, deliv-phaseA, deliv-stabilizer-note]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use, cite]
    - id: ref-baez-octonions
      kind: paper
      locator: "Baez, The Octonions (Bull. AMS 39, 2002); arXiv:math/0105155"
      aliases: ["Baez 2002"]
      role: method
      why_it_matters: "Group facts for Route 2: E_6 = Stab(det); e_6 = f_4 + L(h_3(O)_traceless), dim 78 (Koecher-Tits); SL(2,O) ~ Spin(9,1) (the expected Levi of the parabolic Stab_{E_6}(E_11), structural sanity ~45); OP^2 = F_4/Spin(9). Pins the structural expectations the exact computation must (or must not) meet."
      applies_to: [claim-homogeneity, deliv-stabilizer-note]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [cite, use]
    - id: ref-kollross-rodriguez-vazquez
      kind: paper
      locator: "Kollross & Rodriguez-Vazquez, 'Totally geodesic submanifolds in exceptional symmetric spaces,' Adv. Math. (2023); arXiv:2202.10775, Table 7"
      aliases: ["Kollross-Rodriguez-Vazquez 2023", "KRV Table 7"]
      role: benchmark
      why_it_matters: "Table 7 = the maximal totally-geodesic classification of E_{6(-26)}/F_4. The V_0 = h_2(O) slice (SO(9,1)/SO(9)) and H^3 are ABSENT from the maximal list => leans GREENLIGHT (non-geodesic, SURVIVES) — but absence-from-the-maximal-list is an INFERENCE that MUST be confirmed by the direct II computation (CALC-02), and V_0 must be ruled out as sitting inside a larger geodesic submanifold (F_4(-20)/Spin(9), Sp(1,3)/...). Plan-LOCAL reference: NOT in the project contract; defined here with full locator. NOTE (weakest anchor): the WebFetch of arXiv:2202.10775 returned only the abstract, NOT the table contents — the decisive weight is on the DIRECT II computation, not the literature inference."
      applies_to: [claim-homogeneity, deliv-stabilizer-note]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [cite, compare]
    - id: ref-faraut-koranyi
      kind: paper
      locator: "Faraut & Koranyi, Analysis on Symmetric Cones (Oxford, 1994)"
      aliases: ["FK 1994"]
      role: background
      why_it_matters: "The det=1 hypersurface is the Riemannian symmetric space E_{6(-26)}/F_4 (homogeneous, parallel curvature) — the AMBIENT space whose constant curvature the slice inherits IFF II = 0 (Gauss equation). The source of the homogeneity tension; the ambient against which II is the deviation."
      applies_to: [claim-homogeneity]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [cite]
    - id: ref-52-kkt
      kind: prior_artifact
      locator: "derivations/52-kkt-spacetime.tex + derivations/52-observer-uniqueness.tex"
      aliases: ["52-kkt"]
      role: definition
      why_it_matters: "The slice frame: h_2(C_u) ~ R^{3,1}, index map {17,18,19,26} == engine {x1,x2,x3,x10}; the V_0 = h_2(O) (10-dim) Peirce slice with E_11 fixed. Defines the (basepoint, slice) family Route 2 measures the transitivity of, and the V_0 submanifold whose II is computed."
      applies_to: [claim-homogeneity, deliv-stabilizer-note]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [read, use, cite]
  acceptance_tests:
    - id: test-single-copy-anchor
      subject: claim-homogeneity
      kind: reproducibility
      automation: automated
      procedure: "BEFORE trusting any E_11 stabilizer count: reproduce the single-copy F_4 calibration anchor by running the certified orbit_dimension_gate machinery (check_single_copy_orbit_dim / single_copy_orbit_rank on a generic octonionic point validated by _is_genuinely_octonionic_integer), exact over Q."
      pass_condition: "Single-copy F_4 orbit dim == 24, Spin(8) stabilizer dim == 28, trdeg == 3 (the established anchor). If NOT reproduced, the orbit/stabilizer builder is untrustworthy — RE-CALIBRATE before trusting the E_11-stabilizer count (backtracking trigger)."
      evidence_required: [deliv-stabilizer-note, ref-warm-engine]
    - id: test-e6-dim
      subject: claim-homogeneity
      kind: dimensional_analysis
      automation: automated
      procedure: "Build e_6 = f_4 + L(h_3(O)_traceless) using inner_derivations() (f_4, 52-dim) and jordan_L_matrix(a, basis) for traceless a (26-dim), and verify its dimension as an exact span rank over Q (exact_qq_rank / span_rank_over_QQ over the 78 generators acting on the 27)."
      pass_condition: "dim e_6 == 78 = 52 + 26 exact over Q (the assembled Lie algebra is genuinely E_6). If != 78, the e_6 build is wrong — fix before computing the stabilizer."
      evidence_required: [deliv-stabilizer-note, ref-baez-octonions]
    - id: test-homogeneity
      subject: claim-homogeneity
      kind: oracle
      automation: automated
      procedure: "Compute (a) dim Stab_{E_6}(E_11) = 78 - rank[D . E_11]_{D in e_6 basis} = dim ker{D -> D . E_11}, exact over Q (DomainMatrix-over-QQ); (b) the (basepoint, slice)-family dimension and the orbit dimension under Stab_{E_6}(E_11) via single_copy_orbit_rank applied to the Stab generators acting on a generic FULL-V_0 basepoint (MAX over >= 2 generic octonionic-integer points); (c) the second fundamental form II of the FULL V_0 slice in the cone (II = 0 <=> totally geodesic <=> R^slice = ambient constant => KILL), ruling out V_0 inside a larger Table-7 geodesic submanifold. Route-2 verdict: orbit dim = family dim (transitive) => KILL; orbit dim < family dim => SURVIVES. II = 0 => KILL (independent shortcut); II != 0 => evidence for SURVIVES (via Gauss). All exact over Q (0 numpy float-rank on the decisive path — extend the module-local exact-only source guard)."
      pass_condition: "A DECISIVE Route-2 + II verdict either way, exact over Q, reported without softening; AND it AGREES with Plan 71-01's curvature verdict (see test-two-route-agreement). KILL branch emits the explicit 'Phase A homogeneous -- route dead. STOP.' string."
      evidence_required: [deliv-phaseA, deliv-stabilizer-note, ref-warm-engine, ref-kollross-rodriguez-vazquez]
    - id: test-two-route-agreement
      subject: claim-homogeneity
      kind: cross_method
      automation: automated
      procedure: "Reconcile the two routes: load Plan 71-01's Route-1 curvature verdict (SURVIVES/KILL from R(x), K(x) at >= 2 basepoints on the dim-4 h_2(C_u) sub-slice) and compare it against this plan's Route-2 stabilizer-transitivity verdict AND the CALC-02 II verdict (both on the FULL V_0 family). Also confirm the Riemannian-restriction vs Lorentzian-bridge x-variation verdicts agree (no Wick/coordinate artifact)."
      pass_condition: "All three (Route-1 curvature, Route-2 stabilizer transitivity, CALC-02 II) give the SAME verdict (all KILL or all SURVIVES). The agreement is PART OF THE PASS CONDITION. If they DISAGREE, the plan must STOP and localize the error (do NOT report a verdict): curvature-vs-stabilizer disagreement => neither trustworthy; Riemannian-vs-Lorentzian disagreement => return to Phase 70. Emit the FINAL reconciled two-route verdict only on agreement."
      evidence_required: [deliv-phaseA, deliv-stabilizer-note, test-homogeneity, test-single-copy-anchor]
  forbidden_proxies:
    - id: fp-relabel-homogeneous
      subject: claim-homogeneity
      proxy: "Relabeling a transitive-stabilizer / II=0 (homogeneous) result as 'approximately position-dependent', or gauging away real variation with a group element NOT in Stab_{E_6}(E_11) (a false KILL), or over-symmetrizing ('the cone is homogeneous so the slice is too')."
      reason: "A homogeneous result is a clean valuable KILL (emit 'Phase A homogeneous -- route dead. STOP.'). Fixing E_11 BREAKS transitivity to Stab_{E_6}(E_11) — only the RESIDUAL stabilizer's action counts; using a general E_6 element to 'show' two basepoints are isometric is a false KILL. No softening either way."
    - id: fp-coordinate-curvature
      subject: claim-homogeneity
      proxy: "Reading varying metric COMPONENTS as curvature, or assuming R^slice = R^ambient|_slice (forgetting the II terms in the Gauss equation), or expanding in spacetime x instead of rho_J(X_bg)."
      reason: "Route 2 is purely group-theoretic (immune to the component/chart trap), which is exactly why it is the cross-check. CALC-02 must rest on the Gauss equation R^slice = R^ambient|_slice + (II II - II II): a totally-geodesic slice (II=0) inherits the ambient CONSTANT curvature (KILL); II != 0 lets the slice be inhomogeneous even though the bulk is homogeneous. The verdict rests on INTRINSIC invariants + the honest II, never components."
    - id: fp-float-decisive
      subject: claim-homogeneity
      proxy: "Computing the decisive rank (orbit dim, stabilizer dim, family dim) or II in float."
      reason: "Rank is discontinuous — an SVD/float tolerance fabricates the orbit/stabilizer dimensions (the 6-vs-7, 24-vs-otherwise verdicts). Every decisive rank EXACT over Q via sympy.Matrix.rank() / exact_qq_rank (DomainMatrix-over-QQ), NEVER numpy.linalg.matrix_rank; II exact over Q. Float appears only as labeled non-decisive triage."
  links:
    - id: link-route2-verdict
      source: claim-homogeneity
      target: deliv-phaseA
      relation: supports
      verified_by: [test-homogeneity, test-two-route-agreement]
    - id: link-stabilizer-note
      source: deliv-stabilizer-note
      target: deliv-phaseA
      relation: supports
      verified_by: [test-homogeneity]
    - id: link-anchor-calibration
      source: ref-warm-engine
      target: deliv-stabilizer-note
      relation: benchmarks
      verified_by: [test-single-copy-anchor, test-e6-dim]
    - id: link-totally-geodesic
      source: ref-kollross-rodriguez-vazquez
      target: deliv-stabilizer-note
      relation: benchmarks
      verified_by: [test-homogeneity]
    - id: link-two-route-crosscheck
      source: claim-homogeneity
      target: deliv-stabilizer-note
      relation: depends_on
      verified_by: [test-two-route-agreement]
  uncertainty_markers:
    weakest_anchors:
      - "The Kollross-Rodriguez-Vazquez Table-7 inference (V_0 absent from the maximal totally-geodesic list => non-geodesic => GREENLIGHT) is exactly an INFERENCE: the WebFetch of arXiv:2202.10775 returned only the abstract, NOT the table. The decisive weight is therefore on the DIRECT II computation (CALC-02), NOT the literature inference; the plan also explicitly rules out V_0 sitting inside a larger Table-7 geodesic submanifold."
      - "The exact Stab_{E_6}(E_11) (a parabolic; Levi expected ~ Spin(9,1), dim ~45) is NOT cleanly tabulated — it is computed directly as the kernel {D in e_6 : D.E_11 = 0} over Q. The ~45 structural expectation (ref-baez-octonions) is a sanity guide, NOT the verdict; if the computed value contradicts it grossly, recheck the e_6 build (test-e6-dim) and the E_11 action."
    disconfirming_observations:
      - "The single-copy anchor (orbit 24 / Spin(8) 28 / trdeg 3) does NOT reproduce => the orbit/stabilizer builder is broken => RE-CALIBRATE before trusting the E_11 count (backtracking trigger)."
      - "dim e_6 != 78 => the f_4 + L(traceless) assembly is wrong => fix before the stabilizer."
      - "Route 1 (curvature) and Route 2 (stabilizer) DISAGREE => neither verdict trustworthy => STOP and localize the error (do NOT report a verdict)."
      - "The Riemannian-restriction and Lorentzian-bridge x-variation verdicts DISAGREE => a coordinate/Wick artifact => return to Phase 70."
      - "II = 0 (totally geodesic) while Route 1 reported SURVIVES (position-dependent) => contradiction via Gauss (II=0 forces ambient-constant curvature) => localize the error before any verdict."
---

<objective>
Route 2 (stabilizer transitivity, the MANDATORY cross-check) + CALC-02 (the totally-geodesic second-fundamental-form II shortcut), BOTH on the FULL V_0 (basepoint, slice) family, culminating in the FINAL reconciled two-route verdict for Phase 71's homogeneity KILL gate. With E_11 = diag(1,0,0) fixed, compute dim Stab_{E_6}(E_11) and the dimension of the (basepoint, slice) family modulo that stabilizer (exact ranks over Q), and the second fundamental form II of V_0; then cross-check both against Plan 71-01's curvature-invariant verdict — the two routes MUST AGREE, and the agreement is part of the pass condition.

Purpose: Route 2 is purely group-theoretic/algebraic (no curvature), so it is immune to the Riemann-sign and Wick-artifact hazards of Route 1 — which is exactly why it is the cross-check, and why it (with II) operates on the FULL V_0 family rather than the dim-4 sub-slice Plan 71-01 was restricted to (closing the dim-4-suffices gap). If Stab_{E_6}(E_11) acts transitively on the basepoint family (orbit dim = family dim) all basepoints are isometric => h x-independent => KILL; if the orbit is a proper subset => genuinely inequivalent basepoints => h varies => SURVIVES. II = 0 (totally geodesic) => R^slice = ambient constant => KILL (a cheap independent shortcut via the Gauss equation). The final verdict greenlights or KILLs the milestone.

Output: the e_6 (78-dim) build + dim Stab_{E_6}(E_11) + the (basepoint,slice)-family dim + the II computation ADDED to the certified code/bulk_geometry_verification.py (reusing orbit_dimension_gate.py machinery); the single-copy calibration anchor reproduced FIRST; the FINAL reconciled two-route verdict (with the explicit STOP statement on a KILL); derivations/71-homogeneity-stabilizer.tex.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/CONVENTIONS.md
@.gpd/phases/71-a-homogeneity-kill-gate/71-RESEARCH.md
@.gpd/phases/71-a-homogeneity-kill-gate/71-01-SUMMARY.md
@code/bulk_geometry_verification.py
@code/orbit_dimension_gate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce the single-copy F_4 calibration anchor + build e_6 (78-dim) — HARD GATES, do FIRST</name>
  <files>code/bulk_geometry_verification.py, derivations/71-homogeneity-stabilizer.tex</files>
  <action>
    Two calibration gates that MUST pass before any E_11 stabilizer count is trusted. Reuse the certified machinery; do NOT rebuild; do NOT import octonion_algebra.py.

    1. CALIBRATION ANCHOR (test-single-copy-anchor): reproduce the single-copy F_4 orbit on h_3(O) by running the certified orbit_dimension_gate.py machinery — `check_single_copy_orbit_dim(inner_derivations())` (or `single_copy_orbit_rank` on a generic octonionic point validated by `_is_genuinely_octonionic_integer`), exact over Q. Confirm orbit dim 24 / Spin(8) stabilizer 28 / trdeg 3. If this does NOT reproduce, the builder is untrustworthy — STOP and re-calibrate (backtracking trigger). (You may import the functions from orbit_dimension_gate.py or inline the small needed pieces; either way the f_4 generators are the engine's `inner_derivations()`.)
    2. REAFFIRM the cubic-norm cross-term (deliv-phaseA must_contain): re-run the certified engine LOCK 7a (Cayley-Hamilton norm) + LOCK 7b (324/324 inner-derivation annihilation) confirming det_3 is the F_4-invariant SSOT BEFORE the group/geometry build — reuse `python3 code/bulk_geometry_verification.py` ALL_PASS; do NOT re-derive.
    3. BUILD e_6 (test-e6-dim): assemble e_6 = f_4 + L(h_3(O)_traceless), dim 52 + 26 = 78 (Koecher-Tits, ref-baez-octonions). Use `inner_derivations()` for f_4 (52 generators, 27x27 matrices acting on the flattened h_3(O)) and `jordan_L_matrix(a, basis)` for traceless a (the 26 traceless multiplication operators; `_standard_basis_27()` gives the basis, drop/orthogonalize against the trace direction to get 26). Verify dim e_6 == 78 as an exact span rank over Q via `exact_qq_rank` / `span_rank_over_QQ` (DomainMatrix-over-QQ). If != 78, fix the build.
    4. Write the anchor reproduction + the e_6 build into derivations/71-homogeneity-stabilizer.tex (include the literal token `e_6 = f_4 + L(h_3(O)_traceless), dim 78 = 52 + 26`). Add `main()` gates printing PASS/FAIL for both.

    EXECUTION DISCIPLINE (executor watchdog ~150s silent-compute; single-copy anchor measured ~20-110s per point): foreground `python3 -u`; print progress; use `exact_qq_rank` (DomainMatrix over QQ) for the wide stacks where dense Matrix.rank stalls.
  </action>
  <verify>
    1. Single-copy anchor (test-single-copy-anchor): orbit dim == 24, Spin(8) stabilizer == 28, trdeg == 3 exact over Q. If not, STOP / re-calibrate.
    2. e_6 dimension (test-e6-dim): span rank over Q of the 78 generators (52 f_4 + 26 L(traceless)) acting on the 27 == 78. If not, fix the assembly.
    3. Cubic-norm SSOT reaffirmed: engine LOCK 7a/7b PASS (det_3 F_4-invariant); engine ALL_PASS exit 0.
    4. Exact over Q: 0 numpy float-rank calls on the decisive path (extend the module-local exact-only source guard).
  </verify>
  <done>The single-copy calibration anchor (orbit 24 / Spin(8) 28 / trdeg 3) is reproduced, certifying the orbit/stabilizer builder; e_6 = f_4 + L(traceless) is built and verified dim 78 over Q; the cubic-norm cross-term SSOT is reaffirmed (LOCK 7a/7b). The Route-2 machinery is calibrated and trustworthy BEFORE the E_11 stabilizer count.</done>
</task>

<task type="auto">
  <name>Task 2: Compute dim Stab_{E_6}(E_11) + the (basepoint, slice)-family dimension modulo it, and II of V_0</name>
  <files>code/bulk_geometry_verification.py, derivations/71-homogeneity-stabilizer.tex</files>
  <action>
    The Route-2 transitivity computation + the CALC-02 totally-geodesic shortcut, both on the FULL V_0 family, EXACT over Q.

    1. dim Stab_{E_6}(E_11): build the 27x27 matrix whose columns are {D . E_11}_{D in the 78 e_6 generators} (E_11 = diag(1,0,0) as a flattened 27-vector via the engine's coordinate layout), where D . E_11 is the e_6 action (f_4 derivation action + L(a) multiplication action) on E_11. Then dim Stab_{E_6}(E_11) = 78 - rank[D . E_11] = dim ker{D -> D . E_11}, exact over Q via `exact_qq_rank` (DomainMatrix-over-QQ). Structural sanity (ref-baez-octonions): expect a parabolic, Levi ~ Spin(9,1), dim ~45 — a guide, NOT the verdict.
    2. The (basepoint, slice)-family dimension and the orbit dimension under Stab_{E_6}(E_11): take the Stab generators (a basis of the kernel from step 1) and compute the orbit dimension of a generic FULL-V_0 basepoint under them via `single_copy_orbit_rank`-style rank of the infinitesimal action [D_stab . X_bg], MAX over >= 2 generic octonionic-integer basepoints (validate with `_is_genuinely_octonionic_integer`; rank lower-semicontinuous => MAX). Compare to the (basepoint, slice)-family dimension (the dimension of the space of distinct (basepoint, slice) configurations the family can take).
    3. SECOND FUNDAMENTAL FORM II (CALC-02): compute II of the FULL V_0 slice in the cone — II(X,Y) = (nabla_X Y)^perp, the normal-projected covariant derivative, at a generic rational basepoint, exact over Q. Use the cone Levi-Civita connection from the ambient cone-Hessian metric (Christoffels from Hess(-log det)) and project onto the normal space of V_0. II = 0 <=> totally geodesic <=> R^slice = ambient CONSTANT curvature => homogeneous => KILL (Gauss equation, ref-kollross-rodriguez-vazquez). Explicitly rule out V_0 sitting inside a larger Table-7 geodesic submanifold (F_4(-20)/Spin(9), Sp(1,3)/...) — argue from the computed II / the stabilizer structure, not just the literature inference.
    4. Route-2 + II verdict: orbit dim = family dim (transitive) => KILL; orbit dim < family dim => SURVIVES. II = 0 => KILL (independent shortcut); II != 0 => evidence for SURVIVES (via Gauss). Record both (they should agree with each other). Write into derivations/71-homogeneity-stabilizer.tex (include the literal tokens `dim Stab_{E_6}(E_11) = ker{D -> D . E_11}` and `second fundamental form II`). Add `main()` gates.

    EXECUTION DISCIPLINE: foreground `python3 -u`, progress prints (e.g. "Stab kernel done", "orbit rank pt1 done", "II built"); `exact_qq_rank` for wide stacks; rational/octonionic-integer basepoints; each decisive step under ~150s. II via Christoffels on the dim-4 or restricted V_0 frame if the full dim-10 connection is too heavy — but the II=0? verdict must address the full V_0 (use the algebraic/stabilizer structure to extend beyond dim-4 where the direct connection is intractable).
  </action>
  <verify>
    1. dim Stab_{E_6}(E_11) computed exact over Q as 78 - rank[D.E_11] (= ker dim); reported with the structural-sanity note (parabolic, Levi ~ Spin(9,1), ~45 expected).
    2. The orbit dim under Stab vs the (basepoint, slice)-family dim computed exact over Q, MAX over >= 2 generic basepoints; Route-2 verdict (transitive => KILL / proper subset => SURVIVES) stated.
    3. II of V_0 computed exact over Q; II = 0 (totally geodesic => KILL) or II != 0 (=> SURVIVES via Gauss) stated; V_0-inside-larger-geodesic-submanifold ruled out.
    4. Route-2 transitivity verdict and the II verdict AGREE with each other (both KILL or both SURVIVES); if not, localize the error.
    5. Exact over Q: all decisive ranks via exact_qq_rank / sympy.Matrix.rank(); 0 numpy float-rank on the decisive path (guard PASS); II exact over Q.
  </verify>
  <done>dim Stab_{E_6}(E_11) and the (basepoint, slice)-family dimension modulo it are computed exact over Q (with the single-copy anchor already certifying the method), and the second fundamental form II of the full V_0 slice is computed exact over Q; both give a DECISIVE Route-2 + CALC-02 verdict (transitive/II=0 => KILL; proper-subset/II!=0 => SURVIVES) on the FULL V_0 family, mutually consistent, ready to reconcile against Plan 71-01's Route-1 curvature verdict.</done>
</task>

<task type="auto">
  <name>Task 3: Final two-route reconciliation — emit the DECISIVE Phase-71 homogeneity verdict</name>
  <files>code/bulk_geometry_verification.py, derivations/71-homogeneity-stabilizer.tex</files>
  <action>
    Reconcile the two routes and emit the milestone's KILL-gate verdict. The agreement IS part of the pass condition — this is NOT a rubber stamp.

    1. Load Plan 71-01's Route-1 curvature verdict (SURVIVES/KILL from R(x), K(x) at >= 2 basepoints on the dim-4 h_2(C_u) sub-slice) from the 71-01 SUMMARY / engine output, and this plan's Route-2 stabilizer-transitivity verdict + CALC-02 II verdict (FULL V_0 family).
    2. Confirm the Riemannian-restriction vs Lorentzian-bridge x-variation verdicts agree (no Wick/coordinate artifact) — i.e. the verdict is the same whether read off the Riemannian cone-Hessian restriction or the construction-(ii) Lorentzian slice.
    3. AGREEMENT CHECK (test-two-route-agreement):
       - If Route-1 (curvature), Route-2 (stabilizer transitivity), and CALC-02 (II) ALL give the SAME verdict => emit the FINAL reconciled verdict:
           * all SURVIVES => "Phase A SURVIVES: the inherited slice metric g_mu_nu(x) is genuinely position-dependent. Proceed to Phase 72 (matter-sourcing)." (do NOT auto-proceed; this is the greenlight statement.)
           * all KILL => emit the explicit `Phase A homogeneous -- route dead. STOP.` (milestone complete as a clean valuable KILL; do NOT proceed to Phase 72/73; do NOT relabel — fp-relabel-homogeneous).
       - If they DISAGREE => the plan must STOP and localize the error, NOT report a verdict: curvature-vs-stabilizer disagreement => neither trustworthy (investigate); Riemannian-vs-Lorentzian disagreement => return to Phase 70 (coordinate/Wick artifact). Record the disagreement and the localization, emit NO verdict.
    4. Final forbidden-proxy audit on the emitted verdict: the verdict reads ONLY scalar invariants + exact ranks + the honest II (NOT components — fp-coordinate-curvature); the expansion was in rho_J(X_bg), not x; every decisive value is exact over Q (NOT float — fp-float-decisive); the KILL branch is an explicit STOP (NOT a softened relabel — fp-relabel-homogeneous). Write the FINAL reconciled verdict into derivations/71-homogeneity-stabilizer.tex (include the literal token `two-route agreement`) and add the `main()` gate that prints it.
  </action>
  <verify>
    1. test-two-route-agreement: Route-1 curvature, Route-2 stabilizer transitivity, and CALC-02 II all give the SAME verdict (all KILL or all SURVIVES). The FINAL reconciled verdict is emitted ONLY on agreement.
    2. On disagreement: NO verdict is emitted; the disagreement is recorded and localized (curvature-vs-stabilizer => investigate; Riemannian-vs-Lorentzian => return to Phase 70).
    3. KILL branch emits the explicit 'Phase A homogeneous -- route dead. STOP.' string; SURVIVES branch emits the greenlight statement (without auto-proceeding).
    4. The verdict logic is instrumented against all three forbidden proxies (scalars/ranks/II not components; rho_J not x; exact over Q not float; explicit STOP not relabel).
    5. Engine ALL_PASS exit 0; module-local exact-only source guard PASS (0 octonion_algebra imports, 0 numpy float-rank on the decisive path).
  </verify>
  <done>The FINAL reconciled two-route Phase-71 verdict is emitted: a DECISIVE KILL (explicit 'Phase A homogeneous -- route dead. STOP.', milestone complete) or SURVIVES (position-dependent, greenlight to Phase 72) — but ONLY when Route-1 curvature, Route-2 stabilizer transitivity, and CALC-02 II all AGREE, and the Riemannian/Lorentzian pictures agree. On disagreement, no verdict is emitted and the error is localized. The deliv-phaseA must_contain items (stabilizer dimensions, the reconciled two-route verdict, the explicit STOP on KILL, the cubic-norm reaffirmation) are all satisfied.</done>
</task>

</tasks>

<verification>
- Single-copy F_4 calibration anchor (orbit 24 / Spin(8) 28 / trdeg 3) reproduced FIRST, certifying the orbit/stabilizer builder (test-single-copy-anchor); e_6 dim 78 = 52 + 26 verified over Q (test-e6-dim); cubic-norm SSOT reaffirmed (LOCK 7a/7b).
- dim Stab_{E_6}(E_11) computed exact over Q as ker{D -> D.E_11} (structural sanity: parabolic, Levi ~ Spin(9,1), ~45); the (basepoint, slice)-family dim modulo it computed MAX over >= 2 generic basepoints (test-homogeneity).
- Second fundamental form II of the FULL V_0 slice computed exact over Q; II=0 (totally geodesic => KILL via Gauss) vs II!=0 (=> SURVIVES) decided directly, not by the Table-7 literature inference alone; V_0-inside-larger-geodesic-submanifold ruled out.
- The FINAL verdict is emitted ONLY when Route-1 (curvature, dim-4), Route-2 (stabilizer transitivity, full V_0), and CALC-02 (II, full V_0) AGREE, and the Riemannian/Lorentzian pictures agree (test-two-route-agreement); disagreement => STOP and localize (no verdict).
- All decisive ranks EXACT over Q (exact_qq_rank / sympy.Matrix.rank(), never numpy float-rank); II exact over Q; module-local exact-only source guard PASS; engine ALL_PASS exit 0.
- The three forbidden proxies (fp-relabel-homogeneous, fp-coordinate-curvature, fp-float-decisive) each explicitly designed against in the verdict logic.
</verification>

<success_criteria>
The FINAL reconciled two-route Phase-71 homogeneity verdict, emitted ONLY on agreement of Route-1 (curvature invariants, Plan 71-01), Route-2 (stabilizer transitivity), and CALC-02 (totally-geodesic II) — the latter two on the FULL V_0 (basepoint, slice) family (closing the dim-4-suffices gap) — and on Riemannian/Lorentzian agreement: a DECISIVE KILL (explicit `Phase A homogeneous -- route dead. STOP.`, milestone complete as a clean valuable KILL, do NOT proceed to Phase 72/73) or SURVIVES (genuinely position-dependent, greenlight Phase 72). dim Stab_{E_6}(E_11) and the (basepoint, slice)-family dimension modulo it are computed exact over Q after the single-copy anchor (24 / Spin(8) 28) and e_6 dim 78 calibration gates pass FIRST; II is computed directly (not inferred from Table 7 alone). All three forbidden proxies are explicitly defeated: the verdict reads scalar invariants + exact ranks + the honest II, NOT components (fp-coordinate-curvature); expansion in rho_J(X_bg), NOT x; every decisive value exact over Q, NOT float (fp-float-decisive); the KILL branch is an explicit STOP, NOT a softened relabel (fp-relabel-homogeneous). On any route disagreement, NO verdict is emitted and the error is localized (curvature-vs-stabilizer => investigate; Riemannian-vs-Lorentzian => return to Phase 70).
</success_criteria>

<output>
After completion, create `.gpd/phases/71-a-homogeneity-kill-gate/71-02-SUMMARY.md`.
</output>
