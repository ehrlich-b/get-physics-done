---
phase: 71-a-homogeneity-kill-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - code/bulk_geometry_verification.py
  - derivations/71-homogeneity-curvature.tex
interactive: false

conventions:
  units: "natural (hbar=c=k_B=1); decisive arithmetic EXACT over Q (sympy.Rational/Matrix, DomainMatrix-over-QQ); NEVER numpy float rank or float curvature on the decisive path"
  jordan_product: "(1/2)(XY+YX)"
  octonion: "Fano e1*e2=e4; complex structure u=e7; C_u = span{1,e_7}"
  cubic_norm: "Freudenthal det WITH cross-terms, corrected triple order 2Re((x2 x1) x3); engine det_3 (inv_det_X) is SSOT; NEVER import octonion_algebra.py"
  potential: "Phi = -log det (FIXED); cone metric g_X = Hess(-log det), positive-definite (Riemannian)"
  inverse_metric: "g^{pq} = P(X) = 2 L(X)^2 - L(X^2) (Faraut-Koranyi / McCrimmon quadratic representation); OR substitute matter to small rationals BEFORE inverting; NEVER symbolic g.inv() with matter+coords all symbolic (>200s cliff)"
  signature: "mostly-minus (-,+,+,+) Lorentzian on h_2(C_u) via det_2; ambient h_3(O) cone RIEMANNIAN (positive-definite). Signature bridge = construction (ii): g = eta + h, h := Hess - Hess|center; construction (i) Wick-via-u REJECTED/LOCKED (Phase 70)"
  center: "I/3 (rho_J=0); det(I/3)=1/27; Hess(-log det)|_{I/3} on {x1,x2,x3,x10}=diag(9,9,18,18), det 26244 (Phase-70 regression value)"
  E_11: "diag(1,0,0); Peirce h_3(O)=V_1(1)+V_{1/2}(16)+V_0(10)=h_2(O); spacetime sub-slice engine indices {x1,x2,x3,x10}={17,18,19,26}"
  expansion_variable: "rho_J(X_bg) = off-center-ness of the BASEPOINT, NOT the spacetime coordinate x (which is O(1), the WRONG expansion variable; expanding in x manufactures spurious position-dependence = fp-coordinate-curvature)"
  riemann_sign: "STATE explicitly in code; benchmark on H^3 (constant sectional curvature -d^2/4 = -1, d=2) BEFORE any KILL/SURVIVES verdict is trusted"

dimensional_check:
  R_ricci_scalar: "[length^-2] (curvature dimension); H^3 benchmark exact value -6 (Ricci scalar) / sectional K = -1"
  K_kretschmann: "[length^-4]; H^3 benchmark sectional curvature -1 (Totaro -d^2/4, d=2)"
  curvature_invariants: "PURE NUMBERS over Q at each rational basepoint (real, zero imaginary part — a nonzero imaginary part signals an octonion cross-term association error)"

approximations:
  - name: "Restrict the decisive curvature computation to the dim-4 h_2(C_u) sub-slice"
    parameter: "none (exact points; the dim-4 slice is the decisive arena by design)"
    validity: "the dim-10 V_0 symbolic inverse TIMES OUT (>200s, measured) and is OFF the critical path; the dim-4 h_2(C_u) sub-slice is the tractable decisive arena. The full-V_0 homogeneity question is covered by Plan 71-02 (stabilizer transitivity + II operate on the full V_0 family)."
    breaks_when: "a pathological case where the dim-4 sub-slice is homogeneous while the full dim-10 V_0 is not — MITIGATED by Plan 71-02's full-family routes (two-route cross-check)"
    check: "the H^3=-1 benchmark and the center-regression diag(9,9,18,18)/det 26244 both reproduced on the dim-4 slice BEFORE the verdict"
  - name: "Evaluate curvature invariants at >= 2 distinct generic RATIONAL basepoints (not fully symbolic x)"
    parameter: "none (exact rational points)"
    validity: "always exact over Q; the recommended default (avoids symbolic blow-up / the ~150s executor watchdog). Distinct basepoints = distinct X_bg = I/3 + (different off-center perturbations in V_0/matter directions), each a valid point of the cone."
    breaks_when: "a single basepoint CANNOT detect x-dependence — a constant nonzero curvature is still homogeneous (forbidden proxy fp-coordinate-curvature / single-basepoint trap). MUST use >= 2 distinct generic basepoints."
    check: "report R, K at each basepoint and their exact-over-Q difference; equal across basepoints => KILL, differ => SURVIVES"

contract:
  schema_version: 1
  scope:
    question: "With E_11 = diag(1,0,0) fixed, is the inherited slice metric g_mu_nu(x) = eta_mu_nu + h_mu_nu(x) on the V_0 Peirce slice (decisive arena: the dim-4 h_2(C_u) sub-slice) genuinely POSITION-DEPENDENT (route SURVIVES) or X-INDEPENDENT / HOMOGENEOUS (route DEAD) — decided by exact-over-Q curvature-SCALAR INVARIANTS at >= 2 distinct generic rational basepoints?"
  claims:
    - id: claim-homogeneity
      statement: "With E_11 fixed, the inherited slice metric g_mu_nu(x) is EITHER genuinely position-dependent (route SURVIVES) OR x-independent/homogeneous (route DEAD). This plan advances the claim via Route 1 (the primary curvature verdict): build h_mu_nu(x) on the dim-4 h_2(C_u) sub-slice off-center and decide via exact-over-Q curvature-scalar invariants R(x), K(x) at >= 2 distinct generic rational basepoints; reported honestly either way, a homogeneous result a clean valuable KILL."
      deliverables: [deliv-phaseA, deliv-curvature-note]
      acceptance_tests: [test-homogeneity, test-h3-benchmark, test-center-regression]
      references: [ref-totaro, ref-faraut-koranyi, ref-mccrimmon, ref-52-kkt, ref-warm-engine]
  observables:
    - id: obs-position-dependence
      name: "Position-dependence of slice-metric curvature invariants"
      kind: classification
      definition: "Whether the curvature-scalar invariants R(x), K(x) of the inherited slice metric DIFFER across >= 2 distinct generic rational basepoints (position-dependent => SURVIVES) or are EQUAL (x-independent => KILL)."
      regime: "dim-4 h_2(C_u) sub-slice, off-center expansion in rho_J(X_bg), exact over Q"
  deliverables:
    - id: deliv-phaseA
      kind: derivation
      path: "derivations/"
      description: "SHARED Phase 70+71 deliverable. Phase 70 already COMPLETED & VERIFIED the A0 signature-bridge portion. This plan advances the curvature portion: h_mu_nu(x) computed explicitly on the dim-4 h_2(C_u) sub-slice off-center, and a DECISIVE homogeneous-vs-position-dependent verdict via curvature-scalar invariants R(x), K(x) at >= 2 distinct generic rational basepoints, exact over Q; if homogeneous, an explicit 'route dead' statement and STOP. (The stabilizer-dimension, II, and final two-route reconciliation portions of deliv-phaseA are advanced by Plan 71-02.)"
      must_contain:
        - "h_mu_nu(x) computed explicitly on the dim-4 h_2(C_u) sub-slice (off-center expansion of Hess(-log det))"
        - "DECISIVE homogeneous-vs-position-dependent verdict via curvature-scalar invariants R(x), K(x) at >= 2 distinct generic rational basepoints"
        - "if homogeneous: an explicit 'Phase A homogeneous -- route dead. STOP.' statement"
    - id: deliv-curvature-note
      kind: note
      path: "derivations/71-homogeneity-curvature.tex"
      description: "Route 1 write-up: the off-center slice-metric construction (extending cone_hessian_at_center), the hand-rolled Totaro closed-form Riemann/Ricci/Kretschmann, the H^3=-1 Riemann-sign benchmark, the >= 2-basepoint invariant comparison, and the Route-1 KILL/SURVIVES verdict with the explicit STOP statement on the KILL branch."
      must_contain:
        - "R_ijkl = -(1/4) g^{pq}(f_jlp f_ikq - f_ilp f_jkq)"
        - "rho_J(X_bg)"
        - "Route 1 verdict"
  references:
    - id: ref-totaro
      kind: paper
      locator: "Totaro, B., 'The Curvature of a Hessian Metric,' Int. J. Math. 15 (2004) 369-391; arXiv:math/0401381"
      aliases: ["Totaro 2004", "Hessian-curvature closed form"]
      role: method
      why_it_matters: "THE Hessian-curvature closed-form engine for Route 1: R_ijkl = -(1/4) g^{pq}(f_jlp f_ikq - f_ilp f_jkq), 3rd-derivatives ONLY (det cubic => f_ijkl = 0, the jet terminates), avoiding a sympy.diffgeom blowup on the metric. Gives H^3 constant sectional curvature -d^2/4 = -1 (d=2) — the Riemann-sign benchmark. Plan-LOCAL reference: NOT in the project contract; defined here with full locator."
      applies_to: [claim-homogeneity, deliv-curvature-note]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use, cite]
    - id: ref-faraut-koranyi
      kind: paper
      locator: "Faraut & Koranyi, Analysis on Symmetric Cones (Oxford, 1994), Ch. II-IV"
      aliases: ["FK 1994"]
      role: method
      why_it_matters: "Cone metric g_X = Hess(-log det); inverse metric g^{pq} = P(X) (quadratic representation) — the symbolic inverse without finite-differencing, avoiding the g.inv() blowup. The det=1 hypersurface is the Riemannian symmetric space E_{6(-26)}/F_4 (homogeneous, parallel curvature) — the SOURCE of the homogeneity tension this plan must resolve."
      applies_to: [claim-homogeneity, deliv-phaseA]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [cite, use]
    - id: ref-mccrimmon
      kind: paper
      locator: "McCrimmon, A Taste of Jordan Algebras (Springer, 2004)"
      aliases: ["McCrimmon 2004"]
      role: method
      why_it_matters: "Peirce decomposition; the quadratic representation P(X) = 2 L(X)^2 - L(X^2) used as the inverse metric g^{pq} (built from the existing jordan_L_matrix primitive) to dodge the symbolic g.inv() cliff."
      applies_to: [claim-homogeneity]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [cite]
    - id: ref-52-kkt
      kind: prior_artifact
      locator: "derivations/52-kkt-spacetime.tex + derivations/52-observer-uniqueness.tex"
      aliases: ["52-kkt"]
      role: definition
      why_it_matters: "h_2(C_u) ~ R^{3,1}, mostly-minus eta from det_2; the index map {17,18,19,26} == engine {x1,x2,x3,x10} (confirmed in Phase 70); the det=1 hyperboloid = H^3 = SL(2,C)/SU(2) (the Riemann-sign benchmark target). The frame map (beta,gamma,p,q) -> Minkowski (x0,x1,x2,x3) already lives in the engine."
      applies_to: [claim-homogeneity, deliv-curvature-note]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [read, use, cite]
    - id: ref-warm-engine
      kind: prior_artifact
      locator: "code/bulk_geometry_verification.py (Phase 70 CERTIFIED SSOT; det_3, cone_hessian_at_center, _center_subs, slice_det_form, jordan_L_matrix, inner_derivations, _standard_basis_27) + code/ring_lemma_verification.py (det_3 provenance)"
      aliases: ["warm engine", "bulk_geometry_verification.py"]
      role: benchmark
      why_it_matters: "The certified SSOT det_3 and the cone_hessian_at_center()/_center_subs() machinery this plan EXTENDS for the off-center expansion (swap _center_subs() for a generic-basepoint substitution). The center metric diag(9,9,18,18)/det 26244 is the regression anchor. EXTEND, do NOT rebuild and do NOT import octonion_algebra.py."
      applies_to: [claim-homogeneity, deliv-phaseA, deliv-curvature-note]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use, cite]
  acceptance_tests:
    - id: test-homogeneity
      subject: claim-homogeneity
      kind: oracle
      automation: automated
      procedure: "Build the off-center inherited slice metric g_mu_nu = eta + h on the dim-4 h_2(C_u) sub-slice (extend cone_hessian_at_center with a generic-basepoint substitution; expand in rho_J(X_bg), NEVER in spacetime x). Compute the curvature-SCALAR INVARIANTS R(x) (Ricci scalar) and K(x) (Kretschmann) via the hand-rolled Totaro closed form R_ijkl = -(1/4) g^{pq}(f_jlp f_ikq - f_ilp f_jkq), EXACT over Q (g^{pq}=P(X) or matter-rational-then-invert; never symbolic g.inv() with all symbols). Evaluate at >= 2 distinct generic RATIONAL basepoints; compute the exact-over-Q difference; also test d_x R, d_x K for identical-zero and (optionally) parallel-Riemann nabla R = 0. Assert the test quantity is a SCALAR INVARIANT (not a metric component), is real (zero imaginary part), and that every decisive value is exact over Q (0 numpy float-rank / float-curvature calls on the decisive path)."
      pass_condition: "A DECISIVE Route-1 verdict either way, reported without softening, with exact arithmetic on the decisive comparison: invariants DIFFER across >= 2 basepoints => SURVIVES; invariants EQUAL across >= 2 basepoints => KILL (emit the explicit 'Phase A homogeneous -- route dead. STOP.' string on the KILL branch). Verdict and its exact-over-Q evidence handed to Plan 71-02 for the mandatory two-route cross-check."
      evidence_required: [deliv-phaseA, deliv-curvature-note, ref-totaro, ref-faraut-koranyi]
    - id: test-h3-benchmark
      subject: claim-homogeneity
      kind: limiting_case
      automation: automated
      procedure: "BEFORE any KILL/SURVIVES verdict: compute the constant sectional curvature of the M=0, det=1 sub-slice (H^3 = SL(2,C)/SU(2)) via the SAME hand-rolled Totaro engine on the cone-Hessian slice metric, exact over Q. This pins the Riemann/Ricci SIGN convention on the actual cone-Hessian metric (Phase 70 only reproduced the standard-H^3-metric R=-6/K=-1; this is the cone-Hessian-slice computation Phase 70 deferred)."
      pass_condition: "Constant sectional curvature K = -1 (= Totaro -d^2/4, d=2) exact over Q on the cone-Hessian H^3 slice. If the benchmark does NOT come out -1, the Riemann sign / slice metric is wrong — FIX before any verdict (disconfirming observation, backtracking trigger)."
      evidence_required: [deliv-curvature-note, ref-totaro, ref-52-kkt]
    - id: test-center-regression
      subject: claim-homogeneity
      kind: reproducibility
      automation: automated
      procedure: "Set the new off-center substitution to the center I/3 and confirm the off-center code reduces to the Phase-70 certified center metric. Also re-run the full engine harness."
      pass_condition: "At the center, the restricted cone-Hessian == diag(9,9,18,18), det == 26244 exact over Q (Phase-70 regression value); python3 code/bulk_geometry_verification.py -> ALL_PASS, exit 0; the module-local exact-only source guard reports 0 octonion_algebra imports and 0 numpy float-rank calls on the decisive path."
      evidence_required: [deliv-phaseA, ref-warm-engine]
  forbidden_proxies:
    - id: fp-relabel-homogeneous
      subject: claim-homogeneity
      proxy: "Relabeling an x-independent (equal-invariants) result as 'approximately position-dependent' to avoid the KILL, or softening the verdict."
      reason: "A homogeneous result is a clean, valuable KILL (NEGATIVE-RESULT-IS-SUCCESS) that terminates the milestone. The pass condition is a DECISIVE verdict either way; the KILL branch MUST emit an explicit 'Phase A homogeneous -- route dead. STOP.' string and the milestone halts. No 'approximately position-dependent' language is permitted."
    - id: fp-coordinate-curvature
      subject: claim-homogeneity
      proxy: "Mistaking non-constant metric COMPONENTS in a bad chart for genuine position-dependent curvature; or expanding in the spacetime coordinate x instead of rho_J(X_bg)."
      reason: "Metric components vary by coordinate choice even on a homogeneous space; only curvature-SCALAR INVARIANTS (R, K) decide. The spacetime x is O(1) — expanding in it manufactures spurious 'position dependence.' Expand in rho_J(X_bg) (off-center-ness of the BASEPOINT). Add an explicit assert that the test quantity is a scalar invariant, not a component."
    - id: fp-float-decisive
      subject: claim-homogeneity
      proxy: "Computing the decisive curvature invariants (or any rank) in float."
      reason: "Curvature is dGamma + GammaGamma — a derivative-cancellation factory; float tolerance fabricates the verdict via catastrophic cancellation. Every decisive value EXACT over Q (sympy.Rational/Matrix); float appears only as explicitly-labeled non-decisive triage (e.g. eigenvalue positivity readouts)."
  links:
    - id: link-curvature-verdict
      source: claim-homogeneity
      target: deliv-phaseA
      relation: supports
      verified_by: [test-homogeneity]
    - id: link-curvature-note
      source: deliv-curvature-note
      target: deliv-phaseA
      relation: supports
      verified_by: [test-homogeneity, test-h3-benchmark]
    - id: link-totaro-engine
      source: ref-totaro
      target: deliv-curvature-note
      relation: benchmarks
      verified_by: [test-h3-benchmark]
    - id: link-center-regression
      source: ref-warm-engine
      target: deliv-phaseA
      relation: benchmarks
      verified_by: [test-center-regression]
  uncertainty_markers:
    weakest_anchors:
      - "The dim-4 h_2(C_u) sub-slice is assumed sufficient to decide homogeneity of the full dim-10 V_0 (the dim-10 symbolic inverse times out). MITIGATED, NOT eliminated, here: Plan 71-02's stabilizer-transitivity route AND the II computation operate on the FULL V_0 (basepoint, slice) family, so the decisive weight on the full-slice question rests on the two-route cross-check, not on this dim-4 computation alone."
      - "The Riemann/Ricci SIGN convention on the cone-Hessian metric — pinned by the H^3=-1 benchmark (test-h3-benchmark) computed on the SAME engine BEFORE the verdict. If the benchmark fails, Route 1 is not trustworthy and the verdict must lean on Plan 71-02 (purely algebraic, sign-immune)."
    disconfirming_observations:
      - "The H^3 cone-Hessian benchmark does NOT come out -1 => Riemann sign / slice metric wrong => FIX before any verdict (do not report a Route-1 verdict)."
      - "A curvature invariant with a NONZERO IMAGINARY PART => an octonion cross-term association error has corrupted the Hessian (check det_3 SSOT provenance; never octonion_algebra.py)."
      - "The off-center code does NOT reduce to diag(9,9,18,18)/det 26244 at the center => the generic-basepoint substitution or the off-center expansion is wrong."
      - "A 'position-dependent' result that only appears when expanding in spacetime x (not rho_J(X_bg)) => fp-coordinate-curvature; the wrong expansion variable."
      - "det(metric) = 0 at a chosen basepoint => degenerate (likely on the light cone) => choose a generic timelike/spacelike basepoint."
---

<objective>
Route 1 — the PRIMARY KILL verdict of Phase 71 (the milestone's decisive homogeneity gate). With the primitive idempotent E_11 = diag(1,0,0) fixed, build the inherited slice metric g_mu_nu(x) = eta_mu_nu + h_mu_nu(x) on the dim-4 h_2(C_u) sub-slice off-center, then decide via exact-over-Q curvature-SCALAR INVARIANTS R(x), K(x) at >= 2 distinct generic rational basepoints whether the metric is genuinely POSITION-DEPENDENT (route SURVIVES) or X-INDEPENDENT / HOMOGENEOUS (route DEAD — a clean valuable KILL, milestone STOPS).

Purpose: This is the cheapest, most decisive gate of milestone v17.0. Fixing E_11 breaks E_6 -> Stab_{E_6}(E_11); the route is alive iff that residual group does NOT act transitively enough to make all (basepoint, slice) pairs isometric — equivalently iff the inherited slice metric has genuinely position-dependent curvature invariants. A homogeneous result KILLs everything downstream (Phases 72/73 are conditional on SURVIVES); a position-dependent result greenlights matter-sourcing. Route 1 (this plan) produces the primary curvature verdict; Plan 71-02 produces the mandatory stabilizer-transitivity cross-check + the totally-geodesic II shortcut and emits the final reconciled two-route verdict.

Output: the off-center slice-metric + hand-rolled Totaro curvature engine ADDED to the certified code/bulk_geometry_verification.py; the Route-1 KILL/SURVIVES verdict (exact over Q, >= 2 basepoints); derivations/71-homogeneity-curvature.tex documenting the construction, the H^3=-1 Riemann-sign benchmark, and the verdict.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/CONVENTIONS.md
@.gpd/phases/71-a-homogeneity-kill-gate/71-RESEARCH.md
@.gpd/phases/70-a0-engine-reconciliation-signature-bridge/70-02-SUMMARY.md
@code/bulk_geometry_verification.py
@derivations/70-signature-bridge.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Riemann-sign benchmark — reproduce H^3 = -1 on the cone-Hessian slice (HARD GATE, do FIRST)</name>
  <files>code/bulk_geometry_verification.py, derivations/71-homogeneity-curvature.tex</files>
  <action>
    Implement the hand-rolled Totaro Hessian-curvature engine and validate it on the H^3 benchmark BEFORE building anything off-center. This pins the Riemann/Ricci SIGN convention on the ACTUAL cone-Hessian metric (Phase 70 only did the standard-H^3-metric reinforcement R=-6/K=-1; the cone-Hessian-slice computation was explicitly deferred to this phase).

    1. Add a function `totaro_riemann(g, ginv, f3, coords)` to code/bulk_geometry_verification.py implementing the Totaro closed form EXACT over Q (ref-totaro, arXiv:math/0401381):
         R_ijkl = -(1/4) sum_{p,q} g^{pq} ( f_jlp f_ikq - f_ilp f_jkq )
       where f3[i][j][k] = d^3(-log det)/dx_i dx_j dx_k is the 3rd-derivative tensor (the det is cubic so the -log jet terminates: f_ijkl = 0 — this is WHY Totaro's 3rd-derivative-only form is exact and cheap, NOT a sympy.diffgeom blowup). Do NOT call sympy.diffgeom on the metric. STATE the Riemann/Ricci sign convention explicitly in the docstring (which index ordering, which sign of R = g^{ik} g^{jl} R_ijkl etc.).
    2. Build the Ricci scalar R = g^{ik} g^{jl} R_ijkl and the Kretschmann scalar K = R_{ijkl} R^{ijkl} (raise indices with g^{pq}), both EXACT over Q. For the inverse, use g^{pq} = P(X) = 2 L(X)^2 - L(X^2) (built from the existing `jordan_L_matrix`, ref-mccrimmon / ref-faraut-koranyi) OR substitute matter to small rationals BEFORE inverting; NEVER symbolic g.inv() with all symbols (>200s cliff, measured).
    3. Benchmark on H^3: take the M=0, det=1 sub-slice (H^3 = SL(2,C)/SU(2)) of the cone-Hessian metric and compute its constant sectional curvature K via this same engine, exact over Q. (You may reuse the existing `h3_constant_curvature()` standard-metric reinforcement as a cross-check, but the DECISIVE benchmark is on the cone-Hessian slice metric, not the round-metric chart.)
    4. Write the engine section and the H^3 benchmark into derivations/71-homogeneity-curvature.tex (include the literal token `R_ijkl = -(1/4) g^{pq}(f_jlp f_ikq - f_ilp f_jkq)`), and add a gate to `main()` printing PASS/FAIL.

    EXECUTION DISCIPLINE (executor watchdog, ~150s silent-compute hard kill; background resume has stalled — see project memory): run foreground `python3 -u code/bulk_geometry_verification.py`; print progress between heavy steps (e.g. "f3 built", "ginv built", "Riem(i,j,k,l) done"); keep each decisive symbolic step under ~150s (matter-rational + `cancel`-per-entry stays well inside). If a step risks exceeding this, evaluate at a rational point instead of fully symbolic.
  </action>
  <verify>
    1. Riemann-sign benchmark (test-h3-benchmark): constant sectional curvature K = -1 (= Totaro -d^2/4, d=2) on the cone-Hessian H^3 slice, EXACT over Q (sympy simplify(K - (-1)) == 0). If NOT -1, STOP and fix the Riemann sign / slice metric before any verdict (disconfirming observation).
    2. The curvature engine returns REAL values (zero imaginary part) — a nonzero imaginary part signals an octonion cross-term association error (check det_3 SSOT provenance; never octonion_algebra.py).
    3. f_ijkl == 0 confirmed (the -log det jet terminates — sanity that the cubic-norm structure is respected).
    4. Decisive arithmetic exact over Q: 0 numpy float-rank, 0 float-curvature calls on the decisive path (extend the module-local exact-only source guard `exact_only_guard_p70`).
  </verify>
  <done>The hand-rolled Totaro curvature engine is added to the certified engine with an explicitly-stated Riemann/Ricci sign convention; the H^3 cone-Hessian benchmark reproduces constant sectional curvature -1 exact over Q; the engine still runs ALL_PASS exit 0. The Riemann sign is now pinned BEFORE any KILL/SURVIVES verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Build the off-center inherited slice metric h_mu_nu(x) on the dim-4 h_2(C_u) sub-slice</name>
  <files>code/bulk_geometry_verification.py</files>
  <action>
    EXTEND the certified `cone_hessian_at_center()` to an off-center expansion — a TRIVIAL extension (swap the center substitution for a generic-basepoint substitution); do NOT rebuild and do NOT import octonion_algebra.py.

    1. Add `_offcenter_subs(X_bg)` (parallel to `_center_subs()`): a substitution dict sending the 27 engine-native X-coords to a generic basepoint X_bg = I/3 + (off-center perturbation), keeping the 4 spacetime sub-slice coords {x1,x2,x3,x10} = {beta,gamma,p,q} symbolic. CRITICAL: the perturbation parameterizes rho_J(X_bg) (off-center-ness of the BASEPOINT), NOT the spacetime coordinate x. Mis-choosing the variable is fp-coordinate-curvature. Document this explicitly in the docstring.
    2. Add `cone_hessian_offcenter(X_bg, slice_order=[1,2,3,10])`: the 4x4 cone-Hessian Hess(-log det) restricted to {x1,x2,x3,x10}, evaluated at the off-center basepoint X_bg, EXACT over Q. (Same structure as cone_hessian_at_center but using _offcenter_subs.)
    3. Build the inherited slice metric per the LOCKED construction (ii) bridge (Phase 70): g_mu_nu = eta_mu_nu + h_mu_nu, with eta = diag(+1,-1,-1,-1) (the 52-kkt det_2 background) and h_mu_nu := [restricted cone-Hessian at X_bg] - [its value at (M=0, center I/3)]. Map to the Minkowski (x0,x1,x2,x3) frame with the existing `_frame_jacobian_bg_to_mink()`. Compute the FINAL Lorentzian metric directly (never "rotate the answer" — Pitfall 3 / fp Wick artifact).
    4. CENTER REGRESSION (test-center-regression): assert that setting X_bg -> I/3 makes cone_hessian_offcenter reduce to the Phase-70 center metric diag(9,9,18,18), det 26244, exact over Q. Add this as a gate in `main()`.

    EXECUTION DISCIPLINE: foreground `python3 -u`, progress prints, each symbolic step under ~150s (matter/perturbation rational where possible). Use g^{pq}=P(X) or matter-rational-then-invert; never symbolic g.inv() with all symbols.
  </action>
  <verify>
    1. Center regression (test-center-regression): cone_hessian_offcenter(X_bg=I/3) == diag(9,9,18,18), det == 26244 exact over Q (matches Phase-70 certified value). If NOT, the off-center substitution / expansion is wrong (disconfirming observation).
    2. The expansion variable is rho_J(X_bg) (basepoint off-center-ness), NOT spacetime x — verify by inspection of _offcenter_subs that the perturbation enters the diagonal/off-diagonal background, while {beta,gamma,p,q} remain the free slice coordinates.
    3. h_mu_nu(center) == 0 (4x4 zero) by the centered-subtraction construction (consistency with Phase 70 construction (ii)).
    4. The metric is built on the SSOT det_3 (inv_det_X); no octonion_algebra import (guard PASS).
  </verify>
  <done>The off-center inherited slice metric g_mu_nu(x) = eta + h_mu_nu(x) is built on the dim-4 h_2(C_u) sub-slice by extending the certified engine; it reduces to the Phase-70 center metric diag(9,9,18,18)/26244 at the center; the expansion is in rho_J(X_bg), not spacetime x; h_mu_nu(x) is the explicit object whose curvature invariants the next task tests.</done>
</task>

<task type="auto">
  <name>Task 3: Compute R(x), K(x) at >= 2 distinct generic rational basepoints — the Route-1 KILL verdict</name>
  <files>code/bulk_geometry_verification.py, derivations/71-homogeneity-curvature.tex</files>
  <action>
    Decide homogeneity from coordinate-INVARIANT curvature scalars (NOT metric components, NOT a single basepoint), EXACT over Q.

    1. Choose >= 2 DISTINCT generic RATIONAL basepoints X_bg^(1), X_bg^(2) (distinct off-center perturbations of I/3; genuinely octonionic where the slice allows — at least populate the C_u off-diagonal; ensure det(metric) != 0 at each, i.e. avoid the light cone — pick generic timelike/spacelike points). Rational-basepoint evaluation is the recommended default (exact, decisive, avoids symbolic blow-up / the watchdog).
    2. For EACH basepoint: build g_mu_nu(x) (Task 2), compute R(x) (Ricci scalar) and K(x) (Kretschmann) via the Totaro engine (Task 1), EXACT over Q. Keep the 4 slice coords {beta,gamma,p,q} symbolic if feasible, else evaluate the invariants at the same symbolic-slice expression for both basepoints. Use g^{pq}=P(X) or matter-rational-then-invert for the inverse.
    3. COMPARE: compute the exact-over-Q differences R^(1) - R^(2) and K^(1) - K^(2). Also compute d_x R and d_x K and test for identical-zero, and (optionally, if cheap) test parallel-Riemann nabla R = 0 as a homogeneity diagnostic.
    4. VERDICT (instrumented against all three forbidden proxies):
       - invariants DIFFER across basepoints (R^(1) != R^(2) or K^(1) != K^(2) exact over Q) => Route-1 verdict = SURVIVES (genuinely position-dependent).
       - invariants EQUAL across basepoints => Route-1 verdict = KILL; emit the explicit string `Phase A homogeneous -- route dead. STOP.` (fp-relabel-homogeneous: no 'approximately position-dependent' softening).
       - ASSERT the decisive test quantity is a SCALAR INVARIANT, not a metric component (fp-coordinate-curvature); ASSERT the expansion was in rho_J(X_bg), not spacetime x; ASSERT 0 float-rank / 0 float-curvature on the decisive path (fp-float-decisive — extend exact_only_guard_p70).
    5. Write the >= 2-basepoint comparison, the verdict, and (on the KILL branch) the explicit STOP statement into derivations/71-homogeneity-curvature.tex (include the literal tokens `rho_J(X_bg)` and `Route 1 verdict`). Add a `main()` gate printing the Route-1 verdict and its exact-over-Q evidence (the verdict is consumed by Plan 71-02 for the two-route cross-check).

    EXECUTION DISCIPLINE: foreground `python3 -u`, progress prints between basepoints and between R/K, each step under ~150s; rational basepoints by default to stay inside the watchdog.
  </action>
  <verify>
    1. Route-1 verdict (test-homogeneity): a DECISIVE KILL or SURVIVES from the exact-over-Q equality/inequality of R, K at >= 2 distinct generic rational basepoints. Reported without softening. (KILL branch emits the explicit 'Phase A homogeneous -- route dead. STOP.' string.)
    2. >= 2 distinct basepoints actually used (single-basepoint sampling is the forbidden trap: a constant nonzero curvature is still homogeneous). det(metric) != 0 at each basepoint.
    3. The decisive comparison is on SCALAR INVARIANTS R, K (assert), not metric components (fp-coordinate-curvature); expansion in rho_J(X_bg), not x.
    4. Exact over Q: simplify(R^(1) - R^(2)) and simplify(K^(1) - K^(2)) are exact sympy expressions/rationals; 0 numpy float-rank, 0 float-curvature on the decisive path (guard PASS).
    5. R, K are REAL (zero imaginary part) at each basepoint.
  </verify>
  <done>R(x), K(x) computed exact over Q at >= 2 distinct generic rational basepoints; the exact-over-Q comparison yields a DECISIVE Route-1 verdict (SURVIVES if invariants differ, KILL with an explicit STOP string if equal), reported without softening; the verdict + its exact evidence are recorded in the engine output and the .tex note, ready for Plan 71-02's mandatory two-route cross-check. All three forbidden proxies are explicitly designed against (decisive scalars not components; rho_J not x; exact over Q not float).</done>
</task>

</tasks>

<verification>
- Riemann-sign benchmark on the cone-Hessian H^3 slice gives constant sectional curvature -1 (Totaro -d^2/4, d=2) EXACT over Q, BEFORE any verdict — pins the sign convention (test-h3-benchmark).
- The off-center slice metric reduces to the Phase-70 certified center metric diag(9,9,18,18)/det 26244 at the center (test-center-regression); engine ALL_PASS exit 0 preserved.
- The Route-1 KILL/SURVIVES verdict rests on coordinate-INVARIANT curvature scalars R(x), K(x) at >= 2 distinct generic rational basepoints, EXACT over Q (test-homogeneity) — never metric components, never a single basepoint, never float, never spacetime-x expansion.
- Curvature invariants are REAL (zero imaginary part) — guards against octonion cross-term association errors.
- All decisive geometry built ONLY on the SSOT det_3; module-local exact-only source guard reports 0 octonion_algebra imports and 0 numpy float-rank / float-curvature calls on the decisive path.
- The three forbidden proxies (fp-relabel-homogeneous, fp-coordinate-curvature, fp-float-decisive) are each explicitly designed against in the verdict logic.
</verification>

<success_criteria>
A DECISIVE Route-1 homogeneity verdict, reported without softening: invariants DIFFER across >= 2 distinct generic rational basepoints => SURVIVES; invariants EQUAL => KILL with an explicit `Phase A homogeneous -- route dead. STOP.` statement. The verdict is computed from exact-over-Q curvature-SCALAR INVARIANTS R(x), K(x) (NOT metric components — defeating fp-coordinate-curvature), expanded in rho_J(X_bg) (NOT spacetime x — defeating fp-coordinate-curvature), with every decisive value exact over Q (NOT float — defeating fp-float-decisive), and the KILL branch emitting the explicit STOP string (NOT a softened relabel — defeating fp-relabel-homogeneous). The H^3=-1 cone-Hessian benchmark and the diag(9,9,18,18)/26244 center regression both pass FIRST. The verdict and its exact-over-Q evidence are handed to Plan 71-02 for the mandatory stabilizer-transitivity + II cross-check (the two routes MUST AGREE; the agreement is part of Plan 71-02's pass condition).
</success_criteria>

<output>
After completion, create `.gpd/phases/71-a-homogeneity-kill-gate/71-01-SUMMARY.md`.
</output>
