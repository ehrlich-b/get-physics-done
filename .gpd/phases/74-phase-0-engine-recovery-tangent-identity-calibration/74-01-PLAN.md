---
phase: 74-phase-0-engine-recovery-tangent-identity-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - code/cartan_phase0_tangent.py
  - derivations/74-phase0-engine-tangent-calibration.tex
interactive: false

conventions:
  units: "natural (hbar=k_B=1)"
  metric: "(-,+,+,+) mostly-minus on the h_2(C_u) det_2 Lorentzian slice; bulk cone Riemannian (positive-definite)"
  arithmetic: "EXACT over Q (sympy.Matrix.rank/.nullspace/.eigenvals only; NEVER numpy/float on any decisive path)"
  octonion_multiplication: "Fano e1 e2 = e4 (v17.0 lock)"
  complex_structure: "u = e7"
  primitive_idempotent: "E_11 = diag(1,0,0)"
  det_ssot: "ring_lemma_verification.py det_3, cross-term 2Re(x2* x0* x1) (F_4-invariant); the cyclic-rotation 2Re((x1 x2)x3) variant is the documented bug (off by 16, REJECTED)"
  peirce_layout_27: "V_1={0} (alpha); V_0={1..10}; V_{1/2}=engine indices {11..26} (oct-x2, oct-x3); V_HALF_IDX = list(range(11,27))"

dimensional_check:
  T_E11_OP2: "[dimensionless count] = 16 (exact integer, NOT 'approximately 16')"
  dim_f_4: "[dimensionless count] = 52 (= 324/324 inner-derivation annihilation)"
  K_cone_hessian: "[1/length^2] = -1/2 exactly (sign load-bearing; magnitude is the g_slice|_apex = 2*g_round normalization; round-H^3 K = -1)"
  e_6: "[dimensionless count] = 78 = 52 + 26"
  orbit_E_11: "[dimensionless count] = 17 (affine cone over OP^2; projective tangent = 17 - 1 = 16)"

approximations: []

contract:
  schema_version: 1
  scope:
    question: "Before any geometry is built on it, is the inherited h_3(O) computational engine re-certified (det SSOT CH + 324/324; octonion_algebra.py absent on the decisive path), is the load-bearing soldering-form fact T_{E_11}OP^2 = V_{1/2}(E_11) (16-dim, with E_11 o delta = (1/2)delta) established EXACTLY over Q, and are the v17.0 calibration anchors (single-copy 24/Spin(8)=28/trdeg 3; e_6=78; orbit(E_11)=17; Stab_{E_6}(E_11)=61; Stab_{V_0}=45=Spin(9,1)) plus the K=-1/2 sign benchmark reproduced exactly?"
  claims:
    - id: claim-coframe-reduction
      statement: "Phase 0 SUPPLIES the two consistency anchors of claim-coframe-reduction (it does NOT perform the Phase-75 coframe reduction): (i) the det SSOT re-passes CH + 324/324 with octonion_algebra.py absent on the decisive path; (ii) E_11 o delta = (1/2)delta exactly over Q for a V_{1/2}(E_11) basis AND the tangent space to the primitive-idempotent variety at E_11 equals V_{1/2}(E_11) of dimension 16; (iii) the calibration anchors and the K=-1/2 sign benchmark reproduce exactly."
      deliverables: [deliv-phase0]
      acceptance_tests: [test-tangent-identity, test-calibration]
      references: [ref-baez-octonions, ref-mccrimmon, ref-52-kkt, ref-ring-lemma-engine, ref-orbit-gate, ref-bulk-geometry-prior]
  deliverables:
    - id: deliv-phase0
      kind: derivation
      path: derivations/74-phase0-engine-tangent-calibration.tex
      description: "Phase 0: engine recovery (det SSOT re-passing CH + 324/324; octonion_algebra.py confirmed absent on the decisive path) + the EXACT tangent identity E_11 o delta = (1/2)delta and T_{E_11}OP^2 = V_{1/2}(16) + the reproduced calibration anchors, with supporting exact-SymPy code (code/cartan_phase0_tangent.py for the new Jacobian-kernel check + captured engine outputs)."
      must_contain:
        - "det SSOT (ring_lemma_verification.py det_3) re-passes Cayley-Hamilton + 324/324 inner-derivation annihilation (= dim f_4 = 52); octonion_algebra.py confirmed absent on the decisive path (source guard: 0 outside-fence imports, 0 float-rank calls)"
        - "exact-over-Q verification of E_11 o delta = (1/2)delta for every delta in a V_{1/2}(E_11) basis, and that T_{E_11}OP^2 = V_{1/2}(E_11) is 16-dim (Jacobian rank 11 = 27-16; kernel == span(V_HALF_IDX)); the 16 = 17-1 cross-check against orbit(E_11)=17"
        - "calibration anchors reproduced via orbit_dimension_gate.py (single-copy 24 / Spin(8)=28 / trdeg 3) and bulk_geometry_verification.py (e_6=78=52+26; orbit(E_11)=17; Stab_{E_6}(E_11)=61; Stab_{V_0}=45=Spin(9,1)); K=-1/2 cone-Hessian sign benchmark with round-K=-1 factor-2 cross-check"
  references:
    - id: ref-baez-octonions
      kind: paper
      locator: "Baez, The Octonions (Bull. AMS 39, 2002), Section 3.4 -- h_3(O), F_4, OP^2 = F_4/Spin(9), T_E OP^2 = V_{1/2}"
      aliases: ["Baez 2002"]
      role: definition
      why_it_matters: "Canonical reference: OP^2 = F_4/Spin(9) (16-dim) and the identification of the tangent space with the Peirce half-eigenspace V_{1/2}(E)."
      applies_to: [claim-coframe-reduction]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-mccrimmon
      kind: paper
      locator: "McCrimmon, A Taste of Jordan Algebras (Springer, 2004) -- Peirce decomposition, primitive idempotents, the E o delta = (1/2)delta tangent identity"
      aliases: ["McCrimmon"]
      role: definition
      why_it_matters: "Source of the load-bearing tangent identity E o delta = (1/2)delta and the Peirce-decomposition backbone of the soldering form."
      applies_to: [claim-coframe-reduction]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-52-kkt
      kind: prior_artifact
      locator: "derivations/52-kkt-spacetime.tex + derivations/52-observer-uniqueness.tex -- h_2(C_u) ~ R^{3,1} Minkowski signature (1,3) from det, conformal so(4,2), and the Phase-46 pi_u O->C bottleneck"
      aliases: ["52-kkt-spacetime", "52-observer-uniqueness"]
      role: benchmark
      why_it_matters: "Establishes the Lorentzian slice and the C_u reduction mechanism that Phase 75 will reuse on V_{1/2}; here it is the benchmark for which the Phase-0 tangent identity is the prerequisite (do NOT perform the reduction in Phase 0)."
      applies_to: [claim-coframe-reduction]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [read, use]
    - id: ref-ring-lemma-engine
      kind: prior_artifact
      locator: "code/ring_lemma_verification.py -- det SSOT (F_4-invariant cubic norm det_3, cross-term 2Re(x2* x0* x1); CH generic-norm LOCK 7a; 324/324 inner-derivation annihilation LOCK 7b = dim f_4 = 52; exact_only_guard())"
      aliases: ["ring_lemma_verification.py", "det SSOT"]
      role: benchmark
      why_it_matters: "DERV-01: the det single-source-of-truth and the warm exact-Q h_3(O) engine to inherit verbatim; re-run and assert ALL_PASS exit 0 + the source guard. Do NOT rebuild det_3; octonion_algebra.py is BANNED on the decisive path."
      applies_to: [claim-coframe-reduction]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [use, compare]
    - id: ref-orbit-gate
      kind: prior_artifact
      locator: "code/orbit_dimension_gate.py -- single-copy orbit 24 / Spin(8)=28 / trdeg 3 calibration; exact_qq_rank/span_rank_over_QQ; the GATE hard-fails if orbit_dim != 24"
      aliases: ["orbit_dimension_gate.py"]
      role: benchmark
      why_it_matters: "VALD-01: reproduce the single-copy calibration anchor (computed in-engine, never looked up) before trusting any downstream structure-group count."
      applies_to: [claim-coframe-reduction]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [use]
    - id: ref-bulk-geometry-prior
      kind: prior_artifact
      locator: "code/bulk_geometry_verification.py -- peirce_indices_under_E11() (line 1753; V_{1/2} basis + half-eigenvalue), e6_dimension=78, stab_E6_E11=61 with orbit(E_11)=17, stab_preserving_V0=45=Spin(9,1), the K=-1/2 sectional-curvature benchmark (round-K=-1 factor-2 cross-check); the engine building blocks _standard_basis_27/_flat27/jordan/octmat_*"
      aliases: ["bulk_geometry_verification.py", "peirce_indices_under_E11"]
      role: benchmark
      why_it_matters: "DERV-02: the V_{1/2} half-eigenvalue split + the engine primitives for the Jacobian-kernel tangent check. VALD-01: the e_6/stabilizer anchors + the K=-1/2 sign benchmark. STAY DISTINCT -- the cone-Hessian curvature is the dead v17.0 real-part sector, used here only as the calibration/sign benchmark."
      applies_to: [claim-coframe-reduction]
      carry_forward_to: [planning, execution, verification]
      must_surface: true
      required_actions: [use, compare]
  acceptance_tests:
    - id: test-tangent-identity
      subject: claim-coframe-reduction
      kind: consistency
      procedure: "On the det SSOT engine, verify exactly over Q that E_11 o delta = (1/2)delta for a basis of delta in V_{1/2}(E_11), and that the tangent space to the primitive-idempotent variety {X o X = X} at E_11 (kernel of the 27x27 Jacobian of F(X)=X o X - X) is exactly V_{1/2}(E_11) (16-dim)."
      pass_condition: "E_11 o delta = (1/2)delta holds (exact equality over Q) for all 16 V_{1/2} basis elements; J.rank()==11, dim ker == 16, and rank[ker | V_HALF_IDX-basis]==16 (kernel == span of V_{1/2}); 16 == 17 - 1 against orbit(E_11)=17. Reported as exact equalities, not 'approximately'."
      evidence_required: [deliv-phase0]
      automation: automated
    - id: test-calibration
      subject: claim-coframe-reduction
      kind: consistency
      procedure: "Reproduce the calibration anchors via orbit_dimension_gate.py and the det SSOT: single-copy dim 24 / Spin(8)=28 / trdeg 3; e_6=78=52+26; orbit(E_11)=17; Stab_{E_6}(E_11)=61; Stab_{V_0}=45=Spin(9,1); det_3 re-passes CH + 324/324; and the K=-1/2 cone-Hessian sign benchmark (round-K=-1 factor-2 cross-check)."
      pass_condition: "All calibration anchors reproduced exactly (24, 28, 3, 78, 17, 61, 45 as exact integers; K=-1/2 and round_K=-1 as exact rationals); det SSOT ALL_PASS (exit 0); octonion_algebra.py absent on the decisive path (source guard: 0 outside-fence imports, 0 float-rank calls)."
      evidence_required: [deliv-phase0]
      automation: automated
  forbidden_proxies:
    - id: fp-octonion-algebra
      subject: claim-coframe-reduction
      proxy: "Using octonion_algebra.py (buggy (x1 x2)x3 association, float, ~0.67 associator gap) on any decisive path -- e.g. importing it 'just for the octonion product' or the det."
      reason: "The wrong associator silently corrupts det_3 and every downstream curvature; it is annihilated by only 30 of 324 inner derivations (NOT F_4-invariant). The det SSOT is ring_lemma_verification.py det_3; the source guard must report 0 outside-fence octonion_algebra imports on the decisive path."
    - id: fp-float-decisive
      subject: claim-coframe-reduction
      proxy: "Any decisive verdict (Jacobian rank, nullspace dimension, kernel identity, orbit/stabilizer dimension, signature, K) resting on numpy/floating-point rank or eigenvalue."
      reason: "Rank/signature/curvature involve derivative cancellations; float tolerance fabricates or destroys a rank. Decisive checks must be exact over Q (sympy.Matrix.rank/.nullspace/.eigenvals, never numpy). mpmath is allowed for triage only, never a verdict."
    - id: fp-relabel-approx
      subject: claim-coframe-reduction
      proxy: "Reporting the Phase-0 verdicts (16-dim tangent, dim f_4=52 via 324/324, K=-1/2, the calibration integers) as 'approximately' or with a tolerance instead of as exact rational equalities."
      reason: "Negative-result-is-success discipline (fp-relabel-approx-4d) extends to the foundation: every Phase-0 fact is an EXACT-over-Q equality. An 'approximately 16' tangent or a tolerance-pass det would silently soften the foundation the whole milestone stands on."
  uncertainty_markers:
    weakest_anchors:
      - "That the warm v16.0/v17.0 engines still pass byte-identically in the current env (Python 3.14.2 / SymPy 1.14.0). DERV-01 was re-confirmed ALL_PASS this session (~1.5s) and the DERV-02 Jacobian-kernel validated live; the two slow calibration engines (orbit_dimension_gate.py ~1-2 min, bulk_geometry_verification.py several min) are deterministic and byte-identical to the v17.0 anchors but MUST be run-and-asserted, not assumed."
      - "The X o X = X equation cuts out ALL idempotents (ranks 0..3); the Zariski tangent at E_11 is claimed to be exactly the 16-dim V_{1/2} (E_11 is a smooth point of the rank-1 stratum). Validated live, and cross-checked by orbit(E_11)=17 => projective tangent 17-1=16; the optional X#=0 sharp-equation cut is the referee-proof confirmation if demanded."
    disconfirming_observations:
      - "ring_lemma_verification.py exits nonzero or prints 'FAILURES PRESENT', OR the 324/324 count comes back < 324 (e.g. 30) -> the buggy (x1 x2)x3 association has crept in / the engine drifted from the v17.0 SSOT; STOP and diff against the warm commit, do NOT patch around it."
      - "The Jacobian nullspace is 16-dim but NOT equal to span(V_HALF_IDX) (nonzero components in V_0 indices {1..10} or the V_1 index {0}) -> a basis/layout bug or a wrong E_11; the soldering-form geometric claim is false as stated until resolved."
      - "K_value comes back +1/2 or -1 (instead of -1/2), OR orbit(E_11) != 17 / Stab_{V_0} != 45 -> a sign/normalization slip or an e_6/stabilizer regression; pin/fix before any downstream phase reads a curvature sign or structure-group count."
      - "A float appears in any rank/eigenvalue/zero-test on the decisive path -> fp-float-decisive; remove it (SymPy/QQ only)."
---

<objective>
Phase 0 of the v18.0 Cartan/MacDowell-Mansouri milestone: re-certify the inherited h_3(O) computational engine and establish, EXACTLY over Q, the single load-bearing geometric fact on which the entire milestone (Phases 75-78) stands -- that the soldering form dE is V_{1/2}(E_11)-valued, i.e. T_{E_11}OP^2 = V_{1/2}(E_11) is 16-dimensional. This phase does NOT perform the C_u/pi_u coframe reduction (Phase 75), the Berry curvature (76), the Cartan connection (77), or the circularity audit (78); it SUPPLIES the two consistency anchors (test-tangent-identity, test-calibration) those phases depend on.

This is a re-certification + calibration phase, not a discovery phase. Two of the three requirements (DERV-01 engine recovery; VALD-01 calibration) are RE-PASS gates over byte-identical v16.0/v17.0 SSOT engines. The one genuinely research-bearing piece is the second half of DERV-02: proving over Q that the Zariski tangent space to the primitive-idempotent variety {X o X = X} at E_11 equals V_{1/2}(E_11) (validated live this session: Jacobian rank 11, kernel 16 = V_{1/2}).

Purpose: a weaker rigor here would propagate silently into every later curvature verdict. The det SSOT (DERV-01) underlies every downstream curvature; the V_{1/2}-valued soldering form (DERV-02) is the geometric premise of the coframe reduction, the Berry curvature, and the Cartan connection; the calibration anchors and the K=-1/2 sign benchmark (VALD-01) pin the structure-group counts and the curvature sign BEFORE any downstream phase reads them.
Output: derivations/74-phase0-engine-tangent-calibration.tex (the deliverable, stating the three results with the convention lock and captured exact-over-Q engine outputs) + code/cartan_phase0_tangent.py (the ~15-line new Jacobian-kernel tangent check + a thin driver that re-runs the three engines and asserts the anchors).
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/phases/74-phase-0-engine-recovery-tangent-identity-calibration/74-RESEARCH.md
@code/ring_lemma_verification.py
@code/orbit_dimension_gate.py
@code/bulk_geometry_verification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1 (DERV-01): Re-certify the det SSOT engine and the source guard</name>
  <files>code/cartan_phase0_tangent.py</files>
  <action>
    Re-pass the det SSOT engine. Do NOT rebuild det_3, jordan, or the octonion product -- inherit verbatim. The correct det_3 cross-term is `2Re(x2* x0* x1)`; the "natural" cyclic `2Re((x1 x2)x3)` is the DOCUMENTED bug (off by 16, annihilated by only 30/324 inner derivations) and is REJECTED.

    1. Run the engine UNBUFFERED and capture its full output and exit code:
       `python -u code/ring_lemma_verification.py`
       (Pitfall 3: exact-QQ runs buffer stdout when not a TTY; `-u` keeps progress visible and avoids a watchdog mistaking buffered silence for a stall. This engine is fast, ~1.5s.)
    2. Assert exit code == 0 and that the output contains "ALL_PASS" (the engine exits 0 iff every convention lock and every guard passes; nonzero on any failure).
    3. Capture and record the three load-bearing lines:
       - LOCK 7a: det_3 == Cayley-Hamilton generic norm of jordan (the det_3 = unique cubic norm N(X) from X^o3 - Tr(X)X^o2 + S(X)X - N*I).
       - LOCK 7b: 324/324 inner-derivation annihilation D_xi det_3 = 0 for all 324 brackets [L_a,L_b] (= dim f_4 = 52), i.e. det_3 is F_4-invariant.
       - exact-only guard line: `exact_only_guard()` must report "octonion_algebra imports: 1 in-fence (expect 1), 0 outside (expect 0); float-rank calls: 0 (expect 0)". The 1 in-fence import is the single SANCTIONED ORACLE-FENCE touch (a float reference cross-check, NOT on the decisive path); the load-bearing assertion is `0 outside-fence` and `0 float-rank calls`.
    4. In code/cartan_phase0_tangent.py, add a Phase-0 driver section (top of file) that subprocess-runs the engine with `-u`, asserts exit 0 + "ALL_PASS" in the captured stdout, and re-greps the captured guard line for "0 outside" and "float-rank calls: 0", raising AssertionError on any mismatch. Print a single "DERV-01 PASS" summary line echoing the three captured facts.

    RED FLAG / STOP condition: if the engine exits nonzero, prints "FAILURES PRESENT", or the 324 count comes back < 324, the engine has DRIFTED from the v17.0 SSOT -- STOP, diff against the warm commit, and restore the warm file. Do NOT patch the cross-term or import octonion_algebra.py to "fix" it (that IS fp-octonion-algebra).
  </action>
  <verify>
    Physics/math check (exact over Q):
    1. Engine exit code == 0 AND "ALL_PASS" present.
    2. LOCK 7a: det_3 - cayley_hamilton_norm(jordan) simplifies to 0 (exact) at the engine's octonionic test points.
    3. LOCK 7b: exactly 324 of 324 inner derivations [L_a,L_b] annihilate det_3 (= dim f_4 = 52). NOT 30, NOT any smaller number.
    4. Source guard: 0 outside-fence octonion_algebra imports AND 0 float-rank (np.linalg.matrix_rank / float-tolerance) calls on the decisive path; exactly 1 in-fence (the sanctioned oracle).
    These are EXACT integer/rational facts -- report "324/324" and "dim f_4 = 52", never "approximately".
  </verify>
  <done>DERV-01 re-passed: ring_lemma_verification.py exit 0 + ALL_PASS; det_3 == CH generic norm (LOCK 7a); 324/324 inner-derivation annihilation = dim f_4 = 52 (LOCK 7b), all EXACT over Q; the source guard certifies octonion_algebra.py is absent on the decisive path (0 outside-fence imports, 0 float-rank calls). Driver in code/cartan_phase0_tangent.py asserts all four and prints "DERV-01 PASS". fp-octonion-algebra and fp-float-decisive explicitly rejected (the guard IS the rejection).</done>
</task>

<task type="auto">
  <name>Task 2 (DERV-02): Exact tangent identity E_11 o delta = (1/2)delta and T_{E_11}OP^2 = V_{1/2}(16)</name>
  <files>code/cartan_phase0_tangent.py</files>
  <action>
    Establish, EXACTLY over Q, the load-bearing soldering-form fact. Two distinct (both required) clauses. Cite Baez 2002 Section 3.4 (OP^2 = F_4/Spin(9), T_E OP^2 = V_{1/2}(E)) and McCrimmon (E o delta = (1/2)delta) -- do NOT re-derive these standard facts; the deliverable is the EXACT-over-Q machine verification. Reuse the bulk_geometry_verification.py engine primitives (import them; they are the SSOT-consistent exact-Q building blocks -- NOT octonion_algebra.py).

    Clause (a) -- the half-eigenvalue split (Peirce):
    1. Call `peirce_indices_under_E11()` from bulk_geometry_verification.py. Assert it returns V_{1/2} = engine indices {11..26} (16 elements; == V_HALF_IDX = list(range(11,27))), V_0 = {1..10}, V_1 = {0}, with L_{E_11} diagonal in the engine-native basis.
    2. Build the V_{1/2} basis elements delta_k (k in 11..26) via `_standard_basis_27()`. For EACH delta in the V_{1/2} basis, assert `jordan(E_11, delta) == Rational(1,2) * delta` EXACTLY over Q (component-wise octonion equality; use the engine's octmat compare / simplify, never float). All 16 must pass.
       (E_11 = `h3o_from_coords(1,0,0, oct_zero(), oct_zero(), oct_zero())`.)

    Clause (b) -- the Zariski tangent of the primitive-idempotent variety (the one new computation; ~15 lines, validated live this session):
    3. Define F(X) = X o X - X (the trace-1 idempotent / OP^2 equation; rank-1 locus, cone over OP^2 cut by X^2 = Tr(X)X with the trace-1 normalization). Linearize at E_11: DF(b_k) = 2*jordan(E_11, b_k) - b_k (jordan is symmetric). Build the 27x27 Jacobian J whose column k is _flat27(DF(b_k)) over the standard basis:
         cols = [_flat27(octmat_sub(octmat_scal(Rational(2), jordan(E11, basis[k])), basis[k])) for k in range(27)]
         J = Matrix(27, 27, lambda r, c: cols[c][r])
    4. Compute the kernel EXACTLY over Q: `ker = J.nullspace()`. Assert `J.rank() == 11` (== 27 - 16) and `len(ker) == 16` (= dim T_{E_11}OP^2).
    5. IDENTITY with V_{1/2} (NOT just dimension -- Pitfall 2): build Khalf = the 27x16 matrix of unit columns at indices 11..26, and assert `Matrix.hstack(*ker, Khalf).rank() == 16` (so ker subset of and equal to span(V_HALF_IDX)). Report the kernel as the coordinate index set {11..26}, not merely "16-dim".
    6. Cross-check 16 = 17 - 1 against the affine cone orbit(E_11) = 17 (the projective OP^2 tangent is the cone dimension minus the scale direction); this anchor is reproduced in Task 3.
    7. OPTIONAL secondary check (referee-proof, NOT a blocker): additionally cut the variety with the sharp equations X# = 0 (rank <= 1 component) and re-confirm the same 16-dim kernel. If it costs more than ~20 lines or any complication, skip it and note it as deferred -- the X o X = X kernel already gives the right answer and orbit(E_11)=17 independently certifies the 16.

    All decisive operations via sympy.Matrix.rank/.nullspace over QQ. NEVER numpy/float (fp-float-decisive: a derivative cancellation in the Jacobian can fabricate or destroy a rank under float tolerance).
  </action>
  <verify>
    Physics/math check (exact over Q):
    1. peirce_indices_under_E11() => V_{1/2} == {11..26} (16 elements), L_{E_11} diagonal.
    2. jordan(E_11, delta) == (1/2)delta EXACTLY for ALL 16 V_{1/2} basis elements (the half-eigenspace identity).
    3. J.rank() == 11 and dim ker == 16 (dimensional/limit check: T_{E_11}OP^2 = 16, the Cayley-plane dimension).
    4. IDENTITY: rank[ker | V_HALF_IDX-basis] == 16 -- the kernel EQUALS V_{1/2}(E_11), not merely a 16-dim subspace. RED FLAG if the 16-dim kernel has any nonzero component in V_0 {1..10} or V_1 {0}.
    5. Cross-check: 16 == 17 - 1 against orbit(E_11)=17 (Task 3).
    Report every result as an EXACT equality (16, 11, the index set {11..26}), never "approximately 16".
  </verify>
  <done>DERV-02 established EXACTLY over Q: (a) E_11 o delta = (1/2)delta for all 16 V_{1/2}(E_11) basis elements; (b) the Zariski tangent to {X o X = X} at E_11 (Jacobian rank 11, nullspace dim 16) EQUALS V_{1/2}(E_11) = span(engine indices {11..26}) -- the index-set identity, not just the dimension -- consistent with 16 = orbit(E_11)-1 = 17-1. Baez 2002 / McCrimmon cited (not re-derived). Soldering form dE confirmed V_{1/2}-valued. fp-float-decisive rejected (SymPy/QQ throughout). New code in code/cartan_phase0_tangent.py.</done>
</task>

<task type="auto">
  <name>Task 3 (VALD-01): Reproduce the calibration anchors and the K=-1/2 sign benchmark</name>
  <files>code/cartan_phase0_tangent.py, derivations/74-phase0-engine-tangent-calibration.tex</files>
  <action>
    Reproduce the v17.0 calibration anchors across the THREE engines (the research's key finding: the anchors are SPLIT across engines, not in one). Run each engine UNBUFFERED -- these are the slow steps (exact-QQ ranks on 52x27 / 27x78 matrices; ~55-110s each for the single-copy ranks, several minutes for the e_6/stabilizer/curvature run). Budget minutes, not seconds.

    1. Single-copy anchor -- run `python -u code/orbit_dimension_gate.py`. Assert exit 0 and capture: single-copy generic orbit dim == 24 (the MAX over >=2 generic integer points; the GATE hard-fails if != 24), stabilizer dim == 52 - 24 == 28 == dim Spin(8), trdeg == 27 - 24 == 3 (= R[Tr, Tr^2, det]). These are COMPUTED in-engine over Q (the naive Spin(8)-triality hand-count is a forbidden proxy -- trust the rank).
    2. e_6 / stabilizer / K anchors -- run `python -u code/bulk_geometry_verification.py`. Assert exit 0 and capture:
       - e_6 = 78 = 52 + 26 (e6_dimension via span_rank_over_QQ; Koecher-Tits f_4 (+) L(traceless)).
       - orbit(E_11) = 17 (= dim of the cone over OP^2; the 16 = 17-1 cross-check for DERV-02).
       - Stab_{E_6}(E_11) = 61 (= 78 - 17), exact kernel.
       - Stab_{V_0} = 45 = dim Spin(9,1) (the slice-preserving Levi), exact kernel.
       - K cone-Hessian sign benchmark: K_value == Rational(-1,2) AND round_K == -1 (the factor-2 cross-check; g_slice|_apex = 2*g_round; sectional_curvature on the {det_2=1} hyperboloid at the rational slice point). II(h_2(C_u)) = 0 => constant K. The LOAD-BEARING fact is the negative-constant SIGN; the magnitude 1/2 is the documented cone-Hessian/round normalization, NOT an error.
    3. In code/cartan_phase0_tangent.py, extend the driver to subprocess-run both engines with `-u`, assert exit 0, and grep/assert each captured anchor value (24, 28, 3, 78, 17, 61, 45, K=-1/2, round_K=-1). Print a "VALD-01 PASS" summary with every captured integer/rational.
    4. Write derivations/74-phase0-engine-tangent-calibration.tex (the deliv-phase0 deliverable). It MUST contain the three must_contain items: (i) the DERV-01 det-SSOT re-pass (CH + 324/324 = dim f_4 52; octonion_algebra.py absent: 0 outside-fence, 0 float-rank); (ii) the DERV-02 exact tangent identity E_11 o delta = (1/2)delta for the V_{1/2} basis and T_{E_11}OP^2 = V_{1/2}(16) (Jacobian rank 11, kernel == span(V_HALF_IDX), 16 = 17-1); (iii) the VALD-01 calibration anchors (24/28/3; 78; 17; 61; 45) and the K=-1/2 sign benchmark (round_K=-1). State the convention lock (metric (-,+,+,+) on the det_2 slice; bulk cone Riemannian; natural units; exact over Q; det SSOT = ring_lemma det_3). Cite Baez 2002, McCrimmon, ref-52-kkt. Report ALL verdicts as EXACT equalities, never "approximately".

    RED FLAG / STOP: K_value == +1/2 or -1 (instead of -1/2), OR orbit(E_11) != 17, OR Stab_{V_0} != 45 -> a sign/normalization slip or an e_6/stabilizer regression; the v17.0 anchors must reproduce byte-for-byte. Re-derive nothing; re-read the engine's normalization note and confirm the slice point lies on det_2 = 1.
  </action>
  <verify>
    Physics/math check (exact over Q; each anchor from its NAMED engine):
    1. orbit_dimension_gate.py: single-copy orbit dim == 24 (MAX over >=2 points), Spin(8) == 52-24 == 28, trdeg == 27-24 == 3. Exit 0.
    2. bulk_geometry_verification.py: e_6 == 78 == 52+26; orbit(E_11) == 17; Stab_{E_6}(E_11) == 61 == 78-17; Stab_{V_0} == 45 == dim Spin(9,1). Exit 0.
    3. Sign benchmark (dimensional/limit check): K_value == -1/2 (exact rational) AND round_K == -1 (factor-2 cross-check; round_K + 1 == 0 and round_K - 2*K_value == 0). Negative-constant sign is load-bearing.
    4. Cross-check consistency: DERV-02's 16 == orbit(E_11) - 1 == 17 - 1.
    5. The .tex deliverable contains all three must_contain items and states the convention lock; every verdict reported as an exact equality.
  </verify>
  <done>VALD-01 reproduced EXACTLY over Q from the three named engines: single-copy 24 / Spin(8)=28 / trdeg 3 (orbit_dimension_gate.py); e_6=78=52+26, orbit(E_11)=17, Stab_{E_6}(E_11)=61, Stab_{V_0}=45=Spin(9,1), K=-1/2 with round_K=-1 factor-2 cross-check (bulk_geometry_verification.py). 16 = 17-1 cross-check with DERV-02 confirmed. deliv-phase0 (.tex) written with all three must_contain items + convention lock + Baez/McCrimmon/52-kkt citations. Engines run unbuffered (Pitfall 3). All anchors are exact integers/rationals, never "approximately". fp-float-decisive rejected.</done>
</task>

</tasks>

<verification>
Plan-level physics/math consistency (ALL exact over Q -- this phase has NO controlled approximation; any float on a decisive path is a defect, not an approximation):

- DERV-01: det SSOT re-passes CH (LOCK 7a) + 324/324 inner-derivation annihilation = dim f_4 = 52 (LOCK 7b); engine ALL_PASS (exit 0); source guard reports 0 outside-fence octonion_algebra imports + 0 float-rank calls (fp-octonion-algebra and fp-float-decisive both rejected by construction).
- DERV-02: E_11 o delta = (1/2)delta for all 16 V_{1/2}(E_11) basis elements; the Zariski tangent of {X o X = X} at E_11 (Jacobian rank 11, nullspace 16) EQUALS V_{1/2}(E_11) = span({11..26}) -- the index-set IDENTITY, not merely dimension 16 (Pitfall 2 guarded). Cross-checked by 16 = 17 - 1 against orbit(E_11)=17.
- VALD-01: single-copy 24 / Spin(8)=28 / trdeg 3; e_6=78=52+26; orbit(E_11)=17; Stab_{E_6}(E_11)=61; Stab_{V_0}=45=Spin(9,1); K=-1/2 (round_K=-1 factor-2 cross-check). Each anchor from its named engine, each an exact integer/rational.
- Cross-engine consistency: the orbit(E_11)=17 from bulk_geometry_verification.py (Task 3) is the independent certificate for the DERV-02 (Task 2) projective tangent 17-1=16.
- Forbidden proxies: octonion_algebra.py absent on every decisive path (source guard); no numpy/float rank/eigenvalue/zero-test anywhere decisive; no verdict softened to "approximately".
- STOP discipline: any engine exiting nonzero / printing FAILURES, a sub-324 annihilation count, a 16-dim kernel not equal to V_{1/2}, K != -1/2, orbit(E_11) != 17, or Stab_{V_0} != 45 is engine drift -> STOP and diff against the warm v17.0 commit; do NOT patch around it.
</verification>

<success_criteria>
Maps 1:1 to the four ROADMAP Phase 74 success criteria:

1. (Criterion 1 / DERV-01) The det SSOT ring_lemma_verification.py det_3 re-passes Cayley-Hamilton + 324/324 inner-derivation annihilation (= dim f_4 = 52), engine ALL_PASS (exit 0); a source guard confirms octonion_algebra.py is NOT imported on the decisive path (0 outside-fence imports, 0 float-rank calls).
2. (Criterion 2 / DERV-02) E_11 o delta = (1/2)delta holds exactly over Q for a full basis of delta in V_{1/2}(E_11), and the tangent space to the primitive-idempotent variety at E_11 is exactly V_{1/2}(E_11), of dimension 16 (Jacobian rank 11; kernel == span(V_HALF_IDX); 16 = 17-1).
3. (Criterion 3 / VALD-01) The calibration anchors are reproduced exactly via orbit_dimension_gate.py (single-copy 24 / Spin(8)=28 / trdeg 3) and bulk_geometry_verification.py (e_6=78=52+26; orbit(E_11)=17; Stab_{E_6}(E_11)=61; Stab_{V_0}=45=Spin(9,1)).
4. (Criterion 4 / VALD-01) The H^3 cone-Hessian sign benchmark K=-1/2 is re-passed (round_K=-1 factor-2 cross-check), pinning sign conventions BEFORE any downstream curvature verdict.

Plus: deliv-phase0 (derivations/74-phase0-engine-tangent-calibration.tex) exists with all three must_contain items and the convention lock; code/cartan_phase0_tangent.py contains the new Jacobian-kernel tangent check and the three-engine driver; all results stated as EXACT-over-Q equalities (never "approximately"); Baez 2002, McCrimmon, and ref-52-kkt surfaced (cited).
</success_criteria>

<output>
After completion, create `.gpd/phases/74-phase-0-engine-recovery-tangent-identity-calibration/74-01-SUMMARY.md`
</output>
