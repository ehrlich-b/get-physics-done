---
phase: 78-phase-c-circularity-audit-forced-vs-posited
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - code/cartan_phaseC_contraction.py
  - derivations/78-circularity-audit.tex
interactive: false

conventions:
  units: "natural (hbar=c=k_B=1); pure algebra -- no field theory, no gauge fields, no divergences => Fourier/gauge/regularization = N/A. Decisive output is a DIMENSION (integer) + an IDENTITY, dimensionless."
  metric: "mostly-minus (+,-,-,-) timelike-positive Lorentzian (1,3) on the frame; eta=diag(+1,-1,-1,-1)"
  arithmetic: "EXACT over Q (sympy.Matrix.rank / nullspace over QQ); NEVER numpy.linalg, NEVER float on the decisive dimension/rank verdict"
  det_ssot: "code/ring_lemma_verification.py det_3 (F_4-invariant cubic norm; cross-term 2Re((x2 x1)x3), x2 BEFORE x1, Phase-64.1 fix); code/octonion_algebra.py BANNED (buggy associator)"
  trace_form: "c(X,Y)=Tr(X o Y)=Tr(jordan(X,Y)), jordan=(1/2)(AB+BA); F_4-invariant; the Jordan 1/2 is load-bearing"
  index_layout: "frame indices a,b,c,d = 0..3 (so(3,1), FORCED by (E_11,u) from Phase 75) kept DISTINCT from slice coords mu,nu = engine idx [1,2,3,10]; V_{1/2} survivors = engine idx [11,18,19,26]. The 4-form wedge is a fixed top-form; the invariant count is finite-dim linear algebra on the FRAME indices."
  structure_group: "residual structure group on the 4-dim coframe = so(3,1)[6] (Phase 75 residual 21 = so(3,1)[6] (+) so(6)[15]; the so(6) a trivial-on-spacetime ideal). The invariant count is at the BROKEN so(3,1) level (the Spin(9,1)->SO(3,1) breaking is exactly the 'by hand' step being audited)."
  cartan: "gravity = Lorentz block R[omega] of F=dA+A^A (Wise gr-qc/0611154). The audited EINSTEIN object is the LINEAR-in-Riemann term eps_{abcd} R^{ab} ^ e^c ^ e^d -- NOT a quadratic-in-F Maxwell/Pontryagin stress (the Phase-76 OVERTURNED tautology)."
  cosmological_constant: "Lambda=0 at M=0 (DERIVED & MEASURED in Phase 77; CONVENTIONS §6). At Lambda=0 the eps F^F action is pure Gauss-Bonnet/Euler (topological). Do NOT reintroduce Lambda<0 / R x H^3."

dimensional_check:
  bare_invariant_dim: "[integer] dimension of the so(3,1)-invariant quadratic-in-curvature 4-form space on the frame; expected = 2 (Euler + Pontryagin), exact over Q"
  traceform_invariant_dim: "[integer] dimension of the subspace buildable from Tr(X o Y) + det_3 alone; the DECISIVE integer (1 => candidate STRONG WIN; >1 or eps-unreachable => fp-imported-action), exact over Q"
  eps_norm: "[rational or 'free/imported'] the normalization the cubic norm fixes for the eps-contraction IF eps is reachable; must be READ OFF det_3, not put in by hand"

approximations:
  - name: "None -- exact symbolic invariant theory over Q"
    parameter: "none"
    validity: "the decisive output is an integer (a dimension/rank) plus an identity (generator==eps with cubic-norm-fixed normalization), both exact over Q; no small parameter, no perturbation, no numerical sweep"
    breaks_when: "any decisive dimension/rank resting on float or numpy.linalg (fp-float-decisive)"
    check: "every reported dimension/rank/identity is exact over Q via sympy.Matrix.rank / nullspace; the bare-space dim=2 anchor reproduced before any verdict"

contract:
  schema_version: 1
  scope:
    question: "Is the MM eps-contraction (the Spin(9,1)->SO(3,1) breaking + the eps-tensor that turns F^F into Einstein-Hilbert+Lambda) FORCED by the intrinsic h_3(O) trace-form Tr(X o Y) / cubic-norm det_3 pairing, or does the Einstein term appear ONLY because an MM/EH action was posited by hand? Operationalized as the DIMENSION (exact over Q) of the trace-form-invariant quadratic-in-F 4-form contraction space on the so(3,1) frame, plus the eps-identity and its det_3-fixed normalization."
  claims:
    - id: claim-forced-einstein
      statement: "The MM eps-contraction giving Einstein-Hilbert+Lambda is EITHER FORCED by the h_3(O) trace-form / cubic-norm pairing (STRONG WIN -- gravity from the Lie-sector geometry, non-circular) OR appears only because an MM/EH action was posited by hand (fp-imported-action -- an honest partial, NOT a derivation). This plan computes the decisive DIMENSION (exact over Q): the bare so(3,1)-invariant quadratic-in-curvature 4-form space (expected 2: Euler + Pontryagin) and the strictly smaller trace-form-invariant subspace buildable from Tr(X o Y) + det_3 alone, then tests eps-reachability and the det_3-fixed normalization. The honest level is reported either way; fp-imported-action is the HIGH / most-likely real outcome (the bare space is already 2-dim, and at the Phase-77-MEASURED Lambda=0 the eps-action is purely topological)."
      deliverables: [deliv-phaseC, deliv-phaseC-code]
      acceptance_tests: [test-forced-vs-posited, test-bare-space-dim, test-traceform-restriction]
      references: [ref-mm-1977, ref-wise, ref-gst, ref-ring-lemma-engine, ref-orbit-gate]
  deliverables:
    - id: deliv-phaseC
      kind: derivation
      path: derivations/78-circularity-audit.tex
      description: "Phase C derivation (started here, completed in 78-02): the exact-over-Q invariant-contraction-space computation -- (1) the bare so(3,1)-invariant quadratic-in-curvature 4-form space dim=2 anchor (Euler + Pontryagin), independent on a generic R[omega]; (2) Tr(X o Y)|frame == eta=diag(+1,-1,-1,-1); (3) the DECISIVE trace-form-invariant subspace dimension + eps-reachability + det_3-fixed-normalization read-off; the input ban declared. (The verdict synthesis, contrast framing, and ratification are in 78-02.)"
      must_contain:
        - "the bare so(3,1)-invariant quadratic-in-curvature 4-form space is dim=2 (Euler eps R^R + Pontryagin R^R), reproduced exact over Q before any verdict (sanity anchor)"
        - "Tr(X o Y) restricted to the soldered V_0 ~ R^{3,1} frame == eta=diag(+1,-1,-1,-1) (the trace form supplies the symmetric metric => the Pontryagin source)"
        - "the DECISIVE integer: dim of the trace-form-invariant subspace buildable from Tr(X o Y) + det_3 alone (and (E_11,u)-forced structures), with eps-reachability (is eps_{abcd} derivable, e.g. as sqrt|eta| eps or via a det_3 polarization/orientation?) and the normalization READ OFF det_3 (or named as free/imported)"
        - "explicit determination of whether the Spin(9,1)->SO(3,1) breaking and the eps-tensor are fixed by the trace form / cubic norm (the input feeding the 78-02 verdict)"
    - id: deliv-phaseC-code
      kind: code
      path: code/cartan_phaseC_contraction.py
      description: "The decisive driver: det SSOT re-pass; Tr|frame==eta restriction; the so(3,1)-invariant quadratic-in-F 4-form nullspace (bare dim=2 anchor) over QQ; the trace-form-invariant subspace span/rank over QQ (the DECISIVE dimension) with eps- and Pontryagin-reachability + det_3-normalization read-off; an AST/source input-ban guard (no posited action / -1/2 / 16piG / MM-action / SUSY / octonion_algebra / numpy.linalg on the decisive path)."
      must_contain:
        - "import of the det SSOT (ring_lemma_verification: det_3, Tr, c, jordan, polarize_d) and orbit_dimension_gate (exact_qq_rank, span_rank_over_QQ); NO octonion_algebra import; NO numpy.linalg on the decisive path"
        - "bare_invariant_space(): builds the so(3,1)-invariant quadratic-in-curvature 4-form space on frame indices and returns dim (== 2 expected) exact over Q via sympy nullspace/rank"
        - "traceform_invariant_subspace(): the span generated by Tr(X o Y)- and det_3-polarization-built invariant 4-forms; returns its dim (the DECISIVE integer), whether eps is in it, whether Pontryagin is in it, and the det_3-fixed normalization (or 'free') -- exact over Q via span_rank_over_QQ"
        - "input_ban_guard(): AST/source guard asserting no '∫eps F^F' / '-1/2' / '16piG' / 'MM action' / SUSY / octonion_algebra / numpy.linalg is load-bearing on the decisive path (replicate ast_guard_T / source_guard from cartan_phaseB_einstein.py)"
  references:
    - id: ref-mm-1977
      kind: paper
      locator: "MacDowell & Mansouri, PRL 38 (1977) 739 -- gravity as a broken dS/Lorentz gauge theory; the int eps F^F action. The source of the construction being audited. Verbatim formulas carried in 78-RESEARCH.md (executor has NO web)."
      aliases: ["MM 1977", "MacDowell-Mansouri 1977"]
      role: definition
      why_it_matters: "Defines the MM mechanism / the eps-contraction whose forced-vs-posited status is the verdict; cite as the source of the audited construction, NEVER as the source of the Einstein term"
      applies_to: [claim-forced-einstein]
      carry_forward_to: [planning, execution, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-wise
      kind: paper
      locator: "D. K. Wise, gr-qc/0611154 -- A=omega+(1/l)e; F=(R-(Lambda/3)e^e)+d_omega e; S_MM=(-3/2GLambda)int tr(Fhat^*Fhat) ~ int eps F^F; the SO(4,1)->SO(3,1) breaking done 'by hand' (Wise's words). The EH+Lambda+GB decomposition. Verbatim in 78-RESEARCH.md."
      aliases: ["Wise 2006", "Wise gr-qc/0611154"]
      role: definition
      why_it_matters: "CITE for what F's blocks MEAN and what the eps-contraction IS (the precise circularity locus -- 'broken by hand'); NEVER cite the action as the source of the Einstein term"
      applies_to: [claim-forced-einstein]
      carry_forward_to: [planning, execution, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-gst
      kind: paper
      locator: "Gunaydin-Sierra-Townsend (1983-84), magic N=2 MESGT on E_{6(-26)}/F_4 -- their posited Lagrangian's -R/2 is the assumed N=2 multiplet's own output. THE in-program fp-imported-action precedent (the dead 47-*..50-*, 53-* route)."
      aliases: ["GST", "magic supergravity"]
      role: background
      why_it_matters: "The canonical fp-imported-action contrast class; cite for the geometry/contrast ONLY, do NOT adopt their Lagrangian (that posited Lagrangian is exactly the fp being audited)"
      applies_to: [claim-forced-einstein]
      carry_forward_to: [planning, execution]
      must_surface: false
      required_actions: [cite, avoid]
    - id: ref-ring-lemma-engine
      kind: prior_artifact
      locator: "code/ring_lemma_verification.py -- det SSOT (det_3 = the UNIQUE F_4-invariant cubic norm, cross-term 2Re((x2 x1)x3), 324/324 inner-derivation annihilation; Tr, c=Tr(X o Y), jordan, polarize_d). The intrinsic-pairing source -- CENTRAL here. octonion_algebra.py BANNED."
      aliases: ["ring_lemma_verification.py", "det SSOT"]
      role: method
      why_it_matters: "det_3 and Tr(X o Y) are the ONLY admissible intrinsic invariant-tensor sources for the trace-form-invariant count; the forced-vs-posited question IS whether eps is reachable from these two"
      applies_to: [claim-forced-einstein]
      carry_forward_to: [planning, execution, verification]
      must_surface: true
      required_actions: [use]
    - id: ref-orbit-gate
      kind: prior_artifact
      locator: "code/orbit_dimension_gate.py -- exact_qq_rank, span_rank_over_QQ, infinitesimal_action (exact-over-Q rank/nullspace + so(3,1) generator action). Validated on the Phase-75 residual 21=so(3,1)(+)so(6) and the Phase-64.1/65 324/324 anchors."
      aliases: ["orbit_dimension_gate.py"]
      role: method
      why_it_matters: "the exact-over-Q rank/nullspace tooling for the bare-space dim=2 anchor and the DECISIVE trace-form-invariant subspace dimension"
      applies_to: [claim-forced-einstein]
      carry_forward_to: [planning, execution, verification]
      must_surface: true
      required_actions: [use]
  acceptance_tests:
    - id: test-bare-space-dim
      subject: claim-forced-einstein
      kind: consistency
      procedure: "Build the space of quadratic-in-curvature 4-forms invariant under the residual structure group so(3,1) (forced by (E_11,u) from Phase 75) on the frame indices a,b,c,d=0..3, by solving the invariance linear system (nullspace) over QQ; cross-confirm by exhibiting the two known independent invariants (Euler eps_{abcd} R^{ab}^R^{cd} and Pontryagin R^{ab}^R_{ab} = delta^{ac}delta^{bd} R_{ab}^R_{cd}) acting on a generic R[omega] (reuse the Phase-77 R[omega] at a rational basepoint) and checking their linear independence via sympy.Matrix.rank over QQ."
      pass_condition: "The bare invariant space dimension is EXACTLY 2 over Q (Euler + Pontryagin), and the two invariants are linearly independent on a generic R[omega]. (If not 2, the so(3,1)-invariance setup is BUGGY -- debug before interpreting; do NOT report a verdict on a broken baseline.)"
      evidence_required: [deliv-phaseC, deliv-phaseC-code]
      automation: automated
    - id: test-traceform-restriction
      subject: claim-forced-einstein
      kind: consistency
      procedure: "Restrict the trace form c(X,Y)=Tr(X o Y) to the soldered V_0 ~ R^{3,1} frame (the Phase-75 image / the [1,2,3,10] slice basis), exact over Q via the det SSOT engine; compare the resulting Gram matrix to eta=diag(+1,-1,-1,-1). Separately re-pass the det_3 SSOT (det_3(I)=1, det_3(diag)=abc, polarize_d=6N -- or cite the Phase-64.1/65 324/324 annihilation) and confirm octonion_algebra.py is absent on the decisive path."
      pass_condition: "Tr(X o Y)|frame == eta=diag(+1,-1,-1,-1) exact over Q (the trace form supplies the symmetric frame metric => the Pontryagin source, and up to orientation a volume sqrt|eta| eps); det_3 SSOT re-passes (or Phase-64.1/65 cited); octonion_algebra.py confirmed absent; no numpy.linalg on the decisive path."
      evidence_required: [deliv-phaseC, deliv-phaseC-code]
      automation: automated
    - id: test-forced-vs-posited
      subject: claim-forced-einstein
      kind: oracle
      procedure: "Compute, exact over Q, the dimension of the trace-form-invariant subspace -- the quadratic-in-F 4-form contractions buildable from Tr(X o Y) and det_3 alone (and (E_11,u)-forced structures), with NO posited eps -- via sympy.Matrix.rank / nullspace (span_rank_over_QQ). Test eps-reachability (is the antisymmetric eps_{abcd} derivable from the symmetric Tr / det_3 data -- e.g. as sqrt|eta| eps from the trace-form metric, or as a det_3 polarization/orientation on the Peirce/V_0 block?) AND Pontryagin-reachability; read off whether the subspace is 1-dim AND its generator == the eps-contraction with a det_3-FIXED normalization. (This is the decisive input; the verdict ladder + ratification are rendered in 78-02.)"
      pass_condition: "A DECISIVE, exact-over-Q determination feeding the verdict ladder: (STRONG WIN candidate) dim(trace-form-invariant subspace)==1 AND generator==eps AND normalization fixed by det_3 -- all three; OR (fp-imported-action) any shortfall -- dim>1 (eps one choice among several, e.g. both eps and Pontryagin reachable => dim>=2), OR eps unreachable, OR the normalization free/imported. Reported at true strength as the dimension + identity, NOT relabeled a win; no decisive number on float."
      evidence_required: [deliv-phaseC, deliv-phaseC-code]
      automation: automated
  forbidden_proxies:
    - id: fp-imported-action
      subject: claim-forced-einstein
      proxy: "Declaring a STRONG WIN whose final step is 'and contracting MM-style with eps F^F gives Einstein-Hilbert', i.e. Einstein structure that only appears because an MM/EH action was posited by hand (the GST sin in new clothes)."
      reason: "The route WINS only if the Einstein term falls out of the cubic-norm/trace-form geometry WITHOUT importing the action. The eps-contraction, the SO(4,1)->SO(3,1) breaking, and the 1/16piG normalization are EXTERNAL input unless DERIVED from Tr / det_3. This plan must COUNT whether eps is forced, not posit the eps-action and expand it. fp-imported-action is the HIGH / most-likely real outcome -- report at true strength (an honest partial, same class as GST/Singh/Castro), NOT a derivation. Warning signs: any load-bearing '∫eps F^F' / '-1/2' / '16piG' / 'MM action' / 'SUSY'; a dim=1 claim while the normalization is put in by hand."
    - id: fp-float-decisive
      subject: claim-forced-einstein
      proxy: "Any decisive dimension / rank / contraction-space verdict resting on numpy.linalg or floating point."
      reason: "The cancellations are exact; float tolerance fabricates the dimension. Decisive checks EXACT over Q (sympy.Matrix.rank / nullspace over QQ), never numpy. numpy permitted only in fenced float-triage helpers, never on the decisive path."
    - id: fp-octonion-algebra
      subject: claim-forced-einstein
      proxy: "Using octonion_algebra.py (buggy (x1 x2)x3 associator order, float, 0.67 associator gap) on any decisive path."
      reason: "The wrong associator silently corrupts det and every downstream contraction; det SSOT = ring_lemma_verification.py det_3 (cross-term 2Re((x2 x1)x3)). octonion_algebra.py is BANNED."
    - id: fp-reuse-cone-hessian
      subject: claim-forced-einstein
      proxy: "Using the v17.0 cone-Hessian symmetric/real-sector Riemann (the real part of the QGT) as load-bearing for the contraction-space count."
      reason: "This route is the antisymmetric/Lie sector (the eps-contraction on the connection curvature) -- a DIFFERENT tensor than the cone-Hessian (real-part/Fubini-Study). The cone-Hessian is not relevant to the trace-form-invariant count; do not reuse it as load-bearing."
    - id: fp-wrong-object
      subject: claim-forced-einstein
      proxy: "Auditing a quadratic-in-F Maxwell / Pontryagin stress as 'the Einstein object' (the Phase-76 OVERTURNED tautology)."
      reason: "The Einstein-Hilbert object is the LINEAR-in-Riemann eps_{abcd} R^{ab} ^ e^c ^ e^d (R wedged with the tetrad), which emerges from the R^e^e CROSS-term of the eps F^F square -- NOT the quadratic R^R Pontryagin piece. 'A 2-form's Maxwell stress is traceless in 4d' is a tautology that also kills real GR ([[feedback_test_right_object_not_tautology]]). Keep the audited object the eps that produces the linear-in-R EH term. Note: at Lambda=0 only the R^R piece survives (topological), so a quadratic-in-R audit at the measured vacuum would audit a topological invariant -- manifestly not Einstein."
  links:
    - id: link-forced
      source: claim-forced-einstein
      target: deliv-phaseC
      relation: supports
      verified_by: [test-forced-vs-posited, test-bare-space-dim, test-traceform-restriction]
    - id: link-driver-to-derivation
      source: deliv-phaseC-code
      target: deliv-phaseC
      relation: computes
      verified_by: [test-forced-vs-posited, test-bare-space-dim]
  uncertainty_markers:
    weakest_anchors:
      - "The OUTCOME (FORCED vs POSITED) is MEDIUM by design -- that is exactly what this plan MEASURES. fp-imported-action is the HIGH / most-likely real verdict: the bare invariant space is already 2-dim (Euler + Pontryagin), so eps is non-unique among quadratic invariants; forced-ness requires the trace-form restriction to collapse 2->1 AND fix the normalization. A symmetric bilinear (Tr) + a symmetric trilinear (det_3) do not obviously generate an antisymmetric eps -- but whether they do (via a Pfaffian / det_3 orientation on the Peirce blocks / the (E_11,u)-forced complex structure) is a genuine computation, NOT a slogan. Do NOT over-claim FORCED ('a metric gives a volume form so eps is automatic') NOR over-claim POSITED ('symmetric data can never give eps') without the exact count."
      - "If the trace-form metric supplies eps as sqrt|eta| eps, then BOTH eps (Euler) and delta-delta (Pontryagin) are reachable => the subspace is 2-dim, not 1-dim => eps is a non-unique choice => still fp-imported-action. The STRONG WIN requires the intrinsic data to single out eps OVER Pontryagin AND fix the normalization (a strictly 1-dim trace-form-invariant subspace) -- compute BOTH reachabilities."
    disconfirming_observations:
      - "Bare invariant space dim != 2 => the so(3,1)-invariance setup is BUGGY (the baseline MUST be Euler + Pontryagin) => debug before interpreting, do NOT report a verdict on a broken baseline."
      - "dim(trace-form-invariant subspace) > 1 (e.g. both eps and Pontryagin reachable), OR eps unreachable from Tr / det_3, OR the normalization free/imported => fp-imported-action (the most-likely outcome); report at true strength, do NOT inflate."
      - "'eps-contraction gives Einstein-Hilbert' appearing as the FINAL step => the posited action smuggled in => that is the fp-imported-action being audited, NOT a derivation."
      - "Any float / numpy.linalg rank on the decisive dimension => fp-float-decisive => switch to sympy.Matrix.rank over QQ before any verdict."
---

<objective>
Compute, EXACT over Q, the decisive forced-vs-posited DIMENSION that settles whether the MacDowell-Mansouri eps-contraction (the Spin(9,1)->SO(3,1) breaking + the eps-tensor that turns F^F into Einstein-Hilbert+Lambda) is FORCED by the intrinsic h_3(O) trace form Tr(X o Y) and cubic norm det_3, or is an external MM choice imported by hand (fp-imported-action).

Purpose: This is the decisive computation of Phase C (claim-forced-einstein, requirement VALD-06) and the FINAL phase of milestone v18.0. Phase 77 already established at true strength that F=dA+A^A has a genuine Riemann Lorentz block but G[g] is NOT Einstein-form intrinsically -- so the Einstein term, if recoverable at all, is recoverable ONLY via a posited action. This plan MEASURES whether that action is forced: it computes the bare so(3,1)-invariant quadratic-in-curvature 4-form space (expected 2-dim: Euler + Pontryagin) as a sanity anchor, then the strictly smaller trace-form-invariant subspace buildable from Tr(X o Y) + det_3 ALONE, and tests eps-reachability + the det_3-fixed normalization. The verdict ladder synthesis, the GST/Singh/Castro contrast framing, and the BLOCKING human ratification are rendered in 78-02. fp-imported-action is the HIGH / most-likely real outcome (negative-result-is-success); do NOT structure this plan to "confirm Einstein" -- it MEASURES a dimension.

Output: code/cartan_phaseC_contraction.py (the decisive driver: det SSOT re-pass, Tr|frame==eta, the bare dim=2 nullspace anchor, the DECISIVE trace-form-invariant subspace dimension + eps/Pontryagin-reachability + normalization read-off, the AST/source input-ban guard) and the started derivations/78-circularity-audit.tex (the count and the eps-identity input; completed in 78-02).

NOTE (header): No experiment-designer pass -- this is exact symbolic representation theory / invariant theory over Q, no numerical sweep, no parameter scan. No protocol bundles selected.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/CONVENTIONS.md
@.gpd/phases/78-phase-c-circularity-audit-forced-vs-posited/78-RESEARCH.md
@.gpd/phases/77-phase-b-full-cartan-curvature-4d-gravity/77-02-SUMMARY.md
@code/ring_lemma_verification.py
@code/cartan_phaseB_curvature.py
@code/cartan_phaseB_einstein.py
@code/orbit_dimension_gate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup -- det SSOT re-pass, Tr|frame==eta, and the hard input-ban guard</name>
  <files>code/cartan_phaseC_contraction.py, derivations/78-circularity-audit.tex</files>
  <action>
    Establish the certified intrinsic toolkit and the input ban BEFORE any invariant count. Everything exact over Q. Create code/cartan_phaseC_contraction.py importing ONLY the warm engines (ring_lemma_verification, cartan_phaseB_curvature, orbit_dimension_gate); do NOT import octonion_algebra; do NOT use numpy.linalg on the decisive path.

    1. det SSOT re-pass: call ring_lemma_verification.det_3 and confirm det_3(I)=1, det_3(diag(a,b,c))=a*b*c exactly over Q, and polarize_d(X,X,X)=6*det_3(X) (the polarization identity, d(X,X,X)=6N). You MAY instead cite the Phase-64.1/65 324/324 inner-derivation annihilation as the uniqueness certificate -- but re-pass the cheap algebraic identities here. Confirm octonion_algebra.py is NOT in sys.modules on the decisive path.
    2. Trace-form metric: load c(X,Y)=Tr(X o Y) and restrict it to the soldered V_0 ~ R^{3,1} frame (the [1,2,3,10] slice basis = the Phase-75 image). Build the 4x4 Gram matrix of c on this frame basis exact over Q and confirm it == eta=diag(+1,-1,-1,-1) (up to the established overall sign/normalization convention; if it differs by a uniform positive scale, record the scale -- it is the trace-form normalization the cubic norm must later fix). This establishes that Tr supplies the symmetric frame metric (hence the Pontryagin source delta^{ac}delta^{bd} and, up to orientation, a volume sqrt|eta| eps).
    3. Input-ban guard: replicate the AST/source-guard pattern from cartan_phaseB_einstein.py (ast_guard_T / source_guard / _forbidden_ids_used). Write input_ban_guard() that asserts NO load-bearing appearance of an MM/EH action symbol, '∫eps F^F' as an assumed action, '-1/2', '16piG', 'MM action', 'SUSY', octonion_algebra, or numpy.linalg on the decisive path. The guard MUST be callable and pass on this file.

    Document in derivations/78-circularity-audit.tex (start the file): the certified intrinsic toolkit (det_3 = the unique F_4-invariant cubic norm; Tr(X o Y) = the F_4-invariant trace form supplying eta), the Tr|frame==eta restriction, and the declared input ban (the forced determination may use ONLY Tr, det_3, the Peirce decomposition under E_11, the C_u / pi_u reduction, and standard differential geometry -- NOT a posited action / -1/2 / 16piG / SUSY).
  </action>
  <verify>
    1. det_3(I)=1, det_3(diag(a,b,c))=abc, polarize_d(X,X,X)=6*det_3(X) -- all exact over Q (or Phase-64.1/65 324/324 cited as the uniqueness certificate); octonion_algebra.py absent on the decisive path.
    2. Gram of Tr(X o Y) on the [1,2,3,10] frame == eta=diag(+1,-1,-1,-1) exact over Q (record any uniform positive scale as the trace-form normalization).
    3. input_ban_guard() is implemented (mirroring ast_guard_T/source_guard) and PASSES on this file: no posited action / -1/2 / 16piG / MM-action / SUSY / octonion_algebra / numpy.linalg load-bearing.
    4. No float / numpy.linalg on any decisive step; the file imports only the warm engines.
    5. derivations/78-circularity-audit.tex started with the toolkit, the Tr|frame==eta restriction, and the input ban.
  </verify>
  <done>The certified intrinsic toolkit is in place: det_3 SSOT re-passes its algebraic identities (or Phase-64.1/65 cited), Tr(X o Y)|frame == eta=diag(+1,-1,-1,-1) exact over Q, octonion_algebra.py confirmed absent, and the AST/source input-ban guard is implemented and passes. The .tex is started. test-traceform-restriction is satisfied; the decisive count can proceed on a certified, action-free foundation.</done>
</task>

<task type="auto">
  <name>Task 2: Bare invariant space -- the dim=2 anchor (Euler + Pontryagin) exact over Q</name>
  <files>code/cartan_phaseC_contraction.py, derivations/78-circularity-audit.tex</files>
  <action>
    Reproduce the baseline BEFORE any verdict (the mandatory sanity anchor). The audited object is the LINEAR-in-Riemann Einstein term eps_{abcd} R^{ab} ^ e^c ^ e^d -- keep that in view; here we first count the quadratic-in-curvature invariants whose CROSS-term yields it. Everything exact over Q.

    1. Build the residual-structure-group action: construct the so(3,1)[6] generators on the FRAME indices a,b,c,d=0..3 (the structure group FORCED by (E_11,u) from Phase 75; residual 21 = so(3,1)[6] (+) so(6)[15], the so(6) a trivial-on-spacetime ideal with zero 4-action). Reuse orbit_dimension_gate.infinitesimal_action and the Phase-75 residual generators (derivations/75 / cartan_phaseA_coframe.py) on the frame indices. KEEP frame indices a,b DISTINCT from slice indices mu,nu.
    2. bare_invariant_space(): enumerate the space of quadratic-in-curvature 4-forms invariant under so(3,1) on the frame indices by solving the invariance linear system delta_g T = 0 for all so(3,1) generators g, exact over Q (sympy.Matrix.nullspace over QQ). Expect dim = 2.
    3. Cross-confirm by exhibiting the two known independent invariants acting on a generic R[omega]: the Euler/eps-contraction eps_{abcd} R^{ab} ^ R^{cd} and the Pontryagin R^{ab} ^ R_{ab} = delta^{ac} delta^{bd} R_{ab} ^ R_{cd} (built from the trace-form metric eta from Task 1). Reuse the Phase-77 R[omega] (cartan_phaseB_curvature: curvature_F / split_blocks / riemann_lower_from_F) at a rational basepoint with M != 0 (generic R[omega]); evaluate both invariants and confirm they are two DISTINCT rationals, linearly independent via sympy.Matrix.rank over QQ.

    If dim != 2: the so(3,1)-invariance setup is BUGGY (the baseline MUST be Euler + Pontryagin -- standard 4d topology; Nieh-Yan is torsionful, excluded for the Levi-Civita omega) -- debug Task 1/2, do NOT interpret or report a verdict.

    Document in derivations/78-circularity-audit.tex: the so(3,1) generators on the frame, the nullspace dim=2 result, and the explicit two invariants + their independence on the Phase-77 R[omega].
  </action>
  <verify>
    1. so(3,1)[6] generators built on the frame indices a,b,c,d=0..3 (forced by (E_11,u), Phase-75 residual), distinct from slice indices.
    2. bare_invariant_space() returns dim == 2 EXACTLY over Q (Euler + Pontryagin) via sympy nullspace.
    3. Euler eps_{abcd}R^{ab}^R^{cd} and Pontryagin R^{ab}^R_{ab} evaluated on the Phase-77 generic R[omega] (M!=0, rational basepoint) are two distinct rationals, linearly independent (rank 2 over QQ).
    4. dim != 2 => flagged as a setup BUG (debug), NOT a verdict.
    5. Exact over Q; no float / numpy.linalg on the decisive path; the audited Einstein object kept as the linear-in-R eps R^e^e (not a quadratic Maxwell/Pontryagin stress).
  </verify>
  <done>The bare so(3,1)-invariant quadratic-in-curvature 4-form space is confirmed dim=2 (Euler + Pontryagin) exact over Q, with the two invariants linearly independent on the Phase-77 generic R[omega]. The sanity anchor passes (test-bare-space-dim) -- eps is NON-unique among quadratic invariants, so forced-ness now hinges on whether the trace-form restriction collapses 2->1 (Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: THE decisive count -- trace-form-invariant subspace dimension + eps-reachability + normalization</name>
  <files>code/cartan_phaseC_contraction.py, derivations/78-circularity-audit.tex</files>
  <action>
    The decisive computation (test-forced-vs-posited, requirement VALD-06): compute the dimension of the trace-form-invariant subspace and test whether eps is forced. Everything EXACT over Q via sympy.Matrix.rank / nullspace (span_rank_over_QQ). The input-ban guard (Task 1) must hold throughout -- no posited eps, no posited action.

    1. traceform_invariant_subspace(): build the span of invariant quadratic-in-F 4-form contractions generated ONLY by the intrinsic data -- the symmetric bilinear Tr(X o Y) (=> eta_{ab}, => the Pontryagin delta^{ac}delta^{bd} contraction AND, up to orientation, the volume sqrt|eta| eps) and the symmetric trilinear det_3 polarization polarize_d (the totally-symmetric T_{ijk} on the Peirce/V_0 block), plus the (E_11,u)-forced structures (the complex structure u=e_7 may supply an orientation). Compute the dimension of this span exact over Q via span_rank_over_QQ. This is THE DECISIVE INTEGER.
    2. eps-reachability (the crux): test whether the totally-antisymmetric eps_{abcd} on the 4d frame is IN the span -- concretely, is eps derivable from the symmetric Tr / det_3 data? Test the two candidate routes: (a) eps as sqrt|eta| eps from the trace-form metric (a metric fixes a volume form up to orientation); (b) eps as a det_3 polarization / orientation on the soldered V_0 ~ R^{3,1} block (does det_3 restricted to the Lorentz block carry a natural Pfaffian-like volume/orientation element?). Report YES/NO for each, exact over Q.
    3. Pontryagin-reachability: confirm the Pontryagin form (built from the symmetric eta) IS reachable (it should be -- eta comes from Tr). This matters: if eps is ALSO reachable, then BOTH are in the span => dim >= 2 => eps is a NON-unique choice => fp-imported-action.
    4. Normalization read-off: IF eps is reachable and the subspace is 1-dim, determine whether the eps-contraction's normalization (-> 1/16piG) is FIXED by det_3 (read off the cubic norm) or is FREE / imported. A dim=1 subspace whose generator is eps but whose normalization is free/imported is STILL fp-imported-action.
    5. Assemble the decisive triple for the 78-02 verdict ladder: (dim of trace-form-invariant subspace; eps-in-span YES/NO; normalization det_3-fixed / free). Do NOT render the final verdict here (that is 78-02 with the human checkpoint) -- but state the dimension + identity at true strength.

    Document in derivations/78-circularity-audit.tex: the span construction, the decisive dimension, the eps- and Pontryagin-reachability results, and the normalization read-off -- the exact-over-Q input to the 78-02 verdict.
  </action>
  <verify>
    1. traceform_invariant_subspace() returns the dimension of the Tr+det_3-generated invariant span EXACTLY over Q (span_rank_over_QQ; no float).
    2. eps-reachability tested via BOTH routes (sqrt|eta| eps from the trace-form metric; det_3 polarization/orientation on V_0) with explicit YES/NO over Q; Pontryagin-reachability confirmed.
    3. If eps reachable AND Pontryagin reachable => dim >= 2 recorded => the eps-contraction is a non-unique choice (the disconfirming observation for STRONG WIN).
    4. Normalization read-off: det_3-fixed vs free/imported determined (a free normalization => fp-imported-action even at dim=1).
    5. The decisive triple (dim; eps-in-span; normalization) assembled for 78-02; reported at true strength (the dimension/identity), NOT relabeled a win; no decisive number on float; input-ban guard still passes.
  </verify>
  <done>The DECISIVE trace-form-invariant subspace dimension is computed exact over Q, with eps-reachability (both routes), Pontryagin-reachability, and the det_3-fixed-vs-free normalization all determined. The decisive triple (dim; eps-in-span YES/NO; normalization det_3-fixed/free) is assembled and stated at true strength as the input to the 78-02 verdict ladder. test-forced-vs-posited's decisive computation is complete (the verdict synthesis + ratification follow in 78-02). The HIGH / most-likely result is dim>1 or normalization-free => fp-imported-action -- reported honestly, not inflated.</done>
</task>

</tasks>

<verification>
- SETUP (Task 1, test-traceform-restriction): det_3 SSOT re-passes its algebraic identities (or Phase-64.1/65 324/324 cited); Tr(X o Y)|frame == eta=diag(+1,-1,-1,-1) exact over Q; the AST/source input-ban guard implemented (mirroring cartan_phaseB_einstein) and passing; octonion_algebra.py absent; no numpy.linalg on the decisive path.
- BARE ANCHOR (Task 2, test-bare-space-dim): the so(3,1)-invariant quadratic-in-curvature 4-form space is dim=2 (Euler + Pontryagin) exact over Q, the two invariants linearly independent on the Phase-77 generic R[omega]. (dim != 2 => setup BUG, debug, no verdict.)
- DECISIVE COUNT (Task 3, test-forced-vs-posited): the trace-form-invariant subspace dimension exact over Q; eps-reachability (sqrt|eta| eps AND det_3-polarization routes) + Pontryagin-reachability + det_3-fixed-vs-free normalization -- the decisive triple for the 78-02 verdict ladder.
- EXACT over Q on every decisive verdict (sympy.Matrix.rank / nullspace over QQ); det SSOT = ring_lemma_verification.py det_3; input-ban guard enforced throughout.
- The audited Einstein object is the LINEAR-in-Riemann eps R^e^e (NOT a quadratic-in-F Maxwell/Pontryagin stress -- the Phase-76 OVERTURNED tautology, [[feedback_test_right_object_not_tautology]]).
- Forbidden proxies rejected: fp-imported-action (count whether eps is forced, do NOT posit the eps-action and expand), fp-float-decisive, fp-octonion-algebra, fp-reuse-cone-hessian, fp-wrong-object.
- Uncertainty markers honored: fp-imported-action is the HIGH / most-likely outcome; a metric supplying sqrt|eta| eps makes BOTH eps and Pontryagin reachable => dim>=2 => still fp-imported-action; do NOT over-claim FORCED or POSITED without the exact count.
</verification>

<success_criteria>
The det_3 SSOT re-passes (or Phase-64.1/65 cited) and Tr(X o Y)|frame == eta=diag(+1,-1,-1,-1) exact over Q; the AST/source input-ban guard is implemented and passes; the bare so(3,1)-invariant quadratic-in-curvature 4-form space is confirmed dim=2 (Euler + Pontryagin) with the two invariants independent on the Phase-77 R[omega]; and the DECISIVE trace-form-invariant subspace dimension is computed exact over Q, with eps-reachability (both routes), Pontryagin-reachability, and the det_3-fixed-vs-free normalization determined. The decisive triple (dim; eps-in-span; normalization) is assembled and stated at true strength as the input to the 78-02 verdict ladder -- not relabeled a win, no decisive number on float. The most-likely result (dim>1 or normalization-free => fp-imported-action) is reported honestly per negative-result-is-success.
</success_criteria>

<output>
After completion, create `.gpd/phases/78-phase-c-circularity-audit-forced-vs-posited/78-01-SUMMARY.md` recording the det SSOT re-pass, the Tr|frame==eta restriction, the input-ban guard, the bare dim=2 anchor, and the DECISIVE trace-form-invariant subspace triple (dim; eps-in-span YES/NO; normalization det_3-fixed/free) -- the exact-over-Q input to the 78-02 verdict.
</output>
