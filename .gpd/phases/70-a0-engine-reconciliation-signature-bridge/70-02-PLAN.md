---
phase: 70-a0-engine-reconciliation-signature-bridge
plan: 02
type: execute
wave: 2
depends_on: ["70-01"]
files_modified: [code/bulk_geometry_verification.py, derivations/70-signature-bridge.tex]
interactive: false

conventions:
  arithmetic: "EXACT over Q (sympy.Rational); ranks via sympy.Matrix.rank(), NEVER numpy.linalg.matrix_rank"
  potential: "FIXED as -log det (not det)"
  cone_metric: "g_X(A,B) = -d^2/ds dt log det(X + sA + tB)|_0 = Hess(-log det) (Faraut-Koranyi Ch. II-IV)"
  complex_structure: "u = e_7; C_u = span{1, e_7}"
  center: "I/3 (F_4-symmetric, rho_J = 0)"
  spacetime_sub_slice: "Peirce {17,18,19,26} = h_2(C_u) == engine-native symbols {x1 (beta), x2 (gamma), x3 (x1.e_0), x10 (x1.e_7)}"
  signature: "mostly-minus (1,3); eta = diag(+1,-1,-1,-1)"
  minkowski_coords: "x0 = (beta+gamma)/2, x3 = (beta-gamma)/2 (from 52-kkt); so b = x0+x3 = beta, g = x0-x3 = gamma"
  bridge: "construction (ii): background eta from h_2(C_u)'s own det_2; cone-Hessian supplies ONLY h_mu_nu"
  units: "N/A — pure algebra/geometry, dimensionless"

dimensional_check:
  # ALGEBRAIC GRADING checks, NOT [mass]^n (pure geometry, no SI content):
  det_3_slice: "homogeneous degree 3 in the 4 sub-slice coords; restricted form b*g/3 - p^2/3 - q^2/3"
  minus_log_det_jet: "Taylor jet of -log det around a point TERMINATES appropriately (det cubic => det_ijkl identically 0); the metric is the degree-2 Hessian term"
  g_mu_nu: "Hess(-log det): a 4x4 symmetric form, well-defined (nondegenerate) at I/3"
  eta: "diag(+1,-1,-1,-1), signature (1,3) mostly-minus"

approximations:
  - name: "none (exact symbolic certification + a FIXED modeling choice)"
    parameter: "no small parameter; M=0 is an EXACT evaluation point (no matter), not a perturbative limit"
    validity: "exact over Q at (M=0, center I/3)"
    breaks_when: "N/A for the gate; whether construction (ii) preserves the V_1/V_{1/2} matter coupling is a Phase-71 question, NOT decided here"
    check: "Minkowski reduction residual g_mu_nu(center, M=0) - eta_mu_nu is exact-Q zero; Hess(-log det)|_{I/3} = diag(9,9,18,18), det 26244"

estimated_execution:
  total_minutes: 65
  breakdown:
    - task: 1
      minutes: 25
      note: "State construction (ii) bridge; ASSERT the index map {17,18,19,26} == engine-native {x1,x2,x3,x10} via slice det form b*g/3 - p^2/3 - q^2/3"
    - task: 2
      minutes: 25
      note: "Hess(-log det)|_{I/3} = diag(9,9,18,18), det 26244 (new computed gate); Minkowski reduction g(center,M=0) - eta == 0 exact"
    - task: 3
      minutes: 15
      note: "h_2(C_u) det=1 hyperboloid = H^3 = SL(2,C)/SU(2); VALD-03 state target curvature -1 (Totaro), cite; defer full computation to Phase 71"

patterns_consulted:
  insights: []
  error_patterns: []
  adjustments_made: []

contract:
  schema_version: 1
  scope:
    question: "Does the FIXED construction-(ii) Riemannian-cone-Hessian -> Lorentzian-slice signature bridge (background eta from h_2(C_u)'s own det_2; cone-Hessian supplying only h_mu_nu) reduce to EXACT Minkowski eta_mu_nu (signature (1,3), mostly-minus) at (M=0, center I/3) with zero residual over Q — and is the spacetime sub-slice index assignment {17,18,19,26} == engine-native {x1,x2,x3,x10} correct?"
  claims:
    - id: claim-signature-bridge
      statement: "There is a precise Riemannian-cone-Hessian -> Lorentzian-slice map (construction (ii): background Lorentzian eta from h_2(C_u)'s own det_2, cone-Hessian perturbation h_mu_nu) that reduces to EXACT Minkowski at (M=0, center I/3). Construction (i) (Wick-rotate via u=e_7) is the REJECTED alternative (unproven C*-bottleneck signature-flip conjecture; naive coordinate Wick rotation manufactures spurious curvature on a curved metric, Visser 1702.05572). The potential is FIXED as -log det; the spacetime sub-slice is {17,18,19,26} == engine-native {x1 (beta), x2 (gamma), x3 (x1.e_0), x10 (x1.e_7)} with the restricted det form b*g/3 - p^2/3 - q^2/3 (b=beta, g=gamma); signature mostly-minus (1,3)."
      deliverables: [deliv-bridge-note, deliv-bulk-engine-geom]
      acceptance_tests: [test-minkowski-reduction, test-index-map, test-hessian-benchmark]
      references: [ref-52-kkt, ref-faraut-koranyi, ref-warm-engine, ref-visser]
    - id: claim-h3-subslice
      statement: "The det=1 hyperboloid inside h_2(C_u) is H^3 = SL(2,C)/SU(2) with CONSTANT NEGATIVE curvature; Totaro's constant sectional curvature -d^2/4 for the rank-1 complex line (d=2) gives the target value -1. (Stated and cited as the VALD-03 limiting-case cross-check; the full constant-curvature computation uses the Phase-71 curvature engine.)"
      deliverables: [deliv-bridge-note]
      acceptance_tests: [test-h3-curvature]
      references: [ref-52-kkt, ref-totaro]
  deliverables:
    - id: deliv-bridge-note
      kind: derivation
      path: derivations/70-signature-bridge.tex
      description: "The A0 signature-bridge statement: construction (ii) (eta from h_2(C_u)'s det_2; cone-Hessian supplies h_mu_nu), construction (i) named as the rejected alternative and why; potential = -log det; the {17,18,19,26} == engine-native {x1,x2,x3,x10} index assignment with the b*g/3 - p^2/3 - q^2/3 slice det form; the Minkowski reduction statement (zero residual at M=0, center); the H^3 = SL(2,C)/SU(2) VALD-03 cross-check with target curvature -1 (Totaro), deferring the full computation to Phase 71."
      must_contain: ["construction (ii)", "construction (i)", "-log det", "eta = diag(+1,-1,-1,-1)", "{17,18,19,26}", "H^3 = SL(2,C)/SU(2)", "-d^2/4 = -1", "M=0", "I/3"]
    - id: deliv-bulk-engine-geom
      kind: code
      path: code/bulk_geometry_verification.py
      description: "Geometry section ADDED to the certified Phase-70 engine (from 70-01): the symbolic Hess(-log det) on the 4 spacetime sub-slice coords; the new computed gate Hess(-log det)|_{I/3} == diag(9,9,18,18) with det 26244; the index-map assertion det_3 restricted to {x1,x2,x3,x10} == beta*gamma/3 - p^2/3 - q^2/3; the construction-(ii) Minkowski reduction gate g_mu_nu(center, M=0) - eta_mu_nu == 0 exact over Q (zero residual h_mu_nu)."
      must_contain: ["Hess", "-log", "diag(9,9,18,18)", "26244", "eta", "reduction", "residual", "diff"]
  references:
    - id: ref-warm-engine
      kind: prior_artifact
      locator: "code/bulk_geometry_verification.py (the certified engine from Plan 70-01) + code/embedding_under_E_verification.py (proj_u_exact, cu_to_complex, slice_to_complex, E)"
      aliases: ["certified engine", "70-01 engine", "embedding engine"]
      role: definition
      why_it_matters: "The certified det_3 (from 70-01) is the SOLE source of det used to build the cone metric Hess(-log det); the embedding engine supplies the h_2(C_u) projection helpers for the sub-slice. The geometry MUST be built on the 70-01-certified det, never on octonion_algebra.py."
      applies_to: [claim-signature-bridge]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use, cite]
    - id: ref-52-kkt
      kind: prior_artifact
      locator: "derivations/52-kkt-spacetime.tex (+ derivations/52-observer-uniqueness.tex)"
      aliases: ["52-kkt", "KKT spacetime"]
      role: benchmark
      why_it_matters: "The source of the background eta for construction (ii): det_2(X) = x0^2 - x1^2 - x2^2 - x3^2, signature (1,3) mostly-minus, forward cone, with Minkowski coords x0=(beta+gamma)/2, x3=(beta-gamma)/2; and the det=1 hyperboloid = H^3 = SL(2,C)/SU(2) identification (VALD-03)."
      applies_to: [claim-signature-bridge, claim-h3-subslice]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-faraut-koranyi
      kind: paper
      locator: "Faraut & Koranyi, Analysis on Symmetric Cones (Oxford, 1994), Ch. II-IV (NOT Ch. V)"
      aliases: ["FK", "Faraut-Koranyi"]
      role: definition
      why_it_matters: "g_X = Hess(-log det) is THE canonical cone metric and is POSITIVE-DEFINITE (Riemannian) — which is precisely WHY a Riemannian->Lorentzian signature bridge is needed at all. Cite for the cone-metric definition; do not re-derive."
      applies_to: [claim-signature-bridge]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [cite]
    - id: ref-totaro
      kind: paper
      locator: "Totaro, 'The curvature of a Hessian metric,' arXiv:math/0401381 (Int. J. Math. 15 (2004) 369-391)"
      aliases: ["Totaro"]
      role: benchmark
      why_it_matters: "Gives constant sectional curvature -d^2/4 for the rank-1 cone slice {f=1}; d=2 (complex line C_u) => -1. The target VALD-03 value for H^3. The full curvature ENGINE is Phase 71; here the value is STATED by citation. Executor CANNOT fetch this paper — the needed result (-d^2/4 = -1 for d=2) is stated inline in the task."
      applies_to: [claim-h3-subslice]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [cite]
    - id: ref-visser
      kind: paper
      locator: "Visser, 'How to Wick rotate generic curved spacetime,' arXiv:1702.05572 (2017)"
      aliases: ["Visser"]
      role: must_consider
      why_it_matters: "Naive coordinate Wick rotation is a complex deformation of the METRIC, not the coordinate, and is chart-dependent on curved metrics — manufacturing spurious curvature. This is the argument used to REJECT construction (i). Not load-bearing for (ii). Executor CANNOT fetch this — the argument is stated inline in the task."
      applies_to: [claim-signature-bridge]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [cite]
  acceptance_tests:
    - id: test-minkowski-reduction
      subject: claim-signature-bridge
      kind: limiting_case
      procedure: "Build the construction-(ii) slice metric g_mu_nu on the 4 spacetime sub-slice coords {x1 (beta), x2 (gamma), x3 (x1.e_0), x10 (x1.e_7)}: background eta = diag(+1,-1,-1,-1) in Minkowski coords (x0=(beta+gamma)/2, x3=(beta-gamma)/2), with h_mu_nu defined as [Hess(-log det) restricted to V_0, in h_2(C_u) coords] minus [its value at (M=0, center I/3)] so h=0 at center BY CONSTRUCTION (fix the V_0<->Minkowski frame ONCE at the center as a fixed linear map). Then evaluate g_mu_nu - eta_mu_nu at (M=0, center I/3) and confirm it is the exact 4x4 zero over Q (zero residual h_mu_nu)."
      pass_condition: "g_mu_nu(center, M=0) - eta_mu_nu == 0 exactly over Q (every entry simplify == 0); residual h_mu_nu is the 4x4 zero matrix; signature of g at the basepoint is (1,3)."
      evidence_required: [deliv-bulk-engine-geom, deliv-bridge-note, ref-52-kkt]
      automation: automated
    - id: test-index-map
      subject: claim-signature-bridge
      kind: consistency
      procedure: "ASSERT (do not trust the reconstruction) that the Peirce index set {17,18,19,26} == engine-native symbols {x1 (beta), x2 (gamma), x3 (octonion-x1 component 0), x10 (octonion-x1 component 7)} by restricting the certified det_3 to those 4 engine-native coordinates (all other 23 coords set to the center I/3 values / zero as appropriate) and confirming the resulting cubic form equals beta*gamma/3 - p^2/3 - q^2/3 (with p = x1.e_0, q = x1.e_7) over Q — the det_2/3 Minkowski form with b=beta, g=gamma."
      pass_condition: "det_3 restricted to {x1,x2,x3,x10} simplifies EXACTLY to beta*gamma/3 - p^2/3 - q^2/3 over Q (correct sign pattern: one +-product diagonal pair, two negative squares); no extra terms."
      evidence_required: [deliv-bulk-engine-geom, ref-warm-engine, ref-52-kkt]
      automation: automated
    - id: test-hessian-benchmark
      subject: claim-signature-bridge
      kind: benchmark
      procedure: "Compute the cone metric Hess(-log det) at the center I/3 restricted to the 4 spacetime sub-slice coords symbolically (sympy.diff x2 of -log(det_3) then evaluate at I/3), exact over Q. This is a NEW computed gate (not pre-computed in any in-repo file)."
      pass_condition: "Hess(-log det)|_{I/3} == diag(9,9,18,18) exactly over Q and det(Hess) == 26244 (!= 0, nondegenerate); float eigenvalue triage (non-decisive) shows {9,9,18,18} > 0 (positive-definite Riemannian before the signature bridge)."
      evidence_required: [deliv-bulk-engine-geom]
      automation: automated
    - id: test-h3-curvature
      subject: claim-h3-subslice
      kind: limiting_case
      procedure: "State and cite that the det=1 hyperboloid in h_2(C_u) (det_2 = x0^2 - x1^2 - x2^2 - x3^2 = 1) is H^3 = SL(2,C)/SU(2) with constant negative curvature, target -d^2/4 = -1 for the rank-1 complex line d=2 (Totaro; 52-kkt). Optionally reinforce with a lightweight 3-dim sympy.diffgeom curvature on the det_2=1 hyperboloid (the det_2 form b*g - p^2 - q^2 is available). The full constant-curvature computation is deferred to the Phase-71 curvature engine."
      pass_condition: "The H^3 = SL(2,C)/SU(2) identification and the target curvature -1 are stated with the Totaro citation in deriv-bridge-note; IF the optional 3-dim diffgeom check is run, its constant sectional curvature == -1 exactly over Q."
      evidence_required: [deliv-bridge-note, ref-totaro, ref-52-kkt]
      automation: hybrid
  forbidden_proxies:
    - id: fp-float-decisive
      subject: claim-signature-bridge
      proxy: "Computing the Minkowski residual, the Hessian benchmark equality (== diag(9,9,18,18)), the index-map slice-form equality, or the H^3 curvature in float (numpy ranks, machine-epsilon residuals, float curvature)."
      reason: "These are discontinuous, float-fragile verdicts; a machine-zero residual is insufficient to certify an UNcontaminated background. EXACT over Q only. Float (eigenvalue triage, isolated cross-check) is allowed solely as an explicitly-labeled non-decisive readout."
    - id: fp-wrong-cross-term
      subject: claim-signature-bridge
      proxy: "Building the cone metric Hess(-log det) from a det other than the 70-01-certified SSOT det_3 — e.g. importing code/octonion_algebra.py's buggy (x1 x2) x3 det_3 on the decisive geometry path."
      reason: "A wrong det cross-term silently corrupts the Hessian and the entire downstream curvature. The geometry MUST be built on the 70-01-certified det_3 (2Re((x2 x1) x3)); never import octonion_algebra.py."
    - id: fp-contaminated-background
      subject: claim-signature-bridge
      proxy: "Double-counting Minkowski by taking eta from det_2 AND keeping the eta-like part of the restricted cone-Hessian, yielding a nonzero constant residual h_mu_nu at the center that masquerades as a cosmological constant (Phase B false positive) or position dependence (Phase A false positive)."
      reason: "Construction (i) and (ii) both have a Minkowski piece; mixing them double-counts. Define h_mu_nu := [restricted cone-Hessian] - [its value at (M=0, center)] so h=0 at center by construction, then verify the exact-Q zero residual. A nonzero residual triggers the backtracking trigger (switch to (i) or STOP)."
    - id: fp-coordinate-curvature
      subject: claim-h3-subslice
      proxy: "Claiming the H^3 negative curvature from non-constant metric components in a bad chart, or asserting a value other than -1 without the Totaro/52-kkt anchor."
      reason: "The VALD-03 cross-check is the CONSTANT negative curvature -d^2/4 = -1 of H^3 = SL(2,C)/SU(2), an honest geometric invariant anchored to Totaro; a chart artifact is not curvature. (The full constant-curvature computation is Phase 71; here the value is stated by citation.)"
  links:
    - id: link-bridge-stated
      source: claim-signature-bridge
      target: deliv-bridge-note
      relation: supports
      verified_by: [test-minkowski-reduction, test-index-map]
    - id: link-bridge-computed
      source: claim-signature-bridge
      target: deliv-bulk-engine-geom
      relation: computes
      verified_by: [test-minkowski-reduction, test-index-map, test-hessian-benchmark]
    - id: link-engine-feeds-geometry
      source: ref-warm-engine
      target: deliv-bulk-engine-geom
      relation: depends_on
      verified_by: [test-hessian-benchmark]
    - id: link-h3-stated
      source: claim-h3-subslice
      target: deliv-bridge-note
      relation: supports
      verified_by: [test-h3-curvature]
  uncertainty_markers:
    weakest_anchors:
      - "The index map {17,18,19,26} == engine-native {x1,x2,x3,x10} was RECONSTRUCTED (from the lower-right h_2(O) block = rows/cols {1,2} and the C_u part = components {0,7}), confirmed via the slice det form — NOT read from a single in-repo line. test-index-map asserts it explicitly rather than trusting the reconstruction."
      - "Construction (ii) is a FIXED modeling choice, not a discovery; the gate it passes (exact Minkowski at M=0, center) is a consistency requirement, NOT evidence for the gravity claim. Whether (ii) preserves the V_1/V_{1/2} matter coupling that Phase B needs is an Open Question deferred to Phase 71."
      - "VALD-03 curvature -1 is STATED by citation (Totaro) here; the full constant-curvature computation needs the Phase-71 engine. The optional 3-dim diffgeom check is reinforcement, not the decisive gate."
    disconfirming_observations:
      - "Nonzero residual h_mu_nu at (M=0, center) over Q -> contaminated bridge (double-counted background or wrong index identification). Per the backtracking trigger: switch to construction (i) or STOP — do NOT proceed."
      - "Hess(-log det)|_{I/3} != diag(9,9,18,18) (or det != 26244) -> wrong sub-slice coordinates, wrong potential (det vs -log det), or a det cross-term error inherited from a bad engine."
      - "det_3 restricted to {x1,x2,x3,x10} has the wrong sign pattern or extra terms -> wrong 4 of the 10 V_0 directions (e.g. internal {20..25} instead of spacetime {17,18,19,26}); re-map and recompute."
      - "det(Hess) == 0 at center -> degenerate metric; wrong coordinates or potential."
---

<objective>
State the A0 signature bridge as construction (ii) — background Lorentzian eta from h_2(C_u)'s own det_2, with the positive-definite cone-Hessian Hess(-log det) supplying ONLY the perturbation h_mu_nu — and PROVE it reduces to EXACT Minkowski eta_mu_nu (signature (1,3), mostly-minus) at (M=0, center I/3) with zero residual over Q. Assert the spacetime sub-slice index assignment, certify the new Hessian benchmark, and state the H^3 = SL(2,C)/SU(2) constant-negative-curvature cross-check.

Purpose: Fix the Lorentzian background and the Riemannian->Lorentzian map BEFORE any curvature is computed (Phase 71+). A contaminated background (nonzero constant h_mu_nu) would masquerade downstream as a cosmological constant or spurious position dependence, corrupting the whole milestone — so the reduce-to-Minkowski gate is a hard prerequisite. Construction (i) (Wick-rotate via u=e_7) is reported as the rejected alternative and why.
Output: derivations/70-signature-bridge.tex (the bridge statement + index assignment + H^3 cross-check) and a geometry section added to the 70-01-certified code/bulk_geometry_verification.py (Hessian benchmark + Minkowski reduction gate), plus the Phase-70 SUMMARY.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/70-a0-engine-reconciliation-signature-bridge/70-RESEARCH.md
@.gpd/phases/70-a0-engine-reconciliation-signature-bridge/70-01-SUMMARY.md
@derivations/52-kkt-spacetime.tex
@code/ring_lemma_verification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: State construction (ii) bridge and ASSERT the spacetime sub-slice index map via the slice det form</name>
  <files>derivations/70-signature-bridge.tex, code/bulk_geometry_verification.py</files>
  <action>
    Write derivations/70-signature-bridge.tex stating the A0 signature bridge, and add an index-map assertion to the certified engine (from 70-01). Start with an `% ASSERT_CONVENTION:` header matching 52-kkt (natural_units=dimensionless, metric_signature=mostly_minus).

    LaTeX content (derivations/70-signature-bridge.tex):
    1. POTENTIAL: fix the cone potential as -log det (Faraut-Koranyi Ch. II-IV); g_X = Hess(-log det) is the canonical cone metric and is POSITIVE-DEFINITE (Riemannian) — state that this positivity is exactly why a Riemannian->Lorentzian bridge is needed.
    2. CONSTRUCTION (ii) [USED]: the background Lorentzian metric eta comes from h_2(C_u)'s OWN cubic/quadratic norm det_2 (52-kkt: det_2 = x0^2 - x1^2 - x2^2 - x3^2, eta = diag(+1,-1,-1,-1), signature (1,3) mostly-minus, Minkowski coords x0=(beta+gamma)/2, x3=(beta-gamma)/2). The cone-Hessian supplies ONLY the perturbation h_mu_nu, DEFINED as [g_X restricted to V_0, in h_2(C_u) coords] minus [its value at (M=0, center I/3)], so h=0 at center BY CONSTRUCTION. Fix the V_0<->Minkowski-index frame ONCE at the center (a fixed linear identification), not per-point.
    3. CONSTRUCTION (i) [REJECTED]: Wick-rotate the restricted positive-definite cone metric via the complex structure u=e_7. Report WHY rejected, stated inline (executor cannot fetch Visser): (a) it relies on the unproven C*-bottleneck signature-flip conjecture; (b) per Visser arXiv:1702.05572, a naive coordinate Wick rotation is a complex deformation of the METRIC (not the coordinate) and is chart-dependent on a curved metric, manufacturing spurious curvature. So (i) does not cleanly give exact Minkowski without an ad-hoc continuation.
    4. SPACETIME SUB-SLICE: state the index assignment Peirce {17,18,19,26} = h_2(C_u) == engine-native symbols {x1 (beta), x2 (gamma), x3 (octonion-x1 component e_0), x10 (octonion-x1 component e_7)} (because the lower-right h_2(O) block is rows/cols {1,2} and the C_u part is octonion components {0,7}; engine layout x3..x10 = octonion x1). Internal V_0 = {20..25} is killed by pi_u.

    Engine assertion (code/bulk_geometry_verification.py, geometry section):
    5. ASSERT the index map (do NOT trust the reconstruction). Build det_3 symbolically on the engine-native coords, set the 4 sub-slice symbols {x1=beta, x2=gamma, x3=p, x10=q} free and all other 23 coords to their center I/3 / zero values, and confirm the restricted cubic form simplifies EXACTLY over Q to beta*gamma/3 - p^2/3 - q^2/3 (= det_2/3 with b=beta, g=gamma, the alpha=1/3 normalization at the center). _report PASS/FAIL. This is the explicit check that {17,18,19,26} are the spacetime (not internal {20..25}) directions.
  </action>
  <verify>
    1. The bridge note states construction (ii) as USED (eta from h_2(C_u)'s det_2; cone-Hessian supplies only h_mu_nu) and construction (i) as REJECTED with the C*-bottleneck + Visser chart-dependence reasons stated inline.
    2. The potential is FIXED as -log det; the positive-definiteness of g_X = Hess(-log det) (hence the need for a bridge) is stated with the Faraut-Koranyi citation.
    3. ALGEBRAIC GRADING (not [mass]^n): det_3 restricted to {x1,x2,x3,x10} is homogeneous degree 3 and equals beta*gamma/3 - p^2/3 - q^2/3 exactly over Q (correct sign pattern: product of the two diagonal coords minus two squares); no extra terms.
    4. The index assignment {17,18,19,26} == engine-native {x1,x2,x3,x10} is asserted (not assumed) and passes; internal {20..25} is named as the excluded W-sector.
  </verify>
  <done>derivations/70-signature-bridge.tex states construction (ii) [used] vs (i) [rejected, with reasons], fixes -log det, and assigns the spacetime sub-slice; the engine asserts det_3 restricted to {x1,x2,x3,x10} == beta*gamma/3 - p^2/3 - q^2/3 exactly over Q, confirming the index map.</done>
</task>

<task type="auto">
  <name>Task 2: Compute the Hessian benchmark diag(9,9,18,18)/det 26244 and prove the construction-(ii) Minkowski reduction (zero residual over Q)</name>
  <files>code/bulk_geometry_verification.py</files>
  <action>
    Add the two decisive geometry gates to the certified engine (built on the 70-01 det_3 — NEVER octonion_algebra.py).

    1. HESSIAN BENCHMARK (new computed gate — not pre-computed in any in-repo file): compute the cone metric Hess(-log det) at the center I/3, restricted to the 4 spacetime sub-slice coords {x1 (beta), x2 (gamma), x3 (p=x1.e_0), x10 (q=x1.e_7)}. Concretely: form f = -log(det_3(X(coords))) with X built from the engine-native symbols, take the symbolic Hessian (sympy.diff twice) in those 4 variables, evaluate at the center I/3 (alpha=beta=gamma=1/3, all off-diagonals 0), exact over Q. The det is cubic so the -log jet terminates at the quadratic (Hessian) term appropriately. _report and ASSERT the 4x4 result == diag(9,9,18,18) over Q and det == 26244 (!= 0, nondegenerate). Add a non-decisive float eigenvalue triage (numpy.linalg.eigvalsh) reporting {9,9,18,18} > 0 (positive-definite Riemannian, before the signature bridge) — clearly labeled "informational, not a verdict".

    2. MINKOWSKI REDUCTION GATE (the decisive gate; contract test-minkowski-reduction): assemble the construction-(ii) slice metric g_mu_nu = eta + h_mu_nu where eta = diag(+1,-1,-1,-1) in the Minkowski coords (x0=(beta+gamma)/2, x3=(beta-gamma)/2; transform the {beta,gamma,p,q} frame to {x0,x1=p,x2=q,x3} via the fixed linear map at the center), and h_mu_nu := [Hess(-log det) restricted to V_0, expressed in h_2(C_u) coords] - [its value at (M=0, center I/3)]. By construction h=0 at the center; at (M=0, center I/3) compute g_mu_nu - eta_mu_nu and ASSERT it is the exact 4x4 zero over Q (every entry simplify == 0), i.e. zero residual h_mu_nu. Also confirm the signature of g at the basepoint is (1,3) (exact-Q Sylvester / leading-principal-minor signs, or a clearly-labeled float eigenvalue-sign triage as a non-decisive cross-check on top of the exact residual).

    3. BACKTRACKING TRIGGER (state in code + SUMMARY): if the residual h_mu_nu at (M=0, center) is provably nonzero over Q AND cannot be removed by re-fixing the V_0<->Minkowski frame at the center, STOP per the roadmap trigger — switch to construction (i) or halt. Do NOT proceed to declare the bridge fixed with a nonzero residual.
  </action>
  <verify>
    1. Hess(-log det)|_{I/3} == diag(9,9,18,18) exactly over Q; det(Hess) == 26244 (nondegenerate); float eigenvalue triage {9,9,18,18}>0 is reported as informational only.
    2. Minkowski reduction: g_mu_nu(center, M=0) - eta_mu_nu == 0 exactly over Q (4x4 zero); residual h_mu_nu is identically zero at the center.
    3. Signature of g at the basepoint is (1,3) mostly-minus (eta = diag(+1,-1,-1,-1)).
    4. ALL decisive verdicts exact over Q (simplify == 0); float used only as labeled non-decisive triage (fp-float-decisive forbidden); the geometry is built on the 70-01-certified det_3, not octonion_algebra.py (fp-wrong-cross-term; fp-contaminated-background avoided by the h := Hess - Hess|center construction).
  </verify>
  <done>The new Hessian gate Hess(-log det)|_{I/3} == diag(9,9,18,18), det 26244 passes exactly over Q; the construction-(ii) Minkowski reduction gives g_mu_nu(center, M=0) - eta_mu_nu == 0 exactly (zero residual h_mu_nu), signature (1,3); the bridge is certified to reduce to EXACT Minkowski at (M=0, center).</done>
</task>

<task type="auto">
  <name>Task 3: State the h_2(C_u) det=1 hyperboloid = H^3 = SL(2,C)/SU(2) cross-check with target curvature -1 (VALD-03)</name>
  <files>derivations/70-signature-bridge.tex</files>
  <action>
    Add the VALD-03 limiting-case cross-check to derivations/70-signature-bridge.tex. This is an independent geometric sanity check on the bulk machinery; per the research, STATE the target by citation here and DEFER the full constant-curvature computation to the Phase-71 curvature engine (do not over-scope this gate phase).

    1. IDENTIFICATION: state that the det=1 hyperboloid inside h_2(C_u) — det_2(X) = x0^2 - x1^2 - x2^2 - x3^2 = 1, forward sheet — is H^3 = SL(2,C)/SU(2), the 3-dimensional hyperbolic space, with constant NEGATIVE curvature (52-kkt; standard). The induced metric is the standard hyperbolic metric.
    2. TARGET VALUE (stated inline; executor cannot fetch Totaro): Totaro (arXiv:math/0401381) gives the Hessian metric of {f=1} for a rank-r cone constant sectional curvature -d^2/4, where d is the dimension/degree parameter of the rank-1 piece. For the rank-1 complex line C_u (d=2) this is -d^2/4 = -1. So the target VALD-03 curvature is exactly -1. Cite Totaro and 52-kkt.
    3. SCOPE NOTE: state explicitly that the full constant-sectional-curvature COMPUTATION (Riemann tensor of the 3-dim hyperboloid metric) uses the Phase-71 curvature engine; Phase 70 records the identification and the target value -1 as the cross-check to be confirmed there.
    4. OPTIONAL reinforcement (planner-sanctioned, NOT required for the gate): if cheap, run a lightweight 3-dim sympy.diffgeom curvature on the det_2=1 hyperboloid (the det_2 form b*g - p^2 - q^2 is available) and confirm the constant sectional curvature == -1 exactly over Q. If run, report it as reinforcement; if not, the citation-stated target stands as the VALD-03 cross-check. (sympy.diffgeom was verified this session on the 2-sphere -> sin^2 theta; the 3-dim hyperboloid is comparable.)
  </action>
  <verify>
    1. The H^3 = SL(2,C)/SU(2) identification of the det=1 hyperboloid in h_2(C_u) is stated with the 52-kkt citation.
    2. The target constant negative curvature -d^2/4 = -1 (d=2, rank-1 complex line) is stated inline with the Totaro citation (executor does not need to fetch the paper).
    3. The scope note defers the full constant-curvature computation to Phase 71; this is a stated-and-cited cross-check (hybrid acceptance), not the decisive gate of Phase 70.
    4. IF the optional 3-dim diffgeom check is run, its constant sectional curvature == -1 exactly over Q (fp-coordinate-curvature avoided: it is the CONSTANT invariant, not a chart artifact).
  </verify>
  <done>derivations/70-signature-bridge.tex states the det=1 hyperboloid = H^3 = SL(2,C)/SU(2) with target constant curvature -d^2/4 = -1 (Totaro, 52-kkt), explicitly deferring the full computation to Phase 71; VALD-03 cross-check recorded (optionally reinforced by a lightweight exact 3-dim diffgeom run giving -1).</done>
</task>

</tasks>

<verification>
- Construction (ii) is STATED as the used bridge (eta from h_2(C_u)'s det_2; cone-Hessian supplies only h_mu_nu) with construction (i) named as the rejected alternative and WHY (C*-bottleneck conjecture; Visser chart-dependence) stated inline.
- The spacetime sub-slice index map {17,18,19,26} == engine-native {x1,x2,x3,x10} is ASSERTED (not assumed) via det_3 restricted to those 4 == beta*gamma/3 - p^2/3 - q^2/3 over Q.
- The new Hessian gate Hess(-log det)|_{I/3} == diag(9,9,18,18), det 26244 passes exactly over Q (nondegenerate).
- The decisive Minkowski reduction g_mu_nu(center, M=0) - eta_mu_nu == 0 exactly over Q (zero residual h_mu_nu, by the h := Hess - Hess|center construction); signature (1,3).
- VALD-03: H^3 = SL(2,C)/SU(2) target curvature -1 stated and cited (Totaro/52-kkt), full computation deferred to Phase 71.
- ALL decisive verdicts exact over Q; geometry built on the 70-01-certified det_3 (NEVER octonion_algebra.py); float only as labeled non-decisive triage.
- "Dimensional analysis" here = algebraic degree/grading (det degree 3; -log det jet terminates; Hessian is the degree-2 metric term), NOT SI [mass]^n — pure geometry, no dimensional content.
</verification>

<success_criteria>
The A0 signature bridge is fixed as construction (ii) and PROVEN to reduce to EXACT Minkowski eta_mu_nu (signature (1,3), mostly-minus) at (M=0, center I/3) with zero residual h_mu_nu over Q; the new benchmark Hess(-log det)|_{I/3} == diag(9,9,18,18), det 26244 passes; the spacetime sub-slice index map {17,18,19,26} == engine-native {x1,x2,x3,x10} is asserted via the slice det form b*g/3 - p^2/3 - q^2/3; construction (i) is documented as the rejected alternative; and the H^3 = SL(2,C)/SU(2) constant-curvature target -1 (VALD-03) is stated and cited with the full computation deferred to Phase 71. The certified engine + fixed background are ready for Phase 71 (h_mu_nu(x) + homogeneity verdict).
</success_criteria>

<output>
After completion, create `.gpd/phases/70-a0-engine-reconciliation-signature-bridge/70-02-SUMMARY.md`.
</output>
