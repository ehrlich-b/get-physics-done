---
phase: 70-a0-engine-reconciliation-signature-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [code/bulk_geometry_verification.py]
interactive: false

conventions:
  arithmetic: "EXACT over Q (sympy.Rational); ranks via sympy.Matrix.rank(), NEVER numpy.linalg.matrix_rank"
  jordan_product: "a o b = (1/2)(ab+ba)"
  octonion_basis: "Fano, e_1 e_2 = e_4; complex structure u = e_7"
  cross_term: "SSOT = 2*Re((x2 x1) x3) (x2 BEFORE x1); buggy (x1 x2) x3 FORBIDDEN"
  det_normalization: "det(diag(a,b,c)) = abc, det(I) = 1, d(X,X,X) = 6*det_3(X)"
  engine_native_layout: "x0,x1,x2 = diag(alpha,beta,gamma); x3..x10 = octonion x1 -> X[2][1]; x11..x18 = octonion x2 -> X[0][2]; x19..x26 = octonion x3 -> X[1][0]"
  units: "N/A — pure algebra, dimensionless"

dimensional_check:
  # ALGEBRAIC DEGREE / GRADING checks, NOT [mass]^n (pure algebra, no SI content):
  det_3: "homogeneous degree 3 in the 27 coords (bidegree (3,0))"
  Tr: "homogeneous degree 1 (bidegree (1,0))"
  Tr2: "homogeneous degree 2 (bidegree (2,0))"
  polarize_d: "homogeneous degree 3, symmetric trilinear; d(X,X,X) = 6*det_3"
  cayley_hamilton_norm: "homogeneous degree 3; must equal det_3 identically"

approximations:
  - name: "none (exact symbolic certification)"
    parameter: "no small parameter, no truncation"
    validity: "exact over Q at all hardcoded rational/octonionic test points"
    breaks_when: "N/A — this is exact algebra; only failure mode is a copy/port error"
    check: "every LOCK is simplify(...) == 0 or .rank() over QQ; LOCK 7b reports full 324/324 count, not a sample"

estimated_execution:
  total_minutes: 55
  breakdown:
    - task: 1
      minutes: 20
      note: "Copy Sections 1-3 verbatim; re-run LOCKs 1-5 + LAYOUT round-trip + exact-only guard"
    - task: 2
      minutes: 20
      note: "LOCK 7a (CH norm at 3 octonionic pts) + LOCK 7b (324/324 inner-derivation annihilation — the slow F_4 certificate)"
    - task: 3
      minutes: 15
      note: "Three-ordering reconciliation pre-flight on non-associative e_4..e_7 data"

patterns_consulted:
  insights: []
  error_patterns: []
  adjustments_made: []

contract:
  schema_version: 1
  scope:
    question: "Is code/ring_lemma_verification.py's det_3 (cross 2Re((x2 x1) x3)) the certified single source of truth for the cubic norm on genuinely non-associative octonionic data, and does a copied bulk_geometry_verification.py reproduce ALL_PASS so every downstream v17.0 curvature is built on a correct, F_4-invariant det?"
  claims:
    - id: claim-engine-ssot
      statement: "code/bulk_geometry_verification.py carries a single certified det_3 (cross 2Re((x2 x1) x3)) that equals the Cayley-Hamilton generic norm at octonionic points (LOCK 7a) and is annihilated by all 324 inner derivations [L_a,L_b] (LOCK 7b), i.e. it is the unique F_4 = Aut(h_3(O))-invariant cubic norm; all of LOCK 1-5 + LAYOUT also pass."
      deliverables: [deliv-bulk-engine]
      acceptance_tests: [test-cross-term-association, test-engine-all-pass]
      references: [ref-warm-engine, ref-faraut-koranyi, ref-h3o-tower]
    - id: claim-cross-term-reconciled
      statement: "On genuinely non-associative (e_4..e_7) octonionic data the three in-repo cross-term orderings are reconciled: (x2 x1) x3 [SSOT] and x2* x1* x3 [conjugated, h3o_tower-style] both equal the CH norm (Re(cross) = +4 at the octonionic test point), while (x1 x2) x3 [octonion_algebra.py, BUGGY] does NOT (Re(cross) = -4, det off by 16); the association difference Re((x1 x2) x3) - Re(x1 (x2 x3)) is NONZERO so the test is non-vacuous."
      deliverables: [deliv-bulk-engine]
      acceptance_tests: [test-cross-term-association]
      references: [ref-warm-engine, ref-h3o-tower]
  deliverables:
    - id: deliv-bulk-engine
      kind: code
      path: code/bulk_geometry_verification.py
      description: "The Phase-70 exact-SymPy-over-Q cubic-norm engine: Sections 1-3 of ring_lemma_verification.py (octonion arithmetic, h3o_matmul, jordan, Tr, Tr2, det_3, polarize_d, cayley_hamilton_norm, X_from_symbols, _standard_basis_27, jordan_L_matrix, inner_derivations, exact_only_guard) copied VERBATIM, re-running LOCKs 1-5/7a/7b ALL_PASS, plus an explicit three-ordering reconciliation pre-flight on non-associative data. Does NOT import code/octonion_algebra.py on the decisive path."
      must_contain: ["def det_3", "def cayley_hamilton_norm", "def inner_derivations", "def exact_only_guard", "2Re((x2 x1) x3)", "LOCK 7a", "LOCK 7b", "324", "reconciliation"]
  references:
    - id: ref-warm-engine
      kind: prior_artifact
      locator: "code/ring_lemma_verification.py (960 ln, ALL_PASS, Phase 64.1) + code/embedding_under_E_verification.py"
      aliases: ["ring_lemma", "warm engine", "SSOT engine"]
      role: definition
      why_it_matters: "The decisive ground truth: det_3 cross 2Re((x2 x1) x3), jordan, cayley_hamilton_norm, inner_derivations, the LOCK harness. COPY Sections 1-3 verbatim; the new module must reproduce its ALL_PASS."
      applies_to: [claim-engine-ssot, claim-cross-term-reconciled]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use, compare, cite]
    - id: ref-h3o-tower
      kind: prior_artifact
      locator: "/Users/ehrlich/repos/blog/research/qualia-fixed-point/h3o_tower.py (796 ln, float64)"
      aliases: ["h3o_tower"]
      role: must_consider
      why_it_matters: "Carries the corrected cross-term in its OWN (conjugated) index naming 2Re(x2* x0* x1); structurally = the SSOT ordering. The THIRD labeling in the reconciliation table. Float64 — formula/labeling reference only, NEVER on the decisive path."
      applies_to: [claim-cross-term-reconciled]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [cite]
    - id: ref-faraut-koranyi
      kind: paper
      locator: "Faraut & Koranyi, Analysis on Symmetric Cones (Oxford, 1994), Ch. II-IV (NOT Ch. V)"
      aliases: ["FK", "Faraut-Koranyi"]
      role: background
      why_it_matters: "g_X = Hess(-log det) is THE canonical (positive-definite) cone metric and R[h_3(O)]^{F_4} = R[Tr, Tr^2, det] is free with degrees 1/2/3 — so det is the decisive degree-3 generator the engine must certify. Cite for the cubic-norm / cone-metric definition; do not re-derive uniqueness."
      applies_to: [claim-engine-ssot]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [cite]
  acceptance_tests:
    - id: test-cross-term-association
      subject: claim-cross-term-reconciled
      kind: cross_method
      procedure: "On at least one genuinely non-associative octonionic point (off-diagonals with nonzero e_4..e_7 components; use ring_lemma's octonionic_points()[1]), evaluate Re(cross) for all three orderings — oct_mul(oct_mul(x2,x1),x3) [SSOT], oct_mul(oct_conj(x2),oct_mul(oct_conj(x1),x3)) [conjugated], oct_mul(oct_mul(x1,x2),x3) [buggy] — and compare each det against cayley_hamilton_norm; also assert the association difference Re((x1 x2) x3) - Re(x1 (x2 x3)) != 0. ALL exact over Q. Additionally certify via Cayley-Hamilton (LOCK 7a) and inner-derivation annihilation (LOCK 7b) that det_3 IS the F_4-invariant norm."
      pass_condition: "SSOT and conjugated orderings give Re(cross) = +4 and det == cayley_hamilton_norm exactly; buggy ordering gives Re(cross) = -4 and det != CH norm (differs by 16); association difference is nonzero; LOCK 7a == 0 at all 3 points; LOCK 7b == 324/324."
      evidence_required: [deliv-bulk-engine, ref-warm-engine]
      automation: automated
    - id: test-engine-all-pass
      subject: claim-engine-ssot
      kind: reproducibility
      procedure: "Run code/bulk_geometry_verification.py main() and confirm LOCK 1 (d(X,X,X)==6*det_3), LOCK 2 (c(X,X)==Tr2), LOCK 3/3b (Fano e1e2=e4), LOCK 4 (det(diag)=abc), LOCK 5 (det(I)=1), LAYOUT round-trip, the exact_only_guard, LOCK 7a, LOCK 7b all report PASS, exactly as ring_lemma_verification.py does."
      pass_condition: "All locks print PASS; ALL_PASS true; no FAIL line; the source-token guard reports no forbidden decisive-path use (no octonion_algebra import, no numpy.linalg.matrix_rank on a verdict)."
      evidence_required: [deliv-bulk-engine, ref-warm-engine]
      automation: automated
  forbidden_proxies:
    - id: fp-wrong-cross-term
      subject: claim-engine-ssot
      proxy: "Using the buggy cubic-norm cross-term association 2Re((x1 x2) x3) (octonion_algebra.py:~2178) or importing code/octonion_algebra.py on the decisive path instead of the corrected SSOT 2Re((x2 x1) x3) from ring_lemma_verification.py."
      reason: "The buggy order passes LOCKs 1-5 but is NOT F_4-invariant (annihilated by only 30 of 324 inner derivations; det off by 16 at the octonionic test point). It silently corrupts every downstream Hessian/curvature. NOTE: the stale .gpd/research/METHODS.md / PITFALLS.md wrongly call octonion_algebra.py the corrected SSOT — follow the contract/brief, NOT those files."
    - id: fp-float-decisive
      subject: claim-engine-ssot
      proxy: "Computing any decisive verdict (LOCK 7a '=0?', LOCK 7b rank/count, the three-ordering '= CH norm?' comparison) in float — numpy.linalg.matrix_rank, float det equality, machine-epsilon tolerances."
      reason: "The decisive quantities (rank, '=0?', F_4-invariance) are discontinuous and float-fragile; a machine-zero is insufficient. EXACT over Q only; ranks via sympy.Matrix.rank(). Float is allowed solely as an explicitly-labeled non-decisive cross-check."
    - id: fp-vacuous-association
      subject: claim-cross-term-reconciled
      proxy: "Running the three-ordering reconciliation on diagonal or e_0..e_3 (quaternionic) data, where all orderings agree because the subalgebra is associative."
      reason: "Those subalgebras are associative, so the check is vacuous and HIDES the bug. The association-invariance pre-flight MUST be on e_4..e_7 data with a nonzero associator real part."
  links:
    - id: link-ssot-certified
      source: claim-engine-ssot
      target: deliv-bulk-engine
      relation: supports
      verified_by: [test-engine-all-pass, test-cross-term-association]
    - id: link-reconciliation
      source: claim-cross-term-reconciled
      target: deliv-bulk-engine
      relation: supports
      verified_by: [test-cross-term-association]
  uncertainty_markers:
    weakest_anchors:
      - "Stale .gpd/research/METHODS.md / PITFALLS.md claim octonion_algebra.py is the corrected SSOT — this is EMPIRICALLY WRONG; the engine SSOT is ring_lemma_verification.py per the contract/brief."
      - "h3o_tower.py is a float64 reference living outside code/; used only as the third (conjugated) labeling in the reconciliation table, never decisive."
    disconfirming_observations:
      - "LOCK 7b returns anything other than 324/324 -> the copied det_3 cross-term order is wrong (a port error). STOP and diff line-by-line against ring_lemma_verification.py."
      - "The three-ordering association difference Re((x1 x2) x3) - Re(x1 (x2 x3)) is 0 -> test inputs are accidentally associative; swap in octonionic_points()."
      - "SSOT and buggy orderings give the SAME det at the octonionic point -> the data is not exercising the cross term; the certification is vacuous."
---

<objective>
Stand up `code/bulk_geometry_verification.py` as the Phase-70 exact-SymPy-over-Q cubic-norm engine and CERTIFY that its `det_3` (cross-term `2*Re((x2 x1) x3)`) is the single source of truth: the unique F_4 = Aut(h_3(O))-invariant generic norm. This is the prerequisite GATE for the entire v17.0 milestone — every downstream curvature is built from derivatives of `det`, so a wrong cross-term association or a contaminated engine silently corrupts everything.

Purpose: Decouple the decisive milestone module from the v16.0 file (repo COPY precedent), pin all conventions in one place, and re-prove the F_4 certificate on a fresh module as cheap insurance against a copy error. Reconcile the three in-repo cross-term orderings on genuinely non-associative data so the `fp-wrong-cross-term` forbidden proxy is explicitly rejected with evidence.
Output: `code/bulk_geometry_verification.py` (copied Sections 1-3, ALL_PASS lock harness re-run, plus a three-ordering reconciliation pre-flight) and the Phase-70 SUMMARY recording the certification.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/70-a0-engine-reconciliation-signature-bridge/70-RESEARCH.md
@code/ring_lemma_verification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy Sections 1-3 verbatim into bulk_geometry_verification.py and re-run LOCKs 1-5 + LAYOUT + exact-only guard</name>
  <files>code/bulk_geometry_verification.py</files>
  <action>
    Create `code/bulk_geometry_verification.py` by COPYING (not importing) Sections 1-3 of `code/ring_lemma_verification.py` VERBATIM. The contract requires standing up the new module by "copying/extending that engine"; do NOT import code/octonion_algebra.py on any decisive path.

    1. Keep the `# ASSERT_CONVENTION:` header line at line ~35 verbatim (jordan=(1/2)(AB+BA); fano e1e2=e4; det3 cross=2Re((x2x1)x3); d(X,X,X)=6*det_3; exact-SymPy-over-Q; ranks=sympy.Matrix.rank(); NEVER float64 on decisive path). Update only the module docstring/title to identify it as the Phase-70 bulk-geometry engine, and add a PROVENANCE note: "copied verbatim from ring_lemma_verification.py Sections 1-3 (Phase 64.1 ALL_PASS); SSOT det_3 = 2Re((x2 x1) x3)".

    2. Copy these functions VERBATIM (exact names, exact bodies): oct_zero, oct, oct_real, oct_add, oct_sub, oct_neg, oct_scal, oct_mul, oct_conj, oct_simplify, oct_is_zero, oct_equal, h3o_from_coords, h3o_identity, octmat_zero/add/sub/scal/simplify/is_zero/equal/dagger, h3o_matmul, jordan, _coord_from_octmat, _oct_normsq, Tr, det_3, Tr2, c, polarize_d, X_from_symbols (and the xs/ys symbols), is_in_Rpt, exact_only_guard, generic_rational_X, octonionic_points, cayley_hamilton_norm, _flat27, _standard_basis_27, jordan_L_matrix, inner_derivations.

    3. CRITICAL — carry the engine-native coordinate layout VERBATIM in X_from_symbols and the comment block: x0,x1,x2 = diag(alpha,beta,gamma) -> X[0][0],X[1][1],X[2][2]; x3..x10 (8) = octonion x1 -> X[2][1]; x11..x18 (8) = octonion x2 -> X[0][2]; x19..x26 (8) = octonion x3 -> X[1][0]. Do NOT "tidy" or re-index this — Plan 70-02's spacetime sub-slice index map depends on it exactly.

    4. In a fresh main(), re-run and _report: LOCK 1 (d(X,X,X) == 6*det_3(X) over Q), LOCK 2 (c(X,X) == Tr2(X)), LOCK 3 (oct_mul(e1,e2) == e4), LOCK 3b (FANO_TRIPLES orientation), LOCK 4 (det_3(diag(a,b,c)) == a*b*g symbolic), LOCK 5 (det_3(I) == 1), the LAYOUT round-trip (_coord_from_octmat(X_from_symbols(xs)) == xs and X/Y use identical constructor), and the exact_only_guard() (which must confirm no forbidden decisive-path token: no `octonion_algebra` import, no `numpy.linalg.matrix_rank`). Per Open Question 2, prefer NO octonion_algebra.py touch at all (drop ring_lemma's single sanctioned oracle-fence touch) — rely on LOCK 7a's float-free CH-norm cross-check.

    5. Use `from sympy import Rational, Matrix, symbols, simplify` etc. exactly as ring_lemma does. Confirm env: `python3 -c "import sympy, numpy; print(sympy.__version__, numpy.__version__)"` reports sympy 1.14.0, numpy 2.4.2 (already satisfied; no new packages).
  </action>
  <verify>
    1. ALGEBRAIC GRADING (this is pure algebra — degree/bidegree checks, NOT [mass]^n): det_3 is homogeneous degree 3; Tr degree 1; Tr2 degree 2; polarize_d degree 3 with d(X,X,X) = 6*det_3 (LOCK 1 == 0 over Q).
    2. LOCK 2 c(X,X) - Tr2(X) simplifies to 0 over Q.
    3. LOCK 3 oct_mul(oct(e1), oct(e2)) == oct(e4) (Fano e1 e2 = e4).
    4. LOCK 4 det_3(diag(a,b,c)) == a*b*c symbolically; LOCK 5 det_3(I) == 1.
    5. LAYOUT round-trip recovers the input 27-tuple exactly; X and Y use X_from_symbols identically.
    6. exact_only_guard() returns OK with NO forbidden decisive-path token (no octonion_algebra import, no numpy.linalg.matrix_rank).
    7. The module imports cleanly and main() runs without exception; the env is sympy 1.14.0 / numpy 2.4.2.
  </verify>
  <done>code/bulk_geometry_verification.py exists with Sections 1-3 copied verbatim (engine-native layout intact); LOCKs 1-5, LAYOUT round-trip, and the exact-only guard all report PASS over Q; module is touch-free of octonion_algebra.py on the decisive path.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run LOCK 7a (Cayley-Hamilton norm) and LOCK 7b (324/324 inner-derivation annihilation) — the F_4 certificate</name>
  <files>code/bulk_geometry_verification.py</files>
  <action>
    Add to main() the two locks that DISCRIMINATE the F_4-invariant norm (LOCKs 1-5 alone do NOT — the buggy ordering passes all of them). These two ARE the certification that det_3 is the single source of truth.

    1. LOCK 7a: for each P in octonionic_points() (3 genuinely octonionic rational points with nonzero e_4..e_7 off-diagonal components), assert `simplify(det_3(P) - cayley_hamilton_norm(P)) == 0` over Q. cayley_hamilton_norm computes N from the degree-3 Cayley-Hamilton relation of the Jordan product: X^o3 - Tr(X) X^o2 + S(X) X - N I = 0 with S = (1/2)(Tr^2 - Tr(X o X)), traced to 3N = Tr(X^o3) - Tr(X) Tr(X^o2) + S Tr(X). This certifies det_3 IS the unique F_4-invariant generic norm. _report the per-point result and the AND over all 3.

    2. LOCK 7b: build inner_derivations() = all nonzero [L_a, L_b] = L_a L_b - L_b L_a over the 27 standard basis elements (a<b), each a 27x27 RATIONAL matrix via jordan_L_matrix. Their span is Der(h_3(O)) = f_4 (dim 52). EVERY one must annihilate det_3. Concretely: pick an octonionic point P, form the gradient of det_3 in the 27 engine-native coords, and verify that for each derivation matrix M, the directional derivative grad(det_3) . (M . P_flat) simplifies to 0 over Q. Count how many of the 324 (= number of nonzero brackets) annihilate det_3. _report the full count "k/324". This MUST be 324/324. (This is the slow lock — tens of seconds for 324 x (27x27 matmul over Q) + simplify. Do NOT sample or downgrade; the full count is the decisive F_4 certificate. If symbolic rank/simplify is too slow, fall back to DomainMatrix over QQ per the orbit_dimension_gate pattern — still exact.)

    3. RED FLAG handling: if LOCK 7b returns < 324/324, this is a PORT ERROR (the copied det_3 cross-term order is wrong), NOT a new result. STOP, diff det_3 line-by-line against ring_lemma_verification.py line 335 (cross = oct_mul(oct_mul(x2, x1), x3)), fix, re-run. Do not proceed to Task 3 or Plan 70-02 until 324/324.
  </action>
  <verify>
    1. LOCK 7a: det_3(P) - cayley_hamilton_norm(P) == 0 exactly over Q at all 3 octonionic points (degree-3 consistency; algebraic, not dimensional-SI).
    2. LOCK 7b: exactly 324/324 nonzero inner derivations [L_a, L_b] annihilate det_3 (full count, not a sample); dim f_4 = 52 is the span dimension cross-check.
    3. Both verdicts computed EXACTLY over Q (sympy simplify == 0 / rank over QQ), NEVER in float — fp-float-decisive.
    4. Deterministic: hardcoded octonionic_points(), no random seeds.
  </verify>
  <done>LOCK 7a (det_3 == Cayley-Hamilton norm at 3 octonionic points) and LOCK 7b (324/324 inner-derivation annihilation) both PASS exactly over Q; det_3 is certified the unique F_4-invariant cubic norm; the new module reproduces ring_lemma's ALL_PASS.</done>
</task>

<task type="auto">
  <name>Task 3: Three-ordering cross-term reconciliation pre-flight on non-associative e_4..e_7 data</name>
  <files>code/bulk_geometry_verification.py</files>
  <action>
    Add the explicit three-ordering reconciliation (the SETU-01 heart; contract test-cross-term-association). This rejects fp-wrong-cross-term WITH EVIDENCE and reconciles the three in-repo labelings. Read code/octonion_algebra.py lines 2134-2181 ONLY to quote the buggy ordering for the contrast row — do NOT import it.

    1. Take the octonionic point P = octonionic_points()[1] (verified this session to give the discriminating values). Extract its three off-diagonal octonions x1, x2, x3 via _coord_from_octmat (they have nonzero e_4..e_7 components -> genuinely non-associative).

    2. NON-VACUITY PRE-FLIGHT (do this FIRST): assert `simplify(oct_mul(oct_mul(x1,x2),x3)[0] - oct_mul(x1,oct_mul(x2,x3))[0]) != 0` over Q — i.e. the associator real part is nonzero, so the data genuinely exercises non-associativity. If this is 0, the inputs are accidentally associative: swap in another octonionic_points() entry. (Anti-pattern fp-vacuous-association.)

    3. Tabulate the three orderings and the discriminating norm, all exact over Q:
       - SSOT       : cross = oct_mul(oct_mul(x2,x1),x3);                       Re(cross) = cross[0]
       - conjugated : cross = oct_mul(oct_conj(x2), oct_mul(oct_conj(x1),x3));  Re(cross) = cross[0]   (h3o_tower-style labeling)
       - buggy      : cross = oct_mul(oct_mul(x1,x2),x3);                       Re(cross) = cross[0]   (octonion_algebra.py order)
       For each, form the corresponding det value N = a*b*g - a*|x1|^2 - b*|x2|^2 - g*|x3|^2 + 2*Re(cross) and compare to cayley_hamilton_norm(P).

    4. Assert and _report the reconciliation:
       - SSOT Re(cross) == +4 and N_SSOT == cayley_hamilton_norm(P) (exact)
       - conjugated Re(cross) == +4 and N_conj == cayley_hamilton_norm(P) (exact; reconciles to SSOT)
       - buggy Re(cross) == -4 and N_buggy != cayley_hamilton_norm(P), with N_SSOT - N_buggy == 2*(+4 - (-4)) == 16 (the documented off-by-16)
       Emit a small reconciliation table (ordering | code expression | Re(cross) | == CH norm?) in the printout and the SUMMARY.

    5. Wire this pre-flight to run inside main() BEFORE the geometry would ever be touched (here, before the script ends), matching the contract requirement that test-cross-term-association passes BEFORE any geometry (Plan 70-02).
  </action>
  <verify>
    1. Non-vacuity: associator real part Re((x1 x2) x3) - Re(x1 (x2 x3)) != 0 over Q (data is genuinely non-associative).
    2. SSOT and conjugated orderings: Re(cross) == +4 and det == cayley_hamilton_norm(P) exactly (they reconcile).
    3. Buggy ordering: Re(cross) == -4 and det != CH norm; N_SSOT - N_buggy == 16 exactly (the off-by-16 signature of the bug).
    4. The reconciliation table is emitted; all comparisons exact over Q (fp-float-decisive forbidden).
    5. The pre-flight runs and passes within main() before any geometry step.
  </verify>
  <done>Three-ordering reconciliation passes: SSOT (+4) and conjugated (+4) both equal the Cayley-Hamilton norm; buggy order (-4) does not and is off by exactly 16; the associator is nonzero (non-vacuous); fp-wrong-cross-term is explicitly rejected with exact-over-Q evidence.</done>
</task>

</tasks>

<verification>
- ALL of ring_lemma's ALL_PASS reproduced on the new module: LOCK 1-5, LAYOUT round-trip, exact_only_guard, LOCK 7a, LOCK 7b.
- LOCK 7a (Cayley-Hamilton norm == det_3) AND LOCK 7b (324/324 inner-derivation annihilation) BOTH pass — both are required for the F_4 certificate; 7b is not skippable.
- Three-ordering reconciliation rejects the buggy cross-term with exact evidence (SSOT/conjugated = CH norm at +4; buggy off by 16 at -4) on genuinely non-associative e_4..e_7 data (nonzero associator).
- Every decisive verdict is exact over Q (sympy simplify == 0 / .rank() over QQ); no float verdict; no octonion_algebra.py on the decisive path; engine-native coordinate layout carried verbatim.
- "Dimensional analysis" here = algebraic degree/grading/bidegree consistency (det degree 3, Tr degree 1, Tr2 degree 2), NOT SI [mass]^n — pure algebra, no dimensional content.
</verification>

<success_criteria>
code/bulk_geometry_verification.py is a certified single-source-of-truth cubic-norm engine: det_3 (cross 2Re((x2 x1) x3)) equals the Cayley-Hamilton generic norm at 3 octonionic points and is annihilated by all 324 inner derivations (the unique F_4-invariant norm); LOCKs 1-5 + LAYOUT + exact-only guard all PASS; the three-ordering reconciliation rejects the buggy (x1 x2) x3 ordering with exact-over-Q evidence (off by 16); the module is touch-free of octonion_algebra.py on the decisive path. This certified engine is consumed by Plan 70-02.
</success_criteria>

<output>
After completion, create `.gpd/phases/70-a0-engine-reconciliation-signature-bridge/70-01-SUMMARY.md`.
</output>
