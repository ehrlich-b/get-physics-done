# Research Log — Phase 64, Plan 01 (BASE-01: exact-SymPy h_3(O) foundation)

**Profile:** deep-theory | **Autonomy:** balanced | **Research mode:** balanced
**Context hint:** code-heavy | **Phase class:** formalism validation
**Bounded execution:** segment_task_cap=3, first_result_gate=true, checkpoint_before_downstream=true
**Reproducibility:** SymPy 1.14.0, Python 3.14.2, macOS Darwin 24.6.0. Exact over Q, deterministic (no random seeds; all test elements hardcoded rational).

## Conventions in effect (from state.json convention_lock — matches PLAN frontmatter)

- Arithmetic: EXACT over Q (SymPy Rational); NO float64 on any decisive path.
- Rank operator: sympy.Matrix(...).rank() over QQ; numpy.linalg.matrix_rank FORBIDDEN.
- Jordan product: X∘Y = (1/2)(XY+YX) — the 1/2 is load-bearing.
- Octonion table: Fano FANO_TRIPLES, e1*e2=e4, e_i*e_i=-1 (matches Paper 7).
- Cubic norm: det_3 = N; cross term LEFT-assoc 2*Re((x1*x2)*x3); det_3(diag)=abc; det_3(I)=1.
- Polarization: d(X,Y,Z) symmetric trilinear; LOCKED d(X,X,X)=6*det_3(X).
- Coupling: c(X,Y)=Tr(X∘Y), bidegree (1,1); c(X,X)=Tr(X^2).

## Source provenance (verified by inspection 2026-05-25)

- Warm exact engine: code/embedding_under_E_verification.py (1071 lines, import-safe — main() guarded by `if __name__=="__main__"` at line 1070).
  - Octonion+matmul+jordan block: lines 93–291 (FANO_TRIPLES, oct_*, _oct_normsq L563, octmat_*, h3o_matmul L262, jordan L280 with Rational(1,2)).
  - det_3 source = T3 inside reduced_charpoly_roots L573–576: `cross = oct_mul(oct_mul(x1,x2),x3)` (LEFT-assoc), `T3 = a*b*g - a*n1 - b*n2 - g*n3 + 2*cross[0]`.
  - Tr source = T1 = alpha+beta+gamma L571.
  - _coord_from_octmat L551: x3=X[1][0], x2=X[0][2], x1=X[2][1].
- Float64 reference spec (FORBIDDEN on decisive path): code/octonion_algebra.py (399 numpy / 0 sympy).
  - det_3 L2152 (diag - quad + cross; left-assoc (x1*x2)*x3) — CONFIRMED identical formula to engine T3.
  - polarize_d L2184 (N(X+Y+Z) - N(X+Y) - N(X+Z) - N(Y+Z) + N(X)+N(Y)+N(Z)) — re-port spec.
  - H3O(alpha,beta,gamma,x1,x2,x3) + Octonion(components) constructors — used for one-time oracle only.

---

## Task 1 — Scaffold + verbatim octonion/matmul/jordan block

DONE (sha 3da1014d). Module scaffolded; octonion+matmul+jordan block copied verbatim.
Verify: runs & exits 0; AST-logic diff vs engine byte-faithful for oct_mul/oct_conj/
h3o_from_coords/h3o_matmul/jordan/_coord_from_octmat/_oct_normsq/oct_add/oct_sub/oct_scal/
octmat_add/octmat_scal/octmat_dagger; _MUL_TABLE & FANO_TRIPLES identical to engine; e_i*e_i=(-1,0);
jordan has Rational(1,2). ASSERT_CONVENTION + REP-DECOMP present. Zero forbidden tokens.

## Task 2 — Lift Tr/det_3, add Tr2/c, re-port polarize_d

Functions added (Section 3): Tr (=T1, alpha+beta+gamma), det_3 (=T3 lifted, LEFT-assoc
cross oct_mul(oct_mul(x1,x2),x3)), Tr2=Tr(jordan(X,X)), c=Tr(jordan(X,Y)), polarize_d
(re-ported onto exact det_3 + octmat_add).

Exact spot-checks (all over Q): det_3(I)==1 TRUE; Tr(I)==3 TRUE; jordan(X,X)==h3o_matmul(X,X)
for Hermitian X TRUE; Tr2(X)==Tr(h3o_matmul(X,X)) TRUE; det_3 uses LEFT-assoc cross (verified
left!=right on test X, det_3 matches left) TRUE; Tr2(X) != (Tr X)^2 TRUE (distinct invariants).
No actual octonion_algebra import (0); no actual _polarized_sharp call (0) — grep hits at
L294/L356 (octonion_algebra) and L354-356 (sharp) are PROVENANCE COMMENTS only, handled by the
Task-5 sentinel-window guard. polarize_d body uses exact det_3.
[CONFIDENCE: HIGH on port-correctness — det_3/Tr spot-checks + non-assoc check + matches float
spec by inspection; full lock evaluation is Task 3.]

## Task 3 — FIVE convention locks exactly over Q (headline first) + one-time float-det oracle

**THE FIRST LOAD-BEARING RESULT + hard correctness gate. ALL FIVE PASS exactly over Q.**
Headline ran FIRST. Generic rational test element Xr = h3o_from_coords(2,3,5, x1,x2,x3) with
e_1..e_6 octonion content (genuinely ambient).

Exact values (over Q):
- det_3(Xr)               = 631276435001/25971865920
- polarize_d(Xr,Xr,Xr)    = 631276435001/4328644320   (= exactly 6*det_3; 25971865920/4328644320 = 6)
- Tr2(Xr) = c(Xr,Xr)      = 2705570622641/64929664800

| Lock | Statement | Result |
|------|-----------|--------|
| 1 (HEADLINE) | polarize_d(X,X,X) - 6*det_3(X) | == 0 exact over Q  [FIRST TIME exact; was float 1.4e-13] |
| 2 | c(X,X) - Tr2(X) | == 0 exact over Q |
| 3 | oct_mul(e1,e2) == e4 (Fano) | TRUE |
| 3b | FANO_TRIPLES == documented orientation | TRUE |
| 4 | det_3(diag(a,b,c)) - a*b*c (symbolic) | == 0 exact |
| 5 | det_3(I) == 1 | TRUE |

One-time float-det ORACLE (NON-DECISIVE, fenced ORACLE-FENCE-BEGIN/END, aliased oa_det_3):
exact float(det_3(Xr))=24.30616409870177 vs float64 octonion_algebra det_3=24.306164098701768;
|delta|=3.55e-15 < 1e-12. Port-correctness CONFIRMED. Exactly 1 `from octonion_algebra import`
statement in the whole file, inside the fence (line 463); decisive locks use ZERO tolerance.

No tolerances on the five decisive locks (exact `simplify(...)==0` / `== <exact value>`).
Backtracking comment recorded at headline lock. Cancellation note: `polarize_d - 6*det_3 = 0` is
an exact ALGEBRAIC identity (polarization of the cubic norm; Springer/Blind), not a numerical
near-cancellation — exact zero is the expected/correct structural result.
[CONFIDENCE: HIGH — headline lock + 4 others independently confirm Jordan 1/2, det left-assoc,
Fano orientation, cubic-norm normalization; float oracle is an independent (different-arithmetic)
cross-check. Port error excluded on all five fronts.]

### FIRST-RESULT SANITY GATE (segment boundary; segment_task_cap=3 reached)
- **Decisive vs proxy:** DECISIVE. The five convention locks ARE the deliverable of the phase's
  verification (claim-engine-locks); they are not proxies. They directly establish that the ported
  engine + lifted invariants carry the locked conventions exactly over Q.
- **Anchor/benchmark already checked?** YES (independently): (i) the headline d=6det was previously
  float-verified in octonion_algebra.py:2299; (ii) the float-det oracle cross-checks against the
  float64 reference (different arithmetic) within 3.55e-15; (iii) det_3(I)=1 / det_3(diag)=abc are
  the Springer cubic-norm normalization anchors.
- **Fastest disconfirmation:** the headline lock being nonzero over Q (would expose wrong det
  left-association / polarize_d sign / Jordan-1/2 slip). It is exactly 0 — disconfirmation path
  exhausted clean. No PORT ERROR.
- **Verdict:** PASS. Safe to pause here and let the orchestrator review the exact lock values
  before Tasks 4-6 (checkpoint_before_downstream=true).

---

## SEGMENT 2 (Tasks 4-6) — resumed after orchestrator cleared first-result gate (independently re-ran, ALL_PASS, ratio 25971865920/4328644320=6)

## Task 4 — 54-symbol pair layout + seven base invariants

xs=symbols('x0:27'), ys=symbols('y0:27') (54 real symbols). X_from_symbols(s) on engine-native
map (x0,x1,x2=diag; x3..10=oct x1@[2][1]; x11..18=oct x2@[0][2]; x19..26=oct x3@[1][0]); Xsym,Ysym
built with the SAME constructor. Seven invariants stored as UNEXPANDED SymPy expressions:
inv_Tr_X/inv_Tr2_X/inv_det_X/inv_Tr_Y/inv_Tr2_Y/inv_det_Y/inv_c. SIX_POINTWISE_GENERATORS +
SEVEN_BASE_INVARIANTS labelled collections.

Harness checks PASS: round-trip _coord_from_octmat(X_from_symbols(xs))==xs (exact); X,Y identical
constructor; diagonal-slice degrees Tr=1/Tr2=2/det=3; Tr2 != (Tr)^2 at a rational point; c bidegree
(1,1) via c(X,0)=0 and c(0,Y)=0. Module runtime 0.54s (NO eager 54-var expansion).

Out-of-harness cross-check (full Poly bidegree over all 54 gens, NOT done in the module): all seven
bidegrees match exactly — Tr X (1,0), Tr X^2 (2,0), det X (3,0), Tr Y (0,1), Tr Y^2 (0,2),
det Y (0,3), c (1,1). inv_c == Tr(jordan(Xsym,Ysym)); inv_Tr2_X == Tr(jordan(Xsym,Xsym)) — both
from jordan, not squared trace.

DEVIATION [Rule 1 - code bug]: `expand` not in the sympy import (added `total_degree` but the
diagonal-degree check needs `expand`). Auto-fixed: added `expand` to the import line. Verified by
re-run (exit 0). Correctness-only, no scope change.
[CONFIDENCE: HIGH — round-trip exact + full-Poly bidegrees match + jordan-built confirmed.]

Peirce basis flagged in a Section-4 comment as a DELIBERATE Phase-65 decision (engine-native basis
used here; invariants are basis-agnostic). No f_4/orbit/Jacobian work — deferred.

## Task 5 — Freeze R_pt + install exact-only guard

R_PT_FROZEN_DEFINITION constant (verbatim reusable block for Phases 66/67/68): six generators
{Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y} = R[..X] (x) R[..Y]; "Tr(X)*Tr(Y) IN R_pt" (reducible
(1,1) member); claim (b) "c NOT IN R_pt" STATED-not-proven. is_in_Rpt(p) stub references the
six-generator definition, raises NotImplementedError("decision procedure is Phase 66"). Both
phrasings present ("c not in R_pt" + "c NOT IN R_pt") to satisfy deliverable + acceptance test.

Harness PASS: R_pt records six generators / Tr(X)Tr(Y) member / claim (b); six generators ==
the six pointwise base invariants (Task 4 consistency); is_in_Rpt(inv_c) raises (c NOT decided
in Phase 64); exact-only guard PASS.

EXACT-ONLY GUARD (Success Criterion 4) — robust per orchestrator's correctness flag:
- Matches actual IMPORT STATEMENTS via `re.match(r"^\s*(from\s+octonion_algebra\s+import\b|import\s+octonion_algebra\b)")` on comment-stripped lines, NOT substrings.
- Matches float-rank as a real CALL `(np|numpy)\.linalg\.matrix_rank\s*\(` (trailing '(' distinguishes a live call from string/regex/comment prose).
- Sentinel-window ORACLE-FENCE-BEGIN/END: the single aliased oa_det_3 import is allowlisted; guard asserts EXACTLY 1 octonion_algebra import in-file and it is in-fence; 0 outside; 0 float-rank calls. NOT allowlist-by-filename.
- RANK_ROUTING_CONVENTION documented (all ranks via sympy.Matrix.rank() over QQ).

FIRST GUARD ATTEMPT FAILED (false-fail) on the guard's OWN doc/regex mentions of "numpy.linalg.matrix_rank"
(3 hits: comment L486/507, RANK_ROUTING_CONVENTION string L491, regex pattern L526) — the exact mirror of
the import-prose problem the orchestrator flagged. FIX: require a call-paren + strip comments. This is a
[Rule 1 - code bug] self-caught + auto-fixed; re-run exits 0.

ADVERSARIAL GUARD VERIFICATION (out-of-harness, temp copies, real file untouched):
- baseline real module: PASS
- inject real out-of-fence `from octonion_algebra import`: guard FAIL (caught, 1 outside)
- inject real `np.linalg.matrix_rank(M)` call: guard FAIL (caught)
- inject real `numpy.linalg.matrix_rank(M)` call: guard FAIL (caught)
- inject provenance COMMENT naming both tokens: guard still PASS (prose ignored)
=> guard catches genuine violations AND ignores prose. Temp copies cleaned (no leftovers).
[CONFIDENCE: HIGH — guard verified by adversarial mutation, not just by passing on clean input.]

Scope discipline: grep for f_4/orbit/Jacobian/Molien/Hilbert/peirce_basis_27/rank-call confirms ALL
hits are documentation/comments (phase roadmap L8-9, Peirce-deferral L372-375, guard convention
doc/regex L485-527) — NONE does the work. c-independence NOT decided. No Phase 65/66/67/68 overreach.

## Task 6 — single-state ("Observable") ring by citation

SINGLE_STATE_RING_NOTE constant: R[h_3(O)]^{F_4} = R[Tr, Tr^2, det], free poly algebra, trdeg 3,
generator degrees 1/2/3. Citation: Faraut-Koranyi (OUP 1994) Ch. II-IV (Thm IV.2.5 region) +
Springer 1962/1973. CITATION CORRECTION recorded verbatim: fact lives in Ch. II-IV (II=Euclidean
Jordan algebras, III=Peirce, IV=classification), NOT Ch. V (=conical/spherical polynomials);
"corrected to II-IV". Observable single-frame ring IDENTIFIED with the pointwise single-copy subring
R[Tr_X, Tr_X^2, det_X]. IV.2.5 number MEDIUM-confidence (paywalled); chapter range II-IV + correction
HIGH-confidence (verified twice: survey + TOC).

Harness PASS: note states R[Tr,Tr^2,det]; cites FK Ch. II-IV; records Ch.V->II-IV correction;
identifies Observable ring w/ pointwise single-copy subring; consistency (3 single-copy gens,
degrees 1/2/3, trdeg 3). NO Reynolds/Jacobian re-derivation (the only "Reynolds/Jacobian" grep hit
L479 is the comment stating we do NOT re-derive — Caveat 2 of RESEARCH.md). CITATION, not computation.
[CONFIDENCE: HIGH on ring/chapter/correction (established 1962-1994 math, verified twice); MEDIUM on
exact theorem number IV.2.5 (paywalled) — robust anchor is chapter range + Springer.]

## FULL HARNESS — all 6 tasks end-to-end

`python3 code/ring_lemma_verification.py` exits 0, ALL_PASS, 25 checks PASS, runtime 0.57s.
All 8 deliverable must_contain token sets satisfied (deliv-module, deliv-engine-block,
deliv-invariant-fns, deliv-layout, deliv-seven-invariants, deliv-rpt-freeze, deliv-single-state-doc,
deliv-exact-guard). Reproducibility: SymPy 1.14.0, Python 3.14.2, exact over Q, deterministic.

