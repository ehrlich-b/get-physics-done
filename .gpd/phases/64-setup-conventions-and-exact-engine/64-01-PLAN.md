---
phase: 64-setup-conventions-and-exact-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [code/ring_lemma_verification.py]
interactive: false

conventions:
  arithmetic: "exact symbolic over Q (SymPy 1.14, Rational/QQ); NO float64 on any decisive path"
  rank_operator: "sympy.Matrix(...).rank() over QQ; numpy.linalg.matrix_rank FORBIDDEN on decisive path"
  jordan_product: "X∘Y = (1/2)(XY + YX) -- the 1/2 is load-bearing"
  octonion_table: "Fano FANO_TRIPLES, e1*e2 = e4, e_i*e_i = -1 (matches Paper 7)"
  cubic_norm: "det_3 = cubic norm N; cross term LEFT-assoc 2*Re((x1*x2)*x3); det_3(diag(a,b,c))=a*b*c; det_3(I)=1"
  polarization: "d(X,Y,Z) = N(X+Y+Z)-N(X+Y)-N(X+Z)-N(Y+Z)+N(X)+N(Y)+N(Z); locked d(X,X,X) = 6*det_3(X)"
  coupling: "c(X,Y) = Tr(X∘Y), bidegree (1,1); c(X,X) = Tr(X^2)"
  coordinatization: "engine-native (alpha,beta,gamma + 3 octonions = 3 + 24 = 27 per copy); X and Y identical conventions; Peirce-adapted basis DEFERRED to Phase 65"
  R_pt: "R-subalgebra generated by {Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y} = R[..X] (x) R[..Y]; FROZEN"

dimensional_check:
  Tr_X: 'polynomial degree 1 (bidegree (1,0))'
  Tr_X2: 'polynomial degree 2 (bidegree (2,0))'
  det_X: 'polynomial degree 3 (bidegree (3,0))'
  Tr_Y: 'polynomial degree 1 (bidegree (0,1))'
  Tr_Y2: 'polynomial degree 2 (bidegree (0,2))'
  det_Y: 'polynomial degree 3 (bidegree (0,3))'
  c: 'polynomial total degree 2, bidegree (1,1)'

approximations: []
# NONE. This phase is exact-symbolic over Q end to end. No small parameter, no
# truncation, no convergence. Any float64 on the decisive path is a DEFECT, not
# an approximation.

estimated_execution:
  total_minutes: 95
  breakdown:
    - task: 1
      minutes: 20
      note: "Copy exact octonion+matmul+jordan block with provenance header + ASSERT_CONVENTION + assert harness skeleton"
    - task: 2
      minutes: 20
      note: "Lift Tr/det_3 standalone, add Tr2/c, re-port polarize_d onto exact det_3 (unfamiliar non-assoc formalism x1.5)"
    - task: 3
      minutes: 20
      note: "Verify 5 convention locks exactly over Q (headline d(X,X,X)=6*det_3 first) + one-time float-det oracle cross-check"
    - task: 4
      minutes: 15
      note: "Build 54-symbol pair layout (x0..x26, y0..y26) + seven base invariants as SymPy expressions (no eager expand)"
    - task: 5
      minutes: 10
      note: "Freeze + document R_pt (six-generator predicate, Tr(X)Tr(Y) member, claim (b)); add exact-only guard self-check"
    - task: 6
      minutes: 10
      note: "Confirm + document single-state ring R[Tr,Tr^2,det] with FK Ch II-IV citation (record Ch. V correction)"

patterns_consulted:
  insights: []
  error_patterns: []
  adjustments_made: []
# .gpd/INSIGHTS.md and .gpd/ERROR-PATTERNS.md not present at project root; phase-scoped
# pitfalls (Pitfalls 1-4 / 7) are carried via forbidden_proxies and the convention locks.

contract:
  schema_version: 1

  scope:
    question: "Is the frozen exact-SymPy h_3(O) algebraic foundation (ported engine, 54-symbol pair layout, seven base invariants, frozen R_pt, exact convention locks, single-state ring confirmation) correct over Q so that every downstream (RING) phase reuses identical, convention-locked, float-free objects?"

  claims:
    - id: claim-engine-locks
      statement: "The exact-SymPy h_3(O) engine (octonion arithmetic over Q with Fano e1*e2=e4, Jordan product (1/2)(XY+YX), left-assoc cubic norm det_3, polarization polarize_d) is ported into code/ring_lemma_verification.py and all five convention locks hold EXACTLY over Q: polarize_d(X,X,X)==6*det_3(X); c(X,X)==Tr(X^2); octonion table e1*e2==e4; det_3(diag(a,b,c))==a*b*c; det_3(I)==1."
      deliverables: [deliv-module, deliv-engine-block, deliv-invariant-fns]
      acceptance_tests: [test-lock-polarize, test-lock-coupling, test-lock-fano, test-lock-diag, test-lock-identity, test-port-oracle]
      references: [ref-warm-engine, ref-octonion-algebra-spec, ref-springer, ref-blind]
    - id: claim-layout-invariants
      statement: "The pair (X,Y) in h_3(O) (+) h_3(O) is coordinatized by 54 SymPy symbols (x0..x26 for X, y0..y26 for Y) on the engine-native layout (3 diagonal reals + 3 octonions x 8 per copy), X and Y using IDENTICAL conventions, and the seven base invariants {Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y, c=Tr(X∘Y)} are constructed as SymPy expressions with the correct bidegrees (1,0)/(2,0)/(3,0)/(0,1)/(0,2)/(0,3)/(1,1)."
      deliverables: [deliv-layout, deliv-invariant-fns, deliv-seven-invariants]
      acceptance_tests: [test-layout-roundtrip, test-invariant-bidegree]
      references: [ref-warm-engine]
    - id: claim-rpt-frozen
      statement: "R_pt is FROZEN as the R-subalgebra of R[h_3(O)+h_3(O)]^{F_4} generated by exactly the six single-state generators {Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y} = R[Tr_X,Tr_X^2,det_X] (x) R[Tr_Y,Tr_Y^2,det_Y]; Tr(X)*Tr(Y) is recorded as a MEMBER of R_pt; claim (b) is recorded verbatim as 'c not in R_pt' (NOT proven here)."
      deliverables: [deliv-rpt-freeze]
      acceptance_tests: [test-rpt-membership, test-rpt-frozen-text]
      references: [ref-pitfalls, ref-project-scope]
    - id: claim-single-state-ring
      statement: "The single-state ('Observable') ring R[h_3(O)]^{F_4} = R[Tr, Tr^2, det] (trdeg 3, generator degrees 1/2/3) is confirmed BY CITATION (Faraut-Koranyi Ch. II-IV, Thm IV.2.5 region; Springer 1962/1973), with the citation correction recorded that this fact lives in Ch. II-IV and NOT Ch. V (= conical/spherical polynomials); the 'Observable' single-frame ring is identified with exactly the pointwise single-copy subring."
      deliverables: [deliv-single-state-doc]
      acceptance_tests: [test-citation-recorded]
      references: [ref-faraut-koranyi, ref-springer]
    - id: claim-exact-only-guard
      statement: "The decisive path is exact-only: code/ring_lemma_verification.py imports/embeds the SymPy octonion block and a guard confirms no 'from octonion_algebra import ...' and no 'numpy.linalg.matrix_rank'/'np.linalg.matrix_rank' token is reachable on the decisive path, so Phases 65-67 inherit a float-free rank guarantee."
      deliverables: [deliv-exact-guard]
      acceptance_tests: [test-exact-only-guard]
      references: [ref-pitfalls, ref-octonion-algebra-spec]

  deliverables:
    - id: deliv-module
      kind: code
      path: code/ring_lemma_verification.py
      description: "New exact-SymPy verification module for the (RING) milestone; assert-based _report/ALL_PASS/sys.exit harness exiting nonzero on any lock failure."
      must_contain: ["ALL_PASS", "_report", "if __name__ == \"__main__\"", "ASSERT_CONVENTION"]
    - id: deliv-engine-block
      kind: code
      path: code/ring_lemma_verification.py
      description: "The exact octonion + 3x3 octonion matmul + Jordan block (FANO_TRIPLES, _MUL_TABLE, oct_* ops, _oct_normsq, h3o_from_coords, h3o_identity, octmat_*, h3o_matmul, jordan, _coord_from_octmat), copied verbatim from the warm engine with a provenance header."
      must_contain: ["FANO_TRIPLES", "h3o_matmul", "jordan", "h3o_from_coords", "_coord_from_octmat", "_oct_normsq"]
    - id: deliv-invariant-fns
      kind: code
      path: code/ring_lemma_verification.py
      description: "Standalone Tr(X), Tr2(X)=Tr(jordan(X,X)), det_3(X) (lifted from the inlined T3), polarize_d(X,Y,Z) (re-ported onto exact det_3), c(X,Y)=Tr(jordan(X,Y))."
      must_contain: ["def Tr(", "def Tr2(", "def det_3(", "def polarize_d(", "def c("]
    - id: deliv-layout
      kind: code
      path: code/ring_lemma_verification.py
      description: "54-symbol pair coordinatization: xs=symbols('x0:27'), ys=symbols('y0:27'), and X_from_symbols(s) mapping the engine-native layout (alpha,beta,gamma + 3 octonions) to a Hermitian octonion matrix; X and Y identical."
      must_contain: ["x0:27", "y0:27", "X_from_symbols"]
    - id: deliv-seven-invariants
      kind: code
      path: code/ring_lemma_verification.py
      description: "The seven base invariants as SymPy expression objects on the 54-symbol layout (Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y, c), NOT eagerly expanded."
      must_contain: ["Tr X", "det X", "c = Tr(X"]
    - id: deliv-rpt-freeze
      kind: note
      path: code/ring_lemma_verification.py
      description: "Frozen R_pt definition as a verbatim module docstring/constant: the six-generator predicate, R_pt = R[..X] (x) R[..Y], Tr(X)Tr(Y) in R_pt, claim (b) = 'c not in R_pt' (stated, not proven)."
      must_contain: ["R_pt", "Tr(X)*Tr(Y)", "c not in R_pt"]
    - id: deliv-single-state-doc
      kind: note
      path: code/ring_lemma_verification.py
      description: "Documented single-state ring confirmation: R[h_3(O)]^{F_4} = R[Tr,Tr^2,det], trdeg 3, degrees 1/2/3, cite Faraut-Koranyi Ch. II-IV (Thm IV.2.5) + Springer, with the explicit Ch. V -> Ch. II-IV citation correction recorded."
      must_contain: ["R[Tr, Tr^2, det]", "Faraut", "Ch. II-IV", "NOT Ch. V"]
    - id: deliv-exact-guard
      kind: code
      path: code/ring_lemma_verification.py
      description: "Exact-only guard: a self-check that scans the module source for forbidden tokens (from octonion_algebra, numpy.linalg.matrix_rank, np.linalg.matrix_rank) on the decisive path and asserts none present; documents the sympy.Matrix.rank() rank-routing convention for downstream phases."
      must_contain: ["from octonion_algebra", "numpy.linalg.matrix_rank", "exact-only guard"]

  references:
    - id: ref-warm-engine
      kind: prior_artifact
      locator: "code/embedding_under_E_verification.py"
      aliases: ["warm engine", "exact-SymPy h_3(O) engine", "embedding_under_E"]
      role: definition
      why_it_matters: "THE exact-SymPy engine being ported. Provides the verified octonion block (FANO_TRIPLES, oct_*, h3o_matmul, jordan with 1/2), the coord<->matrix maps, the inlined T1=Tr / T3=det_3 (lines 567-580, left-assoc cross term), and the _report/ALL_PASS/sys.exit harness pattern. The port reuses this verbatim; do NOT re-derive octonion arithmetic."
      applies_to: [claim-engine-locks, claim-layout-invariants]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use, cite]
    - id: ref-octonion-algebra-spec
      kind: prior_artifact
      locator: "code/octonion_algebra.py"
      aliases: ["float64 octonion_algebra", "float reference spec"]
      role: must_consider
      why_it_matters: "FLOAT64 reference spec ONLY (399 numpy / 0 sympy). Carries the canonical standalone det_3 (line 2152) and 3-arg polarize_d (line 2184) formula bodies to RE-PORT to exact SymPy. It is the thing the exact-only guard must FORBID on the decisive path; the single sanctioned touch is a one-time float-det oracle cross-check (test-port-oracle), which is NOT on the decisive path."
      applies_to: [claim-engine-locks, claim-exact-only-guard]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, avoid]
    - id: ref-faraut-koranyi
      kind: paper
      locator: "Faraut, J.; Koranyi, A. -- Analysis on Symmetric Cones, Oxford (1994), Ch. II-IV (Thm IV.2.5 region)"
      aliases: ["Faraut-Koranyi", "FK 1994"]
      role: benchmark
      why_it_matters: "Source for the single-state ring R[h_3(O)]^{F_4} = R[Tr, Tr^2, det], trdeg 3, degrees 1/2/3. CITATION (not a computation). Citation correction REQUIRED: the fact lives in Ch. II-IV (Thm IV.2.5 region), NOT Ch. V (= conical/spherical polynomials)."
      applies_to: [claim-single-state-ring]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [cite, use]
    - id: ref-springer
      kind: paper
      locator: "Springer, T.A. -- Jordan Algebras and Algebraic Groups, Springer (1973); Indag. Math. 24 (1962) 259-265 (cubic norm)"
      aliases: ["Springer 1962/1973", "cubic norm"]
      role: benchmark
      why_it_matters: "Cubic norm structure, F_4 = Aut(h_3(O)), det normalization det(diag(a,b,c))=abc, det(I)=1, uniqueness of the cubic norm. The det_3 / cubic-norm anchor justifying the diagonal and identity locks."
      applies_to: [claim-engine-locks, claim-single-state-ring]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [cite]
    - id: ref-blind
      kind: paper
      locator: "Blind, B. -- J. Lie Theory 21 (2011) 123-144 (arXiv:0906.5525), Section 3"
      aliases: ["Blind 2011"]
      role: benchmark
      why_it_matters: "Polarization normalization d(X,X,X) = 6*det X -- the headline convention lock. Confirms the 6 prefactor for the full polarization of the cubic norm."
      applies_to: [claim-engine-locks]
      carry_forward_to: [verification]
      must_surface: false
      required_actions: [cite]
    - id: ref-pitfalls
      kind: prior_artifact
      locator: ".gpd/research/PITFALLS.md (Pitfall 7 frozen R_pt; convention traps table: Jordan 1/2, det normalization, sharp-vs-d)"
      aliases: ["PITFALLS", "Pitfall 7"]
      role: must_consider
      why_it_matters: "Carries the FROZEN R_pt phrasing (six-generator definition, Tr(X)Tr(Y) member, claim (b)) and the convention-trap detection (Jordan 1/2, det left-assoc, sharp-vs-d). Drives the R_pt freeze and the convention-lock design."
      applies_to: [claim-rpt-frozen, claim-exact-only-guard]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use]
    - id: ref-project-scope
      kind: spec
      locator: ".gpd/PROJECT.md ((RING) scope; reward-hacking guard on 'pointwise'/'reducible'; v16.0 independent of v15.0)"
      aliases: ["PROJECT.md", "RING scope"]
      role: must_consider
      why_it_matters: "Reward-hacking guard: do NOT redefine 'pointwise'/'reducible' so c trivially lands in/out of R_pt; do NOT assert c's independence here (Phase 66); do NOT entangle with v15.0. Binds the R_pt freeze phrasing and the scope discipline."
      applies_to: [claim-rpt-frozen]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [use]

  acceptance_tests:
    - id: test-lock-polarize
      subject: claim-engine-locks
      kind: consistency
      procedure: "Pick a generic rational X in h_3(O) (rational diagonal + rational octonion off-diagonals). Compute simplify(polarize_d(X,X,X) - 6*det_3(X)) over Q. This is the HEADLINE lock, run FIRST. (Previously verified only at float tolerance 1.4e-13; establish it exactly over Q for the first time.)"
      pass_condition: "simplify(polarize_d(X,X,X) - 6*det_3(X)) == 0 exactly over Q."
      evidence_required: [deliv-invariant-fns, ref-blind]
      automation: automated
    - id: test-lock-coupling
      subject: claim-engine-locks
      kind: consistency
      procedure: "With c(X,Y):=Tr(jordan(X,Y)) and Tr2(X):=Tr(jordan(X,X)), compute simplify(c(X,X) - Tr2(X)) at a generic rational X over Q."
      pass_condition: "simplify(c(X,X) - Tr2(X)) == 0 exactly over Q (the coupling reduces to the quadratic trace on the diagonal)."
      evidence_required: [deliv-invariant-fns]
      automation: automated
    - id: test-lock-fano
      subject: claim-engine-locks
      kind: consistency
      procedure: "Compute oct_mul(e1, e2) where e1 has component 1 set and e2 has component 2 set; check the result equals e4 (component 4 == 1, all else 0). Cross-check FANO_TRIPLES matches the octonion_algebra.py table and Paper 7."
      pass_condition: "oct_mul(e1,e2) == e4 exactly; FANO_TRIPLES orientation matches the reference table."
      evidence_required: [deliv-engine-block]
      automation: automated
    - id: test-lock-diag
      subject: claim-engine-locks
      kind: consistency
      procedure: "With symbolic a,b,c, compute simplify(det_3(h3o_from_coords(a,b,c,0,0,0)) - a*b*c) over Q."
      pass_condition: "simplify(det_3(diag(a,b,c)) - a*b*c) == 0 exactly (cubic-norm diagonal normalization)."
      evidence_required: [deliv-invariant-fns]
      automation: automated
    - id: test-lock-identity
      subject: claim-engine-locks
      kind: consistency
      procedure: "Compute det_3(h3o_identity())."
      pass_condition: "det_3(I) == 1 exactly (identity normalization)."
      evidence_required: [deliv-invariant-fns]
      automation: automated
    - id: test-port-oracle
      subject: claim-engine-locks
      kind: oracle
      procedure: "ONE-TIME port-correctness cross-check: evaluate the lifted exact det_3 and the float64 octonion_algebra.py det_3 on a SHARED rational element; confirm the exact value equals the float value within float tolerance. This is the ONLY sanctioned touch of the float64 file and is NOT on the decisive path (it is an external oracle for a single equality, isolated from the locks)."
      pass_condition: "float(exact_det_3(R)) == octonion_algebra.det_3(R_float) within 1e-12 relative; documented as a one-time non-decisive cross-check."
      evidence_required: [deliv-invariant-fns, ref-octonion-algebra-spec]
      automation: automated
    - id: test-layout-roundtrip
      subject: claim-layout-invariants
      kind: consistency
      procedure: "Build X = X_from_symbols(xs) and recover (alpha,beta,gamma,x1,x2,x3) via _coord_from_octmat(X); confirm the recovered coordinates equal the input symbols (x3=X[1][0], x2=X[0][2], x1=X[2][1] per the engine-native map). Confirm X and Y use identical X_from_symbols."
      pass_condition: "Round-trip _coord_from_octmat(X_from_symbols(xs)) returns exactly (xs[0],xs[1],xs[2], xs[3:11], xs[11:19], xs[19:27]); same constructor used for ys."
      evidence_required: [deliv-layout]
      automation: automated
    - id: test-invariant-bidegree
      subject: claim-layout-invariants
      kind: consistency
      procedure: "Confirm the seven invariants are SymPy expressions with the documented bidegrees: Tr X = xs[0]+xs[1]+xs[2] (deg 1 in X, 0 in Y); Tr2(X) deg 2 in X; det_3(X) deg 3 in X; symmetrically for Y; c=Tr(jordan(X,Y)) bidegree (1,1). Verify Tr2 is Tr(jordan(X,X)) and NOT (Tr X)**2 (distinct degree-2 invariant)."
      pass_condition: "All seven expressions exist with correct bidegrees; Tr2 is built from jordan, not squared trace; expressions are NOT eagerly expanded (no full 54-var simplify)."
      evidence_required: [deliv-seven-invariants, deliv-invariant-fns]
      automation: automated
    - id: test-rpt-membership
      subject: claim-rpt-frozen
      kind: existence
      procedure: "Confirm the frozen R_pt definition records (a) the six generators {Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y}; (b) Tr(X)*Tr(Y) explicitly as a MEMBER of R_pt (product of two single-state generators); (c) claim (b) stated verbatim as 'c not in R_pt'. A membership-predicate stub is_in_Rpt referencing the six-generator definition is present for downstream phases."
      pass_condition: "R_pt freeze text contains the six generators, Tr(X)Tr(Y) recorded IN R_pt, and 'c not in R_pt' as the stated (not proven) claim (b)."
      evidence_required: [deliv-rpt-freeze]
      automation: hybrid
    - id: test-rpt-frozen-text
      subject: claim-rpt-frozen
      kind: existence
      procedure: "Confirm R_pt is documented as a single verbatim frozen-text block (docstring/constant) intended to be CITED verbatim by Phases 66/67/68 (anti-drift, Pitfall 7), and that 'pointwise'/'reducible' point back to the six-generator definition. Confirm c's independence is NOT asserted/proven here."
      pass_condition: "Frozen R_pt text present as a reusable block; no proof or assertion of 'c not in R_pt' (independence deferred to Phase 66); no redefinition of 'pointwise'."
      evidence_required: [deliv-rpt-freeze]
      automation: hybrid
    - id: test-citation-recorded
      subject: claim-single-state-ring
      kind: existence
      procedure: "Confirm the module documents R[h_3(O)]^{F_4} = R[Tr, Tr^2, det] (trdeg 3, degrees 1/2/3) with the Faraut-Koranyi Ch. II-IV (Thm IV.2.5) + Springer citation, the explicit 'Ch. V -> Ch. II-IV' correction recorded, and the identification of the 'Observable' single-frame ring with the pointwise single-copy subring. This is a CITATION, NOT a re-derivation."
      pass_condition: "Single-state ring stated as R[Tr,Tr^2,det] by citation; Ch. V citation correction recorded; Observable ring identified with the pointwise single-copy subring; no Reynolds/Jacobian re-derivation attempted."
      evidence_required: [deliv-single-state-doc, ref-faraut-koranyi]
      automation: hybrid
    - id: test-exact-only-guard
      subject: claim-exact-only-guard
      kind: schema
      procedure: "Run the module's exact-only guard: a self-check scanning code/ring_lemma_verification.py source for forbidden tokens ('from octonion_algebra', 'numpy.linalg.matrix_rank', 'np.linalg.matrix_rank') on the decisive path. Confirm none are present (the one-time oracle cross-check in test-port-oracle is isolated/commented as non-decisive). Confirm the rank-routing convention (sympy.Matrix.rank() over QQ) is documented for downstream phases."
      pass_condition: "Guard finds zero forbidden tokens on the decisive path; rank-routing convention documented; module exits 0 only when ALL_PASS including the guard."
      evidence_required: [deliv-exact-guard, deliv-module]
      automation: automated

  forbidden_proxies:
    - id: fp-float-contamination
      subject: claim-engine-locks
      proxy: "Importing any function from the float64 code/octonion_algebra.py (e.g. 'from octonion_algebra import det_3') onto the decisive path, or using numpy.linalg.matrix_rank / float arithmetic for any lock or rank-bearing computation."
      reason: "octonion_algebra.py is float64 (399 numpy / 0 sympy). Float on the decisive path silently turns every exact lock and every downstream rank verdict (Phases 65-67) into a tolerance artifact. The single sanctioned touch is the isolated one-time det oracle (test-port-oracle), which is NOT decisive."
    - id: fp-rpt-drift
      subject: claim-rpt-frozen
      proxy: "Defining R_pt loosely (e.g. '{Tr X, Tr Y} only', or 'any smooth-function closure') so that c trivially lands in or out of R_pt; redefining 'pointwise'/'reducible' to engineer the membership of c."
      reason: "Pitfall 7 / reward-hacking guard. A loose R_pt makes claim (b) ('c not in R_pt') trivially true or false without content. The frozen six-generator definition must be used identically by Phases 66/67/68."
    - id: fp-convention-trap
      subject: claim-engine-locks
      proxy: "Using the matrix product XY where the Jordan product (1/2)(XY+YX) is meant (off-by-1/2 in c and Tr2); right-associating the cubic cross term 2*Re(x1*(x2*x3)) instead of left 2*Re((x1*x2)*x3); using the Freudenthal/sharp cross X#Y or _polarized_sharp instead of polarize_d."
      reason: "All three are silent (the code runs, the numbers are wrong) and octonions are non-associative so left vs right cross-term genuinely differ. The five locks catch them (Jordan 1/2 via c(X,X)=Tr2; left-assoc + sharp-vs-d via d(X,X,X)=6*det and det(diag)=abc), but only if the engine block is reused verbatim and polarize_d (not the sharp) is chosen."
    - id: fp-clean-algebra-no-eval
      subject: claim-engine-locks
      proxy: "Presenting 'clean-looking' formulas/derivations for the locks without the exact-over-Q assertions actually evaluating to True in-harness; or marking a run as passing when an octonion_algebra import slipped in (float-contaminated even if locks 'pass')."
      reason: "The deliverable of the phase is the locks EVALUATING to exact 0 / exact value over Q in the assert harness, not a narrative that they should. A passing run with a forbidden import is a false pass (Red Flag in RESEARCH.md)."
    - id: fp-overreach-independence
      subject: claim-rpt-frozen
      proxy: "Asserting or proving 'c not in R_pt' (c-independence) in this phase, or building the f_4 action / orbit dimension / Jacobian rank / Hilbert-Molien completeness / Peirce-adapted basis."
      reason: "Scope discipline. c-independence is Phase 66; f_4 + orbit dimension is Phase 65; Hilbert/Molien is Phase 68; the Peirce-adapted basis is a deliberate Phase 65 decision. Phase 64 STATES claim (b) as text but does not prove it, and must not pull downstream work forward."

  uncertainty_markers:
    weakest_anchors:
      - "Exact Faraut-Koranyi theorem number IV.2.5 (paywalled; MEDIUM confidence on the precise number, HIGH on the chapter range II-IV and the Ch. V -> II-IV correction). Mitigation: cite the chapter range + Springer as the robust anchor; record IV.2.5 as the best-known number."
      - "Assumption that the engine's inlined T3 (lines 567-580) is EXACTLY the standalone octonion_algebra.py:2152 det_3 (both read left-assoc, same sign pattern, but verified by inspection only). Mitigation: the one-time float-det oracle cross-check (test-port-oracle) doubles as port-correctness insurance."
    disconfirming_observations:
      - "Any convention lock failing over Q -- especially polarize_d(X,X,X) != 6*det_3(X). Per Backtracking: STOP and reconcile against the engine header (wrong det left-association, wrong polarize_d sign pattern, or a Jordan-1/2 slip); treat as a PORT ERROR, not a new result. Never tune a lock to pass."
      - "The ported det_3 disagreeing with the v15.0 warm-engine benchmark (det_3(diag)=abc, det_3(I)=1, det_3 of a single diagonal idempotent = 0, octonion non-associativity (e1*e2)*e3 != e1*(e2*e3)). Treat as a port error; diff against embedding_under_E_verification.py line by line."
      - "A run reporting ALL_PASS while the exact-only guard finds (or fails to scan for) a 'from octonion_algebra import' or 'numpy.linalg.matrix_rank' token -- the result is float-contaminated even if the locks 'passed'."
---

<objective>
Lay the FROZEN exact-SymPy algebraic foundation for the (RING) milestone (v16.0). Port the verified exact-SymPy h_3(O) octonion engine into a new self-contained module `code/ring_lemma_verification.py`, lift/add the four missing standalone invariant functions (Tr, Tr2, det_3, polarize_d) plus the coupling c, build the 54-symbol pair coordinatization (x0..x26, y0..y26) and the seven base invariants, FREEZE the pointwise subring R_pt, verify the five convention locks EXACTLY over Q, install the exact-only guard, and confirm (by citation) the single-state "Observable" ring R[h_3(O)]^{F_4} = R[Tr, Tr^2, det].

Purpose: Every downstream (RING) phase (65 f_4/orbit-dimension GATE, 66 c-independence, 67 Sym^2 branching, 68 Hilbert/Molien, 69 closure) reuses these objects and conventions VERBATIM. A convention error here silently corrupts all of Phases 65-69; exact-over-Q is non-negotiable because rank (the downstream decisive quantity) is float-fragile. This is BASE-01.

Output: `code/ring_lemma_verification.py` -- an assert-based PASS/FAIL harness (exits nonzero on any lock failure) carrying: the exact octonion+matmul+jordan block (provenance header + ASSERT_CONVENTION line), standalone Tr/Tr2/det_3/polarize_d/c, the 54-symbol layout + seven invariants, the frozen R_pt text, the single-state-ring citation block (with the Ch. V correction), and the exact-only source guard.

NON-NEGOTIABLE convention (carry verbatim): exact over Q, ranks via sympy.Matrix.rank(), NEVER float64 on any decisive path. Jordan = (1/2)(XY+YX); det_3 cross term LEFT-assoc 2*Re((x1*x2)*x3); polarize_d (NOT the sharp); d(X,X,X)=6*det_3(X). The Jordan 1/2 and the left-association are the two most error-prone choices -- both are already fixed in the warm engine and MUST be copied, not re-derived.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/REQUIREMENTS.md
@.gpd/phases/64-setup-conventions-and-exact-engine/64-RESEARCH.md
@code/embedding_under_E_verification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the module + copy the exact octonion/matmul/jordan block (provenance header, ASSERT_CONVENTION, assert harness)</name>
  <files>code/ring_lemma_verification.py</files>
  <action>
    Create `code/ring_lemma_verification.py` as a self-contained exact-SymPy verification module for the (RING) milestone.

    1. Module docstring header (provenance): state this is VALD-64-01, the (RING) milestone foundation; record that the exact octonion block is "Ported verbatim from code/embedding_under_E_verification.py §1-2 (the VERIFIED exact-SymPy engine)". Note that copy (not import) is the deliberate choice (self-contained decisive module, matching the slice_clause_iii precedent), even though embedding_under_E_verification.py IS import-safe (its main() is guarded by `if __name__ == "__main__"`).
    2. Add the `# ASSERT_CONVENTION:` header line recording: `jordan=(1/2)(AB+BA); fano e1e2=e4; det3 left-assoc Re((x1x2)x3); det3_normalization d(X,X,X)=6*det_3; coupling c=Tr(X o Y); arithmetic=exact-SymPy-over-Q; ranks=sympy.Matrix.rank(); NEVER float64 on decisive path`. Also record `27 = 1 (trivial/Tr) (+) 26 (trace-free irreducible)` in the header for downstream (c).
    3. Copy VERBATIM (do not re-derive) from embedding_under_E_verification.py the exact octonion + 3x3 octonion matrix block: `FANO_TRIPLES`, `_MUL_TABLE`, all `oct_*` ops (`oct_zero, oct, oct_real, oct_add, oct_sub, oct_neg, oct_scal, oct_mul, oct_conj, oct_is_zero, oct_equal`), `_oct_normsq`, `h3o_from_coords`, `h3o_identity`, `octmat_zero, octmat_add, octmat_sub, octmat_scal, octmat_equal, octmat_dagger`, `h3o_matmul`, `jordan`, and `_coord_from_octmat`. Import only `sympy` (Rational, symbols, simplify, Poly, etc.). Do NOT import anything from octonion_algebra.
    4. Reuse the assert-harness skeleton verbatim: a global `ALL_PASS = True`, `def _report(label, ok)` (prints `[PASS]/[FAIL]`, folds into ALL_PASS), and at the bottom `def main()` + `if __name__ == "__main__": sys.exit(main())` returning `0 if ALL_PASS else 1`. Leave the body of main() to be filled by later tasks (stub the per-lock _report calls as TODO sections in task order).

    Do NOT re-derive the octonion multiplication table from scratch (Fano-sign / Jordan-1/2 / det-left-association drift risk). Do NOT `import` the engine block at runtime -- copy it (self-contained decisive artifact). Do NOT add numpy on the decisive path.
  </action>
  <verify>
    1. `python3 code/ring_lemma_verification.py` runs without import error and exits 0 (empty harness all-pass).
    2. The copied block is byte-faithful in the load-bearing pieces: `jordan` has the `Rational(1,2)` factor; `h3o_matmul` computes entries as sums of single `oct_mul` (non-associative, no triple-product assumption); `_MUL_TABLE` is built from `FANO_TRIPLES` with `e_i*e_i=(-1,0)`.
    3. `grep -E "from octonion_algebra|import numpy|np\\.linalg|numpy\\.linalg" code/ring_lemma_verification.py` returns NOTHING.
    4. The ASSERT_CONVENTION line is present and records Jordan 1/2, Fano e1e2=e4, det3 left-assoc, d=6*det_3, exact-over-Q, ranks via sympy.
  </verify>
  <done>Self-contained module scaffolded with the verbatim exact octonion+matmul+jordan block, provenance header, ASSERT_CONVENTION line (incl. 27=1+26), and the _report/ALL_PASS/sys.exit harness; runs clean; no octonion_algebra/numpy on the decisive path.</done>
</task>

<task type="auto">
  <name>Task 2: Lift Tr and det_3 standalone, add Tr2 and c, re-port polarize_d onto the exact det_3</name>
  <files>code/ring_lemma_verification.py</files>
  <action>
    Add the five standalone invariant functions, using the exact engine block from Task 1. Source formulas are on disk -- LIFT/re-port them, do not invent.

    1. `det_3(X)`: lift the T3 term out of `reduced_charpoly_roots` (embedding_under_E_verification.py:573-576). Exactly:
       ```python
       def det_3(X):
           a, b, g, x1, x2, x3 = _coord_from_octmat(X)
           n1, n2, n3 = _oct_normsq(x1), _oct_normsq(x2), _oct_normsq(x3)
           cross = oct_mul(oct_mul(x1, x2), x3)   # LEFT-assoc: (x1 x2) x3
           return a*b*g - a*n1 - b*n2 - g*n3 + 2*cross[0]
       ```
       The left-association `oct_mul(oct_mul(x1,x2),x3)` is load-bearing (octonions non-associative). Cross-check the formula against octonion_algebra.py:2152 (same sign pattern, same left-assoc) by reading only -- do not import.
    2. `Tr(X)`: the T1 term. `Tr(X) = X[0][0][0] + X[1][1][0] + X[2][2][0]` (= alpha+beta+gamma; component [0] is the real part of each diagonal octonion).
    3. `Tr2(X)`: `Tr2(X) = Tr(jordan(X, X))` (the quadratic trace Tr(X o X)). MUST use `jordan` (the 1/2 product), NOT `h3o_matmul`, and MUST NOT be `Tr(X)**2` -- Tr(X^2)=Tr(X o X) is a genuinely distinct degree-2 invariant from (Tr X)^2.
    4. `c(X, Y)`: `c(X, Y) = Tr(jordan(X, Y))` (the coupling, bidegree (1,1)). c(X,X) must equal Tr2(X) (a convention lock in Task 3).
    5. `polarize_d(X, Y, Z)`: re-port the octonion_algebra.py:2184 body VERBATIM onto the exact det_3 and exact octmat add:
       ```python
       def polarize_d(X, Y, Z):
           XpY, XpZ, YpZ = octmat_add(X, Y), octmat_add(X, Z), octmat_add(Y, Z)
           XpYpZ = octmat_add(XpY, Z)
           return (det_3(XpYpZ) - det_3(XpY) - det_3(XpZ) - det_3(YpZ)
                   + det_3(X) + det_3(Y) + det_3(Z))
       ```
       Use `polarize_d` (the full polarization locked to 6*det), NOT `_polarized_sharp` / Freudenthal cross X#Y (sharp-vs-d trap).

    Do NOT eagerly `simplify()`/`expand()` det_3 of a fully symbolic 54-variable matrix here (SymPy swell) -- these functions take matrix arguments and are exercised on SMALL explicit elements in Task 3 and stored as expression objects in Task 4. Do NOT define Tr2 as Tr(X)**2.
  </action>
  <verify>
    1. All five functions exist with the exact bodies above; det_3 cross term is `oct_mul(oct_mul(x1,x2),x3)` (left-assoc); Tr2 and c are built from `jordan` (the 1/2 product); polarize_d uses `octmat_add` and the exact `det_3`.
    2. Spot sanity (cheap, exact): `det_3(h3o_identity()) == 1`; `Tr(h3o_identity()) == 3`; `jordan(X,X) == h3o_matmul(X,X)` for a Hermitian X (so Tr2 via jordan agrees with the matrix square).
    3. No use of `_polarized_sharp` or any sharp/Freudenthal cross; no `octonion_algebra` import.
  </verify>
  <done>Standalone Tr, Tr2=Tr(jordan(X,X)), det_3 (left-assoc, lifted), c=Tr(jordan(X,Y)), and polarize_d (re-ported onto exact det_3) present and exact over Q; det_3(I)=1 and Tr(I)=3 spot-checks pass; no sharp polarization, no float import.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the five convention locks EXACTLY over Q (headline d(X,X,X)=6*det_3 first) + one-time float-det oracle cross-check</name>
  <files>code/ring_lemma_verification.py</files>
  <action>
    In main(), add the five convention locks as `_report(...)` assertions, ALL exact over Q. Run the HEADLINE lock first. Provide a generic rational test element (e.g. a helper `generic_rational_X()` returning `h3o_from_coords` with rational diagonal like Rational(2),Rational(3),Rational(5) and rational octonion off-diagonals with several nonzero components), and symbolic a,b,c for the diagonal lock.

    Locks (in order):
    1. HEADLINE: `_report("LOCK d(X,X,X) == 6*det_3(X) (exact over Q)", simplify(polarize_d(Xr, Xr, Xr) - 6*det_3(Xr)) == 0)` for a generic rational Xr. (This was previously verified only at float tolerance 1.4e-13 in octonion_algebra.py:2299 -- it is established EXACTLY over Q here for the first time.)
    2. `_report("LOCK c(X,X) == Tr(X^2) (exact)", simplify(c(Xr, Xr) - Tr2(Xr)) == 0)`.
    3. `_report("LOCK octonion table e1*e2 == e4 (Fano)", oct_mul(e1, e2) == e4)` where e1=oct with comp1=1, e2=oct with comp2=1, e4=oct with comp4=1. Also assert FANO_TRIPLES equals the documented list (cross-check vs octonion_algebra.py / Paper 7 orientation, by value).
    4. `_report("LOCK det_3(diag(a,b,c)) == a*b*c", simplify(det_3(h3o_from_coords(a,b,c,oct_zero(),oct_zero(),oct_zero())) - a*b*c) == 0)` with symbolic a,b,c.
    5. `_report("LOCK det_3(I) == 1", det_3(h3o_identity()) == 1)`.

    Then add the ONE-TIME, NON-DECISIVE port-correctness oracle (clearly fenced + commented as the ONLY sanctioned touch of the float64 file, isolated from the decisive locks):
    6. Delimit the oracle with EXPLICIT SENTINEL COMMENTS so the Task-5 source-grep guard can allowlist it mechanically (skip exactly this window, forbid the tokens everywhere else):
       ```python
       # ORACLE-FENCE-BEGIN  (the ONLY sanctioned octonion_algebra touch; NOT on the decisive path; allowlisted by the Task-5 guard)
       from octonion_algebra import det_3 as oa_det_3   # ALIASED -- never shadows the exact det_3 in this module
       # ... construct the SAME rational element Xr as a float64 octonion_algebra H3O object (Xr_float) ...
       _report("ORACLE (non-decisive): exact det_3 matches float64 octonion_algebra det_3 on shared element",
               abs(float(det_3(Xr)) - oa_det_3(Xr_float)) < 1e-12)
       # ORACLE-FENCE-END
       ```
       The `from octonion_algebra import` MUST appear only between `# ORACLE-FENCE-BEGIN` and `# ORACLE-FENCE-END`, MUST use the `oa_det_3` alias (so the module's exact `det_3` symbol is never rebound/shadowed), and is NOT reachable from any rank-bearing path. Document that the exact-only guard (Task 5) treats exactly this sentinel window as the single allowed, isolated exception.

    BACKTRACKING (encode as a comment at the headline lock): if any lock is nonzero -- ESPECIALLY d(X,X,X) != 6*det_3(X) -- STOP and reconcile against the engine header. A failed lock is a PORT ERROR (wrong det left-association, wrong polarize_d sign pattern, or a Jordan-1/2 slip), NOT a new result. NEVER tune a lock to pass.

    Do NOT add tolerances to the five decisive locks (they are exact `== 0` / `== <exact value>` over Q). Do NOT let the oracle's float import leak outside its fenced block.
  </action>
  <verify>
    1. Running `python3 code/ring_lemma_verification.py` prints `[PASS]` for all five locks and exits 0; the headline d(X,X,X)=6*det_3 lock prints FIRST.
    2. Each of the five locks is an exact comparison (`simplify(...) == 0` or `== <exact value>`), NO tolerance anywhere on the decisive locks.
    3. The oracle cross-check passes (exact det_3 == float det_3 within 1e-12) AND its `from octonion_algebra import det_3 as oa_det_3` is confined strictly between `# ORACLE-FENCE-BEGIN` and `# ORACLE-FENCE-END` (not module-level, aliased so it never shadows the exact `det_3`, not on any lock/rank path).
    4. The backtracking comment is present at the headline lock.
  </verify>
  <done>All five convention locks PASS exactly over Q (headline d(X,X,X)=6*det_3 first), no tolerances on decisive locks; the one-time float-det oracle confirms port-correctness within 1e-12 and is fenced/commented as non-decisive; backtracking rule recorded.</done>
</task>

<task type="auto">
  <name>Task 4: Build the 54-symbol pair coordinatization + the seven base invariants as SymPy expressions</name>
  <files>code/ring_lemma_verification.py</files>
  <action>
    Add the pair coordinatization and the seven base invariants on the engine-native layout (pin the table verbatim so X and Y use IDENTICAL conventions).

    1. Declare the 54 symbols:
       ```python
       xs = sp.symbols('x0:27', real=True)   # x0..x26 for X
       ys = sp.symbols('y0:27', real=True)   # y0..y26 for Y
       ```
    2. The coordinate -> matrix constructor (engine-native; matches h3o_from_coords / _coord_from_octmat):
       ```python
       def X_from_symbols(s):   # s = xs or ys (27-tuple)
           alpha, beta, gamma = s[0], s[1], s[2]
           x1 = [s[3 + k]  for k in range(8)]   # octonion x1 -> matrix entry [2][1]
           x2 = [s[11 + k] for k in range(8)]   # octonion x2 -> matrix entry [0][2]
           x3 = [s[19 + k] for k in range(8)]   # octonion x3 -> matrix entry [1][0]
           return h3o_from_coords(alpha, beta, gamma, x1, x2, x3)
       ```
       Layout (pin as a comment table): x0,x1,x2 = diagonal alpha,beta,gamma; x3..x10 = octonion x1 (at [2][1], conj at [1][2]); x11..x18 = octonion x2 (at [0][2], conj at [2][0]); x19..x26 = octonion x3 (at [1][0], conj at [0][1]). Same for ys.
    3. Build `Xsym = X_from_symbols(xs)`, `Ysym = X_from_symbols(ys)`.
    4. Construct the seven base invariants as SymPy EXPRESSION OBJECTS (store, do NOT eagerly expand/simplify the degree-3 ones in 54 vars -- swell):
       - `inv_Tr_X = Tr(Xsym)` (= xs[0]+xs[1]+xs[2]), bidegree (1,0)
       - `inv_Tr2_X = Tr2(Xsym)`, bidegree (2,0)
       - `inv_det_X = det_3(Xsym)`, bidegree (3,0)
       - `inv_Tr_Y, inv_Tr2_Y, inv_det_Y` symmetrically, bidegrees (0,1)/(0,2)/(0,3)
       - `inv_c = c(Xsym, Ysym)` (= Tr(jordan(Xsym,Ysym))), bidegree (1,1)
       Collect them in a dict/list with labels and documented bidegrees.
    5. Add a `_report` round-trip check: `_coord_from_octmat(X_from_symbols(xs))` recovers exactly (xs[0],xs[1],xs[2], xs[3:11], xs[11:19], xs[19:27]).
    6. Add a `_report` cheap degree check on a LOW-dimensional slice (e.g. substitute all but the diagonal symbols to 0): confirm inv_Tr_X is degree 1, inv_Tr2_X degree 2, inv_det_X degree 3 in the surviving variables, and inv_Tr2_X is NOT (inv_Tr_X)**2 (evaluate both on a rational point and show they differ).

    Use the ENGINE-NATIVE coordinate basis (NOT peirce_basis_27()). Flag in a comment that the Peirce-adapted basis is a DELIBERATE Phase-65 decision (the invariants here are basis-agnostic functions of the coordinates). Do NOT eagerly expand the 54-variable det_3 as a "sanity check" (swell -- this is a Phase-66+ operation).
  </action>
  <verify>
    1. `xs` and `ys` are 27-symbol tuples (x0..x26, y0..y26); `X_from_symbols` uses the pinned engine-native map; Xsym and Ysym built with the SAME constructor.
    2. The seven invariants exist as stored SymPy expressions with the documented bidegrees; `inv_c` is `Tr(jordan(Xsym,Ysym))` and `inv_Tr2_X` is `Tr2` (not squared trace).
    3. Round-trip `_report` passes: coordinates recovered exactly from `X_from_symbols(xs)`.
    4. Degree `_report` passes: Tr deg 1, Tr2 deg 2, det deg 3 on the diagonal slice; Tr2 != (Tr)^2 on a rational point.
    5. No eager full-54-variable expand/simplify (module still runs in seconds; exits 0).
  </verify>
  <done>54-symbol pair layout (x0..x26, y0..y26) built on the pinned engine-native map (X,Y identical); the seven base invariants stored as SymPy expressions with correct bidegrees ((1,0)/(2,0)/(3,0)/(0,1)/(0,2)/(0,3)/(1,1)), Tr2 distinct from (Tr)^2; round-trip and degree checks pass; no eager swell; Peirce basis flagged as a Phase-65 decision.</done>
</task>

<task type="auto">
  <name>Task 5: Freeze and document R_pt + install the exact-only source guard</name>
  <files>code/ring_lemma_verification.py</files>
  <action>
    FREEZE R_pt as a verbatim, reusable text block, and install the exact-only guard.

    1. Add R_pt as a module-level frozen-text constant + docstring (intended to be CITED VERBATIM by Phases 66/67/68, anti-drift):
       ```
       R_PT_FROZEN_DEFINITION = '''
       R_pt := the R-subalgebra of R[h_3(O) (+) h_3(O)]^{F_4} generated by
               {Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y}
             = R[Tr X, Tr X^2, det X] (x) R[Tr Y, Tr Y^2, det Y].
       Recorded consequences (anti-Pitfall-7):
         - Tr(X)*Tr(Y) IN R_pt  (product of two single-state generators; the reducible (1,1) member).
         - Claim (b) is precisely: c NOT IN R_pt.  [STATED here; PROVEN in Phase 66 -- NOT proven in Phase 64.]
         - "c is new" means new MODULO products + pointwise terms; the genuine-coupling
           quotient at bidegree (1,1) is 1-dimensional (Tr(X)Tr(Y) in R_pt, plus c) -- the
           precise uniqueness statement is a Phase-67 concern.
       '''
       ```
       Provide a forward-looking membership-predicate stub `def is_in_Rpt(p): ...` whose docstring references EXACTLY this six-generator definition (body may raise NotImplementedError("decision procedure is Phase 66") -- the point is the predicate's DEFINITION is frozen here, identical across phases).
    2. Add `_report` checks: (a) `R_PT_FROZEN_DEFINITION` contains all six generator names and the strings "Tr(X)*Tr(Y) IN R_pt" and "c NOT IN R_pt"; (b) the six base invariants from Task 4 are exactly the generators named (consistency between the prose and the constructed invariants).
    3. Install the EXACT-ONLY GUARD as a self-check (Success Criterion 4). Implement BOTH:
       - Import hygiene: assert no module-level `from octonion_algebra import` and no `numpy.linalg`/`np.linalg` on the decisive path.
       - Source grep self-check (use an EXPLICIT SENTINEL WINDOW, not an ad-hoc grep): open `__file__`, read the source line-by-line, and skip ONLY the contiguous lines strictly between `# ORACLE-FENCE-BEGIN` and `# ORACLE-FENCE-END` (the Task-3 oracle). On every line OUTSIDE that window, assert the forbidden tokens (`from octonion_algebra`, `numpy.linalg.matrix_rank`, `np.linalg.matrix_rank`) are absent. Additionally assert the exact `det_3` symbol is never rebound (the only `octonion_algebra` import in the whole file is the aliased `oa_det_3` inside the fence). `_report("exact-only guard: no float/octonion_algebra on decisive path", ok)`. This sentinel-window mechanism is what prevents the guard from BOTH false-failing on the legitimate oracle AND being weakened so broadly (e.g. allowlist-by-filename) that a future real `octonion_algebra` import could slip onto the decisive path.
       - Document the rank-routing convention: a comment/constant stating ALL downstream ranks go through `sympy.Matrix(...).rank()` over QQ; no float matrix is ever passed to a rank-bearing function. (Phase 64 computes no ranks, but the guard's PRESENCE lets Phases 65-67 inherit the float-free guarantee.)

    Do NOT define R_pt as "{Tr X, Tr Y} only" or "any smooth-function closure" (Pitfall 7 drift). Do NOT redefine "pointwise"/"reducible". Do NOT prove c not in R_pt (Phase 66). Do NOT let the guard mistake the fenced one-time oracle (Task 3) for a decisive-path violation -- it must allowlist exactly that single commented block.
  </action>
  <verify>
    1. `R_PT_FROZEN_DEFINITION` present with the six generators, "Tr(X)*Tr(Y) IN R_pt", and "c NOT IN R_pt" (claim (b) stated, not proven); `is_in_Rpt` stub references the six-generator definition.
    2. `_report` consistency check passes: the six named generators match the six base invariants built in Task 4.
    3. The exact-only guard `_report` passes: the line-by-line source scan finds zero forbidden tokens outside the `# ORACLE-FENCE-BEGIN`/`# ORACLE-FENCE-END` window; the only `octonion_algebra` import in the file is the aliased `oa_det_3` inside that window (exact `det_3` never rebound); rank-routing convention documented.
    4. No proof/assertion of c-independence; no f_4/orbit/Jacobian/Hilbert/Peirce work present.
  </verify>
  <done>R_pt FROZEN as a verbatim reusable text block (six generators, Tr(X)Tr(Y) member, claim (b)="c not in R_pt" stated-not-proven) with a frozen is_in_Rpt predicate stub; prose-vs-invariants consistency check passes; exact-only guard (import hygiene + source grep + rank-routing convention) passes with the one-time oracle correctly allowlisted; no downstream work pulled forward.</done>
</task>

<task type="auto">
  <name>Task 6: Confirm and document the single-state ("Observable") ring by citation (with the FK Ch. V correction)</name>
  <files>code/ring_lemma_verification.py</files>
  <action>
    Document the single-state ring confirmation. This is a CITATION, NOT a computation -- do NOT re-derive R[Tr,Tr^2,det] via Reynolds/Jacobian (established 1962-1994 mathematics).

    1. Add a module-level docstring/constant `SINGLE_STATE_RING_NOTE` recording:
       - `R[h_3(O)]^{F_4} = R[Tr, Tr^2, det]`, a free polynomial algebra; transcendence degree 3; generator degrees 1, 2, 3.
       - Citation: Faraut & Koranyi, Analysis on Symmetric Cones (OUP 1994), Ch. II-IV (Thm IV.2.5 region); Springer 1962/1973 (cubic norm uniqueness; F_4=Aut; det normalization).
       - CITATION CORRECTION (record verbatim): "The single-state ring fact lives in Faraut-Koranyi Ch. II-IV (II = Euclidean Jordan algebras, III = Peirce, IV = classification), NOT Ch. V (Ch. V = conical/spherical polynomials). The earlier 'Ch. V' attribution was imprecise; corrected to II-IV."
       - Identification (the milestone framing): "The 'Observable' single-frame ring of the Chalmers gap = EXACTLY this pointwise single-copy subring R[Tr_X, Tr_X^2, det_X] (one copy of R_pt). Degrees 1/2/3 match the single-copy generators built above."
    2. Add a `_report` consistency check tying this to the constructed objects: the single-copy generators {inv_Tr_X, inv_Tr2_X, inv_det_X} have degrees {1,2,3} (already checked in Task 4) and number 3 = trdeg -- confirm the count and the documented degrees are internally consistent with the citation (3 generators, degrees 1/2/3, trdeg 3).
    3. Ensure `main()` ends by printing the overall verdict and `return 0 if ALL_PASS else 1`; confirm the full harness (all tasks' _report lines) runs end to end and exits 0.

    Do NOT attempt a from-scratch Reynolds-operator or Jacobian computation of the single-state ring (Caveat 2 of RESEARCH.md: wastes budget, risks error in something certain). Do NOT cite Ch. V. Keep the uncertainty marker explicit in a comment: the exact theorem number IV.2.5 is MEDIUM-confidence (paywalled); the chapter range II-IV and the correction are HIGH-confidence.
  </action>
  <verify>
    1. `SINGLE_STATE_RING_NOTE` states R[Tr,Tr^2,det] (trdeg 3, degrees 1/2/3), cites Faraut-Koranyi Ch. II-IV + Springer, records the explicit "Ch. V -> Ch. II-IV" correction, and identifies the Observable ring with the pointwise single-copy subring.
    2. No Reynolds/Jacobian re-derivation of the single-state ring anywhere in the module (citation only).
    3. The consistency `_report` passes (3 single-copy generators, degrees 1/2/3, trdeg 3 -- internally consistent with the citation).
    4. `python3 code/ring_lemma_verification.py` runs the COMPLETE harness (all six tasks' checks) and exits 0; the IV.2.5 MEDIUM-confidence caveat is recorded in a comment.
  </verify>
  <done>Single-state ring R[h_3(O)]^{F_4}=R[Tr,Tr^2,det] confirmed by citation (Faraut-Koranyi Ch. II-IV / Springer) with the Ch. V correction recorded and the Observable ring identified with the pointwise single-copy subring; consistency check passes; full module harness runs end-to-end and exits 0; no from-scratch re-derivation; IV.2.5 caveat recorded.</done>
</task>

</tasks>

<verification>
Overall (all exact over Q -- no tolerances on any decisive path; the module is the proof):

- The five convention locks PASS exactly over Q, headline `polarize_d(X,X,X) == 6*det_3(X)` FIRST, then `c(X,X) == Tr(X^2)`, `e1*e2 == e4`, `det_3(diag(a,b,c)) == a*b*c`, `det_3(I) == 1`. (BASE-01 validation profile = Exact symbolic over Q.)
- The exact octonion block is the VERBATIM warm engine (Jordan 1/2, left-assoc det_3 cross term, Fano e1e2=e4) -- not re-derived; the one-time float-det oracle confirms the lifted det_3 matches the float64 reference within 1e-12 (port-correctness; non-decisive, fenced).
- The 54-symbol pair layout (x0..x26, y0..y26) uses the pinned engine-native map identically for X and Y; the seven invariants are stored SymPy expressions with the correct bidegrees; Tr2 = Tr(X o X) is distinct from (Tr X)^2; round-trip + degree checks pass.
- R_pt is FROZEN as the six-generator R-subalgebra (= R[..X] (x) R[..Y]); Tr(X)Tr(Y) recorded IN R_pt; claim (b) = "c not in R_pt" STATED (not proven); frozen text reusable verbatim by Phases 66/67/68 (anti-Pitfall-7).
- The exact-only guard (import hygiene + source grep + rank-routing convention) passes with zero forbidden tokens on the decisive path; the single fenced oracle is correctly allowlisted; Phases 65-67 inherit a float-free rank guarantee.
- The single-state ring R[Tr,Tr^2,det] is confirmed BY CITATION (Faraut-Koranyi Ch. II-IV, Thm IV.2.5; Springer), the Ch. V -> II-IV correction is recorded, and the Observable ring is identified with the pointwise single-copy subring.
- Scope discipline held: NO c-independence proof (Phase 66), NO f_4/orbit dimension (Phase 65), NO Hilbert/Molien (Phase 68), NO Peirce-adapted basis (flagged as a Phase-65 decision); v16.0 NOT entangled with v15.0.

Backtracking (encoded in the module): any convention lock failing -- especially d(X,X,X) != 6*det_3(X) -- is a PORT ERROR (wrong det left-association / wrong polarize_d sign pattern / Jordan-1/2 slip), NOT a new result. STOP and reconcile against the engine header; diff against embedding_under_E_verification.py line by line; never tune a lock to pass.
</verification>

<success_criteria>
`code/ring_lemma_verification.py` exists as a self-contained exact-SymPy assert-harness that exits 0, with:
1. All five convention locks passing EXACTLY over Q (headline d(X,X,X)=6*det_3 first); no tolerances on the decisive locks.
2. R_pt FROZEN and documented as the six-generator R-subalgebra (= R[..X] (x) R[..Y]); Tr(X)Tr(Y) recorded IN R_pt; claim (b)="c not in R_pt" STATED, not proven.
3. The single-state ring R[h_3(O)]^{F_4}=R[Tr,Tr^2,det] (trdeg 3, degrees 1/2/3) confirmed BY CITATION (Faraut-Koranyi Ch. II-IV with the Ch. V correction recorded; Springer); Observable ring identified with the pointwise single-copy subring.
4. The engine exact-only on the decisive path: module embeds the SymPy octonion block; the guard confirms no `numpy.linalg.matrix_rank`/float arithmetic and no decisive-path `octonion_algebra` import is reachable from rank computations.

Plus: the 54-symbol pair layout + seven base invariants built (correct bidegrees, Tr2 != (Tr)^2, engine-native basis, no eager swell); the verbatim warm-engine octonion block (Jordan 1/2, left-assoc det_3, Fano e1e2=e4); the one-time float-det oracle confirming port-correctness (non-decisive, fenced). NO forbidden proxy realized: no float64 octonion_algebra on the decisive path, no R_pt definitional drift, no convention trap (Jordan 1/2 / det left-assoc / sharp-vs-d), no clean-algebra-without-exact-eval, no downstream overreach.
</success_criteria>

<output>
After completion, create `.gpd/phases/64-setup-conventions-and-exact-engine/64-01-SUMMARY.md`.

The SUMMARY must record: the port route taken (copy vs import) and provenance; the exact values/PASS of all five convention locks (esp. the headline d(X,X,X)=6*det_3 now established exactly over Q for the first time); the result of the one-time float-det oracle; the frozen R_pt text verbatim (for Phases 66/67/68 to cite); the single-state-ring citation with the Ch. V correction; confirmation the exact-only guard is in place; and the explicit note that c-independence (Phase 66), f_4/orbit dimension (Phase 65), Hilbert/Molien (Phase 68), and the Peirce-adapted basis (Phase 65 decision) were DEFERRED, not done.
</output>
