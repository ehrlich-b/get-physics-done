---
phase: 62-coherent-embedding-under-e-the-hard-part
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - derivations/p5-basin-restriction/embedding-under-E.md
interactive: false

conventions:
  units: "natural (hbar=1, k_B=1); pure algebra — no physical dimensions"
  metric: "riemannian_fisher (N/A this plan); type/category + Peirce-grade consistency is the dimensional-analysis analog"
  algebra: "Jordan product a o b = (1/2)(ab+ba); sequential product a&b = sqrt(a) b sqrt(a) (Luders, temporally asymmetric; principal sqrt via CFC)"
  octonion: "Fano e_1 e_2 = e_4 (matches code/octonion_algebra.py infra)"
  complex_structure: "u = e_7 by default (any u in S^6 equivalent under G_2)"
  slice: "A = h_3(C_u) ~ M_3(C)^sa (maximal C*-target inside h_3(O); single F_4-orbit); C_u = R + R*u"
  peirce_eigenvalues: "{0, 1/2, 1}"
  conditional_expectation: "E: h_3(O) -> A, positive unital idempotent (Effros-Stormer); E|_A = id; embedding A -> h_3(O) Jordan-product-preserving"

dimensional_check:
  # Real vector-space / Peirce-grade bookkeeping (the pure-algebra analog of dimensional analysis).
  dim_h3O: "[real dim] = 27 (exceptional Albert algebra; 3 real diagonal + 3 octonionic off-diagonal = 3 + 24)"
  dim_A: "[real dim] = 9 (slice A = h_3(C_u) ~ M_3(C)^sa; 3 real diagonal + 3 C_u off-diagonal = 3 + 6)"
  dim_E_range: "[real dim] = 9 (E is idempotent onto A; rank E = dim A = 9; ker E = 18-dim complement)"
  peirce_at_E11: "h_3(C_u) = V_1(E_11) (+) V_{1/2}(E_11) (+) V_0(E_11) with V_1 ~ R (dim 1), V_{1/2} ~ C_u^2 (dim 4), V_0 ~ h_2(C_u) ~ M_2(C)^sa (dim 4); 1+4+4 = 9 (lem:bottleneck (iii))"

approximations:
  - name: "none (exact algebraic setup; no truncation, no perturbative expansion)"
    parameter: "N/A — pure algebra; this plan sets up E and FRAMES the sequential-product-under-E question analytically"
    validity: "always exact; the decisive computation (does E commute with &) is deferred to 62-02 (exact SymPy)"
    breaks_when: "N/A"
    check: "Every structural claim about E (positive, unital, idempotent, range = JB-subalgebra) is grounded in Effros-Stormer (lem:bottleneck proof, complexification.tex:437) or directly verifiable entrywise; the Jordan-vs-sequential-product distinction is stated as the OPEN crux, NOT asserted resolved"

estimated_execution:
  total_minutes: 55
  breakdown:
    - task: 1
      minutes: 35
      note: "Set up E: h_3(O) -> h_3(C_u) explicitly (entrywise C_u-projection; positive, unital, idempotent; Effros-Stormer range = JB-subalgebra); state precisely what E preserves (Jordan product YES via lem:bottleneck) (unfamiliar-formalism 1.5x + large-output multipliers applied)"
    - task: 2
      minutes: 20
      note: "FRAME the decisive crux as AMBIENT E-TRANSPORT: E(sqrt(X) Y sqrt(X)) vs sqrt(EX)(EY)sqrt(EX) for GENERIC ambient X,Y (non-associativity load-bearing; E not a Jordan morphism on ambient elements); record the slice-internal case as a TRIVIAL control (closed associative subalgebra, leakage 0); state the CORRECTED RESTRICTION (coexistence-as-island) + weakened embedding clause; set up 62-02 + the obstruction-or-preservation fork (obstruction EXPECTED, refines RESTRICTION)"

contract:
  schema_version: 1
  scope:
    question: "Is the Peirce/bottleneck conditional expectation E: h_3(O) -> h_3(C_u) (Effros-Stormer: positive, unital, idempotent, range = JB-subalgebra) set up explicitly, and is the decisive crux framed correctly as AMBIENT E-TRANSPORT coherence — whether E(sqrt(X) Y sqrt(X)) = sqrt(EX)(EY)sqrt(EX) for GENERIC ambient X,Y in h_3(O) (non-associativity load-bearing; E is NOT a Jordan morphism on ambient elements) — WITH the slice-internal case recorded plainly as a trivial control (closed associative subalgebra, leakage 0), and the CORRECTED RESTRICTION (coexistence-as-island) stated — WITHOUT asserting the transport answer (which 62-02 computes on the actual non-associative h_3(O))?"
  claims:
    - id: claim-restriction
      statement: "(Advanced, NOT closed by this plan. CORRECTED FRAMING — coexistence-as-island, Bryan 2026-05-24.) RESTRICTION needs only that the observer is a self-certified M_3(C)^sa system on the slice A = h_3(C_u) (Phase 61: all four Def 1 clauses verbatim, intrinsically) and the slice sits inside h_3(O) as the range of the projection E. It does NOT require the observer's structure to be transported/derived from the ambient via E (the prompt's over-ask). This plan establishes the SETUP half: E: h_3(O) -> A is constructed explicitly as a positive unital idempotent (Effros-Stormer; Jordan-product-preserving embedding, so E IS coherent with the Jordan product a o b ON THE SLICE). The decisive OPEN crux is whether E TRANSPORTS the sequential product coherently from the ambient — E(sqrt(X) Y sqrt(X)) = sqrt(EX)(EY)sqrt(EX) for GENERIC ambient X,Y — where non-associativity is genuinely engaged and E is NOT a Jordan morphism on ambient elements. The slice-internal sqrt(a) b sqrt(a) (a,b in A) is TRIVIAL (closed associative subalgebra, leakage 0, associator 0): the documented control, NOT the decisive test. The ambient-transport verdict is computed in 62-02 and read off in 62-03; an ambient-transport obstruction is EXPECTED and REFINES RESTRICTION (self-contained C* island), it does NOT refute it or establish independent posits. This plan does NOT pre-commit to either branch."
      deliverables: [deliv-embedding]
      acceptance_tests: [test-E-explicit, test-E-preserves-stated, test-crux-framed-ambient, test-slice-triviality, test-corrected-restriction, test-fork-open]
      references: [ref-effros-stormer, ref-lem-bottleneck, ref-hanche-olsen, ref-paper5-def1]
  deliverables:
    - id: deliv-embedding
      kind: derivation
      path: derivations/p5-basin-restriction/
      description: "derivations/p5-basin-restriction/embedding-under-E.md (this plan creates §1-§3 of it; 62-02/62-03 extend it) — explicit setup of the Peirce/bottleneck conditional expectation E: h_3(O) -> h_3(C_u) (entrywise C_u = span{1,e_7} projection; positive, unital, idempotent; range = JB-subalgebra by Effros-Stormer); precise statement of what E preserves (Jordan product a o b ON THE SLICE YES, via lem:bottleneck Jordan-product-preserving embedding; but E is NOT a Jordan morphism on ambient elements); explicit framing of the decisive crux as AMBIENT E-TRANSPORT — whether E(sqrt(X) Y sqrt(X)) = sqrt(EX)(EY)sqrt(EX) for GENERIC ambient X,Y in h_3(O) (non-associativity load-bearing); the slice-internal case recorded as a TRIVIAL control (closed associative subalgebra, leakage 0); the CORRECTED RESTRICTION (coexistence-as-island) and weakened embedding clause; the obstruction-or-preservation fork stated and kept OPEN (obstruction EXPECTED, refines RESTRICTION; no pre-commitment)."
      must_contain:
        - "E: h_3(O) -> h_3(C_u) constructed explicitly as the entrywise C_u-projection (proj_u applied to every entry of the 3x3 octonionic Hermitian matrix); positive, unital (E(I_3)=I_3), idempotent (E o E = E), E|_A = id"
        - "Effros-Stormer: range of a positive unital idempotent on a JB-algebra is a JB-subalgebra; quoted from lem:bottleneck proof (complexification.tex:437) — E's range A ~ M_3(C)^sa ~ h_3(C_u) is a Jordan subalgebra"
        - "what E preserves stated precisely: E IS coherent with the JORDAN product a o b ON THE SLICE (lem:bottleneck: the embedding A -> h_3(O) is Jordan-product-preserving; E|_A = id) — but E is NOT a Jordan morphism on AMBIENT elements (E(X o X) vs (EX) o (EX) differ), so the ambient transport question is genuinely non-trivial"
        - "SLICE-INTERNAL TRIVIALITY recorded plainly: the slice A = h_3(C_u) is a CLOSED, ASSOCIATIVE Jordan subalgebra (= range E); for a,b in A, sqrt(a) b sqrt(a) stays in A with leakage EXACTLY 0 and triple associator EXACTLY 0 — the SP datum is induced TRIVIALLY on the slice, non-associativity is NOT engaged there; this is the documented CONTROL, not the decisive test"
        - "THE DECISIVE CRUX = AMBIENT E-TRANSPORT: does E(sqrt(X) Y sqrt(X)) = sqrt(EX)(EY)sqrt(EX) for GENERIC ambient X (PSD), Y in h_3(O), with sqrt(X) computed in the ambient and non-associativity genuinely engaged ((xy)z != x(yz))? — to be COMPUTED in 62-02, NOT asserted here. The sequential product a&b = sqrt(a) b sqrt(a) is a CFC, non-Jordan triple product; non-associativity in the ambient is the channel"
        - "CORRECTED RESTRICTION (coexistence-as-island, Bryan 2026-05-24): RESTRICTION needs only that the observer self-models on the slice (Phase 61, all four clauses verbatim) and the slice sits inside h_3(O) as the range of E; the embedding clause is WEAKENED from 'coherently induced from the ambient via E' to 'the observer self-models on the slice, which sits inside h_3(O) as the range of the projection E'. E = access/projection map, NOT required to be a Jordan morphism on the ambient. Clause (iii) ITSELF is unchanged (fp-redefine-iii) — only RESTRICTION's embedding clause is weakened"
        - "obstruction-or-preservation fork stated, kept OPEN, with CORRECTED verdict semantics: (P) E transports coherently (R=0) -> RESTRICTION embedding lemma (62-03); (O) E does not (R!=0) -> ambient-transport obstruction (62-03), the EXPECTED/acceptable outcome that REFINES RESTRICTION to coexistence-as-island (self-contained C* island; through-line survives; NOT independent posits). No pre-commitment to either branch"
        - "Peirce decomposition of A = h_3(C_u) at rank-1 idempotent E_11: V_1 ~ R, V_{1/2} ~ C_u^2, V_0 ~ h_2(C_u) ~ M_2(C)^sa (lem:bottleneck (iii), complexification.tex:481-486) — the Peirce structure E restricts through"
        - "consistency with the Phase 60 distinction (fp-conflate-composites): V_BM remains the observer's OWN internal composite A (x) A, NEVER identified with / transported from the BGW universe-tensoring of h_3(O); coexistence-as-island is fully consistent with two-composites"
  references:
    - id: ref-effros-stormer
      kind: paper
      locator: "Effros-Stormer 1979 (positive projections onto JB-subalgebras). Content quoted in the LIVE lem:bottleneck proof: ~/repos/blog/landing/papers/sm-from-self-modeling/sections/complexification.tex line 437: 'Effros-Stormer: the range of a positive unital idempotent on a JB-algebra is a JB-subalgebra. In finite dimensions over R this is just a Jordan subalgebra; by the hypothesis on E the embedding is moreover Jordan-product-preserving.' EXECUTOR HAS NO WEB — use this exact quoted content; do NOT attempt to fetch the paper."
      aliases: ["Effros-Stormer", "Effros-Stormer 1979", "EffrosStormer1979"]
      role: method
      why_it_matters: "Underwrites E: h_3(O) -> A as a positive unital idempotent whose range is a JB-subalgebra (= Jordan subalgebra in fin-dim). This is what makes E a legitimate Jordan conditional expectation. CRUCIALLY: it controls the JORDAN product, NOT the sequential product — the coherent-embedding step turns precisely on whether E ALSO preserves a&b = sqrt(a) b sqrt(a). Must surface where the plan asserts E's range/positivity/idempotency."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-lem-bottleneck
      kind: prior_artifact
      locator: "~/repos/blog/landing/papers/sm-from-self-modeling/sections/complexification.tex lem:bottleneck (line 409): E(h_3(O)) family = positive unital idempotents onto Jordan subalgebras ~ s.a. part of fin-dim complex C*-algebra; (i) range A ~ +_k M_{n_k}(C)^sa, sum n_k <= 3 [Effros-Stormer, line 437]; (ii) maximal range A ~ M_3(C)^sa ~ h_3(C_u), u in S^6, single F_4-orbit; (iii) for rank-1 idempotent E_11 in h_3(C_u): V_1 ~ R, V_{1/2} ~ C_u^2, V_0 ~ h_2(C_u) ~ M_2(C)^sa (lines 481-486). EMBEDDING Jordan-product-preserving (lines 412-413, 440-441). rem:converse is prompt-inline, NOT labeled here (FUTR-01)."
      aliases: ["lem:bottleneck", "Paper 7 bottleneck lemma", "C*-bottleneck universality"]
      role: method
      why_it_matters: "Supplies E (the bottleneck conditional expectation), the slice A = h_3(C_u), and the Peirce decomposition E restricts through. lem:bottleneck builds E for the JORDAN product (positive, unital, idempotent; Jordan-product-preserving embedding) — it says NOTHING about the sequential product. This GAP is the entire Phase 62 crux. Quote the LIVE text directly (executor has Read; live .tex is readable)."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-hanche-olsen
      kind: paper
      locator: "Hanche-Olsen, universal tensor product of Jordan algebras (special vs exceptional). Content as used in Phase 60 (derivations/p5-basin-restriction/two-composites.md, rem-converse-bgw.md): a composite is INDUCED (canonically built from the factors) vs IMPORTED (extra external structure). BGW Prop 3.10 = Hanche-Olsen Thm 5.5 (only complex systems have locally tomographic composites with a qubit). EXECUTOR HAS NO WEB — use this stated content + the Phase 60 derivation files (readable)."
      aliases: ["Hanche-Olsen", "Hanche-Olsen Thm 5.5"]
      role: benchmark
      why_it_matters: "Frames the induced-vs-imported distinction that the obstruction-or-preservation fork turns on: if clause (iii)'s sequential-product datum on A can be INDUCED by E from h_3(O) -> (P); if it requires IMPORTING structure not in the ambient -> (O) obstruction. Surfaces where the plan states the fork and the induced-vs-imported framing."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-paper5-def1
      kind: spec
      locator: "~/repos/blog/landing/papers/qm-from-self-modeling/main.tex def:self-modeling-system (line 342); clause (iii) sms:minimal (lines 351-353, verbatim): 'V_BM is the minimal composite OUS carrying product states, product effects, non-signaling constraints, and product-form sequential product.' The 'product-form sequential product' is the datum whose coherence under E is at stake. LIVE paper only."
      aliases: ["Paper 5 Def 1", "def:self-modeling-system", "clause (iii)", "sms:minimal"]
      role: definition
      why_it_matters: "Clause (iii)'s fourth datum is the PRODUCT-FORM SEQUENTIAL PRODUCT. Phase 61 verified it on the ASSOCIATIVE composite (intrinsically). Phase 62 asks whether E induces it from the NON-ASSOCIATIVE ambient. Clause (iii) must stay verbatim (fp-redefine-iii guarded at source in claim.md)."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
  acceptance_tests:
    - id: test-E-explicit
      subject: claim-restriction
      kind: existence
      procedure: "Confirm embedding-under-E.md constructs E: h_3(O) -> h_3(C_u) EXPLICITLY: (a) the formula — entrywise application of proj_u (octonion projection onto C_u = span{1, u=e_7}) to all 9 entries of the 3x3 octonionic Hermitian matrix, equivalently E acts as identity on the (alpha,beta,gamma) real diagonal and projects each off-diagonal octonion x1,x2,x3 onto C_u; (b) the four conditional-expectation properties named with their justification: positive (entrywise C_u-projection is positive), unital (E(I_3)=I_3 since I_3 has C_u entries), idempotent (proj_u o proj_u = proj_u entrywise => E o E = E), E|_A = id (slice entries already in C_u); (c) range = JB-subalgebra via Effros-Stormer (quoted, complexification.tex:437)."
      pass_condition: "E constructed with explicit entrywise-proj_u formula; positive + unital + idempotent + E|_A=id each stated with justification; Effros-Stormer range=JB-subalgebra quoted from the LIVE lem:bottleneck proof; dim E-range = 9 = dim A confirmed."
      evidence_required: [deliv-embedding, ref-effros-stormer, ref-lem-bottleneck]
      automation: human
    - id: test-E-preserves-stated
      subject: claim-restriction
      kind: consistency
      procedure: "Confirm the deliverable states PRECISELY what E preserves: E IS coherent with the JORDAN product a o b = (1/2)(ab+ba) ON THE SLICE — because lem:bottleneck makes the embedding A -> h_3(O) Jordan-product-preserving and E|_A = id (E recovers lem:bottleneck on the slice). Confirm the deliverable ALSO states that E is NOT a Jordan morphism on AMBIENT elements (E(X o X) vs (EX) o (EX) differ — the ambient transport question is genuinely non-trivial), and that the sequential-product-TRANSPORT question is explicitly a SEPARATE, unresolved question (not folded into 'E preserves everything')."
      pass_condition: "E-preserves-Jordan-product-on-the-slice stated with lem:bottleneck justification (E|_A = id); E-is-NOT-a-Jordan-morphism-on-ambient-elements stated; the sequential-product transport is NOT claimed preserved here (the 'E preserves everything' hand-wave explicitly rejected — fp-assert-preservation)."
      evidence_required: [deliv-embedding, ref-lem-bottleneck, ref-effros-stormer]
      automation: human
    - id: test-crux-framed-ambient
      subject: claim-restriction
      kind: consistency
      procedure: "Confirm THE CRUX is framed explicitly and correctly as AMBIENT E-TRANSPORT: (a) the decisive computable question is E(sqrt(X) Y sqrt(X)) = sqrt(EX)(EY)sqrt(EX) for GENERIC ambient X (PSD),Y in h_3(O), NOT the slice-internal sqrt(a) b sqrt(a); (b) sqrt(X) is computed in the AMBIENT (Jordan/CFC spectral calculus), and non-associativity ((xy)z != x(yz) in h_3(O)) is genuinely engaged — the relevant associator is nonzero (load-bearing); (c) the sequential product a&b = sqrt(a) b sqrt(a) is a CFC-based, non-Jordan triple product; (d) E is NOT a Jordan morphism on ambient elements; (e) this is explicitly deferred to 62-02 for exact computation, NOT asserted here."
      pass_condition: "Crux stated as the AMBIENT transport residual (generic X,Y), non-associativity genuinely engaged (associator-nonzero, load-bearing), NOT the slice-internal trivial case; explicitly marked as COMPUTED IN 62-02 (not resolved in this plan)."
      evidence_required: [deliv-embedding, ref-paper5-def1, ref-lem-bottleneck]
      automation: human
    - id: test-slice-triviality
      subject: claim-restriction
      kind: consistency
      procedure: "Confirm the deliverable RECORDS PLAINLY the slice-internal triviality: A = h_3(C_u) is a CLOSED, ASSOCIATIVE Jordan subalgebra (= range E); for a,b in A, sqrt(a) b sqrt(a) stays in A with leakage EXACTLY 0 and triple associator EXACTLY 0; the SP datum is induced TRIVIALLY on the slice; non-associativity is NOT engaged there. Confirm this is presented as the documented CONTROL, clearly distinguished from the decisive ambient-transport test (so the slice-internal case is NOT mistaken for the decisive result, and the line-loophole of running a non-associativity exerciser on UNRELATED matrices while the decisive test is the trivial slice one is foreclosed)."
      pass_condition: "Slice-internal triviality stated (closed associative subalgebra; leakage 0; associator 0; SP induced trivially; non-associativity not engaged); clearly separated as the CONTROL; the decisive test is the ambient transport residual, not the slice-internal one."
      evidence_required: [deliv-embedding, ref-lem-bottleneck]
      automation: human
    - id: test-corrected-restriction
      subject: claim-restriction
      kind: consistency
      procedure: "Confirm the deliverable states the CORRECTED RESTRICTION (coexistence-as-island): RESTRICTION needs only that the observer self-models on the slice (Phase 61, all four clauses verbatim) and the slice sits inside h_3(O) as the range of E; the embedding clause is WEAKENED to 'the observer self-models on the slice, which sits inside h_3(O) as the range of the projection E' (NOT 'coherently induced from the ambient via E'). Confirm clause (iii) ITSELF is NOT weakened (only RESTRICTION's embedding clause) — fp-redefine-iii; and V_BM remains the observer's OWN internal composite A (x) A, not transported from the ambient — fp-conflate-composites."
      pass_condition: "Corrected RESTRICTION (coexistence-as-island) stated; embedding clause weakened to Bryan's wording; clause (iii) integrity preserved (only the embedding clause weakened); V_BM not conflated with the ambient (Phase 60 distinction explicitly preserved)."
      evidence_required: [deliv-embedding, ref-paper5-def1]
      automation: human
    - id: test-fork-open
      subject: claim-restriction
      kind: consistency
      procedure: "Confirm the obstruction-or-preservation fork is stated and kept OPEN with CORRECTED verdict semantics: (P) if E transports the sequential product coherently (R=0) -> a RESTRICTION embedding lemma (62-03); (O) if E does not (R!=0) -> an ambient-transport obstruction (62-03) that is the EXPECTED, ACCEPTABLE outcome and REFINES RESTRICTION to coexistence-as-island (self-contained C* island; through-line survives; E = access/projection map; NOT independent posits). Confirm NO pre-commitment to either branch, and that (O) is explicitly framed as a refinement, NOT a program collapse."
      pass_condition: "Both fork branches stated; no pre-commitment; (O) framed as EXPECTED + a refinement to coexistence-as-island (not independent posits, not collapse); the v11.0 leakage precedent is NOT carried as a prior that leans toward (O) (different mechanism — Clifford pairs, not the projection restriction)."
      evidence_required: [deliv-embedding]
      automation: human
  forbidden_proxies:
    - id: fp-assert-preservation
      subject: claim-restriction
      proxy: "Asserting (or implying) that E transports the sequential product coherently / 'E preserves everything' / 'E is a conditional expectation so it transports clause (iii)' WITHOUT framing the decisive object as the AMBIENT transport residual E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) for generic X,Y and WITHOUT deferring its computation to the actual non-associative h_3(O) (62-02). Equivalently: treating lem:bottleneck's Jordan-product-preserving property as if it automatically covered the non-Jordan triple product transport (it does NOT — E is not even a Jordan morphism on ambient elements)."
      reason: "The roadmap's central Phase 62 reward-hacking guard (fp-assert-preservation / assert-Peirce-preserves-iii in claim.md). lem:bottleneck builds E for the JORDAN product on the slice only; the sequential-product TRANSPORT from the ambient is a different, non-associative question. The setup must frame the AMBIENT transport question as OPEN — never assert it."
    - id: fp-ignore-nonassociativity
      subject: claim-restriction
      proxy: "Framing the decisive question as the slice-internal sqrt(a) b sqrt(a) (a,b in A) — which is TRIVIAL (closed associative subalgebra, leakage 0, associator 0, non-associativity NOT engaged) — and passing it off as the decisive non-associative test; or the line-loophole of running a non-associativity exerciser on UNRELATED matrices while the decisive test stays the trivial slice-internal one."
      reason: "The NON-ASSOCIATIVITY guard (roadmap). The slice-internal case cannot engage non-associativity (verified: slice associator ~1e-15; range E is closed/associative). The decisive object MUST be the AMBIENT transport residual for generic X,Y where (xy)z != x(yz) is genuinely engaged. The setup must record the slice-internal triviality plainly AND frame the decisive crux as ambient transport."
    - id: fp-force-positive
      subject: claim-restriction
      proxy: "Pre-committing the setup toward preservation OR toward obstruction — wording the crux so one branch is the 'expected' answer in a way that pre-decides 62-02; or omitting either branch / the corrected verdict semantics."
      reason: "The verdict must follow 62-02's exact computation. An ambient-transport obstruction (O) is the EXPECTED, ACCEPTABLE outcome that REFINES RESTRICTION (coexistence-as-island), not a collapse — but the setup must keep the fork genuinely open and NOT carry the v11.0/Phase 42 precedent (sqrt(T_a) T_b sqrt(T_a) exits M_16(R)) as a prior leaning toward (O): that is a DIFFERENT mechanism (Clifford non-commuting pairs, not the slice/projection restriction) (plan-checker warning) — drop it as a prior."
    - id: fp-redefine-iii
      subject: claim-restriction
      proxy: "Weakening clause (iii) ITSELF (dropping any of its four data — product states, product effects, non-signaling, product-form sequential product — or relaxing 'minimal') under the guise of the coexistence-as-island reframe."
      reason: "The reframe weakens RESTRICTION's EMBEDDING clause ('induced from the ambient via E' -> 'observer self-models on the slice, which sits inside h_3(O) as the range of E') ONLY. Clause (iii) was established verbatim and intrinsically in Phase 61 and stays verbatim (claim.md clause-(iii) integrity guard). Do NOT conflate weakening the embedding clause with weakening clause (iii)."
    - id: fp-conflate-composites
      subject: claim-restriction
      proxy: "Under the coexistence-as-island reframe, identifying the observer's internal composite V_BM = A (x) A with the BGW universe-tensoring of h_3(O), or arguing the observer's composite is transported from / a sub-structure of the ambient h_3(O) composite."
      reason: "Phase 60 established V_BM (observer's OWN body (x) model self-composite) is type-distinct from the BGW bifunctor on h_3(O). Coexistence-as-island is FULLY CONSISTENT with that distinction (the island has its own composite; the basin fixes the TYPE, not the composite). The setup must make that explicit, not collapse the two."
  links:
    - id: link-embedding-setup
      source: claim-restriction
      target: deliv-embedding
      relation: supports
      verified_by: [test-E-explicit, test-E-preserves-stated, test-crux-framed-ambient, test-slice-triviality, test-corrected-restriction, test-fork-open]
    - id: link-effros-underwrites-E
      source: ref-effros-stormer
      target: deliv-embedding
      relation: supports
      verified_by: [test-E-explicit, test-E-preserves-stated]
    - id: link-bottleneck-supplies-E
      source: ref-lem-bottleneck
      target: deliv-embedding
      relation: supports
      verified_by: [test-E-explicit, test-crux-framed-ambient]
  uncertainty_markers:
    weakest_anchors:
      - "ref-effros-stormer and ref-lem-bottleneck establish E as a JORDAN conditional expectation (positive, unital, idempotent, Jordan-product-preserving embedding) — but only ON THE SLICE (E|_A = id). NEITHER says E is a Jordan morphism on AMBIENT elements (it is NOT: E(X o X) vs (EX) o (EX) differ), and neither says anything about TRANSPORTING the sequential product from the ambient. The decisive ambient-transport question rests on a property the supplying lemmas do NOT provide — this is the genuinely non-trivial crux."
      - "The slice-internal sequential-product datum is TRIVIAL (the slice is a closed associative subalgebra = range E; leakage 0, associator 0) — verified against code/octonion_algebra.py (slice associator ~1e-15; ambient associator ~170). So the slice-internal case carries NO information about ambient transport; only the AMBIENT transport residual (generic X,Y) is decisive. E's conditional-expectation properties (positivity, idempotency on the full 27-dim algebra) are stated here and COMPUTED exactly in 62-02."
    disconfirming_observations:
      - "If E (entrywise C_u-projection) FAILS to be a positive unital idempotent onto a Jordan subalgebra of h_3(O) — e.g. if proj_u entrywise does not yield a Jordan subalgebra, or E o E != E on the full 27-dim algebra — then the setup itself is wrong and lem:bottleneck's E must be reconstrued. (62-02 verifies idempotency/unitality exactly.)"
      - "THE decisive observation (deferred to 62-02, flagged here): the EXACT AMBIENT transport residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) for generic X,Y in h_3(O) (sqrt(X) in the ambient; non-associativity engaged). R != 0 => E does NOT transport the sequential product coherently => an ambient-transport obstruction — which is the EXPECTED, ACCEPTABLE outcome that REFINES RESTRICTION to coexistence-as-island (self-contained C* island), NOT a refutation or independent posits. R = 0 => coherent transport (a RESTRICTION embedding lemma). The fork must NOT be pre-decided either way; and the v11.0 leakage precedent is NOT carried as a prior (different mechanism — Clifford pairs, not the projection restriction)."
---

<objective>
Set up the Peirce/bottleneck conditional expectation E: h_3(O) -> h_3(C_u) explicitly (Effros-Stormer: positive, unital, idempotent, range = JB-subalgebra), state PRECISELY what E preserves, and FRAME the decisive sequential-product-under-E crux — without asserting its answer.

Purpose: Step 3 of the v15.0 four-step attack (Phase 62), the load-bearing, entirely-unproved step. Phase 61 proved the slice A = M_3(C)^sa satisfies all four Def 1 clauses INTRINSICALLY (in its own right), with the induced-by-E question explicitly deferred here. This plan builds the analytic SETUP: the explicit conditional expectation E, exactly what it preserves (the Jordan product a o b — and crucially NOT, automatically, the sequential product), and the sharp statement of the crux that 62-02 will COMPUTE on the actual non-associative h_3(O). The setup feeds the decisive computation (62-02) and the verdict (62-03).

Output: derivations/p5-basin-restriction/embedding-under-E.md (sections §1-§3: E setup, what E preserves on the slice [+ that E is not a Jordan morphism on ambient elements], the AMBIENT E-transport crux + the slice-internal triviality control + the corrected RESTRICTION + the fork; 62-02 appends §4 the computation, 62-03 appends §5 the verdict). This plan creates the file and writes §1-§3.

CORRECTED FRAMING (2026-05-24, Bryan's decision — plan-checker found the original slice-internal test mathematically trivial; verified): the slice A = h_3(C_u) is a CLOSED, ASSOCIATIVE Jordan subalgebra (= range E), so the slice-internal sqrt(a) b sqrt(a) (a,b in A) has leakage EXACTLY 0 and cannot engage non-associativity — it is a TRIVIAL control, not the decisive test. The decisive object is AMBIENT E-TRANSPORT: E(sqrt(X) Y sqrt(X)) vs sqrt(EX)(EY)sqrt(EX) for GENERIC ambient X,Y in h_3(O). And RESTRICTION is weakened to COEXISTENCE-AS-ISLAND: the observer self-models on the slice (Phase 61) and the slice sits inside h_3(O) as the range of E — it does NOT need its structure transported from the ambient. An ambient-transport obstruction is EXPECTED and REFINES RESTRICTION (island), it does NOT refute it.

CRITICAL SCOPE GUARDS (carry into the deliverable):
- E is built for the JORDAN product ON THE SLICE (lem:bottleneck: Jordan-product-preserving embedding; Effros-Stormer range = JB-subalgebra; E|_A = id). State E-preserves-Jordan-on-the-slice PRECISELY, AND state that E is NOT a Jordan morphism on AMBIENT elements (E(X o X) vs (EX) o (EX) differ). Do NOT assert E transports the SEQUENTIAL product from the ambient — that is the open crux (fp-assert-preservation).
- The decisive object is the AMBIENT transport residual E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) for GENERIC X,Y, where sqrt(X) is in the ambient and non-associativity ((xy)z != x(yz)) is genuinely engaged. Record the slice-internal case as a TRIVIAL control (closed associative subalgebra, leakage 0, associator 0); do NOT pass it off as the decisive non-associative test, and do NOT use the line-loophole of exercising non-associativity on UNRELATED matrices (fp-ignore-nonassociativity).
- State the CORRECTED RESTRICTION (coexistence-as-island) and weaken RESTRICTION's EMBEDDING clause to 'the observer self-models on the slice, which sits inside h_3(O) as the range of the projection E' — but DO NOT weaken clause (iii) itself (fp-redefine-iii), and keep V_BM the observer's own composite A (x) A (fp-conflate-composites; consistent with the Phase 60 distinction).
- Keep the obstruction-or-preservation fork OPEN with CORRECTED verdict semantics: (P) coherent transport -> RESTRICTION embedding lemma; (O) obstruction -> EXPECTED, ACCEPTABLE, REFINES RESTRICTION to coexistence-as-island (NOT independent posits, NOT collapse). Do NOT pre-commit to either branch; do NOT carry the v11.0/Phase 42 precedent (sqrt(T_a) T_b sqrt(T_a) exits M_16(R)) as a prior leaning toward (O) — it is a DIFFERENT mechanism (Clifford non-commuting pairs, not the projection restriction) (fp-force-positive).
- LIVE sources only (~/repos/blog/landing/papers/). The executor has Read access to the live .tex and the repo code/ + derivations/ — point to exact files/lines. The executor has NO web/arxiv: Effros-Stormer and Hanche-Olsen content is quoted in this plan and in the LIVE lem:bottleneck proof (complexification.tex:437) — use the quoted content.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@derivations/p5-basin-restriction/claim.md
@derivations/p5-basin-restriction/STATE.md
@derivations/p5-basin-restriction/slice-clause-iii.md
@derivations/p5-basin-restriction/rem-converse-bgw.md
@code/octonion_algebra.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Construct E: h_3(O) -> h_3(C_u) explicitly (positive unital idempotent, Effros-Stormer range = JB-subalgebra); state what E preserves (the Jordan product)</name>
  <files>derivations/p5-basin-restriction/embedding-under-E.md</files>
  <action>
    Create derivations/p5-basin-restriction/embedding-under-E.md — the Phase 62 deliverable. This task writes the header, §1 (E setup), and §2 (what E preserves). 62-02 will append §4 (the decisive computation) and 62-03 §5 (the verdict); leave a clear "§4/§5 to follow (62-02 / 62-03)" marker.

    HEADER: put at the top an ASSERT_CONVENTION comment matching the milestone artifacts:
    "<!-- ASSERT_CONVENTION: metric_signature=riemannian_fisher, fourier_convention=na, natural_units=natural, gauge_choice=na, renormalization_scheme=na -->"
    plus: "<!-- Pure-algebra: Jordan product a o b = (1/2)(ab+ba); sequential product a&b = sqrt(a) b sqrt(a) (CFC, principal branch). Type/category + Peirce-grade consistency is the dimensional-analysis analog. -->"
    "<!-- Provenance: LIVE papers only (~/repos/blog/landing/papers/). NOT the stale repo papers/. -->"
    "<!-- Slice: A = h_3(C_u) ~ M_3(C)^sa (n=3; u = e_7; any u in S^6 equivalent under G_2). -->"
    Title: "# embedding-under-E.md — Coherent embedding under the bottleneck conditional expectation E (the hard part)"
    Plan/Step line: Plan 62-01 (Phase 62, milestone v15.0) — DERV-62-01. Step 3 of 4.
    Scope line: this file's §1-§3 are the SETUP + CRUX FRAMING (62-01); §4 is the decisive computation (62-02, VALD-62-01); §5 is the verdict on the fork (62-03). The induced-by-E verdict is NOT reached in this plan.

    First READ the inputs: claim.md (RESTRICTION, allowed inputs, prohibited moves, PAUSE conditions — especially fp-assert-preservation / assert-Peirce-preserves-iii and PAUSE condition 2), the derivation-tree STATE.md (Step 2 COMPLETE, Step 3 = THIS, the [62] open question), slice-clause-iii.md §6 (the explicit Phase 62 deferral and what was/was not claimed intrinsically), and code/octonion_algebra.py (the H3O class, _mat_mul_h3o non-associative matrix product, jordan_product, peirce_V1/Vhalf/V0, proj_u, pi_u). Re-read the LIVE lem:bottleneck: ~/repos/blog/landing/papers/sm-from-self-modeling/sections/complexification.tex lines 409-487 (statement, Effros-Stormer at 437, Peirce decomposition (iii) at 481-486).

    §0 — SETUP, INPUTS, TYPE/CATEGORY + PEIRCE-GRADE LEDGER:
    - Recall the slice A = h_3(C_u) ~ M_3(C)^sa (lem:bottleneck (ii)), C_u = R + R*u, u = e_7. Real dims: h_3(O) = 27 (3 real diagonal + 3 octonionic off-diagonal), A = 9 (3 real diagonal + 3 C_u off-diagonal).
    - Type/category ledger (carry the style from slice-clause-iii.md §0.3): tag E (conditional expectation), h_3(O) (exceptional FRJA, non-special, BGW-non-composable), A (special simple FRJA), the Jordan product (Jordan-algebra binary op), the sequential product (CFC triple product on effects — NOT a Jordan-algebra op). The Peirce-grade analog of dimensional analysis: every object carries a Peirce grade under E_11 ({0,1/2,1}) and a real-dimension; track them.

    §1 — EXPLICIT CONSTRUCTION OF E:
    - DEFINE E: h_3(O) -> h_3(C_u) entrywise. For X in h_3(O) with H3O coordinates (alpha,beta,gamma in R; x1,x2,x3 octonions), set E(X) = h_3(O) element with the SAME real diagonal (alpha,beta,gamma) and each off-diagonal octonion projected onto C_u: x_i -> proj_u(x_i) = (component along 1) + (component along u=e_7) [i.e. keep octonion components 0 and 7, zero components 1..6]. This is exactly code/octonion_algebra.py proj_u applied to x1,x2,x3 (note proj_u there is for u=e_7; cite it). Write the explicit 3x3-matrix form: E projects every matrix entry onto the associative subfield C_u entrywise.
    - State the FOUR conditional-expectation properties WITH justification (these are verified exactly in 62-02; here state + justify):
        (a) UNITAL: E(I_3) = I_3 (the identity has real diagonal (1,1,1) and zero off-diagonal, all already in C_u). 
        (b) IDEMPOTENT: E o E = E, because proj_u o proj_u = proj_u entrywise (projecting onto C_u twice = once); diagonal untouched.
        (c) E|_A = id: for X in A = h_3(C_u), every entry is already in C_u, so E(X) = X. (Consistency / limiting check: E restricted to the slice is the identity — this is the "E recovers lem:bottleneck on the slice" check.)
        (d) POSITIVE: E is positive (maps the positive cone of h_3(O) into the positive cone of A). Justify via Effros-Stormer (the range of a positive unital idempotent is a JB-subalgebra) AND note the entrywise-C_u-projection realizes the standard conditional expectation onto h_3(C_u); positivity to be confirmed exactly in 62-02 on test elements (eigenvalue check).
    - EFFROS-STORMER: quote the LIVE lem:bottleneck proof verbatim (complexification.tex:437): "Effros-Stormer: the range of a positive unital idempotent on a JB-algebra is a JB-subalgebra. In finite dimensions over R this is just a Jordan subalgebra; by the hypothesis on E the embedding is moreover Jordan-product-preserving." Conclude: E's range A = h_3(C_u) ~ M_3(C)^sa is a JB-subalgebra (= Jordan subalgebra in fin-dim), and the embedding A -> h_3(O) is Jordan-product-preserving. dim(range E) = dim A = 9; ker E = 18-dim (the components x_i along e_1..e_6, three octonions x6 = 18 real dims).
    - Record the Peirce decomposition of A at the rank-1 idempotent E_11 (lem:bottleneck (iii), complexification.tex:481-486): within h_3(C_u), V_1(E_11) ~ R, V_{1/2}(E_11) ~ C_u^2, V_0(E_11) ~ h_2(C_u) ~ M_2(C)^sa; dims 1+4+4 = 9. This is the Peirce structure E restricts through; carry the prior input V_1 = R bottleneck (v6.0/v8.0: the Peirce interface to V_{1/2} is scalar).

    §2 — WHAT E PRESERVES (PRECISELY):
    - State PRECISELY: E IS coherent with the JORDAN product ON THE SLICE. Specifically, lem:bottleneck makes the embedding A -> h_3(O) Jordan-product-preserving and on the slice E|_A = id recovers lem:bottleneck exactly (limiting/consistency check). Spot-check note for 62-02: E(a o b) = a o b for a,b in the slice is a Jordan-side control that should PASS (and is trivial since the slice is closed).
    - State EXPLICITLY that E is NOT a Jordan morphism on AMBIENT elements: for generic X in h_3(O), E(X o X) != (EX) o (EX) in general (62-02 will confirm these differ by an O(1) amount, ~22 on representative elements). So E does NOT transport even the Jordan structure from the ambient — a fortiori it need not transport the sequential product. This is what makes the AMBIENT transport question (§3) genuinely non-trivial.
    - State EXPLICITLY that the decisive question is AMBIENT TRANSPORT of the SEQUENTIAL product a&b = sqrt(a) b sqrt(a): whether E(sqrt(X) Y sqrt(X)) = sqrt(EX)(EY)sqrt(EX) for generic ambient X,Y (§3). REJECT explicitly the hand-wave "E is a conditional expectation, so it transports clause (iii)" (fp-assert-preservation): E's Jordan-coherence ON THE SLICE does NOT extend to transporting the non-Jordan triple product from the non-associative ambient, and E is not even a Jordan morphism on the ambient.

    Add a TYPE/CATEGORY + PEIRCE check at the end of each of §1, §2 (mirror slice-clause-iii.md's per-clause checks): confirm every step is internal to the stated categories; confirm E maps Peirce grades coherently for the Jordan product (E commutes with the Peirce projections of A, since E|_A = id and E kills the e_1..e_6 octonion components which are orthogonal to C_u).

    CONFIDENCE marker for §1-§2 (HIGH for the explicit construction + Effros-Stormer Jordan-side; the sequential-product coherence is explicitly UNRESOLVED and deferred to §3/§4).
  </action>
  <verify>
    1. embedding-under-E.md created with the ASSERT_CONVENTION header, provenance/slice notes, title, plan/step line, and the §1-§3 (this plan) / §4 (62-02) / §5 (62-03) scope marker.
    2. §1 constructs E EXPLICITLY as entrywise proj_u (C_u = span{1,e_7}) on the off-diagonal octonions x1,x2,x3, identity on the real diagonal (alpha,beta,gamma); the explicit 3x3-matrix form is written; code/octonion_algebra.py proj_u is cited.
    3. §1 states + justifies the four conditional-expectation properties: unital E(I_3)=I_3; idempotent E o E = E (proj_u idempotent); E|_A = id (slice entries in C_u); positive (Effros-Stormer + entrywise C_u-projection, exact confirmation deferred to 62-02). dim(range E)=9, ker E = 18-dim stated.
    4. §1 quotes Effros-Stormer VERBATIM from complexification.tex:437 and concludes range A = h_3(C_u) is a JB/Jordan subalgebra with Jordan-product-preserving embedding. The Peirce decomposition of A at E_11 (V_1~R, V_{1/2}~C_u^2, V_0~h_2(C_u)~M_2(C)^sa; 1+4+4=9) is recorded from complexification.tex:481-486.
    5. §2 states PRECISELY that E preserves the JORDAN product ON THE SLICE (lem:bottleneck; E|_A=id recovers bottleneck on the slice), states that E is NOT a Jordan morphism on AMBIENT elements (E(X o X) != (EX) o (EX)), and EXPLICITLY frames the decisive question as AMBIENT transport of the sequential product (NOT slice-internal); the "E is a conditional expectation so it transports clause (iii)" hand-wave is explicitly rejected (fp-assert-preservation).
    6. Mathematical-rigor / structural checks present: idempotency E o E = E, unitality E(I_3)=I_3, E|_A = id, Jordan-subalgebra membership of the range, E-NOT-a-Jordan-morphism-on-ambient, Peirce-grade bookkeeping (E commutes with A's Peirce projections on the Jordan side). Real-dimension bookkeeping (27 = 9 + 18) consistent.
    7. STRUCTURAL ESTIMATE flagged (full version in §3/Task 2): E not being a Jordan morphism on the ambient makes a nonzero AMBIENT-TRANSPORT residual unsurprising, with E's kernel (the e_1..e_6 octonion components) named as where the transport defect would land; the decisive test is ambient transport (generic X,Y), not the trivial slice-internal case.
  </verify>
  <done>derivations/p5-basin-restriction/embedding-under-E.md exists with the convention header and §0-§2: E: h_3(O) -> h_3(C_u) constructed explicitly as the entrywise C_u-projection (positive, unital, idempotent, E|_A=id; range = JB-subalgebra via Effros-Stormer quoted from complexification.tex:437; Jordan-product-preserving embedding; Peirce decomposition of A at E_11 recorded); §2 states PRECISELY that E preserves the Jordan product a o b and explicitly does NOT claim it preserves the sequential product (fp-assert-preservation rejected); type/category + Peirce-grade and real-dimension bookkeeping clean; §4/§5-to-follow marker present.</done>
</task>

<task type="auto">
  <name>Task 2: Frame the decisive crux as AMBIENT E-transport (not slice-internal); record the slice-internal triviality control; state the CORRECTED RESTRICTION (coexistence-as-island); state and OPEN the fork (obstruction EXPECTED); set up 62-02</name>
  <files>derivations/p5-basin-restriction/embedding-under-E.md</files>
  <action>
    Append §3 to derivations/p5-basin-restriction/embedding-under-E.md — the CRUX framing (AMBIENT E-transport), the slice-internal triviality control, the CORRECTED RESTRICTION, and the obstruction-or-preservation fork. This is the analytic heart of the setup; 62-02 turns §3's decisive question into an exact computation.

    CONTEXT FOR THE EXECUTOR (why this framing): the plan-checker found, and it was independently verified against code/octonion_algebra.py, that the slice-internal test (sqrt(a) b sqrt(a) for a,b IN the slice A = h_3(C_u)) is mathematically TRIVIAL — the slice is a CLOSED, ASSOCIATIVE Jordan subalgebra (= range E), so sqrt(a) for a slice element a is the M_3(C) square root and STAYS in the slice; leakage is EXACTLY 0 and the triple associator is EXACTLY 0 (slice associator ~1e-15; ambient associator ~170). The slice-internal case cannot engage non-associativity. Therefore the DECISIVE object is AMBIENT E-TRANSPORT, and RESTRICTION is corrected to coexistence-as-island (Bryan 2026-05-24).

    §3 — THE CRUX: DOES E TRANSPORT THE SEQUENTIAL PRODUCT COHERENTLY FROM THE NON-ASSOCIATIVE AMBIENT?

    (3.1) The two products, and E on each. State explicitly:
    - Jordan product: a o b = (1/2)(ab + ba) — a symmetric Jordan-algebra binary op. E preserves it ON THE SLICE (§2, lem:bottleneck, E|_A=id). But E is NOT a Jordan morphism on AMBIENT elements: E(X o X) != (EX) o (EX) in general (the executor will confirm in 62-02 that these differ by an O(1) amount, ~22 on representative elements). So even the Jordan structure is not transported from the ambient by E — a fortiori the sequential product need not be.
    - Sequential product: a & b = sqrt(a) b sqrt(a) — a CFC-based, temporally-asymmetric TRIPLE product on effects, NOT a Jordan-algebra op; clause (iii)'s fourth datum (ref-paper5-def1; def:product-sp).
    - KEY POINT: the decisive question is whether E TRANSPORTS the sequential product from the ambient: E(sqrt(X) Y sqrt(X)) =?= sqrt(EX)(EY)sqrt(EX). This is the morphism-coherence of E for the SP, on the ambient — genuinely non-trivial precisely because E is not even a Jordan morphism on the ambient.

    (3.2) Why the slice-internal version is TRIVIAL (record plainly as the CONTROL). State explicitly:
    - The slice A = h_3(C_u) is a CLOSED, ASSOCIATIVE Jordan subalgebra of h_3(O) (= range E). For a,b in A: sqrt(a) is the M_3(C) square root and lies in A; sqrt(a) b sqrt(a) lies in A; hence E(sqrt(a) b sqrt(a)) = sqrt(a) b sqrt(a) (leakage EXACTLY 0) and the triple associator is EXACTLY 0. The SP datum is induced TRIVIALLY on the slice; non-associativity is NOT engaged. (This is Phase 61's intrinsic result, re-seen: the slice carries clause (iii) in its own right — trivially closed.)
    - State that this slice-internal case is the documented CONTROL, carrying NO information about ambient transport. Explicitly FORECLOSE the reward-hack of (i) passing the trivial slice-internal residual off as the decisive non-associative result, or (ii) running a non-associativity exerciser on UNRELATED matrices while the decisive test stays the trivial slice one (the line-loophole) — fp-ignore-nonassociativity.

    (3.3) The decisive computable question = AMBIENT E-TRANSPORT (the 62-02 spec). State it precisely:
    - INPUT: GENERIC X, Y in h_3(O) (X PSD so sqrt(X) exists; Y Hermitian) — NOT confined to the slice. 
    - COMPUTE sqrt(X) in the AMBIENT h_3(O) by the ambient Jordan/CFC spectral calculus (h_3(O) is power-associative, so sqrt(X) is well-defined as a polynomial in X, I — but X, Y generic means the triple product genuinely engages non-associativity). Form SP_ambient(X,Y) = sqrt(X) Y sqrt(X) via the octonionic 3x3 matrix product (code/octonion_algebra.py _mat_mul_h3o style, ported to EXACT arithmetic).
    - TEST the AMBIENT TRANSPORT RESIDUAL: R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX), where the RHS computes the sequential product of the PROJECTED elements EX, EY using the (slice) square root of EX. 
        * EXACT R = 0  =>  E TRANSPORTS the sequential product coherently (P).
        * EXACT R != 0  =>  E does NOT transport it: an AMBIENT-TRANSPORT OBSTRUCTION (O) — the EXPECTED outcome.
    - NON-ASSOCIATIVITY MUST BE LOAD-BEARING: the chosen X,Y must genuinely exercise (xy)z != x(yz) — require 62-02 to verify the associator of the relevant ambient triple is NONZERO on the chosen X,Y (so the test is not accidentally associative).
    - INDEPENDENT CROSS-CHECK (mandate for 62-02): a SECOND route to the same verdict — a Peirce/grade-component argument decomposing sqrt(X) Y sqrt(X) and the projected product into C_u-vs-(e_1..e_6) components / Peirce grades, tracking whether the transport defect populates ker E. The two routes must agree.

    (3.4) EXACT-ARITHMETIC requirement (methodological, critical — carry verbatim into 62-02). State:
    - The transport residual test MUST distinguish a genuine obstruction from numerical round-off. Use EXACT arithmetic (SymPy symbolic / exact rationals/surds), NOT float64. A nonzero R at float64 tolerance is NOT evidence; an EXACT nonzero R IS.
    - Ambient square roots of generic PSD elements may be irrational. Handle via symbolic sqrt OR by choosing X = C^2 with C an ambient element with exact entries (exact-square trick) so sqrt(X) is exact — WHILE keeping X, Y GENERIC ambient (NOT in the slice) so the triple product genuinely exercises non-associativity. Document this and confirm it does NOT trivialize non-associativity (verify the associator is nonzero on the chosen X,Y).
    - ERROR BUDGET: zero-tolerance decisive test. exact R == 0 (SymPy .equals(zeros)) => coherent transport; exact R symbolically nonzero => obstruction. NO float tolerance on the decisive assertion.

    (3.5) STRUCTURAL EXPECTATION (before computing — required, but DO NOT pre-decide). State:
    - E is NOT a Jordan morphism on ambient elements (E(X o X) != (EX) o (EX)). The sequential product is an even more rigid (CFC triple) structure. So it would be UNSURPRISING if E fails to transport it (R != 0). State this as a structural observation, NOT a prior that forces the verdict.
    - DROP the v11.0/Phase 42 precedent as a leaning-toward-O prior: sqrt(T_a) T_b sqrt(T_a) exiting M_16(R) is a DIFFERENT mechanism (Clifford NON-COMMUTING pairs in a fixed matrix algebra, not the h_3(O) -> h_3(C_u) PROJECTION/restriction). Per the plan-checker warning, it does NOT bear on the ambient E-transport verdict — note it only as historically adjacent, not as evidence.
    - HONEST EXPECTATION: an ambient-transport obstruction (O) is the EXPECTED, ACCEPTABLE outcome (and would REFINE RESTRICTION to coexistence-as-island, §3.7), but the verdict is decided by the exact computation (62-02), not by this expectation. Do NOT pre-commit (fp-force-positive).

    (3.6) THE OBSTRUCTION-OR-PRESERVATION FORK (stated, kept OPEN; CORRECTED verdict semantics):
    - (P) COHERENT TRANSPORT: if 62-02 finds exact R = 0, then E transports the sequential product coherently => 62-03 states and proves the RESTRICTION embedding lemma (touching the actual non-associative structure).
    - (O) AMBIENT-TRANSPORT OBSTRUCTION (EXPECTED): if 62-02 finds exact R != 0, then 62-03 characterizes the obstruction precisely — the exact structure E cannot transport from the non-associative ambient (the product-form sequential product), where the defect lands (Peirce grade / e_k), and the minimal extra input — and this REFINES RESTRICTION to coexistence-as-island (§3.7). A clean (O) is a fully acceptable DELIVERABLE, the EXPECTED result; it is NOT a program-collapse PAUSE.
    - This plan does NOT decide the fork; 62-02 provides the decisive evidence and 62-03 reads the verdict. Wire the handoff explicitly: 62-02's exact ambient residual R is the decisive input to 62-03's verdict.

    (3.7) THE CORRECTED RESTRICTION (coexistence-as-island) — state it as the governing frame:
    - RESTRICTION needs only COEXISTENCE-AS-ISLAND: the observer is a self-certified M_3(C)^sa system on the slice (Phase 61: all four Def 1 clauses verbatim, intrinsically) and the slice sits inside h_3(O) as the range of the projection E. It does NOT need the observer's structure transported/derived from the ambient via E.
    - WEAKEN RESTRICTION's EMBEDDING clause from 'the observer's V_BM is realized coherently as a sub-structure of h_3(O) induced by the Peirce/bottleneck restriction E' to 'the observer self-models on the slice, which sits inside h_3(O) as the range of the projection E'. E is the ACCESS/PROJECTION map, NOT required to be a Jordan morphism on the ambient.
    - INTEGRITY (state both): clause (iii) ITSELF is NOT weakened (it stands verbatim, established intrinsically in Phase 61) — only RESTRICTION's embedding clause is weakened (fp-redefine-iii). And V_BM remains the observer's OWN internal composite A (x) A, never identified with / transported from the BGW universe-tensoring of h_3(O) — coexistence-as-island is FULLY CONSISTENT with the Phase 60 two-composites distinction (the island has its own composite; the basin fixes the TYPE M_3(C)^sa, not the composite) (fp-conflate-composites). Consistent with U-B-M (Peirce != tensor; basin fixes TYPE).
    - THROUGH-LINE under (O): even if E does not transport the SP, the through-line SURVIVES as: h_3(O) is the basin whose maximal C* slice is M_3(C)^sa, on which Paper 5 certifies the observer's QM; E is the access/projection map. (O) does NOT establish 'independent posits / two unconnected foundations' — it REFINES the claim to 'self-contained C* island'.

    Add a final TYPE/CATEGORY + PEIRCE self-audit for §3: sequential product tagged as a CFC triple product (NOT a Jordan op); decisive object = ambient transport residual (generic X,Y); slice-internal case tagged TRIVIAL control; E tagged access/projection (not a Jordan morphism on the ambient); clause (iii) integrity preserved; V_BM not conflated with the ambient; fork open with (O) expected/acceptable as a refinement.

    CONFIDENCE marker for §3: the CRUX is correctly framed as ambient E-transport, the slice-internal triviality is recorded as the control, the corrected RESTRICTION is stated, and the decisive question is well-posed for exact computation; the VERDICT is explicitly UNRESOLVED pending 62-02.
  </action>
  <verify>
    1. §3 appended; (3.1) distinguishes the Jordan product (E-preserved ON THE SLICE; but E NOT a Jordan morphism on ambient elements, E(X o X) != (EX) o (EX)) from the sequential product (CFC triple product), and frames the decisive question as ambient E-transport coherence E(sqrt(X) Y sqrt(X)) =?= sqrt(EX)(EY)sqrt(EX).
    2. (3.2) RECORDS the slice-internal triviality plainly: A = h_3(C_u) is a closed associative subalgebra (= range E); slice-internal sqrt(a) b sqrt(a) has leakage EXACTLY 0 and associator EXACTLY 0; the SP datum induced trivially; non-associativity NOT engaged; tagged as the CONTROL; the reward-hack (passing the trivial case off as decisive, or the unrelated-matrices exerciser loophole) explicitly foreclosed (fp-ignore-nonassociativity).
    3. (3.3) states the decisive AMBIENT-TRANSPORT question precisely (GENERIC X,Y in h_3(O); sqrt(X) in the ambient; residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX); R=0 => P, R!=0 => O), MANDATES that non-associativity be load-bearing (associator-nonzero check on X,Y), and mandates an independent Peirce/grade cross-check.
    4. (3.4) carries the EXACT-ARITHMETIC requirement: SymPy exact (not float64); the exact-square trick (X = C^2 ambient, exact entries) WITH X,Y GENERIC ambient so non-associativity is genuinely exercised; the guard that the choice must NOT trivialize non-associativity (associator-nonzero); zero-tolerance error budget.
    5. (3.5) gives the structural expectation WITHOUT pre-deciding: E not a Jordan morphism on the ambient => (O) unsurprising; the v11.0/Phase 42 precedent is DROPPED as a leaning-toward-O prior (different mechanism — Clifford pairs, not the projection restriction); verdict decided by computation, not expectation (fp-force-positive rejected).
    6. (3.6) states BOTH fork branches with CORRECTED semantics: (P) coherent transport -> RESTRICTION lemma (62-03); (O) ambient-transport obstruction -> EXPECTED, acceptable, REFINES RESTRICTION (62-03), NOT a collapse; fork OPEN; 62-02 -> 62-03 handoff wired.
    7. (3.7) states the CORRECTED RESTRICTION (coexistence-as-island): embedding clause weakened to Bryan's wording; clause (iii) integrity preserved (only the embedding clause weakened — fp-redefine-iii); V_BM not conflated with the ambient (Phase 60 distinction preserved — fp-conflate-composites); through-line survives under (O) (not independent posits); U-B-M consistency noted.
    8. Type/category + Peirce self-audit for §3 present; CONFIDENCE marker present (crux well-posed as ambient transport; verdict UNRESOLVED pending 62-02).
  </verify>
  <done>§3 of derivations/p5-basin-restriction/embedding-under-E.md frames the decisive crux as AMBIENT E-TRANSPORT (E(sqrt(X) Y sqrt(X)) =?= sqrt(EX)(EY)sqrt(EX) for GENERIC ambient X,Y, non-associativity load-bearing; E is not even a Jordan morphism on the ambient), records the slice-internal case as a TRIVIAL control (closed associative subalgebra; leakage 0; associator 0; SP induced trivially; non-associativity not engaged; reward-hack loophole foreclosed), specifies the decisive computation for 62-02 (exact ambient residual R with an associator-nonzero check and an independent Peirce cross-check), carries the exact-arithmetic requirement (SymPy; exact-square trick X=C^2 keeping X,Y generic ambient; zero-tolerance), gives a non-pre-deciding structural expectation (E not a Jordan morphism on the ambient => (O) unsurprising; v11.0 precedent DROPPED as a prior — different mechanism), states the obstruction-or-preservation fork with CORRECTED semantics (O expected, refines RESTRICTION, not collapse), and states the CORRECTED RESTRICTION (coexistence-as-island; embedding clause weakened to Bryan's wording; clause (iii) integrity preserved; V_BM not conflated; through-line survives under O; U-B-M consistency). fp-assert-preservation, fp-ignore-nonassociativity, fp-redefine-iii, fp-conflate-composites, fp-force-positive all rejected; no pre-commitment.</done>
</task>

</tasks>

<verification>
- E EXPLICIT: E: h_3(O) -> h_3(C_u) constructed as entrywise proj_u (C_u = span{1,e_7}); positive, unital (E(I_3)=I_3), idempotent (E o E = E, proj_u idempotent), E|_A = id; range = JB/Jordan subalgebra via Effros-Stormer (quoted complexification.tex:437); Jordan-product-preserving embedding ON THE SLICE; dim(range)=9, ker E=18-dim; Peirce decomposition of A at E_11 recorded (complexification.tex:481-486). (test-E-explicit)
- WHAT E PRESERVES (precise): E preserves the JORDAN product a o b ON THE SLICE (lem:bottleneck; E|_A=id recovers bottleneck on slice), but E is NOT a Jordan morphism on AMBIENT elements (E(X o X) != (EX) o (EX)). E does NOT automatically transport the SEQUENTIAL product from the ambient; "E is a conditional expectation so it transports clause (iii)" rejected (fp-assert-preservation). (test-E-preserves-stated)
- CRUX FRAMED = AMBIENT E-TRANSPORT: the decisive object is E(sqrt(X) Y sqrt(X)) =?= sqrt(EX)(EY)sqrt(EX) for GENERIC ambient X,Y (non-associativity load-bearing, associator-nonzero on X,Y; sqrt(X) in the ambient); the precise computable question for 62-02 (exact residual R; independent Peirce cross-check); exact-arithmetic requirement; structural expectation WITHOUT pre-deciding (E not a Jordan morphism on the ambient => O unsurprising; v11.0 precedent DROPPED — different mechanism). (test-crux-framed-ambient)
- SLICE-INTERNAL TRIVIALITY (control): slice A is a closed associative subalgebra (= range E); slice-internal sqrt(a) b sqrt(a) has leakage 0 / associator 0; SP induced trivially; non-associativity not engaged; recorded as the CONTROL, not the decisive test; the reward-hack loophole foreclosed. (test-slice-triviality; fp-ignore-nonassociativity rejected)
- CORRECTED RESTRICTION (coexistence-as-island): observer self-models on the slice (Phase 61) and the slice sits inside h_3(O) as range E; embedding clause weakened to Bryan's wording; clause (iii) integrity preserved (only embedding clause weakened — fp-redefine-iii); V_BM = observer's own A (x) A, not conflated with the ambient (fp-conflate-composites; Phase 60 distinction preserved); E = access/projection map. (test-corrected-restriction)
- FORK OPEN (corrected semantics): (P) coherent transport -> RESTRICTION lemma / (O) ambient-transport obstruction -> EXPECTED, acceptable, REFINES RESTRICTION to coexistence-as-island (NOT independent posits, NOT collapse); no pre-commitment; 62-02 -> 62-03 handoff wired. (test-fork-open)
- MATHEMATICAL RIGOR: idempotency, unitality, E|_A=id, Jordan-subalgebra membership of the range, E-not-a-Jordan-morphism-on-ambient, Peirce-grade bookkeeping, real-dimension bookkeeping (27 = 9 + 18) — all present and consistent.
- ANCHOR DISCIPLINE: ref-effros-stormer (E's range/positivity), ref-lem-bottleneck (E + slice + Peirce), ref-hanche-olsen (induced-vs-imported / coexistence-as-island framing), ref-paper5-def1 (clause iii's sequential-product datum, verbatim) surfaced where the plan depends on them; all LIVE/quoted (executor has no web).
- HONEST FRAMING: the fork is genuinely open; the setup does not pre-decide either branch; an ambient-transport obstruction is EXPECTED and a refinement, not a collapse (fp-force-positive rejected).
</verification>

<success_criteria>
The Peirce/bottleneck conditional expectation E: h_3(O) -> h_3(C_u) is set up EXPLICITLY (Effros-Stormer: positive, unital, idempotent, range = JB-subalgebra; entrywise C_u = span{1,e_7} projection; E|_A = id; Jordan-product-preserving embedding ON THE SLICE; Peirce decomposition of A at E_11 recorded). What E preserves is stated PRECISELY: E IS coherent with the Jordan product a o b ON THE SLICE (lem:bottleneck; E|_A=id recovers the bottleneck on the slice), but E is NOT a Jordan morphism on AMBIENT elements (E(X o X) != (EX) o (EX)) — so the ambient transport question is genuinely non-trivial. The slice-internal sequential product (a,b in A) is recorded plainly as a TRIVIAL control (A is a closed associative subalgebra = range E; leakage 0, associator 0; SP induced trivially; non-associativity not engaged). The decisive crux is framed as AMBIENT E-TRANSPORT: whether E(sqrt(X) Y sqrt(X)) = sqrt(EX)(EY)sqrt(EX) for GENERIC ambient X,Y (sqrt(X) in the ambient; non-associativity genuinely engaged, associator-nonzero on X,Y) — well-posed for 62-02 (exact ambient residual R; independent Peirce-grade cross-check; exact arithmetic, exact-square trick X=C^2 keeping X,Y generic ambient, zero-tolerance error budget). A structural expectation is given WITHOUT pre-deciding (E not a Jordan morphism on the ambient makes (O) unsurprising; the v11.0/Phase 42 precedent is DROPPED as a leaning-toward-O prior — different mechanism, Clifford pairs not the projection restriction; verdict decided by computation). The CORRECTED RESTRICTION (coexistence-as-island) is stated: the observer self-models on the slice (Phase 61, all four clauses verbatim) and the slice sits inside h_3(O) as the range of E; RESTRICTION's EMBEDDING clause is weakened to 'the observer self-models on the slice, which sits inside h_3(O) as the range of the projection E' — but clause (iii) ITSELF is unchanged (fp-redefine-iii) and V_BM stays the observer's own composite A (x) A (fp-conflate-composites; Phase 60 distinction preserved). The obstruction-or-preservation fork is stated and kept OPEN with corrected verdict semantics — (P) -> RESTRICTION embedding lemma (62-03), (O) -> ambient-transport obstruction that is EXPECTED, acceptable, and REFINES RESTRICTION to coexistence-as-island (NOT independent posits, NOT collapse) (62-03) — with the 62-02 -> 62-03 handoff wired. The deliverable rejects fp-assert-preservation (E's Jordan-coherence on the slice is NOT extended to ambient sequential-product transport), fp-ignore-nonassociativity (the decisive crux is ambient transport with non-associativity load-bearing; the slice-internal triviality is the control), fp-redefine-iii (clause iii unchanged; only the embedding clause weakened), fp-conflate-composites (V_BM not conflated with the ambient), and fp-force-positive (no pre-commitment; an obstruction is the expected refinement). No step asserts the ambient-transport verdict; that is 62-02 (evidence) and 62-03 (verdict).
</success_criteria>

<output>
After completion, create `.gpd/phases/62-coherent-embedding-under-e-the-hard-part/62-01-SUMMARY.md`.
</output>
