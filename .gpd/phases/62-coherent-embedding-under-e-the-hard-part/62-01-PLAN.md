---
phase: 62-coherent-embedding-under-e-the-hard-part
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - derivations/p5-basin-restriction/embedding-under-E.md
interactive: false

conventions:
  units: "natural (hbar=1, k_B=1); pure algebra — no physical dimensions"
  metric: "riemannian_fisher (N/A this plan); type/category + Peirce-grade consistency is the dimensional-analysis analog"
  algebra: "Jordan product a o b = (1/2)(ab+ba); sequential product a&b = sqrt(a) b sqrt(a) (Luders, temporally asymmetric; principal sqrt via CFC)"
  octonion: "Fano e_1 e_2 = e_4 (matches code/octonion_algebra.py infra)"
  complex_structure: "u = e_7 by default (any u in S^6 equivalent under G_2)"
  slice: "A = h_3(C_u) ~ M_3(C)^sa (maximal C*-target inside h_3(O); single F_4-orbit); C_u = R + R*u"
  peirce_eigenvalues: "{0, 1/2, 1}"
  conditional_expectation: "E: h_3(O) -> A, positive unital idempotent (Effros-Stormer); E|_A = id; embedding A -> h_3(O) Jordan-product-preserving"

dimensional_check:
  # Real vector-space / Peirce-grade bookkeeping (the pure-algebra analog of dimensional analysis).
  dim_h3O: "[real dim] = 27 (exceptional Albert algebra; 3 real diagonal + 3 octonionic off-diagonal = 3 + 24)"
  dim_A: "[real dim] = 9 (slice A = h_3(C_u) ~ M_3(C)^sa; 3 real diagonal + 3 C_u off-diagonal = 3 + 6)"
  dim_E_range: "[real dim] = 9 (E is idempotent onto A; rank E = dim A = 9; ker E = 18-dim complement)"
  peirce_at_E11: "h_3(C_u) = V_1(E_11) (+) V_{1/2}(E_11) (+) V_0(E_11) with V_1 ~ R (dim 1), V_{1/2} ~ C_u^2 (dim 4), V_0 ~ h_2(C_u) ~ M_2(C)^sa (dim 4); 1+4+4 = 9 (lem:bottleneck (iii))"

approximations:
  - name: "none (exact algebraic setup; no truncation, no perturbative expansion)"
    parameter: "N/A — pure algebra; this plan sets up E and FRAMES the sequential-product-under-E question analytically"
    validity: "always exact; the decisive computation (does E commute with &) is deferred to 62-02 (exact SymPy)"
    breaks_when: "N/A"
    check: "Every structural claim about E (positive, unital, idempotent, range = JB-subalgebra) is grounded in Effros-Stormer (lem:bottleneck proof, complexification.tex:437) or directly verifiable entrywise; the Jordan-vs-sequential-product distinction is stated as the OPEN crux, NOT asserted resolved"

estimated_execution:
  total_minutes: 55
  breakdown:
    - task: 1
      minutes: 35
      note: "Set up E: h_3(O) -> h_3(C_u) explicitly (entrywise C_u-projection; positive, unital, idempotent; Effros-Stormer range = JB-subalgebra); state precisely what E preserves (Jordan product YES via lem:bottleneck) (unfamiliar-formalism 1.5x + large-output multipliers applied)"
    - task: 2
      minutes: 20
      note: "FRAME the sequential-product-under-E crux: E is built for the JORDAN product; the sequential product sqrt(a) b sqrt(a) is a DIFFERENT (CFC, non-Jordan, non-associative) triple product; state the exact decisive question and the order-of-magnitude / structural expectation for leakage (carry v11.0 Phase 42 precedent); set up 62-02 + the obstruction-or-preservation fork"

contract:
  schema_version: 1
  scope:
    question: "Is the Peirce/bottleneck conditional expectation E: h_3(O) -> h_3(C_u) (Effros-Stormer: positive, unital, idempotent, range = JB-subalgebra) set up explicitly, and is what E preserves stated precisely — in particular whether E is coherent with the SEQUENTIAL product a&b = sqrt(a) b sqrt(a) (the decisive crux), not only the Jordan product a o b — WITHOUT asserting the answer (which 62-02 computes on the actual non-associative h_3(O))?"
  claims:
    - id: claim-restriction
      statement: "(Advanced, NOT closed by this plan.) The clause-(iii) self-modeling structure on the slice A = h_3(C_u) ~ M_3(C)^sa is coherently INDUCED by the ambient h_3(O) under E. This plan establishes the SETUP half: E: h_3(O) -> A is constructed explicitly as a positive unital idempotent (Effros-Stormer), and what E preserves is stated precisely — E IS coherent with the Jordan product a o b (lem:bottleneck: embedding Jordan-product-preserving), but whether E is ALSO coherent with the sequential product a&b = sqrt(a) b sqrt(a) is the decisive OPEN crux (the sequential product is a CFC-based, non-Jordan, non-associative triple product; non-associativity is exactly where leakage can enter). The obstruction-or-preservation verdict is computed in 62-02 and read off in 62-03; this plan does NOT pre-commit to preservation."
      deliverables: [deliv-embedding]
      acceptance_tests: [test-E-explicit, test-E-preserves-stated, test-crux-framed, test-fork-open]
      references: [ref-effros-stormer, ref-lem-bottleneck, ref-hanche-olsen, ref-paper5-def1]
  deliverables:
    - id: deliv-embedding
      kind: derivation
      path: derivations/p5-basin-restriction/
      description: "derivations/p5-basin-restriction/embedding-under-E.md (this plan creates §1-§3 of it; 62-02/62-03 extend it) — explicit setup of the Peirce/bottleneck conditional expectation E: h_3(O) -> h_3(C_u) (entrywise C_u = span{1,e_7} projection; positive, unital, idempotent; range = JB-subalgebra by Effros-Stormer); precise statement of what E preserves (Jordan product a o b YES, via lem:bottleneck Jordan-product-preserving embedding); explicit framing of the decisive crux — whether E is coherent with the SEQUENTIAL product a&b = sqrt(a) b sqrt(a) (NOT the Jordan product), on the genuinely non-associative ambient h_3(O); the obstruction-or-preservation fork stated and kept OPEN (no pre-commitment to preservation)."
      must_contain:
        - "E: h_3(O) -> h_3(C_u) constructed explicitly as the entrywise C_u-projection (proj_u applied to every entry of the 3x3 octonionic Hermitian matrix); positive, unital (E(I_3)=I_3), idempotent (E o E = E), E|_A = id"
        - "Effros-Stormer: range of a positive unital idempotent on a JB-algebra is a JB-subalgebra; quoted from lem:bottleneck proof (complexification.tex:437) — E's range A ~ M_3(C)^sa ~ h_3(C_u) is a Jordan subalgebra"
        - "what E preserves stated precisely: E IS coherent with the JORDAN product a o b (lem:bottleneck: the embedding A -> h_3(O) is Jordan-product-preserving; E is a Jordan conditional expectation)"
        - "THE CRUX stated explicitly: the self-modeling sequential product a&b = sqrt(a) b sqrt(a) is NOT the Jordan product — it is a CFC-based, temporally-asymmetric, NON-associative triple product; a Jordan conditional expectation controls a o b = (1/2)(ab+ba) but NOT automatically the non-Jordan triple product"
        - "the decisive Phase 62 question framed: for a,b in A, sqrt(a) computed by CFC in the AMBIENT h_3(O), is E(sqrt(a) b sqrt(a)) = sqrt(a) b sqrt(a) (preserved / lands in slice) or does E . (ambient SP) != (slice SP) . E (leakage / obstruction)? — to be COMPUTED in 62-02, NOT asserted here"
        - "obstruction-or-preservation fork stated and kept OPEN: (P) preservation -> RESTRICTION embedding lemma (62-03); (O) leakage -> precise obstruction + minimal extra input (62-03); no pre-commitment to preservation; a clean (O) is an acceptable outcome"
        - "Peirce decomposition of A = h_3(C_u) at rank-1 idempotent E_11: V_1 ~ R, V_{1/2} ~ C_u^2, V_0 ~ h_2(C_u) ~ M_2(C)^sa (lem:bottleneck (iii), complexification.tex:481-486) — the Peirce structure E restricts through"
  references:
    - id: ref-effros-stormer
      kind: paper
      locator: "Effros-Stormer 1979 (positive projections onto JB-subalgebras). Content quoted in the LIVE lem:bottleneck proof: ~/repos/blog/landing/papers/sm-from-self-modeling/sections/complexification.tex line 437: 'Effros-Stormer: the range of a positive unital idempotent on a JB-algebra is a JB-subalgebra. In finite dimensions over R this is just a Jordan subalgebra; by the hypothesis on E the embedding is moreover Jordan-product-preserving.' EXECUTOR HAS NO WEB — use this exact quoted content; do NOT attempt to fetch the paper."
      aliases: ["Effros-Stormer", "Effros-Stormer 1979", "EffrosStormer1979"]
      role: method
      why_it_matters: "Underwrites E: h_3(O) -> A as a positive unital idempotent whose range is a JB-subalgebra (= Jordan subalgebra in fin-dim). This is what makes E a legitimate Jordan conditional expectation. CRUCIALLY: it controls the JORDAN product, NOT the sequential product — the coherent-embedding step turns precisely on whether E ALSO preserves a&b = sqrt(a) b sqrt(a). Must surface where the plan asserts E's range/positivity/idempotency."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-lem-bottleneck
      kind: prior_artifact
      locator: "~/repos/blog/landing/papers/sm-from-self-modeling/sections/complexification.tex lem:bottleneck (line 409): E(h_3(O)) family = positive unital idempotents onto Jordan subalgebras ~ s.a. part of fin-dim complex C*-algebra; (i) range A ~ +_k M_{n_k}(C)^sa, sum n_k <= 3 [Effros-Stormer, line 437]; (ii) maximal range A ~ M_3(C)^sa ~ h_3(C_u), u in S^6, single F_4-orbit; (iii) for rank-1 idempotent E_11 in h_3(C_u): V_1 ~ R, V_{1/2} ~ C_u^2, V_0 ~ h_2(C_u) ~ M_2(C)^sa (lines 481-486). EMBEDDING Jordan-product-preserving (lines 412-413, 440-441). rem:converse is prompt-inline, NOT labeled here (FUTR-01)."
      aliases: ["lem:bottleneck", "Paper 7 bottleneck lemma", "C*-bottleneck universality"]
      role: method
      why_it_matters: "Supplies E (the bottleneck conditional expectation), the slice A = h_3(C_u), and the Peirce decomposition E restricts through. lem:bottleneck builds E for the JORDAN product (positive, unital, idempotent; Jordan-product-preserving embedding) — it says NOTHING about the sequential product. This GAP is the entire Phase 62 crux. Quote the LIVE text directly (executor has Read; live .tex is readable)."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-hanche-olsen
      kind: paper
      locator: "Hanche-Olsen, universal tensor product of Jordan algebras (special vs exceptional). Content as used in Phase 60 (derivations/p5-basin-restriction/two-composites.md, rem-converse-bgw.md): a composite is INDUCED (canonically built from the factors) vs IMPORTED (extra external structure). BGW Prop 3.10 = Hanche-Olsen Thm 5.5 (only complex systems have locally tomographic composites with a qubit). EXECUTOR HAS NO WEB — use this stated content + the Phase 60 derivation files (readable)."
      aliases: ["Hanche-Olsen", "Hanche-Olsen Thm 5.5"]
      role: benchmark
      why_it_matters: "Frames the induced-vs-imported distinction that the obstruction-or-preservation fork turns on: if clause (iii)'s sequential-product datum on A can be INDUCED by E from h_3(O) -> (P); if it requires IMPORTING structure not in the ambient -> (O) obstruction. Surfaces where the plan states the fork and the induced-vs-imported framing."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-paper5-def1
      kind: spec
      locator: "~/repos/blog/landing/papers/qm-from-self-modeling/main.tex def:self-modeling-system (line 342); clause (iii) sms:minimal (lines 351-353, verbatim): 'V_BM is the minimal composite OUS carrying product states, product effects, non-signaling constraints, and product-form sequential product.' The 'product-form sequential product' is the datum whose coherence under E is at stake. LIVE paper only."
      aliases: ["Paper 5 Def 1", "def:self-modeling-system", "clause (iii)", "sms:minimal"]
      role: definition
      why_it_matters: "Clause (iii)'s fourth datum is the PRODUCT-FORM SEQUENTIAL PRODUCT. Phase 61 verified it on the ASSOCIATIVE composite (intrinsically). Phase 62 asks whether E induces it from the NON-ASSOCIATIVE ambient. Clause (iii) must stay verbatim (fp-redefine-iii guarded at source in claim.md)."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
  acceptance_tests:
    - id: test-E-explicit
      subject: claim-restriction
      kind: existence
      procedure: "Confirm embedding-under-E.md constructs E: h_3(O) -> h_3(C_u) EXPLICITLY: (a) the formula — entrywise application of proj_u (octonion projection onto C_u = span{1, u=e_7}) to all 9 entries of the 3x3 octonionic Hermitian matrix, equivalently E acts as identity on the (alpha,beta,gamma) real diagonal and projects each off-diagonal octonion x1,x2,x3 onto C_u; (b) the four conditional-expectation properties named with their justification: positive (entrywise C_u-projection is positive), unital (E(I_3)=I_3 since I_3 has C_u entries), idempotent (proj_u o proj_u = proj_u entrywise => E o E = E), E|_A = id (slice entries already in C_u); (c) range = JB-subalgebra via Effros-Stormer (quoted, complexification.tex:437)."
      pass_condition: "E constructed with explicit entrywise-proj_u formula; positive + unital + idempotent + E|_A=id each stated with justification; Effros-Stormer range=JB-subalgebra quoted from the LIVE lem:bottleneck proof; dim E-range = 9 = dim A confirmed."
      evidence_required: [deliv-embedding, ref-effros-stormer, ref-lem-bottleneck]
      automation: human
    - id: test-E-preserves-stated
      subject: claim-restriction
      kind: consistency
      procedure: "Confirm the deliverable states PRECISELY what E preserves: E IS coherent with the JORDAN product a o b = (1/2)(ab+ba) — because lem:bottleneck makes the embedding A -> h_3(O) Jordan-product-preserving and E is the corresponding Jordan conditional expectation (consistency check: E restricted to A is the identity, so E recovers lem:bottleneck on the slice). Confirm this is asserted ONLY for the Jordan product, and that the sequential product is explicitly flagged as a SEPARATE, unresolved question (not folded into 'E preserves everything')."
      pass_condition: "E-preserves-Jordan-product stated with lem:bottleneck justification; E|_A = id consistency (recovers bottleneck on slice) noted; the sequential product is NOT claimed preserved here (the 'E preserves everything' hand-wave is explicitly rejected — fp-assert-preservation)."
      evidence_required: [deliv-embedding, ref-lem-bottleneck, ref-effros-stormer]
      automation: human
    - id: test-crux-framed
      subject: claim-restriction
      kind: consistency
      procedure: "Confirm THE CRUX is framed explicitly and correctly: (a) the sequential product a&b = sqrt(a) b sqrt(a) is a CFC-based, temporally-asymmetric, NON-Jordan triple product, distinct from a o b; (b) a Jordan conditional expectation controls a o b but NOT automatically the triple product; (c) non-associativity ((xy)z != x(yz) in h_3(O)) is exactly where leakage can enter; (d) the decisive computable question is stated: for a,b in A, with sqrt(a) computed by CFC in the AMBIENT h_3(O), does E(sqrt(a) b sqrt(a)) = sqrt(a) b sqrt(a) or not; (e) this is explicitly deferred to 62-02 for exact computation, NOT asserted here."
      pass_condition: "Crux stated with the Jordan-vs-sequential distinction, the non-associativity leakage channel, and the exact computable test; explicitly marked as COMPUTED IN 62-02 (not resolved in this plan)."
      evidence_required: [deliv-embedding, ref-paper5-def1, ref-lem-bottleneck]
      automation: human
    - id: test-fork-open
      subject: claim-restriction
      kind: consistency
      procedure: "Confirm the obstruction-or-preservation fork is stated and kept OPEN: (P) if E preserves the sequential product -> a RESTRICTION embedding lemma (62-03); (O) if E leaks -> a precise obstruction naming the exact clause-(iii) structure E cannot induce + the minimal extra input (62-03). Confirm NO pre-commitment to preservation anywhere in the deliverable, and that a clean (O) is stated as an acceptable outcome (per claim.md PAUSE condition 2)."
      pass_condition: "Both fork branches (P) and (O) stated; no pre-commitment to (P); (O) flagged as acceptable; the v11.0/Phase 42 leakage precedent (sqrt(T_a) T_b sqrt(T_a) exits M_16(R)) is noted as raising the prior probability of (O)."
      evidence_required: [deliv-embedding]
      automation: human
  forbidden_proxies:
    - id: fp-assert-preservation
      subject: claim-restriction
      proxy: "Asserting (or implying) that E preserves the sequential product / 'E preserves everything' / 'E is a conditional expectation so it preserves clause (iii)' WITHOUT the explicit Jordan-vs-sequential-product distinction and WITHOUT deferring the decisive triple-product computation to the actual non-associative h_3(O) (62-02). Equivalently: treating lem:bottleneck's Jordan-product-preserving property as if it automatically covered the non-Jordan triple product sqrt(a) b sqrt(a)."
      reason: "This is the roadmap's central Phase 62 reward-hacking guard (fp-assert-preservation / assert-Peirce-preserves-iii in claim.md). lem:bottleneck builds E for the JORDAN product only; the sequential product is a different, non-associative triple product, and non-associativity is exactly where leakage can enter. The setup plan must state E-preserves-Jordan PRECISELY and frame the sequential-product question as OPEN — never conflate the two."
    - id: fp-ignore-nonassociativity
      subject: claim-restriction
      proxy: "Framing the sequential-product-under-E question as if h_3(O) were associative — e.g. silently treating sqrt(a) b sqrt(a) as if it lived in the associative slice, or asserting (sqrt(a) b) sqrt(a) = sqrt(a) (b sqrt(a)) for ambient octonionic-matrix products without flagging that this is exactly what must be tested."
      reason: "The NON-ASSOCIATIVITY guard (roadmap, flagged): the success of Phase 62 REQUIRES the sequential product to be analyzed in the genuinely non-associative ambient h_3(O) where (xy)z != x(yz). A framing that assumes associativity is forbidden false progress. The setup must explicitly identify non-associativity as the leakage channel."
    - id: fp-force-positive
      subject: claim-restriction
      proxy: "Pre-committing the setup toward preservation — wording the crux so that (P) is the 'expected' answer, downplaying the v11.0 leakage precedent, or omitting the (O) branch / the acceptability of a clean obstruction."
      reason: "A clean obstruction is a fully acceptable milestone outcome (claim.md, ROADMAP). The setup must keep the fork genuinely open; the v11.0/Phase 42 precedent (sqrt(T_a) T_b sqrt(T_a) exits M_16(R)) makes (O) a live, arguably favored possibility."
  links:
    - id: link-embedding-setup
      source: claim-restriction
      target: deliv-embedding
      relation: supports
      verified_by: [test-E-explicit, test-E-preserves-stated, test-crux-framed, test-fork-open]
    - id: link-effros-underwrites-E
      source: ref-effros-stormer
      target: deliv-embedding
      relation: supports
      verified_by: [test-E-explicit, test-E-preserves-stated]
    - id: link-bottleneck-supplies-E
      source: ref-lem-bottleneck
      target: deliv-embedding
      relation: supports
      verified_by: [test-E-explicit, test-crux-framed]
  uncertainty_markers:
    weakest_anchors:
      - "ref-effros-stormer and ref-lem-bottleneck establish E as a JORDAN conditional expectation (positive, unital, idempotent, Jordan-product-preserving). NEITHER says anything about the sequential product sqrt(a) b sqrt(a). The entire weight of Phase 62 rests on a property (sequential-product coherence) that the supplying lemmas do NOT provide — this is the crux, not a gap to paper over."
      - "rem:converse remains prompt-inline / not-in-live-paper (FUTR-01); it is NOT used as a premise in this plan (the setup uses lem:bottleneck + Effros-Stormer directly). E's construction as entrywise C_u-projection is the standard realization but its conditional-expectation properties (positivity in the Jordan/order sense, idempotency on the full 27-dim algebra) are stated here and COMPUTED in 62-02."
    disconfirming_observations:
      - "If E (entrywise C_u-projection) FAILS to be a positive unital idempotent onto a Jordan subalgebra of h_3(O) — e.g. if proj_u entrywise does not yield a Jordan subalgebra, or E o E != E on the full 27-dim algebra — then the setup itself is wrong and lem:bottleneck's E must be reconstrued. (62-02 verifies idempotency/unitality exactly.)"
      - "THE decisive disconfirming observation (deferred to 62-02, flagged here): an EXACT nonzero residual E(sqrt(a) b sqrt(a)) - sqrt(a) b sqrt(a) != 0 for a,b in the slice (sqrt computed in the ambient non-associative h_3(O)) => the sequential product is NOT preserved by E => obstruction (O). The v11.0/Phase 42 result that sqrt(T_a) T_b sqrt(T_a) EXITS M_16(R) is the precedent making this the live, arguably favored possibility — the fork must NOT be pre-decided toward (P)."
---

<objective>
Set up the Peirce/bottleneck conditional expectation E: h_3(O) -> h_3(C_u) explicitly (Effros-Stormer: positive, unital, idempotent, range = JB-subalgebra), state PRECISELY what E preserves, and FRAME the decisive sequential-product-under-E crux — without asserting its answer.

Purpose: Step 3 of the v15.0 four-step attack (Phase 62), the load-bearing, entirely-unproved step. Phase 61 proved the slice A = M_3(C)^sa satisfies all four Def 1 clauses INTRINSICALLY (in its own right), with the induced-by-E question explicitly deferred here. This plan builds the analytic SETUP: the explicit conditional expectation E, exactly what it preserves (the Jordan product a o b — and crucially NOT, automatically, the sequential product), and the sharp statement of the crux that 62-02 will COMPUTE on the actual non-associative h_3(O). The setup feeds the decisive computation (62-02) and the verdict (62-03).

Output: derivations/p5-basin-restriction/embedding-under-E.md (sections §1-§3: E setup, what E preserves, the sequential-product crux + fork; 62-02 appends §4 the computation, 62-03 appends §5 the verdict). This plan creates the file and writes §1-§3.

CRITICAL SCOPE GUARDS (carry into the deliverable):
- E is built for the JORDAN product (lem:bottleneck: Jordan-product-preserving embedding; Effros-Stormer range = JB-subalgebra). State E-preserves-Jordan PRECISELY. Do NOT assert E preserves the SEQUENTIAL product — that is the open crux (fp-assert-preservation).
- The sequential product a&b = sqrt(a) b sqrt(a) is a CFC-based, NON-Jordan, NON-associative triple product. Frame the crux on the GENUINELY non-associative ambient h_3(O), where (xy)z != x(yz). Do NOT assume associativity (fp-ignore-nonassociativity).
- Keep the obstruction-or-preservation fork OPEN. Do NOT pre-commit to preservation. The v11.0/Phase 42 precedent (sqrt(T_a) T_b sqrt(T_a) exits M_16(R)) makes obstruction (O) a live, arguably favored possibility. A clean (O) is an acceptable milestone outcome (fp-force-positive).
- LIVE sources only (~/repos/blog/landing/papers/). The executor has Read access to the live .tex and the repo code/ + derivations/ — point to exact files/lines. The executor has NO web/arxiv: Effros-Stormer and Hanche-Olsen content is quoted in this plan and in the LIVE lem:bottleneck proof (complexification.tex:437) — use the quoted content.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@derivations/p5-basin-restriction/claim.md
@derivations/p5-basin-restriction/STATE.md
@derivations/p5-basin-restriction/slice-clause-iii.md
@derivations/p5-basin-restriction/rem-converse-bgw.md
@code/octonion_algebra.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Construct E: h_3(O) -> h_3(C_u) explicitly (positive unital idempotent, Effros-Stormer range = JB-subalgebra); state what E preserves (the Jordan product)</name>
  <files>derivations/p5-basin-restriction/embedding-under-E.md</files>
  <action>
    Create derivations/p5-basin-restriction/embedding-under-E.md — the Phase 62 deliverable. This task writes the header, §1 (E setup), and §2 (what E preserves). 62-02 will append §4 (the decisive computation) and 62-03 §5 (the verdict); leave a clear "§4/§5 to follow (62-02 / 62-03)" marker.

    HEADER: put at the top an ASSERT_CONVENTION comment matching the milestone artifacts:
    "<!-- ASSERT_CONVENTION: metric_signature=riemannian_fisher, fourier_convention=na, natural_units=natural, gauge_choice=na, renormalization_scheme=na -->"
    plus: "<!-- Pure-algebra: Jordan product a o b = (1/2)(ab+ba); sequential product a&b = sqrt(a) b sqrt(a) (CFC, principal branch). Type/category + Peirce-grade consistency is the dimensional-analysis analog. -->"
    "<!-- Provenance: LIVE papers only (~/repos/blog/landing/papers/). NOT the stale repo papers/. -->"
    "<!-- Slice: A = h_3(C_u) ~ M_3(C)^sa (n=3; u = e_7; any u in S^6 equivalent under G_2). -->"
    Title: "# embedding-under-E.md — Coherent embedding under the bottleneck conditional expectation E (the hard part)"
    Plan/Step line: Plan 62-01 (Phase 62, milestone v15.0) — DERV-62-01. Step 3 of 4.
    Scope line: this file's §1-§3 are the SETUP + CRUX FRAMING (62-01); §4 is the decisive computation (62-02, VALD-62-01); §5 is the verdict on the fork (62-03). The induced-by-E verdict is NOT reached in this plan.

    First READ the inputs: claim.md (RESTRICTION, allowed inputs, prohibited moves, PAUSE conditions — especially fp-assert-preservation / assert-Peirce-preserves-iii and PAUSE condition 2), the derivation-tree STATE.md (Step 2 COMPLETE, Step 3 = THIS, the [62] open question), slice-clause-iii.md §6 (the explicit Phase 62 deferral and what was/was not claimed intrinsically), and code/octonion_algebra.py (the H3O class, _mat_mul_h3o non-associative matrix product, jordan_product, peirce_V1/Vhalf/V0, proj_u, pi_u). Re-read the LIVE lem:bottleneck: ~/repos/blog/landing/papers/sm-from-self-modeling/sections/complexification.tex lines 409-487 (statement, Effros-Stormer at 437, Peirce decomposition (iii) at 481-486).

    §0 — SETUP, INPUTS, TYPE/CATEGORY + PEIRCE-GRADE LEDGER:
    - Recall the slice A = h_3(C_u) ~ M_3(C)^sa (lem:bottleneck (ii)), C_u = R + R*u, u = e_7. Real dims: h_3(O) = 27 (3 real diagonal + 3 octonionic off-diagonal), A = 9 (3 real diagonal + 3 C_u off-diagonal).
    - Type/category ledger (carry the style from slice-clause-iii.md §0.3): tag E (conditional expectation), h_3(O) (exceptional FRJA, non-special, BGW-non-composable), A (special simple FRJA), the Jordan product (Jordan-algebra binary op), the sequential product (CFC triple product on effects — NOT a Jordan-algebra op). The Peirce-grade analog of dimensional analysis: every object carries a Peirce grade under E_11 ({0,1/2,1}) and a real-dimension; track them.

    §1 — EXPLICIT CONSTRUCTION OF E:
    - DEFINE E: h_3(O) -> h_3(C_u) entrywise. For X in h_3(O) with H3O coordinates (alpha,beta,gamma in R; x1,x2,x3 octonions), set E(X) = h_3(O) element with the SAME real diagonal (alpha,beta,gamma) and each off-diagonal octonion projected onto C_u: x_i -> proj_u(x_i) = (component along 1) + (component along u=e_7) [i.e. keep octonion components 0 and 7, zero components 1..6]. This is exactly code/octonion_algebra.py proj_u applied to x1,x2,x3 (note proj_u there is for u=e_7; cite it). Write the explicit 3x3-matrix form: E projects every matrix entry onto the associative subfield C_u entrywise.
    - State the FOUR conditional-expectation properties WITH justification (these are verified exactly in 62-02; here state + justify):
        (a) UNITAL: E(I_3) = I_3 (the identity has real diagonal (1,1,1) and zero off-diagonal, all already in C_u). 
        (b) IDEMPOTENT: E o E = E, because proj_u o proj_u = proj_u entrywise (projecting onto C_u twice = once); diagonal untouched.
        (c) E|_A = id: for X in A = h_3(C_u), every entry is already in C_u, so E(X) = X. (Consistency / limiting check: E restricted to the slice is the identity — this is the "E recovers lem:bottleneck on the slice" check.)
        (d) POSITIVE: E is positive (maps the positive cone of h_3(O) into the positive cone of A). Justify via Effros-Stormer (the range of a positive unital idempotent is a JB-subalgebra) AND note the entrywise-C_u-projection realizes the standard conditional expectation onto h_3(C_u); positivity to be confirmed exactly in 62-02 on test elements (eigenvalue check).
    - EFFROS-STORMER: quote the LIVE lem:bottleneck proof verbatim (complexification.tex:437): "Effros-Stormer: the range of a positive unital idempotent on a JB-algebra is a JB-subalgebra. In finite dimensions over R this is just a Jordan subalgebra; by the hypothesis on E the embedding is moreover Jordan-product-preserving." Conclude: E's range A = h_3(C_u) ~ M_3(C)^sa is a JB-subalgebra (= Jordan subalgebra in fin-dim), and the embedding A -> h_3(O) is Jordan-product-preserving. dim(range E) = dim A = 9; ker E = 18-dim (the components x_i along e_1..e_6, three octonions x6 = 18 real dims).
    - Record the Peirce decomposition of A at the rank-1 idempotent E_11 (lem:bottleneck (iii), complexification.tex:481-486): within h_3(C_u), V_1(E_11) ~ R, V_{1/2}(E_11) ~ C_u^2, V_0(E_11) ~ h_2(C_u) ~ M_2(C)^sa; dims 1+4+4 = 9. This is the Peirce structure E restricts through; carry the prior input V_1 = R bottleneck (v6.0/v8.0: the Peirce interface to V_{1/2} is scalar).

    §2 — WHAT E PRESERVES (PRECISELY):
    - State PRECISELY: E IS coherent with the JORDAN product. Specifically, for the JORDAN product a o b = (1/2)(ab+ba), lem:bottleneck makes the embedding A -> h_3(O) Jordan-product-preserving and E the corresponding Jordan conditional expectation; on the slice E|_A = id recovers lem:bottleneck exactly (limiting/consistency check). [Whether E(x o y) relates to E(x) o E(y) for general x,y in h_3(O) is a Jordan-conditional-expectation property; state what is and is not claimed, and note 62-02 can spot-check E(a o b) = a o b for a,b in the slice as a Jordan-side control that should PASS — distinguishing it from the sequential-product test that may FAIL.]
    - State EXPLICITLY what E does NOT automatically preserve: the SEQUENTIAL product a&b = sqrt(a) b sqrt(a). Set up §3's crux: a Jordan conditional expectation controls a o b = (1/2)(ab+ba), but the sequential product is a CFC-based, temporally-asymmetric, NON-Jordan triple product — a different operation entirely. REJECT explicitly the hand-wave "E is a conditional expectation, so it preserves clause (iii)" (fp-assert-preservation): the four data of clause (iii) include the product-form sequential product, and E's Jordan-coherence does NOT transfer to it for free.

    Add a TYPE/CATEGORY + PEIRCE check at the end of each of §1, §2 (mirror slice-clause-iii.md's per-clause checks): confirm every step is internal to the stated categories; confirm E maps Peirce grades coherently for the Jordan product (E commutes with the Peirce projections of A, since E|_A = id and E kills the e_1..e_6 octonion components which are orthogonal to C_u).

    CONFIDENCE marker for §1-§2 (HIGH for the explicit construction + Effros-Stormer Jordan-side; the sequential-product coherence is explicitly UNRESOLVED and deferred to §3/§4).
  </action>
  <verify>
    1. embedding-under-E.md created with the ASSERT_CONVENTION header, provenance/slice notes, title, plan/step line, and the §1-§3 (this plan) / §4 (62-02) / §5 (62-03) scope marker.
    2. §1 constructs E EXPLICITLY as entrywise proj_u (C_u = span{1,e_7}) on the off-diagonal octonions x1,x2,x3, identity on the real diagonal (alpha,beta,gamma); the explicit 3x3-matrix form is written; code/octonion_algebra.py proj_u is cited.
    3. §1 states + justifies the four conditional-expectation properties: unital E(I_3)=I_3; idempotent E o E = E (proj_u idempotent); E|_A = id (slice entries in C_u); positive (Effros-Stormer + entrywise C_u-projection, exact confirmation deferred to 62-02). dim(range E)=9, ker E = 18-dim stated.
    4. §1 quotes Effros-Stormer VERBATIM from complexification.tex:437 and concludes range A = h_3(C_u) is a JB/Jordan subalgebra with Jordan-product-preserving embedding. The Peirce decomposition of A at E_11 (V_1~R, V_{1/2}~C_u^2, V_0~h_2(C_u)~M_2(C)^sa; 1+4+4=9) is recorded from complexification.tex:481-486.
    5. §2 states PRECISELY that E preserves the JORDAN product (lem:bottleneck Jordan-product-preserving; E|_A=id recovers bottleneck on the slice) and EXPLICITLY does NOT claim E preserves the sequential product; the "E is a conditional expectation so it preserves clause (iii)" hand-wave is explicitly rejected (fp-assert-preservation).
    6. Mathematical-rigor / structural checks present: idempotency E o E = E, unitality E(I_3)=I_3, E|_A = id, Jordan-subalgebra membership of the range, Peirce-grade bookkeeping (E commutes with A's Peirce projections on the Jordan side). Real-dimension bookkeeping (27 = 9 + 18) consistent.
    7. STRUCTURAL ESTIMATE present: the order-of-magnitude / structural expectation that the sequential-product residual could be NONZERO is at least flagged (full estimate is §3/Task 2), with E's kernel (the e_1..e_6 octonion components) named as where leakage would land.
  </verify>
  <done>derivations/p5-basin-restriction/embedding-under-E.md exists with the convention header and §0-§2: E: h_3(O) -> h_3(C_u) constructed explicitly as the entrywise C_u-projection (positive, unital, idempotent, E|_A=id; range = JB-subalgebra via Effros-Stormer quoted from complexification.tex:437; Jordan-product-preserving embedding; Peirce decomposition of A at E_11 recorded); §2 states PRECISELY that E preserves the Jordan product a o b and explicitly does NOT claim it preserves the sequential product (fp-assert-preservation rejected); type/category + Peirce-grade and real-dimension bookkeeping clean; §4/§5-to-follow marker present.</done>
</task>

<task type="auto">
  <name>Task 2: Frame the sequential-product-under-E crux (Jordan vs sequential; non-associativity leakage channel); structural leakage estimate; state and OPEN the obstruction-or-preservation fork; set up 62-02</name>
  <files>derivations/p5-basin-restriction/embedding-under-E.md</files>
  <action>
    Append §3 to derivations/p5-basin-restriction/embedding-under-E.md — the CRUX framing and the obstruction-or-preservation fork. This is the analytic heart of the setup; 62-02 turns §3's decisive question into an exact computation.

    §3 — THE CRUX: IS E COHERENT WITH THE SEQUENTIAL PRODUCT ON THE NON-ASSOCIATIVE AMBIENT?

    (3.1) The two products are different. State explicitly:
    - Jordan product: a o b = (1/2)(ab + ba) — a symmetric, Jordan-algebra binary operation. E preserves it (§2, lem:bottleneck).
    - Sequential product: a & b = sqrt(a) b sqrt(a) — a CFC-based (continuous functional calculus square root), temporally-asymmetric (a&b != b&a in general) TRIPLE product on effects. NOT a Jordan-algebra operation. This is clause (iii)'s fourth datum (ref-paper5-def1) and the self-modeling product (def:product-sp).
    - KEY POINT: a Jordan conditional expectation E controls a o b but NOT, automatically, the triple product sqrt(a) b sqrt(a). The two are related (the Jordan product is the symmetrized single product; the sequential product is a CFC triple product) but E's coherence with one does NOT entail coherence with the other.

    (3.2) Non-associativity is the leakage channel. State explicitly:
    - h_3(O) is genuinely NON-associative: for octonionic 3x3 matrices, (XY)Z != X(YZ) in general (Artin: any TWO octonions associate, but THREE generic ones need not; and the 3x3 matrix triple product mixes entries so non-associativity is active). [Note the subtlety the executor must confront: code/octonion_algebra.py _mat_mul_h3o computes each matrix ENTRY (AB)_ij = sum_k A_ik B_kj as a sum of single octonion products (two factors each, so Artin-safe per entry), BUT the TRIPLE matrix product (sqrt(a) b) sqrt(a) vs sqrt(a) (b sqrt(a)) re-associates across entries and is NOT guaranteed equal — this is exactly the non-associativity that must be tested, not assumed away.]
    - The decisive subtlety: even for a, b in the slice A = h_3(C_u) (entries in the associative subfield C_u), the AMBIENT square root sqrt(a) is computed by CFC in h_3(O) (27-dim, non-associative), NOT in the slice. If sqrt(a) has components OUTSIDE C_u (i.e. in the e_1..e_6 octonion directions = ker E), or if the ambient matrix triple product sqrt(a) b sqrt(a) develops components outside the slice via non-associative entry mixing, then E(sqrt(a) b sqrt(a)) != sqrt(a) b sqrt(a) and the sequential product LEAKS out of the slice under E.
    - REJECT fp-ignore-nonassociativity: do NOT assume sqrt(a) b sqrt(a) stays in the slice; do NOT assume (sqrt(a) b) sqrt(a) = sqrt(a) (b sqrt(a)) for ambient products. These are the propositions 62-02 must decide by exact computation.

    (3.3) The decisive computable question (the 62-02 spec). State it precisely:
    - INPUT: a, b in the slice A = h_3(C_u) (a a PSD effect, b Hermitian). 
    - COMPUTE sqrt(a) in the AMBIENT h_3(O) via CFC (principal branch), treating non-associativity explicitly. Then form the ambient sequential product SP_ambient(a,b) = sqrt(a) b sqrt(a) using the octonionic 3x3 matrix product (code _mat_mul_h3o style, but EXACT arithmetic).
    - TEST: does SP_ambient(a,b) land back in the slice A (equivalently E(SP_ambient(a,b)) = SP_ambient(a,b), i.e. zero residual in ker E)? 
        * EXACT residual = 0  =>  PRESERVATION (P): the sequential product is induced/consistent under E.
        * EXACT residual != 0  =>  LEAKAGE / OBSTRUCTION (O): the sequential product is NOT preserved; name where the leakage lands (which Peirce grade / which e_k directions).
    - INDEPENDENT CROSS-CHECK (mandate for 62-02): in addition to the direct E(sqrt(a) b sqrt(a)) computation, require a SECOND, independent route to the same verdict — a Peirce/grade-component argument: decompose sqrt(a) and the triple product into Peirce grades / C_u-vs-(e_1..e_6) components and track whether non-associative cross-terms populate ker E. The two routes must agree.

    (3.4) EXACT-ARITHMETIC requirement (methodological, critical — carry verbatim into 62-02). State:
    - The leakage/obstruction test MUST distinguish genuine non-associativity leakage from numerical round-off. Use EXACT arithmetic (SymPy symbolic / exact rationals/surds), NOT float64. A nonzero "leakage" at float64 tolerance is NOT evidence of an obstruction; an EXACT nonzero residual IS.
    - CFC square roots of generic slice elements may be irrational. Handle via symbolic sqrt OR by choosing slice elements a that are EXACT squares (a = c^2 with c in the slice, exact entries) so sqrt(a) is exact — WHILE keeping b generic in the slice so the ambient triple product still exercises non-associativity. Document this choice and confirm it does NOT trivialize non-associativity (b and the ambient product must still exercise (xy)z != x(yz); i.e. the test elements must NOT all lie in a common associative subalgebra in a way that makes the triple product associative by accident).
    - ERROR BUDGET: zero-tolerance decisive test. exact residual == 0 (SymPy .equals(zeros)) => preservation; exact residual symbolically nonzero => leakage. NO float tolerance on the decisive assertion.

    (3.5) STRUCTURAL / ORDER-OF-MAGNITUDE ESTIMATE (before computing — required). State the prior:
    - Carry the v11.0 / Phase 42 PRECEDENT (allowed input per claim.md item 7): for anticommuting Cl(9,0) pairs, sqrt(T_a) T_b sqrt(T_a) = (i/2) T_b EXITS M_16(R) (the sequential product left the real observable algebra — verify_sequential_product.py). This is a direct precedent that the sequential product is the operation that breaks the smaller algebra. It RAISES the prior probability of obstruction (O) for the h_3(O) -> h_3(C_u) restriction.
    - Carry the v6.0/v8.0 PRECEDENT: V_1 = R bottleneck — the Peirce interface to V_{1/2} is scalar; no C*-subalgebra inside h_3(O) (the observer is necessarily external). Also bears toward (O).
    - Carry the COUNTER-precedent (Phase 46, allowed input): intrinsic h_2(O) Jordan product closes in V_0 with ZERO V_{1/2} leakage — but note this is the JORDAN product on h_2(O), NOT the sequential product on h_3(C_u); it does NOT directly predict (P) for the crux.
    - HONEST STRUCTURAL EXPECTATION: state that the precedents lean toward (O) (the sequential product is historically the leakage-prone operation), but that the verdict is genuinely open and MUST be decided by the exact computation (62-02), not by the prior. Do NOT pre-commit (fp-force-positive).

    (3.6) THE OBSTRUCTION-OR-PRESERVATION FORK (stated, kept OPEN):
    - (P) PRESERVATION: if 62-02 finds exact residual = 0, then E induces the sequential product coherently => 62-03 states and proves the RESTRICTION embedding lemma (touching the actual non-associative structure).
    - (O) OBSTRUCTION: if 62-02 finds exact residual != 0, then 62-03 characterizes the obstruction precisely — naming the exact clause-(iii) structure (the product-form sequential product) that E cannot induce from the non-associative ambient, plus the minimal extra input that would be required to close it — and the milestone PAUSES per claim.md PAUSE condition 2. A clean (O) is a fully acceptable DELIVERABLE, NOT a failure.
    - State that this plan does NOT decide the fork; 62-02 provides the decisive evidence and 62-03 reads the verdict. Wire the handoff explicitly: 62-02's exact residual is the decisive input to 62-03's verdict.

    Add a final TYPE/CATEGORY + PEIRCE self-audit for §3: confirm the sequential product is tagged as a CFC triple product (NOT a Jordan op); confirm the leakage channel is the non-associative ambient matrix product + ker E (e_1..e_6 components); confirm the fork is open and (O) is acceptable.

    CONFIDENCE marker for §3: state that the CRUX is correctly framed and the decisive question is well-posed for exact computation; the VERDICT is explicitly UNRESOLVED pending 62-02.
  </action>
  <verify>
    1. §3 appended; (3.1) distinguishes the Jordan product a o b (E-preserved) from the sequential product a&b = sqrt(a) b sqrt(a) (CFC triple product, temporally asymmetric, NON-Jordan), and states E's Jordan-coherence does NOT entail sequential-product coherence.
    2. (3.2) identifies non-associativity as the leakage channel: h_3(O) non-associative ((XY)Z != X(YZ)); the subtlety that _mat_mul_h3o is Artin-safe per ENTRY but the TRIPLE matrix product re-associates across entries; the key point that ambient sqrt(a) may have components outside C_u / the triple product may leave the slice. fp-ignore-nonassociativity explicitly rejected.
    3. (3.3) states the decisive computable question precisely (input a,b in slice; sqrt(a) in ambient via CFC; test SP_ambient lands in slice <=> E(SP)=SP <=> zero residual in ker E), with the exact pass/fail (residual=0 => P; residual!=0 => O) AND mandates an independent Peirce/grade-component cross-check route for 62-02.
    4. (3.4) carries the EXACT-ARITHMETIC requirement verbatim: SymPy exact (not float64); the exact-square trick (a = c^2, c in slice, exact entries) WITH b generic so non-associativity is still exercised; the guard that the choice must NOT trivialize non-associativity; zero-tolerance error budget (exact residual ==0 / !=0).
    5. (3.5) gives the STRUCTURAL / ORDER-OF-MAGNITUDE estimate BEFORE computing: carries the v11.0/Phase 42 leakage precedent (sqrt(T_a) T_b sqrt(T_a) exits M_16(R)), the v6.0/v8.0 V_1=R bottleneck, and the Phase 46 counter-precedent (with the caveat that it is the Jordan product, not the SP); states the honest expectation leans toward (O) but the verdict is open and decided by computation, not prior (fp-force-positive rejected).
    6. (3.6) states BOTH fork branches: (P) -> RESTRICTION embedding lemma (62-03); (O) -> precise obstruction + minimal extra input + PAUSE condition 2 (62-03), with (O) flagged as an acceptable deliverable. The fork is kept OPEN; this plan does not decide it; the 62-02 -> 62-03 handoff is wired explicitly.
    7. Type/category + Peirce self-audit for §3 present; sequential product tagged as CFC triple product; leakage channel = non-associative ambient product + ker E; fork open, (O) acceptable. CONFIDENCE marker present (crux well-posed; verdict UNRESOLVED pending 62-02).
  </verify>
  <done>§3 of derivations/p5-basin-restriction/embedding-under-E.md frames the decisive crux: the Jordan-vs-sequential-product distinction (E preserves the former, not automatically the latter), non-associativity as the leakage channel (with the per-entry-Artin-safe vs across-entry-re-association subtlety and the ambient-sqrt-leaves-C_u possibility), the precise computable question for 62-02 (exact residual of E(sqrt(a) b sqrt(a)) - sqrt(a) b sqrt(a) for slice a,b, sqrt in the ambient) with an independent Peirce-grade cross-check, the verbatim exact-arithmetic requirement (SymPy, exact-square trick keeping b generic, zero-tolerance budget), the structural pre-computation estimate (precedents lean toward obstruction (O) but verdict open — fp-force-positive rejected), and the obstruction-or-preservation fork stated and kept OPEN with the 62-02 -> 62-03 handoff wired. fp-assert-preservation and fp-ignore-nonassociativity rejected; no pre-commitment to preservation.</done>
</task>

</tasks>

<verification>
- E EXPLICIT: E: h_3(O) -> h_3(C_u) constructed as entrywise proj_u (C_u = span{1,e_7}); positive, unital (E(I_3)=I_3), idempotent (E o E = E, proj_u idempotent), E|_A = id; range = JB/Jordan subalgebra via Effros-Stormer (quoted complexification.tex:437); Jordan-product-preserving embedding; dim(range)=9, ker E=18-dim; Peirce decomposition of A at E_11 recorded (complexification.tex:481-486). (test-E-explicit)
- WHAT E PRESERVES (precise): E preserves the JORDAN product a o b (lem:bottleneck; E|_A=id recovers bottleneck on slice — limiting/consistency check). E does NOT automatically preserve the SEQUENTIAL product; "E is a conditional expectation so it preserves clause (iii)" rejected (fp-assert-preservation). (test-E-preserves-stated)
- CRUX FRAMED: Jordan vs sequential product distinction; non-associativity ((XY)Z != X(YZ)) as the leakage channel; the per-entry-Artin-safe vs across-entry-re-association subtlety; the ambient-sqrt-may-leave-C_u possibility; the precise computable question for 62-02 (exact residual; independent Peirce cross-check); exact-arithmetic requirement verbatim; structural estimate before computing (precedents lean toward O). fp-ignore-nonassociativity rejected. (test-crux-framed)
- FORK OPEN: (P) RESTRICTION lemma / (O) precise obstruction + minimal extra input + PAUSE-2; (O) acceptable; no pre-commitment; v11.0 leakage precedent noted; 62-02 -> 62-03 handoff wired. (test-fork-open)
- MATHEMATICAL RIGOR: idempotency, unitality, E|_A=id, Jordan-subalgebra membership, Peirce-grade bookkeeping, real-dimension bookkeeping (27 = 9 + 18) — all present and consistent.
- ANCHOR DISCIPLINE: ref-effros-stormer (E's range/positivity), ref-lem-bottleneck (E + slice + Peirce), ref-hanche-olsen (induced-vs-imported fork framing), ref-paper5-def1 (clause iii's sequential-product datum) surfaced where the plan depends on them; all LIVE/quoted (executor has no web).
- HONEST FRAMING: the fork is genuinely open; the setup does not pre-decide (P); the sequential-product coherence is explicitly the unresolved crux, not papered over (fp-force-positive rejected).
</verification>

<success_criteria>
The Peirce/bottleneck conditional expectation E: h_3(O) -> h_3(C_u) is set up EXPLICITLY (Effros-Stormer: positive, unital, idempotent, range = JB-subalgebra; entrywise C_u = span{1,e_7} projection; E|_A = id; Jordan-product-preserving embedding; Peirce decomposition of A at E_11 recorded). What E preserves is stated PRECISELY: E IS coherent with the Jordan product a o b (lem:bottleneck; E|_A=id recovers the bottleneck on the slice), but whether E is ALSO coherent with the SEQUENTIAL product a&b = sqrt(a) b sqrt(a) is the decisive OPEN crux — the sequential product being a CFC-based, non-Jordan, non-associative triple product, with non-associativity ((XY)Z != X(YZ) in h_3(O)) the channel through which leakage can enter. The decisive computable question is well-posed for 62-02 (exact residual of E(sqrt(a) b sqrt(a)) - sqrt(a) b sqrt(a) for slice a,b with sqrt computed in the ambient non-associative h_3(O); independent Peirce-grade cross-check; exact arithmetic, exact-square trick keeping b generic, zero-tolerance error budget). A structural pre-computation estimate is given (the v11.0/Phase 42 precedent that sqrt(T_a) T_b sqrt(T_a) exits M_16(R), and the v6.0/v8.0 V_1=R bottleneck, lean toward obstruction (O); the verdict is genuinely open and decided by computation). The obstruction-or-preservation fork is stated and kept OPEN — (P) -> RESTRICTION embedding lemma (62-03), (O) -> precise obstruction + minimal extra input + PAUSE condition 2 (62-03), (O) acceptable — with the 62-02 -> 62-03 handoff wired. The deliverable rejects fp-assert-preservation (E's Jordan-coherence is NOT extended to the sequential product), fp-ignore-nonassociativity (the crux is framed on the genuinely non-associative ambient), and fp-force-positive (no pre-commitment to preservation; a clean obstruction is acceptable). No step asserts the induced-by-E verdict; that is 62-02 (evidence) and 62-03 (verdict).
</success_criteria>

<output>
After completion, create `.gpd/phases/62-coherent-embedding-under-e-the-hard-part/62-01-SUMMARY.md`.
</output>
