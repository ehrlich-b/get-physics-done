---
phase: 62-coherent-embedding-under-e-the-hard-part
plan: 02
type: tdd
wave: 2
depends_on: ["62-01"]
files_modified:
  - code/embedding_under_E_verification.py
  - tests/test_embedding_under_E.py
  - derivations/p5-basin-restriction/embedding-under-E.md
interactive: false

conventions:
  units: "natural (hbar=1, k_B=1); pure algebra — no physical dimensions; exact symbolic/rational/surd arithmetic (SymPy), NEVER float64 for the decisive test"
  metric: "riemannian_fisher (N/A this plan); Peirce-grade + C_u-vs-(e_1..e_6) component bookkeeping is the dimensional-analysis analog"
  algebra: "Jordan product a o b = (1/2)(ab+ba); sequential product a&b = sqrt(a) b sqrt(a) (CFC, principal branch); octonionic 3x3 Hermitian matrix product computed entrywise (non-associative across the triple product)"
  octonion: "Fano e_1 e_2 = e_4 (matches code/octonion_algebra.py infra); octonion = 8-component real vector"
  complex_structure: "u = e_7 by default (any u in S^6 equivalent under G_2); C_u = span{1, e_7} (keep octonion components 0 and 7)"
  slice: "A = h_3(C_u) ~ M_3(C)^sa; entries in C_u; ker E = components along e_1..e_6 (3 off-diagonal octonions x 6 = 18 real dims). A is a CLOSED, ASSOCIATIVE Jordan subalgebra (= range E)"
  conditional_expectation: "E: h_3(O) -> h_3(C_u), entrywise proj_u; positive, unital, idempotent (62-01 setup); verified exactly here. E is NOT a Jordan morphism on ambient elements"

dimensional_check:
  dim_h3O: "[real dim] = 27"
  dim_A: "[real dim] = 9"
  dim_ker_E: "[real dim] = 18 (the e_1..e_6 components of x1,x2,x3 = 3 x 6)"
  transport_defect_lands_in: "ker E (the e_1..e_6 octonion directions) — if E fails to transport, the defect R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) lives partly here; report which e_k / Peirce grade populate"
  decisive_residual: "[exact, AMBIENT TRANSPORT] R := E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) for GENERIC X,Y in h_3(O); R == 0 (exact) => coherent transport (P); R != 0 (exact) => ambient-transport obstruction (O, EXPECTED)"
  slice_internal_control: "[exact, TRIVIAL] for a,b in A: sqrt(a) b sqrt(a) stays in A; leakage 0, triple associator 0 (A is a closed associative subalgebra) — documented control, NOT the decisive test"

approximations:
  - name: "none (EXACT symbolic arithmetic; zero-tolerance decisive test)"
    parameter: "N/A — the entire methodological point is EXACT arithmetic (SymPy), so a genuine ambient-transport obstruction is distinguished from float round-off"
    validity: "always exact; the decisive ambient residual is tested with SymPy .equals(zeros) / simplify(...) == 0, NEVER a float tolerance"
    breaks_when: "float64 would FALSELY report nonzero 'leakage' from round-off; this plan FORBIDS float64 on the decisive assertion (fp-float-pass)"
    check: "ambient sqrt self-check sqrt(X)^2 == X EXACTLY; idempotency E o E = E EXACTLY; the associator of the relevant ambient triple is EXACTLY nonzero (non-associativity load-bearing); the decisive residual via exact symbolic equality only"

estimated_execution:
  total_minutes: 85
  breakdown:
    - task: 1
      minutes: 30
      note: "RED: write tests/test_embedding_under_E.py (assert-based, NO pytest — pytest is NOT in the executor venv) — failing checks pinning E's conditional-expectation properties (exact), the ambient sqrt (exact, non-associative), the non-associativity exerciser (associator nonzero), the slice-internal TRIVIAL control, and the decisive AMBIENT transport residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) (unfamiliar-formalism 1.5x + symbolic+numerical mixed multipliers applied)"
    - task: 2
      minutes: 40
      note: "GREEN: implement code/embedding_under_E_verification.py — exact-SymPy E (entrywise proj_u on octonionic 3x3 Hermitian), ambient octonionic matrix product (non-associative), ambient sqrt, the decisive AMBIENT transport residual + independent Peirce/grade cross-check + slice-internal control; make the assert-based harness pass; emit the decisive P/O verdict"
    - task: 3
      minutes: 15
      note: "Record §4 (the ambient-transport computation + decisive residual + verdict on the fork) in embedding-under-E.md; cite the code/tests; state P or O exactly as computed (O EXPECTED, refines RESTRICTION) — do NOT force"

contract:
  schema_version: 1
  scope:
    question: "On the ACTUAL non-associative h_3(O): does E TRANSPORT the self-modeling sequential product coherently from the ambient — i.e. does E(sqrt(X) Y sqrt(X)) = sqrt(EX)(EY)sqrt(EX) for GENERIC ambient X (PSD),Y in h_3(O) (sqrt(X) computed in the ambient; non-associativity genuinely engaged, associator-nonzero) — exact residual R = 0 (coherent transport P) or R != 0 (ambient-transport obstruction O, EXPECTED)? Decided by EXACT-arithmetic SymPy + an independent Peirce-grade cross-check, with the slice-internal case recorded as a TRIVIAL control — NOT asserted."
  claims:
    - id: claim-restriction
      statement: "(Decisive evidence half. CORRECTED — ambient E-transport, coexistence-as-island.) Whether E TRANSPORTS the self-modeling sequential product coherently from the ambient h_3(O) is settled by EXACT computation on the genuinely non-associative structure: build E onto h_3(C_u) (u=e_7) exactly; compute the AMBIENT transport residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) for GENERIC X (PSD),Y in h_3(O) with sqrt(X) in the ambient and the relevant associator verified nonzero (non-associativity load-bearing); decide R = 0 (coherent transport P) or R != 0 (ambient-transport obstruction O), cross-checked by an independent Peirce/grade-component route. The slice-internal sqrt(a) b sqrt(a) (a,b in A) is the documented TRIVIAL control (closed associative subalgebra; leakage 0; associator 0). The verdict (P or O) is whatever the exact computation yields — not forced; an obstruction is EXPECTED and REFINES RESTRICTION to coexistence-as-island (NOT independent posits)."
      deliverables: [deliv-embedding, deliv-vald-62-01]
      acceptance_tests: [test-E-properties-exact, test-ambient-sqrt-exact, test-nonassociativity-load-bearing, test-decisive-ambient-residual, test-slice-internal-control, test-peirce-crosscheck, test-exact-arithmetic, test-no-pytest, test-verdict-not-forced]
      references: [ref-effros-stormer, ref-lem-bottleneck, ref-paper5-def1, ref-vald-61-01, ref-v11-leakage]
  deliverables:
    - id: deliv-embedding
      kind: derivation
      path: derivations/p5-basin-restriction/
      description: "derivations/p5-basin-restriction/embedding-under-E.md §4 — the decisive AMBIENT-TRANSPORT computation result: the exact residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) for generic X,Y in h_3(O); whether R == 0 (coherent transport P) or R != 0 (ambient-transport obstruction O, with the defect's Peirce grade / e_k directions named); the slice-internal TRIVIAL control (leakage 0); the independent Peirce-grade cross-check agreeing; the verdict on the fork stated exactly as computed (O EXPECTED, refines RESTRICTION). Cites code/embedding_under_E_verification.py + tests/test_embedding_under_E.py."
      must_contain:
        - "§4 reports the EXACT decisive AMBIENT-TRANSPORT residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) for GENERIC X (PSD),Y in h_3(O) (sqrt(X) computed in the ambient), and states the verdict: R == 0 => COHERENT TRANSPORT (P); R != 0 => AMBIENT-TRANSPORT OBSTRUCTION (O, EXPECTED)"
        - "explicit confirmation non-associativity is LOAD-BEARING: the associator of the relevant ambient triple is EXACTLY nonzero on the chosen X,Y (the non-associativity exerciser passes); the test is NOT accidentally associative (NON-ASSOCIATIVITY guard honored)"
        - "the slice-internal case (a,b in A) recorded as a TRIVIAL control: sqrt(a) b sqrt(a) stays in A, leakage EXACTLY 0, associator EXACTLY 0 (A is a closed associative subalgebra = range E); clearly separated from the decisive ambient test, NOT passed off as the decisive result"
        - "if (O): the transport defect R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) is characterized — which Peirce grade (V_0/V_{1/2}/V_1) and which e_k it populates; if (P): explicit statement that E transports the sequential product coherently"
        - "the INDEPENDENT Peirce/grade-component cross-check route reaches the SAME verdict as the direct ambient-residual computation"
        - "EXACT-arithmetic confirmation: the decisive residual is tested by exact SymPy equality (.equals(zeros) / simplify == 0), NEVER a float tolerance; the exact-square trick (X = C^2, ambient C) is documented and confirmed NOT to trivialize non-associativity (X,Y generic ambient; associator nonzero)"
        - "the verdict is stated exactly as computed (P or O), NOT forced; if (O), it is flagged as the EXPECTED, ACCEPTABLE outcome that REFINES RESTRICTION to coexistence-as-island (handed to 62-03), NOT a program collapse / independent posits"
    - id: deliv-vald-62-01
      kind: code
      path: code/embedding_under_E_verification.py
      description: "code/embedding_under_E_verification.py + tests/test_embedding_under_E.py (VALD-62-01) — exact-symbolic SymPy, assert-based __main__ harness (NO pytest — pytest is NOT in the executor venv; runnable as `python tests/test_embedding_under_E.py`, mirroring code/slice_clause_iii_verification.py's _report/ALL_PASS/sys.exit pattern): (1) E onto h_3(C_u) (u=e_7) as entrywise proj_u on an octonionic 3x3 Hermitian, with exact checks E(I_3)=I_3, E o E = E, E|_A = id, positivity on test PSD elements; (2) ambient octonionic 3x3 matrix product (entrywise, non-associative across the triple product) porting code/octonion_algebra.py to EXACT SymPy octonion arithmetic; (3) ambient principal square root of a PSD element (exact via the exact-square trick X=C^2 and/or symbolic spectral sqrt); (4) THE DECISIVE test: the AMBIENT transport residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) for GENERIC X,Y, exact; (5) an independent Peirce/grade-component computation of the same verdict; (6) a non-associativity exerciser proving the associator of the relevant triple is nonzero; (7) the slice-internal TRIVIAL control (a,b in A: leakage 0). Exits nonzero on any self-check/consistency failure; prints the decisive P/O verdict (BOTH valid outcomes)."
      must_contain:
        - "exact-SymPy octonion multiplication (Fano e_1 e_2 = e_4) and 3x3 octonionic Hermitian matrix product (entrywise; the triple product NOT assumed associative — left and right associations computed independently)"
        - "E onto h_3(C_u): entrywise proj_u (keep octonion components 0 and 7, zero 1..6 on each off-diagonal; identity on real diagonal); exact checks E(I_3)=I_3, E o E = E, E|_A = id; AND a check that E is NOT a Jordan morphism on the ambient (E(X o X) != (EX) o (EX) for a generic ambient X)"
        - "ambient principal square root sqrt_ambient(X) for a PSD element X, computed in h_3(O) (27-dim), EXACT (X = C^2 exact-square trick documented, and/or exact symbolic spectral sqrt); self-check h3o_matmul(sqrt_X, sqrt_X) == X EXACT"
        - "THE DECISIVE AMBIENT-TRANSPORT residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) computed EXACTLY for GENERIC X (PSD),Y in h_3(O) (NOT confined to the slice); SymPy exact-zero test, NOT float; >=2 distinct (X,Y)"
        - "a non-associativity exerciser: the associator (X*Y)*Z - X*(Y*Z) is EXACTLY nonzero on the relevant ambient triple (load-bearing; the decisive test genuinely engages (xy)z != x(yz))"
        - "the slice-internal TRIVIAL control: for a,b in A, sqrt(a) b sqrt(a) stays in A with leakage EXACTLY 0 and triple associator EXACTLY 0 — computed and clearly labeled as the control (not the decisive test)"
        - "an independent Peirce/grade-component route to the verdict (decompose into C_u vs e_1..e_6 components / Peirce grades; track the transport defect into ker E); RAISE on a split decision"
        - "ASSERT-BASED __main__ harness (NO `import pytest`, NO pytest fixtures/decorators): _report/ALL_PASS pattern + sys.exit(0 if ALL_PASS else 1), runnable as `python tests/test_embedding_under_E.py`; tests/test_embedding_under_E.py is a thin runner importing + asserting the code's check functions"
        - "decisive verdict printed: R==0 => COHERENT TRANSPORT (GO toward RESTRICTION lemma); R!=0 => AMBIENT-TRANSPORT OBSTRUCTION (EXPECTED; characterize defect; refines RESTRICTION to coexistence-as-island; 62-03); script exits 0 if self-checks pass (verdict P or O are BOTH valid — the script reports which, it does NOT force P)"
  references:
    - id: ref-effros-stormer
      kind: paper
      locator: "Effros-Stormer 1979 — content quoted in LIVE complexification.tex:437 (see 62-01). EXECUTOR HAS NO WEB; use the quoted content. Here it underwrites that E (positive unital idempotent) has range = JB-subalgebra; the exact idempotency/unitality/positivity checks VERIFY E is such a map computationally."
      aliases: ["Effros-Stormer", "EffrosStormer1979"]
      role: method
      why_it_matters: "Underwrites E as a Jordan conditional expectation (Jordan-side, on the slice). The decisive point of this plan: E's Jordan-coherence on the slice does NOT settle whether E TRANSPORTS the sequential product from the ambient (E is not even a Jordan morphism on the ambient) — that is computed here on the non-associative ambient. The exact E-property checks confirm E is a legitimate conditional expectation before the transport test is run."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-lem-bottleneck
      kind: prior_artifact
      locator: "~/repos/blog/landing/papers/sm-from-self-modeling/sections/complexification.tex lem:bottleneck (line 409); slice A = h_3(C_u) (line 424-432); Peirce decomposition (iii) (lines 481-486): V_1~R, V_{1/2}~C_u^2, V_0~h_2(C_u). Readable by executor (live .tex)."
      aliases: ["lem:bottleneck", "C*-bottleneck universality"]
      role: method
      why_it_matters: "Supplies E, the slice, and the Peirce decomposition the independent cross-check route uses. The Peirce grades (V_0/V_{1/2}/V_1) are the coordinates in which the transport defect (if any) is characterized."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-paper5-def1
      kind: spec
      locator: "~/repos/blog/landing/papers/qm-from-self-modeling/main.tex def:self-modeling-system clause (iii) sms:minimal (lines 351-353): 'product-form sequential product' is the datum. LIVE paper. Verbatim text quoted in 62-01 / rem-converse-bgw.md §1,§3 / slice-clause-iii.md §0.2."
      aliases: ["Paper 5 Def 1", "clause (iii)", "sms:minimal"]
      role: definition
      why_it_matters: "The sequential product sqrt(a) b sqrt(a) is clause (iii)'s fourth datum. Phase 61 established it intrinsically on the (closed associative) slice (verbatim). This plan tests whether E TRANSPORTS it from the ambient — the corrected RESTRICTION question. Clause (iii) verbatim, never weakened (fp-redefine-iii); only RESTRICTION's embedding clause is weakened (62-03)."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-vald-61-01
      kind: prior_artifact
      locator: "code/slice_clause_iii_verification.py + tests/test_slice_clause_iii.py (VALD-61-01, Phase 61). Provides the EXACT-SymPy templates: matrix_sqrt_nxn (exact spectral PSD square root via eigenvects + Gram-Schmidt), luders_seq_product (sqrt(a) b sqrt(a)), hermitian_3x3 builder, AND the ASSERT-BASED _report/ALL_PASS/sys.exit __main__ harness pattern (runnable as `python ...`, NO pytest). REUSE the harness + sqrt patterns; the NEW work is the AMBIENT non-associative octonionic computation + the ambient TRANSPORT residual."
      aliases: ["VALD-61-01", "slice_clause_iii_verification.py"]
      role: method
      why_it_matters: "The Phase 61 code is the closest exact-SymPy template (sqrt, harness, _report/ALL_PASS/sys.exit — and note tests/test_slice_clause_iii.py used pytest, but the EXECUTOR VENV HAS NO PYTEST: use the assert-based __main__ harness of slice_clause_iii_verification.py instead). CRITICAL DIFFERENCE: 61-02 computes on the ASSOCIATIVE composite (and asserts h_3(O) is NEVER imported); 62-02 MUST compute the AMBIENT TRANSPORT residual on the NON-ASSOCIATIVE ambient h_3(O) for GENERIC X,Y. Do NOT reuse 61-02's associative-only path, and do NOT use the slice-internal case as the decisive test (it is the trivial control)."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use, cite]
    - id: ref-v11-leakage
      kind: prior_artifact
      locator: "code/verify_sequential_product.py (Phase 42, v11.0): sqrt(T_a) T_b sqrt(T_a) = (i/2) T_b for all 72 anticommuting Cl(9,0) pairs — the sequential product EXITS M_16(R). Readable by executor."
      aliases: ["v11.0 leakage", "Phase 42 SP", "verify_sequential_product.py"]
      role: background
      why_it_matters: "Historically adjacent: the sequential product is the operation that, in a DIFFERENT setting (Clifford NON-COMMUTING pairs in a fixed matrix algebra M_16(R)), exited the smaller real algebra. NOTE (plan-checker warning): this is a DIFFERENT mechanism from the h_3(O) -> h_3(C_u) PROJECTION/restriction transport tested here — it is NOT carried as a prior that the ambient E-transport verdict 'leans toward obstruction'. Cite it only as historical context, NOT as evidence for (O)."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [read, compare]
  acceptance_tests:
    - id: test-E-properties-exact
      subject: claim-restriction
      kind: reproducibility
      procedure: "Run `python tests/test_embedding_under_E.py` (assert-based, NO pytest): confirm E onto h_3(C_u) satisfies, by EXACT SymPy, (a) unital E(I_3)=I_3; (b) idempotent E o E = E (on generic h_3(O) test elements with nonzero e_1..e_6); (c) E|_A = id (on slice elements); (d) positive on test PSD elements; (e) E is NOT a Jordan morphism on the ambient (E(X o X) != (EX) o (EX) for a generic ambient X, exact). Confirm E is the entrywise proj_u (components 0,7 kept; 1..6 zeroed) on x1,x2,x3, identity on the real diagonal."
      pass_condition: "All E-property checks pass EXACTLY (no float tolerance); E matches the 62-01 entrywise-proj_u construction; E-not-a-Jordan-morphism-on-ambient confirmed; dim(range E)=9, dim(ker E)=18 confirmed."
      evidence_required: [deliv-vald-62-01, ref-effros-stormer]
      automation: automated
    - id: test-ambient-sqrt-exact
      subject: claim-restriction
      kind: reproducibility
      procedure: "Confirm the ambient principal square root sqrt_ambient(X) for a PSD element X is computed in the 27-dim NON-associative h_3(O) and is EXACT: self-check h3o_matmul(sqrt_X, sqrt_X) == X (octonionic 3x3 matrix product), exact zero residual. Confirm the exact-square trick (X = C^2, C ambient with exact entries) and/or a symbolic spectral sqrt; confirm sqrt is computed via the AMBIENT octonionic product."
      pass_condition: "sqrt_ambient(X)^2 == X EXACTLY in the ambient octonionic product; the square-root construction is ambient (h_3(O)), documented; exact arithmetic throughout."
      evidence_required: [deliv-vald-62-01, ref-vald-61-01]
      automation: automated
    - id: test-nonassociativity-load-bearing
      subject: claim-restriction
      kind: reproducibility
      procedure: "Confirm the non-associativity exerciser: the associator (X*Y)*Z - X*(Y*Z) is EXACTLY nonzero for the relevant ambient triple (built from the decisive test data X,Y) — proving the AMBIENT transport computation genuinely engages (xy)z != x(yz), not an accidentally-associative corner. Confirm the decisive X,Y are NOT confined to the (associative) slice or a common associative subalgebra."
      pass_condition: "Exact nonzero associator demonstrated on the relevant ambient triple; the decisive transport test genuinely exercises non-associativity (NON-ASSOCIATIVITY guard honored; fp-ignore-nonassociativity rejected); X,Y are generic ambient, not slice-confined."
      evidence_required: [deliv-vald-62-01]
      automation: automated
    - id: test-decisive-ambient-residual
      subject: claim-restriction
      kind: reproducibility
      procedure: "THE DECISIVE TEST. Confirm the AMBIENT-TRANSPORT residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) is computed EXACTLY for GENERIC X (PSD),Y in h_3(O) (sqrt(X) in the ambient; sqrt(EX) the slice square root of the projected EX), and the verdict is read off: R == 0 (exact SymPy) => COHERENT TRANSPORT (P); R != 0 (exact) => AMBIENT-TRANSPORT OBSTRUCTION (O). Confirm the harness ASSERTS the actual computed outcome (whichever it is) via a compute_ambient_transport_residual(X,Y) -> (R, is_zero_exact) with R.equals(zeros) <=> is_zero_exact (NOT hardcoded to P or O). Confirm >=2 distinct generic (X,Y), with the associator-nonzero check passing on each."
      pass_condition: "The exact ambient residual R is computed for >=2 generic (X,Y) (non-associativity load-bearing); the verdict (P if all R==0, O if any R!=0) is determined by exact symbolic equality; the harness reflects the TRUE outcome (not rigged toward P or O); the defect (if O) is localized (which e_k / Peirce grade)."
      evidence_required: [deliv-vald-62-01, deliv-embedding, ref-paper5-def1]
      automation: automated
    - id: test-slice-internal-control
      subject: claim-restriction
      kind: reproducibility
      procedure: "Confirm the slice-internal control: for a,b in the slice A = h_3(C_u), sqrt(a) b sqrt(a) (computed in the ambient) stays in A with leakage EXACTLY 0 and the triple associator EXACTLY 0 (A is a closed associative subalgebra = range E). Confirm this is computed and clearly labeled as the TRIVIAL CONTROL, separated from the decisive ambient test — so it is NOT mistaken for the decisive non-associative result and the line-loophole (exercising non-associativity on unrelated matrices while the decisive test is the trivial slice one) is foreclosed."
      pass_condition: "Slice-internal leakage == 0 EXACT and slice-internal associator == 0 EXACT confirmed; labeled as the control; NOT used as the decisive test (the decisive test is the ambient transport residual)."
      evidence_required: [deliv-vald-62-01]
      automation: automated
    - id: test-peirce-crosscheck
      subject: claim-restriction
      kind: consistency
      procedure: "Confirm an INDEPENDENT second route reaches the same verdict as test-decisive-ambient-residual: a Peirce/grade-component argument — decompose E(sqrt(X) Y sqrt(X)) and sqrt(EX)(EY)sqrt(EX) (and their difference) into C_u-vs-(e_1..e_6) components (and/or Peirce grades V_0/V_{1/2}/V_1 at E_11), and read off whether the transport defect is exactly zero. The two routes (direct ambient residual and Peirce-component) MUST agree on P vs O."
      pass_condition: "The Peirce/grade-component route is implemented and AGREES with the direct ambient-residual verdict (both P or both O); a disagreement RAISES (no verdict on a split decision)."
      evidence_required: [deliv-vald-62-01, ref-lem-bottleneck]
      automation: automated
    - id: test-exact-arithmetic
      subject: claim-restriction
      kind: consistency
      procedure: "Confirm the DECISIVE residual assertion uses EXACT arithmetic ONLY (SymPy .equals(zeros) / simplify(...) == 0 / Rational/surd equality), with NO float64 anywhere on the decisive path. Confirm the exact-square trick (X = C^2, ambient C, exact entries) is documented AND confirmed not to trivialize non-associativity (cross-ref test-nonassociativity-load-bearing). A float-tolerance 'leakage' would be a FALSE obstruction (fp-float-pass)."
      pass_condition: "No float64 on the decisive residual; exact symbolic equality used; exact-square trick documented and non-trivializing; fp-float-pass rejected (only an EXACT nonzero residual is accepted as obstruction evidence)."
      evidence_required: [deliv-vald-62-01]
      automation: automated
    - id: test-no-pytest
      subject: claim-restriction
      kind: reproducibility
      procedure: "Confirm code/embedding_under_E_verification.py and tests/test_embedding_under_E.py do NOT import pytest and do NOT use pytest fixtures/decorators (pytest is NOT installed in the executor venv — sympy/numpy only). Confirm the harness is assert-based with the _report/ALL_PASS/sys.exit pattern (mirroring code/slice_clause_iii_verification.py) and is runnable directly as `python tests/test_embedding_under_E.py` (and/or `python code/embedding_under_E_verification.py`), exiting 0 on self-check success."
      pass_condition: "No `import pytest` anywhere; assert-based __main__ harness; runs under `python tests/test_embedding_under_E.py` with sympy/numpy only; exits 0 on self-check success (for either P or O verdict)."
      evidence_required: [deliv-vald-62-01]
      automation: automated
    - id: test-verdict-not-forced
      subject: claim-restriction
      kind: consistency
      procedure: "Confirm the verdict (P or O) is whatever the exact computation yields — the code/harness/§4 do NOT force preservation. Confirm: if R is exactly nonzero, the deliverable reports AMBIENT-TRANSPORT OBSTRUCTION (O) honestly (characterizing the defect), flags it as the EXPECTED, ACCEPTABLE outcome that REFINES RESTRICTION to coexistence-as-island, and hands it to 62-03 — it does NOT massage X,Y until R==0 nor relax the exact test. Confirm the v11.0 precedent is cited only as historical context (different mechanism), NOT as evidence for (O)."
      pass_condition: "The verdict reflects the true exact computation; (O) reported honestly if found (no cherry-picking X,Y to force P; no relaxing the exact test; O is the expected refinement, not a collapse); (P) reported only if genuinely R==0 for non-associativity-load-bearing elements; fp-force-positive rejected."
      evidence_required: [deliv-vald-62-01, deliv-embedding]
      automation: hybrid
  forbidden_proxies:
    - id: fp-assert-preservation
      subject: claim-restriction
      proxy: "Reporting coherent transport WITHOUT computing the AMBIENT transport residual E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) on the actual non-associative h_3(O) for GENERIC X,Y — e.g. computing only the slice-internal sqrt(a) b sqrt(a) (a,b in A, which is TRIVIAL — closed associative subalgebra, leakage 0), asserting 'E is a conditional expectation so it transports the SP', or testing E(a o b) on the slice (Jordan, trivial) and passing it off as the transport result."
      reason: "The roadmap's central Phase 62 guard. The decisive object is the AMBIENT TRANSPORT residual for generic X,Y. The slice-internal case is mathematically TRIVIAL (verified: slice associator ~1e-15; range E closed/associative) and cannot engage non-associativity — using it as the decisive test, or asserting transport from E's slice-coherence (E is not even a Jordan morphism on the ambient), is forbidden false progress (fp-assert-preservation + NON-ASSOCIATIVITY guard)."
    - id: fp-ignore-nonassociativity
      subject: claim-restriction
      proxy: "Making the decisive test the slice-internal sqrt(a) b sqrt(a) (trivial), or choosing X,Y inside the (associative) slice / a common associative subalgebra so the triple product is accidentally associative, or running the non-associativity exerciser on UNRELATED matrices while the decisive test stays the trivial slice one (the line-loophole), or skipping the associator-nonzero check."
      reason: "NON-ASSOCIATIVITY guard (roadmap): success REQUIRES the decisive AMBIENT transport residual for generic X,Y where (xy)z != x(yz) is genuinely engaged. The associator-nonzero check (test-nonassociativity-load-bearing) on the ACTUAL decisive X,Y is mandatory; the slice-internal case is the control only."
    - id: fp-float-pass
      subject: claim-restriction
      proxy: "Using float64 (or any finite-tolerance) for the DECISIVE ambient-transport residual; reporting a float-tolerance nonzero value as 'obstruction', or a float-tolerance near-zero as 'coherent transport'."
      reason: "EXACT-ARITHMETIC requirement (roadmap, critical): a nonzero residual at float64 tolerance is NOT evidence of an obstruction; an EXACT nonzero residual IS. Float round-off must never be mistaken for a genuine ambient-transport obstruction. The decisive assertion uses exact SymPy equality only. (Float64 may be used ONLY as a non-decisive pre-screen, clearly labeled.)"
    - id: fp-force-positive
      subject: claim-restriction
      proxy: "Cherry-picking X,Y until R==0, relaxing the exact test to a tolerance to make a nonzero residual 'vanish', or reporting COHERENT TRANSPORT when the honest exact computation gives an obstruction. Equivalently, suppressing an obstruction because coherent transport is the 'hoped-for' answer."
      reason: "An ambient-transport obstruction is the EXPECTED, ACCEPTABLE outcome that REFINES RESTRICTION to coexistence-as-island (claim.md, ROADMAP) — it is NOT a failure to be tuned away. The verdict must reflect the true exact computation. Do NOT carry the v11.0 precedent as a thumb on the scale either way (different mechanism)."
    - id: fp-redefine-iii
      subject: claim-restriction
      proxy: "Replacing the clause-(iii) sequential product sqrt(a) b sqrt(a) with a convenient surrogate (e.g. testing the Jordan product, a 'symmetrized' product, or a tensor-composite product-form) and calling that the ambient transport test; or claiming the reframe 'weakens clause (iii)' (it does NOT — only RESTRICTION's embedding clause is weakened, in 62-03)."
      reason: "Clause (iii)'s fourth datum is the product-form SEQUENTIAL product, used verbatim (claim.md), and was established intrinsically in Phase 61. The decisive test must use sqrt(X) Y sqrt(X) (the actual self-modeling SP) in the ambient — not a stand-in; and clause (iii) itself is NOT weakened by the coexistence-as-island reframe."
  links:
    - id: link-vald-evidence
      source: deliv-vald-62-01
      target: claim-restriction
      relation: supports
      verified_by: [test-decisive-ambient-residual, test-peirce-crosscheck, test-nonassociativity-load-bearing, test-exact-arithmetic]
    - id: link-embedding-records-evidence
      source: claim-restriction
      target: deliv-embedding
      relation: supports
      verified_by: [test-decisive-ambient-residual, test-verdict-not-forced]
    - id: link-reuse-61-02-template
      source: ref-vald-61-01
      target: deliv-vald-62-01
      relation: depends_on
      verified_by: [test-ambient-sqrt-exact, test-no-pytest]
    - id: link-control-trivial
      source: ref-lem-bottleneck
      target: deliv-vald-62-01
      relation: supports
      verified_by: [test-slice-internal-control]
  uncertainty_markers:
    weakest_anchors:
      - "The ambient principal square root sqrt(X) in the non-associative h_3(O) is the most delicate construction: h_3(O) is power-associative / formally real, so CFC on a single element X is well-defined (sqrt(X) is a polynomial in X, I, hence in the associative subalgebra generated by {I, X}). The exact-square trick (X = C^2) sidesteps surds, but the executor must confirm sqrt(X) is taken in the AMBIENT and that the AMBIENT TRANSPORT residual (involving the projected sqrt(EX) as well) is where the obstruction would appear — and that the chosen X,Y make the relevant associator nonzero."
      - "E's positivity (order-theoretic) on the full 27-dim h_3(O) is asserted via Effros-Stormer in 62-01 and spot-checked here; if a test PSD element maps to a non-PSD E(X) exactly, the entrywise-proj_u construction would not be the lem:bottleneck E and the setup must be revisited (disconfirming)."
      - "The slice-internal triviality is NOT a weakness of the test — it is the verified reason the decisive object MUST be ambient transport. It is recorded as the control; the decisive verdict rests ENTIRELY on the ambient residual for generic X,Y."
    disconfirming_observations:
      - "THE decisive observation: an EXACT nonzero AMBIENT-TRANSPORT residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) != 0 for generic X,Y (sqrt in the ambient, associator nonzero) => E does NOT transport the sequential product coherently => an AMBIENT-TRANSPORT OBSTRUCTION (O). This is the EXPECTED, ACCEPTABLE outcome that REFINES RESTRICTION to coexistence-as-island (self-contained C* island) — it does NOT establish independent posits and is NOT a bug to be tuned away (fp-force-positive). R == 0 => coherent transport (a RESTRICTION embedding lemma)."
      - "If the direct ambient-residual route and the independent Peirce/grade route DISAGREE on P vs O, neither verdict is trustworthy: the executor must resolve the discrepancy (likely a bug) before reporting — do NOT report a verdict on a split decision."
      - "If the associator-nonzero check FAILS on the decisive X,Y (the test data turns out accidentally associative), the decisive test is void (fp-ignore-nonassociativity): the executor must choose generic ambient X,Y that genuinely exercise (xy)z != x(yz) and re-run."
---

<objective>
On the ACTUAL non-associative h_3(O), decide by EXACT-arithmetic SymPy computation whether the bottleneck conditional expectation E TRANSPORTS the self-modeling sequential product coherently from the ambient — does E(sqrt(X) Y sqrt(X)) = sqrt(EX)(EY)sqrt(EX) for GENERIC ambient X (PSD),Y in h_3(O) (sqrt(X) in the ambient; non-associativity genuinely engaged) — with an independent Peirce-grade cross-check, the slice-internal case recorded as a TRIVIAL control, and the verdict read off exactly as computed.

CORRECTED FRAMING (2026-05-24, Bryan's decision — the plan-checker found, and it was verified, that the original slice-internal test is mathematically trivial): the slice A = h_3(C_u) is a CLOSED, ASSOCIATIVE Jordan subalgebra (= range E), so sqrt(a) b sqrt(a) for a,b in A has leakage EXACTLY 0 and cannot engage non-associativity. The decisive object is therefore AMBIENT E-TRANSPORT for generic X,Y; an obstruction is EXPECTED and REFINES RESTRICTION to coexistence-as-island (NOT independent posits).

Purpose: The decisive-evidence half of Phase 62 (the load-bearing step). 62-01 set up E and framed the ambient-transport crux; this plan (VALD-62-01 + DERV-62-02) COMPUTES the answer on the genuinely non-associative structure and records the verdict on the obstruction-or-preservation fork. 62-03 reads that verdict into the RESTRICTION embedding lemma (P) or the precise ambient-transport obstruction (O, expected — refines RESTRICTION).

Output: code/embedding_under_E_verification.py + tests/test_embedding_under_E.py (exact-SymPy VALD-62-01, ASSERT-BASED — NO pytest) and §4 of derivations/p5-basin-restriction/embedding-under-E.md (the ambient-transport computation + decisive residual + verdict on the fork).

CRITICAL SCOPE GUARDS (binding):
- The DECISIVE object is the AMBIENT TRANSPORT residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) for GENERIC X,Y in h_3(O), where sqrt(X) is in the ambient and non-associativity is genuinely engaged (the relevant associator is EXACTLY nonzero — mandatory check). A demonstration on the (trivial) associative slice alone, or one assuming ambient associativity, is forbidden false progress (fp-assert-preservation, fp-ignore-nonassociativity, NON-ASSOCIATIVITY guard). Record the slice-internal case (leakage 0, associator 0) as the documented CONTROL, clearly separated.
- EXACT arithmetic on the decisive residual — SymPy symbolic/rational/surd, NEVER float64 for the verdict. A float-tolerance nonzero is NOT an obstruction; an exact nonzero residual IS (fp-float-pass). Use the exact-square trick (X = C^2, ambient C, exact entries) with X,Y GENERIC AMBIENT so non-associativity is genuinely exercised.
- Report the TRUE verdict (P or O) — do NOT cherry-pick X,Y or relax the exact test. An ambient-transport obstruction (O) is the EXPECTED, ACCEPTABLE outcome that REFINES RESTRICTION to coexistence-as-island (handed to 62-03), NOT a collapse / independent posits (fp-force-positive). Do NOT carry the v11.0 precedent as a prior leaning toward (O) — it is a different mechanism (Clifford pairs, not the projection restriction); cite it only as historical context.
- Use the actual self-modeling sequential product sqrt(X) Y sqrt(X) — not the Jordan product or a surrogate (fp-redefine-iii). The Jordan-side and slice-internal cases are useful CONTROLS, but they are NOT the decisive ambient-transport test. Clause (iii) itself is NOT weakened by the reframe.
- NO PYTEST: the executor venv has sympy/numpy only (pytest is NOT installed). Use an ASSERT-BASED __main__ harness mirroring code/slice_clause_iii_verification.py (_report/ALL_PASS/sys.exit), runnable as `python tests/test_embedding_under_E.py`. REUSE the exact-SymPy sqrt + harness patterns from code/slice_clause_iii_verification.py and the octonion arithmetic STRUCTURE from code/octonion_algebra.py (Fano table, _mat_mul_h3o, proj_u, peirce_*) — but PORT the octonion path to EXACT SymPy (the existing infra is float64 NumPy). The NEW, decisive work is the AMBIENT non-associative TRANSPORT computation.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
@./.claude/get-physics-done/references/verification/core/code-testing-physics.md
</execution_context>

<context>
@.gpd/ROADMAP.md
@derivations/p5-basin-restriction/claim.md
@derivations/p5-basin-restriction/embedding-under-E.md
@derivations/p5-basin-restriction/slice-clause-iii.md
@code/octonion_algebra.py
@code/slice_clause_iii_verification.py
@code/verify_sequential_product.py
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Write the failing assert-based exact-SymPy harness (NO pytest) pinning E's properties, the ambient sqrt, the non-associativity exerciser, the slice-internal control, and the decisive AMBIENT transport residual</name>
  <files>tests/test_embedding_under_E.py</files>
  <action>
    Write tests/test_embedding_under_E.py FIRST (TDD RED phase) — an ASSERT-BASED harness (NO pytest; pytest is NOT installed in the executor venv) that pins the exact behavior code/embedding_under_E_verification.py must implement. It imports the (not-yet-written) check functions from code/embedding_under_E_verification.py and asserts on their results; it is RED (import/attribute error) until Task 2 implements them. Mirror the ASSERT-BASED __main__ pattern of code/slice_clause_iii_verification.py (_report/ALL_PASS/sys.exit), NOT the pytest style of tests/test_slice_clause_iii.py.

    First READ: code/slice_clause_iii_verification.py (the assert-based _report/ALL_PASS/sys.exit harness, matrix_sqrt_nxn, luders_seq_product, hermitian_3x3), code/octonion_algebra.py (Octonion class, _MUL_TABLE Fano e1e2=e4, _mat_mul_h3o, proj_u, peirce_V1/Vhalf/V0, H3O coordinates; NOTE it is float64 — port to exact SymPy in Task 2), and embedding-under-E.md §1-§3 (the E construction, the AMBIENT-transport crux spec, the slice-internal triviality, written by 62-01).

    Structure: a sys.path insertion to import from code/, then a sequence of check groups, each calling the code's functions and asserting EXACT results (use SymPy .equals(zeros)/simplify==0; NO float tolerance on the decisive path), with a _report/ALL_PASS accumulator and `sys.exit(0 if ALL_PASS else 1)` in __main__. Runnable as `python tests/test_embedding_under_E.py`. Do NOT `import pytest`.

    Check groups (RED — functions unimplemented):

    (A) E conditional-expectation properties (exact):
      - E unital: E(I_3) == I_3.
      - E idempotent: E(E(X)) == E(X) for a generic exact-rational h_3(O) element X (octonion off-diagonals with nonzero e_1..e_6 so the projection is non-trivial).
      - E|_A = id: for X in the slice (off-diagonals in C_u = span{1,e_7}), E(X) == X.
      - E entrywise proj_u: E zeroes octonion components 1..6 of x1,x2,x3, keeps 0,7; identity on real diagonal.
      - E positive (spot): for a PSD slice effect, E(.) PSD.
      - E NOT a Jordan morphism on the ambient: E(X o X) != (EX) o (EX) for a generic ambient X (assert the exact difference is nonzero) — this is what makes the transport question non-trivial.

    (B) Ambient square root (exact, non-associative):
      - sqrt_ambient(X) computed in the ambient; self-check h3o_matmul(sqrt_X, sqrt_X) == X EXACTLY for X = C^2 (C ambient, exact entries) — exact-square trick.
      - assert sqrt_ambient uses the AMBIENT octonionic product (not a slice-only complex sqrt).

    (C) Non-associativity exerciser (mandatory, on the DECISIVE data):
      - the associator (X*Y)*Z - X*(Y*Z) is EXACTLY nonzero for the relevant ambient triple built from the decisive X,Y (assert nonzero). This proves the AMBIENT transport test genuinely engages (xy)z != x(yz).
      - assert the decisive X,Y are generic ambient (NOT slice-confined).

    (D) THE DECISIVE AMBIENT-TRANSPORT residual (exact):
      - compute_ambient_transport_residual(X,Y) -> (R, is_zero_exact) where R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) (sqrt(X) in the ambient; sqrt(EX) the slice/projected square root), for >=2 GENERIC (X,Y) (X PSD; at least one Y with rich off-diagonal structure). 
        * HONEST assertion pattern (not rigged): assert is_zero_exact is a BOOLEAN consistent with R (R.equals(zeros) <=> is_zero_exact). A single verdict step records P (all is_zero_exact True) or O (any False). Do NOT hardcode `assert is_zero_exact == True` (forces P) or `== False` (forces O). The harness PASSES for whichever verdict the true exact computation yields.
      - assert the associator-nonzero check (C) passed on each decisive (X,Y) (so the residual is a genuine non-associative test).

    (E) Slice-internal TRIVIAL control (exact):
      - for a,b in the slice A, assert sqrt(a) b sqrt(a) stays in A (E(sqrt(a) b sqrt(a)) == sqrt(a) b sqrt(a), leakage 0) and the triple associator == 0. Label clearly as the CONTROL (trivial; A is a closed associative subalgebra). This is NOT the decisive test.

    (F) Independent Peirce/grade cross-check (exact):
      - a second route decomposing the transport defect into C_u-vs-(e_1..e_6) / Peirce grades yields the SAME is_zero_exact verdict as (D). Assert agreement; the code RAISES on a split decision.

    (G) No-pytest / runnable check:
      - the harness has NO `import pytest`; it runs as `python tests/test_embedding_under_E.py` and exits 0 on ALL_PASS.

    Module docstring: ASSERT_CONVENTION line (Jordan o, sequential &, exact arithmetic, Fano e1e2=e4, u=e_7, slice A=h_3(C_u)) + a note that this is the VALD-62-01 RED phase, ASSERT-BASED (no pytest), and the decisive object is the AMBIENT transport residual (slice-internal is the trivial control).

    The harness MUST fail initially (functions unimplemented) — the RED state. Do NOT implement code/embedding_under_E_verification.py in this task.
  </action>
  <verify>
    1. tests/test_embedding_under_E.py created; ASSERT-BASED (NO `import pytest`); imports the target functions from code/embedding_under_E_verification.py (not yet existing), so running `python tests/test_embedding_under_E.py` is RED (import/attribute error or failing asserts) — confirm failure, not pass.
    2. Check groups (A)-(G) present: (A) E unital/idempotent/E|_A=id/entrywise-proj_u/positive + E-NOT-a-Jordan-morphism-on-ambient; (B) ambient sqrt self-check (X=C^2); (C) non-associativity exerciser (associator nonzero on the DECISIVE data); (D) decisive AMBIENT transport residual via compute_ambient_transport_residual -> (R, is_zero_exact), HONEST (not hardcoded to P or O), >=2 generic (X,Y); (E) slice-internal TRIVIAL control (leakage 0, associator 0) labeled as control; (F) independent Peirce cross-check agreeing; (G) no-pytest/runnable.
    3. All decisive assertions use EXACT SymPy equality (.equals(zeros)/simplify==0), NO float tolerance on the decisive path.
    4. The decisive verdict is HONEST: it does NOT hardcode R==0 (force P) or R!=0 (force O); it asserts internal consistency (R.equals(zeros) <=> is_zero_exact) and cross-route agreement, so it passes for whichever verdict the true exact computation yields.
    5. Module docstring has the ASSERT_CONVENTION line, the VALD-62-01 RED-phase + assert-based (no pytest) note, and the ambient-transport-is-decisive / slice-internal-is-control framing. Harness mirrors code/slice_clause_iii_verification.py's _report/ALL_PASS/sys.exit pattern.
  </verify>
  <done>tests/test_embedding_under_E.py exists, is ASSERT-BASED (no pytest, runnable as `python tests/test_embedding_under_E.py`), and is RED (target functions unimplemented): exact-SymPy checks pin E's conditional-expectation properties (unital, idempotent, E|_A=id, entrywise proj_u, positive, NOT a Jordan morphism on the ambient), the ambient square root self-check (exact-square trick X=C^2), a mandatory non-associativity exerciser (associator nonzero on the DECISIVE data), the decisive AMBIENT transport residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) via an HONEST compute_ambient_transport_residual returning (R, is_zero_exact) (not rigged toward P or O; >=2 generic X,Y), the slice-internal TRIVIAL control (leakage 0, associator 0), and an independent Peirce/grade cross-check that must agree. All decisive assertions exact (no float tolerance); no pytest.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Implement exact-SymPy E (+ E-not-a-Jordan-morphism check), ambient non-associative octonionic product + sqrt, the decisive AMBIENT transport residual + Peirce cross-check + slice-internal control; make the harness pass; emit the P/O verdict</name>
  <files>code/embedding_under_E_verification.py</files>
  <action>
    Implement code/embedding_under_E_verification.py so `python tests/test_embedding_under_E.py` passes (TDD GREEN). EXACT SymPy throughout; ASSERT-BASED harness (NO pytest). Port the octonion arithmetic to exact SymPy (the existing code/octonion_algebra.py is float64 NumPy — reuse its STRUCTURE: Fano table FANO_TRIPLES / _MUL_TABLE with e1e2=e4, the H3O coordinate layout, _mat_mul_h3o entry formulas, proj_u, peirce_V1/Vhalf/V0 — but with SymPy exact entries).

    Module docstring: ASSERT_CONVENTION (Jordan o = (1/2)(ab+ba); sequential & = sqrt(a) b sqrt(a) CFC principal; Fano e1e2=e4; u=e_7; slice A=h_3(C_u); EXACT symbolic arithmetic; LIVE-paper provenance) + the VALD-62-01 framing: this computes, on the GENUINELY non-associative h_3(O), the AMBIENT TRANSPORT residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) for generic X,Y — coherent transport (P) or ambient-transport obstruction (O, EXPECTED, refines RESTRICTION to coexistence-as-island); the slice-internal case is the TRIVIAL control; EXACT-arithmetic zero-tolerance; do NOT force P; the v11.0 precedent is historical context (different mechanism), not evidence.

    Implement (mirror the _report/ALL_PASS harness of slice_clause_iii_verification.py; provide a main() with sys.exit(0 if ALL_PASS else 1)):

    (1) Exact SymPy octonion arithmetic:
      - oct_mul(a, b): 8-component exact-SymPy octonion product via the Fano table (e1e2=e4, e_i^2=-1, anticommuting). Port _MUL_TABLE from octonion_algebra.py.
      - oct_conj; helpers to build octonions with exact (Rational/symbolic) components.
      - h3o_matmul(A, B): 3x3 octonionic matrix product, entrywise (port _mat_mul_h3o's M11..M33 formulas) — each entry a sum of single oct_mul products. CRITICAL: do NOT assume the triple product associates; h3o_matmul(h3o_matmul(X,Y),Z) and h3o_matmul(X,h3o_matmul(Y,Z)) are computed independently and may differ.
      - represent h_3(O) elements as a full 3x3 matrix of octonions (cleanest for the non-associative triple product), or the H3O coordinate tuple — be consistent.

    (2) E onto h_3(C_u) (u=e_7) + the not-a-Jordan-morphism check:
      - proj_u_exact(oct): keep components 0,7, zero 1..6 (exact).
      - E(X): apply proj_u_exact to every entry of the 3x3 octonionic Hermitian (identity on real diagonal, proj_u on off-diagonals).
      - jordan(A,B) := (1/2)(h3o_matmul(A,B) + h3o_matmul(B,A)).
      - Exact checks: E(I_3)=I_3; E o E = E (generic element with e_1..e_6); E|_A=id (slice element); positivity spot-check (map slice element to its C_u 3x3 complex matrix, eigenvalues >= 0 exact/charpoly); and E(jordan(X,X)) != jordan(E(X),E(X)) for a generic ambient X (E is NOT a Jordan morphism on the ambient — assert exact nonzero difference).

    (3) Ambient principal square root sqrt_ambient(X):
      - For a PSD element X, compute sqrt(X) in the AMBIENT h_3(O). h_3(O) is power-associative, so CFC on a single element X is well-defined (sqrt(X) a polynomial in X, I). Implement via the EXACT-SQUARE TRICK primarily: choose X = C^2 with C an ambient element with exact entries, so sqrt(X) = C (principal PSD) is exact; ALSO provide a spectral/CFC-polynomial variant to confirm sqrt_ambient(X) matches C.
      - SELF-CHECK: h3o_matmul(sqrt_X, sqrt_X) == X EXACTLY (octonionic product).
      - DESIGN NOTE (code comment): the decisive object is the AMBIENT TRANSPORT residual, comparing E(sqrt(X) Y sqrt(X)) [ambient SP then project] against sqrt(EX)(EY)sqrt(EX) [project then slice SP]. For GENERIC ambient X,Y the ambient triple product genuinely engages non-associativity; the residual detects whether E is a morphism for the SP. Compute both associations of the ambient triple and confirm the associator is nonzero (non-associativity load-bearing).

    (4) THE DECISIVE computation:
      - sp_ambient(X,Y) := h3o_matmul(h3o_matmul(sqrt_ambient(X), Y), sqrt_ambient(X)) (ambient SP; also compute the right association and record the associator).
      - sp_projected(X,Y) := h3o_matmul(h3o_matmul(sqrt_ambient(E(X)), E(Y)), sqrt_ambient(E(X))) — note E(X) is in the slice so sqrt_ambient(E(X)) is the slice square root.
      - compute_ambient_transport_residual(X,Y) -> (R, is_zero_exact): R = E(sp_ambient(X,Y)) - sp_projected(X,Y); is_zero_exact = R.equals(zero-matrix) via exact SymPy. Run for >=2 GENERIC (X,Y) (X PSD; rich off-diagonal Y), each with an associator-nonzero precheck.

    (5) Independent Peirce/grade cross-check:
      - decompose the transport defect R into C_u vs e_1..e_6 components (and/or Peirce grades V_0/V_{1/2}/V_1 at E_11; port peirce_V1/Vhalf/V0 exact). Read is_zero_exact_peirce = (the defect is exactly zero). Assert is_zero_exact_peirce == is_zero_exact from (4); RAISE on disagreement.

    (6) Non-associativity exerciser:
      - exhibit, on the DECISIVE data, an explicit ambient triple with associator (X*Y)*Z - X*(Y*Z) EXACTLY nonzero; assert nonzero. Confirm the decisive sp_ambient genuinely engages non-associativity.

    (7) Slice-internal TRIVIAL control:
      - for a,b in the slice A, compute sqrt(a) b sqrt(a) (ambient) and confirm it stays in A (E(.) == . , leakage 0) and the triple associator == 0. Label as the CONTROL; this confirms Phase 61's intrinsic triviality, NOT the decisive verdict.

    (8) Verdict + main():
      - VERDICT = COHERENT TRANSPORT if all is_zero_exact True (and cross-check agrees); AMBIENT-TRANSPORT OBSTRUCTION if any is_zero_exact False.
      - Print clearly: if transport, "E TRANSPORTS the sequential product on h_3(O) -> GO toward RESTRICTION embedding lemma (62-03)"; if obstruction, "E does NOT transport the sequential product -> AMBIENT-TRANSPORT OBSTRUCTION (EXPECTED); defect in <Peirce grade / e_k>; refines RESTRICTION to coexistence-as-island; characterize + 62-03".
      - Exit 0 if all SELF-CHECKS pass (E-properties incl. not-a-Jordan-morphism, sqrt self-check, associator-nonzero on decisive data, cross-route agreement, slice control trivial) — BOTH P and O are valid scientific outcomes; exit nonzero ONLY on a self-check/consistency failure (sqrt^2 != X, E not idempotent, routes disagree, associator zero on the decisive data). Do NOT exit nonzero merely because the verdict is O.
      - Make `python tests/test_embedding_under_E.py` pass.

    Reproducibility footer: SymPy version, Python version, deterministic. NO `import pytest`.
  </action>
  <verify>
    1. code/embedding_under_E_verification.py implemented; `python tests/test_embedding_under_E.py` PASSES (GREEN); NO `import pytest` in either file; runs with sympy/numpy only.
    2. Exact-SymPy octonion arithmetic (oct_mul via Fano e1e2=e4) and h3o_matmul (entrywise, triple product NOT assumed associative; left vs right computed independently). E onto h_3(C_u) implemented (entrywise proj_u_exact); E(I_3)=I_3, E o E=E, E|_A=id, positivity, and E-NOT-a-Jordan-morphism-on-ambient (E(X o X) != (EX) o (EX)) all verified EXACTLY.
    3. Ambient sqrt_ambient(X) implemented (exact-square trick X=C^2 + spectral confirmation); self-check h3o_matmul(sqrt_X,sqrt_X)==X EXACT; ambient product.
    4. THE DECISIVE AMBIENT-TRANSPORT residual R = E(sp_ambient(X,Y)) - sp_projected(X,Y) computed exactly for >=2 GENERIC (X,Y) via compute_ambient_transport_residual -> (R, is_zero_exact) using exact SymPy equality (NO float); associator-nonzero precheck passes on each decisive (X,Y).
    5. Independent Peirce/grade cross-check implemented and AGREES with the direct residual verdict (raises on disagreement). Non-associativity exerciser shows exact nonzero associator on the DECISIVE data. Slice-internal control shows leakage 0 / associator 0, labeled as the trivial control.
    6. Verdict printed clearly (P or O exactly as computed); script exits 0 on self-check success regardless of P/O; exits nonzero only on a self-check/consistency failure. Verdict NOT forced toward P (no cherry-picking; exact test not relaxed); O framed as EXPECTED + refining RESTRICTION.
    7. EXACT arithmetic on the decisive path (fp-float-pass rejected); NON-ASSOCIATIVITY guard honored — the decisive test is the ambient transport residual with the associator nonzero, NOT the trivial slice-internal case (fp-ignore-nonassociativity, fp-assert-preservation rejected); fp-redefine-iii rejected (actual SP, not a surrogate); v11.0 cited only as historical context.
  </verify>
  <done>code/embedding_under_E_verification.py implemented in exact SymPy (assert-based, NO pytest): ported octonion arithmetic (Fano e1e2=e4), non-associative h3o_matmul (triple product by independent left/right association), E onto h_3(C_u) (entrywise proj_u; unital/idempotent/E|_A=id/positive verified exactly AND E-not-a-Jordan-morphism-on-ambient confirmed), ambient principal square root (exact-square trick X=C^2 + spectral confirmation; sqrt(X)^2==X exact), the decisive AMBIENT TRANSPORT residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) for >=2 GENERIC (X,Y) (exact SymPy equality; associator-nonzero precheck so non-associativity is load-bearing), an independent Peirce/grade cross-check agreeing (raises on split), a non-associativity exerciser (exact nonzero associator on the decisive data), and the slice-internal TRIVIAL control (leakage 0, associator 0). The decisive verdict (P or O) is printed exactly as computed and NOT forced (O = EXPECTED, refines RESTRICTION to coexistence-as-island); `python tests/test_embedding_under_E.py` passes (GREEN, no pytest); exits 0 on self-check success for either verdict. fp-assert-preservation, fp-ignore-nonassociativity, fp-float-pass, fp-force-positive, fp-redefine-iii all rejected.</done>
</task>

<task type="auto">
  <name>Task 3: Record §4 (the AMBIENT-transport computation + decisive residual + verdict on the fork) in embedding-under-E.md; cite the code/tests; state P or O exactly as computed (O expected, refines RESTRICTION)</name>
  <files>derivations/p5-basin-restriction/embedding-under-E.md</files>
  <action>
    Append §4 to derivations/p5-basin-restriction/embedding-under-E.md — the DECISIVE AMBIENT-TRANSPORT COMPUTATION and its verdict on the obstruction-or-preservation fork. Records the VALD-62-01 result analytically and hands the verdict to 62-03. Do NOT modify §1-§3 (62-01's setup) except to update the "§4 to follow" marker to "§4 below; §5 to follow (62-03)".

    §4 — THE DECISIVE AMBIENT-TRANSPORT COMPUTATION ON THE NON-ASSOCIATIVE h_3(O) (VALD-62-01):

    (4.1) Setup recap (1 paragraph): the decisive question from §3.3 — for GENERIC ambient X,Y, with sqrt(X) computed in the ambient h_3(O), is E(sqrt(X) Y sqrt(X)) = sqrt(EX)(EY)sqrt(EX) (coherent transport) or not (ambient-transport obstruction)? Computed by exact SymPy (assert-based, no pytest) in code/embedding_under_E_verification.py + tests/test_embedding_under_E.py.

    (4.2) What was computed (cite the code precisely):
      - E onto h_3(C_u) built exactly (entrywise proj_u, u=e_7); E-properties verified EXACTLY (unital; idempotent; E|_A=id; positive); AND E is NOT a Jordan morphism on the ambient (E(X o X) != (EX) o (EX), exact nonzero difference — record the magnitude/components).
      - Ambient square root sqrt_ambient(X) (exact-square trick X=C^2 + spectral confirmation); sqrt(X)^2==X exact in the ambient octonionic product.
      - Non-associativity LOAD-BEARING on the decisive data: the associator (X*Y)*Z - X*(Y*Z) is EXACTLY nonzero (give it / its salient components).
      - THE DECISIVE AMBIENT-TRANSPORT residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) for >=2 GENERIC (X,Y), exact.
      - Slice-internal TRIVIAL control: for a,b in A, sqrt(a) b sqrt(a) stays in A, leakage 0, associator 0 (closed associative subalgebra) — recorded, clearly separated as the control.
      - Independent Peirce/grade cross-check agreeing.

    (4.3) THE VERDICT (state exactly as the computation yields — do NOT force):
      - IF COHERENT TRANSPORT (R == 0 exactly for the non-associativity-load-bearing generic X,Y, both routes agree): state "E TRANSPORTS the sequential product coherently on the actual non-associative h_3(O): E(sqrt(X) Y sqrt(X)) = sqrt(EX)(EY)sqrt(EX)." (P) branch -> 62-03 states+proves the RESTRICTION embedding lemma. Note this is strong/non-trivial (E is not even a Jordan morphism on the ambient, yet would transport the SP).
      - IF AMBIENT-TRANSPORT OBSTRUCTION (R != 0 exactly for some generic X,Y, both routes agree): state "E does NOT transport the sequential product coherently on h_3(O): the residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) is exactly nonzero." CHARACTERIZE the defect: which Peirce grade (V_0/V_{1/2}/V_1 at E_11) and which e_k it populates; give the exact residual / salient entries. (O) branch -> 62-03 characterizes the obstruction precisely + the minimal extra input. State EXPLICITLY: (O) is the EXPECTED, ACCEPTABLE outcome and REFINES RESTRICTION to coexistence-as-island (the observer is a self-contained C* island on the slice; the through-line survives; E is the access/projection map, not a Jordan morphism) — it does NOT establish independent posits and is NOT a program collapse. NOTE the v11.0 precedent only as historical context (different mechanism — Clifford pairs, not the projection restriction), NOT as evidence.
      - State which branch the computation actually took, with the exact evidence.

    (4.4) Why the verdict is trustworthy:
      - Two independent routes (direct ambient residual; Peirce/grade-component) agree.
      - EXACT arithmetic (SymPy), zero-tolerance: R==0 means exactly zero (coherent transport), R!=0 means exactly nonzero (genuine ambient-transport obstruction, NOT round-off) — fp-float-pass rejected.
      - Non-associativity is LOAD-BEARING on the decisive data (associator exactly nonzero); the slice-internal triviality is recorded as the control, NOT the decisive test — fp-ignore-nonassociativity rejected.
      - The decisive object is the actual self-modeling SP sqrt(X) Y sqrt(X) (fp-redefine-iii rejected); clause (iii) itself is unchanged.
      - The computation is on the genuinely non-associative ambient for GENERIC X,Y, not the (trivial) slice — fp-assert-preservation rejected.

    (4.5) HANDOFF to 62-03: state that the verdict (P or O) and, if O, the characterized defect (Peirce grade + e_k + exact residual), are the decisive input to 62-03's reading of the fork (and 62-03 interprets either branch as a REFINEMENT of RESTRICTION). Update the §5-to-follow marker.

    Add a TYPE/CATEGORY + PEIRCE self-audit for §4: defect tagged by Peirce grade / e_k; verdict tagged P or O; slice-internal tagged trivial control; no forcing.

    CONFIDENCE marker for §4: HIGH if all self-checks passed and both routes agree; flag the generality caveat (does the verdict hold for ALL ambient X,Y or the tested generic representatives? — note whether a general argument or specific exact pairs were used; one exact nonzero residual SUFFICES to establish an obstruction, but a (P) on representatives is weaker than a general proof — carry the caveat to 62-03).
  </action>
  <verify>
    1. §4 appended; §1-§3 unmodified except the marker. (4.1) recaps the AMBIENT-transport question; (4.2) cites code/embedding_under_E_verification.py + tests/test_embedding_under_E.py and lists what was computed (E-properties + E-not-a-Jordan-morphism, ambient sqrt, associator-nonzero on decisive data, decisive ambient residual, slice-internal control, Peirce cross-check).
    2. (4.3) states the VERDICT exactly as computed — P (R==0) or O (R!=0) — with the actual branch and exact evidence; if O, the defect is CHARACTERIZED (Peirce grade + e_k + exact residual) and explicitly framed as EXPECTED + REFINING RESTRICTION to coexistence-as-island (NOT independent posits, NOT collapse); the (P)->lemma / (O)->obstruction routing to 62-03 stated; v11.0 noted only as historical context.
    3. (4.4) justifies trustworthiness: two routes agree; exact zero-tolerance (fp-float-pass); non-associativity load-bearing on the decisive data + slice-internal recorded as control (fp-ignore-nonassociativity); actual SP (fp-redefine-iii); genuinely non-associative ambient for generic X,Y (fp-assert-preservation).
    4. (4.5) hands the verdict (+ characterized defect if O) to 62-03; marker updated. Type/category + Peirce self-audit present; CONFIDENCE marker + generality caveat present.
    5. The §4 verdict is consistent with the code's printed verdict and the harness outcome (no prose/computation discrepancy).
  </verify>
  <done>§4 of derivations/p5-basin-restriction/embedding-under-E.md records the decisive AMBIENT-TRANSPORT computation (VALD-62-01): E built and verified exactly (incl. E-not-a-Jordan-morphism-on-ambient), ambient sqrt with self-check, non-associativity load-bearing on the decisive data (associator exactly nonzero), the exact ambient-transport residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) for >=2 generic (X,Y), the slice-internal TRIVIAL control (leakage 0), and an agreeing independent Peirce/grade cross-check. The verdict on the obstruction-or-preservation fork is stated EXACTLY as computed (P: coherent transport -> RESTRICTION lemma in 62-03; O: obstruction -> characterize defect [Peirce grade + e_k + exact residual] + minimal extra input in 62-03), with (O) framed as the EXPECTED, ACCEPTABLE outcome that REFINES RESTRICTION to coexistence-as-island (NOT independent posits, NOT collapse) and the v11.0 precedent noted only as historical context. Trustworthiness justified (two agreeing routes; exact zero-tolerance; non-associativity load-bearing + slice-internal control; actual SP; generic ambient). The verdict + characterized defect are handed to 62-03. fp-assert-preservation, fp-ignore-nonassociativity, fp-float-pass, fp-force-positive, fp-redefine-iii all rejected; verdict not forced.</done>
</task>

</tasks>

<verification>
- E EXACT (VALD): E onto h_3(C_u) verified by exact SymPy — unital, idempotent, E|_A=id, positive, AND NOT a Jordan morphism on the ambient (E(X o X) != (EX) o (EX)) (test-E-properties-exact).
- AMBIENT SQRT EXACT: sqrt_ambient(X)^2==X in the octonionic product (exact-square trick X=C^2 + spectral confirmation); computed in the 27-dim non-associative ambient (test-ambient-sqrt-exact).
- NON-ASSOCIATIVITY LOAD-BEARING: the associator (X*Y)*Z - X*(Y*Z) is EXACTLY nonzero on the DECISIVE data; the decisive ambient transport genuinely engages (xy)z != x(yz) (test-nonassociativity-load-bearing; NON-ASSOCIATIVITY guard honored).
- DECISIVE AMBIENT-TRANSPORT RESIDUAL: R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) computed EXACTLY for >=2 GENERIC (X,Y); verdict P (R==0) or O (R!=0) read off by exact equality (test-decisive-ambient-residual).
- SLICE-INTERNAL CONTROL (trivial): for a,b in A, leakage 0 / associator 0 (closed associative subalgebra); recorded as the control, NOT the decisive test (test-slice-internal-control).
- INDEPENDENT CROSS-CHECK: Peirce/grade-component route agrees with the direct ambient-residual verdict (test-peirce-crosscheck); split decision RAISES.
- EXACT ARITHMETIC: decisive residual via exact SymPy only, no float64 (fp-float-pass rejected); exact-square trick documented and non-trivializing (test-exact-arithmetic).
- NO PYTEST: assert-based __main__ harness (_report/ALL_PASS/sys.exit), runnable as `python tests/test_embedding_under_E.py`, sympy/numpy only (test-no-pytest).
- ERROR BUDGET: zero-tolerance decisive test — exact R ==0 => coherent transport, exact R !=0 => obstruction; no float tolerance on the verdict.
- VERDICT NOT FORCED: P or O exactly as computed; (O) reported honestly + EXPECTED + refines RESTRICTION to coexistence-as-island; no cherry-picking X,Y, no relaxing the exact test; v11.0 only historical context (test-verdict-not-forced; fp-force-positive rejected).
- ANCHOR DISCIPLINE: ref-effros-stormer (E is a Jordan conditional expectation, verified; not a Jordan morphism on the ambient), ref-lem-bottleneck (slice + Peirce coordinates), ref-paper5-def1 (the SP is clause iii's datum, verbatim), ref-vald-61-01 (reused exact-SymPy + assert-based harness; decisive path is AMBIENT TRANSPORT not associative), ref-v11-leakage (historical context only) surfaced.
- HANDOFF: the verdict (+ characterized defect if O) is the decisive input to 62-03 (which reads either branch as a refinement of RESTRICTION), wired via contract links.
</verification>

<success_criteria>
On the GENUINELY non-associative h_3(O), exact-arithmetic SymPy (code/embedding_under_E_verification.py + tests/test_embedding_under_E.py, VALD-62-01; ASSERT-BASED, NO pytest, runnable as `python tests/test_embedding_under_E.py`) decides whether E TRANSPORTS the self-modeling sequential product coherently from the ambient: the decisive object is the AMBIENT TRANSPORT residual R = E(sqrt(X) Y sqrt(X)) - sqrt(EX)(EY)sqrt(EX) for GENERIC X (PSD),Y in h_3(O) (sqrt(X) in the ambient) — R == 0 => coherent transport (P), R != 0 => ambient-transport obstruction (O). E onto h_3(C_u) (u=e_7) is built and verified exactly (unital, idempotent, E|_A=id, positive) AND shown NOT to be a Jordan morphism on the ambient (E(X o X) != (EX) o (EX)) — which is what makes the transport question non-trivial. The ambient square root is exact (X=C^2 exact-square trick + spectral confirmation; sqrt(X)^2==X). Non-associativity is LOAD-BEARING on the decisive data (the associator (X*Y)*Z - X*(Y*Z) is exactly nonzero; NON-ASSOCIATIVITY guard honored). The slice-internal sequential product (a,b in A) is computed and recorded as a TRIVIAL control (closed associative subalgebra = range E; leakage 0, associator 0), clearly separated and NOT used as the decisive test. An independent Peirce/grade-component cross-check AGREES with the direct ambient-residual verdict (split decision raises). The decisive residual uses EXACT arithmetic with zero tolerance (a nonzero residual is a genuine obstruction, not round-off). §4 of embedding-under-E.md records the verdict EXACTLY as computed (P -> RESTRICTION embedding lemma in 62-03; O -> precise obstruction [Peirce grade + e_k + exact residual] + minimal extra input in 62-03), with (O) framed as the EXPECTED, ACCEPTABLE outcome that REFINES RESTRICTION to coexistence-as-island (self-contained C* island; through-line survives; E = access/projection map) — NOT independent posits, NOT a collapse — and the v11.0 precedent noted only as historical context (different mechanism); the verdict + characterized defect are handed to 62-03. The plan rejects fp-assert-preservation (computation on the actual non-associative ambient for generic X,Y, not the trivial slice or a hand-wave), fp-ignore-nonassociativity (non-associativity load-bearing on the decisive data; slice-internal is the control), fp-float-pass (exact arithmetic on the verdict), fp-force-positive (verdict not forced; (O) is the expected refinement), and fp-redefine-iii (actual SP sqrt(X) Y sqrt(X); clause (iii) itself unchanged). NO pytest is used (sympy/numpy only).
</success_criteria>

<output>
After completion, create `.gpd/phases/62-coherent-embedding-under-e-the-hard-part/62-02-SUMMARY.md`.
</output>
