---
phase: 62-coherent-embedding-under-e-the-hard-part
plan: 02
type: tdd
wave: 2
depends_on: ["62-01"]
files_modified:
  - code/embedding_under_E_verification.py
  - tests/test_embedding_under_E.py
  - derivations/p5-basin-restriction/embedding-under-E.md
interactive: false

conventions:
  units: "natural (hbar=1, k_B=1); pure algebra — no physical dimensions; exact symbolic/rational/surd arithmetic (SymPy), NEVER float64 for the decisive test"
  metric: "riemannian_fisher (N/A this plan); Peirce-grade + C_u-vs-(e_1..e_6) component bookkeeping is the dimensional-analysis analog"
  algebra: "Jordan product a o b = (1/2)(ab+ba); sequential product a&b = sqrt(a) b sqrt(a) (CFC, principal branch); octonionic 3x3 Hermitian matrix product computed entrywise (non-associative across the triple product)"
  octonion: "Fano e_1 e_2 = e_4 (matches code/octonion_algebra.py infra); octonion = 8-component real vector"
  complex_structure: "u = e_7 by default (any u in S^6 equivalent under G_2); C_u = span{1, e_7} (keep octonion components 0 and 7)"
  slice: "A = h_3(C_u) ~ M_3(C)^sa; entries in C_u; ker E = components along e_1..e_6 (3 off-diagonal octonions x 6 = 18 real dims)"
  conditional_expectation: "E: h_3(O) -> h_3(C_u), entrywise proj_u; positive, unital, idempotent (62-01 setup); verified exactly here"

dimensional_check:
  dim_h3O: "[real dim] = 27"
  dim_A: "[real dim] = 9"
  dim_ker_E: "[real dim] = 18 (the e_1..e_6 components of x1,x2,x3 = 3 x 6)"
  residual_lands_in: "ker E (the e_1..e_6 octonion directions) — if SP_ambient(a,b) leaks, the leakage component E^perp(SP) = SP - E(SP) lives here; report which e_k populate"
  decisive_residual: "[exact] R := SP_ambient(a,b) - E(SP_ambient(a,b)); R == 0 (exact) => preservation (P); R != 0 (exact) => leakage/obstruction (O)"

approximations:
  - name: "none (EXACT symbolic arithmetic; zero-tolerance decisive test)"
    parameter: "N/A — the entire methodological point is EXACT arithmetic (SymPy), so genuine non-associativity leakage is distinguished from float round-off"
    validity: "always exact; the decisive residual is tested with SymPy .equals(zeros) / simplify(...) == 0, NEVER a float tolerance"
    breaks_when: "float64 would FALSELY report nonzero 'leakage' from round-off; this plan FORBIDS float64 on the decisive assertion (fp-float-pass)"
    check: "matrix_sqrt self-check sqrt(a)^2 == a EXACTLY; idempotency E o E = E EXACTLY; the decisive residual via exact symbolic equality only"

estimated_execution:
  total_minutes: 80
  breakdown:
    - task: 1
      minutes: 30
      note: "RED: write tests/test_embedding_under_E.py — failing tests pinning E's conditional-expectation properties (exact), the ambient CFC square root (exact, non-associative), the non-associativity exerciser, and the decisive landing-in-slice residual (tensor/symbolic + multiplied) (unfamiliar-formalism 1.5x + symbolic+numerical mixed multipliers applied)"
    - task: 2
      minutes: 35
      note: "GREEN: implement code/embedding_under_E_verification.py — exact-SymPy E (entrywise proj_u on octonionic 3x3 Hermitian), ambient octonionic matrix product (non-associative), ambient CFC sqrt, the decisive E(sqrt(a) b sqrt(a)) vs sqrt(a) b sqrt(a) residual + independent Peirce/grade cross-check; make tests pass; emit the decisive verdict"
    - task: 3
      minutes: 15
      note: "Record §4 (the computation + decisive residual + verdict on the fork) in embedding-under-E.md; cite the code/tests; state P or O exactly as computed — do NOT force"

contract:
  schema_version: 1
  scope:
    question: "On the ACTUAL non-associative h_3(O): for a, b in the slice A = h_3(C_u) (sqrt(a) computed by CFC in the AMBIENT h_3(O), non-associativity treated explicitly), does the sequential product sqrt(a) b sqrt(a) land back in the slice (E(sqrt(a) b sqrt(a)) = sqrt(a) b sqrt(a), exact zero residual => PRESERVATION) or does it leak (exact nonzero residual in ker E => OBSTRUCTION)? Decided by EXACT-arithmetic SymPy computation + an independent Peirce-grade cross-check — NOT asserted."
  claims:
    - id: claim-restriction
      statement: "(Decisive evidence half.) Whether the self-modeling sequential product on A is INDUCED by / consistent with the ambient h_3(O) under E is settled by EXACT computation on the genuinely non-associative structure: build E onto h_3(C_u) (u=e_7) exactly; compute sqrt(a) b sqrt(a) for slice a,b with sqrt(a) computed by CFC in the AMBIENT non-associative h_3(O); test whether the result lands back in the slice (preservation P) or leaks (obstruction O), via the exact residual R = SP_ambient - E(SP_ambient) AND an independent Peirce/grade-component route. The non-associativity is genuinely exercised (NON-ASSOCIATIVITY guard). The verdict (P or O) is whatever the exact computation yields — not forced toward preservation."
      deliverables: [deliv-embedding, deliv-vald-62-01]
      acceptance_tests: [test-E-properties-exact, test-ambient-sqrt-exact, test-nonassociativity-exercised, test-decisive-residual, test-peirce-crosscheck, test-exact-arithmetic, test-verdict-not-forced]
      references: [ref-effros-stormer, ref-lem-bottleneck, ref-paper5-def1, ref-vald-61-01, ref-v11-leakage]
  deliverables:
    - id: deliv-embedding
      kind: derivation
      path: derivations/p5-basin-restriction/
      description: "derivations/p5-basin-restriction/embedding-under-E.md §4 — the decisive computation result: the exact residual R = sqrt(a) b sqrt(a) - E(sqrt(a) b sqrt(a)) for slice a,b (sqrt in the ambient non-associative h_3(O)); whether R == 0 (preservation P) or R != 0 (leakage/obstruction O, with the leakage's Peirce grade / e_k directions named); the independent Peirce-grade cross-check agreeing; the verdict on the fork stated exactly as computed. Cites code/embedding_under_E_verification.py + tests/test_embedding_under_E.py."
      must_contain:
        - "§4 reports the EXACT decisive residual R = SP_ambient(a,b) - E(SP_ambient(a,b)) for slice a,b (sqrt(a) computed in the AMBIENT non-associative h_3(O) via CFC), and states the verdict: R == 0 => PRESERVATION (P); R != 0 => LEAKAGE / OBSTRUCTION (O)"
        - "explicit confirmation the computation is on the GENUINELY non-associative h_3(O): the test elements exercise (XY)Z != X(YZ) (the non-associativity exerciser passes), NOT the associative slice alone (NON-ASSOCIATIVITY guard honored)"
        - "if (O): the leakage component SP_ambient - E(SP_ambient) is characterized — which Peirce grade (V_0/V_{1/2}/V_1) and which e_k (in e_1..e_6 = ker E) it populates; if (P): explicit statement that the ambient triple product lands in the slice exactly"
        - "the INDEPENDENT Peirce/grade-component cross-check route reaches the SAME verdict as the direct E(sqrt(a) b sqrt(a)) residual computation"
        - "EXACT-arithmetic confirmation: the decisive residual is tested by exact SymPy equality (.equals(zeros) / simplify == 0), NEVER a float tolerance; the exact-square trick (a = c^2, c in slice) is documented and confirmed NOT to trivialize non-associativity (b generic; triple product genuinely non-associative)"
        - "the verdict is stated exactly as computed (P or O), NOT forced toward preservation; if (O), it is flagged as an acceptable deliverable triggering PAUSE condition 2 (handed to 62-03)"
    - id: deliv-vald-62-01
      kind: code
      path: code/embedding_under_E_verification.py
      description: "code/embedding_under_E_verification.py + tests/test_embedding_under_E.py (VALD-62-01) — exact-symbolic SymPy: (1) E onto h_3(C_u) (u=e_7) as entrywise proj_u on an octonionic 3x3 Hermitian, with exact checks E(I_3)=I_3, E o E = E, E|_A = id, positivity on test PSD elements; (2) ambient octonionic 3x3 matrix product (entrywise, non-associative across the triple product) reusing/porting code/octonion_algebra.py to EXACT SymPy octonion arithmetic; (3) ambient CFC principal square root of a slice PSD effect a, computed in the 27-dim non-associative h_3(O) (exact via the exact-square trick a=c^2 and/or symbolic spectral sqrt); (4) the decisive test: E(sqrt(a) b sqrt(a)) vs sqrt(a) b sqrt(a), exact residual; (5) an independent Peirce/grade-component computation of the same verdict; (6) a non-associativity exerciser proving (XY)Z != X(YZ) on the test data. Exits nonzero on any self-check failure; prints the decisive P/O verdict."
      must_contain:
        - "exact-SymPy octonion multiplication (Fano e_1 e_2 = e_4) and 3x3 octonionic Hermitian matrix product (entrywise; the triple product NOT assumed associative)"
        - "E onto h_3(C_u): entrywise proj_u (keep octonion components 0 and 7, zero 1..6 on each off-diagonal; identity on real diagonal); exact checks E(I_3)=I_3, E o E = E, E|_A = id"
        - "ambient CFC principal square root sqrt(a) for slice PSD effect a, computed in h_3(O) (27-dim), EXACT (a = c^2 exact-square trick documented, and/or exact symbolic spectral sqrt); self-check sqrt(a) o sqrt(a)-type / sqrt(a)*sqrt(a) == a EXACT"
        - "the decisive residual R = sqrt(a) b sqrt(a) - E(sqrt(a) b sqrt(a)) computed EXACTLY for slice a,b (b generic in the slice so non-associativity is exercised); SymPy exact-zero test, NOT float"
        - "an independent Peirce/grade-component route to the verdict (decompose into C_u vs e_1..e_6 components / Peirce grades; track non-associative cross-terms into ker E)"
        - "a non-associativity exerciser: explicit (XY)Z != X(YZ) on the actual test elements (proving the slice/ambient is genuinely non-associative for this data — NON-ASSOCIATIVITY guard)"
        - "decisive verdict printed: R==0 => PRESERVATION (GO toward RESTRICTION lemma); R!=0 => OBSTRUCTION (characterize leakage; PAUSE-2); script exits 0 only if self-checks pass (verdict P or O are BOTH valid outcomes — the script reports which, it does NOT force P)"
  references:
    - id: ref-effros-stormer
      kind: paper
      locator: "Effros-Stormer 1979 — content quoted in LIVE complexification.tex:437 (see 62-01). EXECUTOR HAS NO WEB; use the quoted content. Here it underwrites that E (positive unital idempotent) has range = JB-subalgebra; the exact idempotency/unitality/positivity checks in this plan VERIFY E is such a map computationally."
      aliases: ["Effros-Stormer", "EffrosStormer1979"]
      role: method
      why_it_matters: "Underwrites E as a Jordan conditional expectation (Jordan-side). The decisive point of this plan: E's Jordan-coherence does NOT settle the SEQUENTIAL product — that is computed here on the non-associative ambient. The exact E-property checks confirm E is a legitimate conditional expectation before the sequential-product test is run."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-lem-bottleneck
      kind: prior_artifact
      locator: "~/repos/blog/landing/papers/sm-from-self-modeling/sections/complexification.tex lem:bottleneck (line 409); slice A = h_3(C_u) (line 424-432); Peirce decomposition (iii) (lines 481-486): V_1~R, V_{1/2}~C_u^2, V_0~h_2(C_u). Readable by executor (live .tex)."
      aliases: ["lem:bottleneck", "C*-bottleneck universality"]
      role: method
      why_it_matters: "Supplies E, the slice, and the Peirce decomposition the independent cross-check route uses. The Peirce grades (V_0/V_{1/2}/V_1) are the coordinates in which leakage (if any) is characterized."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-paper5-def1
      kind: spec
      locator: "~/repos/blog/landing/papers/qm-from-self-modeling/main.tex def:self-modeling-system clause (iii) sms:minimal (lines 351-353): 'product-form sequential product' is the datum at stake. LIVE paper. Verbatim text quoted in 62-01 / rem-converse-bgw.md §1,§3 / slice-clause-iii.md §0.2."
      aliases: ["Paper 5 Def 1", "clause (iii)", "sms:minimal"]
      role: definition
      why_it_matters: "The sequential product sqrt(a) b sqrt(a) computed here IS clause (iii)'s fourth datum. Whether E induces it from h_3(O) is exactly the RESTRICTION question. Clause (iii) verbatim, never weakened (fp-redefine-iii guarded in claim.md)."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-vald-61-01
      kind: prior_artifact
      locator: "code/slice_clause_iii_verification.py + tests/test_slice_clause_iii.py (VALD-61-01, Phase 61). Provides the EXACT-SymPy templates: matrix_sqrt_nxn (exact spectral PSD square root via eigenvects + Gram-Schmidt), luders_seq_product (sqrt(a) b sqrt(a)), hermitian_3x3 builder, the _report/ALL_PASS harness pattern, and the product-form factorization on the ASSOCIATIVE M_9(C)^sa. REUSE the harness + sqrt patterns; the NEW work is the AMBIENT non-associative octonionic computation."
      aliases: ["VALD-61-01", "slice_clause_iii_verification.py"]
      role: method
      why_it_matters: "The Phase 61 code is the closest exact-SymPy template (sqrt, harness, report pattern). CRITICAL DIFFERENCE: 61-02 computes on the ASSOCIATIVE composite (and an explicit test there asserts h_3(O) is NEVER imported); 62-02 MUST compute on the NON-ASSOCIATIVE ambient h_3(O) (the whole point). Do NOT reuse 61-02's associative-only path for the decisive test."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use, cite]
    - id: ref-v11-leakage
      kind: prior_artifact
      locator: "code/verify_sequential_product.py (Phase 42, v11.0): sqrt(T_a) T_b sqrt(T_a) = (i/2) T_b for all 72 anticommuting Cl(9,0) pairs — the sequential product EXITS M_16(R) (real -> complex). Exact SymPy section confirms it. Readable by executor."
      aliases: ["v11.0 leakage", "Phase 42 SP", "verify_sequential_product.py"]
      role: benchmark
      why_it_matters: "The direct PRECEDENT that the sequential product is the leakage-prone operation (it left the real observable algebra). It raises the prior probability of obstruction (O) for the h_3(O) -> h_3(C_u) restriction, and is the structural-estimate anchor (62-01 §3.5). It does NOT decide the verdict — the exact h_3(O) computation does — but it means a nonzero residual would be UNSURPRISING, not a bug."
      applies_to: [claim-restriction]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, compare, cite]
  acceptance_tests:
    - id: test-E-properties-exact
      subject: claim-restriction
      kind: reproducibility
      procedure: "Run tests/test_embedding_under_E.py: confirm E onto h_3(C_u) satisfies, by EXACT SymPy, (a) unital E(I_3)=I_3; (b) idempotent E o E = E (on generic h_3(O) test elements); (c) E|_A = id (on slice elements); (d) positive on test PSD elements (eigenvalues of E(X) real >= 0 for PSD X, exact or via the C_u-projected 3x3 complex matrix). Confirm E is the entrywise proj_u (components 0,7 kept; 1..6 zeroed) on x1,x2,x3, identity on the real diagonal."
      pass_condition: "All four E-property checks pass EXACTLY (no float tolerance); E matches the 62-01 entrywise-proj_u construction; dim(range E)=9, dim(ker E)=18 confirmed by the component bookkeeping."
      evidence_required: [deliv-vald-62-01, ref-effros-stormer]
      automation: automated
    - id: test-ambient-sqrt-exact
      subject: claim-restriction
      kind: reproducibility
      procedure: "Confirm the ambient CFC principal square root sqrt(a) for a slice PSD effect a is computed in the 27-dim NON-associative h_3(O) and is EXACT: self-check sqrt(a) * sqrt(a) == a (octonionic 3x3 matrix product), exact zero residual. Confirm the exact-square trick (a = c^2, c in slice with exact entries) is used so sqrt(a) is exact, and/or a symbolic spectral sqrt; confirm sqrt(a) is computed via the AMBIENT product, not the slice's complex-matrix sqrt."
      pass_condition: "sqrt(a)*sqrt(a) == a EXACTLY in the ambient octonionic product; the square-root construction is ambient (h_3(O)), documented; exact arithmetic throughout."
      evidence_required: [deliv-vald-62-01, ref-vald-61-01]
      automation: automated
    - id: test-nonassociativity-exercised
      subject: claim-restriction
      kind: reproducibility
      procedure: "Confirm the non-associativity exerciser: for the actual test elements (or closely related ambient octonionic 3x3 matrices), (X*Y)*Z != X*(Y*Z) for at least one explicit triple — proving the computation lives in the GENUINELY non-associative h_3(O), not an accidentally-associative corner. Confirm the decisive test elements (sqrt(a), b, sqrt(a)) are NOT all confined to a common associative subalgebra in a way that makes the triple product associative by accident (b generic in the slice; sqrt(a) ambient)."
      pass_condition: "Explicit (XY)Z != X(YZ) demonstrated (exact nonzero associator) on representative ambient octonionic matrices; the decisive triple product genuinely exercises non-associativity (NON-ASSOCIATIVITY guard honored; fp-ignore-nonassociativity rejected)."
      evidence_required: [deliv-vald-62-01]
      automation: automated
    - id: test-decisive-residual
      subject: claim-restriction
      kind: reproducibility
      procedure: "THE DECISIVE TEST. Confirm the residual R = sqrt(a) b sqrt(a) - E(sqrt(a) b sqrt(a)) is computed EXACTLY for slice a,b (sqrt(a) in the ambient h_3(O)), and the verdict is read off: R == 0 (exact SymPy) => PRESERVATION (P); R != 0 (exact) => LEAKAGE / OBSTRUCTION (O). Confirm the test ASSERTS the actual computed outcome (whichever it is) — the test must PASS for the TRUE verdict and would FAIL only if the computation were inconsistent (e.g. residual neither provably 0 nor provably nonzero). Confirm at least 2 distinct slice (a,b) pairs are tested (one with off-diagonal structure exercising non-associativity)."
      pass_condition: "The exact residual R is computed for >=2 slice (a,b) pairs; the verdict (P if all R==0, O if any R!=0) is determined by exact symbolic equality; the test reflects the TRUE computed outcome and is not rigged toward P; the leakage (if O) is localized (which e_k / Peirce grade)."
      evidence_required: [deliv-vald-62-01, deliv-embedding, ref-paper5-def1]
      automation: automated
    - id: test-peirce-crosscheck
      subject: claim-restriction
      kind: consistency
      procedure: "Confirm an INDEPENDENT second route reaches the same verdict as test-decisive-residual: a Peirce/grade-component argument — decompose sqrt(a), b, and the triple product into C_u-vs-(e_1..e_6) components (and/or Peirce grades V_0/V_{1/2}/V_1 at E_11), and track whether non-associative cross-terms populate ker E (the e_1..e_6 directions). The two routes (direct E-residual and Peirce-component) MUST agree on P vs O."
      pass_condition: "The Peirce/grade-component route is implemented and AGREES with the direct E-residual verdict (both P or both O); a disagreement is a red flag the executor must resolve before reporting a verdict."
      evidence_required: [deliv-vald-62-01, ref-lem-bottleneck]
      automation: automated
    - id: test-exact-arithmetic
      subject: claim-restriction
      kind: consistency
      procedure: "Confirm the DECISIVE residual assertion uses EXACT arithmetic ONLY (SymPy .equals(zeros) / simplify(...) == 0 / Rational/surd equality), with NO float64 anywhere on the decisive path. Confirm the exact-square trick (a = c^2, c in slice, exact entries) is documented AND confirmed not to trivialize non-associativity (cross-ref test-nonassociativity-exercised). A float-tolerance 'leakage' would be a FALSE obstruction (fp-float-pass)."
      pass_condition: "No float64 on the decisive residual; exact symbolic equality used; exact-square trick documented and non-trivializing; fp-float-pass rejected (a nonzero residual at float tolerance is NOT accepted as evidence — only an exact nonzero residual)."
      evidence_required: [deliv-vald-62-01]
      automation: automated
    - id: test-verdict-not-forced
      subject: claim-restriction
      kind: consistency
      procedure: "Confirm the verdict (P or O) is whatever the exact computation yields — the code/tests/§4 do NOT force preservation. Confirm: if the residual is exactly nonzero, the deliverable reports OBSTRUCTION (O) honestly (characterizing the leakage), flags it as an acceptable outcome triggering PAUSE condition 2, and hands it to 62-03 — it does NOT massage the test elements until R==0. Confirm the v11.0 leakage precedent is cited as making (O) unsurprising."
      pass_condition: "The verdict reflects the true exact computation; (O) is reported honestly if found (no cherry-picking a,b to force P; no relaxing the exact test); (P) is reported only if genuinely R==0 for non-associativity-exercising elements; fp-force-positive rejected."
      evidence_required: [deliv-vald-62-01, deliv-embedding, ref-v11-leakage]
      automation: hybrid
  forbidden_proxies:
    - id: fp-assert-preservation
      subject: claim-restriction
      proxy: "Reporting preservation WITHOUT computing sqrt(a) b sqrt(a) on the actual non-associative h_3(O) — e.g. computing only on the associative slice (reusing 61-02's associative path), asserting 'E is a conditional expectation so the SP is preserved', or testing E(a o b) (the Jordan product, which DOES pass) and passing it off as the sequential-product result."
      reason: "The roadmap's central Phase 62 guard. The whole point is the SEQUENTIAL product on the NON-associative ambient. A demonstration on the associative slice M_3(C)^sa, or one that assumes ambient associativity, is forbidden false progress (fp-assert-preservation + NON-ASSOCIATIVITY reward-hacking guard). The computation MUST use the ambient octonionic product and the ambient CFC sqrt."
    - id: fp-ignore-nonassociativity
      subject: claim-restriction
      proxy: "Computing in a way that silently uses associativity: treating sqrt(a) b sqrt(a) as if (sqrt(a) b) sqrt(a) = sqrt(a) (b sqrt(a)) without it being a computed fact, choosing all test elements inside a common associative subalgebra so the triple product is accidentally associative, or skipping the non-associativity exerciser."
      reason: "NON-ASSOCIATIVITY guard (roadmap, flagged): success REQUIRES sqrt(a) b sqrt(a) computed in the genuinely non-associative ambient where (xy)z != x(yz). The non-associativity exerciser (test-nonassociativity-exercised) is mandatory; the test elements must genuinely exercise non-associativity."
    - id: fp-float-pass
      subject: claim-restriction
      proxy: "Using float64 (or any finite-tolerance) for the DECISIVE landing-in-slice residual; reporting a float-tolerance nonzero value as 'leakage/obstruction', or a float-tolerance near-zero as 'preservation'."
      reason: "EXACT-ARITHMETIC requirement (roadmap, critical): a nonzero 'leakage' at float64 tolerance is NOT evidence of an obstruction; an EXACT nonzero residual IS. Float round-off must never be mistaken for genuine non-associativity leakage. The decisive assertion uses exact SymPy equality only. (Float64 may be used ONLY as a non-decisive pre-screen, clearly labeled, never as the verdict.)"
    - id: fp-force-positive
      subject: claim-restriction
      proxy: "Cherry-picking slice elements a,b until R==0, relaxing the exact test to a tolerance to make a nonzero residual 'vanish', or reporting PRESERVATION when the honest exact computation gives leakage. Equivalently, suppressing an obstruction because preservation is the 'hoped-for' through-line."
      reason: "A clean obstruction is a fully acceptable, valuable outcome (claim.md, ROADMAP). The verdict must reflect the true exact computation. The v11.0 precedent makes (O) a live possibility; forcing (P) is reward-hacking the spine of the program."
    - id: fp-redefine-iii
      subject: claim-restriction
      proxy: "Replacing the clause-(iii) sequential product sqrt(a) b sqrt(a) with a convenient surrogate (e.g. testing the Jordan product, or a 'symmetrized' product, or a product-form expression on a tensor composite) and calling that the sequential-product-under-E test."
      reason: "Clause (iii)'s fourth datum is the product-form SEQUENTIAL product, used verbatim (claim.md). The decisive test must use sqrt(a) b sqrt(a) (the actual self-modeling SP), computed in the ambient — not a stand-in."
  links:
    - id: link-vald-evidence
      source: deliv-vald-62-01
      target: claim-restriction
      relation: supports
      verified_by: [test-decisive-residual, test-peirce-crosscheck, test-nonassociativity-exercised, test-exact-arithmetic]
    - id: link-embedding-records-evidence
      source: claim-restriction
      target: deliv-embedding
      relation: supports
      verified_by: [test-decisive-residual, test-verdict-not-forced]
    - id: link-reuse-61-02-template
      source: ref-vald-61-01
      target: deliv-vald-62-01
      relation: depends_on
      verified_by: [test-ambient-sqrt-exact]
    - id: link-v11-precedent
      source: ref-v11-leakage
      target: claim-restriction
      relation: benchmarks
      verified_by: [test-verdict-not-forced]
  uncertainty_markers:
    weakest_anchors:
      - "The ambient CFC square root sqrt(a) in the non-associative h_3(O) is the most delicate construction: in a non-associative algebra, 'the' principal square root and the spectral/CFC theory require care (h_3(O) is power-associative / formally real, so CFC on a single element a is well-defined — sqrt(a) is a polynomial in a, hence in the associative subalgebra generated by a and I). The exact-square trick (a = c^2) sidesteps the surd issue, but the executor must confirm sqrt(a) is taken in the AMBIENT (its components may include e_1..e_6 if c does), and that the TRIPLE product sqrt(a) b sqrt(a) — not the square root alone — is where non-associativity acts."
      - "E's positivity (order-theoretic) on the full 27-dim h_3(O) is asserted via Effros-Stormer in 62-01 and spot-checked here; if a test PSD element maps to a non-PSD E(X) exactly, the entrywise-proj_u construction would not be the lem:bottleneck E and the setup must be revisited (disconfirming)."
    disconfirming_observations:
      - "THE decisive disconfirming observation: an EXACT nonzero residual R = sqrt(a) b sqrt(a) - E(sqrt(a) b sqrt(a)) != 0 for slice a,b (sqrt in the ambient, non-associativity exercised) => the sequential product is NOT preserved by E => OBSTRUCTION (O). Per the v11.0/Phase 42 precedent (SP exits M_16(R)) this is a live, arguably favored outcome and is a fully acceptable DELIVERABLE (PAUSE condition 2), NOT a bug to be tuned away (fp-force-positive)."
      - "If the direct E-residual route and the independent Peirce/grade-component route DISAGREE on P vs O, neither verdict is trustworthy: the executor must resolve the discrepancy (likely a bug in one route) before reporting — do NOT report a verdict on a split decision."
      - "If the non-associativity exerciser FAILS (the test data turns out accidentally associative), the decisive test is void (fp-ignore-nonassociativity): the executor must choose test elements that genuinely exercise (xy)z != x(yz) and re-run."
---

<objective>
On the ACTUAL non-associative h_3(O), decide by EXACT-arithmetic SymPy computation whether the self-modeling sequential product sqrt(a) b sqrt(a) (a,b in the slice A = h_3(C_u); sqrt(a) computed by CFC in the ambient h_3(O)) is INDUCED by / consistent with the ambient under E (preservation P) or leaks out of the slice (obstruction O) — with an independent Peirce-grade cross-check, the non-associativity genuinely exercised, and the verdict read off exactly as computed.

Purpose: The decisive-evidence half of Phase 62 (the load-bearing step). 62-01 set up E and framed the crux; this plan (VALD-62-01 + DERV-62-02) COMPUTES the answer on the genuinely non-associative structure and records the verdict on the obstruction-or-preservation fork. 62-03 reads that verdict into the RESTRICTION lemma (P) or the precise obstruction (O).

Output: code/embedding_under_E_verification.py + tests/test_embedding_under_E.py (exact-SymPy VALD-62-01) and §4 of derivations/p5-basin-restriction/embedding-under-E.md (the computation result + decisive residual + verdict on the fork).

CRITICAL SCOPE GUARDS (binding):
- Compute sqrt(a) b sqrt(a) on the GENUINELY non-associative ambient h_3(O) — the ambient octonionic 3x3 matrix product and the ambient CFC square root. A demonstration on the associative slice alone, or one assuming ambient associativity, is forbidden false progress (fp-assert-preservation, fp-ignore-nonassociativity, NON-ASSOCIATIVITY guard). The non-associativity exerciser ((XY)Z != X(YZ)) is mandatory.
- EXACT arithmetic on the decisive residual — SymPy symbolic/rational/surd, NEVER float64 for the verdict. A float-tolerance nonzero is NOT an obstruction; an exact nonzero residual IS (fp-float-pass). Use the exact-square trick (a = c^2, c in slice, exact entries) with b generic so non-associativity is still exercised.
- Report the TRUE verdict (P or O) — do NOT cherry-pick a,b or relax the exact test to force preservation. A clean obstruction (O) is an acceptable deliverable triggering PAUSE condition 2 (handed to 62-03). The v11.0 precedent (SP exits M_16(R)) makes (O) unsurprising (fp-force-positive).
- Use the actual self-modeling sequential product sqrt(a) b sqrt(a) — not the Jordan product or a surrogate (fp-redefine-iii). The Jordan-product control E(a o b)=a o b SHOULD pass and is a useful contrast — but it is NOT the sequential-product test.
- REUSE the exact-SymPy harness + sqrt patterns from code/slice_clause_iii_verification.py (matrix_sqrt_nxn, luders_seq_product, hermitian_3x3, _report/ALL_PASS) and the octonion arithmetic from code/octonion_algebra.py (Fano table, _mat_mul_h3o, proj_u, peirce_*) — but PORT the octonion path to EXACT SymPy (the existing infra is float64 NumPy). The NEW, decisive work is the AMBIENT non-associative computation; do NOT reuse 61-02's associative-only path for the verdict.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
@./.claude/get-physics-done/references/verification/core/code-testing-physics.md
</execution_context>

<context>
@.gpd/ROADMAP.md
@derivations/p5-basin-restriction/claim.md
@derivations/p5-basin-restriction/embedding-under-E.md
@derivations/p5-basin-restriction/slice-clause-iii.md
@code/octonion_algebra.py
@code/slice_clause_iii_verification.py
@code/verify_sequential_product.py
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Write failing exact-SymPy tests pinning E's properties, the ambient CFC sqrt, the non-associativity exerciser, and the decisive landing-in-slice residual</name>
  <files>tests/test_embedding_under_E.py</files>
  <action>
    Write tests/test_embedding_under_E.py FIRST (TDD RED phase) — failing tests that pin the exact behavior code/embedding_under_E_verification.py must implement. Import the (not-yet-written) functions from code/embedding_under_E_verification.py; tests fail until Task 2 implements them. Mirror the pytest style of tests/test_slice_clause_iii.py (read it first for the import pattern and the exact-equality idioms).

    First READ: tests/test_slice_clause_iii.py (the closest test template — exact-SymPy idioms, sys.path insertion to import from code/), code/slice_clause_iii_verification.py (matrix_sqrt_nxn, luders_seq_product, hermitian_3x3), code/octonion_algebra.py (Octonion class, _MUL_TABLE Fano e1e2=e4, _mat_mul_h3o, proj_u, peirce_V1/Vhalf/V0, H3O coordinates), and embedding-under-E.md §1-§3 (the E construction and the decisive question spec written by 62-01).

    Write these test groups (all EXACT SymPy; the decisive ones use .equals(zeros)/simplify==0, NO float tolerance):

    (A) E conditional-expectation properties (exact):
      - test E onto h_3(C_u) is unital: E(I_3) == I_3.
      - test E idempotent: E(E(X)) == E(X) for a generic exact-rational h_3(O) element X (with octonion off-diagonals having nonzero e_1..e_6 components so the projection is non-trivial).
      - test E|_A = id: for X in the slice (off-diagonals in C_u = span{1,e_7}), E(X) == X.
      - test E entrywise proj_u: E zeroes octonion components 1..6 of x1,x2,x3 and keeps components 0,7; identity on real diagonal (alpha,beta,gamma).
      - test E positive (spot): for a PSD slice effect a, E(a) is PSD (eigenvalues >= 0); and for a PSD ambient element, E maps into the PSD cone of A (exact or via the C_u 3x3 complex image).

    (B) Ambient CFC square root (exact, non-associative):
      - test sqrt_ambient(a) * sqrt_ambient(a) == a EXACTLY (octonionic 3x3 matrix product) for a slice PSD effect a built as a = c^2 (c in slice, exact entries) — the exact-square trick.
      - test sqrt_ambient is computed via the AMBIENT octonionic product (not the slice complex-matrix sqrt): assert the implementation path uses the octonion matrix multiply.

    (C) Non-associativity exerciser (mandatory):
      - test that there EXISTS an explicit triple of ambient octonionic 3x3 matrices (X,Y,Z) — ideally built from the decisive test data — with (X*Y)*Z != X*(Y*Z) (exact nonzero associator). This proves the computation lives in the genuinely non-associative h_3(O).
      - test that the decisive test elements (sqrt(a), b, sqrt(a)) genuinely exercise non-associativity (b generic in the slice; not all in a common associative corner).

    (D) THE DECISIVE landing-in-slice residual (exact):
      - test the residual R = SP_ambient(a,b) - E(SP_ambient(a,b)) for >=2 slice (a,b) pairs (a = c^2 exact; b generic in the slice, at least one with off-diagonal C_u structure). Write the test to ASSERT THE ACTUAL OUTCOME: the test computes R exactly and the verdict is decided by whether R.equals(zeros). 
        * IMPORTANT (so the test is honest, not rigged toward P): structure the test as a function compute_decisive_residual(a,b) -> (R, is_zero_exact), and assert is_zero_exact is a BOOLEAN consistent with R (R.equals(zeros) <=> is_zero_exact). Then have a single verdict test that records P (all is_zero_exact True) or O (any False) and asserts the verdict is internally consistent across the two routes (see E). Do NOT hardcode "assert is_zero_exact == True" (that would force P); do NOT hardcode "== False" (that would force O). The TRUE outcome is whatever the exact computation gives; the test passes for the true verdict.
      - test the Jordan-product CONTROL: E(a o b) == a o b for slice a,b (a o b = (1/2)(ab+ba) computed in the ambient) — this SHOULD pass (Jordan side preserved) and contrasts with the sequential-product result.

    (E) Independent Peirce/grade cross-check (exact):
      - test that a second route — decomposing SP_ambient(a,b) into C_u-vs-(e_1..e_6) components (and/or Peirce grades at E_11) and reading off whether ker E is populated — yields the SAME is_zero_exact verdict as route (D). Assert agreement (both True or both False).

    Add a module docstring with the ASSERT_CONVENTION line (Jordan o, sequential &, exact arithmetic, Fano e1e2=e4, u=e_7, slice A=h_3(C_u)) and a note that these tests are the VALD-62-01 RED phase.

    The tests MUST fail initially (functions unimplemented) — that is the RED state. Do NOT implement code/embedding_under_E_verification.py in this task.
  </action>
  <verify>
    1. tests/test_embedding_under_E.py created; imports the target functions from code/embedding_under_E_verification.py (which does not yet exist), so the suite is RED (collection/import error or assertion failures) — confirm by running pytest and seeing failures, not passes.
    2. Test groups (A)-(E) all present: (A) E unital/idempotent/E|_A=id/entrywise-proj_u/positive; (B) ambient sqrt self-check (a=c^2 exact-square trick); (C) non-associativity exerciser ((XY)Z != X(YZ)); (D) decisive residual via compute_decisive_residual returning (R, is_zero_exact) with the HONEST assertion pattern (not hardcoded to P or O) + Jordan-product control; (E) independent Peirce/grade cross-check agreeing with (D).
    3. All decisive assertions use EXACT SymPy equality (.equals(zeros)/simplify==0), NO float tolerance anywhere on the decisive path.
    4. The decisive verdict test is HONEST: it does NOT hardcode the residual to be zero (would force P) nor nonzero (would force O); it asserts internal consistency (R.equals(zeros) <=> is_zero_exact) and cross-route agreement, so the suite passes for whichever verdict the true exact computation yields.
    5. Module docstring has the ASSERT_CONVENTION line and the VALD-62-01 RED-phase note; test style mirrors tests/test_slice_clause_iii.py.
  </verify>
  <done>tests/test_embedding_under_E.py exists and is RED (target functions unimplemented): exact-SymPy tests pin E's conditional-expectation properties (unital, idempotent, E|_A=id, entrywise proj_u, positive), the ambient CFC square root self-check (exact-square trick a=c^2), a mandatory non-associativity exerciser ((XY)Z != X(YZ)), the decisive landing-in-slice residual via an HONEST compute_decisive_residual returning (R, is_zero_exact) (not rigged toward P or O), a Jordan-product control (E(a o b)=a o b), and an independent Peirce/grade cross-check that must agree. All decisive assertions exact (no float tolerance).</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Implement exact-SymPy E, ambient non-associative octonionic product + CFC sqrt, the decisive residual + Peirce cross-check; make tests pass; emit the P/O verdict</name>
  <files>code/embedding_under_E_verification.py</files>
  <action>
    Implement code/embedding_under_E_verification.py so tests/test_embedding_under_E.py passes (TDD GREEN). EXACT SymPy throughout; port the octonion arithmetic to exact SymPy (the existing code/octonion_algebra.py is float64 NumPy — reuse its STRUCTURE: Fano table FANO_TRIPLES / _MUL_TABLE with e1e2=e4, the H3O coordinate layout, _mat_mul_h3o entry formulas, proj_u, peirce_V1/Vhalf/V0 — but with SymPy exact entries).

    Module docstring: header with ASSERT_CONVENTION (Jordan o = (1/2)(ab+ba); sequential & = sqrt(a) b sqrt(a) CFC principal; Fano e1e2=e4; u=e_7; slice A=h_3(C_u); EXACT symbolic arithmetic; LIVE-paper provenance) and the VALD-62-01 framing: this computes, on the GENUINELY non-associative h_3(O), whether sqrt(a) b sqrt(a) for slice a,b is preserved by E (P) or leaks (O); a clean obstruction is an acceptable outcome (do NOT force P); EXACT-arithmetic zero-tolerance decisive test; the v11.0 leakage precedent (SP exits M_16(R)) makes (O) unsurprising.

    Implement (mirror the _report/ALL_PASS harness of slice_clause_iii_verification.py):

    (1) Exact SymPy octonion arithmetic:
      - oct_mul(a, b): 8-component exact-SymPy octonion product via the Fano table (e1e2=e4, e_i^2=-1, anticommuting). Port _MUL_TABLE from octonion_algebra.py.
      - oct_conj, and helpers to build octonions with exact (Rational/symbolic) components.
      - h3o_matmul(A, B): the 3x3 octonionic matrix product, entrywise (port _mat_mul_h3o's entry formulas M11..M33 exactly) — each entry a sum of single oct_mul products. CRITICAL: do NOT assume the triple product associates; h3o_matmul(h3o_matmul(X,Y),Z) and h3o_matmul(X,h3o_matmul(Y,Z)) are computed independently and may differ.
      - represent h_3(O) elements either as the H3O coordinate tuple (alpha,beta,gamma,x1,x2,x3) or as a full 3x3 matrix of octonions; pick one and be consistent. (A full 3x3 octonion-matrix representation is cleanest for the non-associative triple product.)

    (2) E onto h_3(C_u) (u=e_7):
      - proj_u_exact(oct): keep components 0 and 7, zero 1..6 (exact). 
      - E(X): apply proj_u_exact to every entry of the 3x3 octonionic Hermitian (equivalently identity on real diagonal, proj_u on off-diagonal octonions). 
      - Exact checks: E(I_3)=I_3; E o E = E (on a generic exact element with nonzero e_1..e_6); E|_A=id (slice element); positivity spot-check (map the slice element to its C_u 3x3 complex matrix and check eigenvalues >= 0, exact or via charpoly).

    (3) Ambient CFC principal square root sqrt_ambient(a):
      - For a slice PSD effect a, compute sqrt(a) in the AMBIENT h_3(O). Since h_3(O) is power-associative (CFC on a single element a is well-defined: sqrt(a) is a polynomial in a and I, hence lies in the associative subalgebra generated by {I, a}), implement via the EXACT-SQUARE TRICK primarily: choose a = c^2 with c a slice element with exact entries, so sqrt(a) = c (principal, PSD) is exact and EITHER in the slice (if c in slice) — note this case: if c is in the slice then sqrt(a)=c is in the slice and the non-associativity must come from the TRIPLE product structure, not the sqrt; ALSO provide a variant where the square root is genuinely computed (e.g. via the spectral/CFC polynomial in a) to confirm sqrt_ambient(a) matches c. 
      - SELF-CHECK: h3o_matmul(sqrt_a, sqrt_a) == a EXACTLY (octonionic product). 
      - IMPORTANT design note (state in code comments): the decisive non-associativity acts in the TRIPLE product sqrt(a) b sqrt(a) = h3o_matmul(h3o_matmul(sqrt_a, b), sqrt_a). Even if sqrt_a and b are in the slice, the octonionic matrix triple product can leave the slice IF the entrywise octonion products generate e_1..e_6 components — this is precisely what the residual test detects. Make BOTH associations available and compare (left vs right association) as part of exercising non-associativity.

    (4) The decisive computation:
      - SP_ambient(a, b) := h3o_matmul(h3o_matmul(sqrt_ambient(a), b), sqrt_ambient(a)) (left association; also compute the right association sqrt_a (b sqrt_a) and record any difference as direct non-associativity evidence).
      - compute_decisive_residual(a, b) -> (R, is_zero_exact): R = SP_ambient(a,b) - E(SP_ambient(a,b)) (entrywise difference of the 3x3 octonion matrices, i.e. the ker-E component of SP_ambient); is_zero_exact = R.equals(zero-matrix) via exact SymPy. Run for >=2 slice (a,b) pairs (a=c^2 exact; b generic slice, >=1 with off-diagonal C_u structure).
      - Jordan-product control: E(a o b) where a o b = (1/2)(h3o_matmul(a,b)+h3o_matmul(b,a)); confirm E(a o b) == a o b (Jordan side preserved) — contrast with the SP residual.

    (5) Independent Peirce/grade cross-check:
      - Decompose SP_ambient(a,b) into C_u-component (proj_u entrywise = E(SP)) vs the e_1..e_6 component (SP - E(SP)); equivalently use peirce_V1/Vhalf/V0 (ported exact) to assign Peirce grades at E_11. Read off is_zero_exact_peirce = (e_1..e_6 component is exactly zero). Assert is_zero_exact_peirce == is_zero_exact from (4). If they disagree, RAISE (do not report a verdict on a split decision).

    (6) Non-associativity exerciser:
      - Exhibit an explicit (X,Y,Z) (from the test data or close to it) with h3o_matmul(h3o_matmul(X,Y),Z) != h3o_matmul(X,h3o_matmul(Y,Z)) (exact nonzero associator), confirming genuine non-associativity. Confirm the decisive (sqrt_a, b, sqrt_a) genuinely exercises it (or document why the chosen elements do).

    (7) Verdict + main():
      - Aggregate: VERDICT = PRESERVATION if all is_zero_exact True (and cross-check agrees); OBSTRUCTION if any is_zero_exact False. 
      - Print the decisive verdict clearly: if PRESERVATION, print "E PRESERVES the sequential product on h_3(O) -> GO toward RESTRICTION embedding lemma (62-03)"; if OBSTRUCTION, print "E does NOT preserve the sequential product -> OBSTRUCTION; leakage in <Peirce grade / e_k>; characterize + PAUSE-2 (62-03)". 
      - Exit 0 if all SELF-CHECKS pass (E-properties, sqrt self-check, non-assoc exerciser, cross-route agreement) — note BOTH P and O are valid scientific outcomes; exit nonzero ONLY on a self-check/consistency failure (e.g. sqrt(a)^2 != a, E not idempotent, routes disagree, or non-associativity not exercised). Do NOT exit nonzero merely because the verdict is O.
      - Make tests/test_embedding_under_E.py pass.

    Reproducibility footer: SymPy version, Python version, deterministic (no random seeds, or explicit seeds if any).
  </action>
  <verify>
    1. code/embedding_under_E_verification.py implemented; tests/test_embedding_under_E.py now PASSES (GREEN) — run pytest and confirm.
    2. Exact-SymPy octonion arithmetic (oct_mul via Fano e1e2=e4) and h3o_matmul (entrywise, triple product NOT assumed associative; left vs right association both available). E onto h_3(C_u) implemented (entrywise proj_u_exact); E(I_3)=I_3, E o E=E, E|_A=id, positivity spot-check all pass EXACTLY.
    3. Ambient CFC sqrt_ambient(a) implemented (exact-square trick a=c^2; plus a spectral/CFC-polynomial confirmation); self-check h3o_matmul(sqrt_a,sqrt_a)==a EXACT; computed via the ambient octonionic product.
    4. THE DECISIVE residual computed exactly for >=2 slice (a,b) pairs via compute_decisive_residual -> (R, is_zero_exact) using exact SymPy equality (NO float). Jordan-product control E(a o b)=a o b passes (Jordan side preserved). 
    5. Independent Peirce/grade cross-check implemented and AGREES with the direct residual verdict (raises on disagreement). Non-associativity exerciser shows explicit (XY)Z != X(YZ) on representative data; the decisive triple product genuinely exercises non-associativity.
    6. Verdict printed clearly (P or O exactly as computed); script exits 0 on self-check success regardless of P/O; exits nonzero only on a self-check/consistency failure. The verdict is NOT forced toward P (no cherry-picking; exact test not relaxed).
    7. EXACT arithmetic on the decisive path confirmed (fp-float-pass rejected); NON-ASSOCIATIVITY guard honored (fp-ignore-nonassociativity, fp-assert-preservation rejected — computation on the genuinely non-associative ambient, not the associative slice); fp-redefine-iii rejected (actual SP sqrt(a) b sqrt(a), not a surrogate).
  </verify>
  <done>code/embedding_under_E_verification.py implemented in exact SymPy: ported octonion arithmetic (Fano e1e2=e4), non-associative h3o_matmul (triple product computed by independent left/right association), E onto h_3(C_u) (entrywise proj_u; unital/idempotent/E|_A=id/positive verified exactly), ambient CFC square root (exact-square trick + spectral confirmation; sqrt(a)^2==a exact), the decisive residual R = sqrt(a) b sqrt(a) - E(sqrt(a) b sqrt(a)) for >=2 slice (a,b) pairs (exact SymPy equality; b generic so non-associativity is exercised), a Jordan-product control (E(a o b)=a o b passes), an independent Peirce/grade cross-check agreeing with the direct verdict, and a non-associativity exerciser ((XY)Z != X(YZ)). The decisive verdict (P or O) is printed exactly as computed and NOT forced; tests/test_embedding_under_E.py passes (GREEN); script exits 0 on self-check success for either verdict. fp-assert-preservation, fp-ignore-nonassociativity, fp-float-pass, fp-force-positive, fp-redefine-iii all rejected.</done>
</task>

<task type="auto">
  <name>Task 3: Record §4 (the decisive computation + residual + verdict on the fork) in embedding-under-E.md; cite the code/tests; state P or O exactly as computed</name>
  <files>derivations/p5-basin-restriction/embedding-under-E.md</files>
  <action>
    Append §4 to derivations/p5-basin-restriction/embedding-under-E.md — the DECISIVE COMPUTATION and its verdict on the obstruction-or-preservation fork. This records the VALD-62-01 result analytically and hands the verdict to 62-03. Do NOT modify §1-§3 (62-01's setup) except to update the "§4 to follow" marker to "§4 below; §5 to follow (62-03)".

    §4 — THE DECISIVE COMPUTATION ON THE NON-ASSOCIATIVE h_3(O) (VALD-62-01):

    (4.1) Setup recap (1 paragraph): the decisive question from §3.3 — for slice a,b, sqrt(a) computed by CFC in the ambient h_3(O), is E(sqrt(a) b sqrt(a)) = sqrt(a) b sqrt(a) (preservation) or not (leakage)? Computed by exact SymPy in code/embedding_under_E_verification.py + tests/test_embedding_under_E.py.

    (4.2) What was computed (cite the code precisely): 
      - E onto h_3(C_u) built exactly (entrywise proj_u, u=e_7); E-properties verified EXACTLY (unital E(I_3)=I_3; idempotent E o E=E; E|_A=id; positive on test PSD). 
      - Ambient CFC square root sqrt_ambient(a) (exact-square trick a=c^2 + spectral confirmation); sqrt(a)^2==a exact in the ambient octonionic product. 
      - Non-associativity exercised: explicit (XY)Z != X(YZ) on representative ambient octonionic matrices (give the associator or its norm/nonzero components); confirm the decisive (sqrt_a,b,sqrt_a) genuinely exercises it. 
      - The decisive residual R = sqrt(a) b sqrt(a) - E(sqrt(a) b sqrt(a)) for >=2 slice (a,b) pairs, exact. 
      - Jordan-product control: E(a o b)=a o b (Jordan side preserved) — contrast. 
      - Independent Peirce/grade cross-check agreeing.

    (4.3) THE VERDICT (state exactly as the computation yields — do NOT force):
      - IF PRESERVATION (R == 0 exactly for the non-associativity-exercising elements, both routes agree): state "E PRESERVES the sequential product on the actual non-associative h_3(O): sqrt(a) b sqrt(a) lands in the slice exactly; E(sqrt(a) b sqrt(a)) = sqrt(a) b sqrt(a)." This is the (P) branch -> 62-03 states + proves the RESTRICTION embedding lemma. Note the result is non-trivial (the Jordan-product preservation did NOT a priori entail it; non-associativity was genuinely exercised).
      - IF OBSTRUCTION (R != 0 exactly for some slice a,b, both routes agree): state "E does NOT preserve the sequential product on h_3(O): sqrt(a) b sqrt(a) LEAKS out of the slice; the residual R = sqrt(a) b sqrt(a) - E(sqrt(a) b sqrt(a)) is exactly nonzero." CHARACTERIZE the leakage: which Peirce grade (V_0/V_{1/2}/V_1 at E_11) and which e_k (in e_1..e_6 = ker E) the residual populates; give the exact residual (or its salient nonzero entries). This is the (O) branch -> 62-03 characterizes the obstruction precisely (the exact clause-(iii) structure — the product-form sequential product — that E cannot induce from the non-associative ambient) + the minimal extra input, and the milestone PAUSES (claim.md PAUSE condition 2). State explicitly: a clean (O) is an acceptable DELIVERABLE, NOT a failure; the v11.0/Phase 42 precedent (SP exits M_16(R)) makes (O) consonant with prior results.
      - State which branch the computation actually took, with the exact evidence.

    (4.4) Why the verdict is trustworthy (the independent-cross-check + exact-arithmetic discipline):
      - Two independent routes (direct E-residual; Peirce/grade-component) agree. 
      - EXACT arithmetic (SymPy), zero-tolerance: R==0 means exactly zero (preservation), R!=0 means exactly nonzero (genuine non-associativity leakage, NOT round-off) — fp-float-pass rejected. 
      - The exact-square trick (a=c^2) keeps arithmetic exact WITHOUT trivializing non-associativity (b generic; the non-associativity exerciser passed) — fp-ignore-nonassociativity rejected. 
      - The Jordan-product control passed (E(a o b)=a o b), confirming the test apparatus is correct and isolating the sequential product as the operation tested (fp-redefine-iii rejected — actual SP, not a surrogate). 
      - The computation is on the genuinely non-associative ambient, not the associative slice (fp-assert-preservation rejected).

    (4.5) HANDOFF to 62-03: state that the verdict (P or O) and, if O, the characterized leakage (Peirce grade + e_k directions + exact residual), are the decisive input to 62-03's reading of the fork. Update the §5-to-follow marker.

    Add a TYPE/CATEGORY + PEIRCE self-audit for §4: residual tagged in ker E / its Peirce grade; verdict tagged P or O; no forcing.

    CONFIDENCE marker for §4: HIGH if all self-checks passed and both routes agree (the verdict — whichever it is — is exact and cross-validated); flag any residual uncertainty (e.g. the generality question: does the verdict hold for ALL slice a,b or only the tested representatives? — note whether a general symbolic argument or only specific exact pairs were used, and carry any generality caveat to 62-03).
  </action>
  <verify>
    1. §4 appended to embedding-under-E.md; §1-§3 unmodified except the "§4 to follow" marker updated. (4.1) recaps the decisive question; (4.2) cites code/embedding_under_E_verification.py + tests/test_embedding_under_E.py and lists what was computed (E-properties exact, ambient sqrt, non-assoc exerciser, decisive residual, Jordan control, Peirce cross-check).
    2. (4.3) states the VERDICT exactly as computed — P (R==0, lands in slice) or O (R!=0, leaks) — with the actual branch taken and exact evidence; if O, the leakage is CHARACTERIZED (Peirce grade + e_k in ker E + exact residual); the (P)->RESTRICTION-lemma / (O)->obstruction+PAUSE-2 routing to 62-03 is stated; (O) flagged as acceptable; v11.0 precedent noted.
    3. (4.4) justifies trustworthiness: two routes agree; exact arithmetic zero-tolerance (fp-float-pass rejected); exact-square trick non-trivializing (fp-ignore-nonassociativity rejected); Jordan-product control passed (fp-redefine-iii rejected); ambient non-associative computation (fp-assert-preservation rejected).
    4. (4.5) hands the verdict (+ characterized leakage if O) to 62-03; §5-to-follow marker updated. Type/category + Peirce self-audit present; CONFIDENCE marker present with any generality caveat flagged.
    5. The verdict in §4 is consistent with the code's printed verdict and the test outcomes (no discrepancy between the prose and the computation).
  </verify>
  <done>§4 of derivations/p5-basin-restriction/embedding-under-E.md records the decisive computation (VALD-62-01): E built and verified exactly, ambient CFC sqrt with self-check, non-associativity genuinely exercised, the exact residual R = sqrt(a) b sqrt(a) - E(sqrt(a) b sqrt(a)) for >=2 slice pairs, a passing Jordan-product control, and an agreeing independent Peirce/grade cross-check. The verdict on the obstruction-or-preservation fork is stated EXACTLY as computed (P: lands in slice -> RESTRICTION lemma in 62-03; O: leaks -> characterize obstruction [Peirce grade + e_k + exact residual] + minimal extra input + PAUSE-2 in 62-03), with (O) flagged acceptable and the v11.0 precedent noted. Trustworthiness justified (two agreeing routes, exact zero-tolerance arithmetic, non-trivializing exact-square trick, passing Jordan control, genuinely non-associative ambient). The verdict + characterized leakage are handed to 62-03. fp-assert-preservation, fp-ignore-nonassociativity, fp-float-pass, fp-force-positive, fp-redefine-iii all rejected; verdict not forced.</done>
</task>

</tasks>

<verification>
- E EXACT (VALD): E onto h_3(C_u) verified by exact SymPy — unital, idempotent, E|_A=id, positive (test-E-properties-exact).
- AMBIENT SQRT EXACT: sqrt_ambient(a)^2==a in the octonionic product (exact-square trick a=c^2 + spectral confirmation); computed in the 27-dim non-associative ambient (test-ambient-sqrt-exact).
- NON-ASSOCIATIVITY EXERCISED: explicit (XY)Z != X(YZ) on representative data; decisive triple product genuinely non-associative (test-nonassociativity-exercised; NON-ASSOCIATIVITY guard honored).
- DECISIVE RESIDUAL: R = sqrt(a) b sqrt(a) - E(sqrt(a) b sqrt(a)) computed EXACTLY for >=2 slice (a,b) pairs; verdict P (R==0) or O (R!=0) read off by exact equality; Jordan-product control E(a o b)=a o b passes (test-decisive-residual).
- INDEPENDENT CROSS-CHECK: Peirce/grade-component route agrees with the direct E-residual verdict (test-peirce-crosscheck); a split decision is a red flag, not a verdict.
- EXACT ARITHMETIC: decisive residual via exact SymPy only, no float64 (fp-float-pass rejected); exact-square trick documented and non-trivializing (test-exact-arithmetic).
- ERROR BUDGET: zero-tolerance decisive test — exact residual ==0 => preservation, exact residual !=0 => leakage; no float tolerance on the verdict.
- VERDICT NOT FORCED: P or O exactly as computed; (O) reported honestly + acceptable (PAUSE-2); no cherry-picking a,b, no relaxing the exact test; v11.0 precedent makes (O) unsurprising (test-verdict-not-forced; fp-force-positive rejected).
- ANCHOR DISCIPLINE: ref-effros-stormer (E is a Jordan conditional expectation, verified), ref-lem-bottleneck (slice + Peirce coordinates), ref-paper5-def1 (the SP is clause iii's datum), ref-vald-61-01 (reused exact-SymPy templates, but the decisive path is AMBIENT not associative), ref-v11-leakage (the obstruction precedent) all surfaced.
- HANDOFF: the verdict (+ characterized leakage if O) is the decisive input to 62-03, wired via contract links.
</verification>

<success_criteria>
On the GENUINELY non-associative h_3(O), exact-arithmetic SymPy (code/embedding_under_E_verification.py + tests/test_embedding_under_E.py, VALD-62-01) decides whether the self-modeling sequential product sqrt(a) b sqrt(a) (a,b in the slice A=h_3(C_u); sqrt(a) computed by CFC in the ambient) is INDUCED/consistent under E (preservation P: exact residual R = sqrt(a) b sqrt(a) - E(sqrt(a) b sqrt(a)) == 0, lands in slice) or LEAKS (obstruction O: exact R != 0, characterized by Peirce grade + e_k in ker E). E onto h_3(C_u) (u=e_7) is built and verified exactly (unital, idempotent, E|_A=id, positive). The ambient CFC square root is exact (a=c^2 exact-square trick + spectral confirmation; sqrt(a)^2==a). The non-associativity is genuinely exercised ((XY)Z != X(YZ) demonstrated; NON-ASSOCIATIVITY guard honored). An independent Peirce/grade-component cross-check AGREES with the direct E-residual verdict. The Jordan-product control E(a o b)=a o b passes (isolating the sequential product as the operation tested). The decisive residual uses EXACT arithmetic with zero tolerance (a nonzero residual is genuine non-associativity leakage, not round-off). §4 of embedding-under-E.md records the verdict EXACTLY as computed (P -> RESTRICTION lemma in 62-03; O -> precise obstruction [Peirce grade + e_k + exact residual] + minimal extra input + PAUSE-2 in 62-03), with (O) flagged as an acceptable deliverable and the v11.0 precedent (SP exits M_16(R)) noted as making (O) unsurprising; the verdict + characterized leakage are handed to 62-03. The plan rejects fp-assert-preservation (computation on the actual non-associative ambient, not the associative slice or a hand-wave), fp-ignore-nonassociativity (non-associativity genuinely exercised), fp-float-pass (exact arithmetic on the verdict), fp-force-positive (verdict not forced; (O) honest and acceptable), and fp-redefine-iii (actual SP sqrt(a) b sqrt(a), not a surrogate).
</success_criteria>

<output>
After completion, create `.gpd/phases/62-coherent-embedding-under-e-the-hard-part/62-02-SUMMARY.md`.
</output>
