---
phase: 56-thm-5-8-upper-bound-w-carries-product-form-sequential-product
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/thm-5-8-identity-verbatim.md
  - .gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/downstream-consumer-scan.md
  - .gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/w-face-status.md
  - .gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/carries-senses.md
  - .gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/sympy-design.md
  - .gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/56-01-SUMMARY.md
interactive: true

conventions:
  units: "N/A (pure algebra; OUS is order-theoretic, not metric)"
  metric: "N/A"
  fourier_convention: "N/A"
  coupling_convention: "N/A"
  renormalization_scheme: "N/A"
  gauge_choice: "N/A"
  sequential_product_symbol: "a ∘ b (Paper 5) ≡ a & b (vdW)"
  compression_symbol: "C_p"
  order_unit_space: "finite-dim archimedean OUS over ℝ with distinguished unit 1"
  peirce_2_space: "V_2(p_i) := range(C_{p_i})"
  peirce_1_space: "V_1(p_i,p_j) := (C_{p_i}+C_{p_j})V − C_{p_i}V − C_{p_j}V"
  as_2001: "Alfsen-Shultz 2001, Birkhäuser PM 179 (C*-algebra volume)"
  as_2003: "Alfsen-Shultz 2003, Birkhäuser PM 190 (Ch. 1-8 pre-Jordan-legal; Ch. 9 FORBIDDEN per Phase 55 Flag 4.1)"
  allowed_axiom_scope: "{S0, S1-S7, linearity of L_a, A-S compression axioms Ch. 2/7/8, finite-dim spectrality}"
  W_definition: "W := span_ℝ{a_i ⊗ b_j : a_i basis V_B, b_j basis V_M} inside V_{BM} (composite-lt.tex L204)"
  frozen_file: "main-jmp-submitted.tex is FROZEN at git tag paper5-jmp-submitted; ALL Phase 56 file writes target the LIVING main.tex + sections/*.tex"
  forbidden_tokens_outside_transcription:
    - "Thm 9.37"
    - "AlfsenShultz.*Ch.~9"
    - "Luders"
    - "Lüders"
    - "M_n(C)^{sa}"
    - "Hanche-Olsen"

dimensional_check: {}

approximations: []

contract:
  schema_version: 1
  scope:
    question: "Extract the exact Thm 5.8 upper-bound identity from the living Paper 5, scan §5/§6 downstream consumers of `thm:local-tomo` to identify which of the three 'carries' senses each requires, resolve whether W is a face of V_{BM} (decisive for routing), formalize the three 'carries' senses in a reusable artifact, and design the SymPy H_3(ℝ) ⊗ H_3(ℝ) spot-check. Close with a human-decision checkpoint that locks the routing for Plan 56-02 (face-restriction vs direct S1-S7 via vdW 2019 Def. 4)."
  claims:
    - id: claim-identity-extracted
      statement: "The 'Thm 5.8 upper bound' is mapped to `thm:local-tomo` upper-bound step (composite-lt.tex L203-221) + `thm:lt-full` upper-bound step (appendix-proofs.tex L228-238). The exact identity ('W carries the product-form SP' and the closure equation `(a_i ⊗ b_j) ∘ (a_k ⊗ b_l) = (a_i ∘ a_k) ⊗ (b_j ∘ b_l)`) is quoted verbatim with file:line citations, and the ambiguity that no `\\label{thm:5.8}` exists in main-jmp-submitted.tex is explicitly recorded."
      deliverables: [deliv-identity-md]
      acceptance_tests: [test-identity-verbatim, test-identity-line-numbers, test-identity-ambiguity-flagged]
      references: [ref-composite-lt, ref-appendix-proofs, ref-main-living, ref-main-submitted]
    - id: claim-downstream-scan
      statement: "Every §5/§6 consumer of `thm:local-tomo` (and, transitively, of 'W carries the product-form SP') in the living Paper 5 is located (composite-lt.tex, type-exclusion.tex, discussion.tex, appendix-proofs.tex) and classified by which of the three 'carries' senses it requires. At minimum, the `sms:minimal` clause in composite-lt.tex is identified as requiring sense (b). Cases where sense (c) would be cleaner are flagged."
      deliverables: [deliv-consumer-scan-md]
      acceptance_tests: [test-consumer-scan-coverage, test-consumer-sense-assignment]
      references: [ref-composite-lt, ref-type-exclusion, ref-discussion, ref-appendix-proofs]
    - id: claim-face-status
      statement: "W's face status in V_{BM} is resolved to one of {IS-FACE, NOT-FACE, INCONCLUSIVE-DEFAULT-TO-NOT-FACE} by explicit application of the A-S 2003 Ch. 1 face definition (order-convex + hereditary under ≤) OR by exhibiting a concrete non-face witness (v ∈ V_{BM}^+, 0 ≤ w ≤ v, v ∈ W, w ∉ W). The verdict is evidence-backed; default expectation is NOT-FACE unless proved otherwise. The verdict routes Plan 56-02: IS-FACE ⟹ face-restriction shortcut available; NOT-FACE or INCONCLUSIVE ⟹ direct S1-S7 on W via vdW 2019 Def. 4 + Thm 1 (the recommended primary path)."
      deliverables: [deliv-face-status-md]
      acceptance_tests: [test-face-status-verdict, test-face-status-evidence]
      references: [ref-as-2003, ref-vdw-2019, ref-composite-lt]
    - id: claim-carries-senses-formalized
      statement: "The three 'carries' senses are formally stated in `carries-senses.md` as a reusable artifact. (a) set-closure: `∘([0,1]_W × [0,1]_W) ⊆ W`. (b) induced-structure: `(W, ≤|_W, 1_W, ∘|_W)` satisfies vdW 2019 Def. 2 (S1-S7). (c) functorial: `ι: W ↪ V_{BM}` is an SPS-morphism. The collapse diagram (b)⇒(a), (c)⇒(b) is stated, and the non-collapse witnesses ((a)⇏(b) via Gudder-Greechie 2002 Ex. 39; (b)⇏(c) via unit-choice subtlety) are named."
      deliverables: [deliv-carries-senses-md]
      acceptance_tests: [test-carries-three-senses-present, test-carries-collapse-diagram]
      references: [ref-vdw-2019, ref-gudder-greechie, ref-bgw-2020]
    - id: claim-sympy-design
      statement: "The SymPy H_3(ℝ) ⊗ H_3(ℝ) spot-check is DESIGNED (not yet executed) with: (i) an explicit resolution of the 'H_3(ℝ) wedge component (3-dim antisymmetric)' roadmap ambiguity in favor of the Peirce-1-space off-diagonal 3-dim subspace of H_3(ℝ) w.r.t. diagonal rank-1 projectors (OR explicit user override during the Task-6 checkpoint); (ii) test cases for sense (a) closure, S1 (additivity), S3 (unitality), and S4 (orthogonality symmetry) on W ⊆ H_3(ℝ) ⊗ H_3(ℝ); (iii) one explicit negative test attempting a sense-(a) counterexample (must fail, confirming sense (a) holds by construction); (iv) reuse of Phase 54 closeout-sympy.py `compress(B,i,n)` and `seq_prod(...)` helpers; (v) runtime budget < 30 sec; (vi) exit-code-0 PASS criterion."
      deliverables: [deliv-sympy-design-md]
      acceptance_tests: [test-sympy-design-has-wedge-resolution, test-sympy-design-has-axiom-checks, test-sympy-design-reuses-phase54]
      references: [ref-phase54-closeout-sympy, ref-phase55-s4-sympy]
    - id: claim-routing-checkpoint
      statement: "A `checkpoint:human-verify` task presents face-status verdict, downstream-consumer sense map, three-'carries'-senses formalization, and SymPy design to the user, and asks for EXPLICIT confirmation of: (1) which 'carries' sense Phase 56 targets (default: sense (b), upgrade to (c) if cheap); (2) face-status handling (default: proceed with direct S1-S7 on W via vdW 2019 Def. 4 regardless); (3) H_3(ℝ) 'wedge component' interpretation (default: Peirce-1 off-diagonal 3-dim). The user's confirmation is recorded in 56-01-SUMMARY.md's hand-off block and locks the Plan 56-02 path."
      deliverables: [deliv-summary-with-handoff]
      acceptance_tests: [test-checkpoint-user-confirmed, test-handoff-routing-locked]
      references: [ref-research-md]
  deliverables:
    - id: deliv-identity-md
      kind: note
      path: ".gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/thm-5-8-identity-verbatim.md"
      description: "Verbatim quote of the Thm 5.8 upper-bound identity from living composite-lt.tex L162-222 (full `thm:local-tomo`) + L203-221 (upper-bound step specifically) + living appendix-proofs.tex L164-243 (full `thm:lt-full`) + L228-238 (upper-bound step specifically), with file:line citations, `\\label` mapping (roadmap 'Thm 5.8' → `thm:local-tomo` upper-bound), and an explicit note that no `\\label{thm:5.8}` exists in main-jmp-submitted.tex."
      must_contain:
        - "Section 1: Mapping from roadmap 'Thm 5.8' to LaTeX label `thm:local-tomo`"
        - "Section 2: Verbatim composite-lt.tex L203-221 quote (upper-bound step)"
        - "Section 3: Verbatim appendix-proofs.tex L228-238 quote (upper-bound step)"
        - "Section 4: Full `thm:local-tomo` context quote (composite-lt.tex L162-222)"
        - "Section 5: Full `thm:lt-full` context quote (appendix-proofs.tex L164-243)"
        - "Section 6: Ambiguity note — `\\label{thm:5.8}` does NOT exist in main-jmp-submitted.tex; 'Thm 5.8' is informal shorthand"
        - "Section 7: Instances of 'carries' / 'inherits' / 'closed' language in the quoted text (token inventory for R7 analysis)"
        - "Every quote is followed by its `file:line-range` citation"
    - id: deliv-consumer-scan-md
      kind: note
      path: ".gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/downstream-consumer-scan.md"
      description: "Per-file scan of the living Paper 5 for consumers of `thm:local-tomo` / `V_{BM}` / 'product-form SP' / 'carries' / 'inherits' language. For each hit, record: file:line, verbatim context (2-4 lines), classification by required 'carries' sense (a/b/c), and rationale. At minimum the `sms:minimal` minimality clause is covered and tagged (b). Count summary at end."
      must_contain:
        - "Section 1: Grep pattern list (tokens: `thm:local-tomo`, `V_{BM}`, `product-form`, `carries`, `inherits`, `sms:minimal`)"
        - "Section 2: composite-lt.tex hits table (file:line | quote | required sense | rationale)"
        - "Section 3: type-exclusion.tex hits table"
        - "Section 4: discussion.tex hits table"
        - "Section 5: appendix-proofs.tex hits table"
        - "Section 6: Consumer-by-sense summary (counts of (a) / (b) / (c) / unclassified)"
        - "Section 7: `sms:minimal` explicit classification (must be sense (b))"
        - "Section 8: Recommendation for Plan 56-02 target sense (sense (b), with (c) upgrade if downstream wants it cheaply)"
    - id: deliv-face-status-md
      kind: note
      path: ".gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/w-face-status.md"
      description: "Resolution of whether W = span{a_i ⊗ b_j} is a face of V_{BM} via A-S 2003 Ch. 1 face definition. Verdict in {IS-FACE, NOT-FACE, INCONCLUSIVE-DEFAULT-TO-NOT-FACE}, with evidence (for IS-FACE: order-convexity check + hereditary check against a general v ∈ V_{BM}^+; for NOT-FACE: concrete witness v ∈ V_{BM}^+ \\ W with 0 ≤ w ≤ v, v ∈ W, w ∉ W OR v ∉ W with v ≤ u ∈ W^+). Routing implication spelled out."
      must_contain:
        - "Section 1: Face definition (A-S 2003 Ch. 1, order-convex + hereditary)"
        - "Section 2: Candidate witness or proof strategy"
        - "Section 3: Verdict ∈ {IS-FACE, NOT-FACE, INCONCLUSIVE-DEFAULT-TO-NOT-FACE}"
        - "Section 4: Evidence (witness construction OR A-S proof of both axioms)"
        - "Section 5: Routing implication — IS-FACE enables face-restriction shortcut; NOT-FACE routes through direct S1-S7 via vdW 2019 Def. 4"
        - "Section 6: Recommendation to Plan 56-02 (regardless of verdict: proceed with direct S1-S7 via vdW 2019 Def. 4 as primary; face-restriction is pure bonus)"
    - id: deliv-carries-senses-md
      kind: note
      path: ".gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/carries-senses.md"
      description: "Reusable artifact formally defining the three 'carries' senses (a)/(b)/(c). Reuses the RESEARCH.md §'Three Senses of Carries' section with explicit formulas, collapse diagram, and non-collapse witnesses (Gudder-Greechie 2002 Ex. 39 for (a)⇏(b); unit-choice subtlety for (b)⇏(c)). Consumed by Plan 56-02 and Plan 56-03."
      must_contain:
        - "Section 1: Formal definition of sense (a) — set-closure"
        - "Section 2: Formal definition of sense (b) — induced-structure SPS (W, ≤|_W, 1_W, ∘|_W) satisfies vdW 2019 Def. 2"
        - "Section 3: Formal definition of sense (c) — ι: W ↪ V_{BM} is an SPS-morphism"
        - "Section 4: Collapse diagram: (b)⇒(a), (c)⇒(b)"
        - "Section 5: Non-collapse witnesses: (a)⇏(b) via Gudder-Greechie 2002 Ex. 39; (b)⇏(c) via unit-choice"
        - "Section 6: Which sense each §5/§6 downstream consumer requires (pointer to downstream-consumer-scan.md)"
        - "Section 7: Minimum sense needed for `sms:minimal` minimality clause = sense (b)"
    - id: deliv-sympy-design-md
      kind: note
      path: ".gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/sympy-design.md"
      description: "Design document for the Plan 56-02 SymPy closeout on H_3(ℝ) ⊗ H_3(ℝ). Specifies: (i) interpretation of the roadmap 'H_3(ℝ) wedge component (3-dim antisymmetric)' phrase (default: Peirce-1-space off-diagonal 3-dim); (ii) concrete test subspace W_test ⊆ H_3(ℝ) ⊗ H_3(ℝ); (iii) axiom checks (S1, S3, S4) + closure test (sense (a)) + one negative test; (iv) reuse of Phase 54 `closeout-sympy.py` helpers; (v) runtime budget < 30 sec; (vi) exit-code-0 PASS criterion; (vii) FAIL diagnosis path (counterexample routing). NO CODE EXECUTED in this task; only design."
      must_contain:
        - "Section 1: 'Wedge component' ambiguity resolution (Peirce-1 off-diagonal 3-dim default; explicit user override path)"
        - "Section 2: Concrete W_test specification (basis, ambient dimension, projective units)"
        - "Section 3: Test cases (S1, S3, S4, closure, negative test)"
        - "Section 4: Reuse plan for Phase 54 `compress(B,i,n)` and `seq_prod(...)`"
        - "Section 5: Runtime budget + exit-code-0 PASS criterion"
        - "Section 6: FAIL routing: counterexample triggers Gudder-Greechie-style Approach 3 (Phase 56 outcome (C))"
    - id: deliv-summary-with-handoff
      kind: note
      path: ".gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/56-01-SUMMARY.md"
      description: "Plan 56-01 summary per the GPD summary template. Includes Wave-1 outputs + user-confirmed routing for Plan 56-02 + explicit hand-off block listing: (1) target 'carries' sense for Phase 56 (default (b)+(c)); (2) face-status verdict; (3) SymPy design filename; (4) Plan 56-02 approach (direct S1-S7 via vdW 2019 Def. 4 + Thm 1, regardless of face status, by researcher recommendation)."
      must_contain:
        - "Outcome: CLASSIFICATION + DESIGN COMPLETE"
        - "Hand-off block for Plan 56-02 with explicit routing"
        - "User-confirmed targets (sense, face-status handling, wedge-component interpretation)"
        - "Pointers to all five Wave-1 artifacts"
  references:
    - id: ref-composite-lt
      kind: prior_artifact
      locator: "/Users/ehrlich/repos/blog/landing/papers/qm-from-self-modeling/sections/composite-lt.tex (living; 265 lines; `thm:local-tomo` at L162)"
      aliases: ["composite-lt.tex"]
      role: must_consider
      why_it_matters: "Primary source of the Thm 5.8 upper-bound identity; L203-221 is the upper-bound step; `prop:inheritance` at L66 is reused for S4-on-W in Plan 56-02."
      applies_to: [claim-identity-extracted, claim-downstream-scan, claim-face-status]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-appendix-proofs
      kind: prior_artifact
      locator: "/Users/ehrlich/repos/blog/landing/papers/qm-from-self-modeling/sections/appendix-proofs.tex (living; 254 lines; `thm:lt-full` at L164)"
      aliases: ["appendix-proofs.tex"]
      role: must_consider
      why_it_matters: "Full `thm:lt-full` proof; L228-238 is the upper-bound step of appendix proof."
      applies_to: [claim-identity-extracted, claim-downstream-scan]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-main-living
      kind: prior_artifact
      locator: "/Users/ehrlich/repos/blog/landing/papers/qm-from-self-modeling/main.tex (living; target for Plan 56-03 revision integration)"
      aliases: ["Paper 5 living main.tex"]
      role: must_consider
      why_it_matters: "Living revision target; `\\ref{ax:S0}` + `\\ref{lem:peirce-preservation}` resolve here."
      applies_to: [claim-identity-extracted, claim-downstream-scan]
      carry_forward_to: [planning]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-main-submitted
      kind: prior_artifact
      locator: "/Users/ehrlich/repos/blog/landing/papers/qm-from-self-modeling/main-jmp-submitted.tex (FROZEN at git tag paper5-jmp-submitted)"
      aliases: ["main-jmp-submitted.tex", "frozen baseline"]
      role: benchmark
      why_it_matters: "Frozen submitted baseline; MUST NOT be modified; used only for zero-diff verification."
      applies_to: [claim-identity-extracted]
      carry_forward_to: [planning, verification, execution]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-type-exclusion
      kind: prior_artifact
      locator: "/Users/ehrlich/repos/blog/landing/papers/qm-from-self-modeling/sections/type-exclusion.tex (living)"
      aliases: ["type-exclusion.tex"]
      role: must_consider
      why_it_matters: "§6 downstream consumer of V_{BM} for real/quaternionic exclusion; sense requirement to be classified."
      applies_to: [claim-downstream-scan]
      carry_forward_to: [planning]
      must_surface: true
      required_actions: [read, use]
    - id: ref-discussion
      kind: prior_artifact
      locator: "/Users/ehrlich/repos/blog/landing/papers/qm-from-self-modeling/sections/discussion.tex (living)"
      aliases: ["discussion.tex"]
      role: must_consider
      why_it_matters: "Potential §7 downstream consumer; must be scanned for `thm:local-tomo` uses."
      applies_to: [claim-downstream-scan]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [read, use]
    - id: ref-vdw-2019
      kind: paper
      locator: "van de Wetering, J., 'Sequential product spaces are Jordan algebras', J. Math. Phys. 60, 062201 (2019), arXiv:1803.11139"
      aliases: ["vdW 2019"]
      role: definition
      why_it_matters: "Def. 2 (S1-S7); Def. 4 (locally tomographic composite — PRIMARY path for Plan 56-02); Thm 1 (finite-dim SPS → EJA)."
      applies_to: [claim-identity-extracted, claim-carries-senses-formalized, claim-face-status]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-as-2003
      kind: paper
      locator: "Alfsen, E. M. & Shultz, F. W., Geometry of State Spaces of Operator Algebras, Birkhäuser PM 190, 2003"
      aliases: ["A-S 2003", "AlfsenShultz2003"]
      role: method
      why_it_matters: "Ch. 1 face definition (order-convex + hereditary); Thm 1.23 state separation. Ch. 9 FORBIDDEN."
      applies_to: [claim-face-status]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [cite, compare]
    - id: ref-gudder-greechie
      kind: paper
      locator: "Gudder, S. & Greechie, R., 'Sequential products on effect algebras', Rep. Math. Phys. 49, 87-111 (2002)"
      aliases: ["Gudder-Greechie 2002"]
      role: must_consider
      why_it_matters: "Example 39: associative non-EJA SEA; witness for (a)⇏(b) in the carries-senses collapse diagram. AXIOM-STATED-IN-SECONDARY-SOURCE tier per Phase 55 discipline."
      applies_to: [claim-carries-senses-formalized]
      carry_forward_to: [planning]
      must_surface: true
      required_actions: [cite]
    - id: ref-bgw-2020
      kind: paper
      locator: "Barnum, H., Graydon, M. A., Wilce, A., 'Composites and Categories of Euclidean Jordan Algebras', Quantum 4, 359 (2020), arXiv:1606.09331"
      aliases: ["BGW 2020"]
      role: must_consider
      why_it_matters: "Sense-(c) SPS-morphism machinery; completely Jordan-preserving maps."
      applies_to: [claim-carries-senses-formalized]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [cite]
    - id: ref-phase54-lemma
      kind: prior_artifact
      locator: "derivations/paper5-peirce-preservation/claim.md AND .gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md"
      aliases: ["Phase 54 Peirce-Preservation Lemma", "lem:peirce-preservation"]
      role: definition
      why_it_matters: "Peirce-Preservation Lemma; cited by `\\ref{lem:peirce-preservation}` + `\\ref{ax:S0}` for any factor-level Peirce invariance invocation in Phase 56 downstream work."
      applies_to: [claim-downstream-scan]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-phase55-result
      kind: prior_artifact
      locator: ".gpd/phases/55-s4-facial-structure-lemma/55-RESULT.md"
      aliases: ["Phase 55 RESULT", "Phase 55 C-i seal"]
      role: benchmark
      why_it_matters: "Locks A-S citation discipline (bracketed, Ch. ≤ 8); Phase 56 inherits. Flag 4.1 forbids Ch. 9."
      applies_to: [claim-identity-extracted, claim-face-status]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-phase54-closeout-sympy
      kind: prior_artifact
      locator: "derivations/paper5-peirce-preservation/closeout-sympy.py"
      aliases: ["Phase 54 closeout-sympy.py"]
      role: method
      why_it_matters: "Reusable `compress(B,i,n)` + `seq_prod(...)` helpers; runtime 0.013s baseline."
      applies_to: [claim-sympy-design]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [read, use]
    - id: ref-phase55-s4-sympy
      kind: prior_artifact
      locator: "derivations/paper5-peirce-preservation/s4-sympy-spot-check.py"
      aliases: ["Phase 55 s4-sympy-spot-check.py"]
      role: method
      why_it_matters: "Phase 55 two-mixing-function verification pattern; symbolic-exact discipline."
      applies_to: [claim-sympy-design]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [read, use]
    - id: ref-research-md
      kind: prior_artifact
      locator: ".gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/56-RESEARCH.md (656 lines)"
      aliases: ["Phase 56 RESEARCH"]
      role: definition
      why_it_matters: "Primary research input; Open Questions 1-4 scoped into Plan 56-01 tasks."
      applies_to: [claim-identity-extracted, claim-downstream-scan, claim-face-status, claim-carries-senses-formalized, claim-sympy-design, claim-routing-checkpoint]
      carry_forward_to: [planning, execution]
      must_surface: true
      required_actions: [read, cite]
  acceptance_tests:
    - id: test-identity-verbatim
      subject: claim-identity-extracted
      kind: consistency
      procedure: "For each quote in thm-5-8-identity-verbatim.md Sections 2-5, grep the referenced file:line range in composite-lt.tex or appendix-proofs.tex and verify verbatim match (modulo whitespace)."
      pass_condition: "Every quote matches verbatim at the cited file:line range; zero mismatches."
      evidence_required: [deliv-identity-md, ref-composite-lt, ref-appendix-proofs]
      automation: hybrid
    - id: test-identity-line-numbers
      subject: claim-identity-extracted
      kind: existence
      procedure: "Read thm-5-8-identity-verbatim.md; verify every quote has a `file:line-range` citation immediately before or after."
      pass_condition: "Zero uncited quotes."
      evidence_required: [deliv-identity-md]
      automation: hybrid
    - id: test-identity-ambiguity-flagged
      subject: claim-identity-extracted
      kind: existence
      procedure: "Read thm-5-8-identity-verbatim.md Section 6; verify it explicitly states that no `\\label{thm:5.8}` exists in main-jmp-submitted.tex and maps 'Thm 5.8' to `thm:local-tomo`."
      pass_condition: "Section 6 is non-empty, non-handwaving, and contains the mapping + ambiguity statement."
      evidence_required: [deliv-identity-md]
      automation: human
    - id: test-consumer-scan-coverage
      subject: claim-downstream-scan
      kind: existence
      procedure: "grep-based scan of composite-lt.tex, type-exclusion.tex, discussion.tex, appendix-proofs.tex for tokens {`thm:local-tomo`, `V_{BM}`, `product-form`, `carries`, `inherits`, `sms:minimal`}. Verify every hit appears in downstream-consumer-scan.md with file:line + sense classification."
      pass_condition: "Zero grep hits in the four files are unaccounted for in downstream-consumer-scan.md."
      evidence_required: [deliv-consumer-scan-md, ref-composite-lt, ref-type-exclusion, ref-discussion, ref-appendix-proofs]
      automation: hybrid
    - id: test-consumer-sense-assignment
      subject: claim-downstream-scan
      kind: consistency
      procedure: "For every row in downstream-consumer-scan.md, verify the 'required sense' column is populated with one of {a, b, c, unclassified-pending-user}. At least the `sms:minimal` row MUST be tagged (b)."
      pass_condition: "Every row has a non-empty sense assignment; `sms:minimal` row is tagged (b) with justification."
      evidence_required: [deliv-consumer-scan-md]
      automation: hybrid
    - id: test-face-status-verdict
      subject: claim-face-status
      kind: existence
      procedure: "Read w-face-status.md Section 3; verify verdict ∈ {IS-FACE, NOT-FACE, INCONCLUSIVE-DEFAULT-TO-NOT-FACE}."
      pass_condition: "Verdict present and in the allowed set."
      evidence_required: [deliv-face-status-md]
      automation: human
    - id: test-face-status-evidence
      subject: claim-face-status
      kind: consistency
      procedure: "Read w-face-status.md Section 4; verify it contains either (a) a concrete witness (v ∈ V_{BM}^+, 0 ≤ w ≤ v, v ∈ W, w ∉ W) with its verification, OR (b) a proof of both A-S 2003 Ch. 1 face axioms (order-convex + hereditary). For INCONCLUSIVE verdict: Section 4 must state what further evidence would be needed to upgrade."
      pass_condition: "Evidence section is evidence-backed (not handwaving); witness constructions have explicit coefficients OR proof appeals have cited A-S 2003 Ch. 1 props."
      evidence_required: [deliv-face-status-md, ref-as-2003]
      automation: human
    - id: test-carries-three-senses-present
      subject: claim-carries-senses-formalized
      kind: existence
      procedure: "Read carries-senses.md; verify Sections 1-3 each formally define one of senses (a), (b), (c) with a symbolic formula (not prose only)."
      pass_condition: "Three senses each have a formal definition line; all three collapse diagram arrows ((b)⇒(a), (c)⇒(b)) are stated."
      evidence_required: [deliv-carries-senses-md]
      automation: human
    - id: test-carries-collapse-diagram
      subject: claim-carries-senses-formalized
      kind: existence
      procedure: "Read carries-senses.md Sections 4-5; verify collapse direction arrows AND non-collapse witnesses are both stated, with Gudder-Greechie 2002 Ex. 39 cited for (a)⇏(b)."
      pass_condition: "Section 4 has both directions; Section 5 cites Gudder-Greechie 2002 for (a)⇏(b) and names the unit-choice subtlety for (b)⇏(c)."
      evidence_required: [deliv-carries-senses-md, ref-gudder-greechie]
      automation: human
    - id: test-sympy-design-has-wedge-resolution
      subject: claim-sympy-design
      kind: existence
      procedure: "Read sympy-design.md Section 1; verify it addresses the 'H_3(ℝ) wedge component (3-dim antisymmetric)' roadmap phrase by either (a) defaulting to Peirce-1 off-diagonal 3-dim interpretation with justification, OR (b) stating the user-override path."
      pass_condition: "Section 1 explicitly resolves the ambiguity; notes that H_3(ℝ) is symmetric-matrix space (dim 6) with no antisymmetric subspace, and picks the Peirce-1 off-diagonal 3-dim as default."
      evidence_required: [deliv-sympy-design-md]
      automation: human
    - id: test-sympy-design-has-axiom-checks
      subject: claim-sympy-design
      kind: existence
      procedure: "Read sympy-design.md Section 3; verify test cases cover S1 (additivity), S3 (unitality), S4 (orthogonality symmetry), closure (sense (a)), and one negative test."
      pass_condition: "All five test categories are present with a concrete test-case sketch (not just name)."
      evidence_required: [deliv-sympy-design-md]
      automation: human
    - id: test-sympy-design-reuses-phase54
      subject: claim-sympy-design
      kind: consistency
      procedure: "Read sympy-design.md Section 4; verify explicit reference to Phase 54 `closeout-sympy.py` helpers (`compress(B,i,n)`, `seq_prod(...)`) and Phase 55 `s4-sympy-spot-check.py` pattern."
      pass_condition: "Both Phase 54 and Phase 55 SymPy artifacts are cited by file path; reuse plan explicit."
      evidence_required: [deliv-sympy-design-md, ref-phase54-closeout-sympy, ref-phase55-s4-sympy]
      automation: hybrid
    - id: test-checkpoint-user-confirmed
      subject: claim-routing-checkpoint
      kind: existence
      procedure: "Read 56-01-SUMMARY.md; verify presence of user-confirmation block recording decisions on: (1) target 'carries' sense, (2) face-status handling, (3) wedge-component interpretation. Blocker if checkpoint was bypassed without user input."
      pass_condition: "All three user decisions explicitly recorded, timestamped, and referenced by Plan 56-02 hand-off block."
      evidence_required: [deliv-summary-with-handoff]
      automation: human
    - id: test-handoff-routing-locked
      subject: claim-routing-checkpoint
      kind: existence
      procedure: "Read 56-01-SUMMARY.md hand-off block; verify it locks Plan 56-02 to one of {direct-S1-S7-via-vdW-Def-4 (primary), face-restriction (only if face verdict IS-FACE and user explicitly approves)} with the default being the primary path."
      pass_condition: "Hand-off block names the Plan 56-02 path unambiguously; default is direct-S1-S7 via vdW 2019 Def. 4."
      evidence_required: [deliv-summary-with-handoff]
      automation: human
  forbidden_proxies:
    - id: fp-identity-paraphrase
      subject: claim-identity-extracted
      proxy: "Paraphrasing the Thm 5.8 upper-bound identity instead of quoting verbatim from composite-lt.tex / appendix-proofs.tex"
      reason: "R5 discipline — paraphrase can silently shift 'carries' sense; only verbatim quote + line number is evidence-ready for the adversarial review in Plan 56-03."
    - id: fp-face-status-handwave
      subject: claim-face-status
      proxy: "Declaring W IS-FACE or NOT-FACE without explicit evidence (no witness, no proof of A-S Ch. 1 face axioms)"
      reason: "Pitfall 2 in RESEARCH.md; the face-status verdict routes Plan 56-02, so unevidence verdict cascades into a silent routing error."
    - id: fp-face-status-shortcut-assumption
      subject: claim-face-status
      proxy: "Assuming W is a face 'because it's spanned by product effects' without verification"
      reason: "RESEARCH.md anti-pattern — span of positive elements is not automatically a face; face = hereditary AND order-convex."
    - id: fp-carries-sense-collapse
      subject: claim-carries-senses-formalized
      proxy: "Writing 'W carries the SP' in any Phase 56 artifact without specifying which of the three senses is meant"
      reason: "R7 equivocation — the central pitfall Phase 56 exists to mitigate; every use of 'carries' must cite its sense."
    - id: fp-wedge-component-silent-interpretation
      subject: claim-sympy-design
      proxy: "Adopting one interpretation of 'H_3(ℝ) wedge component (3-dim antisymmetric)' without explicitly noting that H_3(ℝ) has no antisymmetric subspace and that the interpretation is a reconstruction"
      reason: "Silent reinterpretation of roadmap scope is a fidelity failure; the user must confirm the interpretation at the Task-6 checkpoint."
    - id: fp-frozen-file-edit
      subject: claim-identity-extracted
      proxy: "Any Phase 56 task writing to main-jmp-submitted.tex"
      reason: "Frozen-file discipline per Phase 55 inheritance; main-jmp-submitted.tex is git-tag-frozen. Only living main.tex is editable (and only in Plan 56-03)."
    - id: fp-checkpoint-bypass
      subject: claim-routing-checkpoint
      proxy: "Completing Plan 56-01 without the Task 6 human-decision checkpoint (no user-confirmation block in 56-01-SUMMARY.md)"
      reason: "Plan 56-02 routing depends on user-confirmed sense + face-status handling + wedge interpretation; bypassing the checkpoint produces an under-specified hand-off."
    - id: fp-phase-54-ci-bypass
      subject: claim-downstream-scan
      proxy: "Classifying a downstream consumer of factor-level Peirce invariance without noting that `\\ref{lem:peirce-preservation}` (Phase 54 C-i) is the required citation"
      reason: "R11 cross-phase cascade; any factor-level Peirce invariance in Phase 56 downstream work must cite the lemma, not the submitted-era (A)-proof."
  links:
    - id: link-identity-feeds-consumer-scan
      source: deliv-identity-md
      target: deliv-consumer-scan-md
      relation: supports
      verified_by: [test-identity-verbatim, test-consumer-scan-coverage]
    - id: link-carries-senses-feeds-consumer-scan
      source: deliv-carries-senses-md
      target: deliv-consumer-scan-md
      relation: supports
      verified_by: [test-carries-three-senses-present, test-consumer-sense-assignment]
    - id: link-face-status-routes-sympy
      source: deliv-face-status-md
      target: deliv-sympy-design-md
      relation: supports
      verified_by: [test-face-status-verdict, test-sympy-design-has-axiom-checks]
    - id: link-all-wave1-feeds-summary
      source: claim-routing-checkpoint
      target: deliv-summary-with-handoff
      relation: depends_on
      verified_by: [test-checkpoint-user-confirmed, test-handoff-routing-locked]
    - id: link-backed-by-research
      source: claim-identity-extracted
      target: ref-research-md
      relation: depends_on
      verified_by: [test-identity-verbatim]
  uncertainty_markers:
    weakest_anchors:
      - "W face-status verdict — default expectation NOT-FACE but unresolved pre-plan; Section 4 of w-face-status.md may land on INCONCLUSIVE, which Plan 56-02 must absorb via direct S1-S7 route."
      - "H_3(ℝ) 'wedge component (3-dim antisymmetric)' interpretation — roadmap phrasing is ambiguous; Peirce-1 off-diagonal 3-dim is the reconstruction, but user-override at Task 6 can flip it."
      - "Gudder-Greechie 2002 Example 39 — cited at AXIOM-STATED-IN-SECONDARY-SOURCE tier; full RMP 49 access not obtained in research."
      - "Downstream consumer sense map — if `type-exclusion.tex` or `discussion.tex` needs sense (c) but we target only (b), Plan 56-02 must upgrade."
    disconfirming_observations:
      - "composite-lt.tex L203-221 text has drifted since submission (living has diverged from main-jmp-submitted.tex) — forces re-extraction and potential scope expansion."
      - "`sms:minimal` minimality clause requires sense (c) rather than sense (b) after close reading — Plan 56-02 must upgrade the proof."
      - "A downstream §5/§6 consumer uses 'thm:local-tomo' in a way that REQUIRES W to be a face (not just sense (b)) — would force Plan 56-02 to establish face status affirmatively, not as a bonus."
      - "Phase 54 (C-i) `lem:peirce-preservation` is invoked at factor level in the §5 proof text of `prop:inheritance`, not just at §3 — would force re-examination of R11 touchpoints in Plan 56-02."
---

<objective>
Open Phase 56 with a text-extraction + face-status-resolution + three-'carries'-senses-formalization + downstream-consumer-scan + SymPy-design wave that ends in a user-decision checkpoint locking the Plan 56-02 routing.

Purpose: Paper 5's `thm:local-tomo` (informally "Thm 5.8") upper-bound step asserts "W carries the product-form SP". The word "carries" is R7-equivocation-prone: three distinct senses (set-closure, induced-structure SPS, functorial SPS-morphism) collapse in natural language. Before any proof is written (Plan 56-02) or any revision text is integrated (Plan 56-03), Phase 56 must (1) extract the exact identity, (2) verify W's face status to decide whether the face-restriction shortcut is available, (3) locate the downstream §5/§6 consumers and classify their required 'carries' sense, (4) formally write the three senses as a reusable artifact, (5) design the SymPy H_3(ℝ) ⊗ H_3(ℝ) spot-check resolving the roadmap's "wedge component" ambiguity, and (6) check in with the user to lock the routing. This plan is ALL SETUP — no proof is authored, no .tex file is edited.

Output: five .gpd/phases/56-.../*.md artifacts (identity verbatim, consumer scan, face status, carries senses, sympy design) plus a 56-01-SUMMARY.md with explicit hand-off block for Plan 56-02. No paper file is modified.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/56-RESEARCH.md
@.gpd/phases/54-3-3-peirce-preservation-from-ous-primitives/54-RESULT.md
@.gpd/phases/55-s4-facial-structure-lemma/55-RESULT.md
@derivations/paper5-peirce-preservation/alfsen-shultz-notes.md
@derivations/paper5-peirce-preservation/claim.md
@derivations/paper5-peirce-preservation/s0-axiom.md
@derivations/paper5-peirce-preservation/closeout-sympy.py
@derivations/paper5-peirce-preservation/s4-sympy-spot-check.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Thm 5.8 upper-bound identity verbatim from living composite-lt.tex + appendix-proofs.tex</name>
  <files>.gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/thm-5-8-identity-verbatim.md</files>
  <action>
    Create thm-5-8-identity-verbatim.md with sections 1-7 (populate all now).

    1. Read `/Users/ehrlich/repos/blog/landing/papers/qm-from-self-modeling/sections/composite-lt.tex` lines 1-265 (full file). Identify `thm:local-tomo` at L162 and the upper-bound step at L203-221.
    2. Read `/Users/ehrlich/repos/blog/landing/papers/qm-from-self-modeling/sections/appendix-proofs.tex` lines 1-254 (full file). Identify `thm:lt-full` at L164 and the upper-bound step at L228-238.
    3. Populate Section 1 with the roadmap-to-label mapping: "'Thm 5.8' (roadmap) → `thm:local-tomo` (composite-lt.tex L162) + `thm:lt-full` (appendix-proofs.tex L164), upper-bound portion specifically at composite-lt.tex L203-221 + appendix-proofs.tex L228-238".
    4. Populate Section 2 with a verbatim quote of composite-lt.tex L203-221, wrapped in a fenced code block with `% FILE: composite-lt.tex:203-221 %` header.
    5. Populate Section 3 with a verbatim quote of appendix-proofs.tex L228-238, wrapped in a fenced code block with `% FILE: appendix-proofs.tex:228-238 %` header.
    6. Populate Section 4 with a verbatim quote of the full `thm:local-tomo` context (composite-lt.tex L162-222), for reference.
    7. Populate Section 5 with a verbatim quote of the full `thm:lt-full` context (appendix-proofs.tex L164-243).
    8. Populate Section 6: ambiguity note — grep main-jmp-submitted.tex for `\\label{thm:5.8}` and confirm zero hits; state explicitly "Paper 5 contains no `\\label{thm:5.8}`; 'Thm 5.8' is informal shorthand for the upper-bound step of `thm:local-tomo`".
       ```bash
       grep -n 'label{thm:5.8}' /Users/ehrlich/repos/blog/landing/papers/qm-from-self-modeling/main-jmp-submitted.tex \
         /Users/ehrlich/repos/blog/landing/papers/qm-from-self-modeling/sections/*.tex
       ```
    9. Populate Section 7: token inventory of every 'carries' / 'inherits' / 'closed' / 'restricts' / 'respects' / 'extends' occurrence in the quoted passages, with file:line. Flag each as candidate R7 equivocation site.
    10. Do NOT edit any paper file. Read-only. Do NOT modify main-jmp-submitted.tex.

    Do NOT write carries-senses.md or any other artifact in this task.
  </action>
  <verify>
    (a) thm-5-8-identity-verbatim.md has Sections 1-7 populated.
    (b) Run `grep -c 'composite-lt.tex:' .gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/thm-5-8-identity-verbatim.md` — returns ≥ 3 (Sections 2, 4, 7 all cite composite-lt.tex).
    (c) For each fenced-code-block quote, verify the quoted text matches the source file at the cited line range via `sed -n` or direct Read.
    (d) Section 6 records zero `\\label{thm:5.8}` hits and the roadmap-to-label mapping.
    (e) `git -C /Users/ehrlich/repos/blog diff --stat HEAD -- landing/papers/qm-from-self-modeling/main-jmp-submitted.tex` returns zero changes (frozen-file discipline).
    (f) fp-identity-paraphrase forbidden proxy rejected: every identity statement is a direct quote, not a paraphrase.
    (g) fp-frozen-file-edit forbidden proxy rejected: no write operation on main-jmp-submitted.tex.
  </verify>
  <done>
    thm-5-8-identity-verbatim.md exists with Sections 1-7 populated, every quote matches the source file at the cited line range, no `\\label{thm:5.8}` hit recorded, R7 token inventory captured.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scan §5/§6 downstream consumers of thm:local-tomo and classify by required 'carries' sense</name>
  <files>.gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/downstream-consumer-scan.md</files>
  <action>
    Create downstream-consumer-scan.md with Sections 1-8.

    1. Populate Section 1 (grep patterns). The patterns:
       ```
       thm:local-tomo
       ref{thm:local-tomo}
       Cref{thm:local-tomo}
       V_{BM}
       V_\{BM\}
       product-form
       carries
       inherits
       sms:minimal
       ref{sms:minimal}
       ```
    2. Run the scan on living Paper 5:
       ```bash
       cd /Users/ehrlich/repos/blog/landing/papers/qm-from-self-modeling
       for f in sections/composite-lt.tex sections/type-exclusion.tex sections/discussion.tex sections/appendix-proofs.tex main.tex; do
         echo "=== $f ==="
         grep -nE 'thm:local-tomo|V_\{BM\}|V_BM|product-form|carries|inherits|sms:minimal' "$f"
       done
       ```
    3. Populate Section 2 (composite-lt.tex hits table): file:line | verbatim 2-4-line context | required sense (a/b/c/unclassified-pending-user) | rationale. Include the `thm:local-tomo` theorem body itself (the place where the identity is asserted) and every `\\ref{thm:local-tomo}` invocation.
    4. Populate Section 3 (type-exclusion.tex hits table) — same columns. Pay attention to the real/quaternionic exclusion argument: classify whether it needs W to have induced SPS structure (sense (b)) or just set closure (sense (a)).
    5. Populate Section 4 (discussion.tex hits table) — same columns.
    6. Populate Section 5 (appendix-proofs.tex hits table) — same columns. Include `thm:lt-full` and any use of 'carries'/'inherits' in the proof body.
    7. Populate Section 6 (consumer-by-sense summary): counts of (a) / (b) / (c) / unclassified. Should be at least one (b) consumer (`sms:minimal`).
    8. Populate Section 7: explicit row for `sms:minimal` with justification "minimality clause says V_{BM} is smallest OUS satisfying (C1)-(C4) + product-form SP; for W to be a competitor it must satisfy the same structure, i.e. sense (b)".
    9. Populate Section 8 (recommendation for Plan 56-02 target sense): default is sense (b); upgrade to sense (c) if (c) is cheap AND any consumer needs it. Name the consumers that would benefit from (c).

    Sense-assignment rubric:
      - sense (a) if consumer uses only "a ∘ b ∈ W" closure (no axiom-level reasoning on W).
      - sense (b) if consumer uses `(W, ∘|_W)` as an SPS (e.g., invokes vdW Thm 1 on W, or uses minimality clause, or compares cardinalities of effects of W with another SPS).
      - sense (c) if consumer uses W as a sub-SPS functorially (morphism-preserving inclusion; cleanest for monoidal-category arguments in composites).
      - unclassified-pending-user if the sense is genuinely ambiguous and the Task-6 checkpoint should resolve it.

    Do NOT edit any paper file. Read-only grep.
  </action>
  <verify>
    (a) downstream-consumer-scan.md has Sections 1-8.
    (b) Every grep hit from the Section-1 pattern list across the four files appears in Sections 2-5 (spot-check: re-run grep and compare hit counts to row counts).
    (c) `sms:minimal` consumer row is present in Section 7 with sense (b) assignment and justification.
    (d) Section 6 total hit count equals the sum of Sections 2-5 row counts.
    (e) fp-carries-sense-collapse rejected: every consumer row has a sense assignment (no bare "carries" without sense).
    (f) fp-phase-54-ci-bypass noted: any row consuming factor-level Peirce invariance names `\\ref{lem:peirce-preservation}` as the required citation.
  </verify>
  <done>
    downstream-consumer-scan.md complete; every `thm:local-tomo` / `V_{BM}` / 'product-form' / 'carries' / 'sms:minimal' consumer in §5/§6 is classified; recommendation for Plan 56-02 target sense is provisionally sense (b) + (c) upgrade.
  </done>
</task>

<task type="auto">
  <name>Task 3: Resolve W's face status in V_{BM} via explicit A-S Ch. 1 face-axiom check or concrete non-face witness</name>
  <files>.gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/w-face-status.md</files>
  <action>
    Create w-face-status.md with Sections 1-6.

    1. Populate Section 1: state the A-S 2003 Ch. 1 face definition. A face F of an OUS cone V^+ is a subset satisfying (F1) F is a non-empty subcone of V^+; (F2) F is order-convex — `u, v ∈ F, u ≤ w ≤ v ⟹ w ∈ F`; (F3) F is hereditary under ≤ — `u ∈ F, 0 ≤ w ≤ u ⟹ w ∈ F`. Cite `\\cite[Ch.~1, Def.~1.?]{AlfsenShultz2003}` with the exact prop/def number (read A-S 2003 Ch. 1 via alfsen-shultz-notes.md or, if not in notes, flag VERIFICATION-DEFERRED). NOTE: For Phase 56 purposes, (F3) subsumes (F2) if the cone generates a subspace. Whichever version the A-S text uses, cite exactly.

    2. Populate Section 2: candidate witness or proof strategy. Default strategy: exhibit a non-face witness.
       - Consider the simplest non-trivial case: V_B = V_M = H_2(ℝ) (2×2 real symmetric, dim 3), so dim V_{BM} = 9 (real case; `sms:minimal` forces dim V_{BM} = 9 here since d² = 9).
       - Construct W = span{a_i ⊗ b_j : i,j ∈ {1,2,3}}. This is 9-dim.
       - Note: in the real case the roadmap's earlier note says `dim V_{BM} = 10 > 9` for `V_B = V_M = M_2(ℝ)^{sa} = H_2(ℝ)` — this is a type-exclusion symptom, not a sign that sms:minimal is violated, since sms:minimal is a minimality clause not a strict-equality constraint.
       - Actually, the cleanest non-face witness lives NOT in the real case (where dim W < dim V_{BM}) but in any ambient V_{BM} that contains a positive element OUTSIDE W with a W-element above it.

    3. Populate Section 3: verdict ∈ {IS-FACE, NOT-FACE, INCONCLUSIVE-DEFAULT-TO-NOT-FACE}.

    4. Populate Section 4: evidence.
       - For NOT-FACE: construct v ∈ V_{BM}^+, v ∈ W (a product-effect or sum of product-effects), and w ∈ V_{BM}^+, w ∉ W, with 0 ≤ w ≤ v. Most natural: take `v = 1_B ⊗ 1_M = 1_{V_{BM}}` (which IS in W) and find a non-product-effect state `w` with `0 ≤ w ≤ 1`. For instance, in the real case `V_B = V_M = H_2(ℝ)` with d²=9 and possibly dim V_{BM}=10, a 10th dimension exists where a non-product positive element lives below `1`. Concrete construction: if V_{BM} = H_2(ℝ) ⊗ H_2(ℝ) ⊕ (extra dim) in the tomographic-completion picture, the extra dimension hosts such a `w`. Justify dimensionally.
       - For IS-FACE: prove both (F2) and (F3) directly. E.g., use that every element of V_{BM}^+ admits a decomposition in the product-effect basis that preserves positivity (this is DUBIOUS in general — likely the proof does not go through, which is WHY we expect NOT-FACE).
       - For INCONCLUSIVE: state precisely what further evidence would upgrade (e.g., "confirmed once a witness is explicitly constructed in at least one specific test case like `V_B = V_M = H_2(ℝ)`"). INCONCLUSIVE is acceptable as a final verdict — Plan 56-02 proceeds with direct S1-S7 via vdW Def. 4 regardless.

    5. Populate Section 5: routing implication.
       - IS-FACE ⟹ Plan 56-02 can OPTIONALLY use face-restriction as a bonus shortcut AFTER direct S1-S7 is done; primary path remains direct S1-S7.
       - NOT-FACE or INCONCLUSIVE ⟹ Plan 56-02 proceeds exclusively with direct S1-S7 via vdW 2019 Def. 4 + Thm 1 (the RESEARCH.md primary recommendation).

    6. Populate Section 6: recommendation to Plan 56-02 — regardless of Section 3 verdict, proceed with direct S1-S7 via vdW 2019 Def. 4 + Thm 1 as the primary path. Face-restriction is strictly bonus and only offered as an alternative framing in Plan 56-03's revision text.

    If during the face-check investigation the A-S 2003 Ch. 1 face definition is not accessible through alfsen-shultz-notes.md or an internal derivation, flag VERIFICATION-DEFERRED for the exact prop/def number but do NOT block the verdict (the face definition is standard across multiple OUS references).

    Do NOT edit any paper file.
  </action>
  <verify>
    (a) w-face-status.md has Sections 1-6.
    (b) Section 3 verdict is in {IS-FACE, NOT-FACE, INCONCLUSIVE-DEFAULT-TO-NOT-FACE}.
    (c) Section 4 evidence is concrete: either an explicit witness with coefficients OR a proof-by-appeal-to-Ch.1-prop with the prop number cited. No handwaving.
    (d) Section 5 routing implication names "direct S1-S7 via vdW 2019 Def. 4" as the primary path regardless of verdict.
    (e) Every A-S 2003 citation in the document is bracketed `\\cite[Ch.~X, Prop.~Y.Z]{AlfsenShultz2003}` with X ≤ 8 (or flagged VERIFICATION-DEFERRED).
    (f) fp-face-status-handwave and fp-face-status-shortcut-assumption forbidden proxies both rejected: verdict is evidence-backed and NOT based on "span of positive elements" inference.
  </verify>
  <done>
    w-face-status.md has a verdict with evidence; routing implication spelled out; Plan 56-02 is safe to proceed with direct S1-S7 via vdW 2019 Def. 4 regardless of face-status outcome.
  </done>
</task>

<task type="auto">
  <name>Task 4: Formalize the three 'carries' senses (a)/(b)/(c) as a reusable artifact</name>
  <files>.gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/carries-senses.md</files>
  <action>
    Create carries-senses.md with Sections 1-7. Mostly transcribed from RESEARCH.md §"Three Senses of Carries" (L99-119), expanded with the downstream-consumer mapping.

    1. Populate Section 1: formal definition of sense (a) — set-closure.
       ```
       Sense (a) — set-level closure:
         For all a, b ∈ [0,1]_W := [0,1]_{V_{BM}} ∩ W, a ∘_{V_{BM}} b ∈ W.
         Formally: ∘_{V_{BM}} ([0,1]_W × [0,1]_W) ⊆ W.
       ```
       Status note: weakest; compatible with W failing to be EJA-like per Gudder-Greechie 2002 Ex. 39.

    2. Populate Section 2: formal definition of sense (b) — induced-structure SPS.
       ```
       Sense (b) — induced-structure SPS:
         (W, ≤|_W, 1_W, ∘|_W) is itself an order unit space satisfying vdW 2019 Def. 2 (S1-S7), where:
           - ≤|_W is the restriction of ≤_{V_{BM}} to W,
           - 1_W := 1_{V_{BM}} (valid since 1_B ⊗ 1_M ∈ W when 1_B ∈ span{a_i} and 1_M ∈ span{b_j}),
           - ∘|_W := ∘_{V_{BM}}|_{[0,1]_W × [0,1]_W}, which is a restriction set-theoretically (not a NEW operation).
       ```
       Status: the sense needed by `sms:minimal` and therefore by Plan 56-02.

    3. Populate Section 3: formal definition of sense (c) — functorial SPS-morphism.
       ```
       Sense (c) — functorial SPS-morphism:
         The inclusion ι: W ↪ V_{BM} is an SPS-morphism:
           (c1) ι is linear,
           (c2) ι is unital (ι(1_W) = 1_{V_{BM}}),
           (c3) ι is positive (ι(W^+) ⊆ V_{BM}^+),
           (c4) ι preserves ∘: ι(a ∘|_W b) = ι(a) ∘_{V_{BM}} ι(b) for all a, b ∈ [0,1]_W.
         (c4) is AUTOMATIC when ∘|_W is defined by restriction, since both sides equal a ∘_{V_{BM}} b.
       ```
       Status: cleanest; optional upgrade from sense (b) via BGW 2020 category-of-EJAs machinery.

    4. Populate Section 4: collapse diagram.
       ```
       (c) ⇒ (b): SPS-morphism from (W, ∘|_W) to (V_{BM}, ∘) implies (W, ∘|_W) is an SPS [domain of morphism].
       (b) ⇒ (a): Any SPS (W, ∘|_W) has ∘|_W taking values in [0,1]_W ⊆ W; hence closure.
       (a) ⇏ (b): Gudder-Greechie 2002 Example 39 — an effect algebra with associative SP satisfying closure but NOT the full S1-S7 over the induced order. Sense-(a)-only.
       (b) ⇏ (c): Subtle; (c) additionally requires unit-compatibility `ι(1_W) = 1_V`. In the Paper 5 setting 1_W = 1_V so (b) ⇒ (c) actually HOLDS here — document this observation.
       ```

    5. Populate Section 5: non-collapse witnesses.
       - (a)⇏(b): Gudder-Greechie 2002, RMP 49, 87-111, Example 39. AXIOM-STATED-IN-SECONDARY-SOURCE tier (RMP 49 paper not directly accessed; cited with acknowledgement).
       - (b)⇏(c): In general, requires `ι(1_W) ≠ 1_V` (different order units) OR a cone-preservation failure. In Paper 5's W = span{a_i ⊗ b_j} with 1_B ⊗ 1_M ∈ W, (b)⇒(c) actually holds, so the non-collapse is merely a general-theory caveat.

    6. Populate Section 6: pointer to downstream-consumer-scan.md with the mapping "§5/§6 consumer X needs sense Y" for each row in downstream-consumer-scan.md Section 7. Build a mini-table from the scan.

    7. Populate Section 7: minimum sense for `sms:minimal` clause = sense (b). Justification: the minimality clause says "V_{BM} is the smallest OUS satisfying (C1)-(C4) AND the product-form SP"; for W to be a competitor OUS, W must itself be an OUS satisfying S1-S7 with its product-form SP — this is precisely sense (b). Sense (a) is insufficient because sense (a) W is not an OUS, just a subspace closed under ∘.

    Add a closing note: every use of 'carries' / 'inherits' / 'closed' in Phase 56 artifacts (Plan 56-01 onward) MUST cite which of (a)/(b)/(c) is meant; citation form: 'sense (b) per carries-senses.md §2'.
  </action>
  <verify>
    (a) carries-senses.md has Sections 1-7.
    (b) Sections 1, 2, 3 each have a formal definition (symbolic formula line, not prose-only).
    (c) Section 4 has all four arrows/non-arrows ((c)⇒(b), (b)⇒(a), (a)⇏(b), (b)⇏(c)) stated.
    (d) Section 5 cites Gudder-Greechie 2002 for (a)⇏(b) with AXIOM-STATED-IN-SECONDARY-SOURCE tier.
    (e) Section 6 mini-table corresponds to downstream-consumer-scan.md Section 7 rows.
    (f) Section 7 explicitly names sense (b) for `sms:minimal`.
    (g) fp-carries-sense-collapse rejected: the artifact itself uses 'carries' only with explicit sense-(a/b/c) tagging.
  </verify>
  <done>
    carries-senses.md exists as a reusable artifact; three senses formally defined; collapse diagram stated; downstream-consumer map (mini-table) built from scan output; sense (b) locked as the Plan-56-02 target.
  </done>
</task>

<task type="auto">
  <name>Task 5: Design the SymPy H_3(ℝ) ⊗ H_3(ℝ) spot-check — resolve 'wedge component' ambiguity, specify axiom checks, reuse Phase 54 infra</name>
  <files>.gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/sympy-design.md</files>
  <action>
    Create sympy-design.md with Sections 1-6. NO CODE EXECUTED in this task — this is a design document only. Plan 56-02 executes.

    1. Populate Section 1: 'wedge component' ambiguity resolution.
       - Roadmap scope says "Small-case SymPy check on H_3(ℝ) wedge component — researcher flagged 'H_3(ℝ) is symmetric; plausible interpretation is Peirce-1 off-diagonal 3-dim subspace'". Resolve as follows:
       - H_3(ℝ) = space of 3×3 real symmetric matrices, dim 6. Standard basis: 3 diagonal entries + 3 off-diagonal symmetric pairs.
       - The antisymmetric subspace of 3×3 REAL matrices is 3-dim (Lie algebra so(3)); but the antisymmetric part of a SYMMETRIC matrix is zero. So "3-dim antisymmetric subspace of H_3(ℝ)" literally = {0}.
       - Plausible reconstructions:
         (A) Peirce-1 off-diagonal: w.r.t. the three diagonal rank-1 projectors p_1 = diag(1,0,0), p_2 = diag(0,1,0), p_3 = diag(0,0,1), the three Peirce-1 spaces V_1(p_i, p_j) are each 1-dim (spanned by E_{ij} + E_{ji}, which is the (i,j)-off-diagonal symmetric basis element). Direct sum V_1(1,2) ⊕ V_1(1,3) ⊕ V_1(2,3) = 3-dim off-diagonal subspace of H_3(ℝ). THIS IS THE DEFAULT INTERPRETATION.
         (B) Λ²(ℝ³) ≅ ℝ³: literal exterior algebra; 3-dim but not naturally inside H_3(ℝ).
         (C) "3-dim slice of H_3(ℝ)": any 3-dim subspace; ambiguous and useless.
       - Default: Interpretation (A) — Peirce-1 off-diagonal 3-dim. State explicitly: "Default interpretation per planner decision: 'wedge component' = Peirce-1-space off-diagonal 3-dim subspace of H_3(ℝ) w.r.t. diagonal rank-1 projector family {p_1, p_2, p_3}. User may override at the Task-6 checkpoint."
       - fp-wedge-component-silent-interpretation forbidden proxy is rejected by this explicit reconstruction note.

    2. Populate Section 2: concrete W_test specification.
       - Ambient: V_test := H_3(ℝ) ⊗ H_3(ℝ), dim = 36.
       - Full W_test: span{a_i ⊗ b_j : a_i, b_j basis of H_3(ℝ)} = 36-dim (so W_test = V_test for this small case; product effects span).
       - Specialized W_test (the 'wedge component' slice): W_wedge := span{a_i ⊗ b_j : a_i, b_j ∈ off-diag Peirce-1 space of H_3(ℝ)}, dim = 9.
       - Projective units: p_1, p_2, p_3 = diag(1,0,0), diag(0,1,0), diag(0,0,1) for both factors. Phase 54's `compress(B, i, n)` already computes C_{p_i} for these.
       - Product effects to test: `a_{ij} ⊗ b_{kl}` for `i, j, k, l` drawn from the 6-dim basis of H_3(ℝ).

    3. Populate Section 3: test cases.
       - TEST-CLOSURE (sense (a)): For random rational-coefficient elements a, b, c, d ∈ H_3(ℝ) (each a sum of basis elements with rational coefficients in [0,1] subject to positivity), compute LHS = (a ⊗ b) ∘_W (c ⊗ d) using the product-form definition, and RHS = (a ∘ c) ⊗ (b ∘ d) using factor-level SP from Phase 54 `seq_prod`. Verify LHS - RHS = 0 symbolically. Verify the output lies in span{a_i ⊗ b_j}. Expected: PASS (by definition of ∘_W).
       - TEST-S1 (additivity on W): Pick general effects a, b, c ∈ H_3(ℝ), p, q ∈ H_3(ℝ) (factor-level [0,1]-effects), and verify (a⊗b) ∘_W ((c⊗p) + (c'⊗q)) = (a⊗b)∘_W(c⊗p) + (a⊗b)∘_W(c'⊗q). SymPy simplify symbolic difference; expect zero. Expected: PASS.
       - TEST-S3 (unitality on W): For generic a ∈ [0,1]_{H_3(ℝ)}, b ∈ [0,1]_{H_3(ℝ)}, verify (1_3 ⊗ 1_3) ∘_W (a ⊗ b) = a ⊗ b where 1_3 is the 3×3 identity. Expected: PASS (trivial by factor-level S3).
       - TEST-S4 (orthogonality symmetry on W): Pick compatible product effects a⊗b, c⊗d on W with (a⊗b)∘_W(c⊗d) = 0; verify (c⊗d)∘_W(a⊗b) = 0. The factor-level compatibility p_i | p_j (equal eigenvector bases) reduces to factor-level S4 + tensor bilinearity. Expected: PASS.
       - TEST-NEGATIVE (sense (a) counterexample attempt): Search for a pair (a, b) ∈ [0,1]_W × [0,1]_W where (a ∘_W b) ∉ W. This should FAIL to find a counterexample — if it finds one, Approach 3 (Gudder-Greechie precedent) is triggered and Phase 56 outcome is (C) instead of (C-i). Expected: NO COUNTEREXAMPLE FOUND = PASS for sense (a).
       - Skip S5/S6/S7 on W in the SymPy check — these reduce to factor-level S5/S6/S7, which are already covered by Phase 54 infrastructure. Cite factor-level reductions in the Plan 56-02 proof text.

    4. Populate Section 4: reuse plan for Phase 54 infra.
       - From `derivations/paper5-peirce-preservation/closeout-sympy.py`: reuse `compress(B, i, n)`, `seq_prod(a, b, spectral_decomposition=None)`, and the H_3(ℝ) basis-generator helpers.
       - From `derivations/paper5-peirce-preservation/s4-sympy-spot-check.py`: reuse the symbolic-exact simplification pattern (sp.simplify on difference, assert == 0) and the two-mixing-function verification structure (for f = √(λμ) and f = λ·μ if compatible-case variation is needed).
       - New: `tensor_product(A, B)` helper for SymPy Matrix tensor products; `product_sp(a_tensor, b_tensor)` that decomposes tensors back into factor basis and applies factor-level `seq_prod` on each factor.

    5. Populate Section 5: runtime budget + exit-code criterion.
       - Budget: < 30 seconds on developer workstation (Phase 54 H_3/H_4 baseline is 0.013s; tensor case ~10-100x still well within budget).
       - Exit code: 0 on all-tests-PASS; 1 on any FAIL. sys.exit(0 if all_passed else 1).
       - Output: a test-log header with SymPy version, Python version, timing, and per-test PASS/FAIL.

    6. Populate Section 6: FAIL routing.
       - TEST-CLOSURE FAIL: sense (a) FAILS on H_3(ℝ) ⊗ H_3(ℝ); route to Approach 3 (Gudder-Greechie). Phase 56 outcome = (C). Plan 56-03 writes revision text that abandons the sense-(b)/(c) claim.
       - TEST-S1/S3/S4 FAIL: sense (b) FAILS. Route to Plan 56-02 diagnostic: is the failure algebraic (factor-level error — check Phase 54 closeout passes) or tensor-level (bilinear-extension misdefinition)?
       - TEST-NEGATIVE finds a counterexample: sense (a) FAILS; treat as TEST-CLOSURE FAIL.
  </action>
  <verify>
    (a) sympy-design.md has Sections 1-6.
    (b) Section 1 explicitly states H_3(ℝ) is symmetric-matrix space (dim 6, no antisymmetric subspace) and picks Peirce-1 off-diagonal 3-dim as the default interpretation of 'wedge component'. User-override path at checkpoint is named.
    (c) Section 3 has all five test categories (CLOSURE, S1, S3, S4, NEGATIVE) with concrete test-case sketches.
    (d) Section 4 cites Phase 54 `closeout-sympy.py` and Phase 55 `s4-sympy-spot-check.py` by full path.
    (e) Section 5 states < 30s runtime budget and exit-code-0 PASS criterion.
    (f) Section 6 explicitly routes each FAIL mode.
    (g) fp-wedge-component-silent-interpretation forbidden proxy rejected: Section 1 explicitly acknowledges the roadmap's ambiguity and names the reconstruction.
    (h) No code executed in this task; sympy-design.md is a design document only.
  </verify>
  <done>
    sympy-design.md exists with all five test cases designed, wedge-component ambiguity resolved, Phase 54/55 reuse plan explicit, runtime budget set, FAIL routing spelled out.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 6: Human-decision checkpoint — lock Plan 56-02 routing (sense target + face-status handling + wedge interpretation)</name>
  <files>.gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/56-01-SUMMARY.md</files>
  <action>
    Present Tasks 1-5 outputs to the user and ask for EXPLICIT confirmation on three decisions.

    Write 56-01-SUMMARY.md with the following sections:
      - Outcome: CLASSIFICATION + DESIGN COMPLETE (or escalation if a sub-task surfaced a blocker)
      - Hand-off block for Plan 56-02 (populated AFTER checkpoint)
      - Task-1 output: pointer to thm-5-8-identity-verbatim.md with 2-sentence highlight (which file:line is the target quote; no `\\label{thm:5.8}` in Paper 5)
      - Task-2 output: pointer to downstream-consumer-scan.md with 2-sentence summary (count of (a)/(b)/(c) consumers; `sms:minimal` classification)
      - Task-3 output: pointer to w-face-status.md with verdict + 1-sentence routing implication
      - Task-4 output: pointer to carries-senses.md with 1-sentence recap of the collapse diagram and sense (b) lock-in for `sms:minimal`
      - Task-5 output: pointer to sympy-design.md with 2-sentence summary (wedge interpretation + test set)

    Then present to user (via Claude's natural output channel — not by writing a new file, just by the agent's response text before/after writing 56-01-SUMMARY.md):

    ```
    ## Plan 56-01 Checkpoint — Routing Decisions Needed

    Three decisions to lock Plan 56-02:

    ### Decision 1: Target 'carries' sense

    Default: sense (b) [induced-structure SPS] + sense (c) upgrade if cheap (sense (c) is automatic here because 1_W = 1_V).

    Alternative: sense (b) only — sufficient for `sms:minimal` but not for monoidal-category downstream uses.

    Evidence: carries-senses.md §7 (sense (b) is the minimum for `sms:minimal`); downstream-consumer-scan.md §6-8 (sense map by consumer).

    Recommended: sense (b) + sense (c). Low marginal cost.

    ### Decision 2: Face-status handling

    Default: proceed with direct S1-S7 on W via vdW 2019 Def. 4 + Thm 1, regardless of face-status verdict. Face-restriction is bonus (never primary).

    Alternative: only attempt face-restriction route (fragile; depends on IS-FACE verdict).

    Evidence: w-face-status.md §3-6 (verdict + routing).

    Recommended: direct S1-S7 via vdW 2019 Def. 4. Robust to NOT-FACE or INCONCLUSIVE.

    ### Decision 3: H_3(ℝ) 'wedge component (3-dim antisymmetric)' interpretation

    Default: Peirce-1 off-diagonal 3-dim subspace of H_3(ℝ) w.r.t. diagonal rank-1 projector family {diag(1,0,0), diag(0,1,0), diag(0,0,1)}.

    Alternative A: Literal Λ²(ℝ³) ≅ ℝ³ — not naturally inside H_3(ℝ); non-sequitur for Paper 5.
    Alternative B: Full H_3(ℝ) ⊗ H_3(ℝ) (36-dim) without specialization.

    Evidence: sympy-design.md §1 (H_3(ℝ) has no literal antisymmetric subspace; reconstruction required).

    Recommended: Peirce-1 off-diagonal 3-dim as default; run TEST-CLOSURE and TEST-S1 on BOTH the specialized 9-dim W_wedge AND the full 36-dim W_test for completeness.

    **User, please confirm or override each of the three defaults.** If you type "approved all defaults" or "default", Plan 56-02 proceeds with: sense (b)+(c) target, direct S1-S7 via vdW 2019 Def. 4, Peirce-1 off-diagonal interpretation.
    ```

    AWAIT user response. On user response:
      - Record each decision verbatim in 56-01-SUMMARY.md's hand-off block, with a timestamp and the user's exact confirmation text quoted.
      - Populate the Plan 56-02 routing: approach (direct S1-S7 via vdW 2019 Def. 4), target sense (per user decision), SymPy test set (per user decision 3).
      - If the user overrides, do NOT silently proceed — update the hand-off block AND note the override's implication for Plan 56-02 scope.

    If the user does NOT respond within the checkpoint window OR responds with a blocker, return status: checkpoint in the structured return envelope and do NOT commit the summary as final.
  </action>
  <verify>
    (a) 56-01-SUMMARY.md has an "Outcome" line, pointers to all five Wave-1 artifacts, and a Hand-off Block for Plan 56-02.
    (b) Hand-off Block records user's explicit confirmation on all three decisions (sense target, face-status handling, wedge interpretation) with timestamp and quoted confirmation text.
    (c) Plan 56-02 routing is locked unambiguously: approach = direct S1-S7 via vdW 2019 Def. 4 + Thm 1; target sense = per user; SymPy test set = per user.
    (d) fp-checkpoint-bypass forbidden proxy rejected: user confirmation is recorded, not skipped.
    (e) If the user overrides a default, the override rationale is captured in the hand-off block.
  </verify>
  <done>
    56-01-SUMMARY.md committed with hand-off block locking Plan 56-02 routing. User has explicitly confirmed all three decisions (or explicitly overridden with captured rationale). No forbidden proxies active.
  </done>
</task>

</tasks>

<verification>
Plan-level checks (run after all tasks):

1. **Artifact completeness:** all five Wave-1 artifacts (identity, consumer-scan, face-status, carries-senses, sympy-design) exist under `.gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/` with non-empty content.
2. **Frozen-file integrity:** `git -C /Users/ehrlich/repos/blog diff --stat HEAD -- landing/papers/qm-from-self-modeling/main-jmp-submitted.tex` returns zero changes. Plan 56-01 is read-only against paper files.
3. **A-S citation discipline:** `grep -nE 'AlfsenShultz(2001|2003)' .gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/*.md` — every hit is in bracketed `\\cite[Ch.~X, Prop.~Y.Z]{}` form with X ≤ 8 (or flagged VERIFICATION-DEFERRED).
4. **Forbidden tokens outside transcription scope:** `grep -nE 'Thm 9\\.37|M_n\\(C\\)\\^\\{sa\\}|Hanche-Olsen' .gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/*.md` — every hit is inside a fenced code block labeled as a quote from research or prior artifacts.
5. **Three-senses discipline:** every occurrence of 'carries' / 'inherits' / 'respects' / 'extends' in the Wave-1 artifacts is either (a) followed by a sense tag `(a)`/`(b)`/`(c)`, (b) inside a quoted block, or (c) in the carries-senses.md document itself where it is being formally defined.
6. **Checkpoint completion:** 56-01-SUMMARY.md hand-off block records user confirmation on all three decisions; Plan 56-02 routing is locked.
7. **Phase 54 (C-i) R11 mitigation:** `grep -nE 'Peirce|peirce' .gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/*.md` — every hit that claims factor-level Peirce invariance cites `\\ref{lem:peirce-preservation}` (directly or by pointer to carries-senses.md).
</verification>

<success_criteria>
- thm-5-8-identity-verbatim.md: Thm 5.8 mapped to `thm:local-tomo` upper-bound; verbatim quotes at composite-lt.tex L203-221 + appendix-proofs.tex L228-238 with line numbers; ambiguity ('Thm 5.8' is informal shorthand) flagged.
- downstream-consumer-scan.md: every `thm:local-tomo` / `V_{BM}` / 'product-form' / 'carries' / 'sms:minimal' hit in composite-lt.tex + type-exclusion.tex + discussion.tex + appendix-proofs.tex classified by required sense; `sms:minimal` tagged sense (b); recommendation for Plan 56-02 target sense provided.
- w-face-status.md: verdict ∈ {IS-FACE, NOT-FACE, INCONCLUSIVE-DEFAULT-TO-NOT-FACE} with evidence; routing implication names direct S1-S7 via vdW 2019 Def. 4 as primary path regardless of verdict.
- carries-senses.md: three senses formally defined; collapse diagram ((c)⇒(b)⇒(a); (a)⇏(b) via Gudder-Greechie 2002 Ex. 39; (b)⇒(c) here by 1_W = 1_V); sense (b) lock-in for `sms:minimal`.
- sympy-design.md: 'wedge component' ambiguity explicitly resolved (Peirce-1 off-diagonal 3-dim default); test cases for CLOSURE, S1, S3, S4, NEGATIVE specified; Phase 54/55 reuse plan stated; runtime < 30s budget; exit-code-0 PASS.
- 56-01-SUMMARY.md: user-confirmed routing locked for Plan 56-02 (sense target, face-status handling, wedge interpretation); hand-off block populated.
- main-jmp-submitted.tex zero-diff preserved.
- Every forbidden proxy explicitly rejected in task <done> sections: fp-identity-paraphrase, fp-face-status-handwave, fp-face-status-shortcut-assumption, fp-carries-sense-collapse, fp-wedge-component-silent-interpretation, fp-frozen-file-edit, fp-checkpoint-bypass, fp-phase-54-ci-bypass.
</success_criteria>

<output>
After completion, create `.gpd/phases/56-thm-5-8-upper-bound-w-carries-product-form-sequential-product/56-01-SUMMARY.md` (already scheduled as Task 6 output).
</output>
