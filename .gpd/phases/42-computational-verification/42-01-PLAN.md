---
phase: 42-computational-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [code/verify_sequential_product.py]
interactive: false

conventions:
  units: "dimensionless (pure algebra)"
  clifford_signature: "Cl(9,0) positive definite"
  clifford_normalization: "{T_a, T_b} = (1/2)*delta_{ab}*I_16"
  sequential_product: "a & b = sqrt(a) b sqrt(a)"
  sqrt_branch: "principal branch, Re(sqrt(z)) >= 0"
  effect_definition: "E_a = (I + 2*T_a)/2"
  basis: "V_{1/2} = (x2_0,...,x2_7, x3_0,...,x3_7)"

dimensional_check:
  T_a: "[16x16 real symmetric matrix]"
  sqrt_T_a: "[16x16 complex matrix]"
  sequential_product_offdiag: "[16x16 purely imaginary matrix = (i/2)*T_b]"
  sequential_product_diag: "[16x16 real matrix = (1/4)*I_16]"
  E_a: "[16x16 real projection matrix]"
  effect_product: "[16x16 real matrix = (1/2)*E_a]"

approximations: []

estimated_execution:
  total_minutes: 25
  breakdown:
    - task: 1
      minutes: 15
      note: "Write verification script with SymPy exact + NumPy numerical checks for all 153 pairs"
    - task: 2
      minutes: 10
      note: "Run script, confirm all checks pass, record results in SUMMARY"

contract:
  schema_version: 1
  scope:
    question: "Does sqrt(T_a) T_b sqrt(T_a) = (i/2)*T_b hold for all 72 anticommuting Cl(9,0) generator pairs, confirming the sequential product exits M_16(R) into M_16(C)?"

  claims:
    - id: claim-offdiag-identity
      statement: "sqrt(T_a) T_b sqrt(T_a) = (i/2)*T_b for all 72 anticommuting pairs (a != b), verified to machine precision and exactly via SymPy"
      deliverables: [deliv-script]
      acceptance_tests: [test-offdiag-numpy, test-offdiag-sympy, test-imag-nonzero]
      references: [ref-T-matrices, ref-sqrt-formula, ref-gudder-greechie]
    - id: claim-diag-identity
      statement: "sqrt(T_a) T_a sqrt(T_a) = (1/4)*I_16 for all 9 diagonal pairs"
      deliverables: [deliv-script]
      acceptance_tests: [test-diag]
      references: [ref-T-matrices, ref-sqrt-formula]
    - id: claim-effect-real
      statement: "Effect algebra control: E_a & E_b = (1/2)*E_a for all 72 off-diagonal pairs, confirming effects stay real"
      deliverables: [deliv-script]
      acceptance_tests: [test-effect-identity, test-effect-real]
      references: [ref-T-matrices]
    - id: claim-exit-real
      statement: "The imaginary part of sqrt(T_a) T_b sqrt(T_a) is nonzero for all 72 anticommuting pairs, explicitly demonstrating exit from M_16(R)"
      deliverables: [deliv-script]
      acceptance_tests: [test-imag-nonzero]
      references: [ref-T-matrices, ref-sqrt-formula]

  deliverables:
    - id: deliv-script
      kind: code
      path: "code/verify_sequential_product.py"
      description: "Self-contained Python script with SymPy exact + NumPy numerical verification for all 72 off-diagonal, 9 diagonal, and 72 effect-algebra pairs"
      must_contain:
        - "get_traceless_generators() import"
        - "closed-form sqrt formula"
        - "anticommutation precondition check"
        - "sqrt^2 = T_a sanity check"
        - "off-diagonal identity check for ALL 72 pairs"
        - "diagonal identity check for ALL 9 pairs"
        - "effect algebra check for ALL 72 pairs"
        - "imaginary norm check for ALL 72 pairs"
        - "SymPy exact verification"
        - "pass/fail summary with counts"

  references:
    - id: ref-T-matrices
      kind: prior_artifact
      locator: "code/effective_hamiltonian.py::get_traceless_generators()"
      aliases: ["T_a matrices from v8.0 Phase 29"]
      role: definition
      why_it_matters: "Source of truth for the 9 Cl(9,0) generators in M_16(R); wrong normalization invalidates all downstream checks"
      applies_to: [claim-offdiag-identity, claim-diag-identity, claim-effect-real, claim-exit-real]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [read]
    - id: ref-sqrt-formula
      kind: prior_artifact
      locator: "Closed-form: sqrt(T_a) = ((1+i)I + (1-i)2T_a)/(2*sqrt(2))"
      aliases: ["closed-form sqrt from COMPUTATIONAL.md"]
      role: method
      why_it_matters: "Exact formula avoiding eigendecomposition; must be verified via sqrt^2 = T_a before use"
      applies_to: [claim-offdiag-identity, claim-diag-identity, claim-exit-real]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [compare]
    - id: ref-gudder-greechie
      kind: paper
      locator: "Gudder & Greechie, Rep. Math. Phys. 49 (2002) 87-111"
      aliases: ["Gudder-Greechie 2002"]
      role: definition
      why_it_matters: "Defines sequential product a & b = sqrt(a) b sqrt(a) for effects; Phase 42 extends to indefinite operators"
      applies_to: [claim-offdiag-identity]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [cite]
    - id: ref-clifford-anticomm
      kind: prior_artifact
      locator: "Cl(9,0) anticommutation: {T_a, T_b} = (1/2)*delta_{ab}*I_16"
      aliases: ["Clifford anticommutation from Phase 28/29"]
      role: benchmark
      why_it_matters: "Precondition for the entire computation; if generators are wrong, everything downstream is meaningless"
      applies_to: [claim-offdiag-identity, claim-diag-identity]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [compare]

  acceptance_tests:
    - id: test-precondition
      subject: claim-offdiag-identity
      kind: benchmark
      procedure: "Verify {T_a, T_b} = (1/2)*delta_{ab}*I_16 for all 81 pairs via Frobenius norm of T_a@T_b + T_b@T_a - (1/2)*delta_{ab}*I"
      pass_condition: "Max Frobenius error across all 81 pairs < 1e-14"
      evidence_required: [deliv-script, ref-T-matrices, ref-clifford-anticomm]
      automation: automated
    - id: test-sqrt-sanity
      subject: claim-offdiag-identity
      kind: consistency
      procedure: "Verify sqrt(T_a)^2 = T_a for all 9 generators via Frobenius norm"
      pass_condition: "Max error across 9 generators < 1e-14"
      evidence_required: [deliv-script, ref-sqrt-formula]
      automation: automated
    - id: test-offdiag-numpy
      subject: claim-offdiag-identity
      kind: benchmark
      procedure: "Compute ||sqrt(T_a) T_b sqrt(T_a) - (i/2)*T_b||_F for all 72 anticommuting pairs using NumPy"
      pass_condition: "Max Frobenius error across all 72 pairs < 1e-14"
      evidence_required: [deliv-script, ref-T-matrices]
      automation: automated
    - id: test-offdiag-sympy
      subject: claim-offdiag-identity
      kind: benchmark
      procedure: "Compute sqrt(T_a) T_b sqrt(T_a) - (i/2)*T_b using SymPy exact arithmetic (Rational entries + sqrt(2) + I) and verify the matrix is identically zero for all 72 pairs"
      pass_condition: "SymPy simplify yields exact zero matrix for all 72 pairs"
      evidence_required: [deliv-script, ref-T-matrices]
      automation: automated
    - id: test-imag-nonzero
      subject: claim-exit-real
      kind: consistency
      procedure: "Compute ||Im(sqrt(T_a) T_b sqrt(T_a))||_F for all 72 off-diagonal pairs"
      pass_condition: "Min imaginary Frobenius norm across 72 pairs > 0.5 (expected: ~1.0)"
      evidence_required: [deliv-script]
      automation: automated
    - id: test-diag
      subject: claim-diag-identity
      kind: benchmark
      procedure: "Compute ||sqrt(T_a) T_a sqrt(T_a) - (1/4)*I_16||_F for all 9 diagonal pairs"
      pass_condition: "Max error across 9 diagonal pairs < 1e-14"
      evidence_required: [deliv-script]
      automation: automated
    - id: test-effect-identity
      subject: claim-effect-real
      kind: benchmark
      procedure: "Compute ||E_a @ E_b @ E_a - (1/2)*E_a||_F for all 72 off-diagonal pairs where E_a = (I + 2*T_a)/2"
      pass_condition: "Max error across 72 pairs < 1e-14"
      evidence_required: [deliv-script]
      automation: automated
    - id: test-effect-real
      subject: claim-effect-real
      kind: consistency
      procedure: "Verify ||Im(E_a @ E_b @ E_a)||_F = 0 for all 72 off-diagonal pairs (real matrices produce real products)"
      pass_condition: "Max imaginary Frobenius norm = 0.0 exactly"
      evidence_required: [deliv-script]
      automation: automated

  forbidden_proxies:
    - id: fp-partial
      subject: claim-offdiag-identity
      proxy: "Checking a subset of the 72 pairs and declaring success"
      reason: "Contract requires ALL 72 anticommuting pairs; any single failure kills the route"
    - id: fp-spotcheck
      subject: claim-offdiag-identity
      proxy: "Spot-checking a few representative generators (e.g., only T_1, T_2)"
      reason: "Different generators have different matrix structures from the Fano triple construction; universality must be verified exhaustively"
    - id: fp-numpy-only
      subject: claim-offdiag-identity
      proxy: "Numerical verification only without SymPy exact proof"
      reason: "Numerical agreement to 1e-14 is strong evidence but not algebraic proof; SymPy exact zero is required for proof-level certainty"
    - id: fp-wrong-sqrt
      subject: claim-offdiag-identity
      proxy: "Using fisher_metric._matrix_sqrt or scipy.linalg.sqrtm instead of the closed-form formula"
      reason: "_matrix_sqrt clips negative eigenvalues (designed for PSD); sqrtm may choose wrong branch; both produce incorrect results for indefinite operators"

  uncertainty_markers:
    weakest_anchors:
      - "Branch choice for sqrt(T_a): principal branch gives +i, negative branch gives -i. The EXISTENCE of the imaginary part is branch-independent; only its sign depends on the branch."
    disconfirming_observations:
      - "Any single pair (a,b) with a != b where ||sqrt(T_a) T_b sqrt(T_a) - (i/2)*T_b||_F > 1e-14 would falsify the identity and kill the sequential product route"
      - "If SymPy fails to simplify to exact zero for any pair, the closed-form formula has an error"

  links:
    - id: link-precondition-to-offdiag
      source: ref-clifford-anticomm
      target: claim-offdiag-identity
      relation: depends_on
      verified_by: [test-precondition]
    - id: link-sqrt-to-offdiag
      source: ref-sqrt-formula
      target: claim-offdiag-identity
      relation: supports
      verified_by: [test-sqrt-sanity]
    - id: link-script-to-claims
      source: deliv-script
      target: claim-offdiag-identity
      relation: computes
      verified_by: [test-offdiag-numpy, test-offdiag-sympy]
---

<objective>
Verify the sequential product identity sqrt(T_a) T_b sqrt(T_a) = (i/2)*T_b for all 72 anticommuting Cl(9,0) generator pairs, establishing the computational ground truth that the sequential product exits M_16(R) into M_16(C). Verify diagonal pairs give (1/4)*I_16 and effect algebra stays real as a control.

Purpose: GO/NO-GO gate for v11.0 Gap C closure via sequential product route. If any pair fails, the route is dead.
Output: Self-contained verification script (code/verify_sequential_product.py) with both SymPy exact and NumPy numerical verification for all 153 pairs.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/42-computational-verification/42-RESEARCH.md
@code/effective_hamiltonian.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write exhaustive verification script</name>
  <files>code/verify_sequential_product.py</files>
  <action>
    Create a self-contained verification script that checks all 4 success criteria from the Phase 42 roadmap.

    **Imports and setup:**
    - Import get_traceless_generators from effective_hamiltonian (NOT compute_T_b_matrices -- wrong normalization)
    - Import numpy and sympy (Matrix, Rational, sqrt as sp_sqrt, I as sp_I, zeros as sp_zeros, eye as sp_eye, simplify)
    - Do NOT import fisher_metric._matrix_sqrt (clips negative eigenvalues, WRONG for indefinite operators)
    - Do NOT use scipy.linalg.sqrtm (may choose wrong branch)

    **Helper: closed-form sqrt**
    - Implement sqrt_Ta(T) = ((1+i)*I + (1-i)*2*T) / (2*sqrt(2)) for NumPy arrays
    - Implement the same for SymPy matrices using sp_I and sp_sqrt(2)
    - This formula exploits eigenvalues +/-1/2: sqrt(+1/2) = (1+i)/(2*sqrt(2)), sqrt(-1/2) = (1-i)*(-1)/(2*sqrt(2)) = i*(1-i)/(2*sqrt(2)). Wait -- use the formula as written: ((1+i)*I + (1-i)*2*T_a) / (2*sqrt(2)). This is verified to satisfy sqrt(T_a)^2 = T_a.

    **Helper: NumPy-to-SymPy conversion**
    - Convert each T_a entry to exact Rational: for each entry x, use Rational(int(round(2*x)), 2) since all entries are exactly +/-1/2 or 0
    - Verify round-trip: the Rational matrix converted back to float must match the original to < 1e-15

    **Section 0: Load and precondition check**
    - Load 9 generators via get_traceless_generators()
    - Verify anticommutation: for all 81 (a,b) pairs, check ||T_a @ T_b + T_b @ T_a - (1/2)*delta_{ab}*I_16||_F < 1e-14
    - Print count: "Precondition: 81/81 anticommutation checks passed"
    - If ANY fails, print the failing pair and ABORT

    **Section 1: Sqrt construction and sanity**
    - Compute sqrt_Ta for all 9 generators (NumPy)
    - Verify sqrt(T_a)^2 = T_a for all 9: ||sT @ sT - T_a||_F < 1e-14
    - Print: "Sqrt sanity: 9/9 checks passed"

    **Section 2: Off-diagonal sequential product (72 pairs, NumPy)**
    - For all (a,b) with a != b (72 pairs): compute sT_a @ T_b @ sT_a
    - Check ||result - (1j/2)*T_b||_F < 1e-14
    - Also check ||Im(result)||_F > 0.5 (expected ~1.0 = ||T_b||_F / 2)
    - Also check ||Re(result)||_F < 1e-14 (real part should vanish)
    - Track max_error, min_imag_norm, max_real_norm across all 72 pairs
    - Print: "Off-diagonal: 72/72 passed, max_error={}, min_imag_norm={}, max_real_norm={}"

    **Section 3: Diagonal sequential product (9 pairs, NumPy)**
    - For all a: compute sT_a @ T_a @ sT_a
    - Check ||result - (1/4)*I_16||_F < 1e-14
    - Print: "Diagonal: 9/9 passed, max_error={}"

    **Section 4: Effect algebra control (72 pairs, NumPy)**
    - Compute E_a = (I + 2*T_a)/2 for all 9 generators
    - Verify E_a^2 = E_a for all 9 (projection check)
    - For all (a,b) with a != b: compute E_a @ E_b @ E_a
    - Check ||result - (1/2)*E_a||_F < 1e-14
    - Check ||Im(result)||_F == 0.0 exactly (real matrices produce real products)
    - Print: "Effect algebra: 72/72 passed, max_error={}, max_imag_norm={}"

    **Section 5: SymPy exact verification (72 off-diagonal + 9 diagonal)**
    - Convert all 9 T_a to SymPy exact Rational matrices
    - Compute SymPy sqrt_Ta for all 9
    - Verify sqrt^2 = T_a exactly (simplify to zero matrix)
    - For all 72 off-diagonal: compute sT_a * T_b * sT_a, subtract (sp_I/2)*T_b, simplify, verify == zero matrix
    - For all 9 diagonal: compute sT_a * T_a * sT_a, subtract Rational(1,4)*eye(16), simplify, verify == zero matrix
    - Print: "SymPy exact: 72/72 off-diagonal, 9/9 diagonal -- all identically zero"

    **Section 6: GO/NO-GO summary**
    - Print clear GO/NO-GO verdict
    - Print total pair counts: "Verified: 72 off-diagonal + 9 diagonal + 72 effect = 153 total pairs"
    - Print: "Sequential product exits M_16(R): YES (imaginary part nonzero for all 72 anticommuting pairs)"
    - Print: "Effect algebra stays real: YES (imaginary part zero for all 72 effect pairs)"
    - If any section failed, print "NO-GO: Sequential product route is dead. Proceed to Phase 45."
    - Exit with code 0 on full pass, code 1 on any failure

    **Script structure:** Use if __name__ == '__main__' with sys.exit. Print section headers clearly. Track all errors with descriptive messages.

    **CRITICAL anti-patterns to avoid:**
    - Do NOT use compute_T_b_matrices() -- wrong normalization for T_b[1]
    - Do NOT use fisher_metric._matrix_sqrt -- clips negative eigenvalues
    - Do NOT use scipy.linalg.sqrtm -- wrong branch risk
    - Do NOT check fewer than 72 off-diagonal pairs -- contract forbids partial verification
    - Do NOT use SymPy Float (from .tolist()) -- use explicit Rational conversion to avoid float contamination
  </action>
  <verify>
    1. Script imports get_traceless_generators (not compute_T_b_matrices)
    2. Script does NOT import fisher_metric._matrix_sqrt or scipy.linalg.sqrtm
    3. Script checks ALL 81 anticommutation pairs as precondition
    4. Script checks ALL 72 off-diagonal + 9 diagonal + 72 effect = 153 pairs
    5. Script uses closed-form sqrt formula (not eigendecomposition)
    6. Script converts to SymPy Rational (not Float) for exact verification
    7. Script reports pass/fail counts and max errors for each section
    8. Script prints explicit GO/NO-GO verdict
    9. Script exits with code 0 on pass, code 1 on failure
  </verify>
  <done>Verification script written at code/verify_sequential_product.py with all 6 sections (precondition, sqrt sanity, off-diagonal, diagonal, effect algebra, SymPy exact) covering all 153 pairs</done>
</task>

<task type="auto">
  <name>Task 2: Execute verification and confirm GO/NO-GO</name>
  <files>code/verify_sequential_product.py</files>
  <action>
    Run the verification script and confirm all checks pass.

    ```bash
    cd /Users/ehrlich/scratch/get-physics-done
    python3 code/verify_sequential_product.py
    ```

    **Expected output (from RESEARCH.md pre-verification):**
    - Precondition: 81/81 passed
    - Sqrt sanity: 9/9 passed
    - Off-diagonal: 72/72 passed, max_error < 5e-16, min_imag_norm ~ 1.0
    - Diagonal: 9/9 passed, max_error < 5e-16
    - Effect algebra: 72/72 passed, max_error < 5e-16, max_imag_norm = 0.0
    - SymPy exact: all identically zero
    - GO verdict

    **If any section fails:**
    - Read the error output carefully
    - If precondition fails: check import (get_traceless_generators vs compute_T_b_matrices)
    - If sqrt sanity fails: check the closed-form formula signs
    - If off-diagonal fails: check if the issue is branch choice (+i vs -i); if -(i/2)*T_b, the identity still holds with opposite branch (still proves exit from M_16(R))
    - If SymPy fails to simplify: try expand() before simplify(); check for Float contamination
    - Fix and re-run until all pass or a genuine failure is identified

    **If genuine failure (identity does not hold):**
    - This triggers the GO/NO-GO gate: "Sequential product route is dead"
    - Do NOT attempt to fix the mathematics -- record the failure faithfully
    - The SUMMARY must state NO-GO with the specific failing pair(s)

    **After successful run:**
    - Verify exit code 0
    - Confirm the output matches expected values within tolerance
    - No modifications to the script should be needed if Task 1 was correct
  </action>
  <verify>
    1. Script runs without import errors or exceptions
    2. All 81 anticommutation precondition checks pass (< 1e-14)
    3. All 9 sqrt sanity checks pass (< 1e-14)
    4. All 72 off-diagonal identity checks pass (< 1e-14)
    5. All 72 imaginary norm checks pass (> 0.5)
    6. All 9 diagonal identity checks pass (< 1e-14)
    7. All 72 effect identity checks pass (< 1e-14)
    8. All 72 effect reality checks pass (imaginary norm = 0.0)
    9. SymPy exact verification yields zero for all 81 pairs
    10. Script prints GO verdict and exits with code 0
    11. Actual max errors are < 5e-16 (well within 1e-14 tolerance)
  </verify>
  <done>Script executes successfully, all 153 pairs verified (72 off-diagonal + 9 diagonal + 72 effect), GO verdict confirmed, sequential product exits M_16(R) into M_16(C) for all 72 anticommuting pairs, effect algebra stays real for all 72 pairs</done>
</task>

</tasks>

<verification>
- Clifford precondition: {T_a, T_b} = (1/2)*delta_{ab}*I_16 verified for all 81 pairs before any computation
- Sqrt consistency: sqrt(T_a)^2 = T_a verified for all 9 generators
- Off-diagonal identity: sqrt(T_a) T_b sqrt(T_a) = (i/2)*T_b for all 72 anticommuting pairs (NumPy + SymPy)
- Imaginary exit: ||Im(result)||_F > 0.5 for all 72 off-diagonal pairs (explicit M_16(R) exit)
- Diagonal identity: sqrt(T_a) T_a sqrt(T_a) = (1/4)*I_16 for all 9 diagonal pairs
- Effect algebra control: E_a E_b E_a = (1/2)*E_a for all 72 off-diagonal effect pairs, imaginary part identically zero
- Cross-method: SymPy exact algebraic proof agrees with NumPy numerical verification
- Exhaustiveness: ALL 153 pairs checked, zero exceptions (contract forbids partial verification)
</verification>

<success_criteria>
All 4 roadmap success criteria met:
1. sqrt(T_a) T_b sqrt(T_a) = (i/2)*T_b verified for all 72 anticommuting pairs to < 1e-14 (actual < 5e-16)
2. sqrt(T_a) T_a sqrt(T_a) = (1/4)*I_16 verified for all 9 diagonal pairs to < 1e-14
3. E_a & E_b = (1/2)*E_a verified for all 72 off-diagonal effect pairs, effects stay real
4. Im(sqrt(T_a) T_b sqrt(T_a)) nonzero for all 72 anticommuting pairs (Frobenius norm ~ 1.0)
Plus: SymPy exact algebraic verification yields identically zero for all 81 sequential product pairs.
GO/NO-GO gate: GO (or NO-GO with specific failing pairs documented).
</success_criteria>

<output>
After completion, create `.gpd/phases/42-computational-verification/42-01-SUMMARY.md`
</output>
