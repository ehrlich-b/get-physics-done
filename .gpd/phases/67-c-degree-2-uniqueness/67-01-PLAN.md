---
phase: 67-c-degree-2-uniqueness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [code/degree2_uniqueness.py]
interactive: false

conventions:
  field: "exact over Q (SymPy Rational / DomainMatrix over QQ); NEVER float64"
  rank: "exact_qq_rank = DomainMatrix.from_Matrix(A).convert_to(QQ).rank() at width >27; sympy.Matrix.rank()/nullspace() over QQ for small blocks (<=54 cols) -- NEVER numpy.linalg.matrix_rank / SVD tolerance"
  group: "F_4 = Aut(h_3(O)), compact 52-dim (NOT E_6); fixes Tr, trace form, det. c = Tr(X o Y) is F_4-invariant but NOT E_6-invariant (Blind 2011 contrast)"
  rep: "27 = 1 (trivial / Tr-identity direction) (+) 26 (trace-free irreducible, self-dual). NEVER treat 27 as irreducible (Pitfall 9 / 26-vs-27)"
  jordan: "X o Y = (1/2)(XY + YX)  [E.jordan]"
  coupling: "c = Tr(X o Y) = E.inv_c = Tr(jordan(Xsym, Ysym)); bidegree (1,1); c(X,X) = Tr X^2 (NOT (Tr X)^2)"
  trace_normalization: "Tr = sum of 3 real diagonal entries; Tr(I) = 3 (=> Tr(I)Tr(I)=9 != c(I,I)=Tr(I)=3 -- the linear-independence witness)"
  leibniz_lift: "rho(M)(v(x)w) = (Mv)(x)w + v(x)(Mw) = M(x)I + I(x)M  (NOT M(x)M -- the group-like error). Equivalently the diagonal D_M f = grad_X(f).(M.v_x) + grad_Y(f).(M.v_y) contraction"
  f4_basis: "the 52-independent rref subset of the 324 inner derivations [_f4_basis() -> _select_independent_basis(E.inner_derivations())]; certified Phase 65; REUSE verbatim, do NOT rebuild"
  coordinate_layout: "54 symbols xs = x0:27 (X), ys = y0:27 (Y); 27(x)27 coordinate order = _flatten_729 row-major (r outer, col inner)"
  R_pt: "FROZEN R-subalgebra generated by {Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y} (Phase 64 frozen definition). Tr(X)Tr(Y) in R_pt (product of pointwise gens); c NOT in R_pt. Do NOT redefine 'reducible'/'pointwise' to move c (Pitfall 7 reward-hack)"
  metric: "Riemannian Fisher (pure algebra; no field theory / gauge / Fourier -- those convention fields are n/a)"

dimensional_check:
  bidegree_11_trivial_mult: "2 (= dim End_{F_4}(1 (+) 26) = 1^2 + 1^2 via Schur) = span{Tr(X)Tr(Y), Tr(X o Y)}"
  total_degree2_invariant_dim: "6 (blocks (2,0)=2 {(Tr X)^2, Tr X^2}, (0,2)=2 {(Tr Y)^2, Tr Y^2}, (1,1)=2 {Tr(X)Tr(Y), c})"
  genuine_coupling_quotient: "1 (= (1,1) dim 2 - reducible-product dim 1) => c the UNIQUE NEW degree-2 coupling generator, mod scale + additive multiples of Tr(X)Tr(Y)"
  sym2_27_dim: "378 (Sym^2(27); trivial multiplicity 2 = {(Tr X)^2, Tr X^2})"
  sym2_26_dim: "351 = 1 (+) 26 (+) 324 (Sym^2(26))"
  f4_basis_size: "52 (= dim f_4)"
  leibniz_block_widths: "(1,1) ordered block 27(x)27 = 729 columns (per-bidegree); full degree-2 monomial space the 6-dim total cross-check"

approximations:
  - name: "none -- exact symbolic proof over Q (integer rep-theory + exact rational nullspace)"
    parameter: "n/a"
    validity: "every quantity is an exact integer (a representation dimension or a Q-vector-space dimension); zero tolerance, no rounding"
    breaks_when: "n/a (pure exact-over-Q algebra)"
    check: "any float64 / numpy.linalg.matrix_rank / SVD-tolerance rank on the decisive path is a forbidden proxy (fp-float-rank); the exact-only source guard asserts 0 numpy float-rank calls + 0 octonion_algebra imports on the decisive path"

key_links:
    - from: "code/orbit_dimension_gate.py (_select_independent_basis(E.inner_derivations()), _flatten_729, exact_qq_rank, infinitesimal_action) + code/ring_generating_set.py (_f4_basis, check_f4_invariance recipe, CANDIDATE_GRADS, PAIR_POINTS) + code/ring_lemma_verification.py (E: jordan, Tr, c=inv_c, inv_Tr_X, inv_Tr_Y, X_from_symbols, _flat27, octonionic_points)"
      to: "code/degree2_uniqueness.py (the new harness reuses the certified engine via import; NEVER touches float64 octonion_algebra.py)"
      via: "import the frozen 52-generator f_4 basis + exact-QQ rank/nullspace + named invariants; the ONE new component is the Leibniz lift rho(M)=M(x)I+I(x)M (equivalently the diagonal gradient-contraction)"
      check: "engine imports resolve; SymPy 1.14.0; exact-only guard PASS (0 numpy float-rank, 0 octonion_algebra on the decisive path)"
    - from: "Route A integer rep-theory count (Schur: dim End_{F_4}(1(+)26) = 2) [LITERATURE-anchored corroboration]"
      to: "Route B exact f_4-kernel nullspace over QQ ((1,1) block invariant dim = 2) [the self-certifying proof]"
      via: "two-route agreement on the bidegree-(1,1) trivial multiplicity"
      check: "Route A integer (2) == Route B exact nullspace ((1,1) = 2) AND total degree-2 = 6; on DISAGREEMENT trust the exact nullspace (roadmap backtracking) and STOP for re-derivation -- do NOT tune the irrep arithmetic"
    - from: "the 2-dim (1,1) exact nullspace (abstract Q-span) + the named coordinate-vectors of Tr(X)Tr(Y) and Tr(X o Y)"
      to: "named-basis identification + mod-products quotient = 1"
      via: "show {coord(Tr(X)Tr(Y)), coord(c)} is a BASIS of the kernel (in-kernel AND linearly independent, Tr(I)Tr(I)=9 != c(I,I)=3); then the only reducible (1,1) element is span{Tr(X)Tr(Y)} so the quotient is span{c}"
      check: "dimension-match ALONE is insufficient -- the named invariants must be shown to SPAN the kernel; quotient 2-1 = 1 => c UNIQUE (mod products + pointwise), 'mod products' stated precisely"

contract:
  schema_version: 1
  scope:
    question: "Is c = Tr(X o Y) the UNIQUE degree-2 coupling generator of R[27(+)27]^{F_4} modulo scale, products, and pointwise terms -- i.e. is the bidegree-(1,1) trivial part exactly 2-dimensional = span{Tr(X)Tr(Y), Tr(X o Y)}, of which Tr(X)Tr(Y) is the single reducible product, leaving c as the unique genuine (irreducible, non-product) new degree-2 coupling invariant? Established by exact computation over Q (two-route: F_4 branching count + exact f_4-kernel nullspace), with the 'mod products' qualifier stated precisely."
  claims:
    - id: claim-branching
      statement: "Route A (F_4 rep-theory branching, LITERATURE-anchored corroboration): the integer facts hold exactly -- 27 = 1 (+) 26; dim Sym^2(27) = 378; dim Sym^2(26) = 351 = 1 + 26 + 324; trivial multiplicity in Sym^2(27) = 2; and the bidegree-(1,1) trivial multiplicity in 27_X (x) 27_Y = dim End_{F_4}(1(+)26) = 1^2 + 1^2 = 2 (Schur). Hence the (1,1) trivial part is 2-dimensional. All asserted as runnable integer checks with explicit dimension bookkeeping (1+26=27, 1+26+324=351, 52+273=325, 351+325=676, 1+(1*26)+351=378)."
      deliverables: [deliv-harness, deliv-verdict]
      acceptance_tests: [test-branching-integers, test-dim-bookkeeping]
      references: [ref-methods, ref-pitfalls, ref-wikipedia-slansky, ref-garibaldi-guralnick]
    - id: claim-leibniz-lift
      statement: "The Leibniz lift of an f_4 generator M (27x27) to the 27(x)27 space is rho(M) = M(x)I + I(x)M (the derivation action), NOT M(x)M. CORRECTNESS GUARD (before any dim count): rho(M) -- equivalently the diagonal contraction D_M f = grad_X(f).(M.v_x) + grad_Y(f).(M.v_y) -- annihilates the KNOWN invariant c = Tr(X o Y) for ALL 52 generators (this catches the M(x)M error)."
      deliverables: [deliv-harness]
      acceptance_tests: [test-leibniz-guard]
      references: [ref-orbit-gate, ref-ring-generating-set, ref-warm-engine]
    - id: claim-invariance-gate
      statement: "Invariance gate BEFORE any dimension is reported: D_M f = 0 over Q for BOTH named (1,1) candidates f in {Tr(X o Y), Tr(X)Tr(Y)} and for ALL 52 f_4 generators, at >= 3 genuinely octonionic rational points (reuse the check_f4_invariance recipe + octonionic_points()). A candidate not annihilated is not an invariant -- STOP, compute no dimension."
      deliverables: [deliv-harness]
      acceptance_tests: [test-invariance-gate]
      references: [ref-orbit-gate, ref-ring-generating-set, ref-warm-engine]
    - id: claim-nullspace-11
      statement: "Route B (the self-certifying proof): the exact f_4-infinitesimal-kernel nullspace over QQ on the bidegree-(1,1) 27_X(x)27_Y monomial space has dimension exactly 2 (= 729 - exact_qq_rank of the stacked 52 derivation operators on the (1,1) block, or the per-bidegree gradient-contraction kernel). The named invariants {Tr(X)Tr(Y), Tr(X o Y)} form a BASIS of this 2-dim kernel (both in the kernel AND linearly independent -- witness Tr(I)Tr(I)=9 != c(I,I)=Tr(I)=3). Dimension-match alone is insufficient; the named basis must be tied to the abstract count."
      deliverables: [deliv-harness, deliv-named-basis]
      acceptance_tests: [test-nullspace-11-dim, test-named-basis]
      references: [ref-orbit-gate, ref-warm-engine, ref-iltyakov]
    - id: claim-total-degree2
      statement: "Cross-check: the FULL degree-2 invariant space dimension is exactly 6 over Q, decomposing by bidegree as (2,0)=2 {(Tr X)^2, Tr X^2}, (0,2)=2 {(Tr Y)^2, Tr Y^2}, (1,1)=2 {Tr(X)Tr(Y), c} -- confirming the rep-theory count by direct exact computation."
      deliverables: [deliv-harness, deliv-verdict]
      acceptance_tests: [test-total-degree2]
      references: [ref-methods, ref-orbit-gate]
    - id: claim-mod-products-quotient
      statement: "Modulo products and pointwise terms, c is the UNIQUE new degree-2 coupling generator: the only degree-1 invariants are Tr(X) (1,0) and Tr(Y) (0,1); the only bidegree-(1,1) product of lower-degree invariants is Tr(X)*Tr(Y); so the reducible part of the (1,1) space is span{Tr(X)Tr(Y)} (1-dimensional), and the genuine-coupling quotient (2 - 1) = 1 = span{c}. c is unique up to scale and additive multiples of Tr(X)Tr(Y). 'Mod products' is stated precisely: Tr(X)Tr(Y) in R_pt (a product of pointwise generators), c NOT in R_pt at degree 2. The decision uses the bidegree argument directly (the only degree-<=2 R_pt element at bidegree (1,1) is a scalar multiple of Tr(X)Tr(Y))."
      deliverables: [deliv-harness, deliv-quotient-note]
      acceptance_tests: [test-quotient-1, test-rpt-membership]
      references: [ref-methods, ref-pitfalls, ref-warm-engine]
    - id: claim-two-route-agreement
      statement: "A uniqueness verdict is reported ONLY when Route A (integer count = 2) and Route B (exact nullspace (1,1) = 2) AGREE, with the total-degree-2 = 6 cross-check also holding. On disagreement the harness emits NO VERDICT and STOPs, trusting the EXACT NULLSPACE (roadmap backtracking) and flagging the irrep arithmetic for re-derivation -- it does NOT tune either route to force agreement."
      deliverables: [deliv-harness, deliv-verdict]
      acceptance_tests: [test-two-route-adjudicator]
      references: [ref-methods, ref-pitfalls, ref-orbit-gate]
  deliverables:
    - id: deliv-harness
      kind: code
      path: code/degree2_uniqueness.py
      description: "A single pre-registered, foreground-runnable (python -u, chatty) exact-over-Q harness: pre-registration block (the integer targets + nullspace test + verdict map frozen BEFORE evaluation); Route A integer branching asserts + dimension bookkeeping; the Leibniz lift rho(M)=M(x)I+I(x)M with its annihilate-c correctness guard; the invariance gate (D_M f=0, 52 gens, >=3 octonionic points, for c AND Tr(X)Tr(Y)); the exact (1,1)-block nullspace dim=2 + named-basis identification; the full degree-2 nullspace dim=6 cross-check; the mod-products quotient=1 with the degree-2 R_pt membership decision; the two-route adjudicator; and the exact-only source guard. main() exits 0 iff all checks pass AND the two routes agree. REUSES the frozen engine via import; NEVER touches float64 octonion_algebra.py on the decisive path."
      must_contain: ["exact_qq_rank", "M(x)I + I(x)M", "Tr(X o Y)", "Tr(X)Tr(Y)", "two-route", "PRE-REGISTER", "Tr(I)Tr(I)=9", "mod products"]
    - id: deliv-verdict
      kind: report
      path: code/degree2_uniqueness.py
      description: "The decisive printed verdict: the (1,1) trivial multiplicity (Route A integer 2 vs Route B exact nullspace 2), the total degree-2 dim (6), the genuine-coupling quotient (1), the two-route agreement line, and the uniqueness label ('c is the UNIQUE degree-2 coupling generator mod products + pointwise')."
      must_contain: ["VERDICT", "UNIQUE", "mod products"]
    - id: deliv-named-basis
      kind: report
      path: code/degree2_uniqueness.py
      description: "The named-basis identification: the coordinate vectors of Tr(X)Tr(Y) and Tr(X o Y) shown to lie in the (1,1) f_4-kernel AND be linearly independent (Tr(I)Tr(I)=9 != c(I,I)=3), hence a basis of the 2-dim kernel -- not merely a dimension match."
      must_contain: ["named basis", "linearly independent"]
    - id: deliv-quotient-note
      kind: note
      path: code/degree2_uniqueness.py
      description: "The precise 'mod products' statement and the degree-2 R_pt membership decision: the only bidegree-(1,1) pointwise product is Tr(X)Tr(Y) in R_pt; c not in R_pt at degree 2; quotient (2-1)=1 => c UNIQUE up to scale + additive multiples of Tr(X)Tr(Y). Records WHY the general is_in_Rpt stub is NOT called (it raises NotImplementedError -- a general decision procedure is unneeded; the degree-2 bidegree argument decides membership directly)."
      must_contain: ["mod products", "Tr(X)Tr(Y)", "bidegree (1,1)"]
    - id: deliv-consistency-note
      kind: note
      path: code/degree2_uniqueness.py
      description: "The scope + consistency statement: Phase 67 is the DEGREE-2 uniqueness sub-claim (RING-03), complementary to Phase 66's FIELD-level functional-independence SPINE (RING-02) and distinct from Phase 68 ring generation (RING-01). The (1,1) count 2 here is the bidegree-(1,1) Hilbert coefficient that Phase 68 must reproduce."
      must_contain: ["degree-2", "Phase 66", "Phase 68"]
  references:
    - id: ref-methods
      kind: prior_artifact
      locator: ".gpd/research/METHODS.md section (c) lines 105-118 (project-internal, derived and cross-checked)"
      aliases: ["METHODS (c)"]
      role: method
      why_it_matters: "Already derives the F_4 branching (27=1(+)26; Sym^2(27)=378; Sym^2(26)=351=1+26+324; trivial mult in 27(x)27 = dim End_{F_4}(1(+)26)=2 by Schur) and the precise 'mod products' statement with full reasoning. CITE; do NOT re-derive the conceptual chain. The total degree-2 = 6 target is here (line 114)."
      applies_to: [claim-branching, claim-total-degree2, claim-mod-products-quotient]
      carry_forward_to: [planning, verification, writing]
      must_surface: true
      required_actions: [read, cite]
    - id: ref-pitfalls
      kind: prior_artifact
      locator: ".gpd/research/PITFALLS.md Pitfall 9 (26-vs-27; reducible Tr(X)Tr(Y); the exact target integers {2, 6, 351, 378, 1(+)26(+)324})"
      aliases: ["Pitfall 9"]
      role: must_consider
      why_it_matters: "Names the #1 forbidden-proxy risk (conflating Sym^2(26) with Sym^2(27); forgetting the trivial summand of 27=1(+)26; dropping 'mod products'). READ; encode each target integer as a runnable assert."
      applies_to: [claim-branching, claim-mod-products-quotient, claim-two-route-agreement]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use]
    - id: ref-orbit-gate
      kind: prior_artifact
      locator: "code/orbit_dimension_gate.py (Phase 65, CERTIFIED)"
      aliases: ["orbit_dimension_gate"]
      role: method
      why_it_matters: "The certified 52-generator f_4 basis builder (_select_independent_basis(E.inner_derivations())), the row-major _flatten_729 (the 27(x)27 coordinate order), exact_qq_rank (DomainMatrix-over-QQ, the anti-float guard), infinitesimal_action, and the exact_only_guard pattern. REUSE verbatim for Route B; the f_4 generators must NOT be re-derived."
      applies_to: [claim-leibniz-lift, claim-invariance-gate, claim-nullspace-11, claim-total-degree2]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use]
    - id: ref-ring-generating-set
      kind: prior_artifact
      locator: "code/ring_generating_set.py (Phase 65.1, FROZEN)"
      aliases: ["ring_generating_set"]
      role: method
      why_it_matters: "_f4_basis() (returns the 52-independent basis + count), check_f4_invariance (the exact D_M f = grad_X.(M.v_x) + grad_Y.(M.v_y) gradient-split contraction recipe over Q, blocks [0:27]/[27:54], substitute-point-first), check_bidegree (scaling test), CANDIDATE_GRADS (CANDIDATE_GRADS[6] = grad of c; [0:3] X-pointwise, [3:6] Y-pointwise), PAIR_POINTS. REUSE the invariance-test pattern and the named invariants."
      applies_to: [claim-leibniz-lift, claim-invariance-gate, claim-nullspace-11]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use]
    - id: ref-warm-engine
      kind: prior_artifact
      locator: "code/ring_lemma_verification.py (Phase 64, FROZEN; imported as module E)"
      aliases: ["E", "ring_lemma_verification"]
      role: method
      why_it_matters: "Exact octonion + h_3(O) primitives over Q: jordan, Tr, Tr2, c(X,Y) / inv_c, inv_Tr_X, inv_Tr_Y, det_3, Xsym/Ysym, xs/ys, X_from_symbols, _flat27, octonionic_points, inner_derivations, is_in_Rpt (STUB -- raises; see uncertainty_markers). USE as the exact substrate; the trace form Tr(A o B) = E.Tr(E.jordan(A,B)) is c when A=X,B=Y. Do NOT modify."
      applies_to: [claim-leibniz-lift, claim-invariance-gate, claim-nullspace-11, claim-total-degree2, claim-mod-products-quotient]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [read, use]
    - id: ref-garibaldi-guralnick
      kind: paper
      locator: "Garibaldi, S.; Guralnick, R.M., 'Simple Groups Stabilizing Polynomials', Forum Math. Pi 3 (2015) e3"
      aliases: ["Garibaldi-Guralnick 2015"]
      role: benchmark
      why_it_matters: "F_4 = identity component of Stab(Tr, trace form, det): pins down WHY the trace form (= c) is THE F_4-invariant (1,1) pairing -- the genuine 26(x)26 -> 1 contraction -- and why it is not an E_6 invariant. Grounds the uniqueness-of-trace-form context for Route A."
      applies_to: [claim-branching]
      carry_forward_to: [planning, writing]
      must_surface: false
      required_actions: [cite]
    - id: ref-wikipedia-slansky
      kind: paper
      locator: "Wikipedia 'F4 (mathematics)' (https://en.wikipedia.org/wiki/F4_(mathematics)) + Slansky, Phys. Rep. 79 (1981)"
      aliases: ["Wikipedia/Slansky F_4 dims"]
      role: benchmark
      why_it_matters: "Confirms the F_4 small-irrep dimensions {1, 26, 52, 273, 324, 1053, 1274}; 26 = trace-free Albert part; 52 = adjoint -- the dimension list backing the Route A branching asserts (324 is a genuine irrep)."
      applies_to: [claim-branching]
      carry_forward_to: [planning, writing]
      must_surface: false
      required_actions: [cite]
    - id: ref-iltyakov
      kind: paper
      locator: "Iltyakov, A.V., 'On the Laplace operator and polynomial invariants', J. Algebra 207 (1998)"
      aliases: ["Iltyakov 1998"]
      role: background
      why_it_matters: "F_4 several-copy invariants = trace polynomials + Laplace invariants; c = Tr(X o Y) is a trace monomial -- supports the named-basis identification (the kernel basis is the NAMED trace invariants, not arbitrary)."
      applies_to: [claim-nullspace-11]
      carry_forward_to: [planning]
      must_surface: false
      required_actions: [cite]
    - id: ref-blind
      kind: paper
      locator: "Blind, B., 'Invariants of the pair action of E_6', J. Lie Theory 21 (2011) -- C[2V]^{E6} free on 4 det-polarizations"
      aliases: ["Blind 2011"]
      role: must_consider
      why_it_matters: "CONTRAST case: E_6 does NOT preserve the trace form, so c is NOT an E_6 invariant. Sharpens why c is the genuinely F_4 (1,1) coupling -- the limiting-case control for the uniqueness claim (restricting to E_6 loses c)."
      applies_to: [claim-branching, claim-mod-products-quotient]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [cite, compare]
  acceptance_tests:
    - id: test-branching-integers
      subject: claim-branching
      kind: benchmark
      procedure: "Route A: assert the integer rep-theory facts as runnable checks -- 27 = 1+26; dim Sym^2(27) = 378; dim Sym^2(26) = 351 = 1 + 26 + 324; trivial multiplicity in Sym^2(27) = 2; bidegree-(1,1) trivial multiplicity in 27_X(x)27_Y = dim End_{F_4}(1(+)26) = 1^2 + 1^2 = 2 (Schur). Cite Wikipedia/Slansky for the dims and Garibaldi-Guralnick for F_4 = Stab(Tr, trace form, det)."
      pass_condition: "all integer asserts True: {Sym^2(27)=378, Sym^2(26)=351, (1,1) trivial mult = 2, Sym^2(27) trivial mult = 2}. ROUTE_A_11 = 2 recorded for the adjudicator."
      evidence_required: [deliv-harness, deliv-verdict, ref-methods, ref-wikipedia-slansky, ref-garibaldi-guralnick]
      automation: automated
    - id: test-dim-bookkeeping
      subject: claim-branching
      kind: consistency
      procedure: "Self-consistency of the branching arithmetic: assert 1+26=27; 1+26+324=351; 52+273=325 (=^2(26)); 351+325=676 (=26(x)26); 1+(1*26)+351=378 (=Sym^2(1(+)26))."
      pass_condition: "all five identities True (the branching arithmetic is internally self-consistent)."
      evidence_required: [deliv-harness, ref-pitfalls]
      automation: automated
    - id: test-leibniz-guard
      subject: claim-leibniz-lift
      kind: cross_method
      procedure: "Build the Leibniz lift rho(M) = M(x)I + I(x)M (or keep it implicit as the diagonal gradient-contraction D_M f = grad_X(f).(M.v_x) + grad_Y(f).(M.v_y), planner's discretion). CORRECTNESS GUARD: confirm rho(M) annihilates the KNOWN invariant c = Tr(X o Y) for ALL 52 generators at a genuinely octonionic point -- this catches the M(x)M error (M(x)M would NOT annihilate c)."
      pass_condition: "D_M c == 0 over Q for all 52 generators (the lift is a derivation, not M(x)M). A non-annihilating M => the lift is wrong (likely M(x)M) -- STOP."
      evidence_required: [deliv-harness, ref-orbit-gate, ref-ring-generating-set, ref-warm-engine]
      automation: automated
    - id: test-invariance-gate
      subject: claim-invariance-gate
      kind: symmetry
      procedure: "Invariance gate BEFORE any dimension: reuse the check_f4_invariance recipe + E.octonionic_points() to verify D_M f = 0 over Q for BOTH (1,1) candidates f in {Tr(X o Y) = E.inv_c, Tr(X)Tr(Y) = E.inv_Tr_X * E.inv_Tr_Y} and for ALL 52 f_4 generators, at >= 3 genuinely octonionic rational points (substitute the point FIRST)."
      pass_condition: "all 52 generators annihilate BOTH candidates at all >= 3 octonionic points (both are genuine F_4 invariants). Any surviving (non-annihilated) candidate => STOP, compute no dimension."
      evidence_required: [deliv-harness, ref-orbit-gate, ref-ring-generating-set, ref-warm-engine]
      automation: automated
    - id: test-nullspace-11-dim
      subject: claim-nullspace-11
      kind: benchmark
      procedure: "Route B (the proof): compute the exact f_4-infinitesimal-kernel nullspace over QQ on the bidegree-(1,1) 27_X(x)27_Y monomial space = 729 - exact_qq_rank(stacked 52 derivation operators on the (1,1) block) [or the equivalent per-bidegree gradient-contraction kernel]. Use exact_qq_rank / DomainMatrix over QQ (Matrix.rank() too slow at width >27); substitute rational points first where a point-evaluation form is used."
      pass_condition: "the (1,1) block invariant dimension is exactly 2 over Q. ROUTE_B_11 = 2 recorded. If != 2, recheck 27=1(+)26 and whether Tr(X)Tr(Y) was double-counted/dropped; TRUST THE NULLSPACE and re-derive the irrep arithmetic (roadmap backtracking)."
      evidence_required: [deliv-harness, deliv-named-basis, ref-orbit-gate, ref-warm-engine]
      automation: automated
    - id: test-named-basis
      subject: claim-nullspace-11
      kind: existence
      procedure: "Named-basis identification: form the coordinate vectors of Tr(X)Tr(Y) and Tr(X o Y) in the (1,1) representation; show BOTH lie in the f_4-kernel (D_M f = 0, already gated) AND are linearly independent over Q (the explicit normalization witness: Tr(I)Tr(I) = 9 while c(I,I) = Tr(I) = 3, 9 != 3). Conclude {coord(Tr(X)Tr(Y)), coord(c)} is a BASIS of the 2-dim kernel."
      pass_condition: "the two named coordinate vectors are in the kernel AND linearly independent (rank 2) => they span the 2-dim (1,1) kernel. Dimension-match alone is NOT accepted -- the named invariants must tie to the abstract count. If dim is 2 but the named invariants do NOT span it, the _flatten_729 / coordinatization is inconsistent -- STOP and recheck."
      evidence_required: [deliv-harness, deliv-named-basis, ref-warm-engine, ref-iltyakov]
      automation: automated
    - id: test-total-degree2
      subject: claim-total-degree2
      kind: consistency
      procedure: "Full degree-2 cross-check: compute the exact-QQ invariant dimension of the FULL degree-2 space (all bidegrees (2,0),(1,1),(0,2)); confirm the per-bidegree decomposition (2,0)=2, (1,1)=2, (0,2)=2."
      pass_condition: "total degree-2 invariant dim == 6 with blocks (2,0)=2, (1,1)=2, (0,2)=2. If != 6, isolate the miscounted bidegree block."
      evidence_required: [deliv-harness, deliv-verdict, ref-methods, ref-orbit-gate]
      automation: automated
    - id: test-quotient-1
      subject: claim-mod-products-quotient
      kind: consistency
      procedure: "Quotient (mod products): the only degree-1 invariants are Tr(X) (1,0) and Tr(Y) (0,1); the only bidegree-(1,1) product of lower-degree invariants is Tr(X)*Tr(Y); so the reducible part of the (1,1) space = span{Tr(X)Tr(Y)} (1-dim). Assert genuine-coupling quotient = (1,1) dim - reducible-product dim = 2 - 1 = 1."
      pass_condition: "quotient == 1 => c = Tr(X o Y) is the UNIQUE new (irreducible, non-product) degree-2 coupling generator, unique up to scale + additive multiples of Tr(X)Tr(Y). The 'mod products' qualifier is stated precisely (NOT 'c is the unique (1,1) invariant' -- that is false, Pitfall 9.2)."
      evidence_required: [deliv-harness, deliv-quotient-note, ref-methods, ref-pitfalls]
      automation: automated
    - id: test-rpt-membership
      subject: claim-mod-products-quotient
      kind: existence
      procedure: "Degree-2 R_pt membership decision (Pitfall 7 anti-drift): assert Tr(X)Tr(Y) in R_pt (it is a product of the frozen pointwise generators Tr X, Tr Y) and c NOT in R_pt at degree 2 (the only bidegree-(1,1) R_pt element is a scalar multiple of Tr(X)Tr(Y); c != a*Tr(X)Tr(Y) as a symbolic identity over Q since c(X,X)=Tr X^2 != (Tr X)^2). Decide this by the bidegree argument directly -- do NOT call the general E.is_in_Rpt stub (it raises NotImplementedError)."
      pass_condition: "Tr(X)Tr(Y) decided IN R_pt and c decided NOT in R_pt at degree 2, via the explicit bidegree/identity argument (no a makes simplify(E.inv_c - a*E.inv_Tr_X*E.inv_Tr_Y) == 0). The frozen R_pt definition (six pointwise generators) is used as-is; 'reducible'/'pointwise' is NOT redefined to move c."
      evidence_required: [deliv-harness, deliv-quotient-note, ref-warm-engine, ref-pitfalls]
      automation: automated
    - id: test-two-route-adjudicator
      subject: claim-two-route-agreement
      kind: cross_method
      procedure: "The two-route adjudicator: compare ROUTE_A_11 (integer branching count) against ROUTE_B_11 (exact nullspace (1,1) dim) against the total-degree-2 = 6 cross-check, per the pre-registered agreement rule."
      pass_condition: "report the uniqueness VERDICT ONLY when ROUTE_A_11 == ROUTE_B_11 == 2 AND total-degree-2 == 6 AND quotient == 1. On any disagreement (e.g. ROUTE_A_11 != ROUTE_B_11), emit NO VERDICT, print 'DISAGREEMENT -- TRUST THE EXACT NULLSPACE, re-derive Route A integers; STOP', and force main() nonzero -- do NOT tune either route."
      evidence_required: [deliv-harness, deliv-verdict, ref-methods, ref-pitfalls, ref-orbit-gate]
      automation: automated
  forbidden_proxies:
    - id: fp-26-vs-27
      subject: claim-branching
      proxy: "Computing Sym^2(26) where Sym^2(27) is needed, or forgetting the trivial summand of 27 = 1 (+) 26 (giving the wrong trivial multiplicity, e.g. 'Sym^2(27) trivial mult = trivial mult of Sym^2(26) = 1' -- WRONG, it is 2)."
      reason: "Pitfall 9, the #1 forbidden proxy. '27' and '26' are used interchangeably in physics prose; the Tr/identity direction is easy to drop. Carry 27 = 1 (+) 26 explicitly everywhere; encode the target integers {2, 6, 351, 378} as runnable asserts. A Sym^2 dimension that is not 378 or 351, or a trivial multiplicity != 2, is the warning sign."
    - id: fp-drop-mod-products
      subject: claim-mod-products-quotient
      proxy: "Claiming 'c is the unique (1,1) invariant' -- FALSE, since Tr(X)Tr(Y) is also a bidegree-(1,1) invariant. The uniqueness is in the QUOTIENT (mod the reducible product), not the full (1,1) space."
      reason: "Pitfall 9.2. Omitting the 'mod products' qualifier makes the claim false. The (1,1) space is 2-dim {Tr(X)Tr(Y), c}; modulo the reducible product Tr(X)Tr(Y) (a product of pointwise generators) the genuine-coupling space is 1-dim = span{c}. A (c) statement with no 'modulo Tr(X)Tr(Y)' clause is the warning sign."
    - id: fp-float-rank
      subject: claim-nullspace-11
      proxy: "numpy.linalg.matrix_rank / sympy.Matrix.rank() float fallback / any SVD-tolerance rank on the decisive nullspace path; or a float64 octonion_algebra.py import on the decisive path."
      reason: "Pitfall 3, the GLOBAL forbidden proxy. Rank is discontinuous; a float tolerance fabricates the dim-2 verdict. Decisive rank/nullspace = exact_qq_rank (DomainMatrix over QQ) ONLY (or Matrix.rank()/nullspace() over QQ for small blocks); the exact-only source guard asserts 0 numpy float-rank calls and 0 octonion_algebra imports on the decisive path."
    - id: fp-leibniz-MxM
      subject: claim-leibniz-lift
      proxy: "Writing the Leibniz lift as rho(M) = M(x)M instead of M(x)I + I(x)M (a group-like, not Lie-algebra-derivation, action)."
      reason: "The derivation action of f_4 on the tensor product is M(x)I + I(x)M. M(x)M is wrong and would NOT annihilate the known invariant c. Defeated by the correctness guard: rho(M) MUST annihilate Tr(X o Y) for all 52 generators (test-leibniz-guard)."
    - id: fp-numerical-haar
      subject: claim-nullspace-11
      proxy: "Numerical Haar / finite-group-sampling Reynolds averaging as the proof of the dim-2 count, instead of the exact f_4-infinitesimal-kernel over Q."
      reason: "Group-sampling is fragile and not exact; F_4 group-element generation is nontrivial. The exact infinitesimal-kernel-over-Q is the decisive route (METHODS line 118). AVOID numerical Reynolds on the decisive path."
    - id: fp-dim-match-only
      subject: claim-nullspace-11
      proxy: "Concluding c is the unique generator from a bare dimension match (nullspace dim = 2) WITHOUT showing the named invariants {Tr(X)Tr(Y), Tr(X o Y)} actually SPAN the kernel."
      reason: "A nullspace basis returned by the solver is in an arbitrary basis. Dimension-match alone does not tie the abstract count to the named invariants. Must verify the named coordinate vectors are in-kernel AND linearly independent (Tr(I)Tr(I)=9 != c(I,I)=3) -- test-named-basis."
    - id: fp-redefine-rpt
      subject: claim-mod-products-quotient
      proxy: "Redefining 'reducible' / 'pointwise' / R_pt to make c trivially land in or out of the ring (e.g. silently including c among the 'pointwise' generators, or excluding Tr(X)Tr(Y) from R_pt)."
      reason: "Pitfall 7, the definitional reward-hack. Freeze R_pt = the subalgebra generated by {Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y} (Phase 64 frozen def). Tr(X)Tr(Y) in R_pt (product of pointwise gens); c not in R_pt at degree 2. Use the frozen definition verbatim; decide membership by the bidegree argument, not by moving the goalposts."
    - id: fp-assert-without-exact-witness
      subject: claim-two-route-agreement
      proxy: "Reporting the uniqueness verdict from the Route A branching count ALONE (the literature/Schur argument) without the Route B exact f_4-kernel nullspace witness, OR reporting a verdict when the two routes disagree."
      reason: "The project standard demands the exact harness witness (two-route, like Phase 66). The verbatim Sym^2(26) plethysm could not be confirmed from a single readable source; the exact nullspace is the self-certifying proof. The adjudicator emits a verdict ONLY on agreement; on disagreement it STOPs and trusts the exact nullspace (roadmap backtracking) -- it does NOT tune."
  uncertainty_markers:
    weakest_anchors:
      - "The VERBATIM published plethysm 'Sym^2(26)_{F_4} = 1 (+) 26 (+) 324' was not retrievable as readable text this session (arXiv PDFs returned compressed; no indexed LiE table). MEDIUM-confidence literature gap. MITIGATED: the dimension arithmetic is fully internally self-consistent (1+26+324=351; 52+273=325; 351+325=676), the constituent irreps {1,26,52,273,324} are textbook-standard (Wikipedia/Slansky), and the DECISIVE route is the self-certifying exact-QQ nullspace which proves the (1,1) count = 2 independently of any literature table (roadmap: TRUST THE EXACT NULLSPACE)."
      - "E.is_in_Rpt is a STUB that raises NotImplementedError (a general R_pt decision procedure was deferred and Phase 66 used the bidegree argument instead, not a general procedure). The degree-2 R_pt membership MUST be decided directly by the bidegree argument (only degree-<=2 (1,1) R_pt element is a scalar multiple of Tr(X)Tr(Y); c != a*Tr(X)Tr(Y) since c(X,X)=Tr X^2 != (Tr X)^2). Calling E.is_in_Rpt(inv_c) would crash -- the harness must NOT call it."
      - "Whether to build rho(M) explicitly (729x729 = M(x)I + I(x)M) or keep it implicit (per-bidegree gradient-contraction, check_f4_invariance style) is an implementation choice, not a correctness one. Both are exact; the implicit per-bidegree contraction is cheaper and the (1,1) block is the contract target. The explicit operator is only needed if a direct nullspace BASIS is wanted for the named-basis step; if the explicit 729-wide kernel is slow, drop to the per-bidegree (1,1) block."
    disconfirming_observations:
      - "The (1,1) exact nullspace dimension comes out != 2 (e.g. 1 or 3): the irrep arithmetic or the 27=1(+)26 split is wrong, or Tr(X)Tr(Y) was double-counted/dropped. TRUST THE EXACT NULLSPACE, re-derive Route A, STOP (do not tune)."
      - "The total degree-2 invariant dimension comes out != 6: a bidegree block ((2,0)/(0,2)/(1,1)) is miscounted -- isolate per-bidegree before doubting the engine."
      - "rho(M) does NOT annihilate Tr(X o Y) for some generator: the Leibniz lift is wrong (likely M(x)M) -- STOP."
      - "The nullspace dim is 2 but the named invariants {Tr(X)Tr(Y), Tr(X o Y)} do NOT span the kernel (not linearly independent in-kernel, or not in-kernel): the _flatten_729 coordinate order or the named-invariant coordinatization is inconsistent -- recheck before reporting."
      - "Route A integer count and Route B exact nullspace disagree on the (1,1) multiplicity: NO VERDICT -- trust the exact nullspace, re-derive the irrep arithmetic, STOP."
---

<objective>
Establish, by exact computation over Q, that c = Tr(X o Y) is the UNIQUE degree-2 coupling generator of R[27(+)27]^{F_4} modulo scale, products, and pointwise terms. Concretely: the bidegree-(1,1) trivial part of the invariant ring is exactly 2-dimensional = span{Tr(X)Tr(Y), Tr(X o Y)}, of which Tr(X)Tr(Y) is the single reducible product (a product of pointwise generators), leaving c as the unique genuine (irreducible, non-product) NEW degree-2 coupling invariant. The "mod products" qualifier is stated precisely.

This is the (c) sub-claim (RING-03), complementary to Phase 66's FIELD-level functional-independence SPINE (RING-02) and distinct from Phase 68's ring generation (RING-01). The verdict is reached by TWO ROUTES that must AGREE: Route A (F_4 rep-theory branching + Schur count -- the LITERATURE-anchored conceptual proof) and Route B (the exact f_4-infinitesimal-kernel nullspace over QQ -- the self-certifying harness witness). On disagreement, the EXACT NULLSPACE is authoritative (roadmap backtracking).

Purpose: RING-03 -- the degree-2 uniqueness sub-claim of the (RING) lemma. Pins c as the single new degree-2 coupling invariant; the (1,1) count = 2 is the bidegree-(1,1) Hilbert coefficient Phase 68 must reproduce.
Output: code/degree2_uniqueness.py -- a single pre-registered, foreground-runnable (python -u), chatty exact-over-Q harness producing the Route A integer count, the Route B exact (1,1)-block nullspace (= 2) with named-basis identification, the total-degree-2 = 6 cross-check, the mod-products quotient = 1, the two-route agreement line, and the uniqueness verdict; main() exits 0 iff all checks pass AND the two routes agree.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/67-c-degree-2-uniqueness/67-RESEARCH.md
@code/ring_generating_set.py
@code/orbit_dimension_gate.py
@code/ring_lemma_verification.py
</context>

<execution_environment_note>
RUN THE HARNESS FOREGROUND with `python -u` (unbuffered) and PRINT progress between every check (per-route, per-bidegree, per-point) so it never goes silent: the executor's stream-watchdog kills long no-output symbolic runs (~150s precedent in Phase 65.1) at 600s. The most expensive pieces are the 52-generator invariance gate (simplify over Q -- substitute the genuinely octonionic point FIRST so the contraction is over rationals, the engine pattern) and the (1,1)-block exact nullspace (use exact_qq_rank / DomainMatrix over QQ; Matrix.rank() does NOT return at width >27 in >5 min -- see orbit_dimension_gate.py line 159). REUSE the frozen engine -- do NOT rebuild octonion/jordan/f_4-basis/exact_qq_rank/_flatten_729 from scratch. The harness imports `ring_lemma_verification as E`, plus the gate + candidate machinery exactly as `ring_generating_set.py` does (sys.path insert then import). NEVER import or call the float64 `code/octonion_algebra.py` on the decisive path. If a background resume stalls, the orchestrator can commit + SUMMARY on stall (per project memory). The whole harness should run well under a minute foreground.

IMPORTANT ENGINE NOTE: `E.is_in_Rpt` is a STUB that raises NotImplementedError (the general R_pt decision procedure was never implemented; Phase 66 used the bidegree argument). Do NOT call `E.is_in_Rpt(...)` -- decide degree-2 R_pt membership directly via the bidegree argument (Task 6).
</execution_environment_note>

<tasks>

<task type="auto">
  <name>Task 1: PRE-REGISTER the integer targets + nullspace test + verdict map BEFORE any computation; engine import; Route A branching asserts</name>
  <files>code/degree2_uniqueness.py</files>
  <action>
    Create the harness skeleton + pre-registration block FIRST (before any dimension is computed), then run Route A (the integer rep-theory branching count -- LITERATURE-anchored corroboration).

    1. Header: module docstring + an ASSERT_CONVENTION line restating the frozen conventions (jordan 1/2; c = Tr(X o Y), c(X,X)=Tr X^2; 27 = 1 (+) 26; Tr(I)=3; arithmetic exact over Q; ranks/nullspace = exact_qq_rank / Matrix over QQ, NEVER float; Leibniz lift = M(x)I + I(x)M, NOT M(x)M; R_pt frozen = six pointwise generators). sys.path insert + `import ring_lemma_verification as E`; from orbit_dimension_gate import exact_qq_rank, _select_independent_basis, _flatten_729, infinitesimal_action; from ring_generating_set import _f4_basis, check_f4_invariance, check_bidegree, CANDIDATE_GRADS, PAIR_POINTS. Print SymPy version and confirm imports resolve.
    2. PRE-REGISTRATION block (printed at startup, BEFORE evaluation):
       - The frozen integer targets: BIDEG_11_TRIVIAL_MULT = 2, TOTAL_DEG2_DIM = 6, GENUINE_COUPLING_QUOTIENT = 1, SYM2_27 = 378, SYM2_26 = 351, F4_BASIS_SIZE = 52.
       - The decisive test: the exact f_4-kernel nullspace over QQ on the (1,1) block (exact_qq_rank / DomainMatrix over QQ), aggregated as a single exact dim.
       - The verdict map (frozen here, not after seeing the numbers): ROUTE_A_11 == ROUTE_B_11 == 2 AND TOTAL_DEG2 == 6 AND QUOTIENT == 1 -> "c is the UNIQUE degree-2 coupling generator (mod products + pointwise)"; ROUTE_A_11 != ROUTE_B_11 -> "DISAGREEMENT -- TRUST THE EXACT NULLSPACE, re-derive Route A; STOP"; any (1,1) dim not 2 -> "ANOMALY / STOP".
    3. Route A (branching, conceptual): assert the integer facts as runnable checks -- SYM2_27 == 378; SYM2_26 == 351 and 351 == 1 + 26 + 324; trivial multiplicity in Sym^2(27) == 2; bidegree-(1,1) trivial multiplicity = dim End_{F_4}(1(+)26) == 1**2 + 1**2 == 2 (Schur). Set ROUTE_A_11 = 2. Print each with its literature anchor (Wikipedia/Slansky dims {1,26,52,273,324}; Garibaldi-Guralnick 2015 F_4 = Stab(Tr, trace form, det)).
    4. Dimension bookkeeping: assert 1+26 == 27; 1+26+324 == 351; 52+273 == 325; 351+325 == 676; 1+(1*26)+351 == 378 (Sym^2(1(+)26) self-consistency).
    5. Define a chatty `_report(label, ok)` (reuse the gate pattern) + a module-level results accumulator. Stub `main()` that will call the per-task checks in order and exit 0 iff all pass AND the two routes agree.

    Do NOT compute the exact nullspace in this task -- this task fixes the integer targets, the verdict map, and Route A only.
  </action>
  <verify>
    1. The pre-registration block (integer targets + verdict map) prints BEFORE any exact_qq_rank / nullspace call (grep: the targets are defined above any nullspace computation).
    2. Route A integer asserts all True: SYM2_27=378, SYM2_26=351=1+26+324, Sym^2(27) trivial mult=2, (1,1) trivial mult via Schur=2; ROUTE_A_11=2 recorded.
    3. Dimension bookkeeping (1+26=27; 1+26+324=351; 52+273=325; 351+325=676; 1+26+351=378) all True.
    4. Imports resolve (E, exact_qq_rank, _f4_basis, CANDIDATE_GRADS, _flatten_729); ASSERT_CONVENTION line present; no float-rank symbol (numpy.linalg / .rank()-float) on the decisive path; E.is_in_Rpt is NOT called.
  </verify>
  <done>Harness skeleton + pre-registration written: frozen integer targets {(1,1)=2, total=6, quotient=1, Sym^2(27)=378, Sym^2(26)=351} + verdict map (agreement-only), engine imports confirmed, Route A branching asserts pass with ROUTE_A_11=2, dimension bookkeeping self-consistent. No exact nullspace computed yet.</done>
</task>

<task type="auto">
  <name>Task 2: Leibniz lift rho(M) = M(x)I + I(x)M + its correctness guard (annihilates the known invariant c)</name>
  <files>code/degree2_uniqueness.py</files>
  <action>
    Build the ONE genuinely new component -- the Leibniz lift of an f_4 generator to the 27(x)27 space -- and immediately guard it against the M(x)M error.

    1. basis, count = _f4_basis() (the function returns a 2-TUPLE: basis = the list of 52 independent 27x27 rational matrices, count = 52; reuse verbatim, do NOT rebuild). Assert count == 52 (= F4_BASIS_SIZE). Iterate generators as `for M in basis` everywhere below -- NOT `for M in f4` (that would iterate the (basis, count) tuple and crash).
    2. Implement the Leibniz lift. EITHER (implicit, RECOMMENDED -- cheaper, per the research): keep the action implicit as the diagonal gradient-contraction D_M f = grad_X(f).(M.v_x) + grad_Y(f).(M.v_y), exactly the check_f4_invariance recipe (split a cached 54-var gradient into X-block [0:27], Y-block [27:54], substitute the rational point FIRST). OR (explicit, only if a direct nullspace BASIS is wanted in Task 4): build rho(M) = M(x)I27 + I27(x)M as a 729x729 rational matrix using the _flatten_729 row-major coordinate order. Planner-discretion default = implicit per-bidegree gradient-contraction; build the explicit operator only if the named-basis step needs a direct kernel basis.
    3. CORRECTNESS GUARD (catches fp-leibniz-MxM): confirm the lift annihilates the KNOWN invariant c = Tr(X o Y) for ALL 52 generators at a genuinely octonionic rational point. Using CANDIDATE_GRADS[6] (the cached gradient of c), for each M_i compute D_{M_i} c = grad_X(c).(M_i.v_x) + grad_Y(c).(M_i.v_y) at the point and assert == 0 over Q for all 52. (M(x)M would NOT annihilate c -- this is the discriminating test.)

    Print the lift form chosen and the per-generator annihilation tally (52/52). Substitute the point FIRST. Print progress (watchdog).
  </action>
  <verify>
    1. _f4_basis() returns 52 generators (count asserted == 52).
    2. The lift is the derivation form M(x)I + I(x)M (or the equivalent diagonal gradient-contraction), explicitly NOT M(x)M (stated in code/comment).
    3. Correctness guard: D_M c == 0 over Q for ALL 52 generators at a genuinely octonionic point (52/52 annihilate). A non-annihilating M => the lift is wrong (M(x)M) -> STOP.
    4. If the explicit 729x729 rho(M) is built, it uses the _flatten_729 row-major order consistently.
  </verify>
  <done>Leibniz lift built (M(x)I + I(x)M, or the implicit diagonal gradient-contraction) and GUARDED: it annihilates the known invariant c for all 52 f_4 generators over Q (catching the M(x)M error). f_4 basis size 52 confirmed.</done>
</task>

<task type="auto">
  <name>Task 3: Invariance gate BEFORE any dim count -- D_M f = 0 for both (1,1) candidates, 52 generators, >=3 octonionic points</name>
  <files>code/degree2_uniqueness.py</files>
  <action>
    Run the STRONG invariance gate on BOTH named bidegree-(1,1) candidates BEFORE any dimension is reported (mirrors the Phase 65/65.1/66 discipline: invariance precedes the count). REUSE the check_f4_invariance recipe + E.octonionic_points().

    1. The two (1,1) candidates: c = E.inv_c (= Tr(X o Y)) and m = E.inv_Tr_X * E.inv_Tr_Y (= Tr(X)Tr(Y)). Compute their 54-var gradients (or reuse CANDIDATE_GRADS[6] for c; build the gradient of m directly via diff over the 54 symbols, cached).
    2. For each candidate f and each genuinely octonionic point P in E.octonionic_points() (>= 3 points): substitute P into the gradient FIRST (subs over rationals); for each of the 52 f_4 generators M compute D_M f = grad_X(f).(M.v) + grad_Y(f).(M.v) (diagonal action, same P on both copies -- the orbit tangent of the diagonal pair (P,P), exactly the check_f4_invariance choice) and assert simplify(D_M f) == 0 over Q. Tally killed-count per point (must be 52/52).
    3. Assert BOTH candidates are annihilated by ALL 52 generators at ALL >= 3 octonionic points. A surviving (non-annihilated) candidate => it is NOT a genuine F_4 invariant -- STOP, compute NO dimension (this is the gate).

    This certifies that {Tr(X o Y), Tr(X)Tr(Y)} are both genuine (1,1) F_4 invariants BEFORE we count the (1,1) space. Print per-point, per-candidate progress (watchdog).
  </action>
  <verify>
    1. Both (1,1) candidates {Tr(X o Y), Tr(X)Tr(Y)} are annihilated by ALL 52 f_4 generators at >= 3 genuinely octonionic rational points (D_M f == 0 over Q; 52/52 per point per candidate).
    2. The point is substituted into the gradient FIRST (rational contraction; no symbolic 54-var rank).
    3. Any nonzero D_M f for either candidate => STOP, no dimension computed (the gate fires).
    4. The gate runs BEFORE the Task 4 nullspace (grep: octonionic invariance asserts precede the (1,1) nullspace computation).
  </verify>
  <done>Invariance gate PASSED before any dim count: both named (1,1) candidates {Tr(X o Y), Tr(X)Tr(Y)} are genuine F_4 invariants (D_M f=0 over Q for all 52 generators at >=3 octonionic points). Cleared to compute the (1,1) dimension.</done>
</task>

<task type="auto">
  <name>Task 4: Route B -- exact (1,1)-block f_4-kernel nullspace over QQ (dim 2) + named-basis identification</name>
  <files>code/degree2_uniqueness.py</files>
  <action>
    Compute the decisive Route B proof: the exact f_4-infinitesimal-kernel nullspace over QQ on the bidegree-(1,1) 27_X(x)27_Y monomial space, then tie the 2-dim kernel to the NAMED invariants.

    1. (1,1)-block nullspace dimension: build the constraint that an element of the (1,1) space (a bilinear form on 27_X x 27_Y, i.e. a 27x27 coefficient matrix C, 729 coords in _flatten_729 order) is f_4-invariant iff rho(M) annihilates it for all 52 generators, i.e. M^T C + C M = 0 for each M (the (1,1) Sylvester/derivation condition; equivalently stack the 52 operators and take the kernel). Compute invariant_dim = 729 - exact_qq_rank(stacked 52 derivation operators on the (1,1) block) using exact_qq_rank / DomainMatrix over QQ (NOT Matrix.rank at width >27). Set ROUTE_B_11 = invariant_dim; assert == 2. [If the explicit 729-wide stack is slow, this IS the per-bidegree contract target -- keep it to the (1,1) block; do NOT compute the full 729-space here.]
    2. Backtracking guard: if ROUTE_B_11 != 2, print "recheck 27=1(+)26 split / Tr(X)Tr(Y) double-count-or-drop; TRUST THE EXACT NULLSPACE, re-derive Route A" and force STOP (do NOT tune).
    3. NAMED-BASIS identification (defeats fp-dim-match-only): form the coordinate vectors of Tr(X)Tr(Y) and Tr(X o Y) as elements of the (1,1) coefficient space (the 27x27 bilinear-form matrix of each, flattened via _flatten_729). Show (a) both lie in the kernel (M^T C + C M = 0 for all 52 M -- already gated in Task 3, re-assert in coordinate form), and (b) the two coordinate vectors are linearly independent over Q -- the explicit normalization witness: evaluate Tr(I)Tr(I) = 9 and c(I,I) = Tr(I) = 3 (9 != 3 distinguishes them on the diagonal), and confirm rank([coord(m), coord(c)]) == 2 over QQ. Conclude {coord(Tr(X)Tr(Y)), coord(c)} is a BASIS of the 2-dim (1,1) kernel.
    4. If ROUTE_B_11 == 2 but the named invariants do NOT span the kernel (not rank-2 in-kernel): print "coordinate convention (_flatten_729) or named-invariant coordinatization inconsistent -- recheck" and STOP.

    Print the nullspace dim and the named-basis rank as they compute (watchdog).
  </action>
  <verify>
    1. ROUTE_B_11 = (1,1)-block invariant nullspace dim == 2, computed via exact_qq_rank / DomainMatrix over QQ (NOT float, NOT Matrix.rank at width >27).
    2. The named coordinate vectors of Tr(X)Tr(Y) and Tr(X o Y) both lie in the (1,1) f_4-kernel.
    3. The two named vectors are linearly independent over Q (rank 2; witness Tr(I)Tr(I)=9 != c(I,I)=3) => they SPAN the 2-dim kernel (named-basis identification, not a bare dim match).
    4. Backtracking guard wired: ROUTE_B_11 != 2 => STOP (trust the nullspace, re-derive Route A); dim 2 but named invariants don't span => STOP (coordinate inconsistency).
  </verify>
  <done>Route B complete: the exact (1,1)-block f_4-kernel nullspace over QQ has dim exactly 2 (ROUTE_B_11=2); the named invariants {Tr(X)Tr(Y), Tr(X o Y)} are shown to be a BASIS of the kernel (in-kernel AND linearly independent, Tr(I)Tr(I)=9 != c(I,I)=3) -- the abstract count tied to the named invariants, not a bare dimension match.</done>
</task>

<task type="auto">
  <name>Task 5: Full degree-2 nullspace cross-check (total dim 6 = blocks (2,0)=2, (1,1)=2, (0,2)=2)</name>
  <files>code/degree2_uniqueness.py</files>
  <action>
    Confirm the rep-theory count by the full degree-2 exact computation -- the total-degree-2 cross-check (the second leg of two-route corroboration).

    1. Per-bidegree exact-QQ invariant dimensions over the full degree-2 space, using the same exact f_4-kernel nullspace machinery:
       - (2,0) block (Sym^2 of the X-copy, 27_X only): assert invariant dim == 2 (= {(Tr X)^2, Tr X^2}). [X-only: the f_4 action on Sym^2(27_X); reuse single-copy machinery.]
       - (0,2) block (Sym^2 of the Y-copy): assert invariant dim == 2 (= {(Tr Y)^2, Tr Y^2}).
       - (1,1) block: ROUTE_B_11 == 2 from Task 4.
    2. Assert TOTAL_DEG2 = 2 + 2 + 2 == 6. Cross-check against the named degree-2 invariant set {(Tr X)^2, Tr X^2, (Tr Y)^2, Tr Y^2, Tr(X)Tr(Y), Tr(X o Y)} (6 elements; METHODS line 114).
    3. Red-flag guard: if TOTAL_DEG2 != 6, print "a bidegree block ((2,0)/(0,2)/(1,1)) is miscounted -- isolate per-bidegree" and STOP.

    The (2,0)/(0,2) blocks should be cheap (X-only or Y-only, width <= 27 -- Matrix.rank() over QQ is fine here; the pair (1,1) block needs exact_qq_rank). Print each block dim (watchdog).
  </action>
  <verify>
    1. (2,0) block invariant dim == 2 {(Tr X)^2, Tr X^2}; (0,2) block invariant dim == 2 {(Tr Y)^2, Tr Y^2}; (1,1) block == 2 (from Task 4).
    2. TOTAL_DEG2 == 6, matching the named six-element degree-2 invariant set.
    3. Red-flag guard wired: TOTAL_DEG2 != 6 => STOP (isolate the miscounted block).
    4. The (2,0)/(0,2) blocks use exact QQ rank/nullspace (Matrix over QQ at width <=27 OK); the (1,1) block uses exact_qq_rank.
  </verify>
  <done>Total-degree-2 cross-check PASSED: the full degree-2 invariant space is exactly 6-dimensional over Q, decomposing per-bidegree as (2,0)=2, (1,1)=2, (0,2)=2, matching the named six-element set -- the rep-theory count confirmed by direct exact computation.</done>
</task>

<task type="auto">
  <name>Task 6: Mod-products quotient = 1 + frozen-R_pt degree-2 membership decision (c is the UNIQUE new degree-2 coupling generator)</name>
  <files>code/degree2_uniqueness.py</files>
  <action>
    Take the "mod products" quotient precisely and decide degree-2 R_pt membership directly (do NOT call the E.is_in_Rpt stub -- it raises).

    1. The reducible part of the (1,1) space: the only degree-1 invariants are Tr(X) (1,0) and Tr(Y) (0,1) (cite Phase 64 single-state ring R[Tr, Tr^2, det] -- degree-1 part is just Tr); the only bidegree-(1,1) PRODUCT of lower-degree invariants is Tr(X)*Tr(Y). So the reducible part of the (1,1) space = span{Tr(X)Tr(Y)}, dimension 1. State this with the bidegree argument explicitly (use check_bidegree / the (1,0),(0,1),(1,1) bidegrees from ring_generating_set as corroboration).
    2. Quotient: GENUINE_COUPLING_QUOTIENT = ROUTE_B_11 - 1 = 2 - 1 = 1. Assert == 1.
    3. Degree-2 R_pt membership decision (frozen R_pt = subalgebra gen by {Tr X, Tr X^2, det X, Tr Y, Tr Y^2, det Y}; Pitfall 7 anti-drift -- use the frozen def verbatim, do NOT redefine reducible/pointwise):
       - Tr(X)Tr(Y) IN R_pt: it is literally a product of the pointwise generators Tr X and Tr Y -- assert membership by construction (it equals E.inv_Tr_X * E.inv_Tr_Y, a product of two frozen pointwise generators).
       - c NOT in R_pt at degree 2: the only bidegree-(1,1) R_pt element is a scalar multiple of Tr(X)Tr(Y); assert there is NO rational a with simplify(E.inv_c - a*E.inv_Tr_X*E.inv_Tr_Y) == 0 as a symbolic identity over Q (the witness: c(X,X)=Tr X^2 != (Tr X)^2, so no scalar multiple of (Tr X)(Tr Y) equals c). Do NOT call E.is_in_Rpt (it raises NotImplementedError) -- record in a comment WHY the bidegree argument suffices at degree 2.
    4. Conclude + print the precise statement: "Modulo the reducible product Tr(X)Tr(Y) (in R_pt) and pointwise terms, the genuine-coupling quotient is 1-dimensional = span{c}; c = Tr(X o Y) is the UNIQUE NEW degree-2 coupling generator, unique up to scale and additive multiples of Tr(X)Tr(Y)." Explicitly NOT "c is the unique (1,1) invariant" (false -- Pitfall 9.2).
    5. Limiting-case / consistency controls (print): (a) X=Y restricts the (1,1) invariants to the (2,0) pointwise quadratics -- c(X,X)=Tr X^2 (in R_pt on the diagonal), Tr(X)Tr(X)=(Tr X)^2; (b) E_6 contrast -- c is NOT an E_6 invariant (Blind 2011), so dropping trace-form invariance loses c (the (1,1) E_6 invariants are fewer); (c) the Tr(I)=3 linear-independence witness (9 != 3).
  </action>
  <verify>
    1. GENUINE_COUPLING_QUOTIENT == 1 (= (1,1) dim 2 - reducible-product dim 1).
    2. Tr(X)Tr(Y) decided IN R_pt (a product of frozen pointwise generators); c decided NOT in R_pt at degree 2 (no rational a makes simplify(inv_c - a*inv_Tr_X*inv_Tr_Y)==0; c(X,X)=Tr X^2 != (Tr X)^2).
    3. E.is_in_Rpt is NOT called (it raises); the degree-2 membership is decided by the bidegree/identity argument with a comment recording why.
    4. The precise "mod products" statement is printed (NOT "c is the unique (1,1) invariant"); R_pt used verbatim (no redefinition of reducible/pointwise).
    5. The three consistency controls (X=Y diagonal collapse; E_6 contrast; Tr(I)=3 witness) are printed.
  </verify>
  <done>Mod-products quotient = 1 established: the only reducible (1,1) element is Tr(X)Tr(Y) in R_pt, c not in R_pt at degree 2 (decided by the bidegree/identity argument, NOT the is_in_Rpt stub), quotient (2-1)=1 => c is the UNIQUE new degree-2 coupling generator (mod products + pointwise), stated precisely with 'mod products'. Consistency controls (X=Y, E_6 contrast, Tr(I)=3) printed.</done>
</task>

<task type="auto">
  <name>Task 7: Two-route adjudicator + exact-only source guard + assemble main(), exit-0-iff-agree</name>
  <files>code/degree2_uniqueness.py</files>
  <action>
    Wire the two-route adjudicator, the exact-only source guard, and assemble main().

    1. Two-route adjudicator (verdict ONLY on agreement -- the reward-hacking guard):
       - ROUTE_A_11 == ROUTE_B_11 == 2 AND TOTAL_DEG2 == 6 AND GENUINE_COUPLING_QUOTIENT == 1 -> VERDICT = "c = Tr(X o Y) is the UNIQUE degree-2 coupling generator (mod scale, products, and pointwise terms)"; print the (1,1) count from both routes, the total-degree-2 (6), the quotient (1), and the named basis.
       - ROUTE_A_11 != ROUTE_B_11 -> NO VERDICT; print "DISAGREEMENT -- TRUST THE EXACT NULLSPACE (Route B), re-derive the Route A irrep arithmetic; STOP" (roadmap backtracking); force main() nonzero -- do NOT tune.
       - any (1,1) dim not 2, or TOTAL_DEG2 != 6, or QUOTIENT != 1 -> "ANOMALY -- STOP"; force nonzero.
       Print a compact two-route table (Route A integer | Route B exact nullspace | total deg-2 | quotient | verdict).
    2. Exact-only source guard (reuse the orbit_dimension_gate.exact_only_guard / ring_generating_set.exact_only_guard pattern): scan THIS module's source and assert 0 numpy.linalg / numpy float-rank / .rank()-float calls on the decisive path and 0 `octonion_algebra` imports. Run it as a check inside main().
    3. Scope + consistency note (print): Phase 67 is the DEGREE-2 uniqueness sub-claim (RING-03), complementary to Phase 66's FIELD-level functional-independence SPINE (RING-02, c independent of R_pt, rank 7) and distinct from Phase 68's ring generation (RING-01). The (1,1) count = 2 here is the bidegree-(1,1) Hilbert coefficient Phase 68 must reproduce. Note exact-over-Q throughout; frozen-engine reuse; is_in_Rpt stub NOT called.
    4. main() calls in order: pre-registration print (Task 1) -> Route A + bookkeeping (Task 1) -> Leibniz lift + guard (Task 2) -> invariance gate (Task 3) -> (1,1) nullspace + named basis (Task 4) -> total degree-2 (Task 5) -> quotient + R_pt membership (Task 6) -> adjudicator + exact-only guard (Task 7). Collect every _report ok/fail into the accumulator. main() returns 0 iff ALL checks pass AND the verdict was emitted on the agreement cell (ROUTE_A_11==ROUTE_B_11==2, TOTAL_DEG2==6, QUOTIENT==1); returns nonzero on any failed check, any disagreement, or any STOP.
    5. Watchdog discipline: every check prints a one-line progress marker; top-of-main prints the pre-registration, bottom-of-main prints the final VERDICT + two-route table. `if __name__ == "__main__": sys.exit(0 if main() else 1)`.

    Confirm the harness runs FOREGROUND with `python -u code/degree2_uniqueness.py` end-to-end (well under a minute), prints progress throughout, and exits 0.
  </action>
  <verify>
    1. `python -u code/degree2_uniqueness.py` runs foreground end-to-end (well under a minute), printing per-route/per-bidegree/per-point progress (never silent), and exits 0.
    2. main() returns 0 IFF all checks pass AND the verdict is emitted on the agreement cell (ROUTE_A_11==ROUTE_B_11==2, TOTAL_DEG2==6, QUOTIENT==1); disagreement / anomaly / STOP => nonzero.
    3. The two-route table is printed (Route A integer 2 | Route B exact nullspace 2 | total deg-2 6 | quotient 1 | verdict UNIQUE mod products).
    4. The exact-only source guard passes: 0 numpy float-rank calls, 0 octonion_algebra imports on the decisive path.
    5. Final output prints: the (1,1) count (both routes = 2), total degree-2 (6), quotient (1), the named basis {Tr(X)Tr(Y), c}, the precise "mod products" uniqueness verdict, and the scope note (RING-03; distinct from Phase 66 SPINE / Phase 68 generation).
  </verify>
  <done>Full harness assembled: main() runs Route A + Leibniz lift/guard + invariance gate + (1,1) nullspace/named-basis + total-degree-2 + quotient/R_pt-membership + adjudicator + exact-only guard, exits 0 iff all pass AND the two routes agree on (1,1)=2 (total 6, quotient 1); foreground `python -u` run is chatty and exits 0 with the uniqueness verdict + two-route table printed.</done>
</task>

</tasks>

<verification>
- **Exact over Q only:** every decisive dimension is an exact integer -- a rep dimension (Route A) or a Q-vector-space nullspace dimension (Route B via exact_qq_rank / DomainMatrix over QQ). The exact-only source guard asserts 0 numpy float-rank calls and 0 octonion_algebra imports on the decisive path (fp-float-rank rejected). Zero tolerance; any float rank is a forbidden proxy.
- **Pre-registration:** the integer targets {(1,1)=2, total=6, quotient=1, Sym^2(27)=378, Sym^2(26)=351} + the verdict map are frozen as code BEFORE any nullspace is computed (an honest disagreement cannot be re-rolled).
- **26-vs-27 discipline:** 27 = 1 (+) 26 is carried explicitly everywhere; the target integers are runnable asserts; Sym^2(26) is never conflated with Sym^2(27) (fp-26-vs-27 rejected).
- **Leibniz-lift correctness:** the lift is M(x)I + I(x)M (or the equivalent diagonal gradient-contraction), guarded by annihilation of the KNOWN invariant c for all 52 generators (fp-leibniz-MxM rejected).
- **Invariance BEFORE the count:** both named (1,1) candidates {Tr(X o Y), Tr(X)Tr(Y)} pass D_M f = 0 over Q for all 52 generators at >= 3 octonionic points BEFORE any dimension is reported (the gate fires on a surviving candidate).
- **Named-basis identification (not a bare dim match):** the 2-dim (1,1) kernel is shown to be SPANNED by the named invariants (in-kernel AND linearly independent, Tr(I)Tr(I)=9 != c(I,I)=3), tying the abstract count to {Tr(X)Tr(Y), c} (fp-dim-match-only rejected).
- **Mod-products precision:** the (1,1) space is 2-dim {Tr(X)Tr(Y), c}; modulo the single reducible product Tr(X)Tr(Y) (in R_pt) the genuine-coupling quotient is 1-dim = span{c}. The statement is "unique mod products + pointwise", NEVER "c is the unique (1,1) invariant" (fp-drop-mod-products rejected).
- **Frozen R_pt (anti-drift):** R_pt = subalgebra gen by the six pointwise generators, used verbatim; Tr(X)Tr(Y) in R_pt, c not in R_pt at degree 2, decided by the bidegree/identity argument -- the E.is_in_Rpt stub is NOT called; reducible/pointwise are NOT redefined (fp-redefine-rpt rejected).
- **Two-route agreement:** the uniqueness verdict is reported ONLY when Route A (integer 2) and Route B (exact nullspace (1,1)=2) agree, with total-degree-2=6 and quotient=1; on disagreement the harness emits NO VERDICT, trusts the exact nullspace (roadmap backtracking), and STOPs (fp-assert-without-exact-witness rejected).
- **Frozen primitives:** jordan / Tr / c / f_4 builder / exact_qq_rank / _flatten_729 are REUSED from the certified engine, never re-implemented; the float64 octonion_algebra.py never touches the decisive path.
- **Total-degree-2 cross-check:** the full degree-2 space is 6-dim (blocks (2,0)=2, (1,1)=2, (0,2)=2), corroborating the (1,1)=2 count.
</verification>

<success_criteria>
- code/degree2_uniqueness.py runs foreground via `python -u` end-to-end (well under a minute), prints per-route/per-bidegree/per-point progress (watchdog-safe), and exits 0.
- **Route A (branching, corroboration):** the integer rep-theory facts hold exactly -- 27 = 1 (+) 26; dim Sym^2(27) = 378; dim Sym^2(26) = 351 = 1 + 26 + 324; trivial mult in Sym^2(27) = 2; bidegree-(1,1) trivial mult = dim End_{F_4}(1(+)26) = 1^2 + 1^2 = 2 (Schur); the dimension bookkeeping (1+26=27; 1+26+324=351; 52+273=325; 351+325=676; 1+26+351=378) is self-consistent. ROUTE_A_11 = 2.
- **Route B (the exact proof):** the exact f_4-infinitesimal-kernel nullspace over QQ on the bidegree-(1,1) 27_X(x)27_Y space has dimension exactly 2 (ROUTE_B_11 = 2, via exact_qq_rank / DomainMatrix over QQ); the named invariants {Tr(X)Tr(Y), Tr(X o Y)} are a BASIS of this kernel (in-kernel AND linearly independent, Tr(I)Tr(I)=9 != c(I,I)=Tr(I)=3) -- not a bare dimension match. The Leibniz lift M(x)I + I(x)M is correctness-guarded (annihilates c, 52/52); the invariance gate (D_M f=0, 52 gens, >=3 octonionic points) passes for both (1,1) candidates BEFORE the count.
- **Total-degree-2 cross-check:** the full degree-2 invariant space is exactly 6-dimensional (blocks (2,0)=2, (1,1)=2, (0,2)=2 = {(Tr X)^2, Tr X^2, (Tr Y)^2, Tr Y^2, Tr(X)Tr(Y), c}).
- **Mod-products quotient = 1:** the only reducible (1,1) element is Tr(X)Tr(Y) (in the frozen R_pt, a product of pointwise generators); c is not in R_pt at degree 2 (no rational a makes c = a*(Tr X)(Tr Y) a symbolic identity; c(X,X)=Tr X^2 != (Tr X)^2); the genuine-coupling quotient (2-1)=1 => c = Tr(X o Y) is the UNIQUE new degree-2 coupling generator, unique up to scale + additive multiples of Tr(X)Tr(Y). The 'mod products' qualifier is stated precisely (NOT "c is the unique (1,1) invariant"). E.is_in_Rpt (a stub that raises) is NOT called; reducible/pointwise/R_pt are NOT redefined.
- **Two-route agreement:** the uniqueness verdict is emitted ONLY when ROUTE_A_11 == ROUTE_B_11 == 2 AND TOTAL_DEG2 == 6 AND QUOTIENT == 1; on disagreement the harness emits NO VERDICT, trusts the EXACT NULLSPACE, and STOPs (nonzero exit). The two-route table is printed.
- **Consistency controls printed:** X=Y restricts (1,1) to the (2,0) pointwise quadratics (c(X,X)=Tr X^2); E_6 contrast (c is NOT an E_6 invariant, Blind 2011); the Tr(I)=3 linear-independence witness.
- All forbidden proxies (fp-26-vs-27, fp-drop-mod-products, fp-float-rank, fp-leibniz-MxM, fp-numerical-haar, fp-dim-match-only, fp-redefine-rpt, fp-assert-without-exact-witness) are explicitly rejected by a concrete guard in the harness.
</success_criteria>

<output>
After completion, create `.gpd/phases/67-c-degree-2-uniqueness/67-01-SUMMARY.md`.

The SUMMARY must record: the (1,1) trivial-multiplicity verdict (Route A integer 2 AND Route B exact nullspace 2, two-route agreement); the named basis {Tr(X)Tr(Y), Tr(X o Y)} shown to span the kernel (with the Tr(I)Tr(I)=9 != c(I,I)=3 witness); the total-degree-2 dim (6, blocks (2,0)=2/(1,1)=2/(0,2)=2); the mod-products quotient (1) and the precise uniqueness statement (c UNIQUE mod scale + products + pointwise; Tr(X)Tr(Y) in R_pt, c not in R_pt at degree 2, decided by the bidegree argument since E.is_in_Rpt is a stub); the Leibniz-lift correctness guard (annihilates c, 52/52) and the invariance gate (52/52 x >=3 points for both candidates). Note exact-over-Q throughout (0 float-rank, 0 octonion_algebra on the decisive path), the frozen engine reuse, the weakest anchor (unconfirmed verbatim Sym^2(26) plethysm -- mitigated by the self-certifying exact nullspace), and the E_6 contrast control. Flag SCOPE: DEGREE-2 uniqueness sub-claim (RING-03) ONLY -- complementary to Phase 66's FIELD-level functional-independence SPINE (RING-02), distinct from Phase 68's ring generation (RING-01); the (1,1)=2 count is the bidegree-(1,1) Hilbert coefficient Phase 68 must reproduce.
</output>
